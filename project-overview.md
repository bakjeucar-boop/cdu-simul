# CDU Feasibility Study — 한 장 개요

> **최종 갱신: 2026-08-27 · 저장소 확정(`github.com/bakjeucar-boop/cdu-simul`) · 코드 착수 전**
> **· 세션 1을 1-A(환경·골격)/1-B(모델·balance)로 분할**
> 이 파일은 웹 대화창에 처음 들어왔을 때 **상황을 빠르게 잡기 위한 안내판**이다.
> 배경·설비구조·도구선정 근거·시나리오 가정치의 **정본은
> `AI서버_CDU_디지털트윈_프로젝트정리.md`** 다 — 수치를 여기 옮겨 적지 않는다.
> 규칙·명령·금지사항은 `CLAUDE.md`, 역할·판단원칙은 `collaboration.md`.
> **이 파일이 파일럿 종료 조건의 정본이다** — 아래 절에서 관리한다.

## 이 프로젝트는 파일럿이다

Claude 프로젝트명이 "CDU Feasibility Study"인 이유: 이건 **본 프로젝트(실제 설계
사양 확보 후 진행) 착수 전에, 이 모델링 접근 자체가 말이 되는지 싼 비용으로
먼저 확인**하는 단계다. 파일럿이 하는 것과 안 하는 것을 가른다.

| | 파일럿 (지금) | 본 프로젝트 (이후) |
|---|---|---|
| 입력 | 업계 공개자료 기반 가정치 (5장 표) | 실제 설계사양(펌프·밸브·HX 사양, 랙별 실측 발열량) |
| 목표 | 모델 구조가 물리적으로 붕괴하지 않는가 (feasibility) | 예측이 실측과 얼마나 가까운가 (정확도) |
| 판정 기준 | 6장 feasibility 네 기준 | (미정 — 본 프로젝트 착수 시 정의) |
| 산출물 성격 | "가정값 기반 — 실측 아님" 표시 필수 | 실 데이터셋으로 AI 학습 가능 |
| 해당 게이트 | `CLAUDE.md` 세션 1~5 | `CLAUDE.md` 세션 6 이후 |

## 파일럿 종료 조건 — 확정 시점은 정했다, 기준 자체는 아직

**기준 자체는 아직 미정이다.** 짐작으로 채우지 않는다. 다만 **언제 정할지는
확정했다** — 세션 **1-B**(단일 CDU balance 검증) 결과가 나온 뒤. 세션 1-A(저장소·환경·
가정치 전사)는 물리 결과가 없으므로 이 절을 채우는 시점이 아니다. 이유를 적진 않았으므로
추측하지 않는다: 세션 1-B 결과를 보고 판단하겠다는 사람의 결정으로만 남긴다.

문서(4장·7장) 구조상 다음이 후보이지만, 실제 기준(어떤 항목이 얼마나 확보돼야
전환인지, 세션 1~5를 전부 통과해야 하는지 일부만 통과해도 되는지)은 세션 1-B 결과를
보고 **사람이 확정한다**:

- (후보) 실제 설계데이터(펌프 특성곡선·밸브 Cv·열교환기 사양·랙별 발열량) 확보
- (후보) 파일럿 세션 1~5의 feasibility 게이트 전부 통과
- (후보) 둘 중 하나만 충족해도 되는지, 둘 다 필요한지 — 미정

**세션 1-B가 끝나면, 결과를 검토하기 전에 먼저 이 절을 채울지 사람에게 확인한다.**
그전까지는 세션 6(가정치→설계값 교체) 진입 여부를 이 대화창이 임의로 판단하지
않는다.

## 시각화 방향 — 본 프로젝트 이후, 지금은 기록만

**지금(파일럿) 단계에서는 손대지 않는다.** 결과는 수치·표로 충분하다.

- **시점**: 파일럿이 끝나고 본 프로젝트로 넘어간 뒤. 세션 1~7 게이트 어디에도
  시각화 단계를 끼워 넣지 않는다.
- **원하는 형태**: HYSYS/UniSim 스타일 정적 PFD — 장치 박스 + 스트림 번호 +
  온도·압력·유량·Duty/Power 태그 박스. pygame류 동적 애니메이션은 **그 이후,
  모델이 어느 정도 완성된 뒤**로 다시 미룬다.
- **후보 도구**: Python + 경량 GUI(예: FreeSimpleGUI — PySimpleGUI가 유료화된
  뒤 LGPL3로 남은 커뮤니티 포크, tkinter 기반. `Graph` 엘리먼트로 박스·선을
  그릴 수 있어 이런 스타일에 맞는다). 이 라이브러리로 확정된 것은 아니다.
- **제작 주체**: 아직 미정 — 사람이 직접 할지, 로컬 Claude Code에 별도 판으로
  지시할지 나중에 정한다.

이 절은 결정이 아니라 **참고 기록**이다 — 본 프로젝트 착수 시점에 다시 확인한다.

D2C(Direct-to-Chip) 액체냉각 CDU를 파이썬 물리모델로 시뮬레이션해, 정상/이상/누출
시나리오를 반복 생성하고 그 결과를 **누출 조기감지·이상상태 예측 AI 모델의 학습
데이터셋**으로 쓴다. 기존 방식(누출감지센서)은 사고 **이후**에야 감지되는데, 미량
누출을 조기에 잡으려는 것이 목적이다.

**지금 가장 중요한 제약**: 실제 설계데이터(펌프·밸브·열교환기 사양, 랙별 발열량)가
아직 없다. 5장의 모든 수치는 업계 공개자료 기반 **가정값**이고, 지금 단계의 목표는
"정확한 예측"이 아니라 **"이 모델 구조가 무너지지 않고 물리적으로 말이 되는가
(feasibility)"** 를 확인하는 것이다.

## 실제 설비 구조 (요약)

CDU 1대에 다수 랙이 병렬 연결, 이런 CDU가 여러 대 반복 배치되고 **공유 2차측**에
연결된다 — CDU 간 상호작용이 있다는 뜻. 냉각방식은 D2C 우선 검토. 상세는 원본
문서 2장.

## 도구 선정 (결론만)

`Python + NumPy + SciPy + CoolProp + Pandas` 로 확정 (3장에서 IDAES+Pyomo+CoolProp
조합과 비교 검토 후 결정). CoolProp을 직접 호출해 미분가능성 문제를 피한다. HYSYS는
교차검증용 병행 고려 대상이며 필수 경로 아님. Pyomo/IDAES는 최적화가 실제로
필요해지는 단계가 오면 재검토.

## 단계적 확장 전략 (4장 — 정본)

```
1. 단일 CDU 모델 (현재 단계) — 2차측은 고정 경계조건
2. 랙 개수 확장 — 헤더 압력평형 기반 유량분배
3. 누출 시나리오 주입 — 배관 K값 변화로 근사
4. 다중 CDU 확장 — 공유 2차측 결합, 상위 레벨 연립방정식
```

단일 CDU 모델 구조(파라미터·상태·핵심 함수)는 원본 문서 4장의 `CDUModel` 스케치를
따른다. 압력-유량은 매 시점 quasi-steady 대수방정식, 온도는 시간적분 — 하이브리드
방식(관성 시간상수 차이 반영).

## 시나리오 가정치 — 어디 있나

랙당 발열량·펌프 특성곡선·밸브 Cv·열교환기 NTU·배관 사양·누출 K값 변화율 등
모든 구체 수치는 **원본 문서 5장 표**에 있다. 여기 옮기지 않는다 — 대화가 길어지면
숫자가 반올림되거나 흐려질 수 있으므로, 프롬프트에 구체적 수치를 쓸 때는 기억이
아니라 **5장 표를 다시 확인한 값**을 쓴다.

핵심만: 랙당 IT 발열 80kW · CDU당 8랙(총 640kW, 정격 ~750kW) · 냉각액 PG25
(`INCOMP::MPG-25%`) · 1차측 32/42℃ · 누출 시나리오는 K값 +5%/+20%/+50% (미량/중간/대규모).

## Feasibility 검증 기준 (6장 — 정본)

- energy balance 오차 <0.1%
- 부하 증가 시 T_return이 상승 방향으로 반응
- 정상상태 수렴 시간이 열용량 대비 합리적
- 극단 케이스(부하 0 / 최대)에서 모델이 발산하지 않음

이 넷을 통과하는 것은 "무너지지 않는다"는 뜻이지 "정확하다"는 뜻이 아니다
(실측 데이터가 없으므로 정확도는 지금 잴 수 없다).

## 지금 어디인가

**코드 착수 전.** 저장소는 `https://github.com/bakjeucar-boop/cdu-simul`,
로컬 경로는 `C:\Users\SAMSUNG\Documents\cdu-simul`(Windows)로 정해졌고,
GitHub로 여러 PC 간 동기화한다(`CLAUDE.md`「작업 시작 전 필독」).
프로젝트정리 문서(배경·설비구조·도구선정·전략·가정치)까지 정리된 상태이고, 7장의
다음 단계 체크리스트가 아직 하나도 시작되지 않았다.

```
[ ] 1-A 저장소 clone · 가상환경 · 저장소 골격 · 5장 가정치 전사 · CoolProp 래퍼
    · `PROCEED.md` · `daily_brief.py` · 문서 4종 커밋                        ← 다음 판
    (물리 모델 없음 — feasibility 기준을 여기서 보지 않는다)
[ ] 1-B 단일 랙 + 단일 CDU, 2차측 고정온도, 정상상태 모델 구현 → balance 검증
    (끝나면: 결과 검토 + 「파일럿 종료 조건」 절을 채울지 여기서 확인)
[ ] 시간축 추가(solve_ivp) → 부하 변화에 대한 온도 응답 확인
[ ] 랙 개수 확장 → 유량분배 로직 추가
[ ] 누출 시나리오 주입 → 신호 패턴 분석
[ ] 다중 CDU 공유 2차측 결합 구조로 확장
[ ] (설계데이터 확보 시) 가정치 → 실측/설계값으로 교체
[ ] HYSYS 결과와 교차검증 검토
```

세션 1-A·1-B의 게이트는 `CLAUDE.md` 「개발 순서 — 게이트」 표를 본다.
(세션 1은 판이 커서 둘로 나눴다 — 1-A는 저장소·환경·가정치 전사, 1-B는 정상상태
모델과 balance 검증. 게이트가 붙는 것은 1-B다.)

> 세션이 쌓이면 저장소 안의 `PROCEED.md`가 "오늘 상태"를 관리하게 된다.
> **이 절과 `PROCEED.md`가 어긋나면 `PROCEED.md`가 맞다** — 이 파일은 구조를
> 적고, `PROCEED.md`는 당일 상태를 적는다. 대화 시작 시 `PROCEED.md` 맨 위
> 표를 새 대화 첫 메시지에 붙이는 것을 기본으로 한다.

## 정본 문서가 어디 있나

| 무엇 | 어디 |
|---|---|
| 배경·설비구조·도구선정 근거·시나리오 가정치·feasibility 기준 | `AI서버_CDU_디지털트윈_프로젝트정리.md` |
| 코딩 규약·절대 규칙·개발 게이트·저장소 경로 | `CLAUDE.md` |
| 역할 분담·판단 원칙·프롬프트 형식·하루의 모양(git pull/push) | `collaboration.md` |
| **파일럿 종료 조건·본 프로젝트 전환 기준** | 이 파일 (「파일럿 종료 조건」 절) |
| **오늘 상태·미해결·다음 작업** (세션 1-A 이후) | 저장소 내 `PROCEED.md` — 지식 파일로는 올리지 않는다(커지면 무거워진다) |

> **이 4개 문서는 저장소 루트에도 커밋된다(세션 1-A).** 즉 같은 문서가 웹 프로젝트
> 지식과 저장소에 두 벌 존재한다. 어긋나면 **저장소 사본이 정본**이고, 사람이 프로젝트
> 지식을 다시 업로드해 맞춘다. 로컬 Claude Code는 이 문서들을 임의로 고치지 않는다
> (`CLAUDE.md` 「작업 시작 전 필독」 4).

저장소가 생기고 세션이 쌓이면, 이 파일에 "정본 수치"(세션 1-B 검증 통과 시의 실제
balance 오차 등 **잰 값**)와 "지금 진행 중인 판" 절을 추가한다. 지금은 아직 잰
값이 없으므로 비워둔다 — 짐작한 숫자를 채워 넣지 않는다.
