# PROCEED.md — 진행 상태

> 이 파일이 "오늘 상태"의 정본이다. `project-overview.md` 「지금 어디인가」 절과
> 어긋나면 이 파일이 맞다. 대화 시작 시 `daily_brief.py` 출력을 첫 메시지에 붙인다.
> **모든 수치는 가정값 기반이며 실측이 아니다.**

## 현재 상태

| 세션 | 단계 | 게이트 통과 여부 | 다음 세션 첫 작업 |
|---|---|---|---|
| 1-A | 저장소 clone · 가상환경 · 저장소 골격 · 5장 가정치 전사 · CoolProp 래퍼 · `PROCEED.md`·`daily_brief.py` 초기 구성 · 문서 4종 커밋 | **해당 없음 (물리 모델 없음)** — 환경 스모크 테스트 5건 통과했으나 이는 feasibility 판정이 아니다 | (완료) |
| 1-B | 단일 랙 + 단일 CDU, 2차측 고정온도, 정상상태 모델 (`model.py`) · energy balance 자동 검사 | **통과** — 4케이스 최대 \|잔차\| = **0.00506 %** (기준 <0.1%). 단 아래 ※를 반드시 함께 읽는다 | (완료) |
| 1-C | 저장소 위생 정비 — ruff·mypy 도입 · 위생 미해결(#6·#9·#10·#11·#16) 처리 · 5-1 출처 반영 | **게이트 없음** (물리 모델을 건드리지 않음). 대신 **정비 전후 4케이스 수치가 `repr` 전체자리까지 동일**함을 확인했다. pytest 17건·ruff·mypy 전부 통과 | (완료) |
| 2 | 시간축 추가 — `dynamics.py` (2노드 온도 `solve_ivp` 적분) · 부하 스텝 응답 | **통과** — 부하 상승/하강 스텝에서 T_return 이 타당한 방향으로 반응(스텝 직후 기울기·순변화 둘 다). 5장 범위 **양 끝 16조합 전부**에서 성립. 단 아래 ※2 를 함께 읽는다 | (완료) |
| 2.5 | 문서 정합 판 — 5장 빈칸을 5-1 로 닫고 파일럿 종료 조건 확정 (코드 변경 없음) | **게이트 없음** (문서 판이다. feasibility 판정 없음). 코드를 건드리지 않았고 pytest 53건·ruff·mypy 전부 통과를 재확인했다 | (완료) |
| 2.5-B | `project-overview.md` 잔재 정리 (종료 조건 확정 반영 · 6장 기준 요약 동기화) | **게이트 없음** (문서 판) | (완료) |
| 3-A | 수력 모듈 — 5-1 수력 가정치 전사 · `hydraulics.py` (펌프곡선·밸브·분기저항·잔여저항 역산) · 8랙 헤더 압력평형 `fsolve` | **판정 없음** — 6장 기준은 열모델이 붙는 3-B 에서 본다. `fsolve` 수렴은 세션 3 게이트의 **절반**이고 8조합 전부 `ier=1` 이었다. 나머지 절반(극단 케이스 비발산)은 **아직 안 봤다** | (완료) |
| 3-A2 | 물성 단일화 보정 — 5-1 의 밸브 Kv·배관 K 를 값에서 **역산 규칙**으로 전환 · SG 정의를 CoolProp 래퍼 한 곳으로 · 수력 물성 온도 인자화 | **판정 없음** (물성 정합 보정 판). 정격점 ΔP 가 5장 표값(2/3 · 3/5 mAq)을 **잔차 1e-15 mAq 로 항등 재현**한다 — 세션 3-A 의 1.995/2.993 이 닫혔다. pytest 97건·ruff·mypy 통과 | (완료) |
| 3-B | 열모델 결합 — 8랙 확장 · 수력↔열 결합(고정점 안에 quasi-steady 수력) · 동적 매 시점 결합 | **세션 3 게이트 통과** — ㉠ `fsolve` 수렴: 열결합 32조합에서 수력·열·결합 고정점 세 solver 전부 `ier=1` · ㉡ 극단 케이스 비발산: 부하 0%/100% 정상상태 32조합 + 동적 4케이스 전부. **앞 게이트 재확인**: 1-B energy balance 32조합 최대 |잔차| **0.00507 %**(<0.1%) · 세션 2 방향성 16조합 전부. **수렴시간은 여전히 판정하지 않았다**(미해결 #21·#31). pytest 294건·ruff·mypy 통과 | (완료) |
| 4 | 누출 시나리오 주입 — 배관 K값 +5/+20/+50% 를 랙 1개에 · 정상상태 신호 32조합 × 3수준 · 동적 스텝 주입 · M 배분 민감도 | **세션 4 게이트 통과** — 판정 기준(A 부호 일관성 · B 수준 간 엄격 단조 · C 잡음 대비)을 코드보다 먼저 적고(커밋 803142e) 그대로 판정했다. 96건 전부 통과. **앞 게이트 유지**: 정상 경로 결과가 3-B 와 바이트 단위 동일 · 누출 상태 energy balance 최대 |잔차| 0.00507 % · 누출 3수준에서 수력·열·결합 fsolve 전부 수렴. pytest 659건·ruff·mypy 통과 | (완료) |
| 5 | 다중 CDU · 공유 2차측 결합 — CDU 2대 · 총 2차측 유량 고정 · 1차측 유량 비례 배분 · 상위 레벨 연립 `fsolve` | **세션 5 게이트 통과** — 판정 기준(A 수렴 · B 잔차 동시 소멸 · C 연동의 실재)을 코드보다 먼저 적고(커밋 acbc31c) 96 케이스 전수 판정했다. **단 C4(대칭 케이스의 세션 4 재현)는 실패** — 32조합 최대 편차 0.0245 K. 원인은 5-1 두 행의 내부 모순이며 미해결 #33 으로 올렸다. 앞 게이트 유지: 단일 CDU 경로가 세션 4와 부동소수점까지 동일 · 결합·누출 상태 energy balance <0.1%. pytest 1109건·ruff·mypy 통과 | (완료 — 단 게이트는 세션 5-B 에서 재판정됐다) |
| 5-B | Cr 유도로 전환 — 5장 「1:1」을 부피유량비로 읽고 2차측을 정격 15.5 L/s 고정 · Cr 을 매번 물성에서 유도 · 단일 CDU 에 소급 | **세션 1-B~5 다섯 게이트 전부 재판정 통과** — 1-B 32조합 최대 |잔차| 0.00506% · 2 방향성 16조합 · 3 수렴·비발산 32조합 · 4 누출 신호 96건 · 5 연동 수렴 96케이스. **판정 기준·허용오차를 하나도 조정하지 않았다.** C4 대칭 재현 편차가 0.0245 K → **0.000e+00 K** 로 해소됐다(미해결 #33 닫힘). pytest 1109건·ruff·mypy 통과 | (완료) |
| 5.5-A | 데이터셋 설계 · 조합 계수 · 실행시간 측정 (생성 없음) | **판정 없음 — 설계·측정 판이다.** 아무것도 확정하지 않았다: 스키마는 제안, 부하 축 값 미선택, 규모 3안 미선택. 모델 코드·문서 4종·기존 테스트를 건드리지 않았고 세 결과 표가 불변임을 확인했다. pytest 1109건·ruff·mypy 통과 | (완료) |
| 5.5 | 시나리오 배치 실행 → 텍스트 데이터셋 (**파일럿 마지막 판**) | **세션 5.5 게이트 통과** — 기준 A 순서 무관 · B 단독 재현 · C 정상 복귀 · D 전 행 표기. 판정 기준을 코드보다 먼저 적었다(커밋 cc790fa). 데이터셋 310,016행 · 48열 · 305 MB · 28분 · solver 실패 행 0. 앞 게이트 1-B·2·3·4·5 전부 재확인 통과 | **파일럿의 마지막 판이 끝났다.** 데이터셋 310,016행·48열·305 MB 가 `results/` 에 있고(원격에 없다) 요약 리포트가 종료 판정 재료를 모았다. **사람이 정할 것 — 다중 CDU 전이의 누출 주입 지점(미해결 #34). 그 결정 전에는 새 판을 열지 않는다.** 파일럿 종료 판정도 사람의 몫이다 — `python -m cdu_simul.dataset_report` |
| 5.5-C | 문서 정합 · 미해결 정리 · 브리핑 축약 (`project-overview.md` 체크리스트 3·4·5 · `CLAUDE.md` 경로 표기 PC 독립화 · `daily_brief.py` 축약 출력) | **게이트 없음** (문서·위생 판이다. feasibility 판정 없음). 모델 코드·기존 테스트·데이터셋을 건드리지 않았고 pytest 1,153건·ruff·mypy 전부 통과를 재확인했다 | **파일럿의 판은 전부 끝났다.** 남은 것은 사람의 판정 둘 — 미해결 #34(다중 CDU 전이의 누출 주입 지점)와 파일럿 종료 판정. 그 전에는 새 판을 열지 않는다 |
| 5.5-D | 다중 CDU 누출 주입 지점 정정 (미해결 #34) — `plant.py` CDU별 누출 템플릿 · 저장 격자를 `storage_times_s` 로 · `leak_cdu_index` 열 · 데이터셋 재생성 | **앞 게이트·세션 5.5 게이트 전부 재판정 통과** — 판정 기준·허용오차를 하나도 바꾸지 않았다. **이 판의 목적이 확인됐다**: 누출을 CDU A 에만 걸자 부하가 그대로인 B 의 2차측 몫이 단조 증가(+50% 에서 +0.01908 L/s)하고 환수온도가 단조 감소(−0.01064 K)한다 — 세션 5 C7 정상상태 관측(+0.01905 L/s · −0.012649 K)과 **방향 동일 · 배분 이동 세 자리 일치**. 종전 양쪽 누출에서는 배분 이동이 +1.9e-05 L/s 로 상쇄됐다. 데이터셋 310,016행 · **49열** · 305.1 MB · 61.3분 · solver 실패 행 0. pytest 1,175건·ruff·mypy 통과 | **파일럿 종료 판정 전에 세 판이 남았다 — 5.6(누출 부호) · 5.7(#31 시간축) · 5.8(이동지연 감도). 이 셋을 넘어가는 것은 전부 설계데이터가 필요하다** (아래 「사람이 정한 것 — 세션 5.5-D 마무리」). 미해결 #34 는 닫혔다. **사람이 정할 것 — C4 의견**(다중 CDU 전이도 누출을 스텝으로 넣을 것인가) |
| 5.6 | 누출 질량손실 **부호 확인** (수력 한정 · 절대 규칙 8 예외, 사람 승인) — `leak_massloss.py` 단독 모듈 · K 근사 대 질량손실 전수 272회 | **게이트 없음 · 관측 판이다** (6장 기준을 하나도 판정하지 않았다 — 열모델이 없다). 판정 기준을 코드보다 먼저 적고(커밋 `7285ef7`) 그대로 판정했다. **부호가 갈린다**: 총유량·누출랙 유량이 K 근사(−)와 질량손실(+)에서 **정반대**, 펌프 양정은 5장·5-1 이 정하지 않는 **잔여저항 배치에 따라 갈린다**. 질량손실 누출은 헤더가 저항 0 인 이 모델에서 **랙에 국소화되지 않는다**(랙 간 비대칭 2.220e-16 L/s). 격리 확인 2.220e-16 L/s · `fsolve` 272회 전부 `ier=1`. pytest 1,175건·ruff·mypy 통과 | **모델·데이터셋을 고치지 않았다** — 관측을 미해결 **#36** 으로 올렸다. 어느 쪽으로 갈지는 사람이 정한다. 남은 판: 5.7(#31 시간축) · 5.8(이동지연 감도) |
| 5.6-B | 공급·환수 유량 불일치 Δ = Q_sup − Q_ret 확인 (5.6 결과 **재출력** — 새로 푼 것 없음) | **게이트 없음 · 관측 판이다** (6장 기준을 하나도 판정하지 않았다). 판정 기준을 코드보다 먼저 적고(커밋 `9d210e3`) 그대로 판정했다. **A 검산 통과**(질량손실 Δ−Q_leak 최대 8.882e-16 L/s · K 근사 Δ 정확히 0) — **단 구성상 항등식이라 물리 검증이 아니다**. **B 단조 통과**(전 배치·전 조합 엄격 증가). **C 배치 불변 통과**(배치 6 편차 1.776e-15 L/s) — **가설이 맞았다**. `g=0/펌프=공급` 배치에서 다섯 양이 전부 0 인데 Δ 만 +2.838e-02 ~ +1.877e-01 L/s (최대 1.211%)로 **유일한 생존 신호**다. 격리 확인: 5.6 의 표 1·2·3 이 **6,295 바이트 동일**. pytest 1,175건·ruff·mypy 통과 | **모델·데이터셋·5-1·#36 문구를 고치지 않았다.** 5.7 스키마의 Δ 열 필요 여부는 **#36 의 선택에 종속**이다 — (가)면 불필요, (나)·(다)면 배치 자유도에 흔들리지 않아 열로 넣을 수 있다. 사람이 정한다 |
| 5.7 | 미해결 **#31 시간축 정정**(M 을 8랙으로) · 위생 3건(#18·#19·#22) · 스키마 3열(`dataset_version`·`stimulus_kind`·`leak_model`) · 데이터셋 재생성 | **앞 게이트 전부 기존 기준 그대로 재판정 통과** — 판정 기준·허용오차를 하나도 바꾸지 않았고 새 게이트를 만들지 않았다. **정상상태 해가 M 8배에 불변**(model·hydraulics 표 바이트 동일) — 5-1 서술이 코드에서 성립했다. **τ·t63·t95 가 정확히 8.000000배 이동**(부하 스텝 4 · 누출 스텝 6 전부). 위생 3건은 수치 표 6개가 바이트 동일. 데이터셋 **310,016행 · 52열 · 359.8 MB · 57.0분 · solver 실패 행 0** · 표기·신3열 빈 칸 0. 검사기 4회(②④⑥⑨) 전부 pytest 1,175건·ruff·mypy 통과 | **#31 닫힘. 그러나 6장 「수렴시간」은 여전히 판정 불가** — #21·#25·#23 이 열려 있다. **사람이 할 일: 5-1 「누출 시나리오」·「누출 주입 지점」에 #36 한계 문구 추가**(정본 수정 · 재업로드 필요). 남은 판: 5.8(이동지연 감도) |
| 5.7-C | 오늘 결정 기록 (기록 판 — 정지선 변경 · 보충수 불필요 · 계통 dP 정리 · 시연 관점 · 미해결 #36 갱신 · #37 신규) | **게이트 없음 · feasibility 판정 없음 · 수치 변경 0.** 모델 코드·테스트·데이터셋(52열본)·`dataset_report`·`leak_massloss.py`·정본 문서 4종을 하나도 건드리지 않았다. 바뀐 파일은 `PROCEED.md` 하나. pytest 1,175건·ruff·mypy 그대로 통과 | **정지선이 한 판 넓어졌다** — 새 순서는 **5.7-D(질량손실 열·전이 관측) → 5.8(이동지연 감도) → 종료 판정**. **보충수 온도 문제가 닫혀**(미량 조기감지 구간에서는 보충 로직이 작동하지 않는다) 5.6 이 수력 한정을 택한 이유가 사라졌다. **사람이 할 일: 5-1 에 문구 둘 추가**(누출 시나리오 한계 · 보충수 처리) → 재업로드 필요 |
| 5.7-E | 정본 5-1 에 문구 둘 추가 (사람 지시 · 정본 수정) — 「누출 주입 지점」 근거 끝에 누출 K값 근사의 한계(#36 · 세션 5.6·5.6-B) · 새 행 「보충수 처리」(세션 5.7-C 확정) | **게이트 없음 · 문서 판이다 · 수치 변경 0.** 모델 코드·테스트·데이터셋(52열본)·`dataset_report`·`leak_massloss.py` 를 하나도 건드리지 않았다. 바뀐 파일은 `AI서버_CDU_디지털트윈_프로젝트정리.md`(5-1 두 줄) 와 `PROCEED.md` 둘. diff 가 5-1 밖으로 나가지 않음을 확인했다(2 insertions · 1 deletion · 전부 5-1 표 안). pytest 1,175건·ruff·mypy 그대로 통과 | **정본을 고쳤다 — 프로젝트 지식 재업로드가 필요하다.** 미해결 #36 의 (가)/(나)/(다) 최종 선택은 여전히 사람 몫이며 이 판은 고르지 않았다. 다음 판: **5.7-D**(질량손실 열·전이 관측 · #37 확인 포함) → 5.8(이동지연 감도) → 종료 판정 |
| 5.7-D | 질량손실 누출의 **열·전이 관측**(절대 규칙 8 예외 · 사람 승인) — `leak_massloss_thermal.py` 별도 모듈 · A~G 전수 · #37 확인 | **게이트 없음 · 관측 판이다**(6장 기준을 하나도 판정하지 않았다). 판정 기준을 코드보다 먼저 적고(커밋 `9bd246e`) 그대로 판정했다. **열에서도 부호가 갈린다** — 누출랙 출구온도·CDU 환수온도가 K 근사(**+**)와 질량손실(**−**)에서 **정반대**이고 전 조합(1,536건)에서 뒤집히지 않는다. **수력이 못 보는 것을 열이 본다** — 세션 5.6 이 다섯 양 전부 0 이던 `g=0/펌프=공급` 배치에서도 환수온도가 −7.368e-02 ~ −1.886e-03 K 로 살아 있다(배치 6 전부 같은 부호). **balance 는 누출 엔탈피 항을 넣어야 닫힌다** (빼면 1.2129% · 넣으면 0.000062%). **τ ∝ M 이 정확히 성립**(τ 비 = t63 비 = M 비 = 12.832779). **다중 CDU 는 K 근사와 방향이 같다**(B 몫 + · B 환수온도 −). 격리 7.105e-15 K · `fsolve`·`solve_ivp` 실패 0건. pytest 1,199건·ruff·mypy 통과 | **모델·데이터셋·5-1·규칙 8 문언을 하나도 고치지 않았다.** 관측을 #36 에 append 하고 **#38**(상수 M 유보) · **#39**(2차측 배분이 보는 1차측 유량)를 새로 열었다. #36 의 (가)/(나)/(다) 선택은 여전히 사람 몫이다. 남은 판: 5.8(이동지연 감도) |
| 5.8 | **이동지연 감도**(N-CSTR 수치 수렴) · #37 문구 — `transport_lag.py` 별도 모듈 · A~E 전수 · **파일럿의 마지막 판** | **게이트 없음 · 관측 판이다**(6장 기준을 하나도 판정하지 않았다). 판정 기준을 코드보다 먼저 적고(커밋 `2cfaa7a`) 그대로 판정했다. **A 정상상태 N 불변** — 누출 전 값은 **완전 일치**, 누출 후는 적분기 잡음(≤3e-9 K)만. **D 누출 신호 부호가 N 에 뒤집히지 않는다** — 전 조합 384건에서 N=2 와 N=16 이 같은 부호(최대 차이 7.5e-9 K) 이고 전이 경로도 역부호로 벗어나지 않는다(6.4e-14 K). **E τ ∝ M 이 N 과 무관하게 성립**(t63 비 12.8327~12.8328 · M 비 12.832779). **그러나 C 수렴은 실패했다 — N≤64 안에서 수렴하지 않았다.** 파형 편차가 직전 N 대비 3.05e-03 → 2.44e-03 K 로 16배 세분에도 1.25배밖에 줄지 않고, 순신호 대비 **9.5~24.8%** 를 유지한다. N=2 대 N=16 은 순신호 대비 **31.7~58.0%** · t63 이 **−11.7%~+80.9%** 이동한다. `solve_ivp` 실패 0건(1단 144 + 2단 768 = 912 적분). pytest 1,207건·ruff·mypy 통과 | **모델·데이터셋·5-1 을 하나도 고치지 않았다.** **#40**(2노드 전이 파형 미수렴)을 새로 열었다. `dataset_report` 에 #37 문구를 넣었다(데이터셋 무수정). **파일럿의 판은 전부 끝났다 — 남은 것은 사람의 종료 판정뿐이다** |
| 5.9 | 파일럿 **종료 판정 재료 통합** — `dataset_report` 에 세션 1-A~5.8 의 재료를 한 자리에 모은다 (문서 판) | **게이트 없음 · 문서 판이다 · 판정하지 않았다 · 수치 변경 0 · 새 실행 0.** 모델 코드·테스트·데이터셋(52열본)·`transport_lag.py`·`leak_massloss*.py`·정본 문서 4종을 하나도 건드리지 않았다. 바뀐 파일은 `dataset_report.py` 와 `PROCEED.md` 둘. **새로 만든 수치 0개** — 전부 `PROCEED.md`·기존 리포트에서 옮겼고 세션 번호를 붙였다. pytest 1,207건·ruff·mypy 그대로 통과 | **발견: 리포트의 6장 ③·④ 번호가 정본 6장·`CLAUDE.md` 규칙 10 과 뒤바뀌어 있었다** — 정본 순서로 번호·표시순서만 맞췄고 판정 문구·수치는 무수정. **남은 것은 사람의 종료 판정뿐이다** — `python -m cdu_simul.dataset_report` |
| 6.0 | **파일럿 종료 판정 기록** (사람 판정 · 정본 수정) — `project-overview.md` 「파일럿 종료 조건」 아래에 판정 결과를 붙인다 | **게이트 없음 · 판정 기록 판이다 · 코드 무수정 · 데이터셋 무수정 · 수치 변경 0 · 새 실행 0.** 판정은 **사람이 이미 내린 것**을 옮겨 적었고 이 판이 판정하거나 근거를 새로 만들지 않았다. 여덟 항목을 **문구 그대로** 실었다. 미해결을 **하나도 닫지 않았다.** pytest 1,207건·ruff·mypy 그대로 통과 | **정본을 고쳤다 — 프로젝트 지식 재업로드가 필요하다**(대상: `project-overview.md` 하나). **파일럿이 끝났다.** 다음 판 = **시연 데이터 준비** (범위는 사람이 정한다) |
| 7.0 | **시연 데이터 준비 ①** — PFD 태그 열 매핑 + 시연용 정상상태 실행 → JSON (파일럿 이후의 판) | **게이트 없음 · feasibility 판정 없음 · 데이터셋(52열본) 무수정 · 모델 계산 로직 무수정.** 새 모듈 `demo_steady.py` 와 새 파일 `demo/tag-map.md`·`demo/demo_steady.json` 만 늘었다. 136 케이스 12.8 초 · **solver 미수렴 0 건** · 태그 빈 칸 0. pytest 1,207건·ruff·mypy 통과 | **정본 문서 무수정 — 재업로드 필요 없음.** 미해결을 **하나도 닫지 않았다.** 다음 판 = **시연 화면(HTML)** — JSON 을 읽어 PFD 로 그린다 |
| 7.0-B | **오늘 마무리 · 미해결 #41 기재** — 세션 7.0 이 「보고만」으로 남긴 발견을 미해결 목록에 올린다 (기록 판) | **게이트 없음 · 판정 없음 · 수치 변경 0 · 코드 무수정 · 데이터셋 무수정 · 새 실행 0.** 발견을 **고치지 않았다** — 목록에 올리기만 했다(`collaboration.md` ⑤). pytest 1,207건·ruff·mypy 그대로 통과 | **확인: `dataset_report` 는 이 열의 값을 읽지 않는다** — 따라서 **2026-08-31 종료 판정 재료에 영향이 없다.** 다음 판 = **시연 화면(HTML)** — 세션 7.0 ⑻ 이 넘긴 것을 따른다 |
| 7.0-C | **시연 데이터 훑기** — `demo/demo_steady.json` 이 무엇을 그릴 수 있고 각 태그가 케이스 간에 얼마나 움직이는지를 숫자로 확정 (읽기 전용 판) | **게이트 없음 · feasibility 판정 없음 · 계산 0 · 코드 무수정 · 데이터셋 무수정 · JSON 무수정 · 새 실행 0.** JSON 에 있는 값을 훑어 요약만 했다(일회성 스크립트는 커밋하지 않았다). 축의 곱 **2 × 17 × 4 = 136 확인**(중복·결손 0) · 금지 항목(압력·양정·ΔP · 2차측 환수 · `heat_capacity_ratio`)이 **`cases[]` 안에 하나도 없음** 확인 · NaN·부호역전·단위 불일치 **0건** · `all_converged` 136 케이스 전부 true. 바뀐 파일은 `PROCEED.md` 하나. pytest 1,207건·ruff·mypy 그대로 통과 | **정본 문서 무수정 — 재업로드 필요 없음.** 새 미해결 **#42**(`T_supply_C` 의 저항 응답이 비단조) 하나를 열었고 다른 미해결은 **하나도 닫지 않았다.** 다음 판 = **시연 화면(HTML)** |
| 7.0-D | **시연 화면** — `demo/demo_steady.json` 136 케이스를 정적 PFD 한 장으로 (`demo/pfd.html` · CSS·JS 인라인 · 외부 라이브러리 0) | **게이트 없음 · feasibility 판정 없음 · 새 가정치 0개 · 모델 코드 무수정 · 데이터셋 무수정 · JSON 무수정.** 화면은 **계산하지 않는다** — 허용 산술은 표시 자릿수 반올림과 Δ 배지의 뺄셈 둘뿐이다. 태그는 `demo/tag-map.md` 로 닫았고 압력·양정·ΔP·2차측 환수·Cr 을 하나도 싣지 않았다. **C9 대조 3케이스 95개 태그 전부 일치**(불일치 0) — 페이지 스크립트를 최소 DOM 위에서 그대로 돌려 대조했다(일회성 · 미커밋). pytest 1,207건·ruff·mypy 그대로 통과 | **정본 문서 무수정 — 재업로드 필요 없음.** 새 미해결 **#43**(고정 CDU `total_flow_Lps` 가 4자리 반올림 경계를 걸친다) 하나를 열었고 다른 미해결은 **하나도 닫지 않았다.** 여는 방법은 아래 세션 로그에 있다 — `file://` 로는 열리지 않는다 |
| 7.2 | **랙별 등가길이 배분 · 시연 JSON 에 질량유량** — 5장 등가길이 20~30 m 를 랙 8개의 공간 분포로 읽는다 (5-1 새 행 · 사람 확정) | **게이트 없음 · feasibility 판정 없음 · 6장 재판정 없음 · 새 숫자 0개.** **랙별 배분을 끈 상태(모델 기본값)에서 `plant`·`model`·`hydraulics` 세 표가 이 판 이전과 바이트 단위로 동일**하고 pytest 1,207건·ruff·mypy 통과 — 기존 게이트 기록이 움직이지 않았다. 배분을 켠 시연 재생성: 136 케이스 · 549 KB · **5.4 초** · solver 미수렴 0 · 태그 빈 칸 0. **랙 간 유량 차 7.70%(567 kg/h) · 출구온도 차 0.166~0.825 K** | **정본을 고쳤다 — 프로젝트 지식 재업로드 필요**(대상: `AI서버_CDU_디지털트윈_프로젝트정리.md` 5-1 새 행 「랙별 등가길이 배분」). `hydraulics.py` 는 한 줄도 안 고쳤다 — 세션 3-A 의 `branch_K_multipliers` 진입점을 그대로 썼다. 새 미해결 **#44 · #45** 를 열었고 다른 미해결은 **하나도 닫지 않았다.** `demo/pfd.html` 무수정 — 화면은 아직 kg/h 를 모른다 |
| 7.2-B | **저항 주입 랙을 랙7(가장 먼 랙)로 · 5-1 두 행 정정 · tag-map 에 질량유량 태그** — #44·#45 를 닫는 판 | **게이트 없음 · feasibility 판정 없음 · 6장 재판정 없음 · 새 숫자 0개**(주입 랙 번호는 랙 개수에서 유도한 시나리오 정의다). **랙별 배분을 끈 상태에서 `plant`·`model`·`hydraulics` 세 표가 이 판 이전과 MD5 동일**(`8d34d94a…`·`a84b4b61…`·`0be5952b…`)하고 pytest 1,207건·ruff·mypy 통과. 시연 재생성 `demo-7.2-B`: 136 케이스 · solver 미수렴 0 · 태그 빈 칸 0. **주입 랙7 은 가장 가까운 정상 랙(랙6)과 유량 6.12 vs 6.84 m³/h · 출구온도 43.898 vs 42.664℃ 로 표시 자릿수에서 확실히 구분된다 — #45 닫힘** | **정본을 고쳤다 — 프로젝트 지식 재업로드 필요**(대상: `AI서버_CDU_디지털트윈_프로젝트정리.md` 5-1 「누출 주입 지점」·「랙별 등가길이 배분」 두 행). `hydraulics.py`·`assumptions.py`·52열본 데이터셋·`demo/pfd.html` **무수정**. **#44·#45 를 닫았다** |
| 7.3 | **시연 화면을 랙 중심으로 재설계** — `demo/pfd.html` 한 장을 P&ID 형태에서 **랙 슬래브 8개가 주인공인 경영층 화면**으로 (CSS·JS 인라인 · 외부 CDN·폰트 0) | **게이트 없음 · feasibility 판정 없음 · 6장 재판정 없음 · 새 숫자 0개 · 모델 코드·데이터셋·JSON·정본 무수정.** 화면은 계산하지 않는다 — 허용 산술 셋(L/s × 3.6 · 표시 자릿수 반올림 · 같은 파일 두 케이스의 차)만 썼다. **C10 대조 3케이스 134항목 전부 일치**(불일치 0)이고 주입 랙이 화면에서 **랙 8**(JSON 인덱스 7)로 표시됨을 확인했다. 최소 DOM 스텁 위에서 페이지 스크립트를 그대로 돌려 **paint 가 만지는 id 69개 전부 골격에 존재**·시나리오 4종 예외 0·전 136 케이스 `view()` 통과. pytest 1,207건·ruff·mypy 그대로 통과 | **정본 문서 무수정 — 재업로드 필요 없음.** 새 미해결 **#46**(고부하에서 온도 계조가 랙을 거의 못 가른다) 하나를 열었고 다른 미해결은 **하나도 닫지 않았다.** 여는 방법은 세션 7.0-D 와 같다 — `file://` 로는 열리지 않는다 |
| 7.4 | **시연 화면에 서버실 탭 추가** — `demo/pfd.html` 한 장을 탭 둘로: **「서버실」(등축 데이터홀 뷰 · 기본 탭)** 과 **「계통도」(세션 7.3 화면 그대로)** (CSS·JS 인라인 · 외부 CDN·폰트·라이브러리 0) | **게이트 없음 · feasibility 판정 없음 · 6장 재판정 없음 · 새 숫자 0개 · 모델 코드·데이터셋·JSON·정본 무수정.** 화면은 계산하지 않는다 — 허용 산술은 넷뿐이다(`Lps × 3.6` · 표시 자릿수 반올림 · 같은 케이스 안 값들의 차·평균 · 등축 좌표 변환). **대조 236항목 불일치 0**(두 탭 모두 · paint 가 만지는 id 중 골격에 없는 것 0). **기하 검사: 도형 18개 겹침 0 · viewBox 이탈 0 · 글자↔도형 0 · 글자↔글자 0.** 미해결 **#46 을 닫았다** — 온도 계조를 8랙 평균 대비 편차(−1.5~+1.5 K 고정)로 바꿨고 전 136 케이스 실제 편차가 −0.554~+1.385 K 라 잘리는 랙이 0 개다. pytest 1,207건·ruff·mypy 그대로 통과 | **정본 문서 무수정 — 재업로드 필요 없음.** **공기 열기둥(플룸)을 그리지 않았다** — 모델에 공기가 없다. 대신 냉각수 배관이 그 일을 한다(색 = 환수온도 편차 · 굵기 = 유량 · 둘 다 계산한 값). 새 미해결 **#47** 하나를 열었고(등축에서 헤더 이름표를 놓을 자리가 없다) 다른 미해결은 닫지 않았다 |
| 7.5 | **`CLAUDE.md` 에 컨텍스트 규칙 둘** — 세션 7.2-B·7.3·7.4 의 `ctx` 관측 셋에서 **반복으로 확인된 둘**을 정본 규약에 세웠다: ⑴ 진행기록 문서를 통째로 읽지 않는다(시작 4번) ⑵ 셸로 파일을 만들거나 고치지 않는다(「하지 말 것」). 종료 6번에 `ctx` 한 줄 남기기를 더했다 | **게이트 없음 · feasibility 판정 없음 · 6장 재판정 없음 · 새 숫자 0개 · 코드·데이터셋·시연 화면·다른 정본 3종 무수정.** `git diff --stat` 이 `CLAUDE.md` 한 파일 **21 삽입 · 1 삭제**만 낸다(삭제 1행은 시작 3번을 두 줄로 늘려 쓴 것). 문안은 사람이 준 것을 **그대로** 넣었다 — 요약·개작 없다 | **정본을 고쳤다 — 프로젝트 지식 재업로드 필요**(대상: `CLAUDE.md` 「판 시작·종료 정형 절차」·「하지 말 것」). 미해결은 **하나도 열거나 닫지 않았다** |
| 7.6 | **오늘(7.0-C ~ 7.5)을 닫는 기록 판** — 오늘 관측에서 나왔으나 어디에도 안 적힌 둘을 미해결로 등재하고, 「현재 상태」 표에 오늘 행이 다 있는지 확인하고, 내일 첫 작업을 적었다 | **게이트 없음 · feasibility 판정 없음 · 6장 재판정 없음 · 새 숫자 0개 · 새 일 없음.** 바꾼 파일은 `PROCEED.md` **한 장**이다 — 정본 4종·`src/cdu_simul/`·`demo/`·데이터셋을 **하나도 열지 않았다.** 「현재 상태」 표 확인 결과 오늘 일곱 판(7.0-C·7.0-D·7.2·7.2-B·7.3·7.4·7.5) **전부 등재돼 있다 — 이상 없음** | **정본 문서 무수정 — 재업로드 필요 없음.** 새 미해결 **#48·#49** 둘을 열었고 다른 미해결은 **닫지 않았다.** 둘 다 `CLAUDE.md` 를 고쳐야 닫히지만 **이것만으로 정본을 열지 않는다 — 다음에 정본을 고치는 판에 얹는다.** **다음 판 첫 작업: 사람이 `demo/pfd.html` 을 브라우저로 직접 열어 본다** |
| 7.7 | **서버실 탭에 CDU 내부 그림** — `demo/pfd.html` 의 서버실 탭을 왼쪽 약 28 %(CDU 내부 그림 — 열교환기·펌프·1차/2차 스트림)와 오른쪽 약 72 %(등축 서버실)로 나눴다. **새 값을 만들지 않았다 — 이미 화면에 있던 값을 그것을 내는 장치의 자리로 옮긴 것이 전부다** | **게이트 없음 · feasibility 판정 없음 · 6장 재판정 없음 · 새 숫자 0개 · 모델 코드·`assumptions.py`·시연 JSON·생성 코드·데이터셋·정본 4종 무수정** (`demo/demo_steady.json` git 무변경 · `demo_version` 그대로). 화면의 값 글자 **36개로 옛 파일과 id 집합이 동일**하다 — 자리만 바뀌었다. **대조 165항목 불일치 0**(두 탭 · 세 케이스 `single/L100/R0`·`single/L100/R50`·`dual/L70/R50` · 표시 문자열을 수로 되돌려 JSON 원본과 대조 · paint 가 만진 id 중 골격에 없는 것 0). **기하 검사 single·dual 양쪽 위반 0**(그림칸↔등축 겹침 0 · viewBox 이탈 0 · 글자↔글자 0 · 글자↔도형 0 · 도형↔도형 0 · 그림칸 이탈 0). pytest 1,207건·ruff·mypy 통과 | **정본 문서 무수정 — 재업로드 필요 없음.** 등축 쪽은 `G` 도 `iso()` 도 손대지 않았다(그림칸을 왼쪽에 내고 좌표만 평행이동). **스트림 색은 절대온도 27~44 ℃ 고정 클램프** — ±1.5 K 계조·77 px 축척과 같은 등급의 **표시 규약**이지 5장의 물리 가정이 아니다. 새 미해결 **#50** 하나를 열었고(등축의 2차측 배관 두 줄이 이름표를 잃었다 — #47 과 같은 성격) 다른 미해결은 **닫지 않았다.** **우하단 Short Spec 상자는 이 판에서 만들지 않았다**(`pic/edit.jpg` 의 그 자리는 비워 뒀다) |
| 7.8 | **글자를 줄이고 도형으로 옮겼다** — `demo/pfd.html` 서버실 탭에서 설명 문구를 지우고 그 뜻을 도형이 지게 했다: **2차측을 파선 사각형**으로 세우고(모델 밖 = 파선), 하단 띠를 통째로 지우고 **편차 막대를 값 옆으로** 옮겼다 | **게이트 없음 · feasibility 판정 없음 · 6장 재판정 없음 · 새 숫자 0개 · 모델 코드·`assumptions.py`·시연 JSON·생성 코드·데이터셋·`demo/tag-map.md`·`pic/`·「계통도」 탭·정본 4종 무수정** (`demo/demo_steady.json` git 무변경 · `demo_version` 그대로). **대조 166항목 불일치 0**(두 탭 · 세 케이스 · 표시 문자열을 수로 되돌려 JSON 원본과 대조 · **paint 가 찾았으나 골격에 없는 id 0**). **편차 막대 둘이 같은 축척 77/(m³/h) 임을 px 로 확인**(R0 에서 둘 다 0 · R50 에서 랙 45.1 px · 총유량 11.1 px). **기하 검사 single·dual 양쪽 위반 0.** pytest 1,207건·ruff·mypy 통과 | **정본 문서 무수정 — 재업로드 필요 없음.** 화면의 값 글자에서 **「배분」·「2차측 공유」·「2차측 환수 — 모델 밖」·「스트림 색 = 절대온도」·「유량은 표시 자릿수에서…」·하단 띠 설명 넷을 전부 뺐다** — 남은 글자는 장치 이름과 운전값뿐이다. **#47·#50 은 고치지 않았다**(이번 화면을 보고 사람이 판단한다). 새 미해결 **#51** 하나를 열었다 |
| 7.9 | **랙 위 두 줄에 단위와 색을 붙였다** — `demo/pfd.html` 서버실 탭에서 맨 앞 랙(랙 01)의 유량·온도 숫자에만 단위(`m³/h`·`℃`)를 값보다 작고 옅게 붙였고, 온도 줄을 우상단 색 범례와 **같은 계조**(그 랙 슬래브와 같은 색)로 칠했다. 맨 아래 한 줄에서 「 · 프로젝트정리 5장」을 뺐다 — 「가정값 기반 — 실측 아님」만 남는다 | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개** · 대조 166항목 불일치 0 · 기하 위반 0(single·dual) · pytest 1,207건·ruff·mypy 통과 · `demo/demo_steady.json` **git 무변경** | 사람이 브라우저로 열어 본다 — #47·#50·#51 은 그 화면을 보고 판정 |
| 7.10 | **왼쪽 CDU 그림을 등축으로 세웠다** — `demo/pfd.html` 의 CDU 내부 그림에서 평면 사각형이던 셋(CDU 경계·열교환기·2차측 파선 상자)을 **서버실과 같은 `iso()`·`boxFaces()` 로 세운 육면체**로 바꿨다. 스트림 네 줄과 화살촉·펌프 심볼은 평면 그대로다. 계통도 탭에서 설명 문구 둘(`#sec` 「2차측 배분 · CDU A/B」·`SYS_B` 「유량은 표시 자릿수에서…」)을 걷어 **#51 을 닫았다** | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개** · 값·라벨·id 무수정(바뀐 것은 도형 좌표뿐) · **대조 166항목 불일치 0**(표시 문자열 집합이 세션 7.9 와 **한 글자도 다르지 않음** — 세 케이스 166개 전부 대조) · 편차 막대 px 0 / 45.1 / 11.1 (7.9 와 같음) · 기하: 그림칸 안 글자↔도형 겹침 0 · 스트림 끝점 6개 전부 육면체 면 위 · pytest 1,207건·ruff·mypy 통과 · `demo/demo_steady.json` **git 무변경** | 사람이 브라우저로 열어 본다 — #47·#50 은 그 화면을 보고 판정. 새로 연 **#52**(등축 dual 에서 B열 글자가 A열 배관 위에 얹힌다)도 같이 본다 |
| 7.11 | **CDU 경계를 2D 로 되돌렸다** — 세션 7.10 이 육면체로 세운 셋 중 **경계 하나만** 평면 사각형으로 돌렸다(`.frame` · `fill:none` · 선 굵기 1 · `opacity .45` · 국소 `[24,148,520,280]`). 경계의 윗면·옆면이 열교환기 뒤에 비쳐 장치가 상자에 갇혀 보이던 것을 없앤다 — **3차원으로 서는 것은 장치(열교환기·2차측 파선 육면체)뿐이다.** 안의 장치·스트림·펌프·글자는 좌표를 하나도 옮기지 않았다 | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개** · 값·라벨·id 무수정 · **대조 166항목 불일치 0**(46/46/74 · 편차 막대 px 0 / 45.1 / 11.1 — 7.10 과 같음) · 기하: 글자↔도형 겹침 **10→5**(single) · **38→31**(dual), 사라진 7건이 전부 경계 육면체 면이 값 줄을 지나던 것이고 **새 위반 0** · #52 의 20건과 2차측 파선 5건 **그대로** · 글자↔글자 0 · viewBox·그림칸 이탈 0 · 그림칸↔등축 0 · 스트림 끝점 6개 전부 면 위 · pytest 1,207건·ruff·mypy 통과 · `demo/demo_steady.json` **git 무변경** | 사람이 브라우저로 열어 본다 — #47·#50·#52 는 그 화면을 보고 판정 |
| 7.12 | **시연 꾸러미를 쌌다** — 저장소가 없는 PC 에서도 시연 화면을 열 수 있도록 `demo/start.bat`·`demo/start.sh`(표준 라이브러리 `http.server` 로 제 폴더를 띄우고 브라우저를 연다 · 파이썬 3.12 미만이면 그 사실을 적고 멈춘다) · `demo/README.txt`(4줄) · `demo/make_bundle.py`(`zipfile` 만 쓰는 꾸러미 스크립트 → `dist/cdu-demo.zip`) 넷을 새로 넣었다. **화면 자체(`demo/pfd.html`·`demo/demo_steady.json`)는 한 글자도 고치지 않았다** | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개** · 외부 라이브러리 0개(표준 라이브러리만) · `demo/pfd.html`·`demo/demo_steady.json` **git 무변경** · 꾸러미 87,814 B · 다섯 파일 전부 들었음 · 임시 자리에 풀어 **평평한 구조** 확인(`pfd.html` 과 `demo_steady.json` 이 같은 자리) · `dist/` 는 이미 `.gitignore` 에 있었음 · pytest 1,207건·ruff·mypy 통과 | 소개자료 HTML 이 다음 판 후보 — #47·#50·#52 는 여전히 사람이 화면을 보고 판정한다 |
| 7.13 | **시연 화면을 파일 하나로 만들었다** — `demo/make_single_html.py`(표준 라이브러리만 · `pfd.html:865` 의 `fetch("demo_steady.json")` **한 자리만** 데이터로 갈아끼운다)가 `dist/cdu-demo.html` 을 낸다. 압축 해제·스크립트 실행 없이 **더블클릭 하나**로 열린다. `.gitattributes` 에 `*.sh text eol=lf` 한 줄(미해결 #53 등재·닫음). **화면 자체(`demo/pfd.html`·`demo/demo_steady.json`)는 한 글자도 고치지 않았다** | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개** · 외부 라이브러리 0개 · 빌드 자가검사 3종 통과(⑴ 데이터 왕복 동일·케이스 **136개** ⑵ `demo_steady.json` fetch 잔존 0 ⑶ 「가정값 기반 — 실측 아님」 유지) · 산출물 **596,859 B (582.9 KB)** · `node --check` 로 산출물 JS 문법 통과(브라우저로 열어 보지는 않았다) · `demo/pfd.html`·`demo/demo_steady.json` **git 무변경** · `git ls-files --eol demo/start.sh` → `i/lf w/lf`(정규화 불필요) · pytest 1,207건·ruff·mypy 통과 | 메일로 보낼 것은 `dist/cdu-demo.html` 하나다(`dist/` 는 커밋되지 않으므로 보낼 때마다 다시 뽑는다) · 꾸러미가 둘이 된 것(zip + 단일 HTML)을 정리할지는 사람이 정한다 |
| 7.14 | **소개자료에 들어갈 수치를 지금 코드로 다시 확인해 한 파일에 모았다** — `docs/intro-figures.md`(25항목 대조표 · 열: 항목·값·단위·출처·재산출 여부·outline 값과의 차이·**케이스 조건**). `docs/intro-outline.md` §4·§5 의 수치를 재산출해 대조했고 **outline 을 고치지 않았다**(눈에 띈 넷은 목록으로만). 모델·가정치·데이터셋·`demo/` 는 읽기만 했다 | **게이트 없음** · feasibility 판정 없음 · **새 가정치 0개** · 재산출 20항목 전부 outline 표기와 **일치** · 다만 **PROCEED 기록과 갈린 둘**: cp=환수 잔차 0.31524→**0.3152212**(멈춤 조건 · 미해결 **#54**) · 저항 +50% 총유량 −0.3704~−0.1638→**−0.3710~−0.1642** · `dist/` 는 이미 `.gitignore` 에 있고 추적 파일 0(`git ls-files dist/` 빈 결과) · pytest 1,207건·ruff·mypy 통과 · `demo/`·`src/`·데이터셋 **git 무변경** | **#54 를 사람이 판정한다** — 게이트 재판정은 하지 않았다 · 소개자료 HTML 을 만들 때 이 대조표의 값을 쓴다(케이스 조건을 함께 적는다) |
| 7.15 | **소개자료 HTML 의 골격을 세웠다** — `intro/intro.html`(**14 화면** · scroll-snap · 제목과 자리표시만 · 본문은 비어 있다) · `intro/make_intro_html.py`(시연 화면을 iframe `srcdoc` 으로 품어 `dist/cdu-intro.html` 하나를 낸다 · 검사 4종). 색 여섯은 시연 화면 값을 **가져오기만** 했고 CSS 를 복사하지 않았다. `demo/` 세 파일은 **읽기 전용**으로 다뤘다 | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개**(자료에 나갈 수치는 전부 `docs/intro-figures.md` 항목 번호로 자리표시만 두었다) · 외부 라이브러리 0개(표준 라이브러리만) · 빌드 검사 4종 통과(⑴ 품은 시연 화면이 `dist/cdu-demo.html` 과 **476,063 자 동일** ⑵ 금지어 3종 **0회**(iframe 안 제외) ⑶ 화면 14 = 표기 14 ⑷ 14 ∈ [12,15]) · 검사가 헛돌지 않음을 **일부러 어긋뜨려** 확인(셋 다 걸림) · 산출물 **702,743 B (686.3 KB)** · `demo/`·`src/`·데이터셋·`docs/intro-figures.md` **git 무변경** · pytest 1,207건·ruff·mypy 통과 | **브라우저로 열어 보지 않았다 — 사람이 확인한다.** 골격을 보고 화면 구성·조형을 고칠지 정한 뒤 다음 판이 본문과 수치를 채운다(표기 규칙 넷은 `intro.html` 머리 주석에 못박아 두었다) |
| 7.16 | **소개자료 본문을 채웠다** — `intro/intro.html` 의 **자리표시 48개**를 문구와 수치로 갈았다. 자료에 나간 수치는 **전부 `docs/intro-figures.md`** 에서 **자릿수 그대로** 왔고(반올림 0회), 각 수치 옆에 **케이스 조건**을 달았다. Aspen 비교 화면을 **부록에서 본문으로** 올렸다(5장 뒤 · 6장 앞 · 「부록」 표시 제거 · id 는 `s13` 그대로 두어 문서 순서와 id 가 어긋난다). s09 의 편차 막대를 **자료 안에서 SVG 로 직접 그렸다**(캡처 아님 · 두 막대 같은 축척 · 눈금은 px 가 아니라 m³/h) | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개**(figures 표 밖 숫자 0개) · 빌드 검사 4종 통과(⑴ 품은 시연 화면 **476,063 자 동일** ⑵ 금지어 3종 **0회** ⑶ 화면 14 = 표기 14 ⑷ 14 ∈ [12,15]) · 어휘 재검사(C5) 「감지·검출·조기·예측·경보·탐지」 **0회** · 0-based 랙 번호 **0회** · 산출물 **709,061 B (692.4 KB)** · `demo/`·`src/`·`docs/`·데이터셋 **git 무변경** · pytest 1,207건·ruff·mypy 통과 | **브라우저로 열어 보지 않았다 — 사람이 읽고 판정한다** · figures 표에 없어 **빼둔 문구 하나**: 「냉각수 통상 설계유속 2~3 m/s」(outline §5) — 넣으려면 figures 항목으로 먼저 올려야 한다 |
| 7.17 | **소개자료를 읽히게 다시 썼다** — 사람이 실물을 읽고 낸 판정(「단촐하다 · 무엇을 말하려는지 안 보인다 · 어렵다」) 넷을 고쳤다. **scroll-snap 을 걷어 자유 스크롤**로 바꾸고(한 화면 다섯 줄 제약 소멸), **골격 어휘 41개**(「읽는 법 ①」·「그래서 무엇을 했나」·「한 줄 결론」 따위)를 지우거나 내용으로 갈았고, **절 제목 13개를 전부 문장으로** 다시 썼다. 계층을 세웠다 — 절마다 큰 문장 하나(`.key`)를 두고 나머지는 작게, **흰 상자는 표지·각주·6장 3단계에만**, 절 사이는 테두리 대신 여백 108 px, 본문 글줄 38em. 수치를 **두 겹**으로 적는다(본문에 반올림값 + 이름 · 그 밑에 figures 전체 자릿수). 식과 코드 문자열은 지우지 않고 작게 내렸다. **내용을 새로 만들지 않았다 — 있는 것을 다시 쓰고 다시 배치했다** | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개**(figures 표 밖 숫자 0개 · 반올림은 이 파일에서만 하고 figures 표는 무수정) · 빌드 검사 4종 통과(⑴ 품은 시연 화면 **476,063 자 동일** ⑵ 금지어 3종 **0회** ⑶ 화면 14 = 표기 14 ⑷ 14 ∈ [12,15]) · 어휘 재검사 「감지·검출·조기·예측·경보·탐지」 **0회** · 0-based 랙 번호 **0회** · 외부 CDN·폰트·이모지 **0** · 산출물 **715,140 B (698.4 KB)** · `demo/`·`src/`·`docs/`·데이터셋·`intro/make_intro_html.py` **git 무변경**(바뀐 파일은 `intro/intro.html` 하나) · pytest 1,207건·ruff·mypy 통과 | **헤드리스 크롬으로 실제로 렌더해 보고 판정했다**(표지·s01·s02·s03·s04·s05·s07·s09·s10·s11·s12) — 다만 **사람의 브라우저에서 등장 효과가 실제로 도는지는 아직 못 봤다**(아래 세션 로그 「검사 4종과 렌더 확인」) · 빌드 검사 ⑶⑷ 의 **뜻이 달라졌다 — 고치지 않고 보고만 한다**(아래) |
| 7.18 | **소개자료를 구성부터 다시 짰다 — 14 절 → 11 절.** 사람이 실물을 보고 낸 판정(「디자인·구성·내용 무엇 하나 마음에 들지 않는다 · 14 절 중 5 절만 읽힌다 · 원인은 문장이 아니라 구성과 조형」)에 따라 앞 판의 골격을 버리고 새로 세웠다. ⓐ 뼈대를 「우리가 한 일」에서 「무엇이 새롭고 · 왜 막혀 있었고 · 무엇이 열렸고 · 그래서 무엇을 할 수 있나」로 바꾸고 **「어떻게 계산하나」 다섯 절을 한 절로 접었다** · ⓑ **새 절 하나**(상용 시뮬레이터 배경 → 물성을 이제 불러온다 → 그래서 직접 만든다) · ⓒ **5 절 3층 다이어그램을 SVG 로 새로 그렸다**(장치 다섯 → 코드 다섯 → 시스템 하나 · 밑층에 물성 조회 하나를 다섯이 모두 가리킨다 · 등축 도형은 `demo/pfd.html` 의 투영·면 규칙을 **읽어서** 같은 방식으로 다시 그렸다) · ⓓ 절마다 **2 단 배치**(왼쪽 주장 · 오른쪽 근거) · 글 기둥 1520 px · 글자 확대(본문 15→17.5 px) · ⓔ **시연 iframe 을 1600 px 원본 크기 그대로 두고 화면 폭에 맞춰 축소**한다 · ⓕ **수치를 크게 줄였다**(전체 자릿수 병기 · 코드 문자열 · 논증을 지지 않는 수치 전부 제거) · ⓖ 빌드 검사 ⑷(절 수 12~15)를 **없앴다** | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개**(figures 표 밖 숫자 0개) · 자료에 남은 수치 **9개**(#7·#8 · #15 · #16 · #17 · #18 · #19 · #20·#21 · #24·#25) · 빌드 검사 **3종** 통과(⑴ 품은 시연 화면 **476,063 자 동일** ⑵ 금지어 3종 **0회** ⑶ 절 11 = 표기 11) · 엔지니어 어휘 13종(양정·대향류·NTU·잔차·시간축 …) **0회** · 외부 CDN·폰트·이모지 **0** · 새 색 계열 **0**(시연 여섯 + `--faced` + `--ink` 투명도 단계) · 산출물 **728,559 B (711.5 KB)** · `demo/`·`src/`·`docs/`·데이터셋 **git 무변경** · pytest·ruff·mypy 통과 | **헤드리스 크롬으로 11 절 중 10 절을 렌더해 보고 판정했다**(표지·02·03·04·05·06·07·08·09·10 — 렌더로 잡아 고친 것 둘: 5 절 층 이름표가 화살표에 깔린 것 · 7 절 막대 이름표가 viewBox 밖으로 잘린 것) · **남은 것 하나 — 시연 조작 막대의 축 눈금 「50 %」가 두 줄로 접힌다.** 폭 탓이 아니다(원본 시연을 2000 px 로 띄워도 같다) — `demo/` 는 읽기 전용이라 손대지 않았다(아래 세션 로그) |
| 7.19 | **소개자료를 사람 지적 열넷대로 고쳤다 — 11 절 → 10 절.** 세션 7.18 이 세운 구성을 **다시 짜지 않고 그 위에서 고쳤다.** ⓐ 표지 — 제목(「AI 데이터센터 냉각 시스템 시뮬레이션」)을 가장 큰 글자로 올리고 부제를 그보다 작게 내렸다(앞 판은 부제가 제목보다 컸다) · 「짓기 전에」를 뺐다(지은 뒤에도 돌려볼 수 있어 말이 안 맞는다) · 「지금 무엇이 되어 있고…」 줄 삭제 · ⓑ **「가정값 기반 — 실측 아님」을 표지에만** 두고 나머지 9 절에서 뺐다 · ⓒ **어휘 6 종 교체**(펌프 압력 · 밸브 개도율 · 배관 차압 · 열 교환 · 랙 발열 · 냉매) · ⓓ **04 절 다이어그램 다섯 상자에 대표 계산식을 넣었다**(펌프·밸브·배관·열교환기·랙 — 전부 정본 5 장·5-1) · ⓔ 04 절 마무리에 **되풀이해 푼다**를 말로 적었다(함수 이름 없이) · ⓕ 06 절 결론을 「계기가 곧 비용」에서 **「전체만 봐서는 보이지 않는 변화가 있고, 그 차이를 미리 볼 수 있다」**로 · ⓖ 07 절 데이터 문구·마무리 축소 · ⓗ 08 절 — 무엇의 열인지 밝히고 **99.9 %/99.995 % 로 뒤집어 썼으며** 「검사가 헛돌지 않는다」 문단을 삭제하고 자동 시험을 **검증 장치**로 고쳐 썼다 · ⓘ **09(아직 못 하는 일) + 10(다음)을 표 하나로 합쳤다** — 칸에는 「할 수 있는 일」만 적고 **시운전·운영의 왼쪽 칸이 비는 것**으로 못 하는 일을 보인다 · ⓙ **어두운 바탕** — 색을 `:root` 한 벌로 모으고 SVG 도형도 그 변수를 쓰게 했으며 `@media print` 에 흰 바탕 한 벌을 두었다 · ⓚ 빌드 검사 ⑶ 을 「절 수 = 표기 수」에서 **「최소 한 번」**으로 바꿨다 | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개**(figures 표 밖 숫자 0개 · 99.995 %/99.9 % 는 #7·#8 의 0.005 %/합격선 0.1 % 를 「맞는 몫」으로 뒤집어 적은 것이고 값이 새로 생기지 않았다) · 빌드 검사 3종 통과(⑴ 품은 시연 화면 **476,063 자 동일** ⑵ 금지어 3종 **0회** ⑶ 표기 **2회**(≥1) — 보이는 것은 표지 하나이고 나머지 하나는 머리 주석이다 · 절 **10 개**) · 어휘 교체 확인: 옛말 6 종 **0회** · 「냉각액」 **0회** · 외부 CDN·폰트·이모지 **0** · 하드코딩 색 **0**(전부 `var(--*)` · 색값은 `:root`·`@media print` 두 블록에만) · 산출물 **729,780 B (712.7 KB)** · `demo/`·`src/`·`docs/`·데이터셋 **git 무변경** · pytest **1,207건**·ruff·mypy 통과 | **헤드리스 크롬으로 어두운 한 벌 7 절 + 인쇄 한 벌 3 절을 렌더해 보고 판정했다**(C14 넷 전부 확인) · 렌더로 잡아 고친 것 하나: 인쇄에서 표의 **빈칸에도 테두리**가 둘려 「비어 있다」가 덜 보였다 → `.can td:not(.none)` 으로 좁혔다 · **C9 ⑴ 은 지시와 다르게 적었다** — 지시는 「랙에서 나온 열과 **열교환기가 빼낸 열**」이었으나 게이트가 실제로 재는 것은 랙 발열 대 **냉매가 실어 나른 열**(CoolProp 엔탈피 경로)이고 열교환기 쪽은 항등적으로 0 인 다른 검사다(`hx_duty_identity_residual_percent` · 게이트가 아니다). 지시의 뜻(무엇의 열인지 밝힌다)은 지키고 대상만 맞게 적었다 |
| 7.20 | **소개자료의 말을 낮췄다 — 구성(10 절)은 그대로.** 사람이 「09 절 표와 06·07 절이 **실제보다 크게** 적혀 있다 · 지금 못 하는 일을 할 수 있는 것처럼 썼다 · 이 자료의 목적은 설득이 아니라 **무엇이 되어 있고 무엇이 안 되어 있는지를 알리고 의견을 받는 것**」으로 판정했다. ⓐ **09 절 표를 3 단 4 줄로 다시 짰다** — 열: 단계 · **필요한 것** · 그러면 할 수 있는 일 / 줄: 지금까지 한 것(필요한 것 「—」) · 다음 · 설계데이터가 오면 · 실측이 오면. **가운데 칸이 요점**이다(오른쪽이 왜 아직 안 되는지가 거기서 보인다) · 첫 줄의 이름은 「할 수 있는 일」이 아니라 **「한 것」** · 옛 표의 빈칸(`.none`) 처리 삭제 · ⓑ 표 앞뒤에 짧은 문단 둘을 붙였다(앞: 여기까지가 무엇인지 · 뒤: **「무엇을 더 해야 하는지에 대한 의견을 기다립니다」**) · ⓒ **06 절** — 「그 차이를 **미리 볼 수 있습니다**」 → 「한 자리를 이렇게 비교해 봤습니다 … 이만큼 다르게 나왔습니다」로. 「못 잡습니다」도 뺐다 · ⓓ **07 절 제목을 「설계를 어떻게 돕는가」 → 「돌려 보니 이런 것들이 나왔습니다」**로 바꾸고 세 항목을 관측 시제로 고쳤다. **「랙을 늘렸을 때 가장 먼 랙」 항목은 절에서 뺐다** — 랙 8 개가 가정치 고정이라 **지금 못 한다**. 09 절 표의 「다음」 줄로 보냈다 · ⓔ **숫자 셋 삭제**(「정상 320 행 · 이상 960 행」 · 「검사 1,207 건」) — 한 번에 나오는 것인지 누적인지를 자료가 말하지 않아 뜻이 서지 않는다. **0.005 %/99.995 % 와 136 케이스는 남겼다** · ⓕ **「물」 → 「냉매」 8 자리** · ⓖ 말 다듬기 5 종(「몇 개 달지」→「몇 개 설치할지」 등) | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개**(수치를 **빼기만** 했다 · figures 표 밖 숫자 0개) · 빌드 검사 3종 통과(⑴ 품은 시연 화면 **476,063 자 동일** ⑵ 금지어 3종 **0회** ⑶ 표기 **2회**(≥1) · 절 **10 개**) · 산출물 **732,284 B (715.1 KB)** · `demo/`·`src/`·`docs/`·`intro/make_intro_html.py`·데이터셋 **git 무변경** · pytest **1,207건**·ruff·mypy 통과 | **헤드리스 크롬으로 06·07·08·09 절을 1440 px·900 px 두 폭으로 렌더해 판정했다**(검사 ⑴⑵⑶ 전부 확인) · **렌더로 잡아 고친 것 하나**: `table-layout:fixed` 는 **첫 줄(thead)의 너비만** 본다 — 너비를 `tbody th`·`td` 에 줬더니 무시돼 단계 칸이 15 % 대신 3 분의 1 을 먹었다 → **`<colgroup>` 으로 옮겼다** · 인쇄 한 벌도 봤다(스크래치패드 사본에서 `@media print` → `@media all` 로 끌어내 화면 렌더) — 빈칸이 없어졌으므로 `.can td:not(.none)` 을 **`.can td`** 로 되돌렸고, 흰 바탕에 네 줄 전부 테두리가 선다 · **확인 못 한 것 둘**: ⑴ 실제 종이/PDF 렌더(이 환경에 PDF 를 그려 볼 도구가 없다 — `pdftoppm` 미설치) ⑵ 사람의 브라우저에서 절이 하나씩 떠오르는 것(헤드리스 한계 · 앞 판들과 같다) |
| 7.21 | **09 절을 표에서 「전폭 한 줄 + 가로 박스 셋」으로 바꿨다 — 글은 무수정.** 사람이 「직전 판이 09 절을 4 행 표로 만들었는데 원한 형식은 그게 아니다 · 단계가 행이 아니라 **열(가로 박스)**이고 박스 안에 「필요한 것 / 그러면」이 세로로 쌓이는 모양」으로 판정했다. 형식의 본은 **세션 7.17 판 s12**(`ul.slots.cols`) — git 이력(`54e2802`)에서 그 절의 마크업과 `.cols` CSS 를 찾아 **형식만** 가져왔다. ⓐ `<table class="can">` + `<colgroup>` 을 걷어내고 **`div.done`(전폭 한 줄) + `ul.steps`(가로 박스 셋)** 으로 다시 짰다 — 박스마다 세로 세 층(`.step-name` / `.need` 「필요한 것 —」 / 「그러면 —」) · ⓑ **맨 위 「지금까지 한 것」에는 「필요한 것」 층을 두지 않았다**(이미 한 것이라 필요한 것이 없다 — 직전 판의 빈칸 「—」 도 함께 없앴다) · ⓒ 「필요한 것」이 이 절의 요점이라 **강조 색(`--warn`)으로 세웠다**(흐리게 묻지 않는다) · ⓓ 좁은 화면(≤1180 px)은 박스 셋을 세로로 쌓고, 그러면 전폭 한 줄과 한 줄로 보이므로 `.steps` 위 여백을 26 px 로 벌렸다 · ⓔ 앞 문단의 「아래 **표**는」 → 「**아래는**」 한 낱말만 고쳤다 · ⓕ `@media print` 의 테두리 선택자를 `.card,.can td` → `.card,.done,.steps > li` 로 옮겼다. **절 제목·앞뒤 문단·글 전부 직전 판 그대로다 — 배치만 바꿨다** | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개 · 새 글 0줄**(직전 판의 표 칸 넷을 그대로 옮겨 담았다) · 빌드 검사 3종 통과(⑴ 품은 시연 화면 **476,063 자 동일** ⑵ 금지어 3종 **0회** ⑶ 표기 **2회**(≥1) · 절 **10 개**) · 산출물 **732,736 B (715.6 KB)** · `demo/`·`src/`·`docs/`·`intro/make_intro_html.py`·데이터셋 **git 무변경**(바뀐 파일은 `intro/intro.html` 하나) · pytest **1,207건**·ruff·mypy 통과 | **헤드리스 크롬으로 09 절을 1440 px·900 px 두 폭 + 인쇄 한 벌로 렌더해 판정했다**(C4 넷 중 셋 통과 · 앞 판의 「절 앞을 접은 사본」 방식 그대로) — 박스 셋이 뭉개지지 않고(1440 px 에서 셋이 같은 높이로 서고 가로 넘침 0), 전폭 한 줄과 박스 셋의 층이 구분되며, 어두운 바탕에서 세 층이 색으로 갈린다 · **렌더로 잡아 고친 것 하나**: 좁은 화면에서 박스 셋을 세로로 쌓으니 전폭 한 줄이 넷째 박스처럼 보였다 → `.steps` 위 여백 26 px · **인쇄 한 벌에서 걸리는 것 하나(고치지 않았다)**: 인쇄 `--warn:#d99425` 이 흰 바탕에서 본문보다 옅어 **「필요한 것」이 화면만큼 서지 않는다.** 색 한 벌은 09 절 밖이라 손대지 않고 보고만 한다 · **확인 못 한 것 둘**: ⑴ 실제 종이/PDF 렌더(`pdftoppm` 미설치 — 앞 판과 같다) ⑵ 사람의 브라우저에서 절이 하나씩 떠오르는 것(헤드리스 한계) |
| 7.22 | **오늘(7.13~7.21)을 닫는 기록 판 — 코드·소개자료·시연 화면을 하나도 고치지 않았다.** 바꾼 파일은 `PROCEED.md` 하나. ⓐ 「현재 상태」 표에 오늘 아홉 판(7.13~7.21)이 **빠짐없이 올라와 있음을 확인했다** — 채워 넣은 행 0 · ⓑ **미해결 셋을 등재했다** — **#55**(인쇄 한 벌의 `--warn` 이 흰 바탕에서 옅다) · **#56**(`docs/intro-outline.md` 가 지금 자료와 어긋난 자리 열둘 — 세션 7.15·7.18·7.19 의 보고를 **한 항목으로 묶었다**) · **#57**(긴 시험을 백그라운드로 돌리고 완료를 폴링해 왕복이 헛돌았다) · ⓒ 오늘 정해진 것(자료의 성격 = 알리는 자료 · 독자 = 사내 경영층 · 「한 것」만 적는다 · 표기는 표지 한 번 · 어두운 바탕 · 시연 화면은 읽기 전용 · 용어 일곱)을 세션 로그에 모았다 · ⓓ **내일 첫 작업은 후보로만 적었다 — 고르지 않았다**(사람이 고른다) · ⓔ 추적 안 된 `start-cdu.txt`(이 PC 전용 실행 메모 · 절대경로 포함)는 **사람 판정으로 그대로 두었다** — 커밋하지 않았고 `.gitignore` 에도 넣지 않았다 | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개 · 코드 무수정** · pytest **1,207건 전부 통과**(482.01 s · **앞에서 한 번** 돌렸다) · ruff `All checks passed!` · mypy `no issues found in 29 source files` · `intro/`·`demo/`·`docs/`·`src/`·`assumptions`·데이터셋 **git 무변경** · 정본 문서 4종 무수정 | **사람이 내일 첫 작업을 고른다** — 후보 셋은 세션 7.22 로그 「내일 첫 작업 후보」에 있다 |
| 7.23 | **`collaboration.md` 에 「웹 대화창이 실제로 저지른 것」 절을 만들었다 — 사람의 지시로 고친 정본 문서 하나.** 기존 「이 프로젝트에서 특히 조심할 결함 유형」 절은 다섯 항목이 전부 **코드 쪽** 결함이고 웹 대화창 쪽은 한 줄도 없었다 — 그 자리를 채웠다. 사람이 1~7일차 대화창 각각에 「너와 나 사이 소통에서 어긋난 자리」를 정리하게 해 **여섯 벌**을 모았고, 그중 **여러 날에 걸쳐 반복된 것 다섯**만 추려 온 것을 그대로 올렸다(하루만 나온 것은 넣지 않았다). 다섯: ① 볼 수 있는 것은 본다(6일 중 5일) · ② 프롬프트는 바깥 코드 블록 하나로만(전달 사고 1회) · ③ 「거의」·「영향 없음」을 쓰기 전에 깨지는 경우를 하나 찾는다(4일) · ④ 정할 수 있으면 권고로 낸다(4일) · ⑤ 결정을 청할 때는 「하면/안 하면」을 함께 적는다(2일). 각 줄에 **실례를 한 줄씩** 붙였다(세션 7.20 · 7.6 무렵 · 5 무렵 · 5.7 무렵 · 5.6-B 무렵 — **지어내지 않고 사람이 준 것만**). 자리는 「프롬프트를 쓸 때 (웹 → Code)」 바로 아래 | **게이트 없음** · feasibility 판정 없음 · **새 숫자 0개 · 코드 무수정** · pytest **1,207건 전부 통과**(569.77 s · **앞에서 한 번** 돌렸다 — #57) · ruff `All checks passed!` · mypy `no issues found in 29 source files` · `src/`·시험·`intro/`·`demo/`·`docs/`·`assumptions`·데이터셋 **git 무변경**(바뀐 파일은 `collaboration.md`·`PROCEED.md` 둘) | **`collaboration.md` 를 고쳤음 — 프로젝트 지식 재업로드 필요** · 보고한 것 둘(고치지 않았다): ⑴ 새 ③ 이 판단 원칙 **⑥**(가설은 반례와 함께 든다)과 뜻이 겹친다 ⑵ 문서 앞머리 「이 파일은 *누가 무엇을 맡고, 무엇을 믿을 만한 것으로 치는가* 만 적는다」의 범위를 새 절이 넘는다 — 앞머리를 고칠지는 **사람이 정한다** |
| 7.24 | **`daily_brief.py` 에 `--web` 을 더했다 — 웹 대화창에 붙이는 축약 출력.** 셋만 낸다: ⓐ 마지막 판 한 줄(세션 로그의 마지막 `### ` 제목) · ⓑ **열린 미해결 전부**(번호·제목만, 건수로 자르지 않는다) · ⓒ 「현재 상태」 마지막 행의 「다음 세션 첫 작업」 **첫 문장만**. 출처는 `PROCEED.md` 하나이고 목록을 옮겨 적은 새 파일을 만들지 않았다. **글자 수 1,472자** — 같은 시점 기본 출력 25,030자 대비 **94.1% 감소**. **[후속 정정 · 같은 날]** ⓒ 의 절단 경계를 첫 ` — ` 에서 **첫 ` · `** 로 고쳤다 — 첫 ` — ` 로는 7.23 행의 「프로젝트 지식 재업로드 필요」가 떨어져 나갔고, 그것은 웹 대화창이 놓치면 정본이 갈리는 항목이다. **정정 후 글자 수 1,627자**(상한 2,200자 안). 물리 수치를 싣지 않으므로 「가정값 기반 — 실측 아님」 표기 대상이 아니다. **기본·`--full` 출력은 한 글자도 바뀌지 않았다**(HEAD 판과 문자열 비교로 확인) | **게이트 없음** (도구 판이다. 물리 모델·가정치·데이터셋을 건드리지 않았다) | ⑴ 「샘」(질량손실)을 정식 시나리오로 올릴 때의 비용 계측 C1~C6 (세션 7.25 프롬프트 그대로) ⑵ 7.24 D4-3 을 미해결로 신설한다 — 전체 pytest 가 569.77 s 로 도구 호출 상한 10분에 여유 5% 뿐이라 배치 실행이 붙는 판에서 다시 걸린다 (번호는 #58 자리) |
| 7.25 | **「샘」(질량손실)을 정식 시나리오로 올릴 때의 비용을 쟀다 — 재는 판이다. 정의를 바꾸지 않았다.** ⓐ **C1 「샘」 크기 수준값의 출처** — **독립한 수준값이 없다.** 세션 5.6·5.7-D 는 K 근사 **+50 %** 가 만든 누출랙 통과유량 **감소량**을 스윕 상한으로 삼고(`leak_massloss.leak_flow_bound_Lps`) 거기에 분수 `(0, 0.25, 0.5, 0.75, 1.0)`(`SWEEP_FRACTIONS`)를 곱해 쓴다 — 5장 밖 숫자가 아니라 **5장 3수준에서 유도한 값**이다. 5장 표에도 5-1 에도 「샘」 크기 항목은 **없다** · ⓑ **C2 지금 데이터셋 재계측**(오늘 코드 · 스크래치패드에 써서 `results/` 는 건드리지 않았다) — 시나리오 **1,792** · 행 **310,016** · 열 **52** · 실패 행 **0** · **1,515.16 s(25.3 분)** · **377,295,246 B(359.8 MiB)**. 저장소의 `results/cdu_dataset.csv` 는 **48열 · 304,608,266 B (2026-08-28)** 로 **오늘 코드의 산출물이 아니다**(재생성하지 않았다) · ⓒ **C3 「샘」 케이스당 소요**(표본을 64 케이스에 고루 흩어 뽑았다 · 데이터셋 파일을 만들지 않았다) — 정상상태 **0.0081 s/해**(32회) · 전이 **0.3139 s/적분**(8회 · 시각 표본 201) · 다중 CDU 정상상태 **0.0481 s/해**(8회). **수렴 실패 0** · ⓓ **C4 세 규모 산출** — 아래 표 · ⓔ **C5** 「샘」이 배치 실행에 그대로 얹히지 않는다 — 손볼 곳 **아홉**을 목록으로만 보고했다(고치지 않았다) · ⓕ `.gitignore` 에 `start-cdu.txt` 를 넣었다(파일은 지우지 않았다) · ⓖ 미해결 **#58** 신설 | **게이트 없음** (재는 판이다. 물리 결과를 판정하지 않는다) · feasibility 판정 없음 · **새 가정치 0개 · 새 수준값 0개 · 모델 코드 무수정** · pytest **1,207건 전부 통과**(**반으로 나눠** 46.22 s + 476.24 s = 522.46 s — #57·#58) · ruff `All checks passed!` · mypy `no issues found in 29 source files` · `src/`·시험·`assumptions`·`intro/`·`demo/`·`docs/`·정본 문서 4종·`results/` **git 무변경**(바뀐 파일은 `.gitignore`·`PROCEED.md` 둘) | **사람이 규모를 정한다** — 규모 A/B/C 의 시간·크기는 아래 세션 로그 C4 표에 있고, 그보다 먼저 정해야 하는 것은 **구조 자유도 셋**(g · 펌프가 보는 유량 · 2차측 배분 읽기)을 전수로 돌릴지 값으로 고정할지다(고정하면 5-1 에 새 값이 들어간다 — 절대 규칙 1). C5 아홉 중 **⑴ import 금지 경계**와 **⑷ 다중 CDU 전이 부재**는 사람의 승인·새 코드가 있어야 열린다 |
| 7.26 | **이상 상태를 「막힘」·「샘」 둘로 세웠다 — 정의 판이다. 코드를 한 줄도 고치지 않았다.** ⓐ **C1 `CLAUDE.md` 절대 규칙 8 개정** — 「누출 시나리오는 배관저항(K값) 변화로 근사한다」를 「**이상 상태는 「막힘」과 「샘」 둘이다**」로 바꿨다. 막힘 = 배관저항(K값) 증가(5장 +5/+20/+50%) · 샘 = 질량손실. 막힘을 **샘의 대용(proxy)이 아니라 독립된 이상 상태**로 못박고, 셋째 갈래 추가·정의 변경은 사람 확인, **총유량 부호가 갈린다(#36)** 는 것과 「한쪽 기구의 신호를 다른 쪽 방향으로 읽지 않는다」를 규칙 본문에 넣었다 · ⓑ **C2 프로젝트정리 5-1 에 「「샘」(질량손실) 크기 수준」 신설** — **역산(빈칸 처리 순서 1번)** 으로 기록했다: 상한 = 막힘 +50% 케이스의 누출랙 통과유량 **감소량** · 스윕 `(0, 0.25, 0.5, 0.75, 1.0) × 상한` → 비영 4수준. **새 숫자 0개.** 한계 넷(부호 반대 · 일부 배치에서 수력 흔적 전혀 없음 · 랙에 국소화되지 않음 · balance 에 누출 엔탈피 항 필요)을 함께 적었다 · ⓒ **C3 「누출 주입 지점」 문언** — 「샘」에 **부분적으로만 걸린다**. 「랙 1개에 주입」은 샘에도 성립하지만 근거인 **랙 대칭 논리는 샘에서 무의미**하다(샘은 랙에 국소화되지 않는다 — 랙 간 비대칭 2.220e-16 L/s). **보고만 했다** · ⓓ **C4 구조 자유도 셋(g · 펌프가 보는 유량 · 2차측 배분 읽기)의 5-1 기재 초안** — 값으로 고정하지 않고 **전수(6 배치 · 다중 CDU 12)** 로 돌린다는 형태다. **초안만 냈고 5-1 에 넣지 않았다**(지시 문언이 「초안을 낸다」다) · ⓔ **C5 「누출」 낱말의 어긋난 자리 — 문서 10곳 · 코드 11곳.** 고치지 않고 목록으로 올렸다(미해결 #59) | **게이트 없음** (문서 판이다. 물리 모델·가정치 값·데이터셋을 건드리지 않았다). pytest **1,207건 전부 통과**(반으로 나눠 — 392 + 815) · ruff · mypy 통과 | **「누출」 낱말 정리(#59)를 사람이 정한다** — 5장 표의 항목명 「누출 시나리오」가 이제 「막힘」만 가리키는데 낱말은 그대로다. 5장 본문은 이 판에서 고치지 않았다. 그 다음이 C4 초안을 5-1 에 넣을지, 그리고 규모 B(정상상태만 · 335 s · 18.4 MB)를 실제로 생성하는 판이다 — 생성 전에 7.25 C5 의 ⑵⑶⑹⑺(스키마·축 모양·표기 분기)이 먼저 열려야 한다 |
| 7.27 | **「누출」 낱말을 규약으로 세우고 정본 문서 넷을 그 규약과 맞췄다 — 문서 판이다. 코드를 한 줄도 고치지 않았다.** 사람이 정한 (가)안: **「누출」은 상위 개념(실제 이상 사건) 전용이고, 모델 안의 기구는 언제나 「막힘」·「샘」으로 적는다.** ⓐ **C1 5-1 「「샘」 구조 자유도 셋」 신설** — 세션 7.26 C4 초안을 넣었다. g `{0.0, 0.5, 1.0}` × `pump_sees_supply_flow` 둘 = **6 배치**, 다중 CDU 는 `share_uses_return_flow` 까지 **12 배치**. **값을 고르지 않고 전수로 돈다** — 고정하면 5장 밖 숫자가 들어온다(절대 규칙 1). **새 숫자 0개** · ⓑ **C2 「누출 주입 지점」을 기구별로 둘로 갈랐다** — 「「막힘」 주입 지점」(원 항목의 값·근거를 그대로 유지 · 랙7 근거 포함)과 **「「샘」 주입 지점」**(신설). 샘 쪽에 **국소화되지 않는다**(랙 간 비대칭 2.220e-16 L/s)는 것과 **랙7 근거가 걸리지 않는다**(랙7 은 K 배수 1.8 에서 나온 것인데 샘은 K 를 건드리지 않는다)는 것을 적었다 · ⓒ **C3 문서 아홉 자리를 고쳤다** — 정리 4장 3번 · 5-1 위 둘 · `CLAUDE.md` 목적문·규칙 6·세션 4 게이트·세션 5.5 게이트·「하지 말 것」 · `collaboration.md` 99행. **5장 표 항목명 「누출 시나리오」는 고치지 않았다 — 중단 조건대로 보고하고 멈췄다** · ⓓ **C4 `project-overview.md` 에 「그 뒤 무엇이 달라졌는가」 절 신설** — **판정표는 한 글자도 고치지 않았다**(2026-08-31 사람 판정의 기록이다). 판정 7 이 가리킨 「교체 · 파일럿 뒤」와 실제로 일어난 「둘로 세움 · 지금」의 차이, 「절대 규칙 8 예외」가 **지워진 것이 아니라 규칙 본문으로 올라간 것**임, 낱말 규약, #59 코드 11곳이 열린 채임을 적고 「지금 어디인가」에 7.0~7.23·7.24~7.27 을 이었다 · ⓔ **낱말 규약의 예외를 한 줄 남겼다** — 소개자료(`intro/`)는 빌드가 「누출」을 막고 「랙 배관 저항 증가」로 쓰게 하므로(`intro/make_intro_html.py:44`) 정본 문서 넷과 `intro/` 는 이 낱말에서 **서로 다른 규칙**을 갖는다. `intro/` 는 고치지 않았다 · ⓕ **C6 미해결 #59 갱신** — 문서 10곳 중 **아홉이 닫혔고**(5장 표 항목명 하나가 사람 대기) **코드 11곳은 열린 채**다 | **게이트 없음** (문서 판이다. 물리 모델·가정치 값·데이터셋·스키마·`demo/`·`intro/`·`dist/`·`daily_brief.py` 를 건드리지 않았다). pytest **1,207건 전부 통과**(#58 대로 반으로 나눠 · 백그라운드 아님) · ruff · mypy 통과 | **5장 표 항목명 「누출 시나리오」를 사람이 정한다** — 제안은 「**이상 시나리오 — 「막힘」**」이고 값(+5/+20/+50%)은 건드리지 않는다(세션 7.27 D2). 그 다음이 **#59 코드 11곳**(주석이 옛 규칙 8 을 인용한다 — 코드 판)이고, 규모 B 생성은 그 뒤다 |
> **※ 1-B 게이트가 실제로 검증한 것 (과대해석 금지)**
> 게이트가 쓰는 잔차는 *CoolProp 엔탈피 경로로 다시 계산한 1차측 흡열량* 과
> *5장 랙 발열량* 의 차다. 이것이 재는 것은 **모델이 쓴 상수 cp 선형화가 실제
> 엔탈피 변화와 어긋나지 않는가** 하나다.
> 반면 *HX ε-NTU duty 와 랙 발열량의 일치* 는 모델 구조상 **항등적으로 0**이다
> (실측 1.8e-14 %). 그쪽은 아무것도 증명하지 않으며 게이트에 쓰지 않았다.
> 통과의 뜻은 "무너지지 않는다"까지이고 **"정확하다"가 아니다** — 실측이 없다.

> **※2 세션 2 게이트가 실제로 판정한 것 (과대해석 금지)**
> 판정한 feasibility 기준은 **T_return 방향성 하나**다.
> · **수렴시간은 판정하지 않았다.** M(계통 보유수량)이 배관 보유량만 담고 있어
>   열교환기·CDU 내부·콜드플레이트 보유량이 빠져 있다(프로젝트정리 5-1 한계).
>   실제보다 M 이 작고 수렴시간도 짧게 나온다 — 6장 「수렴시간」 기준은 이
>   상태로 판정할 수 없다(미해결 #21).
> · **극단 케이스(부하 0/최대)는 돌리지 않았다** — 세션 3 게이트다.
> · t→∞ 값이 1-B 정상상태와 4.5e-9 K 안에서 일치했으나, 이는 **구현 일관성
>   검사**이지 물리의 독립 확인이 아니다. 동적 모델의 평형점은 설계상 1-B 와
>   같은 방정식이다 — 미해결 #15 는 여전히 열려 있다.

## 세션 로그

### 세션 1-A · 2026-08-27 · 저장소 clone · 개발환경 · 가정치 전사

**완료한 항목**

- 저장소 clone (`https://github.com/bakjeucar-boop/cdu-simul`, 빈 저장소 · 기본 브랜치 `main`)
- 정본 문서 4종을 저장소 루트에 복사 (`CLAUDE.md` 포함 — Claude Code 자동 인식 위치)
- Python 3.12.10 확인 → 저장소 루트에 `.venv` 생성
- 저장소 골격: `pyproject.toml` · `.gitignore` · `README.md` · `src/cdu_simul/` · `tests/`
- 승인 패키지 설치 (numpy 2.5.2 · scipy 1.18.1 · CoolProp 8.0.0 · pandas 3.0.5 · pytest 9.1.1)
- `src/cdu_simul/assumptions.py` — 프로젝트정리 5장 표 전사 (출처 태그 포함)
- `src/cdu_simul/fluid.py` — CoolProp 물성 래퍼 (밀도·비열)
- `tests/test_environment.py` — 환경 스모크 테스트 5건, 전부 통과
- `PROCEED.md` · `daily_brief.py` 초기 구성
- `CLAUDE.md` 「명령어」 절을 확정된 실제 값으로 채움

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `pyproject.toml` · `.gitignore` · `README.md` · `src/cdu_simul/__init__.py` | 신규 (골격) |
| `AI서버_CDU_디지털트윈_프로젝트정리.md` · `CLAUDE.md` · `collaboration.md` · `project-overview.md` | 신규 (문서 4종 복사) |
| `CLAUDE.md` 「명령어」 절 | 수정 — **웹 프로젝트 지식 재업로드 필요** |
| `src/cdu_simul/assumptions.py` | 신규 (5장 전사) |
| `src/cdu_simul/fluid.py` | 신규 (CoolProp 래퍼) |
| `tests/test_environment.py` | 신규 (환경 스모크) |
| `PROCEED.md` · `daily_brief.py` | 신규 |

**돌린 것 / 안 돌린 것**

- 돌렸음: `pytest` (5 passed) · `daily_brief.py` · `pip list` · CoolProp 물성 조회
- **안 돌렸음**: 물리 모델 일체. 이 세션에는 `CDUModel`·압력평형(`fsolve`)·ε-NTU·
  `solve_ivp`·유량분배가 없다. energy balance 오차 · T_return 방향성 · 수렴시간 ·
  극단 케이스 발산 — **6장 네 기준 중 어느 것도 이 세션에서 보지 않았다.**

**미해결 항목** — 아래 「미해결 목록」 절 참조.

**다음 세션이 알아야 할 결정**

1. **CoolProp 유체 문자열 위치**: `assumptions.py` 의 `SCENARIO.coolant_coolprop_id`
   에 두고 `fluid.py` 가 참조한다. PG25 선택 자체가 5장 가정치이므로 교체 지점이
   assumptions 한 곳이어야 한다는 판단(절대 규칙 2 · collaboration.md ④).
2. **가정치 파일 형식은 `.py`** (yaml 아님) — 타입힌트·frozen dataclass 에 맞고
   의존성이 늘지 않는다.
3. **범위값은 `Range(low, high, unit)` 로 범위 그대로 보존.** 중앙값 등 대표값을
   임의로 고르지 않았다. `Range` 에 대표값 추출 메서드를 일부러 두지 않았다.
4. **5장 표 원단위를 그대로 보존**(mAq · L/s · ℃ · kW · %). 단위 변환은 하지 않았다.
   유일한 변환은 `fluid.py` 의 ℃→K (CoolProp 호출 경계 전용).
5. **파생값은 만들지 않았다** — 밸브 Cv 역산, 펌프 곡선 계수 H0·a·b, 배관 K값.
   5장 표에 수치가 없다. 1-B에서 필요해지면 사람에게 먼저 확인한다.
6. **Python 3.12.10 사용** (시스템에 3.13·3.12 존재, 3.11 없음). CoolProp 8.0.0 은
   cp312 휠로 설치됐다.
7. **패키지 임포트는 편집설치**(`pip install -e ".[dev]"`)로 해결했다. `PYTHONPATH`
   조작이나 `sys.path` 삽입을 코드에 넣지 않았다.

### 세션 1-B · 2026-08-27 · 단일 랙·단일 CDU 정상상태 모델 · energy balance 검증

**완료한 항목**

- 세션 시작 결정사항 ㉮~㉳ 를 미해결 목록에 반영 (#1·#2 → 세션 3, #3 → 방침 (B) 확정,
  #5 문구 교체, #7 → 세션 3 재검토, #10·#11 신규)
- `src/cdu_simul/model.py` — 단일 랙·단일 CDU 정상상태 모델 (ε-NTU + 고정점 fsolve)
- `src/cdu_simul/fluid.py` — `coolant_enthalpy_Jkg` 추가 (balance 검사용 독립 경로)
- `tests/test_energy_balance.py` — balance 자동 검사 12건
- 4케이스 실행 (2차측 27/30℃ × NTU 2/3, 부하 100% 고정)

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/model.py` | 신규 (정상상태 모델) |
| `src/cdu_simul/fluid.py` | 수정 (엔탈피 조회 추가) |
| `tests/test_energy_balance.py` | 신규 (balance 자동 검사) |
| `PROCEED.md` | 수정 (결정사항 반영 + 이 로그) |

**결과 표** (`.venv/Scripts/python.exe -m cdu_simul.model` · 가정값 기반 · 실측 아님)

| 케이스 | T_supply [℃] | T_return [℃] | ΔT [K] | HX duty [kW] | balance 잔차 [%] | solver |
|---|---|---|---|---|---|---|
| 2차측 27℃ · NTU 2 | 32.15 | 42.44 | 10.29 | 80.00 | -0.00502 | 수렴 |
| 2차측 27℃ · NTU 3 | 30.43 | 40.72 | 10.29 | 80.00 | -0.00506 | 수렴 |
| 2차측 30℃ · NTU 2 | 35.14 | 45.43 | 10.29 | 80.00 | -0.00494 | 수렴 |
| 2차측 30℃ · NTU 3 | 33.43 | 43.72 | 10.29 | 80.00 | -0.00498 | 수렴 |

**돌린 것 / 안 돌린 것**

- 돌렸음: 4케이스 정상상태 해 · `pytest` 17건 전부 통과 · cp 규칙 3종 감도 비교
- `fsolve` 성공 플래그(`ier`)를 4케이스 모두 확인했다 — 전부 `ier == 1`
  ("The solution converged.")
- **안 돌렸음**: 시간축·`solve_ivp`(세션 2) · 압력평형·`fsolve` 유량분배·랙 확장(세션 3) ·
  부하 0/최대 극단 케이스(세션 3 게이트) · 누출 시나리오(세션 4) · 다중 CDU(세션 5) ·
  시각화. **6장 네 기준 중 energy balance 하나만 봤다.** T_return 방향성 · 수렴시간 ·
  극단 케이스 발산은 보지 않았다.

**다음 세션이 알아야 할 결정**

1. **balance 잔차 정의** = ( m_dot·[h(T_return) - h(T_supply)] - Q_rack ) / Q_rack × 100.
   CoolProp 엔탈피 경로를 써서 모델이 해를 구할 때 쓴 상수 cp 경로와 **독립**하게 만들었다.
   이 정의를 바꾸면 1-B 통과의 의미가 바뀐다.
2. **HX duty 잔차는 구조상 항등적으로 0**(실측 1.8e-14 %). `test_energy_balance.py` 의
   `test_hx_duty_residual_is_structural_identity` 가 이 사실 자체를 박아 두었다 —
   게이트가 아니라 오해 방지용이다.
3. **cp·ρ 평가 온도 규칙 = 1차측 벌크 평균온도(`bulk_mean`)**. 5장 표에 없는
   수치처리 규약이며 이 판에서 정했다. **이 선택이 게이트 통과/실패를 가른다**:
   `bulk_mean` 0.00506% (통과) · `supply` 0.30891% (실패) · `return` 0.31524% (실패).
   즉 게이트는 헛돌지 않는다.
4. **HX 유동배열을 대향류로 골랐다** — 5장에 배열 지정이 없다(미해결 #12).
   ε = NTU/(1+NTU) (열용량유량비 1).
5. **2차측 유체를 1차측과 같은 냉각액으로 보았다** — 5장에 2차측 유체가 없다.
   5장의 유량비 1:1 과 합쳐 열용량유량비 Cr = 1 로 두었다(미해결 #13).
   물 등 다른 유체를 도입하는 것이야말로 5장 밖 도입이라고 판단했다.
6. **압력-유량을 풀지 않았다.** 단일 랙이라 분배할 곳이 없다. 하이브리드 구조
   (절대 규칙 4)를 없앤 것이 아니라 **아직 만들지 않은 것**이다 — 세션 3에서 도입한다.
7. **결과 표 출력 진입점은 `python -m cdu_simul.model`** 이다. 새 스크립트 파일을
   만들지 않고 모듈 `__main__` 으로 두었다. `CLAUDE.md` 「명령어」 절에는 넣지 않았다 —
   이번 판은 문서 수정 권한이 없다(미해결 #16).
8. **계산 ΔT ≈ 10.29 K** 로 5장의 ΔT 10℃ 를 근사 재현했다(미해결 #5 확인). 다만
   **관측이며 판정이 아니다.** 5장 값에 맞추려고 아무것도 조정하지 않았다.

### 세션 1-C · 2026-08-27 · 저장소 위생 정비 (물리 모델 손대지 않음)

**완료한 항목**

- 정본 문서 4종 상태 확인 — 갱신본이 **이미 저장소 루트에 있었다**. `cdu-docs` 쪽이
  세션 1-A 시점 구본이어서 **복사하지 않았다**(복사했으면 사람이 고친 내용을 되돌렸다).
- `ruff` 0.16.4 · `mypy` 2.3.1 도입 — 의존성·설정 모두 `pyproject.toml` 한 곳에
- ruff 위반 4건(전부 E501) 수정 — **주석 줄바꿈만**, 문구·의미 그대로
- mypy 위반 2건 수정 — `__defaults__` 의 `None` 좁히기만, 판정 내용 그대로
- `#12`(대향류)·`#14`(cp 규칙) 근거를 **프로젝트정리 5-1** 로 교체 — 주석·출처만
- `.gitattributes` 추가(#9) · #6·#10·#11 관측만 · #16 확인 후 닫음
- 정비 전후 4케이스 동일성 증명 · pytest 17건 · ruff · mypy 전부 통과

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` · `CLAUDE.md` · `project-overview.md` | 사람이 고친 갱신본을 커밋 (5-1 소절 · 정형 절차 절 · 1-C 게이트 행) |
| `pyproject.toml` | 수정 (ruff·mypy 의존성 + 설정) |
| `src/cdu_simul/assumptions.py` | 수정 (**주석 줄바꿈만** — 값·구조 불변) |
| `src/cdu_simul/model.py` | 수정 (**주석·출처만** — 계산식 불변) |
| `tests/test_environment.py` | 수정 (타입 좁히기만 — 판정 내용 불변) |
| `.gitattributes` | 신규 (줄바꿈 정규화) |
| `PROCEED.md` | 수정 (이 로그) |

**정비 전후 동일성 (이 판의 게이트 대용)**

`git stash` 로 정비 전 코드(HEAD)를 되살려 같은 스크립트를 돌리고, 정비 후 결과와
대조했다. **T_supply · T_return · ΔT · HX duty · ε · m_dot · cp · 물성평가온도 ·
balance 잔차 — 9개 물리량 × 4케이스가 `repr` 전체자리까지 한 글자도 다르지 않다.**
`-m cdu_simul.model` 출력 파일도 문자 단위로 동일했다.

**돌린 것 / 안 돌린 것**

- 돌렸음: `ruff check .` · `mypy src tests` · `pytest`(17 passed) ·
  `-m cdu_simul.model` 정비 전/후 2회 · `git ls-files --eol`
- **안 돌렸음**: 물리 모델 변경 일체 · 시간축(세션 2) · 압력평형(세션 3) ·
  누출(세션 4) · 다중 CDU(세션 5) · 시각화.
  **6장 feasibility 기준은 이번 판에서 하나도 보지 않았다.**
  ruff·mypy 통과는 "코드가 문법·타입·스타일상 성립한다"는 뜻이지 "물리적으로
  맞다"는 뜻이 아니다 — feasibility 로 적지 않는다.

**다음 세션이 알아야 할 결정**

1. **`cdu-docs` 폴더는 구본이다.** 갱신본은 저장소 루트에만 있다. `CLAUDE.md`
   「작업 시작 전 필독」 4 대로 **저장소 사본이 정본**이므로 그대로 두었으나,
   사람이 `cdu-docs` 를 동기화하거나 폐기하는 편이 안전하다(미해결 #17).
2. **ruff 는 한글을 2칸 폭으로 센다.** `line-length = 90` 은 표시폭 기준이며
   기존 코드의 최장 표시폭은 99(한글 주석)였다. 코드 자체의 최장 줄은 89자다.
   위반 4건은 전부 한글 주석이었고 줄바꿈으로 해소했다.
3. **`.gitattributes` 는 파일 내용을 하나도 바꾸지 않았다.** `git ls-files --eol`
   로 확인한 결과 저장소·작업트리 모두 이미 LF 였다(`i/lf w/lf`). 기존의
   `LF will be replaced by CRLF` 경고는 **앞으로의 체크아웃 방향**에 대한 것이었지
   저장된 바이트가 CRLF 였다는 뜻이 아니다. 따라서 "줄바꿈만 바뀌는 커밋"은
   실제로 발생하지 않았고, 해당 커밋에는 `.gitattributes` 한 파일만 들어 있다.
4. **`requires-python = ">=3.11"`** 은 그대로 두었다(고치지 않았다 — 환경 정책은
   사람 소관, 절대 규칙 12). 의견은 미해결 #10 에 적었다.
5. **커밋 작성자 설정을 바꾸지 않았다.** 현재 값은 `WIR Generator
   <seccpi002@gmail.com>` 이다(미해결 #11 유지).

### 세션 2 · 2026-08-27 · 시간축 추가 (solve_ivp) · T_return 응답 방향성

**완료한 항목**

- 프로젝트정리 5-1 갱신본(배관 내경 · 계통 보유수량 M) 커밋
- `assumptions.py` — 배관 내경 3종 `[규격값: ASME B36.10M Sch40 · 5-1]` +
  M 범위를 내는 두 극단 구경(`holdup_bound_inner_diameters_mm`)
- `src/cdu_simul/dynamics.py` — 계통 보유수량 계산 + 2노드 온도 `solve_ivp` 적분
- `tests/test_dynamics.py` — 방향성 게이트 16건 · solver 확인 16건 · 정상상태 대조 4건
- 부하 스텝 4케이스 실행 · `requires-python` 을 `">=3.12"` 로 좁힘

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 사람이 고친 5-1 갱신본을 커밋 (배관 내경 · 계통 보유수량 M) |
| `src/cdu_simul/assumptions.py` | 수정 (배관 내경 · M 규칙 — 5-1 출처 태그) |
| `src/cdu_simul/dynamics.py` | 신규 (동적 모델 + 열용량 계산) |
| `tests/test_dynamics.py` | 신규 (세션 2 게이트 + 정상상태 대조) |
| `pyproject.toml` | 수정 (`requires-python ">=3.12"`) |
| `PROCEED.md` | 수정 (이 로그) |
| `src/cdu_simul/model.py` | **건드리지 않았다** — 1-B 결과가 1-A 기준값과 문자 단위로 동일함을 재확인 |

**결과 표** (`.venv/Scripts/python.exe -m cdu_simul.dynamics` · 가정값 기반 · 실측 아님)

| 케이스 | M [kg] | τ [s] | t63 [s] | t95 [s] | T_return 초기→최종 [℃] | 방향 | solve_ivp |
|---|---|---|---|---|---|---|---|
| 상승 20→100% · M 하한 | 11.28 | 5.72 | 5.37 | 18.72 | 30.091 → 42.437 | 상승 | 성공 |
| 하강 100→20% · M 하한 | 11.28 | 5.75 | 5.39 | 18.71 | 42.437 → 30.091 | 하강 | 성공 |
| 상승 20→100% · M 상한 | 144.70 | 73.46 | 68.87 | 240.21 | 30.091 → 42.437 | 상승 | 성공 |
| 하강 100→20% · M 상한 | 144.70 | 73.75 | 69.14 | 240.06 | 42.437 → 30.091 | 하강 | 성공 |

※ M 은 배관 보유량만 — HX·CDU 내부·콜드플레이트 제외(과소평가). 위 수렴시간은
실제보다 짧다. **"합리적"이라고 판정하지 않았다.**

**정상상태 대조 (C7)**

| 1-B 케이스 | ΔT_supply [K] | ΔT_return [K] |
|---|---|---|
| 2차측 27℃ · NTU 2 | -2.342e-10 | +1.453e-10 |
| 2차측 27℃ · NTU 3 | +2.235e-09 | -4.476e-09 |
| 2차측 30℃ · NTU 2 | -1.602e-09 | +2.510e-09 |
| 2차측 30℃ · NTU 3 | -3.294e-10 | +6.555e-10 |

최대 편차 **4.476e-09 K**. 허용오차는 **1e-6 K** 로 정했다 — 관측된 적분 잡음보다
200배 이상 느슨하고, 두 경로가 실제로 어긋났을 때 나타날 크기(4케이스 온도 폭 ~3 K)
보다는 6자리 이상 엄격하다.

**돌린 것 / 안 돌린 것**

- 돌렸음: 부하 스텝 4케이스 · 방향성 게이트 16조합 · 정상상태 대조 4케이스 ·
  `pytest` 53건 전부 통과 · `ruff` · `mypy` · 1-B 결과 불변 재확인
- `solve_ivp` 의 `success` 를 20회 적분 전부에서 확인했다 — 전부 True
- **안 돌렸음**: 압력평형·`fsolve` 유량분배·랙 확장(세션 3) · 극단 케이스 부하
  0/최대(세션 3 게이트) · 누출(세션 4) · 다중 CDU·2차측 동특성(세션 5) · 시각화.
  **6장 네 기준 중 이 판에서 판정한 것은 T_return 방향성 하나뿐이다.**

**다음 세션이 알아야 할 결정**

1. **2노드 구조**: T_supply 노드(HX 출구~랙 입구) · T_return 노드(랙 출구~HX 입구).
   `dT/dt = 0` 을 넣으면 1-B 정상상태 식이 그대로 나온다 — C7 대조가 성립하는 이유다.
   동시에 그래서 C7 은 **구현 일관성 검사**이지 물리의 독립 확인이 아니다.
2. **M 을 두 노드에 절반씩 배분**했다. 5장 등가길이를 "왕복 전체"로 읽으라는 5-1
   규칙에서 공급측·환수측이 그 왕복의 두 다리이기 때문이다. **배분 비율 자체는
   5장에도 5-1 에도 없다**(미해결 #20).
3. **M 산출용 ρ 는 고정**(5장 1차측 벌크평균 37℃ → 1011.4681 kg/m³). 반면 평형
   온도를 정하는 cp·ρ 는 **미분방정식 안에서 매 시점 다시 평가**한다 — 그래야
   t→∞ 극한이 1-B 해와 정확히 일치한다.
4. **M 범위는 구경과 등가길이 양 끝을 함께 취했다**: 하한 = 25A + 20 m = 11.276 kg,
   상한 = 80A + 30 m = 144.698 kg. 둘 다 5장에서 범위로 주어졌으므로 한쪽만
   극단으로 잡으면 M 의 실제 범위가 아니다.
5. **적분 설정**: RK45 · rtol 1e-10 · atol 1e-12 · 구간 30τ. 물리 가정이 아니라
   수치 설정이며 `dynamics.py` 상수로 노출돼 있다.
6. **결과 표는 2차측·NTU 를 범위 하단으로 고정**했다(부하와 M 만 변화시켜 보려고).
   **게이트 테스트는 고정하지 않는다** — 2차측·NTU 양 끝까지 16조합 전부 돌린다.
7. **`dynamics.py` 가 `model._property_temperature_from_state`(비공개 이름)를
   import** 한다. cp 평가 규칙을 두 곳에 적지 않으려는 것이고, `model.py` 수정
   금지 때문에 공개화하지 않았다(미해결 #22).
8. **T_return 응답은 실질적으로 단조**다 — 반대부호 증분의 최댓값이 1.4e-10 K 이고
   전부 적분 끝의 평탄한 구간에서 나온다(전체 변화량 12.35 K). 적분기 잡음이며
   물리적 오버슈트가 아니다. **단조성을 게이트로 삼지는 않았다.**

### 세션 2.5 · 2026-08-28 · 문서 정합 판 (코드 변경 없음)

**완료한 항목**

- 사람이 어제 채운 5-1 파생 가정치(펌프 곡선 계수 H₀·a·b · 밸브 개도특성/Cv · 배관 K값 ·
  mAq 단위 규약)와 `CLAUDE.md` Python 버전 정정·`project-overview.md` 진행 표시를 커밋
- 프로젝트정리 5-1 머리말에 **「5-1 빈칸 처리 순서」** 추가 (역산 → 규격·정의값 →
  범위 양 끝 → 추가 파라미터 0의 규약 → 그래도 안 되면 사람에게 확인)
- 5-1 표에 2행 추가: **M 의 노드 배분(공급 50% · 환수 50% — 수치처리 규약)** ·
  **계통 잔여저항(HX 1차측 + CDU 내부배관 + 헤더) 배정 규칙**
- 6장 「수렴시간」 기준 재정의 — 절대 수렴시간의 "합리성"을 판정하지 않고,
  **τ 가 M 에 비례하고 유량에 반비례하는 정합성**만 본다
- `project-overview.md` 「파일럿 종료 조건」 절 전면 교체 — 사전 수치 기준을 두지 않고
  **세션 5.5 산출물을 보고 사람이 종합 판단**한다
- **세션 5.5 신설** (시나리오 배치 실행 → 텍스트 데이터셋) — `CLAUDE.md` 게이트 표와
  `project-overview.md` 체크리스트 양쪽에 반영

**만든/바꾼 파일** — 전부 문서다. **코드·테스트는 한 줄도 건드리지 않았다.**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 빈칸 처리 순서 · 5-1 표 2행 추가 · 6장 수렴시간 기준 교체 |
| `project-overview.md` | 파일럿 종료 조건 절 교체 · 체크리스트 5.5 추가 · 「지금 어디인가」 첫 줄 정정 |
| `CLAUDE.md` | 게이트 표 5.5 행 추가 · 표 하단 문단 교체 |
| `PROCEED.md` | 이 로그 · 미해결 목록 갱신 |

**다음 세션이 알아야 할 결정 사항**

1. **5장 빈칸은 이제 「기다린다」가 선택지가 아니다.** 5-1 빈칸 처리 순서 4단계가
   정본이며, 넷 다 안 될 때만 사람에게 묻는다. 설계데이터를 기다리는 항목은 없다.
2. **계통 잔여저항은 집중저항 하나**로 총유량 경로에 배정한다. 헤더는 저항 0 의 공통
   노드다. 잔여저항은 독립 범위가 아니라 각 조합의 **종속값**이므로 조합 수가 늘지 않는다.
   → **세션 3 의 유량분배 결과는 이 배정 방식이 지배한다**(미해결 #24). 수렴·비발산
   게이트 통과를 "유량분배 값이 타당하다"로 읽지 않는다.
3. **M 의 노드 배분 50:50 은 물리 가정이 아니라 규약**이다. 정상상태 해에는 영향이
   없고 전이 파형에만 걸린다 — 누출 신호가 전이 파형에 걸리는 **세션 4에서 민감도 확인**.
4. **파일럿의 마지막 판은 세션 5.5** 이고, 종료 판정 주체는 사람이다. 세션 6 은
   설계데이터가 있어야 열리며 **파일럿의 정상 종착지가 아니다.**
5. **5-1 의 새 값들을 `assumptions.py` 에 전사하는 것은 세션 3 이다** — 이 판은 문서
   판이라 넣지 않았다. 코드는 아직 잔여저항·M 배분·펌프 곡선 계수를 모른다.
6. 이 판에서 판정한 feasibility 기준은 **없다.** 검사기(pytest·ruff·mypy) 통과는
   "코드를 건드리지 않았음"의 확인이지 물리 판정이 아니다.

### 세션 3-A · 2026-08-28 · 수력 모듈 (유량분배 단독 · 열모델 미결합)

**완료한 항목**

- `assumptions.py` 에 5-1 수력 가정치 전사 — mAq 규약(`PASCAL_PER_MAQ`) · 펌프
  특성곡선 계수 2세트 · 밸브 개도특성(선형)·Kv_max 2값 · 랙 분기 K 범위 ·
  M 노드 배분 50:50. **전부 5-1 에 적힌 숫자를 그대로 옮겼고 코드가 유도하지 않았다**
- `hydraulics.py` 신규 — 펌프곡선 · 밸브 ΔP(Kv 식) · 분기 ΔP(K·ρv²/2) ·
  계통 잔여저항 역산(5-1 규칙) · 8랙 헤더 압력평형 `fsolve`
- `tests/test_hydraulics.py` 신규 — 36건 (수렴 8 · 균등분배 8 · K+50% 방향성 8 ·
  잔여저항 몫 8 · 펌프곡선 단조 2 · 전사값 고정 1 · 정격점 재현 1)
- 8조합 결과 표 출력 (`python -m cdu_simul.hydraulics`)

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/assumptions.py` | 수정 (+약 100줄 — 5-1 수력 가정치 전사) |
| `src/cdu_simul/hydraulics.py` | 신규 (약 400줄) |
| `tests/test_hydraulics.py` | 신규 (약 190줄) |
| `PROCEED.md` | 수정 (이 로그) |
| `model.py` · `dynamics.py` | **건드리지 않았다** — 두 모듈 출력이 판 시작 전과 **바이트 단위로 동일**함을 확인했다 |

**결과 표** (`python -m cdu_simul.hydraulics` · 개도 80% · 8랙 동일 · 가정값 기반 · 실측 아님)

| 조합 | 잔여저항 몫 [%] | 운전점 총유량 [L/s] | 정격 15.5 대비 [%] | ier |
|---|---|---|---|---|
| H20/K3.20/Kv16.19 | 75.01 | 15.5045 | +0.029 | 1 |
| H20/K3.20/Kv12.54 | 65.01 | 15.5062 | +0.040 | 1 |
| H20/K4.80/Kv16.19 | 70.01 | 15.5054 | +0.035 | 1 |
| H20/K4.80/Kv12.54 | 60.01 | 15.5071 | +0.046 | 1 |
| H30/K3.20/Kv16.19 | 83.34 | 15.5030 | +0.019 | 1 |
| H30/K3.20/Kv12.54 | 76.67 | 15.5042 | +0.027 | 1 |
| H30/K4.80/Kv16.19 | 80.01 | 15.5036 | +0.023 | 1 |
| H30/K4.80/Kv12.54 | 73.34 | 15.5048 | +0.031 | 1 |

**다음 세션이 알아야 할 결정 사항**

1. **잔여저항 몫이 60.01~83.34% 로 나왔다** — 5-1 이 적은 60~83% 가 코드에서
   그대로 재현됐다. 미해결 #24 가 **관측으로 확인된 것**이다. 유량분배 결과는
   사실상 이 배정 방식이 지배하며, `fsolve` 수렴을 "유량분배 값이 타당하다"로
   읽지 않는다. 이 문구를 결과 표 안에 넣어 산출물이 스스로 달고 다니게 했다.
2. **랙 간 유량은 정확히 균등하게 나왔다**(상대오차 1e-9 이내). 이것은 물리의
   발견이 아니라 **5-1 배정 규칙이 강제하는 결과**다 — 잔여저항이 랙 공통 경로에
   있고 8랙 조건이 동일하기 때문이다. 테스트 docstring 에도 그렇게 적었다.
3. **두 정격 기준의 0.13% 어긋남**(펌프 15.5 L/s 대 8×1.94=15.52)은 5장 자체의
   반올림이며 **어느 쪽도 고치지 않았다**. 운전점에는 +0.019~+0.046% 로 전파된다
   — 양쪽이 모두 Q² 형태라 어긋남이 줄어든 것이다. "충분히 작다"고 판단하지
   않았고 값만 적었다.
4. **SG 가 5-1 기록(1.0124)과 CoolProp 값(1.01147)에서 0.09% 다르다.** 5-1 의
   Kv·K 는 5-1 의 SG 로 역산된 값이라, 이 모듈이 정격점에서 재현하는 ΔP 가 5장
   표값과 0.1% 이내로 어긋난다(분기 1.995 vs 2.000 · 밸브 2.993 vs 3.000).
   **어느 쪽도 고치지 않았다** — 5-1 값 전사가 이 판의 규칙이다(미해결 #27).
5. **물성 온도가 아직 5장 고정값이다.** `property_temperature_C()` 는 5장 1차측
   공급·환수의 벌크평균(37℃)을 쓴다. 열모델이 붙으면 이 온도가 풀린 상태에서
   나와야 하고 밀도가 유량에 의존하므로 **수력-열 연립**이 된다 — 세션 3-B 의
   첫 결정 사항이다(미해결 #28).
6. **개도 0 을 막아 뒀다.** 선형 특성에서 개도 0 은 Kv=0 이라 ΔP 가 정의되지
   않으므로 `valve_dp_mAq` 가 `ValueError` 를 던진다. **세션 3-B 의 "부하 0"
   극단 케이스는 개도가 아니라 발열량 0 이다** — 혼동하지 않는다.
7. **누출은 구현하지 않았다.** K+50% 방향성 검사 하나만 했고(랙0 −7.93% ·
   나머지 랙 +0.85% · 총유량 −0.25%), 이것을 누출 결과로 적지 않는다. 신호 패턴
   판정은 세션 4 게이트다. `HydraulicCase.branch_K_multipliers` 가 세션 4 의
   진입점이 되도록 자리만 만들어 뒀다.

### 세션 3-A2 · 2026-08-28 · 물성 단일화 보정 (Kv·K 재역산 · 수력 물성 온도 인자화)

**왜 열었나** — 세션 3-A 관측 ④. 5-1 이 SG 를 숫자(1.0124)로 적어 두었고 CoolProp
값(1.01147)과 0.09% 갈렸다. 5-1 은 Kv·K 를 **「역산」 등급**으로 기록했는데, 역산값
이라면 같은 물성원에서 다시 역산했을 때 같은 값이 나와야 한다. 그것이 안 나와서
정격점 ΔP 가 5장 표의 2.000/3.000 이 아니라 1.995/2.993 으로 재현되고 있었다
(`collaboration.md` 결함유형 ③ — 같은 물성치가 다른 자리에서 다르게 정의).

**완료한 항목**

- 프로젝트정리 5-1: 「밸브 Cv」·「배관 K값」 두 행을 **값에서 역산 규칙으로** 교체
  (「계통 잔여저항」 행과 같은 형태). 「수력 계산의 물성 평가 온도」 행 신규 추가
- `fluid.py`: SG 정의(`SG_REFERENCE_DENSITY_kgm3` = 1000 · `coolant_specific_gravity`)를
  **CoolProp 래퍼 한 곳으로** 모음 — SG 가 두 자리에서 다르게 정의될 수 없게 했다
- `assumptions.py`: Kv 상수 2개 · K 상수 2개 **삭제**. 5장 원 조건(1.94 L/s ·
  개도 80% · ΔP 3~5 / 2~3 mAq)만 남기고 역산 규칙 포인터를 주석으로 남김
- `hydraulics.py`: 역산 함수 2개 신규(`valve_Kv_max_m3h_from_rated_dP` ·
  `branch_K_from_rated_dP`) · 고정 37℃ 제거 · 물성 온도를 **기본값 없는 필수 인자**로
- `tests/test_hydraulics.py`: 36건 → 44건. 허용오차를 늘리지 않았다

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 두 행 교체 + 한 행 추가 (사람의 지시) |
| `src/cdu_simul/fluid.py` | 수정 (SG 정의 단일화) |
| `src/cdu_simul/assumptions.py` | 수정 (Kv·K 상수 삭제 → 규칙 포인터) |
| `src/cdu_simul/hydraulics.py` | 수정 (역산 함수 · 물성 온도 인자화) |
| `tests/test_hydraulics.py` | 수정 (44건) |
| `model.py` · `dynamics.py` | **건드리지 않았다** — 두 모듈 출력이 판 시작 전과 바이트 단위로 동일 |

**정격점 ΔP 재현 (C6 — 이 판의 핵심)** · 37℃ · 랙당 1.94 L/s · 개도 80%

| 항목 | 5장 표값 [mAq] | 역산 K/Kv | 되계산 ΔP [mAq] | 잔차 [mAq] |
|---|---|---|---|---|
| 분기 하한 | 2.000 | K = 3.2014125498 | 2.000000000000000 | −4.4e-16 |
| 분기 상한 | 3.000 | K = 4.8021188247 | 3.000000000000000 | −4.4e-16 |
| 밸브 하한 | 3.000 | Kv = 16.1871124195 | 2.999999999999998 | −1.8e-15 |
| 밸브 상한 | 5.000 | Kv = 12.5384833648 | 5.000000000000002 | +1.8e-15 |

세션 3-A 에서는 같은 자리가 1.995 / 2.993 mAq(오차 2.5e-3 · 7e-3)였다.
**잔차가 배정밀도 반올림(1e-15)까지 내려간 것은 역산의 역이 항등적으로 성립한다는
뜻이며, 물리적 정확도가 좋아졌다는 뜻이 아니다** — 5장 표값 자체가 가정치다.

**8조합 재출력 · 세션 3-A 대비 변화** (가정값 기반 · 실측 아님)

| 조합 | 잔여저항 몫 [%] (3-A → 3-A2) | 운전점 총유량 [L/s] (3-A → 3-A2) |
|---|---|---|
| H20/dPb2/dPv3 | 75.01 → 75.00 | 15.5045 → 15.5045 |
| H20/dPb2/dPv5 | 65.01 → 65.00 | 15.5062 → 15.5062 |
| H20/dPb3/dPv3 | 70.01 → 70.00 | 15.5054 → 15.5054 |
| H20/dPb3/dPv5 | 60.01 → 60.00 | 15.5071 → 15.5071 |
| H30/dPb2/dPv3 | 83.34 → 83.33 | 15.5030 → 15.5030 |
| H30/dPb2/dPv5 | 76.67 → 76.67 | 15.5042 → 15.5042 |
| H30/dPb3/dPv3 | 80.01 → 80.00 | 15.5036 → 15.5036 |
| H30/dPb3/dPv5 | 73.34 → 73.33 | 15.5048 → 15.5048 |

유량은 소수 4자리에서 변화가 없고, 잔여저항 몫만 0.01%p 씩 움직였다 — 분기·밸브
ΔP 가 5장 표값을 정확히 맞추게 되면서 잔여분이 그만큼 정확해진 것이다.
**케이스 이름이 바뀌었다**: `K3.20/Kv16.19` → `dPb2/dPv3`. 5장이 주는 것은 ΔP 이고
K·Kv 는 그 역산값이므로, 이름도 5장 원 조건을 쓰는 것이 맞다.

**다음 세션이 알아야 할 결정 사항**

1. **Kv·K 는 이제 상수가 아니라 역산 결과다.** `assumptions.py` 에 그 숫자를 다시
   넣으면 물성원이 두 개로 갈린다 — `test_valve_Kv_and_branch_K_are_not_hardcoded_constants`
   가 이름의 부재로 그것을 막는다.
2. **Kv·K·잔여저항은 정격 물성(37℃)에서 한 번 역산하고 고정한다**(5-1). 온도에
   따라 변하는 것은 그것들이 아니라 그것으로 계산한 ΔP 다. 역산 함수는 온도 인자를
   받지 않으며, 테스트가 시그니처로 그것을 고정한다.
3. **물성 온도는 이제 호출부의 책임이다** — 기본값이 없다. 세션 3-B 는 열모델이
   푼 공급·환수 온도를 `bulk_mean_temperature_C(...)` 로 넣게 된다.
4. **잔여저항만 온도 스케일을 따르지 않는다**(미해결 #30). 5-1 규칙을 이 판에서
   바꾸지 않기로 했으므로 고치지 않고 관측으로 남겼다 — 3-B 에서 사람이 정한다.
5. **세션 3 게이트는 여전히 절반이다**(미해결 #29). 극단 케이스(부하 0/최대)
   비발산은 안 봤다. 이 판은 물성 정합 보정이며 feasibility 를 하나도 판정하지 않았다.

### 세션 3-B · 2026-08-28 · 열모델 결합 (8랙 정상상태·동적) · 세션 3 게이트 판정

**완료한 항목**

- 5-1 에 3행 추가(사람 확정, 코드보다 먼저): 계통 잔여저항의 물성 의존(ρ 비례) ·
  펌프곡선의 물성 의존(없음) · 부하 프로파일의 랙 배분(CDU 전체 일괄)
- `hydraulics.py`: 잔여저항을 ρ 스케일로. **37℃ 8조합 표가 세션 3-A2 와 바이트
  단위로 동일**함을 격리 확인한 뒤 단독 커밋
- `model.py`: 랙 축을 펴 8랙으로 확장(랙 수는 `assumptions.py` 에서 읽는다) ·
  `CduCase`/`solve_cdu_steady_state` 로 수력↔열 결합 · 부하 0 잔차 가드
- `dynamics.py`: 매 시점 압력평형 결합(절대 규칙 4 완성)
- `tests/test_session3_gates.py` 신규 · `test_energy_balance.py`·`test_dynamics.py` 갱신
- 결과 표 3종에 과대해석 금지 ※ 문구(`SESSION_3B_CAVEAT`) 삽입

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 에 3행 추가 (사람의 지시) |
| `src/cdu_simul/assumptions.py` | 수정 (`SESSION_3B_CAVEAT`) |
| `src/cdu_simul/hydraulics.py` | 수정 (잔여저항 ρ 스케일 · 표 문구) |
| `src/cdu_simul/model.py` | 수정 (8랙 확장 · 수력 결합 · 32조합 표) |
| `src/cdu_simul/dynamics.py` | 수정 (매 시점 수력 결합) |
| `tests/test_session3_gates.py` | 신규 (세션 3 게이트) |
| `tests/test_energy_balance.py` · `tests/test_dynamics.py` | 수정 (8랙 재확인) |
| `PROCEED.md` | 수정 (이 로그) |

**세션 3 게이트 판정** (가정값 기반 · 실측 아님)

| 게이트 | 판정 | 근거 |
|---|---|---|
| ㉠ 헤더 압력평형 유량분배 `fsolve` 수렴 | **통과** | 열결합 32조합 전수. 수력 `fsolve` `ier=1` · 열 `fsolve` 수렴 · 결합 고정점 `fsolve` 수렴 — 셋 다 32/32 |
| ㉡ 극단 케이스(부하 0/최대) 비발산 | **통과** | 32조합 × {0%, 100%} 정상상태 + 동적 4케이스. 아래 표 |

**앞 게이트 재확인**

| 게이트 | 판정 | 값 |
|---|---|---|
| 1-B energy balance <0.1% | **통과** | 32조합 최대 \|잔차\| = **0.00507 %** |
| 세션 2 T_return 방향성 | **통과** | 8랙에서 16조합 전부 성립 (스텝 직후 기울기·순변화 둘 다) |

**극단 케이스 — 예상과 결과** (예상을 먼저 적고 대조했다)

| 부하 | 사전 예상 | 결과 (32조합 범위) | 일치 |
|---|---|---|---|
| 0% | Q=0 이면 T_return = T_2차 + 0/(εC) = T_2차, T_supply = T_return − 0/C = T_2차. **둘 다 2차측 공급온도, ΔT=0.** ε·C 는 유량이 0 이 아닌 한 0 이 아니므로 0 으로 나누는 자리 없음 | T_supply·T_return 모두 **27.000000 / 30.000000 ℃**(2차측 값과 정확히 일치) · ΔT = 0.000000 K | **맞음** |
| 100% | 5장 정격이므로 32/42℃ 근방 · ΔT>0 · T_return > T_2차 | T_supply 30.435~35.146 ℃ · T_return 40.740~45.438 ℃ · ΔT 10.289~10.308 K | **맞음** |

동적: 100→0% 는 두 온도가 2차측 온도로 단조 접근(최종 잔차 <1e-6 K), 0→100% 는
정격 정상상태로 접근. `solve_ivp` 성공 · 매 시점 수력 `fsolve` 수렴 전부 확인.

**실행시간**: 동적 1케이스 평균 **0.71 초**(RK45 · rtol 1e-10 · 4001 출력점 ·
우변마다 8변수 `fsolve`). 전체 테스트 294건 **31 초**. 최적화하지 않았다.

**다음 세션이 알아야 할 결정 사항**

1. **8랙은 열적으로 1랙 집중모델과 동치다.** 랙이 동일하고 유량이 균등하므로
   Q 와 C 가 같은 배수로 커져 온도해가 변하지 않는다 — 8랙 대 1랙 온도 편차가
   **정확히 0.000e+00 K** 로 나왔다. "8랙 거동을 확인했다"로 읽지 않는다.
   랙 간 비대칭은 누출 K값이 들어오는 **세션 4에서 처음 생긴다.**
2. **결합 구조는 「고정점 안에 quasi-steady 수력」이다.** 바깥 `fsolve`(물성 온도)
   안에서 매번 8변수 수력 `fsolve` 를 푼다. 연립이 아니라 **중첩**이며, 절대 규칙 4
   의 하이브리드를 그대로 지킨다(미해결 #28 이 물은 것의 답).
3. **M 의 8랙 해석이 없다**(미해결 #31 신설). 새 숫자를 만들지 않았으므로 τ·t63·t95
   의 절대값은 8랙에서 해석할 수 없다. 게이트에는 영향이 없다.
4. **부하 0 은 밸브 개도 0 이 아니라 발열량 0 이다.** 개도 0 은 Kv=0 이라 ΔP 가
   정의되지 않아 `valve_dp_mAq` 가 `ValueError` 를 던진다. 개도가 변수가 되는 것은
   세션 4다.
5. **누출은 손대지 않았다.** `HydraulicCase.branch_K_multipliers` 진입점을 쓰지
   않았고, 랙별 부하·K·개도는 전부 동일하다(5-1 부하 랙 배분 규약).
6. **수렴시간은 여전히 판정하지 않는다** — 미해결 #21(M 결손) 과 #31(8랙 해석 부재)
   이 둘 다 열려 있다.

### 세션 4 · 2026-08-28 · 누출 시나리오 주입 · 신호 패턴 식별

**완료한 항목**

- `CLAUDE.md` 세션 4 게이트 셀: 「펌프RPM변화」 → 「펌프 운전점 이동」
- 5-1 에 2행 추가(사람 확정, 코드보다 먼저): 펌프 운전 방식(정속) · 누출 주입 지점(랙 1개)
- `assumptions.py`: 누출 수준 목록(정상 배율 1.0 + 5장 3수준) · 주입 랙 번호
- `hydraulics.py`: `apply_leak_to_rack` — 3-A 의 `branch_K_multipliers` 진입점 사용
- `dynamics.py`: 누출 스텝 적분기(`LeakStepCase`/`integrate_leak_step`) ·
  `_derivative` 가 수력 케이스를 인자로 받아 t0 전후 K값을 바꾼다
- `leak.py` 신규: 신호 산출·결과 표 2종
- 테스트 3종 신규: 세션 4 게이트 · M 배분 민감도 · 누출 후 앞 게이트 재확인

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `CLAUDE.md` | 게이트 표 세션 4 셀 하나 (사람의 지시) |
| `AI서버_...정리.md` | 5-1 에 2행 추가 (사람의 지시) |
| `src/cdu_simul/assumptions.py` | 수정 (누출 수준 목록 · 주입 지점 · `SESSION_4_CAVEAT`) |
| `src/cdu_simul/hydraulics.py` | 수정 (`apply_leak_to_rack`) |
| `src/cdu_simul/dynamics.py` | 수정 (누출 스텝 적분기 · 관측 함수 Protocol) |
| `src/cdu_simul/leak.py` | 신규 (누출 시나리오 실행) |
| `tests/test_session4_gates.py` | 신규 (판정 기준 선기재) |
| `tests/test_holdup_split_sensitivity.py` | 신규 (시험값 40:60·60:40) |
| `tests/test_gates_after_leak.py` | 신규 (정상 경로 불변성) |
| `model.py` | **건드리지 않았다** |

**세션 4 게이트 판정** (가정값 기반 · 실측 아님)

판정 기준은 `tests/test_session4_gates.py` 머리말에 **코드보다 먼저** 적었다(커밋 803142e).

| 기준 | 판정 | 근거 |
|---|---|---|
| A 부호 일관성 | **통과** | 32조합 × 3수준 = 96건 전부. 누출랙 유량↓ · 누출랙 출구온도↑ · 총유량↓ · 펌프 양정↑ · 비누출랙 유량↑ |
| B 수준 간 엄격 단조 | **통과** | 32조합 전부에서 +5% < +20% < +50% 로 다섯 신호의 크기가 커진다 |
| C 잡음 대비 | **통과** | +5% 신호가 임계보다 2~4자리 크다 (임계는 3-A/3-B 관측 잡음의 1000배 이상) |

→ **세션 4 게이트 통과.**

**정상상태 신호** (32조합 범위 · 랙 0 주입 · 같은 조합의 정상 해 대비)

| 누출 수준 | 누출랙 유량 [%] | 타랙 유량 [%] | 총유량 [%] | 누출랙 출구온도 [K] | 펌프 양정 [mAq] |
|---|---|---|---|---|---|
| +5% | −1.1159 ~ −0.6372 | +0.0608 ~ +0.1262 | −0.0412 ~ −0.0183 | +0.06666 ~ +0.11835 | +0.00130 ~ +0.00196 |
| +20% | −4.2459 ~ −2.4757 | +0.2362 ~ +0.4802 | −0.1589 ~ −0.0704 | +0.26383 ~ +0.46476 | +0.00501 ~ +0.00754 |
| +50% | −9.6855 ~ −5.8569 | +0.5580 ~ +1.0954 | −0.3704 ~ −0.1638 | +0.64640 ~ +1.12302 | +0.01163 ~ +0.01759 |

누출 상태 energy balance 최대 \|잔차\| = **0.00507 %** (6장 기준 <0.1%).

**동적 신호** (정격 운전 중 t=0 에 K값 계단 주입 · 부하는 그대로)

| 케이스 | τ [s] | t63 [s] | t95 [s] | T_return [℃] | 누출랙 Q [%] | 총 Q [%] | 양정 [mAq] |
|---|---|---|---|---|---|---|---|
| +5% · M 하한 | 0.72 | 0.67 | 2.34 | 42.4512 → 42.4555 | −0.8904 | −0.0274 | +0.00130 |
| +20% · M 하한 | 0.72 | 0.67 | 2.35 | 42.4512 → 42.4676 | −3.4208 | −0.1056 | +0.00501 |
| +50% · M 하한 | 0.72 | 0.67 | 2.35 | 42.4512 → 42.4892 | −7.9343 | −0.2455 | +0.01163 |
| +5% · M 상한 | 9.23 | 8.58 | 30.04 | 42.4512 → 42.4555 | −0.8904 | −0.0274 | +0.00130 |
| +20% · M 상한 | 9.23 | 8.58 | 30.11 | 42.4512 → 42.4676 | −3.4208 | −0.1056 | +0.00501 |
| +50% · M 상한 | 9.23 | 8.65 | 30.11 | 42.4512 → 42.4892 | −7.9343 | −0.2455 | +0.01163 |

`solve_ivp` 성공 · 매 시점 수력 `fsolve` 수렴 전수 확인. **시간 규모의 절대값은
해석하지 않았다**(#21 · #31).

**C7 정상 경로 불변 확인**: `model`·`dynamics`·`hydraulics` 세 표가 세션 3-B 와
**바이트 단위로 동일**. 배율 1.0 경로와 직접 경로의 32조합 편차 = **0.000e+00**
(허용오차 없이 `==` 로 고정했다 — `tests/test_gates_after_leak.py`).

**M 노드 배분 민감도** (시험값 — 5-1·`assumptions.py` 에 넣지 않았다)

| 배분(공급:환수) | t63 [s] (M 하한) | t63 [s] (M 상한) | 50:50 대비 T_return 최대 편차 [K] | t→∞ |
|---|---|---|---|---|
| 40:60 | 0.5663 | 7.2667 | 2.5e-4 ~ 2.2e-3 | 동일 |
| 50:50 | 0.6687~0.6741 | 8.5817~8.6509 | (기준) | (기준) |
| 60:40 | 0.7658 | 9.8274 | 2.0e-4 ~ 1.8e-3 | 동일 |

t→∞ 값은 세 배분비에서 편차 <2e-9 K 로 같았다 — 5-1 의 "정상상태에 영향하지
않는다"가 성립한다. **판정은 사람이 한다 — 결론을 쓰지 않는다.**

**다음 세션이 알아야 할 결정 사항**

1. **랙 간 비대칭이 여기서 처음 생겼다.** 3-B 까지 8랙은 열적으로 1랙 집중모델과
   동치였는데, 누출이 걸린 랙 하나만 유량·온도가 갈라진다. `rack_return_temps_C`
   가 그 차이를 담는다.
2. **펌프는 정속이다**(5-1 확정). RPM 은 모델에 없다 — 게이트의 「펌프 운전점
   이동」은 정속 곡선 위의 이동이며, 양정 변화는 +0.0013 ~ +0.0176 mAq 로
   양정(20~30 mAq) 대비 **상대 1e-4 수준**이다. 세 신호 중 가장 약하다.
3. **누출은 K값 대용(proxy)이다.** 질량 손실이 모델에 없으므로 총유량 감소는
   "누출된 만큼 빠져나갔다"가 아니라 "저항이 커져 덜 흐른다"다. 데이터셋에
   이 사실을 붙이지 않으면 학습 모델이 잘못된 물리를 배운다(세션 5.5).
4. **밸브 개도는 80% 고정이었다.** 5-1 「밸브 개도 특성」이 "개도가 변수가 되는
   시점에 재검토"라고 적었으나 누출은 K값 변화이므로 이번 판의 재검토 대상이
   아니었다 — **선형 특성은 아직 시험되지 않았다**(미해결 #32).
5. **전 랙 동시 누출은 돌리지 않았다** — 5장에 근거가 없다(5-1 「누출 주입 지점」).

### 세션 5 · 2026-08-28 · 다중 CDU · 공유 2차측 결합

**완료한 항목**

- 5-1 (사람 확정, 코드보다 먼저): 2차측 유체 확정 · 물성 조회 기준 압력 확정 ·
  CDU 대수(2대) · 공유 2차측 결합 방식 · 2차측 공급온도 고정 유지
- `assumptions.py`: `PlantAssumptions`(대수·총 2차측 유량·비례 배분)
- `model.py`: `hx_capacity_terms`(ε·C_min 을 한 곳에) · `SteadyStateCase.secondary_flow_Lps` ·
  `cdu_property_temperature_residual`(상위 레벨이 쓰는 공개 잔차)
- `dynamics.py`: `hx_capacity_terms` 사용 · 2차측 유량 인자
- `plant.py` 신규: 상위 레벨 연립 정상상태 · 비대칭 부하 스텝 동적 결합 · 표 2종
- 테스트 2종 신규 · `daily_brief.py` 에 열린 미해결 목록 출력

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 2행 확정 + 3행 추가 (사람의 지시) |
| `src/cdu_simul/assumptions.py` | 수정 (`PlantAssumptions` · `SESSION_5_CAVEAT`) |
| `src/cdu_simul/model.py` | 수정 (`hx_capacity_terms` · 2차측 유량 · 잔차 공개) |
| `src/cdu_simul/dynamics.py` | 수정 (`hx_capacity_terms` 사용 · 2차측 유량 인자) |
| `src/cdu_simul/plant.py` | 신규 (다중 CDU 결합) |
| `tests/test_session5_gates.py` · `tests/test_gates_after_plant.py` | 신규 |
| `daily_brief.py` | 수정 (열린 미해결 목록) |
| `leak.py` · `hydraulics.py` | **건드리지 않았다** |

**세션 5 게이트 판정** (가정값 기반 · 실측 아님)

판정 기준은 `tests/test_session5_gates.py` 머리말에 **코드보다 먼저** 적었다(커밋 acbc31c).

| 기준 | 판정 | 근거 |
|---|---|---|
| A 상위 연립 수렴 | **통과** | 96 케이스(32조합 × 부하 시나리오 3) 전부 상위 `fsolve` `ier=1` · 각 CDU 물성 온도·헤더 압력평형 `fsolve` 수렴 |
| B 잔차 동시 소멸 | **통과** | 최대 잔차 1e-13~1e-11 K (순차 대입이면 K 단위로 남는다) |
| C 연동의 실재 | **통과** | 비대칭에서 부하가 그대로인 CDU B 의 환수온도·2차측 배분이 실제로 움직인다 |

→ **세션 5 게이트 통과.**

**C4 는 실패했다 — 정지 조건에 해당한다.**

대칭 케이스가 세션 4 단일 CDU 결과를 재현하지 못한다: **32조합 최대 편차 0.0245 K**
(최악 `H22.4/dPb3/dPv5 / NTU=2 / T2nd=30C`). 원인은 5-1 두 행의 내부 모순이며
**미해결 #33 으로 등록**했다. 나머지 항목이 이 답에 의존하지 않으므로 완주한 뒤
보고한다 — 자세한 것은 #33 과 세션 5 보고를 본다.

**정상상태 결합 결과** (수력·NTU·2차측 하단 고정 · 가정값 기반 · 실측 아님)

| 시나리오 | CDU | 부하 [%] | 1차 Q [L/s] | 2차 배분 [L/s] | 배분비 [%] | Cr | T_sup [℃] | T_ret [℃] |
|---|---|---|---|---|---|---|---|---|
| 대칭 100/100 | A | 100 | 15.5056 | 15.5000 | 50.000 | 0.9982665 | 32.1683 | 42.4690 |
| | B | 100 | 15.5056 | 15.5000 | 50.000 | 0.9982665 | 32.1683 | 42.4690 |
| 대칭 20/20 | A | 20 | 15.4780 | 15.5000 | 50.000 | 0.9989152 | 28.0319 | 30.0978 |
| | B | 20 | 15.4780 | 15.5000 | 50.000 | 0.9989152 | 28.0319 | 30.0978 |
| 비대칭 100/20 | A | 100 | 15.5055 | 15.5138 | 50.044 | 0.9991572 | 32.1591 | 42.4599 |
| | B | 20 | 15.4780 | 15.4862 | 49.956 | 0.9998046 | 28.0328 | 30.0988 |

**연동 크기**: 비대칭에서 B 는 부하가 20% 로 같은데도 대칭 20/20 대비
T_return 이 **+0.0010 K**(30.0978 → 30.0988) 움직였다. 배분비가 50.000% 에서
49.956% 로 줄었기 때문이다. **크다/작다를 판정하지 않는다.**

**동적 결합** (A 만 20→100% · B 는 20% 불변)

| CDU | 부하 | T_ret 초기 [℃] | T_ret 최종 [℃] | ΔT_ret [K] | 2차 배분 초기 → 최종 [L/s] |
|---|---|---|---|---|---|
| A | 20→100% | 30.09784 | 42.45989 | +12.36205 | 15.50000 → 15.51378 |
| B | 20→20% | 30.09784 | 30.09876 | **+0.00092** | 15.50000 → 15.48622 |

M 하한·상한 둘 다 같은 값이다(정상상태 종점은 M 에 의존하지 않는다).
`solve_ivp` 성공 · 매 시점 수력 `fsolve` 수렴 전수 확인.

**C7 — 결합 상태의 누출 관측** (CDU A 에만 주입 · 게이트가 아니다)

| 수준 | A T_ret [℃] | B T_ret [℃] | B 변화 [K] | B 2차 배분 [L/s] |
|---|---|---|---|---|
| 정상 | 42.46905 | 42.46905 | — | 15.50000 |
| +5% | 42.47188 | 42.46763 | −0.001414 | 15.50213 |
| +20% | 42.47995 | 42.46361 | −0.005441 | 15.50819 |
| +50% | 42.49442 | 42.45640 | −0.012649 | 15.51905 |

A 의 누출로 A 의 1차측 유량이 줄자 A 의 배분 몫이 줄고 **B 의 몫이 늘어** B 가
더 냉각된다. 세션 4 신호 방향(A 유량↓·환수온도↑·양정↑)은 결합 뒤에도 유지된다.

**다음 세션이 알아야 할 결정 사항**

1. **#33 을 먼저 정해야 한다.** Cr 을 배분에서 유도할지 5장 명목 1 로 둘지에 따라
   세션 5 의 모든 수치가 바뀐다. 세션 5.5 데이터셋은 그 결정 뒤에 내는 것이 안전하다.
2. **연동은 유량 경로 하나뿐이다.** 2차측 공급온도가 고정이라 열 경로 결합이
   모델에 없다(5-1 한계). 연동 크기(+0.0010 K)를 "CDU 간 상호작용이 작다"로
   읽지 않는다 — 모델에 있는 경로만 잰 값이다.
3. **단일 CDU 경로는 그대로 남아 있다.** 2차측 유량을 주지 않으면 세션 4까지와
   부동소수점까지 같다 — 테스트가 `==` 로 고정한다.
4. **CDU 2대만 돌렸다.** 3대 이상은 배치·부하 비대칭 등 5장에 없는 선택을 부른다.

### 세션 5-B · 2026-08-28 · Cr 유도로 전환 · 전 게이트 소급 재판정

**왜 열었나** — 세션 5 C4 가 드러낸 것: 세션 1-B 이후의 Cr=1 은 **유도된 값이 아니라
5-1 이 선언한 값**이었다. 5장 「1차:2차 유량비 1:1」을 부피유량으로 읽으면 양쪽 온도가
달라 ρ·cp 가 다르므로 Cr≠1 이 된다. CDU 대수와 무관하므로 **단일 CDU 에도 소급**한다.

**사람이 정한 전제** (코드가 다시 정하지 않았다)

1. 5장 「1:1」은 **부피유량 기준**
2. 2차측 유량은 1차측 **정격** 15.5 L/s 와 1:1 로 **고정** (운전점 추종 없음)
3. Cr 은 선언하지 않고 **매번 물성에서 유도**
4. **단일 CDU 에도 소급 적용**

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 「2차측 유체」 교체 · 「열교환기 유동배열」 근거 재계산 (사람의 지시) |
| `src/cdu_simul/assumptions.py` | 수정 (부피유량비 명시 · `secondary_flow_Lps` · `SESSION_5B_CAVEAT`) |
| `src/cdu_simul/model.py` | 수정 (Cr 필드 제거 · `hx_capacity_terms` 의 `None` 분기 제거) |
| `src/cdu_simul/dynamics.py` · `leak.py` · `hydraulics.py` · `plant.py` | 수정 (Cr 인자 제거 · ※ 추가) |
| 테스트 7종 | 수정 (생성자 갱신 · 전제가 뒤집힌 검사 2건 교체) |

**C1(2) 유동배열 근거 재계산** — 중단 조건이었다

| | T_supply [℃] | T_return [℃] | ε |
|---|---|---|---|
| 세션 1-B 기록 (Cr=1 선언) | 32.1504 | 42.4512 | 0.666667 |
| 세션 5-B 재계산 (Cr 유도) | **32.1683** | **42.4690** | 0.667052 |
| 5장 표값 | 32 | 42 | — |

반올림하면 32/42℃ 로 **여전히 정합** → 대향류 확정 유지. dT = 10.3008 K.

**C2 Cmin 판정**: 32조합 **전부 2차측이 Cmin** 이다((ρcp)₂₇=4001.52 < (ρcp)₃₇=4007.02
J/(L·K)). 세션 5-B 이전 코드는 1차측을 Cmin 으로 가정하고 있었다. Cr 범위
**0.9976198 ~ 0.9989254**.
**Cr=1 전용 분기**: `hx_effectiveness_counterflow` 에 있었고 **남겼다** — 일반식이
0/0 이 되는 수학적 특이점 가드이지 5장 「1:1」을 되살리는 경로가 아니다(부하 0
극단 케이스에서 실제로 필요하다). 성격을 주석에 적었다. **가정으로서의 Cr=1 은 전부 제거**.

**C4 대칭 재현**: 32조합 최대 온도 편차 **0.000e+00 K** (세션 5 에서 0.0245 K 였다).
대칭 배분에서 각 CDU 가 정확히 15.5 L/s 를 받는다(오차 <1e-12).

**C5 전 게이트 소급 재판정 — 다섯 개 전부 통과**

| 게이트 | 판정 | 근거 |
|---|---|---|
| 1-B energy balance <0.1% | **통과** | 32조합 최대 \|잔차\| **0.00506 %** |
| 2 T_return 방향성 | **통과** | 16조합 전부 |
| 3 수렴 · 극단 케이스 비발산 | **통과** | 32조합 × 부하 0/100% · 동적 4케이스 |
| 4 누출 신호 식별 (기준 A·B·C) | **통과** | 96건 전부 (세션 4 선기재 기준 그대로) |
| 5 CDU 간 연동 수렴 (기준 A·B·C) | **통과** | 96 케이스 전부 (세션 5 선기재 기준 그대로) |

**판정 기준·허용오차를 하나도 조정하지 않았다.**

**C6 이동량 대조표** (세션 5-B 이전 → 이후)

| 항목 | 이전 | 이후 | 이동 |
|---|---|---|---|
| 1-B T2nd=27/NTU=2 T_sup [℃] | 32.15 | 32.1731 | +0.0231 |
| 1-B T2nd=27/NTU=2 T_ret [℃] | 42.44 | 42.4643 | +0.0243 |
| 1-B T2nd=27/NTU=3 T_sup·T_ret [℃] | 30.43 · 40.72 | 30.4524 · 40.7454 | +0.0224 · +0.0254 |
| 1-B T2nd=30/NTU=2 T_sup·T_ret [℃] | 35.14 · 45.43 | 35.1695 · 45.4580 | +0.0295 · +0.0280 |
| 1-B T2nd=30/NTU=3 T_sup·T_ret [℃] | 33.43 · 43.72 | 33.4498 · 43.7398 | +0.0198 · +0.0198 |
| energy balance 최대 \|잔차\| [%] | 0.00507 | 0.00506 | −0.00001 |
| 누출 +5% 온도 신호 [K] | +0.06666~+0.11835 | +0.06505~+0.11419 | 신호 **축소** |
| 누출 +50% 온도 신호 [K] | +0.64640~+1.12302 | +0.63153~+1.09541 | 신호 **축소** |
| 누출 유량·양정 신호 | −9.6855~−0.6372% · +0.00130~+0.01759 mAq | −9.6859~−0.6372% · +0.00130~+0.01763 mAq | 거의 불변 |
| 세션 5 연동 크기 [K] | +0.0010 | +0.000917 | −0.00008 |
| Cr | 1 (선언) | **0.9976198~0.9989254 (유도)** | — |
| C4 대칭 재현 편차 [K] | 0.0245 | **0.000000** | 해소 |

**"작다/무시할 만하다"는 판단을 쓰지 않는다 — 값만 적었다.**

**다음 세션이 알아야 할 결정 사항**

1. **세션 5-B 이전 로그의 수치를 이후 것과 나란히 읽지 않는다.** 세션 1-B~5 의
   기록이 전부 소급 이동했다. 5-1 「2차측 유체」에 그 한계를 적었다.
2. **Cr 은 어디에서도 선언되지 않는다.** `assumptions.py` 에 Cr 상수가 없고,
   케이스 dataclass 에 `heat_capacity_ratio` 필드가 없다. 되살리면 세션 5 C4 가
   드러낸 오류가 그대로 돌아온다.
3. **Cmin 은 2차측이다**(현재 조건에서 32조합 전부). 2차측 유량이나 온도가 바뀌면
   뒤집힐 수 있으므로 `hx_capacity_terms` 가 매번 판정한다.
4. **누출 온도 신호가 2.4% 작아졌다**(예: +50% 상한 1.12302 → 1.09541 K).
   Cmin 이 2차측으로 바뀌면서 열교환 용량이 조금 줄었기 때문이다. 세션 4 게이트
   기준 C(잡음 대비)는 여전히 2~4자리 여유로 통과한다.

### 세션 5.5-A · 2026-08-28 · 데이터셋 설계 · 조합 계수 · 실행시간 측정 (생성 없음)

**이 판은 아무것도 확정하지 않았다.** 스키마는 제안이고, 부하 축 값은 고르지 않았으며,
규모 3안 중 어느 것도 선택하지 않았다. **CSV 를 생성하지 않았다.**
미해결을 새로 닫지 않았다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/dataset_plan.py` | 신규 (축 정의 · 계수 · 시간 측정 · 크기 추정). 5.5-B 재사용 |
| `PROCEED.md` | 수정 (이 로그) |
| 모델 코드 전부 · 문서 4종 · 기존 테스트 | **건드리지 않았다** — 세 결과 표 불변 확인 |

**C1 스키마 초안 (제안 · 확정 아님)** — 46열. 행은 **(시나리오 × CDU × 시각) 하나당 하나**,
랙별 값은 열로 편다(rack0..rack7). 행으로 펴면 CDU 총계가 8번 중복된다.

| 묶음 | 열 |
|---|---|
| 시나리오 식별 (10) | `scenario_id` · `scenario_kind`(정상/이상/누출) · **`regime`(steady/transient)** · `cdu_config`(single/dual_symmetric/dual_asymmetric) · `cdu_index` · `leak_level_percent` · `leak_rack_index` · `load_percent` · `holdup_mass_kg` · `t_s` |
| 5장 범위 축 (5) | `pump_head_rated_mAq` · `branch_dp_rated_mAq` · `valve_dp_rated_mAq` · `ntu` · `T_secondary_supply_C` |
| 랙별 관측량 (16) | `rack{0..7}_flow_Lps` · `rack{0..7}_outlet_C` |
| CDU 총계 (7) | **`total_flow_Lps`**(부호 불확실) · **`pump_head_mAq`**(부호 불확실) · `T_supply_C` · `T_return_C` · `hx_duty_kW` · `secondary_share_Lps` · `heat_capacity_ratio`(유도값) |
| 검증 (1) | `energy_balance_residual_percent` |
| solver 플래그 (4) | `hydraulic_solver_ier` · `thermal_solver_converged` · `plant_solver_ier` · `integrator_success` |
| 출처·한계 (5) | `assumption_tag` · `signal_sign_caveat` · `transient_caveat` · `total_flow_caveat` · `cr_caveat` |

**표기 방식 두 안** (사람이 정한 것 1·2·3 의 표기를 어디에 둘지)

| 안 | 방식 | 행당 추가 바이트 | 안2 규모(31만 행) 기준 증가 |
|---|---|---|---|
| A | 전 행에 문구 5열 반복 | 약 210 B | +0.065 GB (0.20 → 0.26 GB, **+31%**) |
| B | 참조 키 1열 + 별도 메타 파일 | 약 12 B | 기준 |

**의견**: `CLAUDE.md` 세션 5.5 게이트가 **"전 행에"** 표기를 요구하므로 A 가 게이트
문언에 맞다. B 는 파일이 30% 작지만 CSV 한 장만 떼어 보면 표기가 사라진다 —
데이터셋이 나중에 조각나 유통될 것을 생각하면 A 가 안전하다. **사람이 정한다.**

**C2 조합 계수** (누출 랙 축 끔 · 정상상태에 M 축 없음)

축: 5장 범위 8(양정 2 × 분기ΔP 2 × 밸브ΔP 2) × NTU 2 × 2차측 2 × 부하 L ×
누출 수준 4 × CDU 구성 3. 전이는 여기에 M 2수준.

| 부하 축 후보 | 정상상태 | 전이 | 합계 | 5장 밖 숫자 |
|---|---|---|---|---|
| 양 끝만 (20 · 100%) | 768 | 1,536 | 2,304 | **0개** |
| 양 끝 + 중점 (20 · 60 · 100%) | 1,152 | 2,304 | 3,456 | 1개(60%) |
| 4수준 | 1,536 | 3,072 | 4,608 | 2개 |
| 5수준 (20/40/60/80/100%) | 1,920 | 3,840 | 5,760 | 3개 |

**중복·무의미 조합 지적**

1. **누출 랙 번호를 축으로 둘 필요가 없다.** 8랙이 전부 동일해 결과가 같다(5-1).
   축으로 두면 행이 8배가 되고 그 8배가 전부 같은 물리다. **랙 0 고정을 권한다** —
   단 `leak_rack_index` **열은 남긴다**(비대칭 랙이 생겨도 스키마가 안 바뀌게).
2. **단일 CDU 에 「비대칭」은 성립하지 않는다.** CDU 구성 축이 부하 축과 독립이 아니다.
3. **2대 대칭이 단일 CDU 를 정확히 재현한다**(세션 5-B: 편차 0.000e+00 K).
   둘 중 하나는 완전 중복이다 — 전이에서 2대 대칭을 빼면 시간이 크게 준다.
4. **정상상태에 M 축을 두면 전부 중복**이다(정상상태 해는 M 에 의존하지 않는다).

**C3 실행시간 실측** (이 PC · 케이스마다 1회 · 캐시 워밍업 별도 확인)

| 종류 | 평균 [s] | 범위 [s] | 산포 | n |
|---|---|---|---|---|
| 반복 영향(같은 케이스 3회) | 0.0061 | 0.0056~0.0064 | 1.15배 | 3 |
| 정상상태 · 단일 CDU | 0.0068 | 0.0054~0.0085 | 1.58배 | 5 |
| 정상상태 · 2대 결합 | 0.0485 | 0.0461~0.0544 | 1.18배 | 5 |
| 전이 · 단일 CDU | 0.3095 | 0.2870~0.3329 | 1.16배 | 3 |
| 전이 · 2대 결합 | **3.8800** | 3.7235~4.0729 | 1.09배 | 3 |

같은 케이스를 세 번 돌려도 시간이 1.15배 흔들린다 — **결과 캐시는 없고**
(케이스마다 solver 를 새로 돌린다) 흔들림은 OS 스케줄링에서 온다. 아래 추정에는
평균을 썼고, 산포만큼 추정이 흔들린다.

**C4 전량 추정 · 규모 3안** (부하 2수준 기준 · **추정이지 실측이 아니다**)

| 안 | 정상 케이스 | 전이 케이스 | 전이 표본 | 예상 시간 | 행 수 | 크기(A안) |
|---|---|---|---|---|---|---|
| **1 전수** | 768 | 1,536 | 4,001 | **1.16시간** | 10,243,840 | **8.75 GB** |
| **2 정상 전수 + 전이 부분집합** | 768 | 1,024 | 201 | **36.2분** | 310,016 | **0.26 GB** |
| **3 둘 다 부분집합** | 768 | 1,024 | 41 | **36.2분** | 64,256 | **0.05 GB** |

**무엇을 잃는가**

- **안1**: 잃는 것 없음. 다만 8.75 GB 는 git 에 올릴 수 없고 파일 하나로 열기도 어렵다
- **안2**: 전이에서 **2대 대칭 구성**을 뺐다 — 세션 5-B 가 단일 CDU 와 정확히 같음을
  증명했으므로 **정보 손실이 없다**. 전이 표본을 4001 → 201 점으로 줄였다 —
  t63·t95 를 읽기에는 충분하지만 **스텝 직후 기울기**의 해상도가 20배 낮아진다
  (세션 2 방향성 게이트가 그 기울기를 쓴다)
- **안3**: 표본 41점. 시간은 안2 와 같은데(적분 자체가 비용이고 표본 추출은 공짜다)
  **파일만 1/5** 이 된다. 전이 파형의 모양을 학습시키기에는 성기다

**권고(의견이며 선택이 아니다)**: **안2**. 전이 시간의 대부분을 차지하던 2대 대칭이
증명된 중복이라 공짜로 빠지고, 0.26 GB 는 다룰 수 있는 크기다. 다만 **전이 표본
201점이 세션 2 방향성 게이트가 쓰는 스텝 직후 기울기를 충분히 담는지**는 5.5-B
에서 확인해야 한다 — 담지 못하면 표본을 늘리는 대신 **스텝 직후 구간만 조밀하게**
뽑는 방식을 검토한다.

**주의**: 시간과 크기는 **다른 축이 지배한다.** 시간은 전이 케이스 수(적분 횟수)가,
크기는 전이 표본 수가 정한다. 규모를 줄일 때 무엇을 줄이는지에 따라 어느 쪽이
줄어드는지가 다르다.

**C6 상태 이월 검사 설계안** (세션 5.5 게이트 · **코드는 5.5-B 에서 만든다**)

이월이 있다면 이렇게 드러난다:
· 같은 시나리오를 **다른 순서로** 돌렸을 때 결과가 달라진다
· 앞 시나리오의 해가 뒤 시나리오의 `fsolve` 초기값으로 새면 수렴점이 미세하게 이동한다
· 누출 시나리오 다음의 정상 시나리오가 정상값을 정확히 되찾지 못한다

검사안 셋:
1. **순서 무관성** — 시나리오 목록을 뒤집어(또는 섞어) 다시 돌리고 각 행이
   **바이트 단위로 동일**한지 본다. 세션 3-B 이후 여러 판에서 써 온 기법이고
   **여기에 그대로 쓸 수 있다** — 이월은 순서 의존으로 나타나므로 이 검사가
   정확히 그것을 잡는다
2. **단독 재현** — 임의의 시나리오 하나를 **혼자** 돌린 결과가 배치 안의 그 행과
   같은지 본다. 배치 문맥이 결과에 남지 않음을 직접 확인한다
3. **정상 복귀** — 누출 → 정상 순서로 돌렸을 때 정상 행이 배치 첫 정상 행과 같은지

1과 2가 서로를 보완한다: 1은 순서 의존을, 2는 배치 자체의 오염을 잡는다.
**셋 다 새 허용오차가 필요 없다** — 전부 `==` 로 본다.

**다음 세션이 알아야 할 결정 사항**

1. **사람이 정해야 할 것 넷**: ① 부하 축 값(양 끝만 / 중간값 추가) ② 규모 안
   (1·2·3) ③ 표기 방식(A 행마다 / B 메타 분리) ④ 전이 표본 수
2. **스키마는 아직 어디에도 기록하지 않았다** — `SCHEMA_DRAFT` 는 코드 안의 제안이다.
   5.5-B 에서 확정하면 그때 5-1 이나 `assumptions.py` 에 넣을지 정한다.
3. **시간과 크기는 다른 축이 지배한다** — 위 「주의」 참조.

### 세션 5.5-B · 2026-08-28 · 시나리오 배치 실행 → 텍스트 데이터셋 (파일럿 마지막 판)

**파일럿의 마지막 판이다.** 종료 판정은 하지 않았다 — 판정 주체는 사람이다
(`project-overview.md` 「파일럿 종료 조건」). 이 판은 **재료를 손에 잡히게** 했다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 「데이터셋 스키마 규약」 1행 추가 (사람의 지시) |
| `src/cdu_simul/dynamics.py` | 수정 (저장 표본 비균등 추출 — 적분 설정 불변) |
| `src/cdu_simul/dataset.py` | 신규 (시나리오 배치 실행 · CSV 생성) |
| `src/cdu_simul/dataset_report.py` | 신규 (파일럿 종료 판단용 요약) |
| `tests/test_session55_gates.py` | 신규 (게이트 — 기준 선기재) |
| `project-overview.md` | 체크리스트 5.5 완료 · 「지금 어디인가」 첫 줄 |
| `model.py`·`plant.py`·`leak.py`·`hydraulics.py`·`assumptions.py` | **건드리지 않았다** |

**세션 5.5 게이트 판정** — 판정 기준은 `tests/test_session55_gates.py` 머리말에
**코드보다 먼저** 적었다(커밋 cc790fa).

| 기준 | 판정 |
|---|---|
| A 순서 무관성 (목록을 뒤집어 재실행 · `==`) | **통과** |
| B 단독 재현 (배치 문맥이 새는가 · `==`) | **통과** |
| C 정상 복귀 (누출 뒤 정상값 오염 · `==`) | **통과** |
| D 전 행 표기 (프로그램 확인) | **통과** |

→ **세션 5.5 게이트 통과.** 표본은 전량 1,792 중 stride 61(약 30개)이고, 표본이
정상상태·전이·세 CDU 구성·네 누출 수준을 모두 덮는지도 테스트가 확인한다.

**데이터셋** (가정값 기반 · 실측 아님)

| 항목 | 값 |
|---|---|
| 시나리오 | 1,792 (정상상태 768 · 전이 1,024) |
| 행 | 310,016 |
| 열 | 48 |
| 파일 크기 | 304.6 MB |
| 소요시간 | 27.5분 (5.5-A 추정 36분) |
| solver 실패 행 | 0 |

출력은 `results/cdu_dataset.csv` 이고 `.gitignore` 대상이다 — **커밋하지 않았다.**

**이 판에서 스스로 판단해 조정한 것 — 사람의 확인이 필요하다**

1. **다중 CDU 전이의 저장 표본**: `plant.integrate_plant_load_step` 이 자기
   `t_eval`(균등 2001점)을 쓰는데 **`plant.py` 가 이 판의 범위 밖**이라, 단일 CDU 와
   시간 격자를 맞추려고 `dataset.py` 에서 비균등 격자에 **가장 가까운 점을 골랐다**
   (보간하지 않는다). 이 조정이 없었을 때 행 수가 합의 규모의 **3.5배**(109만 행)로
   나왔다. 근본 해결은 `plant.py` 의 `t_eval` 을 `storage_times_s` 로 바꾸는 것이다.
2. **다중 CDU 전이의 자극**: 처음에 `load_before == load_after` 로 두었더니 계가
   정상상태에서 출발해 아무것도 하지 않는 **평평한 「전이」 행**이 나왔다 — 그런 행으로
   학습하면 누출이 전이를 만들지 않는다고 배운다. **CDU A 에 부하 스텝**을 주고 누출은
   **t=0 이전부터 있는 조건**으로 바꿨다. 단일 CDU 전이는 누출이 **스텝으로** 들어오고
   다중 CDU 전이는 **기왕에 있는 조건**이라는 차이가 생겼다 — `cdu_config` 로 구분해
   읽어야 한다. `PlantLoadStepCase` 가 CDU 마다 다른 템플릿을 받지 않아 누출이 두 CDU
   **모두**의 랙 0 에 걸린다.
3. **비대칭 부하의 거울상 중복**: 부하 축이 2수준뿐이라 (100,20)과 (20,100)이 서로
   거울상이다 — 정보로는 중복이지만 **사람이 정한 규모를 코드가 줄이지 않았다.**

**기록만 — 미해결이 아니다** (파일럿 밖이거나 본 프로젝트의 몫)

· **펌프 스타트업·밸브 개도 변화 등 과도 시나리오는 파일럿 밖이다.** 5장이 아무것도
  주지 않아 새 숫자가 최소 넷 필요하다(N(t) 프로파일 · x(t) 프로파일 · 밸브
  rangeability 또는 최소 Kv · 배관 관성 L/A). 절대 규칙 1에 걸린다
· **quasi-steady 가정(규칙 4)이 기동에서 성립하지 않는다** — 유체 관성 시간상수가
  0.5초 안팎으로 추정되어 열 시간상수 0.72초(M 하한)와 같은 자릿수다
· **실 누출감지 AI의 오탐 주요 원인이 기동·정지 과도**이므로, 본 프로젝트에서
  설계데이터 확보 시 우선 확장 대상이다

**다음이 알아야 할 것**

1. **파일럿 종료 판정은 사람이 한다.** `python -m cdu_simul.dataset_report` 가
   `project-overview.md` 「파일럿 종료 조건」이 볼 것으로 적은 넷에 답하는 재료를 낸다.
2. **위 「스스로 판단해 조정한 것」 셋을 먼저 확인해 달라.** 특히 2번은 데이터셋의
   의미를 바꾼다.
3. **미해결 #21·#24·#31·#32 는 열려 있다.** 각각 데이터셋의 어느 열에 영향하는지를
   요약 리포트가 적었다.

### 세션 5.5-C · 2026-08-29 · 문서 정합 · 미해결 정리 · 브리핑 축약

**문서·위생 판이다. feasibility 판정은 하나도 하지 않았다.** 모델 코드·기존
테스트·데이터셋을 건드리지 않았다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `project-overview.md` | 체크리스트 3·4·5 를 완료로 · 「지금 어디인가」 두 줄 교체 (사람의 지시) |
| `CLAUDE.md` | 「작업 시작 전 필독」 1항의 로컬 경로 표기를 **PC 독립**으로 (사람의 지시) |
| `daily_brief.py` | 축약 출력 · **열린 미해결 전문 보장** · `--full` 플래그 |
| `PROCEED.md` | 미해결 #5·#6·#26 닫음 · #11·#18·#19 문구 정정 · #35 신설 · 이 로그 |
| `collaboration.md`·`AI서버_…정리.md`·`src/`·`tests/`·`results/`·`pyproject.toml` | **건드리지 않았다** |

**미해결 정리**

| 항목 | 처리 | 근거 |
|---|---|---|
| #5 5장 표 자기정합 | **닫음** | 세션 5-B 재계산 ΔT = 10.3008 K · 1차측 32.17/42.47℃ 가 5장 32/42℃ 와 정합 |
| #6 한글 파일명 | **닫음** | 새 PC clone 에서 확인 — UTF-8 NFC 정상 저장·내용 정상. 8진 이스케이프는 `core.quotepath` 기본값에 따른 **표시 문제**. `core.quotepath false` 는 개인 설정이라 저장소에 넣지 않는다 |
| #26 세션 5.5 신설 | **닫음** | 5.5-A·5.5-B 로 이행 완료 |
| #11 커밋 작성자 | 문구 정정 (**열림 유지**) | 한 PC 의 값만 적고 있었다. 작업 PC 가 둘이므로 **PC 를 새로 붙일 때마다 확인하는 상시 항목**으로 바꿨다 |
| #18 `REFERENCE_PRESSURE_Pa` | 문구 정정 (**열림 유지**) | 5-1 을 「잠정」으로 인용하고 있었으나 **세션 5 에서 확정으로 바뀌었다**. 주석 자체는 모델 코드라 이 판에서 손대지 않았다 |
| #19 `heat_capacity_ratio` | 문구 정정 (**열림 유지**) | **코드로 확인했다** — 세션 5-B 가 Cr=1 선언을 제거해 「(Cr=1)」 형태는 없다. 그러나 이름은 `hx_effectiveness_counterflow` 의 인자로 남아 있고(값은 물성에서 유도), Cr 유도 규약의 인용이 「세션 5-B」이지 5-1 의 행이 아니다. 그래서 **닫지 않았다** |
| #35 의존성 버전 미고정 | **신설** | C7 — `pyproject.toml` 은 **고치지 않았다**. 사람이 정한다 |

**의존성 버전 기록** (이 PC · 2026-08-29 시점 — 재생성 시 대조용, 미해결 #35)

| 구분 | 패키지 | 버전 |
|---|---|---|
| 런타임 | `numpy` | 2.5.2 |
| 런타임 | `scipy` | 1.18.1 |
| 런타임 | `CoolProp` | 8.0.0 |
| 런타임 | `pandas` | 3.0.5 |
| 인터프리터 | Python | 3.12.10 |
| 개발 | `pytest` 9.1.1 · `ruff` 0.16.5 · `mypy` 2.3.1 | (수치 무관) |

**`daily_brief.py` 축약 — 자르는 우선순위를 뒤집었다**

세션 5 이전 브리핑은 「현재 상태」 표와 마지막 세션 로그를 **통째로** 싣고 열린
미해결만 **88자에서 잘랐다**. 그 결과 미해결 #34 의 선택지 (가)·(나) 처럼 *사람이
정해야 할 것* 이 브리핑에서 사라졌다. 지금은 반대다.

| 모드 | 내용 | 길이 |
|---|---|---|
| 기본(축약) | 한 줄 현재 위치+다음 할 일 · 「현재 상태」 최근 2판만 상세(나머지 한 줄) · 마지막 세션 로그의 **「다음이 알아야 할 것」 절만** · **열린 미해결 전문** · 참조 한 줄 | **133줄 / 12,922자** |
| `--full` | 세션 5.5-B 까지와 같은 전문(「현재 상태」 절 전체 + 마지막 세션 로그 전체) + **열린 미해결 전문** | **211줄 / 25,070자** |
| (참고) 세션 5.5-B 까지 | 전문 + 미해결 **88자 절단** | 139줄 / 16,178자 |

길이는 이 로그를 포함한 **최종 `PROCEED.md` 기준**으로 다시 잰 값이다 — `--full` 은
마지막 세션 로그를 통째로 싣기 때문에 이 표 자체가 길이에 들어간다. 139줄/16,178자는
**구 스크립트를 구 `PROCEED.md` 에 돌린** 값이라 직접 비교 대상이 아니다.

**열린 미해결은 두 모드 모두 전문이고, 절단 코드(`SUMMARY_MAX_CHARS`)는 제거했다.**
#34 의 (가)·(나)·⑷ 가 축약형에서 끝까지 보이는 것을 출력으로 확인했다.

**`PROCEED.md` 파싱 규칙이 바뀌었다** (모듈 docstring에도 적었다)

- (신규) 「현재 상태」 표의 **행을 셀 단위로 읽는다** — 셀 안의 `\|`(escape 된 파이프,
  예 `\|잔차\|`)는 구분자로 세지 않는다.
- (신규) 마지막 세션 로그 블록에서 `**다음이 알아야 할 것**` 문단을 찾아 그 뒤를
  축약형에 싣는다. **못 찾으면 블록 전체**를 싣는다(조용히 비우지 않는다).
- (삭제) 열린 미해결의 88자 절단.
- 기존 규칙(절 경계 `## `, 마지막 `### ` 블록, 닫힘 표시 `[해소`·`[닫힘`·`[확정`·
  `**해소됨**`)은 그대로다.

**검사기** — pytest **1,153건** · ruff · mypy 전부 통과. 세션 5.5-B 와 같은 건수다
(모델 코드·테스트를 건드리지 않았으므로 같아야 한다).

**이 판에서 판정한 feasibility 기준: 없음.**

**프로젝트 지식 재업로드 필요** — `CLAUDE.md` · `project-overview.md` 2종.
`PROCEED.md` 는 대상이 아니다.

**다음이 알아야 할 것**

1. **파일럿의 판은 전부 끝났다.** 남은 것은 사람의 판정 둘 — 미해결 **#34**(다중
   CDU 전이의 누출 주입 지점: (가) 재생성 · (나) 해당 행 제외)와 **파일럿 종료
   판정**. 그 전에는 새 판을 열지 않는다.
2. **미해결 #35 를 사람이 정해야 한다** — `pyproject.toml` 에 런타임 4종을 고정할
   것인가, 기록만 하고 재생성 시 대조할 것인가.
3. **브리핑 기본이 축약형이 됐다.** 게이트 근거 수치와 과대해석 금지 문구는
   축약형에 없다 — `--full` 이나 `PROCEED.md` 를 직접 본다.

### 세션 5.5-D · 2026-08-29 · 다중 CDU 누출 주입 지점 정정 · 데이터셋 재생성

**미해결 #34 를 닫은 판이다.** 사람이 선택지 **(가)** 를 골랐다 — `plant.py` 를
고치고 `leak_cdu_index` 열을 추가한 뒤 재생성한다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/plant.py` | `plant_case_from_templates` 신설 · `PlantLoadStepCase.templates` · 저장 격자를 `storage_times_s` 로 |
| `src/cdu_simul/dataset.py` | `LEAK_CDU_INDEX` · `leak_cdu_index` 열 · 전이에 CDU별 템플릿 · `_storage_indices` 제거 |
| `src/cdu_simul/dataset_report.py` | 게이트 표 2행 갱신 · 열린 한계 2건 추가 |
| `tests/test_session55d_gates.py` | 신규 (정정 확인 22건 — 기준 선기재) |
| `project-overview.md` | 머리말 「최종 갱신」 · 「지금 어디인가」 로컬 경로 (사람의 지시) |
| `model.py`·`dynamics.py`·`leak.py`·`hydraulics.py`·`assumptions.py`·`dataset_plan.py` | **건드리지 않았다** |
| `CLAUDE.md`·`collaboration.md`·`AI서버_…정리.md` | **건드리지 않았다** |

**C1 — 5-1 에 새 행이 필요한가: 필요 없다(범위 안)**

| 물음 | 판단 |
|---|---|
| 누출점을 1개로 줄이는 것이 새 규약인가 | **아니다.** 5-1 「누출 주입 지점」이 이미 "**랙 1개**에 주입한다"이다. 종전 다중 전이가 **2개**여서 규정 **밖**이었고, 이 판은 코드를 **규정 안으로 되돌린 것**이다. 규정을 어기던 코드를 고치는 데 새 규정은 필요 없다 |
| 어느 CDU 인지 고르는 것이 새 숫자인가 | **아니다.** 5-1 이 랙 번호에 쓴 논리(대칭이므로 번호 선택이 새 숫자를 만들지 않는다)를 그대로 쓴다. CDU 사양은 전부 같다(5-1 「CDU 대수」). 게다가 **정상상태 경로는 세션 5.5-B 부터 이미 CDU 0 에만 걸고 있었다** — 전이를 거기에 맞춘 것이다 |
| 그래도 남는 것 | **대칭 논리가 전이에서는 완전하지 않다.** 비대칭 전이에서 CDU A 는 **부하가 스텝하는** 쪽, B 는 **불변인** 쪽이라 **역할이 다르다**. 누출을 A 에 고정하면 「누출이 정지 상태의 CDU 에 있고 옆 CDU 가 부하 변동을 겪는」 조합이 데이터셋에 없다. 이것은 새 가정이 아니라 **한계**이므로 5-1 이 아니라 `dataset_report` 열린 한계에 적었다 |

**C3 — 저장 격자 변경 후 세션 5 결과** (적분 설정 RK45·rtol·atol·구간 불변)

| 항목 | 종전 | 지금 |
|---|---|---|
| `t_eval` | 균등 2001점 | `storage_times_s` 201점 (`dynamics` 와 **같은 함수**) |
| `dataset.py` 의 보정 | 최근접 점 선택(`_storage_indices`) | **제거** |
| 저장 시각의 목표 대비 오차 | 최대 **0.005 τ** | **0** (정확히 같은 시각) |
| 데이터셋 시각 표본 수 | 201 | 201 (**불변**) |

`python -m cdu_simul.plant` 출력 표가 **바이트 단위로 동일**하다(`diff` 무차이).
`t_eval` 은 `solve_ivp` 의 스텝 선택에 영향하지 않고 dense output 을 어느 시각에서
평가할지만 정하므로 해가 이동하지 않는다. **세션 5 게이트 판정 불변** — 세션 5
게이트 테스트 450건(`test_session5_gates.py`·`test_gates_after_plant.py`)이 그대로
통과했다.

**C5 — 데이터셋 재생성** (가정값 기반 · 실측 아님)

| 항목 | 세션 5.5-B | 세션 5.5-D |
|---|---|---|
| 시나리오 | 1,792 | **1,792** (불변 — 사람이 정한 규모) |
| 행 | 310,016 | **310,016** (불변) |
| 열 | 48 | **49** (`leak_cdu_index` 추가) |
| 파일 크기 | 304.6 MB | **305.1 MB** |
| 소요시간 | 27.5분 | **61.3분** |
| solver 실패 행 | 0 | **0** |

**소요시간이 2.2배다 — 코드 때문이 아니라 PC 가 다르다.** 5.5-B 는 다른 PC 에서
돌았고 이 판은 새 PC 에서 돌았다. 이 판의 변경은 오히려 일을 **줄인다**(최근접 점
탐색 제거). 중단 조건(3배 = 82.5분)에는 걸리지 않았다. **두 판의 실행시간을 나란히
읽지 않는다** — 미해결 #35(의존성 버전 미고정)와 같은 이유로 PC 간 비교는 성립하지
않는다.

검증(`pandas` 로 전량 확인): 누출 있는 행의 `leak_cdu_index` 는 **전부 0**,
누출 없는 행은 **전부 빈 값**. 전이 시각 표본은 시나리오·CDU 마다 **201점 균일**.
표기 5열에 빈 칸 **0개**. `results/` 는 `.gitignore` 대상이라 커밋하지 않았다.

**C7 — 게이트 재판정 · 연동이 전이에 나타나는가**

앞 게이트 1-B·2·3·4·5 와 세션 5.5 게이트를 **판정 기준·허용오차를 하나도 바꾸지
않고** 다시 돌려 전부 통과했다(pytest 전량).

**이 판의 목적 — 부하가 그대로인 CDU B 가 A 의 누출에 반응하는가**
(범위 하단 조합 · A 20→100% 스텝 · B 20% 고정 / B 100% 고정 두 경우)

| 누출 | B 2차측 몫 변화 [L/s] | B 환수온도 변화 [K] (B=100%) | B 환수온도 변화 [K] (B=20%) |
|---|---|---|---|
| +5% | +2.130e-03 | −1.413e-03 | −1.419e-04 |
| +20% | +8.198e-03 | −5.437e-03 | −5.457e-04 |
| +50% | **+1.908e-02** | **−1.064e-02** | −1.268e-03 |

**세션 5 C7(정상상태·양쪽 100%)은 +50% 에서 B 가 −0.012649 K · 배분 +0.01905 L/s
였다.** 방향이 같고 **배분 이동은 세 자리까지 일치**한다(+0.01908 vs +0.01905).
온도 변화가 B=20% 에서 10배 작은 것은 B 의 ΔT 자체가 10배 작기 때문이다 — 같은
Cr 변화가 더 작은 K 변화로 나타난다.

**종전(양쪽 누출)에서는 상쇄됐다** — 같은 +50% 에서 배분 이동이 **+1.9e-05 L/s**
(≈0, A만일 때의 1/1000)이고 B 의 환수온도는 **오히려 +0.021 K 올랐다**(B 자신에게도
누출이 있었으므로). #34 가 말한 상쇄가 수치로 확인됐고, 이제 사라졌다.

**C4 의견 — 다중 CDU 전이도 누출을 스텝으로 넣는 편이 나은가 (사람이 정한다)**

**낫다고 본다. 다만 이 판에서 바꾸지 않았다.**
- **근거 ⑴** 데이터셋의 목적이 **누출 조기감지**(1장)다. 감지 모델이 배워야 할 것은
  「누출이 시작될 때의 파형」인데, 지금 다중 전이 행에는 그 파형이 없다 — 누출은
  t=0 이전부터 있고 움직이는 것은 부하다. 누출 스텝 파형은 `single` 행에만 있다.
- **근거 ⑵** 두 전이의 자극이 달라 `regime=transient` 를 한 덩어리로 읽을 수 없다.
  308,736 전이 행 중 205,824 행(67%)이 다른 자극이다.
- **반대 근거** 부하 스텝을 없애면 **연동을 보는 자극도 없어진다.** 5.5-B 가 부하
  스텝을 넣은 이유가 그것이었다(자극이 없으면 전이 행이 평평해진다). 누출 스텝만
  주면 A 의 누출이 A 의 유량을 바꾸고 그것이 배분을 움직이므로 연동은 **여전히
  생긴다** — 다만 크기가 부하 스텝보다 훨씬 작다.
- **가장 나은 안(제안)**: 부하 스텝 행은 그대로 두고 **다중 CDU 누출 스텝 행을
  따로 추가**한다. 규모가 늘어나므로 **사람이 정할 일이다**(규모는 사람의 결정).

**검사기** — pytest **1,175건**(종전 1,153 + 신규 22) · ruff · mypy 전부 통과.
**지운 테스트는 없다.**

**이 판에서 판정한 feasibility 기준** — 세션 5.5 게이트(A·B·C·D)와 앞 게이트
1-B·2·3·4·5. **전부 재판정 통과.** 새 기준을 만들지 않았고 허용오차도 바꾸지 않았다.
`test_session55d_gates.py` 의 넷은 **게이트가 아니라 정정의 확인**이다.

**프로젝트 지식 재업로드 필요** — `project-overview.md` 1종.
(`AI서버_…정리.md` 는 **고치지 않았다** — C1 판단이 「범위 안」이었다.)

**다음이 알아야 할 것**

1. **미해결 #34 가 닫혔다.** 파일럿 종료 판정을 막던 항목이 사라졌다 — 종료 판정
   재료가 완결됐다. **판정 주체는 사람이다**(`project-overview.md` 「파일럿 종료 조건」).
   `python -m cdu_simul.dataset_report` 가 재료를 낸다.
2. **C4 의견에 답이 필요하다** — 다중 CDU 전이도 누출을 스텝으로 넣을 것인가.
   추가하면 규모가 늘고 재생성이 다시 필요하다(이 PC 기준 약 1시간).
3. **데이터셋을 재생성했다면 `leak_cdu_index` 열이 있는 49열본인지 확인한다.**
   48열본(5.5-B)과 섞어 읽으면 안 된다 — 다중 전이 행의 **누출점 자체가 다르다**.
4. **미해결 #35 는 여전히 열려 있다** — 의존성 버전이 고정돼 있지 않다. 이 판의
   재생성이 5.5-B 와 실행시간이 2.2배 다른 것도 PC 가 다르기 때문이다.

**사람이 정한 것 — 세션 5.5-D 마무리** (기록이다. 미해결이 아니다)

**⑴ 파일럿 종료 판정 전에 세 판을 더 한다.** 판정 순서가 「판정 → (전환이면)
설계데이터 투입」이므로, **지금 자력으로 확인 가능한 것을 남긴 채 판정하면 비싼
데이터 투입 뒤에 무용지물임이 드러날 위험**이 있다.

| 판 | 내용 | 데이터셋 |
|---|---|---|
| **5.6** | 누출 질량손실 **부호 확인** (수력 한정) | **바꾸지 않는다** |
| **5.7** | 미해결 **#31 시간축 정정** (M 을 8랙으로) | **재생성** |
| **5.8** | **이동지연 감도** (N-CSTR 스윕) | 바꾸지 않는다 · **관측만** |

**⑵ 정지선 — 위 셋이 파일럿이 자력으로 갈 수 있는 끝이다.** 그 밖의 항목
(**#21** M 결손 · **#25** 등가길이 오차 · **#24** 잔여저항 분해 · 고정 NTU ·
2차측 온도 결합)은 **전부 설계데이터가 있어야 하며 판정을 막지 않는다.**

**⑶ 절대 규칙 8 예외 승인 (사람).** 누출을 K값 근사가 아닌 방식(**유량 손실**)으로
모사하는 것을 **부호 확인 목적의 별도 코드에 한해** 허용한다. 데이터셋과 5-1 의
누출 정의는 **K값 근사 그대로 둔다.** **규칙 8 자체를 바꾸지 않는다.**

**⑷ #31 배분 규칙 확정 (사람).** M 의 양 끝(전부 25A 하한 · 전부 80A 상한)을
**둘 다 8배**로 읽는다. 5-1 이 이미 두 극단으로 쓰므로 각각을 8벌로 읽는 것이며
**추가 숫자가 0**이다.

**⑸ 파일럿이 끝내 답하지 못하는 것** — 정확도(실측 없음) · 신호가 실측 가능한
크기인가(계측기 사양 없음) · 잔여저항 60~83% 가 실제 CDU 에서도 그런가(설계값).
**세 판을 다 해도 판정은 "정확하다"가 아니라 "이 구조로 설계데이터를 넣을 만하다"
까지다.**

**물리 검산 기록 (세션 5.5-D 마무리)**

**웹 대화창이 로그 수치를 손으로 대조한 것이다 — 이 판이 코드를 돌려 낸 값이
아니다.** 즉 **독립적인 재계산이 아니라 기록된 수치들 사이의 정합성 확인**이다.

· ΔT 가 32조합에서 **10.29 K 로 균일** — Q/(ṁcp) 손계산 10.30 K 와 일치.
  2차측·NTU 와 무관해야 하는 것이 그대로 나왔다
· 2차측 27→30℃ 에서 1차측이 **+2.9937~+2.9964 K 평행이동** (Q·ε 고정 시 강체 이동)
· NTU 2→3 에서 **ε 비 1.125 · 접근온도 비 1.125** — ΔT_max ∝ 1/ε 성립
· 대향류 Cr≈1 의 접근온도 균일: **저온단 5.17 K · 고온단 5.14 K**
· 누출 3수준에서 유량 감소율과 랙 온도상승이 **ΔT_rack=q/(ṁcp)** 로 상호 정합
· 질량보존: **(1/8)(누출랙) + (7/8)(타랙) = 총유량 변화**
· K 비 **1:4:10** 에 유량 신호 비 **1:3.80:8.68** — **Q ∝ 1/√C** 성립
· 양정 신호: 펌프곡선 **dH/dQ = −0.306 mAq/(L/s)** × 총유량 변화 = **+0.00130 mAq**,
  보고값과 **5자리 일치**
· τ 비 **12.79~12.82** 대 M 비 **12.83** — 6장 재정의 기준 **τ ∝ M** 성립
· 연동: 정상상태 대수해 **+0.01905 L/s** 대 전이 적분 **+0.01908 L/s**, 세 자리 일치

→ **설명되지 않는 수치는 없다.** 어색해 보이는 다섯(유속 3.48 m/s · 잔여저항
60~83% · 양정 신호 5e-5 · τ 0.72~9.2 s · 총유량 신호 0.16~0.37%)은 원인이 **전부
5장 가정치 또는 기록된 결손에 있다.**

### 세션 5.6 · 2026-08-30 · 누출 질량손실 부호 확인 (수력 한정) — 판정 기준 선기재

> **이 절은 코드보다 먼저 적고 먼저 커밋한다** — 세션 3-B 이후 원칙이며 세션 4
> (커밋 `803142e`) · 세션 5 (`acbc31c`) · 세션 5.5 (`cc790fa`) 와 같은 방식이다.
> **아래 기준을 결과에 맞춰 고치지 않는다.**

**절대 규칙 8 예외 (사람 승인 · 「사람이 정한 것 — 세션 5.5-D 마무리」⑶)**
이 판에 한해 누출을 **유량 손실**로 모사하는 것을 **별도 코드에서** 허용한다.
규칙 8 문구 · 데이터셋 · 5-1 「누출 시나리오」 정의는 **하나도 바꾸지 않는다.**
이 판의 코드는 데이터셋 생성 경로에 **들어가지 않는다**(어느 모듈도 import 하지
않는다). 기존 모듈은 **읽기만** 한다.

**이 판은 수력 한정이다.** 열·온도를 보지 않는다. 이유는 시간이 아니라 **절대
규칙 1**이다 — 빠져나간 유체를 보충수로 채우면 **보충수 온도**라는 5장에 없는
숫자가 필요해진다. 수력만 보면 그 숫자가 필요 없다. 물성은 정격 운전점의 1차측
벌크평균(37℃ · `rated_property_temperature_C()`) 하나로 고정한다 — 온도가 움직이지
않으므로 5-1 「수력 계산의 물성 평가 온도」 규약과 어긋나지 않는다.

#### 무엇을 대조하는가

네 양의 **부호**를 K 근사와 질량손실에서 각각 표로 낸다. 전부 **같은 수력 조합의
정상(누출 없음) 해 대비 차이**다 — 조합 안에서만 비교한다.

    ⑴ 펌프 총유량   ⑵ 펌프 양정   ⑶ 누출랙 콜드플레이트 통과유량   ⑷ 타 7랙 유량

#### 가설 (예단하지 말 것)

K 근사는 저항을 늘리므로 총유량이 줄고 양정이 오른다. 질량손실은 대기압으로
빠지는 **병렬 경로가 열리는 것**이라 계통 저항이 오히려 줄어 총유량이 늘고 양정이
내릴 수 있다 — 즉 ⑴⑵ 의 부호가 정반대일 수 있다. **이것은 가설이다. 결과를 여기에
맞추지 않는다. 반대로 나오면 반대로 적는다.**

#### 구조 자유도 — 5장·5-1 이 정하지 않는 둘 (코드를 읽어 먼저 적는다)

질량손실을 넣으려면 **누출점의 상류와 하류를 구분해야 한다.** 현재 모델은 공통
경로 저항을 **집중저항 하나**(계통 잔여저항 `C_res`)로 두고, 5-1 이 그것을 **분해
하지 말라고 못박고 있다**(「HX·CDU 내부·헤더로 분해하지 않는다」 · 미해결 #24).
그래서 다음 둘이 **5장에도 5-1 에도 없다**:

- **g — 잔여저항 중 환수측(누출점 → 펌프) 몫** [0, 1]. `g=0` 이면 누출점 하류에
  저항이 없어 질량손실이 **수력적으로 아무 흔적도 남기지 않는다**. `g=1` 이면
  최대로 남는다.
- **펌프가 보는 유량** — 계통 재고(보충수를 모델링하지 않으므로 줄어드는 재고)가
  펌프 **흡입** 쪽에서 채워지면 펌프는 공급유량 ΣQ 를, **토출** 쪽이면 환수유량
  ΣQ−Q_leak 을 본다.

**이 둘에 값을 고르지 않는다**(절대 규칙 1 · §7). 대신 **정의상 양 끝을 포함해
전수로 돌리고, 부호가 그것에 의존하는지를 결과로 적는다.** 분수의 양 끝(0·1)은
측정이 필요한 숫자가 아니므로 새 가정치를 만드는 것이 아니다. 세션 4 가 M 노드
배분 민감도(미해결 #20)를 **시험 코드 안에서만** 40:60·50:50·60:40 으로 돌리고
`assumptions.py` 에 넣지 않은 것과 같은 취급이다 — **이 판의 g 도 `assumptions.py`
에 넣지 않는다.**

#### 누출 위치 — 랙 출구 고정

5-1 「누출 주입 지점」이 누출을 **랙 1개 단위**로 정의했고 모델의 랙 분기는 집중
저항 하나이므로, 출구는 그 정의를 쪼개지 않는 지점이다. 분기 저항의 상류/하류를
나누면 5장에 없는 **배관 형상 선택**을 새로 만드는 것이 된다(5-1 빈칸 처리 순서
4번의 대상이며 방침 B 의 대상이 아니다 — 방침 B 는 5장이 범위로 준 값에만 쓴다).

> **먼저 적어 두는 구조적 사실**: 5-1 은 헤더를 **저항 0 의 공통 노드**로 둔다.
> 그러면 랙 0 의 출구 노드와 나머지 7랙의 출구 노드는 **같은 노드**다. 즉 이
> 모델에서 질량손실 누출은 **랙에 국소화될 수 없다.** ⑶ 과 ⑷ 가 정확히 같이
> 움직일 것으로 예상되며, 그렇게 나오면 그것이 K 근사와의 가장 큰 구조 차이다.
> **예상이지 판정이 아니다** — 코드가 낸 값으로 확인한다.

#### 누출 크기 — 새 값을 정하지 않는다

K 근사 3수준(+5/+20/+50%)이 만든 **누출랙 통과유량 감소량**을 코드가 조합마다 먼저
산출하고, **0 부터 그 상한까지 스윕**한다. 부호는 크기와 무관하게 정해지므로 크기를
고를 필요가 없다.

#### 판정 기준

- **기준 A — 부호 표.** ⑴~⑷ 를 두 방식에서 각각 낸다. 부호가 갈리면 갈린다고
  적는다. 어느 쪽이 "옳다"고 적지 않는다 — 이 판은 관측이다.
- **기준 B — 반례 확인**(collaboration.md ⑥). **누출 크기 × 양정 양 끝 × 분기 ΔP
  양 끝 × 밸브 ΔP 양 끝 × 구조 자유도(g · 펌프 유량 위치)** 전 조합에서 부호가
  **뒤집히는 경우가 있는지** 반드시 같이 돌리고 적는다. "전 조합에서 일정"과
  "조건에 따라 갈린다"는 결론의 무게가 다르다.
- **기준 C — 격리 확인.** 누출 크기 0 에서 질량손실 해가 K 근사의 정상 해와
  **부동소수점까지** 같아야 한다. 다르면 코드가 정상 경로를 건드린 것이다.
- **기준 D — solver**(절대 규칙 5). 전 조합의 `fsolve` `ier` 를 확인해 결과에
  싣는다. 실패 조합이 있으면 **버리지 않고 표시한다.**

#### 이 판이 판정하지 않는 것 (과대해석 금지)

- **6장 기준 넷을 하나도 보지 않았다** — energy balance · T_return 방향성 ·
  수렴시간 · 극단 케이스. 열모델이 이 판에 없다.
- **어느 누출 모사가 실제에 가까운가** — 실측이 없다. 두 방식의 부호를 나란히
  놓을 뿐이다.
- **계통이 비어가는 것** — 보충수를 모델링하지 않으므로 재고 감소는 시간축 문제이고
  이 판 범위 밖이다.
- **부호가 갈려도 모델을 고치지 않는다**(§7). 관측으로 기록하고 미해결 목록에
  올린다. 기존 데이터셋·5-1 누출 정의를 이 결과에 맞춰 손대지 않는다 — **사람 몫이다.**

#### 결과 (2026-08-30 · 위 기준 그대로 판정했다. 기준을 고치지 않았다)

**돌린 것이다 — 표의 모든 수치는 `python -m cdu_simul.leak_massloss` 실행 결과다.**
손계산은 아래 「물리 검산」 넷뿐이며 그 자리에 표시했다.

**[표 1] K 근사 — 정상 대비 변화 (8조합 × 3수준 범위)**

| 양 | 누출 +5% | 누출 +20% | 누출 +50% | 부호 |
|---|---|---|---|---|
| ⑴ 랙 통과 총유량 [L/s] | −6.405e-03 ~ −2.843e-03 | −2.468e-02 ~ −1.094e-02 | −5.755e-02 ~ −2.546e-02 | **−** |
| ⑴' 펌프 통과유량 [L/s] | (⑴ 과 같다) | (⑴ 과 같다) | (⑴ 과 같다) | **−** |
| ⑵ 펌프 양정 [mAq] | +1.305e-03 ~ +1.960e-03 | +5.018e-03 ~ +7.551e-03 | +1.166e-02 ~ +1.761e-02 | **+** |
| ⑶ 누출랙 통과유량 [L/s] | −2.163e-02 ~ −1.235e-02 | −8.230e-02 ~ −4.798e-02 | −1.877e-01 ~ −1.135e-01 | **−** |
| ⑷ 타 7랙 유량 [L/s] | +1.178e-03 ~ +2.445e-03 | +4.574e-03 ~ +9.301e-03 | +1.081e-02 ~ +2.122e-02 | **+** |

K 근사에서는 계통 밖으로 나가는 유량이 없으므로 ⑴ 과 ⑴' 이 같은 값이다.

**[표 2] 질량손실 — 정상(Q_leak=0) 대비 부호 (8조합 × 크기 4수준 × 배치 6)**

누출 크기는 조합마다 0 부터 **K 근사 +50% 가 만든 누출랙 유량 감소량**(0.113515 ~
0.187732 L/s)까지 스윕했다 — 새 숫자가 0이다.

| 배치 (구조 자유도) | ⑴ 총유량 | ⑴' 펌프유량 | ⑵ 양정 | ⑶ 누출랙 | ⑷ 타 7랙 |
|---|---|---|---|---|---|
| g=0.0 / 펌프=공급 | **0** | **0** | **0** | **0** | **0** |
| g=0.0 / 펌프=환수 | + | − | + | + | + |
| g=0.5 / 펌프=공급 | + | + | **−** | + | + |
| g=0.5 / 펌프=환수 | + | − | + | + | + |
| g=1.0 / 펌프=공급 | + | + | **−** | + | + |
| g=1.0 / 펌프=환수 | + | − | + | + | + |

**어느 칸에도 `갈림` 이 없다.** 배치를 고정하면 부호는 **누출 크기 4수준 × 양정 양
끝 × 분기 ΔP 양 끝 × 밸브 ΔP 양 끝 전 조합에서 뒤집히지 않는다**(기준 B). 부호를
가르는 것은 5장 가정치의 범위가 아니라 **배치(구조 자유도)** 다.

크기 범위(참고 · 배치별 최대):
g=1.0/펌프=공급 에서 ⑴ +1.673e-02 ~ +1.326e-01 L/s · ⑵ −6.110e-02 ~ −2.559e-03 mAq
· ⑶=⑷ +2.091e-03 ~ +1.657e-02 L/s.

#### 관측 넷 (결론이 아니라 관측이다 — 모델을 고치지 않았다)

**① ⑴ 과 ⑶ 의 부호가 정반대다.** K 근사는 총유량이 **줄고** 누출랙 유량이 **줄지만**,
질량손실은 (배치가 무신호인 경우를 빼면) 총유량이 **늘고** 누출랙 통과유량도 **는다**.
§5 의 가설이 이 두 양에서는 맞았다 — 질량손실은 대기압으로 빠지는 **병렬 경로**라
계통 저항이 줄어든다. **가설에 맞춰 고른 결과가 아니다**: ⑵ 는 아래처럼 가설과
다르게 나왔고 그대로 적었다.

**② ⑵ 펌프 양정의 부호는 5장·5-1 이 정하지 않는다.** 펌프가 **공급유량**을 보면
양정이 내리고(가설대로), **환수유량**을 보면 양정이 **오른다**(K 근사와 같은 부호).
줄어드는 계통 재고가 펌프 흡입 쪽에서 채워지는가 토출 쪽인가는 5장에도 5-1 에도
없다 — **양정만으로는 두 누출 모사를 구분할 수 없다.**

**③ 질량손실 누출은 이 모델에서 랙에 국소화되지 않는다.** 랙 간 유량 비대칭이
전 조합에서 최대 **2.220e-16 L/s**(부동소수점 잡음)였다. 5-1 이 헤더를 **저항 0 의
공통 노드**로 두므로 랙 0 의 출구 노드는 나머지 7랙의 출구 노드와 **같은 노드**다.
그래서 ⑶ 과 ⑷ 가 **정확히 같이** 움직인다 — K 근사가 ⑶↓ / ⑷↑ 로 갈라지는 것과
**구조가 다르다**. 데이터셋의 랙별 신호(어느 랙이 새는가)는 K 근사가 만들어 낸
것이고, 질량손실 쪽에는 대응물이 없다.

**④ 배치 g=0 / 펌프=공급 에서는 질량손실이 수력적으로 아무 흔적도 남기지 않는다**
(다섯 양 전부 정확히 0). 누출점 하류에 저항이 없고 펌프도 유량 변화를 못 보는
배치다. 즉 **"질량손실이 수력 신호를 만든다"는 것 자체가 잔여저항 배치에 의존한다.**

#### 물리 검산 (손계산 — 위 표의 실행값과 대조했다)

- **랙 간 질량보존**: `H33.6/dPb3/dPv3` · g=1.0/펌프=공급 · 최대 크기에서
  ΔQ_sup = **1.325955e-01** L/s, 8 × ΔQ_rack = **1.325955e-01** L/s — 차 **0.000e+00**.
- **펌프곡선 위 이동**: 같은 점에서 dH/dQ = −(a + 2bQ) = **−0.45890** mAq/(L/s),
  선형항 −0.45890 × 0.1325955 = **−6.0848e-02**, 곡률항 −b·ΔQ² = −0.014615 × 0.1326²
  = **−2.57e-04**, 합 **−6.1105e-02** — 실행값 **−6.110482e-02** 와 일치.
- **g 선형성**: g=0.5 의 ⑴ 최대 +6.616e-02 대 g=1.0 의 +1.326e-01 — 비 **2.004**.
- **격리 확인(기준 C)**: Q_leak=0 의 질량손실 해와 K 근사 정상 해의 랙 유량 최대 차
  **2.220e-16** L/s. 정상 경로를 건드리지 않았다.

#### solver (기준 D · 절대 규칙 5)

`fsolve` **272 회 전부 `ier=1` · 실패 0 건.** 압력평형 최대 잔차는 K 근사 경로
**5.592e-12 mAq** · 질량손실 경로 **2.324e-11 mAq** (양정 20~30 mAq 대비 상대
1e-12 수준). 실패 조합이 없었으므로 버린 결과도 없다.

**기존 테스트**: `pytest` **1,175건 전부 통과**(21분 05초) · `ruff`·`mypy` 통과.
이 판은 테스트를 하나도 더하거나 고치지 않았다 — 새 모듈이 기존 경로를 건드리지
않았음을 기존 테스트가 그대로 확인한 것이다.

#### 이 판이 판정하지 않은 것 (과대해석 금지)

- **6장 feasibility 기준을 하나도 보지 않았다** — energy balance · T_return 방향성 ·
  수렴시간 · 극단 케이스. 이 판에 열모델이 없다. 게이트 판정이 아니다.
- **어느 누출 모사가 실제에 가까운지 판정하지 않았다.** 실측이 없다. 두 방식의
  부호를 나란히 놓았을 뿐이다.
- **신호가 계측 가능한 크기인지 보지 않았다** — 계측기 사양이 없다.
- **계통이 비어가는 것을 보지 않았다** — 보충수를 모델링하지 않았고, 재고 감소는
  시간축 문제라 이 판 범위 밖이다.
- **모델·데이터셋·5-1 을 고치지 않았다.** §7 대로 관측으로만 기록하고 미해결
  **#36** 으로 올렸다. 어느 쪽으로 갈지는 **사람이 정한다.**

#### 만든/바꾼 파일

- **새로**: `src/cdu_simul/leak_massloss.py` (데이터셋 생성 경로 밖 · 어느 모듈도
  import 하지 않는다)
- **고침**: `PROCEED.md` (이 절 · 「현재 상태」 표 · 미해결 #36)
- **건드리지 않음**: `model.py` · `plant.py` · `dynamics.py` · `hydraulics.py` ·
  `leak.py` · `dataset*.py` · `assumptions.py` · 데이터셋 · 문서 4종 · 기존 테스트

#### 보고만 하는 발견 (절대 규칙 13 — 고치지 않았다)

1. `CLAUDE.md` 「명령어」 절에 `python -m cdu_simul.leak_massloss` 가 없다. 넣을지는
   사람이 정한다 — 이 모듈이 상시 실행 대상인지가 먼저다.
2. 세션 5.7·5.8 항목(#18·#19·#22 위생 · M 8배 · `stimulus_kind` · `dataset_version`)은
   이 판에서 **손대지 않았다.** 다음 판의 것이다.

### 세션 5.6-B · 2026-08-30 · 공급·환수 유량 불일치 확인 (5.6 결과 재출력) — 판정 기준 선기재

> **이 절은 코드보다 먼저 적고 먼저 커밋한다** — 세션 3-B 이후 원칙이다.
> **아래 기준을 결과에 맞춰 고치지 않는다.**

**새로 푸는 것이 없다.** 세션 5.6 이 이미 돌린 8조합 × 누출 크기 4수준 × 배치 6
전수를 **다른 양으로 출력**하는 판이다. `leak_massloss.py` 의 계산 로직·solver
설정·스윕 범위·배치 정의를 **하나도 바꾸지 않는다.** 바뀌는 것은 출력부와,
출력에 필요한 값을 모으는 자리뿐이다.

**격리 확인 방법**: 세션 5.6 이 낸 표 1·2·3 이 **바이트 단위로 동일**해야 한다.
표 4 만 새로 붙는다. 동일하지 않으면 계산부를 건드린 것이다.

#### 무엇을 내는가

각 케이스마다 넷을 낸다.

    ⑴ 펌프 공급유량 Q_sup   ⑵ 환수유량 Q_ret (= HX 1차측 입구 유량)
    ⑶ 불일치 Δ = Q_sup − Q_ret  [L/s 와 공급유량 대비 %]
    ⑷ 같은 케이스의 누출 유량 Q_leak

**K 근사 경로도 같은 표로 낸다.** 전 케이스에서 Δ = 0 이 나오는지 확인한다 —
0 이 아니면 그것은 5.6 계산의 결함이므로 **그 자리에서 멈추고 보고한다**(§6).

#### 먼저 적어 두는 것 — Δ 는 구성상 항등식이다 (결과보다 먼저)

`leak_massloss.solve_massloss` 는 환수유량을 `Q_ret = Q_sup − Q_leak` 으로 **정의**
한다. 따라서 Δ = Q_leak 은 **모델이 참이면 성립하는 물리 검산이 아니라, 코드가
정의대로 계산했는지 보는 항등식**이다. 기준 A 가 통과해도 그것이 확인해 주는 것은
"코드가 적힌 대로 계산한다"까지이고 **질량보존이 물리적으로 검증됐다는 뜻이 아니다**
— 애초에 이 모델에는 계통 밖으로 나가는 경로가 하나뿐이라 다른 결과가 나올 수 없다.
같은 이유로 기준 B·C 의 결과도 **대부분 정의에서 따라 나올 것으로 예상한다.**
**예상이지 판정이 아니다 — 코드가 낸 값으로 확인하고, 어긋나면 어긋난 대로 적는다.**

이 판의 실질 내용은 검산이 아니라 **크기(%)와, 그 양이 배치에 어떻게 반응하는가**다.

#### 판정 기준

- **기준 A — 검산.** 질량손실 경로에서 **Δ = Q_leak** 인가(수치오차 범위 내).
  K 근사 경로에서 **Δ = 0** 인가. 부동소수점 잔차를 값으로 적는다.
- **기준 B — 단조.** 누출 크기 4수준에서 Δ 가 **단조 증가**하는가.
- **기준 C — 배치 불변.** Δ 가 **배치 6에 걸쳐 같은가.** 세션 5.6 이 드러낸 구조
  자유도 둘(잔여저항 하류 몫 g · 펌프가 보는 유량 위치)에 이 양이 흔들리는지 본다.
  **가설**: Δ 는 누출 유량 그 자체이므로 배치와 무관할 것이다. **이것은 가설이다.**
  특히 **`g=0 / 펌프=공급` 배치를 반드시 포함해 확인한다** — 세션 5.6 관측 ④ 에서
  다섯 양이 전부 정확히 0 이었던 배치다. 그 배치에서 **Δ 도 0 이면 가설이 틀린
  것이고, Δ 가 0 이 아니면 그 배치의 유일한 생존 신호**라는 뜻이다. **어느 쪽이든
  그대로 적는다.**

#### 하지 않는 것

- **세션 5.6 이 낸 네 양(총유량·양정·누출랙 유량·타 7랙 유량)과 크기를 비교하지
  않는다.** 크기 비교는 계측기 사양이 있어야 뜻이 생긴다(「사람이 정한 것 — 세션
  5.5-D 마무리」⑸).
- **6장 feasibility 기준을 하나도 보지 않는다** — 이 판도 수력 한정이고 열모델이
  없다. 게이트 판정이 아니다.
- **새 숫자를 하나도 만들지 않는다.** 이 판은 새 숫자가 **0 이어야 한다** — 필요해
  지면 멈춘다(절대 규칙 1).
- **모델·데이터셋·5-1·`CLAUDE.md`·미해결 #36 문구를 고치지 않는다.** 5.7 스키마에
  열을 넣을지는 **사람이 정한다** — 이 판은 **재료만** 적는다.

#### 결과 (2026-08-30 · 위 기준 그대로 판정했다. 기준을 고치지 않았다)

**새로 푼 것이 없다.** 세션 5.6 의 272 회 해를 다른 양으로 읽었을 뿐이다. 표의
수치는 전부 `python -m cdu_simul.leak_massloss` 재실행 결과이며, 재실행은 5.6 과
같은 값을 낸다(아래 격리 확인).

**[표 4] 공급·환수 유량 불일치 Δ = Q_sup − Q_ret**

| 경로 · 배치 | Δ [L/s] | Δ/Q_sup [%] | 항등식 잔차 [L/s] | 단조 |
|---|---|---|---|---|
| K 근사 | +0.000e+00 ~ +0.000e+00 | +0.000e+00 ~ +0.000e+00 | 0.000e+00 | 해당없음 |
| g=0.0 / 펌프=공급 | +0.000e+00 ~ +1.877e-01 | 0 ~ **+1.211** | 8.882e-16 | 예 |
| g=0.0 / 펌프=환수 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.209 | 8.882e-16 | 예 |
| g=0.5 / 펌프=공급 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.206 | 8.882e-16 | 예 |
| g=0.5 / 펌프=환수 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.205 | 8.882e-16 | 예 |
| g=1.0 / 펌프=공급 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.202 | 8.882e-16 | 예 |
| g=1.0 / 펌프=환수 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.200 | 8.882e-16 | 예 |

Δ 의 하한 0 은 **누출 크기 0(정상)** 행이다. 누출이 있는 4수준만 보면
`g=0.0/펌프=공급` 배치에서 **+2.838e-02 ~ +1.877e-01 L/s** 다.

#### 판정

**기준 A — 검산: 통과.** 질량손실 경로에서 Δ 와 누출 유량의 차가 전 케이스 최대
**8.882e-16 L/s**(부동소수점 1 ulp 수준), K 근사 경로에서 Δ 가 전 케이스 정확히
**0.000e+00 L/s** 다. K 근사에서 Δ ≠ 0 인 케이스는 **없었다** — §6 의 멈춤 조건에
걸리지 않았다.

> **그러나 이것은 구성상 항등식이다**(판정 기준에 미리 적어 둔 대로).
> `solve_massloss` 가 환수유량을 `Q_ret = Q_sup − Q_leak` 으로 **정의**하고 K 근사
> 에는 유출 경로가 아예 없다. 통과의 뜻은 **「코드가 정의대로 계산한다」까지이고
> 질량보존이 물리적으로 검증됐다는 뜻이 아니다.** 8.882e-16 은 물리가 아니라
> 부동소수점 반올림의 크기다.

**기준 B — 단조: 통과.** 질량손실 **전 배치 · 전 조합**에서 Δ 가 누출 크기 4수준에
대해 **엄격히 증가**한다. K 근사는 누출 크기 축 자체가 없고(축은 K 배율이다)
Δ ≡ 0 이라 기준 B 의 대상이 아니다 — 표에 `해당없음` 으로 적었다.

**기준 C — 배치 불변: 통과. 가설이 맞았다.** 같은 (조합 · 누출 크기)에서 배치 6 에
걸친 Δ 의 최대 편차가 **1.776e-15 L/s** 다. Δ 는 세션 5.6 이 드러낸 구조 자유도
둘(잔여저항 하류 몫 g · 펌프가 보는 유량 위치)에 **흔들리지 않는다.**

**`g=0.0 / 펌프=공급` 배치에서 Δ 는 0 이 아니다** — 누출 4수준에서 **+2.838e-02 ~
+1.877e-01 L/s**(공급유량 대비 최대 **1.211%**). 세션 5.6 관측 ④ 에서 다섯 양
(총유량·펌프유량·양정·누출랙 유량·타 7랙 유량)이 **전부 정확히 0** 이었던 배치다.
즉 **Δ 는 그 배치의 유일한 생존 신호다.**

#### 관측 (판단이 아니라 재료다)

**① Δ 는 K 근사에 원리적으로 대응물이 없다.** K 근사는 질량이 보존되므로 Δ ≡ 0
이다. 세션 5.6 이 낸 네 양은 **부호가 갈리거나(⑴⑵⑶) 같았지만(⑷) 어쨌든 양쪽에
존재했다.** Δ 는 **한쪽에만 존재하는 첫 양**이다.

**② Δ 는 세션 5.6 이 드러낸 구조 자유도에 의존하지 않는 유일한 양이다.**
5.6 의 ⑵ 펌프 양정은 배치에 따라 부호가 갈렸고, 다섯 양 전부가 0 이 되는 배치도
있었다. Δ 는 배치 6 전체에서 편차 1.776e-15 L/s 다. 미해결 **#24**(잔여저항 분해
금지)가 닫히지 않아도 이 양은 정해진다.

**③ 다만 ①②는 모델 구조에서 따라 나온다.** Δ 는 이 모델에서 **강제한 누출 유량
그 자체**이고, K 근사에 유출 경로가 없다는 것도 정의다. **새로 알게 된 사실이라기
보다 정의를 값으로 확인한 것**이며, 그 값의 크기(공급유량 대비 최대 1.2%)만이
정의에서 바로 읽히지 않는 부분이다.

#### 세션 5.7 스키마에 대한 재료 (판단하지 않는다 — 사람이 정한다)

- **넣는 쪽 재료**: Δ 는 배치에 흔들리지 않고(편차 1.8e-15 L/s) 누출 크기에 엄격히
  단조이며, 다섯 양이 전부 0 인 배치에서도 **유일하게 살아남는다**. 데이터셋에
  대응물이 없다.
- **안 넣는 쪽 재료**: 현재 데이터셋은 **K 근사 전용**이라 이 열은 **전 행에서 항상
  0** 이 된다 — 정보량이 0 인 열이 하나 늘 뿐이다. 질량손실을 데이터셋에 넣기로
  정하지 않는 한(미해결 **#36** 의 (나)·(다)) Δ 열은 값을 갖지 못한다.
- **그래서 이 판이 남기는 것은**: 「Δ 열이 필요한가」가 **#36 의 선택에 종속**이라는
  사실이다. #36 이 (가)로 정해지면 Δ 열은 필요 없고, (나)·(다)로 정해지면 Δ 는
  **배치 자유도에 흔들리지 않으므로 열로 넣을 수 있는 양**이다.

#### 격리 확인 — 계산부를 건드리지 않았다

세션 5.6 이 낸 **표 1·2·3 의 6,295 바이트가 바이트 단위로 동일**하다(표 4 앞에
넣은 빈 줄 하나만 다르다). `git stash` 로 5.6 판 코드를 그대로 되돌려 실행해
대조했다. 계산 경로에 닿는 유일한 diff 는 스윕 루프에 `enumerate` 를 붙여 크기
순번을 얻은 것 한 줄이고, **순회 대상과 값은 그대로다.**

#### solver

세션 5.6 의 플래그를 그대로 인용한다 — `fsolve` **272 회 전부 `ier=1` · 실패 0 건**.
이 판에서 재실행했고 같은 값이 나왔다(위 바이트 동일성이 그것을 보인다).

**기존 테스트**: `pytest` **1,175건 전부 통과**(18분 38초) · `ruff`·`mypy` 통과.
이 판도 테스트를 하나도 더하거나 고치지 않았다 — 출력부만 바뀌었고 기존 경로가
그대로임을 기존 테스트가 확인한 것이다.

#### 이 판이 판정하지 않은 것 (과대해석 금지)

- **Δ 가 크게 나와도 계측 가능하다는 뜻이 아니다** — 계측기 사양이 없다.
- **실제 누출에 가깝다는 뜻도 아니다** — 실측이 없다.
- **세션 5.6 이 낸 네 양과 크기를 비교하지 않았다** — 크기 비교는 계측기 사양이
  있어야 뜻이 생긴다(「사람이 정한 것 — 세션 5.5-D 마무리」⑸).
- **6장 feasibility 기준을 하나도 보지 않았다** — 이 판도 수력 한정이고 열모델이
  없다. 게이트 판정이 아니다.
- **모델·데이터셋·5-1·`CLAUDE.md`·미해결 #36 문구를 고치지 않았다.**

#### 만든/바꾼 파일

- **고침**: `src/cdu_simul/leak_massloss.py` (**출력부만** — `MismatchRow` ·
  `_mismatch_row` · `format_mismatch_table` · `_mismatch_spread_across_topologies`
  추가 · `ComparisonRun` 에 수집 자리 하나)
- **고침**: `PROCEED.md` (판정 기준 · 이 절 · 「현재 상태」 표)
- **건드리지 않음**: `model.py` · `plant.py` · `dynamics.py` · `hydraulics.py` ·
  `leak.py` · `dataset*.py` · `assumptions.py` · 데이터셋 · 문서 4종 · 기존 테스트 ·
  미해결 #36 문구

#### 보고만 하는 발견 (절대 규칙 13 — 고치지 않았다)

1. 세션 5.6 의 「보고만」 1번이 그대로 열려 있다 — `CLAUDE.md` 「명령어」에
   `python -m cdu_simul.leak_massloss` 가 없다.
2. 세션 5.7 항목(#18·#19·#22 · M 8배 · `stimulus_kind` · `dataset_version` ·
   `leak_model` · #36 리포트 출력)은 이 판에서 **손대지 않았다.**

### 세션 5.7 · 2026-08-30 · M 8랙 정정(#31) · 스키마 3열 · 데이터셋 재생성

**게이트를 새로 만들지 않았다.** 앞 게이트(1-B · 2 · 3 · 4 · 5 · 5.5)를 기존 기준
그대로 재판정했다 — **판정 기준·허용오차를 하나도 바꾸지 않았다.** 그래서 판정 기준
선기재 대상이 없다.

#### 실행 순서 — 재생성 전에 문제를 전부 드러냈다

① 위생 3건 → ② 검사기 → ③ M 8배 → ④ 검사기 → ⑤ 스키마·리포트 → ⑥ 검사기 →
**⑦ 멈춤·사람 승인** → ⑧ 재생성 → ⑨ 검사기. ⑦ 을 넣은 이유는 M 이 정상상태 해에
영향하지 않는다는 5-1 서술을 재생성 **전에** 확인할 수 있었기 때문이다.

#### ① 위생 3건 — 계산 결과를 비트 하나도 바꾸지 않았다

| # | 내용 | 성격 |
|---|---|---|
| **#18** | `fluid.py` `REFERENCE_PRESSURE_Pa` 주석이 5-1 **확정** 문언을 인용하게 | 주석만 |
| **#19** | Cr 유도 규약 인용을 「세션 5-B」 → **5-1 「2차측 유체」 행** | 주석·docstring만 |
| **#22** | `model._property_temperature_from_state` → **공개 이름** 승격 | 이름만, 본문 무수정 |

확인: 수치 표 6개(`model`·`hydraulics`·`dynamics`·`leak`·`plant`·`leak_massloss`)가
**바이트 단위로 동일**. `dataset_plan` 만 달랐고 차이는 **측정 벽시계 시간 줄뿐**이다
(그 모듈은 실행시간을 재는 것이라 원래 비결정적이다 — 수치 표가 아니다).
② 검사기: pytest **1,175건 전부 통과**(19분 11초) · ruff · mypy 통과.

#### ③ M 8랙 정정 (#31)

`holdup_bounds()` 가 내던 M 은 5-1 등가길이 정의(「랙 1개 회로의 왕복 전체」) 때문에
**랙 1개 회로분**이었다. 사람이 정한 규칙대로 **양 끝을 둘 다 8배**로 읽는다.
8 은 `SCENARIO.racks_per_cdu`(5장 「CDU당 연결 랙 수」)에서 읽으므로 **코드에 숫자를
박지 않았고 추가 숫자가 0**이다. 노드 배분(공급 50% · 환수 50%)은 5-1 그대로이며
**8배는 총량에만** 걸린다.

| | 랙 1개 회로분 | 8랙 계통 | 배수 |
|---|---|---|---|
| M 하한 (전부 25A · 등가길이 하한) | 11.2756 kg | **90.2050 kg** | 8.0000000000 |
| M 상한 (전부 80A · 등가길이 상한) | 144.6976 kg | **1157.5805 kg** | 8.0000000000 |

**⒝ 정상상태 해는 불변이다** — `model`(32조합 열결합) · `hydraulics`(8조합 유량분배)
표가 **바이트 단위로 동일**하고 전이 종점 온도도 그대로다(30.098 / 42.469 ℃).
**5-1 「M 은 정상상태 해에 영향하지 않는다」가 코드에서 성립했다** — 멈춤 조건에
걸리지 않았다.

**⒞ τ·t63·t95 의 실제 이동 배수 = 정확히 8.000000배** (부하 스텝 4케이스 · 누출
스텝 6케이스 전부). 예상(약 8배)과 어긋나지 않았고 **정확히 8배인 까닭도 안다** —
적분 격자가 τ 단위로 잡히고 2노드 ODE 가 M 에 대해 선형이라 궤적 전체가 그대로
스케일된다. (인쇄 표의 반올림값으로 계산하면 7.99~8.17 배로 보인다. 위는 반올림 전
원값으로 재계산한 것이다.)
④ 검사기: pytest **1,175건 전부 통과**(16분 14초) · ruff · mypy 통과.

#### ⑤ 스키마 3열 (49 → **52열**) · 리포트

| 열 | 값 | 성격 |
|---|---|---|
| `dataset_version` | **`session-5.7`** | 판본 축을 세션 번호로. 48열본(5.5-B)·49열본(5.5-D)은 이 열 자체가 없어 **부재가 곧 5.7 이전**이라는 표지다. **파일명은 바꾸지 않았다** |
| `stimulus_kind` | `none` / `leak_step` / `load_step` | `regime`·`cdu_config` 에서 **읽는다**. 새 시나리오 0 · 규모 증가 0 |
| `leak_model` | `K_approx` (전 행 상수) | 질량손실과 부호가 반대이므로(#36) 이 열이 없으면 어느 쪽 데이터인지 알 수 없다 |

행별 `transient_caveat` 문구도 고쳤다 — #31 이 닫혔고 τ 가 정확히 8배로 이동했으나
**#21·#25·#23 때문에 절대 시간 규모는 여전히 해석하지 않는다**는 것을 전 행이 싣는다.

`dataset_report` 에 셋을 넣었다: **⒜ 생성 환경 절**(Python 3.12.10 · Windows 11 ·
numpy 2.5.2 · scipy 1.18.1 · CoolProp 8.0.0 · pandas 3.0.5 — **#35 는 닫지 않는다**,
「본 프로젝트 전환 시 고정」) · **⒝ ③절에 #36 종료 판정 재료 6항목** · **⒞ #24 문구
보강**(「유량분배 값의 크기」에 더해 **「누출 신호의 존재 여부까지」** 지배).
⑥ 검사기: pytest **1,175건 전부 통과**(20분 22초) · ruff · mypy 통과.

#### ⑦ 멈춤 — 사람이 재생성을 승인했다

⒜~⒡ 를 재생성 **없이** 보고하고 승인을 받은 뒤 ⑧ 로 넘어갔다.

#### ⑧ 재생성 — 판본 수치

| | |
|---|---|
| 행 | **310,016** (5.5-D 와 같다 — 규모를 늘리지 않았다) |
| 열 | **52** (49 → 52) |
| 용량 | **377,295,246 바이트 (359.8 MB)** |
| 소요 | **3,417.7 초 (57.0 분)** |
| solver 실패 행 | **0** |
| `dataset_version` | `session-5.7` — 310,016행 전부 |

CSV 를 직접 읽어 검증했다: 헤더가 `column_names()` 와 일치 · 표기 5열과 신 3열에
**빈 칸 0** · `leak_model` 이 전 행 `K_approx` · `stimulus_kind` 분포가
steady 1,280 (`none`) · single 전이 102,912 (`leak_step`) · dual_asymmetric 전이
205,824 (`load_step`) 로 자극 형태와 일치.

**중단 한 번 있었다(기록)**: 첫 실행이 하네스 백그라운드 작업 한도에 걸려 1200/1792
지점(37.9분)에서 kill 됐다. 모델·코드 문제가 아니다. `nohup` 으로 분리해 다시 돌려
완주했고, 두 번째 실행이 처음부터 새로 쓴 것이다(이어붙이지 않았다).

⑨ 검사기(재생성 후): pytest **1,175건 전부 통과**(16분 37초) · ruff · mypy 통과.

#### 앞 게이트 재판정

**전부 기존 기준 그대로 통과했다 — 판정 기준·허용오차를 하나도 조정하지 않았다.**
검사기 ②·④·⑥·⑨ 네 번 전부 pytest 1,175건 통과이며, 여기에 1-B·2·3·4·5·5.5·5.5-D
게이트 테스트가 모두 들어 있다. M 8배는 정상상태 게이트의 **값을 바꾸지 않았고**,
전이 게이트의 **부호·방향을 바꾸지 않았다**(시간축만 정확히 8배로 이동).

#### 6장 기준 — 무엇을 봤고 무엇을 안 봤나

- ① energy balance · ② T_return 방향성 · ③ 극단 케이스 비발산 — 기존 판정 유지
- ④ **수렴시간은 여전히 판정 불가다.** #31 이 닫혀도 **#21**(HX·CDU 내부·콜드플레이트
  M 결손) · **#25**(등가길이의 과대 방향 오차) · **#23**(배관 규격 계열 약 7% 이동)이
  그대로 열려 있다

#### ※ 과대해석 금지

**M 8배는 결손을 메운 것이 아니다.** 랙 1개 회로분을 8랙 계통으로 **해석**한 것뿐이고,
M 에 빠진 것(#21)·등가길이의 방향 반대 오차(#25)·규격 계열 의존(#23)은 하나도 줄지
않았다. 두 오차의 방향이 반대라 **상쇄 정도를 모른다.** **τ 의 절대값은 여전히
해석 불가**이며, 이 판을 「이제 수렴시간을 판정할 수 있다」로 읽지 않는다.

#### 만든/바꾼 파일

- **고침**: `fluid.py`(#18 주석) · `model.py`(#19 인용 · #22 이름) · `dynamics.py`
  (#22 호출부 · #31 M 8배 · 한계 문구) · `leak.py`·`plant.py`·`assumptions.py`(한계
  문구) · `dataset.py`(3열) · `dataset_plan.py`(행별 caveat) · `dataset_report.py`
  (⒜⒝⒞) · `PROCEED.md`
- **재생성**: `results/cdu_dataset.csv` (52열본 · 원격에 없다 — `.gitignore` 대상)
- **건드리지 않음**: 5장 표 · `CLAUDE.md` · `collaboration.md` ·
  `project-overview.md` · 5-1 · `pyproject.toml` · `leak_massloss.py` · 기존 테스트 ·
  미해결 #36 의 (가)/(나)/(다) 선택

#### 사람이 밟아야 하는 것

**5-1 「누출 시나리오」·「누출 주입 지점」에 #36 한계 문구 추가 — 사람이 직접 한다.**
정본 수정이라 이 판은 손대지 않았다. **고치면 프로젝트 지식 재업로드가 필요하다.**

#### 보고만 하는 발견 (절대 규칙 13 — 고치지 않았다)

1. `CLAUDE.md` 「명령어」에 `python -m cdu_simul.leak_massloss` 가 없다 (세션 5.6 부터
   열려 있다). 상시 실행 대상인지 사람이 정할 일이다
2. `dataset_plan.SCHEMA_DRAFT` 는 5.5-D 의 `leak_cdu_index` 와 이 판의 3열을 담고
   있지 않다. 5-1 이 「열 목록은 코드가 정본이다」라고 적었고 그 목록은 설계 초안이라
   **고치지 않았다** — 초안으로 남길지 지울지는 사람이 정한다


### 세션 5.7-C · 2026-08-30 · 오늘 결정 기록 (기록 판 · 코드 무수정)

**기록 판이다. 게이트 없음 · feasibility 판정 없음 · 수치 변경 0.**
모델 코드·테스트·데이터셋(52열본)·`dataset_report`·`leak_massloss.py`·정본 문서
4종을 **하나도 건드리지 않았다.** 바뀐 파일은 `PROCEED.md` 하나다.

#### ⑴ 정지선 변경 — 사람이 정했다

세션 5.5-D 「정지선」(⑵항 — 5.6·5.7·5.8 이 파일럿 자력의 끝)을 **한 판 넓힌다.**

**까닭**: 세션 5.6·5.6-B 는 그 정지선을 그은 **뒤에** 나왔다. 그 둘이 드러낸 것은
**누출 정의가 교체 대상**이라는 사실이고(K 근사와 질량손실의 부호가 총유량·누출랙
유량에서 정반대 · 미해결 #36), **교체된 정의가 열·전이에서도 서는지는 아직 아무도
보지 않았다** — 5.6·5.6-B 는 **수력 정상상태 한정**이었다.

**새 순서**::

    5.7-D (질량손실 열·전이 관측) → 5.8 (이동지연 감도) → 파일럿 종료 판정

**5.7-D 를 5.8 앞에 두는 까닭**: 5.8 은 **시간축 이산화**를 보는 판이고, 5.7-D 가
누출 정의를 바꿔 열·전이를 돌리면 그 결과도 **같은 시간축 위에** 있다. 순서를
바꾸면 5.8 을 두 정의에 대해 **두 번** 보게 된다.

**5.5-D 정지선의 나머지는 그대로다** — #21 · #25 · #24 · 고정 NTU · 2차측 온도
결합은 여전히 **설계데이터가 있어야 하며 종료 판정을 막지 않는다.**

#### ⑵ 보충수 불필요 — 사람이 닫았다 (5-1 문구는 사람이 직접 넣는다)

질량손실 누출 모사에 **보충수 온도는 필요하지 않다.**

**까닭**: 이 과제의 목적은 **미량 누출 조기감지**(1장)이고, 미량 구간에서는 밀폐루프
**보충 로직이 작동하지 않는다.** 보충이 돌 만큼 커진 누출은 이미 조기감지 구간이
아니다. 따라서 조기감지 시나리오에서 보충수는 **정의상 등장하지 않는다** — 새
가정치를 만드는 것이 아니라 **1장 목적과 5장 누출 크기에서 따라 나오는 역산**이다
(5-1 빈칸 처리 순서 1번).

**이것이 세션 5.6 §3 의 전제를 바꾼다.** 5.6 은 "빠져나간 유체를 보충수로 채우면
보충수 온도라는 5장에 없는 숫자가 필요해진다"는 이유로 **수력 한정**을 택했다.
그 이유가 닫혔으므로 **5.7-D 가 열·전이를 볼 수 있다.**

**한계 — 그 대신 계통이 시간에 따라 줄어든다.** 계통 **정압** 강하를 누출 신호로
쓰려면 **팽창탱크 용적 · 초기 충전압**이라는 **5장 밖 숫자 둘**이 필요하다 —
설계데이터 확보 시 재검토한다. **누출을 푸는 것 자체에는 필요 없다**: 세션 5.6 이
계통 압력 없이 272 회를 수렴시켰다(전부 `ier=1`).

**5-1 에 넣는 것은 사람이 직접 한다**(아래 「사람이 밟아야 하는 것」). 여기 적은
것은 **인계용 기록**이다.

#### ⑶ 계통 dP 정리 (오늘 대화에서 확인한 것)

폐쇄루프 **총 ΔP = 펌프 양정** = 5장 **20~30 mAq = 196.1~294.2 kPa**.
내역: 랙 분기 **2~3 mAq** · 밸브 **3~5 mAq** · 계통 잔여저항(**종속값** — 총양정의
**60~83%** · 미해결 #24).

**이것은 순환 dP 이고 계통 정압이 아니다.** 실제 CDU 운전압은 **정압 + dP** 이며,
**정압은 이 모델에 없다**(팽창탱크를 모델링하지 않는다). 누출 신호로서의 **압력
강하는 정압 쪽**이고, 그래서 ⑵ 의 「5장 밖 숫자 둘」이 거기에 걸린다.

#### ⑷ 시연 관점 — 앞으로의 판이 감안할 것 (사람 지시)

`project-overview.md` 「시각화 방향」이 **파일럿에서는 손대지 않는다**로 정해 뒀고
**그것은 그대로다.** 다만 데이터셋 작업이 끝난 뒤 「보여줄 것이 없다」가 되지 않도록,
앞으로의 판은 **결과를 남길 때** 다음을 감안한다.

- **시연 후보 재료는 현재 데이터셋에 이미 있다** — 정상상태 **1,280행**(단일 256 ·
  2대 대칭 512 · 2대 비대칭 512) · 다중 CDU 부하 전이 **205,824행**
  (`stimulus_kind=load_step`). 특히 **다중 CDU 연동**(한쪽 부하가 오르면 공유 2차측
  배분이 이동한다 — 세션 5 C7 · 5.5-D)은 **2장 문제의식을 직접 보여주는 재료**다.
- **미확인 하나 → 미해결 #37 로 열었다.** `stimulus_kind=none`(steady) 1,280행에
  **누출 K값이 걸린 정상상태 케이스가 섞여 있는지 확인되지 않았다.** `none` 은
  「자극이 없다」는 뜻이지 **「누출이 없다」는 뜻이 아니다.** 시연에서 누출을 빼려면
  누출 관련 열(`leak_level_percent` · `leak_cdu_index`)로 한 번 더 걸러야 하는지
  **다음 판에서 확인한다.**
- **시연의 범위·도구·형태는 지금 정하지 않는다.** 종료 판정 뒤다 — 판정이 「전환」
  으로 나오면 **누출 정의가 바뀌고 보여줄 것도 바뀐다.**

#### ⑸ 미해결 갱신 — 전부 append. 덮어쓰지 않았다

- **#36 갱신** — (가)/(나)/(다) 중 **파일럿 안에서 실행되는 것은 (가)**(K 근사 유지 +
  한계 명시)이고, 5-1 문구 추가로 실행된다. (나)로 가는 데 필요했던 것 중 **보충수
  온도는 오늘 닫혔다**(⑵). 남은 것은 **#24 잔여저항 분해**와 **계통 정압**이며 둘 다
  설계데이터 대기다. **최종 선택은 여전히 사람 몫이다.**
- **#37 신규** — 시연 재료의 누출 혼입 여부 미확인.

#### 검사기 — 코드를 안 건드렸으므로 그대로 통과해야 한다

`pytest` **1,175건 전부 통과**(16분 21초) · `ruff` · `mypy` 통과. 하나라도 실패하면
멈추기로 했으나 걸린 것이 없었다. 데이터셋 파일도 재생성본 그대로다(377,295,246 바이트).

#### 만든/바꾼 파일

- **고침**: `PROCEED.md` 하나
- **건드리지 않음**: 모델 코드 전부 · 테스트 · 데이터셋(52열본) · `dataset_report` ·
  `leak_massloss.py` · 정본 문서 4종 · 세션 5.8 항목

#### 사람이 밟아야 하는 것 — 5-1 에 문구 둘 (사람이 직접)

정본 수정이므로 이 판은 건드리지 않았다. **넣은 뒤 프로젝트 지식 재업로드가 필요하다.**

⒜ **누출 시나리오 한계** — 「누출 시나리오」 또는 「누출 주입 지점」 행.
   K 근사가 5장이 정의한 **대용(proxy)** 이고 질량손실과 총유량·누출랙 유량의 부호가
   **정반대**라는 것(#36 · 세션 5.6).
⒝ **보충수 처리** — 위 ⑵ 문안(미량 조기감지 구간에서는 보충 로직이 작동하지 않으므로
   보충수 온도가 정의상 등장하지 않는다 · 대신 계통 정압은 5장 밖 숫자 둘이 필요하다).

**문안은 웹 대화창이 초안을 냈고 사람이 확정한다** — 이 판은 초안을 만들지 않았다.

#### 다음 판

**5.7-D — 질량손실 열·전이 관측.** ⑵ 가 보충수 문제를 닫았으므로 세션 5.6 이 수력
한정을 택한 이유가 사라졌다.



### 세션 5.7-E · 2026-08-31 · 5-1 에 문구 둘 추가 (사람 지시 · 정본 수정)

**문서 판이다. 게이트 없음 · 코드 무수정 · 수치 변경 0.** 모델 코드·테스트·
데이터셋(52열본)·`dataset_report`·`leak_massloss.py` 를 하나도 건드리지 않았다.

#### 무엇을 어디에 넣었는가

**`AI서버_CDU_디지털트윈_프로젝트정리.md` 의 5-1 절만** 고쳤다. 문안은 사람이
확정한 것을 **한 글자도 바꾸지 않고** 실었다 — 표 한 줄에 담기게 줄바꿈만
이었다(마크다운 표 셀은 개행을 담지 못한다).

⒜ **「누출 주입 지점」 행** — 근거란 **끝에 이어 붙였다**. 기존 문장은 지우지
   않았다. 내용: K값 근사가 누출을 **배관 저항 변화로** 모사하는 **대용(proxy)**
   이고, 질량이 보존되는 모사라 실제 누출(질량손실)과 ⑴ 총유량·누출랙 통과유량의
   **부호가 정반대** ⑵ 잔여저항 배치(#24)에 따라 수력 흔적이 **전혀 남지 않는
   경우**가 있다 ⑶ 실제 누출의 **공급−환수 유량 불일치**가 K 근사에는 **원리적으로
   존재할 수 없다**는 것. **데이터셋의 누출 신호를 실제 누출의 방향으로 그대로
   읽지 않는다.** 세션 4 게이트 96건 전수 통과는 「K 근사 대용의 거동으로서」
   **여전히 유효하다**. 미해결 #36.

⒝ **새 행 「보충수 처리」** — 「누출 주입 지점」 **바로 다음 줄**에 넣었다.
   확정값은 「보충수를 모델링하지 않는다 — 조기감지 시나리오에 정의상 등장하지
   않는다」, 확정 시점은 **세션 5.7-C**. 근거: 미량 구간에서는 밀폐루프 보충
   로직이 작동하지 않으므로 보충수는 **정의상 등장하지 않는다** — 새 가정치가
   아니라 1장 목적과 5장 누출 크기에서 따라 나오는 **역산**이다(5-1 빈칸 처리
   순서 1번). 한계도 함께 실었다 — 계통 정압 강하를 누출 신호로 쓰려면
   팽창탱크 용적·초기 충전압이라는 **5장 밖 숫자 둘**이 필요하다.

#### diff 확인

`git diff --stat` = **1 file changed · 2 insertions · 1 deletion**. 바뀐 두 줄은
전부 5-1 표 안이다(125행 「누출 주입 지점」 · 126행 신규 「보충수 처리」).
**5장 표 본체 · 6장 · 7장 · 그 밖의 절 · `CLAUDE.md` · `collaboration.md` ·
`project-overview.md` 는 하나도 건드리지 않았다.**

#### 검사기

코드를 안 건드렸으므로 그대로 통과했다 — pytest **1,175건** · `ruff check .`
**All checks passed** · `mypy src tests` **24 files, no issues**.

#### 재업로드

**정본 문서를 고쳤다 — 프로젝트 지식 재업로드가 필요하다**(`CLAUDE.md`
「작업 시작 전 필독」 4). 대상: `AI서버_CDU_디지털트윈_프로젝트정리.md`.

#### 이 판이 정하지 않은 것

- 미해결 **#36 의 (가)/(나)/(다) 최종 선택** — 사람 몫이다. 5-1 문구 추가는
  **(가)의 실행**이지만 (나)·(다)를 닫지 않는다
- 세션 5.7-D · 5.8 의 내용

#### 다음 판

**5.7-D — 질량손실 열·전이 관측**(#37 확인 포함) → 5.8(이동지연 감도) → 종료 판정.

### 세션 5.7-D · 2026-08-31 · 질량손실 누출의 열·전이 관측 — 판정 기준 선기재

> **이 절은 코드보다 먼저 적고 먼저 커밋한다** — 세션 3-B 이후 원칙이며 세션 4
> (커밋 `803142e`) · 세션 5 (`acbc31c`) · 세션 5.5 (`cc790fa`) · 세션 5.6
> (`7285ef7`) · 세션 5.6-B (`9d210e3`) 와 같은 방식이다.
> **아래 기준을 결과에 맞춰 고치지 않는다.**

**절대 규칙 8 예외 — 사람 승인이 이 판까지 이어진다.** 세션 5.5-D 마무리 ⑶ 이
질량손실 모사를 **부호 확인 목적의 별도 코드에 한해** 허용했고, 세션 5.7-C 에서
사람이 정지선을 한 판 넓혀 이 판을 열었다. 조건은 그대로다 — **별도 코드 ·
데이터셋 미변경 · 5-1 의 누출 정의는 K 근사 그대로 · 규칙 8 문언 무수정.**
이 판의 코드는 데이터셋 생성 경로에 **들어가지 않는다.**

**5-1 「보충수 처리」(세션 5.7-C 확정)가 이 판의 전제다.** 보충수를 모델링하지
않는다 — 조기감지 시나리오에 정의상 등장하지 않는다. 세션 5.6 이 수력 한정을
택한 이유가 이것이었고, 그것이 닫혔으므로 열·전이를 볼 수 있다.

**게이트가 아니다.** 통과/실패가 아니라 **방향을 적는 것**이 산출물이다.

#### 열모델을 어떻게 세우는가 — 먼저 적어 둔다

세션 5.6 의 수력 위에 **2노드 온도 모델**(`dynamics.py` 구조)을 그대로 얹는다.
질량손실이 바꾸는 것은 **딱 한 군데**다.

    환수(hot) 노드 :  M_hot·cp·dT_ret/dt = ṁ_sup·cp·(T_sup − T_ret) + Q_rack
    공급(cold) 노드:  M_cold·cp·dT_sup/dt = ṁ_ret·cp·(T_ret − T_sup) − Q_hx

- **환수 노드는 그대로다.** 유입 ṁ_sup, 유출 ṁ_ret + ṁ_leak = ṁ_sup 이므로
  이 노드의 **질량은 정확히 보존된다.** 누출 유체가 T_ret 로 나가므로 유출
  엔탈피가 ṁ_sup·cp·T_ret 로 합쳐져 식의 형태가 밀폐루프와 같아진다.
- **공급 노드만 ṁ_ret 로 바뀐다.** 열교환기를 지나는 것은 환수유량이다.
  `Q_hx = ε·C_min·(T_ret − T_2차)` 의 `C_1차` 도 ṁ_ret·cp 다.
- 재고가 줄어드는 것은 **공급 노드**다 (dM_cold/dt = −ṁ_leak).

**절대온도가 식에 남지 않는다.** 변질량 형태 `d(M·T)/dt` 에서 출발해 M 을
얼리면 `T·dM/dt` 항이 유출항의 절대온도 항과 **정확히 상쇄**되어 위 식이 된다.
온도 영점에 의존하는 항이 남았다면 그것은 상수 M 근사를 잘못 적용한 것이다 —
**이 상쇄를 먼저 적어 두는 이유는 결과를 보고 식을 고르지 않기 위해서다.**

**정상상태 해**(dT/dt = 0)는 닫힌 형태다::

    ΔT = Q_rack / (ṁ_sup·cp)
    T_ret = T_2차 + ṁ_ret·cp·ΔT / (ε·C_min)
    T_sup = T_ret − ΔT

물성 평가온도의 고정점과 매 시점 quasi-steady 수력은 **기존 규약 그대로**다
(5-1 「cp·ρ 평가 온도」·「수력 계산의 물성 평가 온도」 · 절대 규칙 4).

#### 무엇을 대조하는가 — A~G

각 항목을 **K 근사와 질량손실에서 각각** 내고 부호를 대조한다. 전부 **같은
조합의 정상(누출 0) 해 대비 차이**다.

- **A. 누출랙 온도** — 콜드플레이트 출구온도의 변화 방향
- **B. CDU 환수온도 T_return** — 변화 방향
- **C. CDU 공급온도 T_supply** — 변화 방향
- **D. 에너지 balance** — 질량손실에서는 **누출 유체가 엔탈피를 들고 나간다.**
  잔차를 **두 벌** 낸다::

      leak 항 없이 : (Q_hx − Q_rack) / Q_rack × 100
      leak 항 포함 : (Q_hx + ṁ_leak·[h(T_ret) − h(T_sup)] − Q_rack) / Q_rack × 100

  `Q_hx` 는 ε-NTU 경로, `Q_rack` 은 5장 입력, 엔탈피 차는 **CoolProp 직접
  조회**다 — 세 경로가 다르므로 항등식이 아니다. **leak 항을 넣어야 닫히는지**가
  이 기준이 묻는 것이고, 넣고도 닫히지 않으면 **멈추고 보고한다**(§9).
  **6장 기준 ①의 판정이 아니다** — 별도 코드의 자기정합성 확인이다.
- **E. 시간응답** — τ·t63 이 M 에 정합하는가(τ ∝ M). 질량손실에서도 성립하는지
- **F. 다중 CDU** — 누출을 CDU A 에만 걸었을 때 **B 의 반응 방향**. 세션 5.5-D 가
  K 근사에서 관측한 것(B 의 2차측 몫 단조 증가 · 환수온도 단조 감소)과 대조한다
- **G. 배치 불변성** — A~F 중 **배치 6 에 흔들리지 않는 양이 있는가.** 특히
  **g=0/펌프=공급 배치**(세션 5.6 관측 ④ 에서 수력 다섯 양이 전부 0 이던 배치)
  에서 **열 신호가 살아 있는지** 반드시 본다

#### 가설 (예단하지 말 것)

질량손실은 누출랙 통과유량을 **늘리므로** ΔT_rack = q/(ṁcp) 가 줄어 랙 출구온도가
**내려갈** 수 있다. K 근사는 유량을 줄이므로 **올라간다** — 즉 A 의 부호도 정반대일
수 있다. B·C·F 는 유량과 온도가 반대로 움직여 예측이 갈린다.
**가설을 세우지 말고 결과를 그대로 적는다.**

#### 구조 자유도 — 세션 5.6 의 둘 + 이 판이 마주치는 하나

- **g** (잔여저항 중 환수측 몫) · **펌프가 보는 유량** — 세션 5.6 과 같다.
  배치 6 전수. `assumptions.py` 에 넣지 않는다.
- **[이 판에서 새로 마주친다] 공유 2차측 배분이 보는 「1차측 유량」** — 5-1
  「공유 2차측 결합 방식」이 배분을 "1차측 유량에 비례"로 정하는데, 누출 CDU 는
  ṁ_sup ≠ ṁ_ret 라 **어느 쪽인지가 처음으로 갈린다.** 열교환기를 지나는 것은
  환수유량이므로 `Q_ret` 가 물리적으로 정해진 읽기이지만, **값을 고르지 않고
  양 끝(Q_ret · Q_sup)을 둘 다 돌려** F 의 부호가 그것에 의존하는지 적는다.
  분기의 양 끝이므로 새 가정치가 아니다 — g 와 같은 취급이다.

#### 계통이 줄어드는 처리 — M 을 상수로 둔다

보충이 없으므로 계통 보유량 M 이 시간에 따라 줄어든다. **이 판은 M 을 상수로
두고 돌린다** — 감소를 넣으면 초기 보유량이라는 새 처리가 붙고, 이 판은 방향을
보는 판이다.

**대신 그 근사의 크기를 반드시 낸다.** 케이스마다::

    소실률[%] = Q_leak × 관측시간 × ρ / M × 100

를 표로 적는다. **작으면 상수 M 근사가 그 창 안에서 성립하고, 크면 결과 해석에
그만큼의 유보가 붙는다. 어느 쪽인지 미리 정하지 않는다** — 수치를 내고 나서
적는다. 관측 창 길이는 **기존 전이 설정을 그대로 쓴다**(새 숫자 0개).

#### 판정 기준 (부호 판정 외)

- **반례 확인**(collaboration.md ⑥). 부호가 어느 쪽으로 나오든 **배치 6 × 누출
  크기 × 부하 2수준 × 양정·분기 ΔP·밸브 ΔP 양 끝 전 조합**에서 뒤집히는 경우가
  있는지 같이 돌리고 적는다.
- **격리 확인.** 누출 크기 0 에서 열·전이 해가 기존 밀폐루프 해와 **부동소수점
  까지** 같아야 한다. 다르면 코드가 정상 경로를 건드린 것이다.
- **solver**(절대 규칙 5). `fsolve` `ier` · `solve_ivp` `success` 를 전 조합에서
  확인해 결과에 싣는다. 실패 조합은 **버리지 않고 표시한다.**

#### 이 판이 판정하지 않는 것 (과대해석 금지)

- **6장 기준 넷을 게이트로 판정하지 않는다.** D 의 balance 는 별도 코드의
  자기정합성이지 6장 ① 판정이 아니다.
- **어느 모사가 실제에 가까운지 재지 않는다** — 실측이 없다.
- **신호가 계측 가능한 크기인지 재지 않는다** — 계측기 사양이 없다.
- **M 을 상수로 뒀다** — 위 소실률 표가 그 유보의 크기다.
- 미해결 **#36 의 (가)/(나)/(다) 선택을 하지 않는다.** 사람 몫이다.

### 세션 5.7-D · 2026-08-31 · 질량손실 누출의 열·전이 관측 (관측 판 · 게이트 없음)

**관측 판이다. 게이트 없음 · feasibility 판정 없음 · 데이터셋 재생성 없음.**
판정 기준은 위 「세션 5.7-D … 판정 기준 선기재」에 **코드보다 먼저** 적고 먼저
커밋했다(`9bd246e`). **기준을 결과에 맞춰 고치지 않았다.**

**절대 규칙 8 예외**(사람 승인 · 세션 5.5-D ⑶ + 5.7-C 정지선 확장). 조건을 그대로
지켰다 — 별도 모듈 `leak_massloss_thermal.py` 는 **데이터셋 생성 경로에 들어가지
않고**(어느 모듈도 import 하지 않는다), 데이터셋·5-1·규칙 8 문언을 하나도 고치지
않았다. `model.py`·`plant.py`·`dynamics.py` 는 **읽기·호출만** 했다.

#### 규모와 실행시간 (C4 — 먼저 산출하고 돌렸다)

| 스윕 | 규모 | 측정 |
|---|---|---|
| 정상상태 | 64 케이스 × 배치 6 × 누출 5수준 = **1,920 해** | 약 0.8 분 |
| 전이 | 64 × 배치 6 × M 양 끝 = **768 적분** | 약 15 분 |
| 다중 CDU | 64 × 배치 6 × 누출 5 × 배분 읽기 2 = **3,840 결합 해** | 약 9 분 |

합계 약 25 분으로 **60분 정지선 안**이다. 64 케이스는 `model.default_cdu_cases`
(5장·5-1 양 끝 32조합) × 부하 양 끝 2수준이며 **축을 새로 고르지 않았다**.

#### A~G 결과 — 방향을 적는다 (판정이 아니다)

**A·B 누출랙 출구온도 · CDU 환수온도 — 부호가 정반대다**

| 양 | K 근사 | 질량손실 | 질량손실 범위 [K] |
|---|---|---|---|
| A 누출랙 출구온도 | **+** | **−** | −1.719e-01 ~ −1.886e-03 |
| B CDU 환수온도 | **+** | **−** | −1.719e-01 ~ −1.886e-03 |
| C CDU 공급온도 | **+/−** | **−** | −7.391e-02 ~ −1.389e-03 |

K 근사 192건 · 질량손실 1,536건에서 **하나도 뒤집히지 않았다**(반례 확인 —
누출 크기 × 부하 2수준 × 양정·분기 ΔP·밸브 ΔP 양 끝 × 배치 6 전 조합).

**가설이 맞았다**: 질량손실은 누출랙 통과유량을 늘려 ΔT_rack = q/(ṁcp) 를 줄이므로
랙 출구온도가 **내려가고**, K 근사는 유량을 줄이므로 **올라간다**.

**A 와 B 의 값이 정확히 같다.** 5-1 이 헤더를 저항 0 의 공통 노드로 두므로
질량손실이 **랙에 국소화되지 않고**(세션 5.6 관측), 그 결과 전 랙 출구온도가
환수 헤더 온도와 같아진다. **K 근사에는 없는 구조 차이다** — K 근사는 누출랙만
온도가 갈린다. **「랙별 온도로 누출 랙을 특정한다」는 규칙은 질량손실 쪽에
대응물이 없다.**

**C 는 K 근사에서만 갈린다.** 질량손실은 전 조합에서 −, K 근사는 조건에 따라
갈린다 — 세 온도 중 C 만 두 모사의 「부호가 정반대」라고 말할 수 없다.

**D 에너지 balance — 누출 엔탈피 항을 넣어야 닫힌다**

| | 최소 [%] | 최대 [%] | 최대 절대값 [%] |
|---|---|---|---|
| leak 항 **없이** | −1.212916 | −0.182636 | **1.212916** |
| leak 항 **포함** | −0.000062 | −0.000009 | **0.000062** |

`Q_hx`(ε-NTU) · `Q_rack`(5장 입력) · 엔탈피 차(CoolProp 직접 조회) **세 경로가
다르므로 항등식이 아니다.** 항을 빼면 닫히지 않고(최대 1.21%) 넣으면 닫힌다
(최대 0.000062%). **모사가 자기정합적이다.** 판정 기준이 요구한 「멈추고 보고」
조건에 해당하지 않는다.

**격리 확인**: 누출 0 에서 기존 밀폐루프 해와 최대 편차 **7.105e-15 K** —
정상 경로를 건드리지 않았다.

**※ 이것은 6장 기준 ① 판정이 아니다** — 별도 코드의 자기정합성 확인이다.

**E 시간응답 — τ ∝ M 이 정확히 성립한다**

| 양 | 최소 | 최대 |
|---|---|---|
| τ 비 (M 상한/M 하한) | 12.832779 | 12.832779 |
| t63 비 (M 상한/M 하한) | 12.832779 | 12.832779 |

M 비(1157.5805/90.2050 = **12.832779**)와 **소수점까지 일치**하고 768 적분 전부
같은 값이다. 세션 5.7 이 K 근사에서 8.000000배를 확인한 것과 같은 성질이
**질량손실에서도 성립한다.** T_return 순변화는 −0.171875 ~ −0.007523 K 로
전부 음수 — 정상상태 B 와 부호가 같다.

**F 다중 CDU — 배분 읽기가 부호를 가른다**

| 배분이 보는 1차측 유량 | B 몫 부호 | B 환수온도 부호 | B 몫 변화 [L/s] |
|---|---|---|---|
| `Q_ret` (HX 통과 · 정해진 읽기) | **+** | **−** | +1.142e-02 ~ +9.457e-02 |
| `Q_sup` (대안 읽기) | **+/−** | **+/−** | −7.586e-02 ~ +1.337e-04 |

**`Q_ret` 읽기에서는 세션 5.5-D 의 K 근사 관측과 방향이 같다** — 누출 없는 B 의
2차측 몫이 늘고 환수온도가 내려간다(5.5-D: +0.01908 L/s · −0.01064 K). 누출
크기에 대한 **엄격 단조 위반 0건**.

**`Q_sup` 읽기에서는 부호가 갈린다.** 5-1 「공유 2차측 결합 방식」이 배분을
「1차측 유량에 비례」로만 정해 두었는데, 누출 CDU 는 ṁ_sup ≠ ṁ_ret 라 **이 판에서
처음으로 그 문구가 갈렸다.** 새 미해결 **#39** 로 올린다.

**G 배치 불변성 — 열 신호는 배치 6 전부에서 살아 있다**

| 배치 | A | B | C | B 크기 범위 [K] |
|---|---|---|---|---|
| g=0.0/펌프=공급 | − | − | − | −7.368e-02 ~ −1.886e-03 |
| g=0.0/펌프=환수 | − | − | − | −8.675e-02 ~ −2.219e-03 |
| g=0.5/펌프=공급 | − | − | − | −1.159e-01 ~ −2.840e-03 |
| g=0.5/펌프=환수 | − | − | − | −1.287e-01 ~ −3.177e-03 |
| g=1.0/펌프=공급 | − | − | − | −1.591e-01 ~ −3.767e-03 |
| g=1.0/펌프=환수 | − | − | − | −1.719e-01 ~ −4.104e-03 |

**이 판의 가장 큰 소득이다.** 세션 5.6 관측 ④ 는 `g=0/펌프=공급` 배치에서
**수력 다섯 양이 전부 정확히 0** 이라 「잔여저항 배치에 따라 누출이 수력 흔적을
전혀 남기지 않는다」고 적었다. **열은 그 배치에서도 신호를 낸다** — 환수온도가
−7.368e-02 ~ −1.886e-03 K 로 움직인다.

**까닭**: 질량손실은 배치와 무관하게 **열교환기를 지나는 유량을 줄인다**
(ṁ_ret = ṁ_sup − ṁ_leak). 그것은 잔여저항이 어디 있든 성립하므로 열 신호는
구조 자유도에 걸리지 않는다. 세션 5.6-B 가 수력에서 찾아낸 Δ 와 **같은 성질**이다.

**크기는 배치에 걸린다**(g=1.0/펌프=환수 쪽이 g=0/펌프=공급 쪽의 약 2.3배).
**부호는 걸리지 않는다.**

#### 상수 M 근사의 크기 — 판정 기준이 요구한 표

| 창 | 최소 [%] | 최대 [%] |
|---|---|---|
| 전체 창(30τ) | 21.8876 | 36.3704 |
| **t63 까지** | **0.6154** | **1.8669** |

**둘을 나란히 읽어야 한다.** 전체 창에서는 계통의 **22~36%** 가 빠져나가므로
상수 M 근사가 그 구간에서 성립하지 않는다 — **30τ 파형의 뒷부분을 그대로 읽지
않는다.** 반면 신호가 서는 시간척도인 **t63 까지는 0.6~1.9%** 라 그 구간의
근사 오차는 작다. **위 A~G 의 부호는 t63 구간에서 이미 정해지므로 이 유보가
부호를 뒤집지 않는다.** 정량값(특히 30τ 종점값)에는 유보가 붙는다.
새 미해결 **#38** 로 올린다.

#### solver (절대 규칙 5)

정상상태 1,920 해 · 전이 768 적분 · 결합 3,840 해 **전부 성공**. `fsolve` `ier`
와 `solve_ivp` `success` 를 전 조합에서 확인했고 **실패 0건**이다.

#### 이 판이 판정하지 않은 것 (과대해석 금지)

- **6장 기준 넷을 게이트로 판정하지 않았다.** D 의 balance 는 별도 코드의
  자기정합성이지 6장 ① 판정이 아니다. T_return 방향성(6장 ②)도 이 판의 관측
  대상이 아니다 — 여기서 본 것은 **누출에 대한** 온도 반응이지 부하에 대한 것이
  아니다. 수렴시간(③)은 #21·#25·#23 때문에 여전히 판정 불가다.
- **어느 모사가 실제에 가까운지 재지 않았다** — 실측이 없다.
- **신호가 계측 가능한 크기인지 재지 않았다** — 계측기 사양이 없다. B 의 크기가
  1.9e-03 ~ 1.7e-01 K 라는 것은 적었지만 그것이 「검출된다」는 뜻이 아니다.
- **M 을 상수로 뒀다** — 위 소실률 표가 그 유보의 크기다.
- **미해결 #36 의 (가)/(나)/(다) 를 고르지 않았다.** 사람 몫이다.

#### 만든/바꾼 파일

- **신규**: `src/cdu_simul/leak_massloss_thermal.py` ·
  `tests/test_session57d_massloss_thermal.py`
- **바꿈**: `src/cdu_simul/leak_massloss.py` — `_SWEEP_FRACTIONS` 를
  `SWEEP_FRACTIONS` 로 **순수 rename**(본문·값 무수정). 새 모듈이 같은 스윕을
  쓰기 위해서다.
- **바꿈**: `PROCEED.md`
- **안 건드림**: 데이터셋(52열본) · `dataset_report` · `model.py` · `plant.py` ·
  `dynamics.py` · `assumptions.py` · 정본 문서 4종

#### 다음 판

**5.8 — 이동지연 감도**(N-CSTR 스윕). 그 뒤 종료 판정.

### 세션 5.8 · 2026-08-31 · 이동지연 감도 (N-CSTR 수치 수렴) — 판정 기준 선기재

> **이 절은 코드보다 먼저 적고 먼저 커밋한다** — 세션 3-B 이후 원칙이며 세션 4
> (`803142e`) · 5 (`acbc31c`) · 5.5 (`cc790fa`) · 5.6 (`7285ef7`) · 5.6-B
> (`9d210e3`) · 5.7-D (`9bd246e`) 와 같은 방식이다.
> **아래 기준을 결과에 맞춰 고치지 않는다.**

**관측 판이다. 게이트 없음 · feasibility 판정 없음 · 데이터셋 재생성 없음 ·
모델 무수정.** 세션 5.5-D ⑴ 이 이 판을 「바꾸지 않는다 · 관측만」으로 정했다.
스윕은 **별도 파일**에서 하고 현재 2노드 구조를 건드리지 않는다.

**파일럿의 마지막 판이다.** 이 판이 끝나면 남는 것은 사람의 종료 판정뿐이다.

#### N 을 무엇으로 읽는가 — 수치처리이지 물리 가정이 아니다

**사람이 정했다**: N 값 집합은 **수치 수렴 확인**이다. N 을 늘리다가 결과가 더
움직이지 않는 지점을 찾는 것이며 **격자 수렴 시험과 같은 성격**이다. 5-1 빈칸
처리 순서 **4번**(추가 파라미터 0의 규약)에 해당하고 **새 가정치를 만들지
않는다.** N 을 `assumptions.py` 에 넣지 않는다 — 세션 4 의 M 배분 민감도(#20),
세션 5.6 의 배치 자유도와 **같은 취급**이다.

**누출은 K 근사로 본다**(사람이 정했다). 이 판이 답하는 것은 「현재 데이터셋의
시간축을 믿을 수 있는가」이고 데이터셋은 K 근사 전용이다. 질량손실 정의(#36)는
이 판에서 보지 않는다.

#### 모델 — 현재 구조를 N=2 로 정확히 포함한다

현재 2노드는 **공급 다리 1탱크 · 환수 다리 1탱크**다. N 을 **총 노드 수**로
읽고 각 다리에 **N/2 탱크를 직렬**로 둔다(N 은 짝수). 5-1 「M 의 노드 배분」이
공급 50 · 환수 50 이므로 다리별 질량은 M/2 이고, **각 다리 안에서 균등 분배**
한다 — 불균등 분배는 5장에 없는 배분비를 새로 요구한다(추가 파라미터 0)::

    [공급 다리 N/2 탱크] → 랙(Q_rack 주입) → [환수 다리 N/2 탱크] → HX(Q_hx 제거) ↩

    공급 탱크 j : m_c·cp·dTc_j/dt = ṁ·cp·(Tc_{j-1} − Tc_j),  Tc_0 = T_ret − Q_hx/(ṁcp)
    환수 탱크 j : m_h·cp·dTh_j/dt = ṁ·cp·(Th_{j-1} − Th_j),  Th_0 = T_sup + Q_rack/(ṁcp)

    T_sup = Tc_마지막 · T_ret = Th_마지막 · m_c = m_h = (M/2)/(N/2)

랙과 열교환기는 **보유량 0 의 경계 열원**이다 — 5-1 이 콜드플레이트·HX 보유량을
M 에서 빼 두었으므로(#21) 그것이 현재 모델의 읽기이고, 이 판은 그 읽기를 바꾸지
않는다. 물성 평가온도와 매 시점 quasi-steady 수력은 **기존 규약 그대로**다
(5-1 · 절대 규칙 4).

**N=2 에서 위 식은 현재 `dynamics._derivative` 와 항등적으로 같다** — 대입하면
`ṁcp·(T_ret − T_sup) − Q_hx` 와 `ṁcp·(T_sup − T_ret) + Q_rack` 이 그대로 나온다.
그래서 N=2 는 **대조 기준**이지 새 계산이 아니다.

#### N 값 집합

**N ∈ {2, 4, 8, 16, 32, 64}** — 2 를 반드시 포함하고(현재 구조) 배로 늘린다.
현재 구조의 **32배**까지 뻗으므로 기준 C 의 수렴 판정이 성립할 여지가 있다.
**수렴하지 않으면 「스윕 범위 안에서 수렴하지 않았다」를 그대로 적는다.**

#### 판정 기준 — 게이트가 아니다

- **A. 정상상태 불변.** N 을 바꿔도 정상상태 해가 변하지 않아야 한다. 노드 분할은
  보유량을 나누는 것이지 정상상태 방정식을 바꾸지 않는다(dT/dt=0 이면 같은 다리
  안의 탱크가 전부 같은 온도가 되므로 N 이 사라진다). **변하면 그 자리에서 멈추고
  보고한다** — 스윕 구현에 결함이라는 뜻이다.
- **B. 전이 지표의 이동.** N 별로 **τ · t63 · t95** 를 내고 **N=2 대비 변화율(%)**
  을 적는다.
- **C. 수렴.** N 을 늘릴 때 B 의 지표가 **더 움직이지 않는 지점이 있는가.** 있으면
  그 N 과 **그 지점에서 N=2 대비 차이**를 적는다. 없으면 **수렴하지 않았다고 적는다.**
- **D. 누출 신호의 부호.** 누출 3수준의 신호(총유량 · 펌프 양정 · 누출랙 온도 ·
  환수온도)의 **부호가 N 에 따라 뒤집히는가.** 뒤집히지 않으면 세션 4 게이트가 N 에
  강인하다는 뜻이다. **뒤집히면 그것이 이 판의 가장 큰 발견이므로 그대로 적는다.**
- **E. M 정합성.** **τ ∝ M** 이 N 을 바꿔도 성립하는가 — 6장 재정의 기준(세션 2.5)이
  노드 수에 흔들리는지 본다.

#### 가설 (예단하지 말 것)

이동지연은 실제 배관에서 **플러그 흐름**에 가깝고 CSTR 1개는 그것을 완전혼합으로
근사하므로, N 이 늘면 응답이 **더 지연되고 파형이 가팔라질** 수 있다.
**가설이다. 결과를 여기에 맞추지 않는다.**

#### 규모 — 측정 후 확정하고 줄인 것을 적는다

**전 조합은 32(5장·5-1 양 끝) × 부하 2 × M 2 × 누출 3 = 384 적분/N** 이고 N 6수준
이면 2,304 적분이다. 현재 2노드 1건이 **약 1.18 s**(실측)이고 N 이 늘면 ODE 차원과
스텝 수가 함께 늘므로 **60분 정지선을 넘는다.** 그래서 **2단으로 나눈다**:

1. **수렴 스윕** — N 6수준 전부. 케이스는 **5장 범위를 벌리는 축만** 남긴다.
2. **반례 확인** — N 을 **2 와 수렴 N 둘**로 고정하고 **전 조합 384** 를 돌려
   C·D 의 결론이 조합에 걸리는지 본다.

**정확한 규모와 실측 시간은 코드를 돌리기 전에 산출해 적고, 줄였다면 무엇을 왜
줄였는지 함께 적는다**(§6). 60분을 넘으면 멈추고 보고한다.

#### solver (절대 규칙 5)

`fsolve` `ier` 와 `solve_ivp` `success` 를 전 조합에서 확인해 결과에 싣는다.
실패는 **버리지 않고 표시한다**. **수치해 수렴(기준 C)과 solver 수렴은 다른
것이며 보고에서 헷갈리게 적지 않는다.**

#### 이 판이 판정하지 않는 것 (과대해석 금지)

- **6장 기준 넷을 게이트로 판정하지 않는다.**
- **N 수렴은 「이동지연이 실제와 맞다」는 뜻이 아니다** — 실측이 없다. 수치
  이산화가 결과를 지배하지 않는다는 뜻**까지**다.
- **수렴시간의 절대값을 해석하지 않는다** — #21·#25·#23 이 열려 있다.
- 미해결 **#36 의 (가)/(나)/(다) · #38 · #39 를 고르지 않는다.**

### 세션 5.8 · 2026-08-31 · 이동지연 감도 (N-CSTR 수치 수렴) — 파일럿의 마지막 판

**관측 판이다. 게이트 없음 · feasibility 판정 없음 · 데이터셋 재생성 없음 ·
모델 무수정.** 판정 기준은 위 「세션 5.8 … 판정 기준 선기재」에 **코드보다 먼저**
적고 먼저 커밋했다(`2cfaa7a`). **기준을 결과에 맞춰 고치지 않았다.**

스윕은 **별도 파일** `transport_lag.py` 에서 했고 현재 2노드 구조를 건드리지
않았다. `model.py`·`plant.py`·`dynamics.py` 는 **읽기·호출만** 했다.

#### 규모와 실행시간 — 줄인 것과 이유 (C3 · §6)

| 단 | 규모 | 실측 |
|---|---|---|
| 1단 수렴 스윕 | 24 케이스 × N 6수준 = **144 적분** | 약 13 분 |
| 2단 반례 확인 | **384 전 조합** × N 2수준 = **768 적분** | 약 33 분 |

합계 약 **46 분**으로 60분 정지선 안이다. **줄인 것 둘과 그 이유**:

- **1단의 케이스를 24 로 줄였다** — 수력 양 끝 두 모서리 × 부하 2 × M 2 × 누출 3.
  NTU·2차측 공급온도를 5장 **하한**에 고정했다. 까닭은 시간이다: 전 조합 384 ×
  N 6수준 = 2,304 적분이면 정지선을 넘는다(현재 2노드 1건 실측 1.18 s, N=64 는
  15.49 s). **줄인 축은 2단에서 전수로 되살렸다.**
- **2단의 고운 격자를 N=16 으로 잡았다** — **이것은 「수렴한 N」이 아니다.**
  1단이 수렴을 찾지 못했으므로 수렴 N 이 존재하지 않는다. 60분 안에서 전 조합을
  볼 수 있는 가장 고운 격자가 N=16 이었다(N=32 면 합계 71분 · N=64 면 106분).
  **schedule 이 정한 값이며 물리가 정한 값이 아니다.**

#### 모델과 N=2 항등 — 대조 기준이 성립하는가

N 을 **총 노드 수**로 읽고 각 다리에 N/2 탱크를 직렬로 뒀다. 다리별 질량은 5-1
「M 의 노드 배분」대로 M/2 씩이고 **다리 안에서 균등 분배**다(추가 파라미터 0).
랙·HX 는 **보유량 0 의 경계 열원**이다 — 5-1 이 콜드플레이트·HX 보유량을 M 에서
빼 둔 읽기 그대로다(#21).

**N=2 가 현재 `dynamics.integrate_leak_step` 과 항등임을 실측으로 확인했다** —
같은 저장격자에서 최대 편차 **6.3e-13 K**. 테스트가 이것을 고정한다.

#### A 정상상태 불변 — 성립한다

- **누출 전** 정상상태는 대수적으로 푼 값이라 적분오차가 없다 — N=2,4,8,16,32,64
  에서 **완전히 같다**(42.469049144965 ℃, 비트 단위).
- **누출 후** 값은 30τ 까지 적분한 끝점이라 적분기 허용오차가 쌓인다. 편차는
  최대 **2.9e-9 K** 이고 **부호가 N 에 대해 무작위**다(+5.6e-11, −6.2e-10,
  +4.8e-10, −2.9e-09, −6.1e-10) — 구조적 차이가 아니라 잡음이다.

> **테스트 허용오차를 한 번 고쳤다 — 무엇을 고쳤는지 적는다.** 처음 1e-9 K 로
> 잡았다가 4건이 실패했다. 실패 원인을 확인해 보니 위의 적분기 잡음이었고(누출
> 전 값은 완전 일치했다), 허용오차가 **적분기 잡음 바닥보다 낮게** 잡혀 있었다.
> 그래서 누출 전은 **완전 일치**로 강화하고 누출 후만 잡음 바닥(1e-7 K)에 맞췄다.
> **기준 A 의 문언을 고친 것이 아니라 측정 정밀도를 잡음 바닥에 맞춘 것이다** —
> 신호(~1.6e-2 K)보다 다섯 자리 작다.

#### B·C 전이 지표 — **수렴하지 않았다**

| N | t63 변화율 [%] (N=2 대비) | 파형 편차 [K] (직전 N 대비) | 편차 / 순신호 [%] |
|---|---|---|---|
| 2 | +0.000 ~ +0.000 | — | — |
| 4 | −1.991 ~ +33.483 | 7.256e-05 ~ 3.049e-03 | 12.7553 ~ 23.2707 |
| 8 | −8.195 ~ +59.169 | 6.580e-05 ~ 2.865e-03 | 11.5646 ~ 22.1061 |
| 16 | −11.668 ~ +78.796 | 6.116e-05 ~ 2.914e-03 | 10.7529 ~ 24.7881 |
| 32 | −13.231 ~ +92.683 | 5.722e-05 ~ 2.711e-03 | 10.0503 ~ 22.7794 |
| 64 | −14.422 ~ +102.090 | 5.423e-05 ~ 2.438e-03 | 9.5241 ~ 19.0882 |

**스윕 범위(N≤64) 안에서 수렴하지 않았다.** 억지로 수렴했다고 적지 않는다.

- **파형 편차가 거의 줄지 않는다** — N=4→64 로 **16배** 세분했는데 직전 N 대비
  편차가 3.05e-03 → 2.44e-03 K 로 **1.25배**밖에 줄지 않았다. CSTR 직렬이
  플러그 흐름으로 가는 수렴은 원래 느리지만(체류시간분포 표준편차 ~1/√N),
  그것보다도 느리다.
- **t63 이 계속 이동한다** — N=64 에서도 N=2 대비 −14.4%~+102.1% 이고 **폭이
  N 과 함께 계속 벌어진다.**
- **부호가 갈린다** — 어떤 조합은 t63 이 줄고(−14%) 어떤 조합은 는다(+102%).
  「N 이 늘면 응답이 더 지연된다」는 가설은 **전 조합에서 성립하지 않았다.**

> **t63 을 선형보간으로 바꿨다 — 관측 정의를 바꾼 것이지 물리를 바꾼 것이
> 아니다.** `dynamics.time_to_fraction_s` 는 저장격자에 스냅한 값을 낸다. 처음
> 그 정의로 재니 **N=32 와 N=64 의 t63 이 소수점 16자리까지 같게** 나왔고,
> 그것은 수렴이 아니라 **격자 해상도가 차이를 가린 것**이었다. 브래킷 구간에서
> 선형보간해 격자 아래 해상도를 얻었다. 저장격자는 N 에 의존하지 않으므로
> (τ·t_end 가 N 무관) N 간 비교는 그대로 공정하다. **수렴 지표도 t63 하나가
> 아니라 파형 최대 편차를 함께 본다** — 위 표의 셋째·넷째 열이 그것이다.

#### D 누출 신호의 부호 — **N 에 뒤집히지 않는다**

| 양 | N=2 부호 | N=16 부호 | 최대 \|차이\| |
|---|---|---|---|
| ⑴ 총유량 | **−** | **−** | 1.862e-11 |
| ⑵ 펌프 양정 | **+** | **+** | 7.283e-12 |
| ⑶ 누출랙 출구온도 | **+** | **+** | 4.303e-09 |
| ⑷ CDU 환수온도 | **+** | **+** | 7.455e-09 |

**전 조합 384건에서 부호가 하나도 뒤집히지 않았다.** 차이는 전부 적분기 잡음
수준이다. **전이 경로가 순변화와 반대 부호로 벗어난 최대폭은 6.395e-14 K** —
경로 도중에도 부호가 뒤집히지 않는다(오버슈트·언더슈트 없음).

**까닭을 함께 적는다**: 이 신호 넷은 **정상상태 사이의 순변화**이고 기준 A 가
정상상태의 N 불변을 보장하므로, **부호가 N 에 불변인 것은 구조적으로 당연하다.**
D 가 확인한 것은 그 구조가 코드에서 실제로 성립한다는 것과, **전이 경로도 부호를
뒤집지 않는다**는 것 둘이다. **세션 4 게이트는 노드 수에 강인하다.**

#### E τ ∝ M — N 과 무관하게 성립한다

| N | t63 비 (M 상한/M 하한) |
|---|---|
| 2 | 12.832649 ~ 12.832794 |
| 4 | 12.832768 ~ 12.832781 |
| 8 | 12.832665 ~ 12.832800 |
| 16 | 12.832766 ~ 12.832791 |
| 32 | 12.832766 ~ 12.832783 |
| 64 | 12.832770 ~ 12.832794 |

M 비(1157.5805/90.2050 = **12.832779**)와 **여섯 자리까지 일치**하고 N 이 32배
늘어도 흔들리지 않는다. **6장 재정의 기준(세션 2.5 — τ 가 M 에 비례하는가)은
노드 수에 흔들리지 않는다.**

#### 반례 확인 (collaboration.md ⑥)

- **C·D 를 전 조합 384(수력 8 × NTU 2 × 2차측 2 × 부하 2 × M 2 × 누출 3)에서
  확인했다.** D 는 뒤집힘 0건. C 의 미수렴도 조합에 관계없이 성립한다 — N=2 대
  N=16 파형 편차가 순신호 대비 **31.72~57.96%** 로 **전 조합에서 크다.**
- 1단에서 줄였던 NTU·2차측 축은 2단에서 되살렸고 결론이 바뀌지 않았다.

#### solver (절대 규칙 5)

1단 144 + 2단 768 = **912 적분 전부 `solve_ivp` success**. 내부 수력 `fsolve` 는
실패 시 예외를 던진다(`solve_flow_distribution`) — 예외 없었다. **실패 0건.**

> **수치해 수렴(기준 C)과 solver 수렴은 다른 것이다.** solver 는 전부 수렴했고
> (912/912), **수치해가 N 에 대해 수렴하지 않았다.** 둘을 섞어 읽지 않는다.

#### 이 판이 판정하지 않은 것 (과대해석 금지)

- **6장 기준 넷을 게이트로 판정하지 않았다.**
- **N 수렴 실패는 「이동지연이 실제와 다르다」는 뜻이 아니다** — 실측이 없다.
  뜻하는 것은 **수치 이산화(노드 수)가 전이 파형을 지배한다**는 것까지다.
- **수렴시간의 절대값을 해석하지 않는다** — #21·#25·#23 이 열려 있다.
- 미해결 **#36 의 (가)/(나)/(다) · #38 · #39 를 고르지 않았다.**

#### 만든/바꾼 파일

- **신규**: `src/cdu_simul/transport_lag.py` ·
  `tests/test_session58_transport_lag.py`
- **바꿈**: `src/cdu_simul/dataset_report.py` — **#37 문구만**(§7). 행수는
  하드코딩하지 않고 `enumerate_specs()` 에서 센다.
- **바꿈**: `PROCEED.md`
- **안 건드림**: 데이터셋(52열본) · `model.py` · `plant.py` · `dynamics.py` ·
  `assumptions.py` · `leak_massloss*.py` · 정본 문서 4종

#### 종료 판정에 넘기는 것 — **판정하지 않는다. 재료만 넘긴다**

**파일럿의 판은 전부 끝났다.** 남은 것은 사람의 종료 판정뿐이다
(`project-overview.md` 「파일럿 종료 조건」). 이 판이 종료 판정에 더하는 재료:

1. **정상상태 행과 부호 기반 게이트는 노드 수에 강인하다.** 세션 4 누출 신호의
   부호(전 조합 384) · τ ∝ M(6장 재정의 기준) 둘 다 N 에 흔들리지 않는다.
2. **전이 파형은 그렇지 않다.** 2노드와 16노드의 파형이 순신호 대비 31.7~58.0%
   다르고, N≤64 안에서 수렴하지 않았다. **데이터셋의 전이 행에서 파형의 모양과
   t63 의 값을 그대로 읽으면 안 된다** — 새 미해결 **#40**.
3. 그 유보는 **세션 5.7-D 의 #38**(질량손실 전이의 상수 M 근사)과 **다른
   결손**이다. #38 은 질량손실 모사 안에서의 유보이고, #40 은 **현재 데이터셋의
   K 근사 전이 행 전체**에 걸린다.

#### 다음 판

**없다 — 파일럿의 마지막 판이다.** 사람의 종료 판정을 기다린다.

### 세션 5.9 · 2026-08-31 · 파일럿 종료 판정 재료 통합 (문서 판 · 판정하지 않았다)

**문서 판이다. 게이트 없음 · 코드 로직 무수정 · 데이터셋 무수정 · 수치 변경 0 ·
새 실행 0.** 기존 기록의 수치를 **옮긴 것**이지 새로 잰 것이 아니다.

**★ 판정하지 않았다.** `project-overview.md` 「파일럿 종료 조건」이 판정 주체를
**사람**으로 못박는다. 이 판은 재료를 모으는 것까지이고, 「통과」·「충분하다」·
「전환할 만하다」 같은 판정어를 **새로 쓰지 않았다.** 기존 게이트 기록의
「판정: 통과」는 과거 판의 문구를 **옮긴 것**이며 이 판이 새로 내린 판정이 아니다.

#### 어디에 무엇을 실었는가 — 중복을 만들지 않았다

`collaboration.md` ④(단일 진실)에 따라 **기존 절과 겹치는 것은 새로 쓰지 않고
그 절을 보강**했다. 새로 세운 절은 기존에 자리가 없던 셋뿐이다.

| 재료 | 어디에 |
|---|---|
| ⑴ 구성요소 독립·결합 | **기존 ① 절 보강** — 5.5-D 물리 검산 12항 + 성격 + #15 |
| ⑵ 6장 네 기준 | **기존 ② 절 보강** — ③ 에 #40 · 재정의 기준 구분 |
| ⑶ 시나리오 신호 구분 | **기존 ③ 절 보강** — 5.8 D · 5.7-D 넷 · #37 |
| ⑷ 가정치 양 끝 대조 | **기존 ④ 절 보강** — 5.7 M 8배 · 5.8 A |
| ⑸ 데이터셋 읽는 법 | **새 ⑤ 절** — 자리가 없었다 |
| ⑹ 답하지 못하는 것 | **새 ⑥ 절** — 자리가 없었다 |
| ⑺ 설계데이터가 오면 | **새 ⑦ 절** — 자리가 없었다 |
| #40 의 영향 열 | **기존 「열린 한계」에 추가** — 열 단위 영향은 그 절이 정본 |

⑸ 의 표는 한 줄 판정만 싣고 **열 단위 영향은 「열린 한계」 #40 을 가리킨다** —
같은 사실이 두 자리에 다른 상세도로 적히지 않게 했다.

#### 기록끼리 어긋난 것 — 하나 있었다

**`dataset_report` 의 6장 ③·④ 번호가 정본과 뒤바뀌어 있었다.**

- **정본** 「AI서버_CDU_디지털트윈_프로젝트정리.md」 6장(136~141행):
  ① 에너지 balance · ② T_return 방향성 · **③ 수렴시간(τ ∝ M)** · **④ 극단 케이스**
- **`CLAUDE.md` 절대 규칙 10**: 같은 순서
- **`dataset_report`(고치기 전)**: **③ 극단 케이스 · ④ 수렴시간** — 둘이 바뀌어 있었다

**고르지 않았다** — 정본과 `CLAUDE.md` 가 일치하고 리포트만 어긋났으므로 정본이
정한 것이다. **번호와 표시 순서만** 맞췄고 **판정 문구·수치는 한 글자도 고치지
않았다.** 정본 문서는 건드리지 않았다.

#### 세션 5.5-D ⑸ 를 옮기면서 뺀 문장 하나

5.5-D ⑸ 는 목록 끝에 「**세 판을 다 해도 판정은 "정확하다"가 아니라 "이 구조로
설계데이터를 넣을 만하다"까지다**」라는 문장을 달고 있다. **그 문장은 옮기지
않았다** — 이 판은 판정어를 쓰지 않기로 했고(§6), 리포트에 실으면 **리포트의
결론으로 읽힐** 수 있기 때문이다. 목록 항목 자체는 전부 옮겼다.
**그 문장은 `PROCEED.md` 세션 5.5-D 기록에 그대로 남아 있다.**

#### 출처 표기

리포트에 새로 실은 수치는 **전부 세션 번호를 달았다** — 5.5-D(물리 검산 12항) ·
5.6(관측 ④) · 5.6-B(Δ) · 5.7(M 8배) · 5.7-D(A·B·G·D · #37 320행) ·
5.8(A·C·D·E). **이 판이 새로 계산한 값은 하나도 없다.**

#### 검사기

코드 **로직**을 건드리지 않았으므로 그대로 통과했다 — pytest **1,207건** ·
`ruff check .` All checks passed · `mypy src tests` 28 files, no issues.

#### 이 판이 하지 않은 것

- **판정하지 않았다.** 종료 판정은 사람 몫이다
- 미해결 **#36 (가)/(나)/(다) · #38 · #39 · #40** 을 고르거나 좁히지 않았다 —
  재료로만 적었다
- ⑦ 「설계데이터가 오면」에 **우선순위를 매기지 않았다**
- 과거 게이트 판정을 다시 쓰지 않았다 — 옮겼을 뿐이다
- 정본 문서 4종을 고치지 않았다(**재업로드 필요 없음**)

#### 다음

**없다 — 파일럿의 판은 전부 끝났다.** 사람이
`python -m cdu_simul.dataset_report` 를 보고 종료 판정을 한다.

### 세션 6.0 · 2026-08-31 · 파일럿 종료 판정 기록 (사람 판정 · 정본 수정)

**판정 기록 판이다. 게이트 없음 · 코드 무수정 · 데이터셋 무수정 · 수치 변경 0 ·
새 실행 0.**

**판정은 이미 내려졌다.** 사람이 `python -m cdu_simul.dataset_report` 를 보고
판정했고(`project-overview.md` 「파일럿 종료 조건」 — 판정 주체는 사람), 이 판은
그것을 **옮겨 적었다.** 판정을 다시 하거나 근거를 새로 만들지 않았다.

#### 사람이 내린 판정 — 여덟 항목

| # | 항목 | 판정 |
|---|---|---|
| 1 | 구성요소가 각각 독립 모델로 서고, 물렸을 때 전체 거동이 나오는가 | **O** |
| 2 | 6장 ①②④(balance · T_return 방향성 · 극단 케이스) 통과를 근거로 받아들이는가 | **O** |
| 3 | 6장 ③(수렴시간) 판정 불가를 감수하고 넘어가는가 | **O** |
| 4 | 가정치를 바꿨을 때 결과가 예상대로 움직이는가 | **O** |
| 5-a | 데이터셋 **안에서** 정상/이상/누출이 구분되는가 | **O** |
| 5-b | 그 구분이 **실제 누출**의 방향과 같은가 | **X** |
| 6 | 현재 데이터셋(52열본)을 AI 학습 데이터로 쓸 것인가 | **X** |
| 7 | 본 프로젝트에서 누출 정의를 질량손실로 교체할 것인가 (#36) | **O — 단, 이 파일럿이 끝난 뒤 새로 시도한다** |
| 8 | 본 프로젝트로 전환할 것인가 | **O — 조건부** |

**8 의 조건부**:

> **모델 구조는 설계데이터를 넣을 만하다.** 단, ⑴ 누출 정의를 질량손실로
> 교체하고 ⑵ 전이 시간축 표현을 다시 세우고(#40) 들어간다.

⑵ 의 까닭은 **조기감지가 본질적으로 시간 문제**라는 것이다 — 전이 파형을 못 쓰는
상태로 학습 데이터를 만들면 「언제부터 이상했는가」를 배울 수 없다.

**「정확하다」가 아니다.** 이 판정은 「이 구조로 설계데이터를 넣을 만하다」까지이며
정확도는 실측 없이 잴 수 없다.

#### `project-overview.md` 를 어떻게 고쳤는가

- **머리말** — 「세션 1-A~5.5 완료 · 남은 것은 사람의 종료 판정」 →
  「세션 1-A~5.9 완료 — 파일럿이 끝났다 · 종료 판정이 났다(2026-08-31 · 사람)」
- **「파일럿 종료 조건」 절** — **지우지 않았다.** 조건이 무엇이었는지가 판정의
  근거이므로 남기고, 그 아래에 **「판정 결과」** 를 새로 붙였다: 여덟 항목 ·
  판정 일자 · 판정 주체(사람) · 재료(`dataset_report`).
  **근거 수치는 되풀이하지 않고 리포트를 가리켰다**(`collaboration.md` ④).
  다만 판정문에 **직접 적은 넷**이 있다 — 1 의 한계(#15) · 3 의 근거(6장 ③ 이
  세션 2.5 에서 τ ∝ M 으로 재정의됐고 5.8 E 가 그 정합성의 노드 강인성을
  확인했다 · #21·#25·#23 은 설계데이터로 닫히지만 **#40 은 닫히지 않는다**) ·
  5-b 의 뜻(**답하지 못한 것이 아니라 답이 「같지 않다」다** · 총유량 신호는
  +50% 에서도 −0.16~0.37%) · 6 의 뜻(**「버린다」가 아니다** — 정상상태 행과
  부호 기반 신호는 시연·구조 검증에 쓸 수 있다).
- **「다음 단계」** — 판정 7·8 이 가리키는 것을 **목록으로만** 적었다.
  시연 데이터 준비 · 누출 정의 질량손실 교체(#36) · 전이 시간축 재수립(#40) ·
  설계데이터 확보 시 채울 것(`dataset_report` ⑦절을 가리킨다).
  **`CLAUDE.md` 게이트 표를 고치지 않았다** — 그것은 설계데이터가 올 때 정한다.
- **「지금 어디인가」** — 체크리스트에 **5.6 · 5.7 · 5.7-D · 5.8 · 5.9** 와
  **파일럿 종료 판정** 행을 더했다. 기존 형식(한 줄 요약 + 게이트/판정 결과)을
  따랐고 **기존 1-A~5.5 행은 건드리지 않았다.**
- **그대로 둔 문구 둘** — 「세션 6은 여전히 설계데이터가 있어야 열린다」(판정이
  「전환」으로 나왔어도 데이터가 없으면 세션 6 이 아니라 다른 형태의 다음 단계라는
  뜻이며 지금이 정확히 그렇다) · 「이 절과 `PROCEED.md` 가 어긋나면 `PROCEED.md`
  가 맞다」.

#### diff — `project-overview.md` 밖의 정본 변경 0

`git diff --name-only` = **`project-overview.md` 하나**(87 insertions · 4 deletions).
`CLAUDE.md` · `collaboration.md` · `AI서버_CDU_디지털트윈_프로젝트정리.md` 를
하나도 건드리지 않았다. 모델 코드·테스트·데이터셋(52열본)·`dataset_report`·
`leak_massloss*.py`·`transport_lag.py` 도 무수정이다.

#### 미해결 — 하나도 닫지 않았다

**#15 · #21 · #23 · #24 · #25 · #35 · #36 · #37 · #38 · #39 · #40** 전부 열린 채다.
**판정이 났다고 결손이 사라지지 않는다** — 그 사실을 판정 결과 절에도 적어 두었다.

#### 검사기

코드를 안 건드렸으므로 그대로 통과했다 — pytest **1,207건** ·
`ruff check .` All checks passed · `mypy src tests` 28 files, no issues.

#### 재업로드

**정본 문서를 고쳤다 — 프로젝트 지식 재업로드가 필요하다**
(`CLAUDE.md` 「작업 시작 전 필독」 4). 대상은 **`project-overview.md` 하나**다.

#### 다음 판

**시연 데이터 준비.** 범위는 사람이 정한다.

### 세션 7.0 · 2026-08-31 · 시연 데이터 준비 ① (열 매핑 · 시연용 정상상태 실행)

**파일럿 이후의 판이다.** 파일럿은 2026-08-31 에 종료 판정이 났고(「전환 O,
조건부」), 이 판은 판정 8 이 가리킨 「시연 데이터 준비」다.
**게이트 없음 · feasibility 판정 없음 · `CLAUDE.md` 게이트 표 무수정.**

#### ⑴ 열 매핑 — `demo/tag-map.md` (먼저 커밋했다)

코드를 쓰기 전에 **무엇을 채울 수 있는지부터** 확인했다.

| 구분 | 개수 | 무엇 |
|---|---|---|
| ⑴ 52열본에 열로 있다 | 9 | 랙별 유량·랙별 출구온도·`T_supply_C`·`T_return_C`·`total_flow_Lps`·`T_secondary_supply_C`·`secondary_share_Lps`·`hx_duty_kW`·`load_percent` |
| ⑵ 기존 공개 함수로 계산된다 | 1 | CDU 부하 [kW] = `CduCase.rack_load_kW` × `SCENARIO.racks_per_cdu` — **곱셈 하나**이고 새 숫자가 아니다 |
| ⑶ 둘 다 아니다 → **뺐다** | 1 | **2차측 환수온도** |

**2차측 환수온도를 뺀 이유**: 모델에 없는 양이다. 열도 없고 그것을 내는 공개
함수도 없다 — 2차측은 **고정 경계조건**이다(절대 규칙 7 · 5-1). `model.py` 가
「2차측 출구온도는 모델에 없으므로 벌크평균을 만들 수 없다 — 선택이 아니라
강제다」라고 적고 있다. ε-NTU 로 역산하면 숫자는 나오지만 **그것은 새 모델**이다
— 다중 CDU 에서는 두 CDU 의 환수가 공유 2차측에서 섞이므로 혼합까지 세워야 하고
그것이 「2차측 동특성을 임의로 모델링」이다. **새 숫자를 만들어 채우지 않았다**
(절대 규칙 1).

**있지만 쓰지 않은 태그** — 양정·분기 ΔP·밸브 ΔP·잔여저항 ΔP·지점별 정압.
세션 7.0 §3⑵ 의 사람 결정이다. 없어서가 아니라 **계통 정압이 모델에 없고
잔여저항이 집중저항 하나여서**(미해결 #24) 화면에 붙일 근거가 없다.

#### ⑵ 시연용 실행 — `src/cdu_simul/demo_steady.py`

**규모와 예상 실행시간을 먼저 산출했다**(§9 멈춤 조건): 부하 17점 × 저항 4수준
× 구성 2종 = **136 케이스**, 단일 0.020 s/건 · 다중 0.143 s/건 측정에서 **약
11 초** 예상 — 10분 한참 아래라 진행했다. **실측 12.8 초.**

- **정상상태만 돌렸다. 전이를 돌리지 않았다** — 전이 파형이 노드 수에 수렴하지
  않는다(미해결 #40 · 세션 5.8 C). 그래서 **케이스 배열은 시간축이 아니다**:
  화면에서 케이스가 바뀌는 것은 **케이스 전환의 표시**이지 시간 경과가 아니며,
  그 문구를 JSON 전 케이스에 실었다
- **부하 축 20~100% · 5% 간격 17점** (5장 양 끝 20·100 포함). 양 끝이 빠지면
  코드가 `ValueError` 로 막는다
- **다중 CDU(2대)**: CDU 0 만 부하를 올리고 CDU 1 은 정격 100% 고정 — 공유 2차측
  총유량이 고정이라 배분이 이동하는 것이 연동의 실체다(5-1)
- **저항 4수준**: 기준 + 5장 3수준(+5/+20/+50%). CDU 0 의 랙 0 에만 건다

#### ★ 부하 중간값은 5장에 없는 숫자다 — 어디에 두었는가

5장은 「유휴 20% ~ 정격 100%」를 **범위로만** 주고 그 안의 점을 지정하지 않는다.

- **`assumptions.py` 에 넣지 않았다** — `LOAD_STEP_PERCENT` 는 시연 모듈 안에만 있다
- **5-1 에 적지 않았다** — 물리 가정이 아니라 **시연용 표본 추출**이다
- JSON 전 케이스에 「시연용 범위 샘플링 — 5장은 양 끝만 준다」를 실었다
- **데이터셋(52열본)은 여전히 양 끝 2수준 그대로다.** 둘을 섞어 읽지 않는다

**§6 이 허용한 예외는 이것 하나뿐이고, 그 밖에 5장·5-1 밖 숫자를 만들지 않았다.**

#### ⑶ 고정한 축 조합

| 축 | 값 | 왜 |
|---|---|---|
| 펌프 양정 | 20 mAq (하한) | 아래 |
| 분기 ΔP | 2 mAq (하한) | 아래 |
| 밸브 ΔP | 3 mAq (하한) | 아래 |
| NTU | 2 (하한) | 아래 |
| 2차측 공급온도 | 27℃ (하한) | 아래 |
| **M (보유수량)** | **쓰이지 않는다** | 정상상태 해가 M 에 불변임이 **세션 5.7** 에서 확인됐다(M 을 8랙 계통 전체로 정정해 8배가 됐어도 정상상태 해는 그대로였고 τ·t63·t95 만 8배 이동했다). 전이를 안 돌리므로 고정할 축이 아니다 |

**하단 조합을 고른 이유**: 세션 5 의 대표 표(`plant.format_plant_table`)가 이미
「수력·NTU·2차측을 범위 하단으로 고정」한 3케이스를 내고 있다. **같은 조합이라야
기존 기록과 나란히 읽힌다** — 시연은 전수가 필요 없다(§6). 조합은 JSON
`meta.fixed_axes` 와 전 케이스 표기에 밝혔다.

#### ⑷ 「누출」 대신 `resistance_increase`

**JSON 의 필드명·라벨에 「누출(leak)」을 단독으로 쓰지 않았다** —
`resistance_increase_percent` · `resistance_k_multiplier` · `resistance_label`
(「랙 저항 +20%」) · `resistance_rack_index` · `resistance_cdu_index` 다.
`leak_model: "K_approx"` 표기를 함께 싣는다(값은 `dataset.LEAK_MODEL_K_APPROX`
를 그대로 읽는다 — 두 벌로 적지 않았다).

**까닭은 파일럿 종료 판정 5-b 가 X 라는 것이다** — 데이터셋의 구분이 실제 누출의
방향과 같지 않다(세션 5.6 수력 272회 · 세션 5.7-D 열 1,536건에서 부호가 정반대로
나왔다). 다음 판의 화면도 이 이름을 따른다.

#### ⑸ JSON — `demo/demo_steady.json`

- **481 KB · 케이스 136 · solver 미수렴 0 건 · 태그 빈 칸 0**
- 구조: `demo_version` · `meta` · `cases[]`.
  케이스마다 `case_id` · `cdu_config` · `driven_load_percent` ·
  저항 5필드 · `leak_model` · `caveats` · `cdus[]`
- CDU 1대분: `load_percent` · `load_kW` · `T_supply_C` · `T_return_C` ·
  `total_flow_Lps` · `hx_duty_kW` · `T_secondary_supply_C` ·
  `secondary_share_Lps` · `rack_flows_Lps[8]` · `rack_outlet_C[8]` · `solver{}`
- **화면이 계산하지 않아도 되게 완성된 수치로 담았다.** HTML 은 표시만 한다
- **표기를 전 케이스에 반복해 실었다**(5-1 스키마 규약 안 A 와 같은 취지):
  `assumption_tag`(가정값 기반 — 실측 아님) · `steady_only`(시간 경과가 아니다) ·
  `load_sampling`(시연용 범위 샘플링) · `resistance_proxy`(누출의 대용) ·
  `total_flow_caveat` · `fixed_axes` · `excluded_tags`
- **solver 플래그를 전부 실었다**(절대 규칙 5): 수력 `ier` · 열 고정점 · CDU 바깥
  고정점 · 플랜트 상위 `ier`. **실패를 버리는 경로가 없다** —
  `failed_cases()` 가 목록으로 낸다

#### ⑹ 다시 읽어 검증했다 (C5)

`main()` 이 JSON 을 쓴 뒤 **파일에서 다시 읽어** 검증한다 — 케이스 수 136 ·
표기 존재 · 태그 10종 빈 칸 0 · 랙 배열 길이 8. 문제가 있으면 종료코드 1 이다.
**검증 통과.**

#### 데이터로 확인한 것 (판정이 아니라 관측이다)

- 부하 20→100% 에서 `T_supply` 28.03→32.17℃ · `T_return` 30.10→42.47℃ ·
  duty 128→640 kW 로 단조 상승
- 저항 +50% 에서 누출 랙 유량 1.938→1.784 L/s(**감소**) · 그 랙 출구온도
  42.47→43.35℃(**상승**) · 다른 랙 유량은 증가 · 총유량은 15.5056→15.4674 L/s
  (**−0.25%**, 총유량 표기 그대로다)
- 다중에서 **부하를 올리지 않은 CDU 1 이 움직인다** — 2차측 배분 15.5138→15.5000
  L/s, `T_return` 42.4599→42.4690℃. 작다(0.009 K). `plant.py` 가 적어 둔
  까닭 그대로다: 2차측 공급온도가 고정이라 **연동이 유량 경로로만 생기고 열
  경로로는 생기지 않는다** — 이것을 「CDU 간 상호작용이 작다」로 읽지 않는다
- 대칭점(다중 100/100 · 저항 0)에서 배분이 정확히 15.5 L/s 이고 `T_return` 이
  단일 CDU 값과 같다 — 세션 5-B C4 의 성질이 그대로 재현된다

#### 보고만 한다 — 고치지 않았다 (절대 규칙 13)

**52열본의 `heat_capacity_ratio` 열에 Cr 이 아니라 ε(열교환기 유효도)가 들어
있다.** `dataset_plan.PROVENANCE_COLUMNS` 는 그 열을 「Cr — 유도값이다(세션
5-B)」로 적어 두었는데, `dataset.steady_rows` 는 그 자리에
`result.thermal.hx_effectiveness` 를 넣는다. 같은 케이스에서 ε = 0.66705,
Cr = 0.99827 로 **다른 값**이다(Cr 은 `PlantSteadyStateResult.heat_capacity_ratios()`
가 낸다). Cr 은 이 시연의 태그가 아니라 시연 데이터에는 영향이 없다.
**데이터셋 수정은 이 판의 범위 밖이므로 고치지 않았다.**
`demo/tag-map.md` 「4. 보고만 한다」에도 적어 두었다.

#### 이 판이 하지 않은 것

- **데이터셋(52열본)을 건드리지 않았다** — `git status` 로 확인했다.
  `dataset.py`·`dataset_plan.py`·`dataset_report.py` 무수정, CSV 무수정
- **모델 계산 로직을 건드리지 않았다** — `model.py`·`plant.py`·`dynamics.py`·
  `hydraulics.py`·`leak*.py`·`transport_lag.py` 전부 읽기·호출만 했다
- **정본 문서 4종을 고치지 않았다** — **재업로드 필요 없음**
- **미해결을 하나도 닫지 않았다**
- **의존성을 늘리지 않았다** — 표준 라이브러리 `json` 뿐. matplotlib·GUI
  라이브러리를 도입하지 않았다(절대 규칙 12)
- **HTML 화면을 만들지 않았다** — 다음 판이다
- **전이·시간축을 돌리지 않았다**

#### 검사기

pytest **1,207건** · `ruff check .` All checks passed ·
`mypy src tests` 29 files, no issues.

#### 다음 판 — 시연 화면(HTML) 이 알아야 할 것

- **읽을 파일은 `demo/demo_steady.json` 하나**다. 계산하지 않는다
- **그릴 태그는 `demo/tag-map.md` 「1. 채운 태그」로 닫혀 있다.**
  2차측 환수 스트림을 그리지 않는다. 압력·양정·ΔP 를 붙이지 않는다
- **케이스 전환은 시간 경과가 아니다.** 화면에도 그 구분을 적는다
- **「누출」이라 쓰지 않는다** — `resistance_increase` 를 따르고, 그것이 누출의
  대용이라는 표기를 화면에 남긴다(판정 5-b 가 X)
- **「가정값 기반 — 실측 아님」을 화면에 표시한다**(절대 규칙 11).
  `meta.assumption_tag` 와 각 케이스 `caveats` 에 문구가 들어 있다
- 52열본에서 **Cr 을 읽으려 하지 않는다**(위 발견)
- 다중 CDU 의 연동 신호는 **작다**(0.009 K · 배분 0.09%). 축을 그 규모에 맞춰야
  보인다

### 세션 7.0-B · 2026-08-31 · 오늘 마무리 · 미해결 #41 기재 (기록 판)

**기록 판이다. 게이트 없음 · 판정 없음 · 수치 변경 0 · 코드 무수정 ·
데이터셋 무수정 · 새 실행 0.**

세션 7.0 이 「보고만 한다」로 남긴 발견을 **미해결 #41 로 열었다.**
`collaboration.md` ⑤ 세 줄 형식(관측 · 참이면 같이 보여야 할 것 · 가설)으로
적었고, **고치지 않았다** — 열 이름도 내용도 이 판에서 건드리지 않았다.

#### 확인한 것 — `dataset_report` 가 이 열을 읽는가

**읽지 않는다.** 코드로 확인했다.

| 무엇 | 결과 |
|---|---|
| `dataset_report` 가 `cdu_simul.dataset` 에서 가져오는 이름 | `DATASET_VERSION` · `LEAK_MODEL_K_APPROX` · `column_names` · `enumerate_specs` **넷뿐** |
| `column_names()` 를 어떻게 쓰나 | 373행 `f"열 {len(column_names())}개 …"` — **`len()` 안에서만.** 열 **개수 52** 를 낸다 |
| `column_names()` 내부 | `steady_rows(probe)[0].keys()` — **이름만** 취하고 값을 버린다 |
| `steady_rows`·`rows_for`·`generate` 직접 호출 | **없다** |
| CSV·pandas 로 데이터셋을 읽는 코드 | **없다** — `pandas` 는 55행 환경 확인 문자열에만 나온다 |
| 리포트에 나오는 Cr 언급 | 430행 「대향류 Cr≈1 의 접근온도 균일」 · 551·590행 「세션 5-B 에서 Cr 을 선언값에서 유도값으로 바꿨다」 — **세션 5-B 유도 규약에 대한 산문**이며 이 열에서 온 수치가 아니다. 리포트 어디에도 0.667·0.998 같은 값이 없다 |

**따라서 2026-08-31 파일럿 종료 판정 재료에 이 결손이 닿지 않는다.**
(판정 재료가 흔들리는지를 이 판이 판단한 것이 아니다 — **닿지 않는다는
사실만** 코드로 확인해 적었다.)

걸리는 것은 **52열본을 직접 읽는 쪽**이다. 시연 데이터
(`demo/demo_steady.json`)는 Cr 을 태그로 쓰지 않아 영향이 없다.

#### 이 판이 하지 않은 것

- **발견을 고치지 않았다** — `dataset.py`·`dataset_plan.py`·`dataset_report.py`
  무수정, CSV 무수정. 열 이름도 내용도 그대로다
- **가설 (가)/(나)/(다) 중 아무것도 고르지 않았다** — 사람이 정한다
- 정본 문서 4종 · 모델 코드 · 테스트 · `demo_steady.py` · JSON 무수정
  (**재업로드 필요 없음**)
- **다른 미해결을 하나도 닫지 않았다**

#### 검사기

코드를 안 건드렸으므로 그대로 통과했다 — pytest **1,207건** ·
`ruff check .` All checks passed · `mypy src tests` 29 files, no issues.

#### 다음 판

**시연 화면(HTML).** 세션 7.0 ⑻ 이 넘긴 것을 따른다 — 읽을 파일은
`demo/demo_steady.json` 하나 · 그릴 태그는 `demo/tag-map.md` 로 닫혀 있다 ·
케이스 전환은 시간 경과가 아니다 · 「누출」 대신 `resistance_increase` ·
「가정값 기반 — 실측 아님」 표시 · **52열본에서 Cr 을 읽지 않는다(#41)**.

### 세션 7.0-C · 2026-09-01 · 시연 데이터 훑기 (읽기 전용 판 · 계산하지 않았다)

**읽기 전용 판이다. 게이트 없음 · feasibility 판정 없음 · 계산 0 ·
코드 무수정 · 데이터셋 무수정 · JSON 무수정 · 새 실행 0.**
`demo/demo_steady.json` 에 **이미 있는 값**을 훑어 요약했을 뿐이다. 훑는 데 쓴
일회성 파이썬은 저장소에 커밋하지 않았다(사람이 정한 범위).

#### ⑴ 채운 태그 — `demo/tag-map.md` 「1. 채운 태그」 그대로

| PFD 태그 | 단위 | 어디에 붙는가 | JSON 경로 |
|---|---|---|---|
| 랙별 유량 | L/s | 랙 분기 스트림 (8개) | `cases[].cdus[].rack_flows_Lps[0..7]` |
| 랙별 출구온도 | ℃ | 랙 출구 스트림 (8개) | `cases[].cdus[].rack_outlet_C[0..7]` |
| CDU 1차측 공급온도 | ℃ | 1차측 공급 스트림 | `cases[].cdus[].T_supply_C` |
| CDU 1차측 환수온도 | ℃ | 1차측 환수 스트림 | `cases[].cdus[].T_return_C` |
| CDU 1차측 총유량 | L/s | 1차측 공급 스트림(펌프 토출) | `cases[].cdus[].total_flow_Lps` |
| 2차측 공급온도 | ℃ | 2차측 공급 스트림(고정 경계조건) | `cases[].cdus[].T_secondary_supply_C` |
| 2차측 배분유량 | L/s | 2차측 공급 스트림(CDU 몫) | `cases[].cdus[].secondary_share_Lps` |
| 열교환기 duty | kW | 열교환기 장치 | `cases[].cdus[].hx_duty_kW` |
| CDU 부하 | % | CDU 장치(랙 묶음) | `cases[].cdus[].load_percent` |
| CDU 부하 | kW | CDU 장치(랙 묶음) | `cases[].cdus[].load_kW` |

solver 플래그 넷도 케이스마다 실려 있다 — `cases[].cdus[].solver` 의
`hydraulic_ier` · `thermal_converged` · `outer_converged` · `plant_ier`
(+ 합성 `all_converged`).

#### ⑵ JSON 구조

- 최상위 키 **셋** — `demo_version`("demo-7.0") · `meta` · `cases`(길이 **136**)
- 케이스 한 건의 키(값 말고 이름만): `case_id` · `cdu_config` ·
  `driven_load_percent` · `resistance_increase_percent` ·
  `resistance_k_multiplier` · `resistance_label` · `resistance_rack_index` ·
  `resistance_cdu_index` · `leak_model` · `caveats` · `cdus`
  (+ dual 케이스에만 `fixed_cdu_load_percent`)
- `cdus[]` 원소의 키는 **전 케이스 동일**: 위 ⑴ 의 열 태그 열 개 + `solver`
- **축의 곱이 136 으로 맞는다** — `cdu_config` 2(`single` · `dual_asymmetric`)
  × `driven_load_percent` 17(20~100%, 5% 간격) × `resistance_increase_percent`
  4(0 · 5 · 20 · 50%) = **136**. 세 축의 조합이 중복·결손 없이 136 개 전부
  나온다(조합 유일값 136). 케이스당 `cdus` 길이는 single 1 · dual 2 이고
  68 : 68 로 갈린다

#### ⑶ 금지 항목 — `cases[]` 안에 하나도 없다

| 확인 대상 | `cases[]` 안 | 비고 |
|---|---|---|
| 압력 · 펌프 양정 · ΔP 계열 | **없음** | `mAq`·`kPa`·`head`·`dp` 를 이름에 가진 키가 `cases[]` 전체에 0개 |
| 2차측 환수 스트림 | **없음** | `T_secondary_return_C` 는 `meta.excluded_tags` 의 **키 이름**으로만 나온다(뺀 이유를 적은 자리이며 값이 아니다) |
| `heat_capacity_ratio` | **없음** | 파일 전체 텍스트에서도 0회. ε 도 실리지 않았다 — 미해결 #41 이 시연 데이터에 닿지 않음이 재확인됐다 |

**다만 `meta.fixed_axes` 에 rated 입력 상수 셋이 있다** —
`pump_head_rated_mAq` 20.0 · `branch_dp_rated_mAq` 2.0 ·
`valve_dp_rated_mAq` 3.0, 그리고 `hydraulic_label` "H22.4/dPb2/dPv3".
이것은 **케이스마다 계산된 압력값이 아니라 고정축 조합을 적은 입력 표기**이고,
`meta.excluded_tags` 가 같은 파일 안에서 「싣지 않았다」고 선언한 대상과 같은
이름들이다(`H22.4` 는 체절양정 H₀ = 22.40 mAq, `pump_head_rated_mAq` 는 정격
양정 20 mAq — 5-1 의 같은 곡선이라 서로 어긋난 것이 아니다).
**화면은 이 셋을 스트림 태그로 그리지 않는다** — 세션 7.0 §3⑵ 의 사람 결정이
그대로 유효하다.

#### ⑷ 태그가 케이스 간에 얼마나 움직이는가

`cdus[0]`(부하가 움직이는 CDU · 136 케이스)와 `cdus[1]`(부하 100% 고정 CDU ·
dual 68 케이스)을 나눠 봤다. **최소 변화량은 「0 이 아닌 최소 |차|」이고 그 그룹의
모든 케이스 쌍을 훑은 값이다.**

**부하 CDU (`cdus[0]` · 136 케이스)**

| 태그 | 최소 | 최대 | 0 아닌 최소 변화량 | 그 케이스 쌍 |
|---|---|---|---|---|
| `load_percent` [%] | 20 | 100 | 5 | `single/L20/R0` ↔ `single/L25/R0` |
| `load_kW` [kW] | 128 | 640 | 32 | `single/L20/R0` ↔ `single/L25/R0` |
| `hx_duty_kW` [kW] | 128.000000 | 640.000000 | 32 (1.4e-14 는 부동소수점 잡음) | `single/L75/R20` ↔ `single/L80/R50` |
| `T_supply_C` [℃] | 28.031866 | 32.168287 | **1.24e-08** | `dual/L35/R0` ↔ `dual/L35/R5` |
| `T_return_C` [℃] | 30.097839 | 42.494423 | **1.98e-04** | `single/L90/R5` ↔ `dual/L90/R0` |
| `total_flow_Lps` [L/s] | 15.439903 | 15.505582 | **2.00e-06** | `single/L95/R0` ↔ `dual/L95/R0` |
| `secondary_share_Lps` [L/s] | 15.467141 | 15.500000 | **9.00e-06** | `dual/L45/R5` ↔ `dual/L80/R20` |
| `rack_flows_Lps[0]`(저항 걸린 랙) | 1.781229 | 1.938198 | **2.49e-07** | `single/L95/R0` ↔ `dual/L95/R0` |
| `rack_flows_Lps[1..7]` | 1.934749 | 1.954723 | **2.49e-07** | 〃 |
| `rack_outlet_C[0]` | 30.097839 | 43.356805 | **5.70e-04** | 〃 |
| `rack_outlet_C[1..7]` | 30.080383 | 42.469049 | **5.70e-04** | 〃 |
| `T_secondary_supply_C` [℃] | 27.0 | 27.0 | **변화 없음** | — |

**부하 100% 고정 CDU (`cdus[1]` · dual 68 케이스) — 다중 CDU 연동 신호**

| 태그 | 최소 | 최대 | 전 범위 폭 | 0 아닌 최소 변화량 (케이스 쌍) |
|---|---|---|---|---|
| `T_return_C` [℃] | 42.449252 | 42.469049 | **0.0198 K** | 5.98e-06 (`dual/L45/R5` ↔ `dual/L80/R20`) |
| `T_supply_C` [℃] | 32.148415 | 32.168278 | **0.0199 K** | 5.996e-06 (〃) |
| `secondary_share_Lps` [L/s] | 15.500000 | 15.532859 | **0.0329 L/s (0.21%)** | 9.00e-06 (〃) |
| `total_flow_Lps` [L/s] | 15.505512 | 15.505582 | **7.0e-05 L/s** | 2.10e-08 (〃) |
| `rack_flows_Lps[0..7]` | 1.938189 | 1.938198 | **8.8e-09 L/s** | 2.63e-09 (〃) |
| `rack_outlet_C[0..7]` | 42.449252 | 42.469049 | **0.0198 K** | 5.98e-06 (〃) |
| `load_percent`·`load_kW`·`hx_duty_kW`·`T_secondary_supply_C` | 100 / 640 / 640 / 27 | 〃 | **변화 없음** | — |

**축 하나만 움직였을 때의 변화량**(화면 축 잡는 데 쓸 값이다)

| 무엇 | 부하 +5% 한 칸 | 저항 R0→R5 | 저항 R0→R50 |
|---|---|---|---|
| 부하 CDU `T_return_C` | 0.772 ~ 0.777 K | **+**5.7e-04 ~ +2.8e-03 K | **+**5.10e-03 ~ +2.54e-02 K |
| 부하 CDU `total_flow_Lps` | 1.6e-03 ~ 4.4e-03 L/s | **−**4.26e-03 L/s | **−**3.81e-02 L/s (−0.25%) |
| 부하 CDU `rack_flows_Lps[0]`(저항 랙) | 1.9e-04 L/s | **−**1.72e-02 L/s (−0.89%) | **−**0.1538 L/s (**−7.9%**) |
| 부하 CDU `rack_flows_Lps[1..7]` | 2.0e-04 L/s | **+**1.85e-03 L/s | **+**1.65e-02 L/s (+0.85%) |
| 부하 CDU `rack_outlet_C[0]`(저항 랙) | 0.772 K | **+**1.86e-02 ~ +9.25e-02 K | **+**0.178 ~ **+0.888 K** |
| 부하 CDU `rack_outlet_C[1..7]` | 0.767 K | **−**1.83e-03 ~ −1.13e-02 K | **−**1.62e-02 ~ −9.60e-02 K |
| **고정 CDU `T_return_C`** | 2.7e-04 K | **−1.41e-03 K** | **−1.06e-02 ~ −1.26e-02 K** |
| **고정 CDU `secondary_share_Lps`** | 8.2e-04 L/s | **+2.13e-03 L/s (+0.014%)** | **+1.90e-02 L/s (+0.123%)** |

**화면에서 가장 크게 움직이는 것은 랙 계열이다** — 저항이 걸린 랙0 은
유량 −7.9% · 출구온도 +0.888 K 이고, **나머지 7랙은 부호가 반대**로
유량 +0.85% · 출구온도 −0.096 K 다. 총유량은 −0.25% 뿐이라
세션 4 관측 ③(총유량만 보면 놓친다)이 이 데이터에서도 그대로다.

고정 CDU 의 R50 값(−0.010640 K @L20 · −0.012649 K @L100 · +0.019 L/s)은
**세션 5.5-D(−0.01064 K · +0.01908 L/s)·세션 5 C7(−0.012649 K · +0.01905 L/s)
관측과 일치**한다 — 새로 나온 숫자가 아니다.

**연동 신호가 실제로 얼마나 작은가** — 고정 CDU 환수온도의 전 범위 폭이
**0.0198 K** 이고 배분유량은 **0.21%** 다. 화면에서 이 태그의 축을 부하 CDU 와
같은 눈금으로 그리면 **선이 움직이지 않는 것으로 보인다.**

#### ⑸ 케이스를 바꿔도 한 번도 변하지 않는 태그

| 태그 | 값 | 왜 |
|---|---|---|
| `T_secondary_supply_C` (양쪽 CDU 전부) | 27.0 ℃ | 2차측이 고정 경계조건이다(절대 규칙 7 · `meta.fixed_axes`) |
| 고정 CDU `load_percent`·`load_kW`·`hx_duty_kW` | 100% · 640 kW · 640 kW | 「부하 고정 CDU」가 이 축의 정의다 |
| single 케이스의 `secondary_share_Lps` | 15.5 L/s | 단일 CDU 는 배분이 없다 — `HEAT_EXCHANGER` 고정값 |
| `solver.hydraulic_ier` · `plant_ier` | 1 | 136 케이스 전부 수렴 |
| `solver.thermal_converged`·`outer_converged`·`all_converged` | true | 〃 |
| `resistance_rack_index` · `resistance_cdu_index` | 0 · 0 | 저항은 언제나 랙0 · CDU0 에만 걸린다 |
| `leak_model` | `K_approx` | 절대 규칙 8 |
| `fixed_cdu_load_percent` (dual 케이스) | 100.0 | 〃 |

**태그는 아니지만 화면이 알아야 할 중복 둘**

- `hx_duty_kW` 는 **전 케이스에서 `load_kW` 와 같다**(최대 차이 1.4e-13 kW).
  정상상태라 duty = 부하이며, **저항을 바꿔도 전혀 움직이지 않는다.**
  독립된 정보를 담은 태그가 아니다
- `rack_flows_Lps[1..7]` · `rack_outlet_C[1..7]` 는 **서로 완전히 같다**
  (전 케이스 · 유일한 예외가 `dual/L45/R20` 의 2.22e-16 = 1 ULP 이다).
  랙 8개 중 실제로 구분되는 계열은 **랙0 과 나머지 둘뿐**이고, 랙0 과 나머지가
  갈리는 것은 **저항이 걸린 102 케이스에서만**이다

#### ⑹ 이 판이 확인한 위생

- **NaN · Inf 0건**
- **부호 방향 위반 0건** — 136 케이스 204 개 CDU 전부에서
  `T_return > T_supply > T_secondary_supply` 다
- **`hx_duty_kW` = `load_kW`** 잔차 최대 1.4e-13 kW
- **dual 케이스의 2차측 몫 합 = 31.0 L/s** 정확히 (잔차 <1e-9, 68 케이스 전부)
- **단위가 태그맵과 어긋난 자리 없음**
- **`all_converged` = true** 136 케이스 전부

#### 이 판이 하지 않은 것

- **계산하지 않았다** — 모델을 한 번도 돌리지 않았다. JSON 의 값을 읽었을 뿐이다
- `src/cdu_simul/` · `assumptions.py` · 데이터셋 CSV · `demo/*` · 정본 문서 4종
  **하나도 건드리지 않았다**
- **새 가정치 0개** — 5장·5-1 에 아무것도 추가하지 않았다
- **게이트를 만들지 않았고 6장 기준을 재판정하지 않았다**
- **화면(HTML)을 만들지 않았다** — 다음 판이다
- 발견을 **고치지 않았다** — 미해결 **#42** 로 올리기만 했다
  (`collaboration.md` ⑤)

#### 검사기

코드를 안 건드렸으므로 그대로 통과했다 — pytest **1,207건** ·
`ruff check .` All checks passed · `mypy src tests` 29 files, no issues.

#### 다음 판이 알아야 할 것

- 그릴 수 있는 것은 **⑴ 의 열 태그와 solver 플래그 넷**뿐이다. 그 밖은 없다
- **랙 계열은 실질 2개다**(랙0 · 나머지 7). 8개를 다 그려도 7개가 겹쳐 보인다
- **`hx_duty_kW` 는 `load_kW` 의 복사본**이다 — 둘 다 그릴 이유가 없다
- **연동 신호(고정 CDU)는 0.0198 K · 0.21%** 다. 부하 CDU 와 축을 공유하면
  안 보인다
- **`T_supply_C` 를 저항 신호로 읽지 않는다** — 미해결 #42
- `meta.fixed_axes` 의 `pump_head_rated_mAq`·`branch_dp_rated_mAq`·
  `valve_dp_rated_mAq` 는 **입력 표기이지 스트림 태그가 아니다** — 그리지 않는다

### 세션 7.0-D · 2026-09-01 · 시연 화면 (정적 PFD 한 장 · demo/pfd.html)

**게이트 없음 · feasibility 판정 없음 · 새 가정치 0개 · 모델 코드 무수정 ·
데이터셋 무수정 · JSON 무수정 · 정본 문서 4종 무수정.**
새 파일은 `demo/pfd.html` **하나**뿐이다 — CSS·JS 인라인 · 외부 CDN·폰트·
라이브러리 0 · 새 파이썬 모듈 0 · 새 의존성 0.

**화면은 계산하지 않는다.** JSON 값을 표시만 하며, 코드 안의 산술은 허용된 둘뿐이다
— ⑴ `toFixed`(표시 자릿수 반올림) ⑵ Δ 배지의 뺄셈(같은 파일 안 두 케이스 값의 차).
물성·수력·열 계산이 화면에 없다.

#### 여는 방법 (C8)

```bash
# 저장소 루트에서
.venv/Scripts/python.exe -m http.server 8765
# 브라우저에서
http://127.0.0.1:8765/demo/pfd.html
```

**`file://` 로 직접 열면 안 된다** — 브라우저가 `fetch("demo_steady.json")` 를
CORS 로 막아 화면이 비고 「demo_steady.json 을 읽지 못했다」만 뜬다.
`http.server` 는 표준 라이브러리라 새 의존성이 아니다.

#### 화면 구성

| 무엇 | 내용 |
|---|---|
| 케이스 선택 | `single`/`dual` 토글 · 부하 20~100% 5% 간격 17단 슬라이더 · 저항 0/5/20/50% 4단 버튼 |
| 없는 조합 | 「해당 케이스 없음 — 보간하지 않는다」를 적고 전 태그를 `—` 로 비운다 |
| 장치 | 펌프 · 열교환기 · 랙 8개 · CDU 점선 묶음 |
| 스트림 | 1차측 공급 · 1차측 환수 · 랙 분기 8 · 랙 출구 8 · 2차측 공급(초록) |
| dual | CDU 를 둘 그리고 **2차측 공급 헤더를 공유**로 그린다. 둘째는 「CDU B · 고정 CDU · 부하 100% 고정」 |
| **2차측 환수** | **그리지 않는다** (절대 규칙 7 · 모델에 없는 양) |
| solver | 한 귀퉁이에 `all_converged` 하나만 |

#### 태그 · 자릿수

`demo/tag-map.md` 「1. 채운 태그」로 닫았다 — **거기 없는 값은 화면에 하나도
올리지 않았다.** 압력·양정·ΔP 계열 없음(`meta.fixed_axes` 의 세 값도 싣지 않았다) ·
`heat_capacity_ratio`·`Cr` 어디에도 없음(#41).

자릿수는 태그별 고정이며 케이스가 바뀌어도 바뀌지 않는다 — 온도 3자리 ·
유량 4자리 · duty 1자리 · 부하 정수.

#### 깜빡임

`setText` 가 **표시 문자열을 비교해 다를 때만** 한 번(0.9 초) 색으로 깜빡인다.
반올림 후 같으면 깜빡이지 않는다 — 이것이 규칙이다.
깜빡임 옆에 「케이스 전환 — 시간 경과가 아님」을 상시 적었다(#40).

#### Δ 배지

`dual` 일 때 **고정 CDU 의** `T_return_C`·`T_supply_C`·`secondary_share_Lps`
셋에만 붙는다. 기준은 **같은 `cdu_config`·같은 부하의 저항 0 케이스**이고,
부호 포함 유효숫자 2자리로 적으며 옆에 「같은 부하 · 저항 0 대비」를 쓴다.
기준 케이스 자신(R0)에서는 띄우지 않는다. **부하 CDU 에는 붙이지 않는다.**

#### 상시 표기 (스크롤로 사라지지 않는 `sticky` 머리말)

⑴ 가정값 기반 — 실측 아님 · ⑵ 케이스 전환은 시간 경과가 아니다(#40) ·
⑶ `resistance_increase` 는 실제 질량손실 사고의 **대용**이며 신호 방향이 같지
않다(종료 판정 5-b: X) · ⑷ 양정·분기ΔP·밸브ΔP 는 5장 범위의 한쪽 끝으로 고정된
한 조합이며 화면에 싣지 않는다(숫자 없음) · ⑸ `T_supply_C` 의 저항 응답은
비단조이고 중간부하에서 부호가 뒤집힌다 — |변화| ≤ 0.009 K · #42.

**⑶ 의 문안은 「누출」이라는 낱말을 쓰지 않고 적었다** — 지시가 준 예시 문장에는
그 낱말이 들어 있었으나 같은 항목이 「화면 어디에도 쓰지 않는다」고 정했으므로,
뜻을 유지한 채 **「실제 질량손실 사고의 대용」**으로 바꿔 적었다. 화면 전체에
그 낱말이 0회다.

#### C9 대조 — 전부 일치

브라우저 확장이 이 PC 에서 연결되지 않아 화면을 띄워 눈으로 보지 못했다.
대신 **`demo/pfd.html` 의 스크립트를 그대로 최소 DOM 껍데기 위에서 실행**해
(Node · 일회성 · 저장소에 커밋하지 않음) 표시 문자열을 JSON 원본값과 대조했다.
페이지의 조회·분기·서식 코드가 실제로 도는 대조다.

| 케이스 | 대조한 태그 | 결과 |
|---|---|---|
| `single/L45/R0` | 2차측 공급온도 + CDU A 의 열 태그 7 + 랙 16 = **24** | 전부 일치 |
| `single/L45/R50` | 〃 **24** | 전부 일치 |
| `dual/L70/R50` | 2차측 + CDU A 23 + CDU B 23 = **47** | 전부 일치 |

**불일치 0건** — 자릿수 반올림 외의 차이가 없다. 함께 확인한 것:
`viewBox` 와 `g-B` 표시가 single/dual 에 맞게 바뀐다 · `all_converged: true` ·
없는 조합(`single/L22/R0`)에서 「해당 케이스 없음」이 뜨고 전 태그가 `—` 가 된다 ·
`dual/L70/R50` 의 배지가 `Δ −0.013` · `Δ −0.013` · `Δ +0.019`(원본
−1.2645e-2 K · −1.2687e-2 K · +1.9057e-2 L/s)로 맞다 · R0 에서는 배지가 비어 있다 ·
부하 CDU 에는 배지가 없다.

#### 이 판이 하지 않은 것

- 새 파이썬 모듈·새 의존성 0 · 외부 CDN·폰트·라이브러리 0
- `src/cdu_simul/` · `assumptions.py` · 데이터셋 CSV · `demo/*.json` ·
  정본 문서 4종 **무수정**(**재업로드 필요 없음**)
- 새 가정치 0개 — 5장·5-1 에 아무것도 추가하지 않았다
- 게이트를 만들지 않았고 6장 기준을 재판정하지 않았다
- 미해결을 **하나도 닫지 않았다.** 발견 하나를 **#43** 으로 올리기만 했다

#### 검사기

파이썬을 안 건드렸으므로 그대로 통과했다 — pytest **1,207건** ·
`ruff check .` All checks passed · `mypy src tests` 29 files, no issues.
(`demo/pfd.html` 은 파이썬 도구의 대상이 아니다.)

#### 다음 판이 알아야 할 것

- 화면은 **정상상태 136 케이스 전용**이다. 전이(시간축)를 붙이려면 #40 이 먼저다
- 랙 8개 중 실제로 갈리는 것은 랙0 과 나머지뿐이다(세션 7.0-C) — 화면에서도
  랙1~7 은 같은 숫자로 겹쳐 보인다. 정상이다
- 고정 CDU 의 `total_flow_Lps` 는 4자리에서 **가끔 깜빡인다** — #43

### 세션 7.2 · 2026-09-01 · 랙별 등가길이 배분 · 시연 JSON 에 질량유량

**게이트 없음 · feasibility 판정 없음 · 6장 기준을 재판정하지 않았다.**
**새 숫자 0개** — 5장 등가길이 20~30 m 범위 안에서 닫혔고 범위 밖으로 나가지
않았다. 화면(`demo/pfd.html`)·52열본 데이터셋·5-1 「계통 보유수량 M」 항목·
`assumptions.py` 의 기존 값을 하나도 건드리지 않았다.

#### ⑴ 5-1 에 먼저 적었다 (코드보다 먼저 · 절대 규칙 1)

정본 `AI서버_CDU_디지털트윈_프로젝트정리.md` 5-1 표에 새 행
**「랙별 등가길이 배분」**을 추가했다(커밋 `a8a749c` — 코드 커밋보다 앞선다).

| 무엇 | 내용 |
|---|---|
| 확정값 | 5장 등가길이 20~30 m 를 **랙 8개의 공간 분포**로 읽는다. 가장 가까운 랙 20 m · 가장 먼 랙 30 m · 사이 6랙 균등(간격 10/7 ≈ 1.4286 m). K 는 이 등가길이에 비례하고 **8랙 평균(25 m) 기준으로 스케일**한다 → 랙별 K 배수 0.800~1.200 |
| 근거 | 범위 안에서 닫히므로 **새 숫자가 0**. 헤더로부터의 거리가 랙마다 다르다는 물리를 반영한다. **범위의 뜻을 「불확실성」에서 「공간 분포」로 바꾸는 시나리오 정의 변경이며 사람이 확정했다(2026-09-01)** |
| 한계 | 같은 범위를 5-1 「계통 보유수량 M」이 방침 B(양 끝)로 쓰고 있다. **이 판은 M 을 건드리지 않았다** — 정상상태 해가 M 에 불변이기 때문이다(세션 5.7). 두 읽기를 맞추는 것은 전이 시간축을 다시 세울 때(#40) |
| 확정 시점 | 세션 7.2 |

**정본을 고쳤다 — 프로젝트 지식 재업로드가 필요하다**(대상:
`AI서버_CDU_디지털트윈_프로젝트정리.md` 하나).

#### ⑵ 모델 — 새 메커니즘을 만들지 않았다

랙별 K 진입점 `HydraulicCase.branch_K_multipliers` 는 **세션 3-A 가 이미
만들어 둔 것**이고 `apply_leak_to_rack` 도 이미 그 위에 곱하는 구조였다.
그래서 `hydraulics.py` 를 **한 줄도 고치지 않았다** — 바뀐 것은
`assumptions.py` 에 5-1 값을 전사한 두 함수뿐이다.

- `PIPING.rack_equivalent_lengths_m()` → 20 · 21.4286 · 22.8571 · 24.2857 ·
  25.7143 · 27.1429 · 28.5714 · 30 m
- `PIPING.rack_branch_K_multipliers()` → 0.800000 · 0.857143 · 0.914286 ·
  0.971429 · 1.028571 · 1.085714 · 1.142857 · 1.200000 (**평균 정확히 1.0**)

**기본값은 균등이다.** 이 배수는 명시적으로 켤 때만 들어간다
(`demo_steady.USE_RACK_LENGTH_DISTRIBUTION`). 밸브 Kv · 잔여저항 · 헤더 처리는
건드리지 않았다.

#### ⑶ 시연 JSON — 질량유량 추가

`total_flow_kgph` · `rack_flows_kgph[0..7]` · `secondary_share_kgph` 를 넣었다.
**기존 L/s 열은 그대로 두었다.**

ρ 는 5-1 「수력 계산의 물성 평가 온도」 규칙대로 **1차측 벌크평균온도**에서
CoolProp 으로 얻는다 — 새 상수를 만들지 않았다. 환산은
`kg/h = L/s × ρ[kg/m³] × 3.6` 이고 3.6 은 단위 정의값이다(절대 규칙 9).
`meta.mass_flow_density_rule` 에 이 규칙을 한 줄로 실었고, **거기에
`secondary_share_kgph` 도 같은 ρ 를 쓴다는 한계를 함께 적었다** — 2차측 유체는
같은 PG25 이나 온도가 27℃ 로 달라 그 스트림의 실제 밀도와는 다르다. 2차측 물성
평가 온도는 5-1 이 정한 바 없어 **새 규칙을 만들지 않았다**.

`meta` 에 `rack_equivalent_length_m` · `rack_branch_K_multipliers` ·
`rack_length_note` 도 실어 어떤 배분으로 낸 데이터인지 파일 안에서 닫히게 했다.

#### ⑷ 재생성

`demo_version` 을 **`demo-7.0` → `demo-7.2`** 로 올렸다. 축은 그대로
2 × 17 × 4 = **136 케이스** · **549 KB** · **solver 미수렴 0 건** ·
태그 빈 칸 0 · **소요 5.4 초**(세션 7.0 의 12.8 초는 같은 축을 균등으로 돌린
것이며, 이 차이는 PC 상태 차이지 모델 변화가 아니다).

#### ⑸ 차등이 얼마나 나오는가 — 이 판의 핵심

`single`/R0(저항 증가 없음). 랙0(가장 가까움) → 랙7(가장 멂) 순서다.

| 부하 | 랙별 유량 [kg/h] (랙0→랙7) | 최대−최소 |
|---|---|---|
| 20% | 7367.1 · 7277.3 · 7190.6 · 7107.0 · 7026.2 · 6948.2 · 6872.7 · 6799.5 | **567.58 kg/h · 7.70%** |
| 60% | 7359.2 · 7269.5 · 7182.9 · 7099.4 · 7018.7 · 6940.7 · 6865.3 · 6792.2 | **566.97 kg/h · 7.70%** |
| 100% | 7350.9 · 7261.3 · 7174.8 · 7091.4 · 7010.8 · 6932.9 · 6857.5 · 6784.6 | **566.33 kg/h · 7.70%** |

| 부하 | 랙별 출구온도 [℃] (랙0→랙7) | 최대−최소 |
|---|---|---|
| 20% | 30.015 · 30.040 · 30.064 · 30.088 · 30.111 · 30.135 · 30.158 · 30.181 | **0.1655 K** |
| 60% | 36.038 · 36.112 · 36.184 · 36.256 · 36.327 · 36.397 · 36.466 · 36.534 | **0.4959 K** |
| 100% | 42.058 · 42.180 · 42.300 · 42.419 · 42.537 · 42.654 · 42.769 · 42.883 | **0.8254 K** |

**소수 첫째 자리 kg/h 표시에서 충분히 보인다** — 랙 간 차이가 567 kg/h 이고
이웃 랙끼리도 73~90 kg/h 라 표시 분해능(0.1 kg/h)의 700배 이상이다.

유량 차등이 K 비(1.5배)보다 훨씬 작은 것(7.70%)은 **분기 K 가 랙 경로 저항의
일부일 뿐이기 때문**이다 — 밸브 ΔP 와 계통 잔여저항이 함께 걸린다(잔여저항이
총양정의 60~83% · 미해결 #24). 부하가 바뀌어도 7.70% 가 유지되는 것은 저항비가
부하에 거의 무관하기 때문이다.

#### ⑹ 기존 결과 — 안 움직였다

- **랙별 배분을 끈 상태**(모델 기본값)에서 `python -m cdu_simul.plant` ·
  `-m cdu_simul.model` · `-m cdu_simul.hydraulics` 세 표가 이 판 이전과
  **바이트 단위로 동일**하다. 세션 5 대표 표(같은 고정축 조합
  H22.4/dPb2/dPv3)가 그대로다
- **pytest 1,207건 전부 통과** · `ruff check .` · `mypy src tests` 통과

#### ⑺ 소요시간

**5.4 초** (136 케이스 · solver 미수렴 0).

#### 이 판이 하지 않은 것

- `demo/pfd.html` **무수정** — 화면은 이 판에서 고치지 않았다
- 52열본 데이터셋 **무수정** · 5-1 「계통 보유수량 M」 항목 **무수정** ·
  `assumptions.py` 의 **기존 값 무수정** · `hydraulics.py` **무수정**
- 새 의존성 0 · 새 숫자 0 · 게이트 0
- 미해결을 **하나도 닫지 않았다.** 발견 둘을 **#44 · #45** 로 올리기만 했다

#### 다음 판이 알아야 할 것

- **화면이 아직 kg/h 를 모른다.** `demo/pfd.html` 은 L/s 만 그린다 —
  질량유량을 붙이려면 `demo/tag-map.md` 에 태그를 먼저 추가해야 한다
- **랙0 과 랙7 이 R50 에서 완전히 겹친다**(#45). 화면에서 「저항 걸린 랙을
  찾아보라」로 쓰면 안 된다
- 5-1 근거란의 「계통 전체 저항이 보존된다」는 **평균 K 기준**이며 총유량은
  +0.023% 이동한다(#44)

### 세션 7.2-B · 2026-09-01 · 저항 주입 랙을 랙7 로 · 5-1 두 행 정정 (#44·#45 닫음)

**게이트 없다.** 6장 기준을 이 판으로 재판정하지 않았다. **새 숫자 0개** —
주입 랙 번호는 랙 개수에서 유도한 **시나리오 정의**이지 물리 가정이 아니다.

#### 한 일

- **C1 · 5-1 「누출 주입 지점」 정정**(정본 수정 · 사람 지시) — 배분을 켠 경우
  주입 랙을 **랙7(가장 먼 랙)** 으로. 기존 근거(「어느 랙인지 결과에 영향하지
  않는다」)는 배분을 켜면 성립하지 않는다. 겹침 수치(유량 차 −9.1e-13 kg/h ·
  출구온도 차 0.000 K)와 **한계**(랙 간 비교로 저항 걸린 랙을 찾는 판독은
  배분을 켠 경우 일반적으로 성립하지 않는다 · 같은 랙의 기준 대비 신호는
  유효하다)를 함께 적었다. **52열본은 균등 경로라 영향받지 않는다**도 명기
- **C2 · 5-1 「랙별 등가길이 배분」 근거란 정정**(#44) — 「계통 전체 저항이
  보존된다」 → 「**평균 K 가 보존된다**」. 병렬 합성이 K 에 비선형(Q ∝ 1/√K)이라
  총유량이 15.505582 → 15.509148 L/s(**+0.023%**) 이동한다를 함께 적었다
- **C3 · 시연 JSON 생성 코드** — `demo_steady.RESISTANCE_RACK_INDEX =
  SCENARIO.racks_per_cdu - 1` 을 두고 `leak_case(...)` 두 곳과
  `resistance_rack_index` 태그에 넘겼다. `DEMO_VERSION` → **`demo-7.2-B`**.
  축(2 × 17 × 4 = 136)은 그대로. `assumptions.LEAK.injection_rack_index` 는
  **안 고쳤다** — 52열본(균등 경로)이 그것을 쓰고 랙 대칭이 성립하기 때문이다
- **C5 · `demo/tag-map.md`** 「1. 채운 태그」에 질량유량 3행 추가
  (`rack_flows_kgph[0..7]` · `total_flow_kgph` · `secondary_share_kgph`, 전부
  구분 ⑵). ρ 규칙(1차측 벌크평균온도 · 2차측도 같은 ρ 를 쓴다는 한계)을 함께
  적었다. **부피유량 태그는 지우지 않았다**

#### 확인한 것 (C4 · #45 닫힘)

`demo-7.2-B` · `single/L100/R50` 8랙:

| 랙 | 유량 [m³/h] | 출구온도 [℃] |
|---|---|---|
| 0 | 7.334043 | 41.959345 |
| 1 | 7.244600 | 42.080337 |
| 2 | 7.158352 | 42.199872 |
| 3 | 7.075112 | 42.318000 |
| 4 | 6.994711 | 42.434770 |
| 5 | 6.916989 | 42.550228 |
| 6 | 6.841802 | 42.664417 |
| **7 (주입)** | **6.122805** | **43.898024** |

주입 랙(7)과 **가장 가까운 정상 랙은 랙6** — 유량 차 **−0.718998 m³/h** ·
출구온도 차 **+1.233607 K**. 표시 자릿수(유량 소수 2자리 · 온도 소수 3자리)에서
**6.12 vs 6.84** · **43.898 vs 42.664** 로 확실히 구분된다 → **#45 닫힘.**
겹침은 랙0 에 걸었을 때만 생기던 것이고, 랙7 은 배수 1.2 × 1.5 = 1.8 이라
어느 랙과도 겹치지 않는다.

#### 다음 판(화면)의 축척 재료 (C6 · 전 케이스 136 · m³/h·℃)

| 양 | 최소 | 최대 | 표본 수 |
|---|---|---|---|
| 랙별 유량 [m³/h] (전 랙) | 6.1119 | 7.3341 | 1,632 |
| 랙별 출구온도 [℃] (전 랙) | 29.9974 | 43.9081 | 1,632 |
| CDU A 총유량 [m³/h] | 55.5893 | 55.8329 | 136 |
| 2차측 배분 — CDU A [m³/h] | 55.6781 | 55.8000 | 136 |
| 2차측 배분 — CDU B [m³/h] | 55.8000 | 55.9219 | 68 |

(단일 CDU 케이스의 2차측 배분은 정격 고정값 55.8000 m³/h 이므로 CDU A 범위의
한쪽 끝이 거기에 닿는다. CDU B 는 2대 케이스에만 있다.)

#### 확인한 것 (C7 · 기존 결과 무이동)

랙별 배분을 **끈 상태**(모델 기본값)에서:

- `python -m cdu_simul.plant` · `model` · `hydraulics` 세 표가 이 판 **이전과
  MD5 동일** — `8d34d94abff420854eae9257a5a069fe` ·
  `a84b4b61bf1b60925b1069690ca44ef9` · `0be5952b2832c2332ced89e90bb79def`
- pytest **1,207건 전부 통과**(585.94 s) · ruff 통과 · mypy 29파일 통과
- 시연 재생성 solver 미수렴 **0 건** · 태그 빈 칸 0 · 검증 통과

#### 안 한 것 (범위 밖)

- `demo/pfd.html` **무수정** — 화면은 다음 판에서 고친다
- 52열본 데이터셋 · `hydraulics.py` · `assumptions.py` · `CLAUDE.md` ·
  5-1 「계통 보유수량 M」 · 등가길이 배분값(20~30 m) **전부 무수정**
- 새 숫자 0 · 새 의존성 0 · 게이트 0

#### 다음 판이 알아야 할 것

- **화면(`demo/pfd.html`)이 아직 `demo-7.2` 를 전제한다** — 저항 걸린 랙이
  랙0 이 아니라 **랙7** 이고 `demo_version` 이 `demo-7.2-B` 다
- 화면에 질량유량을 붙일 재료는 `demo/tag-map.md` 에 올라가 있다.
  **부피유량 태그를 대체하는 것이 아니라 얹는 것**이다
- 축척은 위 C6 표를 쓴다. **랙 간 비교로 「저항 걸린 랙을 찾아보라」식 시연은
  여전히 하지 않는다** — 랙7 이 구분되는 것은 이 배수 조합에서 그런 것이다

### 세션 7.3 · 2026-09-01 · 시연 화면을 랙 중심으로 (`demo/pfd.html`)

**게이트 없다.** 6장 기준을 이 판으로 재판정하지 않았다. **새 숫자 0개** —
화면이 쓴 산술은 셋뿐이다: ⑴ `*_Lps × 3.6` ⑵ 표시 자릿수 반올림 ⑶ 같은 파일 안
두 케이스 값의 차(Δ). 축척의 상·하한(30.0~43.9℃ · 6.11~7.33 m³/h · 편차 막대
77 px/(m³/h))은 **세션 7.2-B 가 이미 낸 전 케이스 범위**를 화소로 옮긴 것이지
새 가정치가 아니다.

#### 한 일

바꾼 파일은 `demo/pfd.html` **한 장**이다. 읽은 파일은 `demo/demo_steady.json`
(`demo-7.2-B`) 과 `demo/tag-map.md` 둘.

- **⑴ 시나리오 띠 · ⑵ 랙 8개 · ⑶ CDU 계통 띠 · ⑷ 2차측 띠** 를 위에서 아래로
  넷. 1600×900 에서 세로 스크롤이 없도록 `flex` 로 짰고 **랙 행이 남는 높이를
  전부 먹는다**(단일 CDU 기준 화면 높이의 약 2/3). `dual_asymmetric` 이면 랙 행이
  두 줄이 되고 ⑷ 를 공유한다
- **랙은 세로 슬래브 하나가 값 셋을 진다** — 채움 높이 = 부하(20~100%) · 채움 색 =
  출구온도(**30.0~43.9℃ 고정 계조**) · 바닥 리본 두께 = 유량(**6.11~7.33 m³/h
  고정**, 4~20 px). 슬래브 아래는 숫자 두 줄(유량 m³/h 2자리 · 출구온도 ℃ 3자리),
  랙 번호는 **1~8**(JSON 인덱스 0~7), 저항이 걸린 랙은 `#E8A33D` 테두리
- **색은 여섯만** 썼다(지면·장비면·구조선·냉측·온측·이상). 계조는 냉측↔온측
  보간이고 다른 색을 만들지 않았다. 외부 폰트를 받지 않고 시스템 폰트를 쓰며 숫자에
  `font-variant-numeric: tabular-nums` 를 걸었다
- **총유량은 일부러 무력하게** 냈다 — 랙 편차 막대와 **같은 축척**(77 px/(m³/h))의
  편차 막대를 총유량 옆에 둔다. `single/L100/R50` 에서 랙8 은 **45.1 px**(Δ −0.5859
  m³/h) 인데 CDU 총유량은 **11.1 px**(Δ −0.1445 m³/h) 다
- **CDU B** 는 총유량·랙별 유량을 크게 띄우지 않는다. 실제로 움직이는 둘(2차측
  배분 m³/h 2자리 · 환수온도 ℃ 3자리)만 내고 그 둘에 **Δ 배지**(같은 부하 · 저항 0
  대비 · 부호 포함 유효숫자 2자리)를 붙였다. R0 에서는 배지를 띄우지 않는다
- **시나리오 넷** — 「부하 20 → 100%」 · 「랙 배관 저항 증가 0 → 50%」 ·
  「부분부하 40% · 랙 배관 저항 증가 0 → 50%」 · 「CDU 2대 · 부하 20 → 100%」.
  **컨트롤 이름이 축 이름 그대로**이고 「재생」이 없다. 진행 표시는 시간 막대가
  아니라 **축의 눈금**(0 · 5 · 20 · 50 / 20~100)이다. 시나리오 밖에서 축 셋을
  손으로 고르는 컨트롤을 남겼다
- **움직임은 한 군데** — 슬래브 높이·리본 두께·색만 200 ms CSS transition 으로
  이동하고, 숫자는 즉시 바뀌며 한 번 깜빡인다(투명도 420 ms). **표시 문자열이 같으면
  깜빡이지 않는다**
- **표기는 화면 맨 아래 한 줄**: 「가정값 기반 — 실측 아님 · 프로젝트정리 5장」.
  앞 판 머리말의 나머지 넷(#40 · 5-b · ΔP 고정축 · #42)은 **사람 지시로 지웠다**
  (2026-09-01). 저항 축의 이름은 **「랙 배관 저항 증가」**이고 화면 어디에도
  「누출」이 없다
- 태그는 `demo/tag-map.md` 「1. 채운 태그」로 닫았다. **압력·양정·ΔP·2차측
  환수온도·`heat_capacity_ratio`/`Cr` 을 하나도 싣지 않았다**

#### C10 대조 검증 — 불일치 0

| 케이스 | 대조 항목 | 불일치 | 주입 랙 표시 |
|---|---|---|---|
| `single/L100/R0` | 42 | **0** | 없음 (저항 0) |
| `single/L100/R50` | 42 | **0** | **랙 8** (JSON `resistance_rack_index=7`) |
| `dual_asymmetric/L70/R50` | 50 (CDU 2대) | **0** | **랙 8** · `resistance_cdu_index=0` |
| **합계** | **134** | **0** | |

방법은 세션 7.0-D 와 같다 — 페이지의 순수 표시 함수(`view`)를 HTML 에서 그대로
꺼내 node 에서 돌리고, **표시 문자열을 다시 수로 되돌려** JSON 원본과 비교했다.
허용 오차는 「표시 자릿수의 반」뿐이고(유량·배분 ±0.005 · 온도 ±0.0005) 자릿수
자체도 함께 검사했다. 편차 막대가 쓰는 Δ 는 두 케이스 값의 차와 **부동소수점까지
동일**함을 확인했다. 일회성 스크립트라 커밋하지 않았다.

#### 확인한 것

- 최소 DOM 스텁 위에서 페이지 스크립트를 그대로 부팅 — **paint 가 만지는 id 69개가
  전부 골격에 있다**(없는 id 0) · 시나리오 4종 진입 예외 0 · **전 136 케이스
  `view()` 통과**(실패 0)
- 기하: `single/L100/R50` 에서 랙8 리본 **4.2 px** · 랙7 리본 **13.6 px**,
  랙8 색 `rgb(194,69,45)` · 랙7 색 `rgb(181,74,56)`
- pytest 1,207건 · ruff · mypy 그대로 통과 (모델 코드를 건드리지 않았다)

#### 안 한 것 (범위 밖)

- `src/cdu_simul/` 전부 · `demo/*.json` · `demo/tag-map.md` · 정본 문서 4종 ·
  52열본 데이터셋 **무수정**
- 새 가정치 0 · 새 의존성 0 · 게이트 0 · 미해결 닫음 0
- 브라우저 확장이 이 PC 에 붙지 않아 **실제 렌더 화면은 보지 못했다** — 검증은
  전부 node 위에서 했다. 레이아웃(1600×900 세로 스크롤 없음)은 CSS 로 짠 것이고
  **사람이 직접 열어 확인해야 한다**

#### 다음 판이 알아야 할 것

- 여는 방법은 세션 7.0-D 와 같다. `file://` 은 `fetch` 가 막히므로 저장소 루트에서
  `python -m http.server` 로 열고 `/demo/pfd.html` 를 본다
- 화면은 `demo_version` 을 검사하지 않는다 — 바닥 한 줄의 **툴팁**에만 실어 두었다
  (표기 줄을 한 줄로 묶으라는 C8 때문이다). JSON 을 갈아끼우면 화면이 조용히 따라간다
- 새 미해결 **#46** 을 열었다 — 고부하에서 온도 계조가 랙을 거의 못 가른다

### 세션 7.4 · 2026-09-01 · 시연 화면에 「서버실」 탭 (`demo/pfd.html`)

**게이트 없다.** 6장 기준을 이 판으로 재판정하지 않았다. **새 숫자 0개** —
화면이 쓴 산술은 넷뿐이다: ⑴ `*_Lps × 3.6` ⑵ 표시 자릿수 반올림 ⑶ **같은 케이스
안 값들의 차·평균**(8랙 평균 대비 편차 · Δ 배지) ⑷ 등축 좌표 변환. 축척의 상·하한
(30.0~43.9℃ · 6.11~7.33 m³/h · 편차 막대 77 px/(m³/h))은 세션 7.2-B·7.3 이 이미
낸 값 그대로이고, 새로 고정한 것은 **평균 대비 계조 −1.5~+1.5 K** 하나인데 이는
가정치가 아니라 화소 축척이다.

#### 한 일

바꾼 파일은 `demo/pfd.html` **한 장**이다. 읽은 파일은 `demo/demo_steady.json`
(demo-7.2-B)과 `demo/tag-map.md`.

- **탭 둘.** 「서버실」이 **기본 탭**이고 「계통도」는 세션 7.3 화면 그대로다.
  시나리오·구성·부하·저항 컨트롤은 **탭 위에 공유**로 두어 탭을 바꿔도 같은
  케이스를 본다(범례만 탭에 따라 바뀐다)
- **등축 뷰.** `sx = (x−y)·cos30 · sy = (x+y)·sin30 − z` 를 상수로 박고 바닥
  타일 그리드 위에 **랙 8개를 한 열로**(앞 = 랙 01 · 뒤 = 랙 08 = 주입 랙) 세웠다.
  CDU 는 열 앞 끝의 설비 박스이고, 공급 헤더(랙 뒤)·환수 헤더(랙 앞) 두 줄이
  바닥을 따라 랙으로 갈라진다. `dual` 이면 열이 둘이고 2차측을 공유한다
- **랙이 지는 값 셋** — 앞면 채움 높이 = 부하 · 앞면 색 = 출구온도의 8랙 평균 대비
  편차 · 환수 갈래 배관의 굵기 = 유량(색도 같은 편차 계조). 랙마다 `RACK 01`~`08`
  라벨과 숫자 두 줄(유량 m³/h 2자리 · 출구온도 ℃ 3자리). 저항 걸린 랙은 `#E8A33D`
  테두리
- **색 계조를 평균 대비로 바꿨다(#46).** 계조 범위는 **−1.5~+1.5 K 로 고정**하고
  케이스마다 재정규화하지 않는다(재정규화하면 케이스 간 색 비교가 깨진다).
  범례에 **「출구온도 — 8랙 평균 대비」**를 적었다. **「계통도」 탭의 색은 손대지
  않았다** — 절대온도 계조(30.0~43.9℃) 그대로다
- 색은 여섯만 썼다. 등축 옆면의 명암은 흰색의 밝기 88%(`--faced`)로만 냈다 —
  새 색이 아니다. 외부 폰트 0 · 외부 CDN 0 · 라이브러리 0
- 태그는 `demo/tag-map.md` 「1. 채운 태그」로 닫았다. **압력·양정·ΔP·2차측
  환수온도·`heat_capacity_ratio`/`Cr` 을 하나도 싣지 않았다.** 화면 어디에도
  「누출」을 쓰지 않았고 저항 축 이름은 「랙 배관 저항 증가」다

#### 그리지 않은 것 — 중요하다

**공기 열기둥(플룸)을 그리지 않았다.** 모델에 공기가 없다 — D2C 는 냉각수가 칩에서
직접 열을 받으므로 공기 온도장을 계산한 적이 없고, 그리면 없는 계산을 그린 것이
된다(절대 규칙 11). **대신 냉각수 배관이 그 일을 한다** — 배관 색 = 환수온도 편차,
굵기 = 유량. 둘 다 실제로 계산한 값이다. 2차측도 벽 밖으로 나가는 배관 두 줄과
라벨만 그렸다 — **냉각탑·드라이쿨러를 그리지 않았다**(모델에 없다).

#### C11 대조 검증 — 불일치 0 (두 탭 모두)

| 케이스 | 대조 항목 | 불일치 |
|---|---|---|
| `single/L100/R0` | 70 | **0** |
| `single/L100/R50` | 70 | **0** |
| `dual_asymmetric/L70/R50`(JSON 키 `dual/L70/R50`) | 96 (CDU 2대) | **0** |
| **합계** | **236** | **0** |

방법은 세션 7.0-D·7.3 과 같다 — 페이지 스크립트를 최소 DOM 스텁 위에서 그대로
부팅하고, **실제 이벤트 경로**(구성·부하·저항 버튼)로 케이스를 몰아 표시된
문자열을 **다시 수로 되돌려** JSON 원본과 비교했다. 허용 오차는 「표시 자릿수의
반」뿐이다(유량·배분 ±0.005 · 온도 ±0.0005). 문자열 외에 **도형이 진 값**도 함께
봤다 — 랙별 배관 굵기와 앞면 색이 **같은 색인의 랙 값**에서 나왔는지, 이상 테두리가
`resistance_rack_index`/`resistance_cdu_index` 와 일치하는지, `viewBox` 가 구성에
맞게 바뀌는지. **paint 가 만지는 id 중 골격에 없는 것 0.** 탭 검사도 함께 했다
(기본 탭 = 서버실 · 범례 문구 전환 · **탭 전환이 케이스를 바꾸지 않음** ·
`single` 에서 둘째 열 숨김). 일회성 스크립트라 커밋하지 않았다.

#### C12 등축 기하 검사 — 겹침·이탈 0

렌더를 눈으로 볼 수 없으므로 좌표로 검사한다. 검사 대상은 랙 16개 + CDU 2개
= **도형 18개**와 글자상자 전부(한글 1em·그 밖 0.56em 로 폭을 잡는다).

| 검사 | 결과 |
|---|---|
| 도형끼리 바운딩 박스 겹침 | **0** |
| `viewBox` 이탈 (single·dual 각각 · 도형과 글자 전부) | **0** |
| 글자상자 ↔ 도형 | **0** |
| 글자상자 ↔ 글자상자 (추가로 본 것) | **0** |

`viewBox` 는 손으로 적지 않고 **그린 점을 전부 모아 그 bbox 로 낸다** — 잘림이
원리적으로 생기지 않는다(single `-1414.7 -240.0 1673.8 1084.8` · dual
`-1414.7 -240.0 1933.6 1328.0`). 랙 간격 140 을 랙 폭+깊이(84+50)보다 크게 잡은
것이 도형 겹침 0 의 근거다. 첫 판정에서 나온 **겹침 5건을 좌표로 고쳤다** —
CDU A 글자 묶음을 12 올리고, CDU B 글자 묶음은 상자 **아래**로 옮겼으며(위는
등축상 CDU A 쪽 랙 01 자리다), CDU B 의 2차측 라벨을 24 내렸다.

#### 확인한 것

- **#46 을 닫는 근거** — 8랙 평균 대비 편차의 전 136 케이스 실제 범위는
  **−0.5538 ~ +1.3849 K**(표본 1,632). 고정 계조 ±1.5 K 안에 전부 들어와
  **잘리는(clamp) 랙이 0 개**다. 절대온도 계조에서 주입 랙과 이웃 랙이 계조 폭의
  8.9% 밖에 안 벌어지던 것(#46)이 평균 대비로는 계조 폭의 상당 부분을 쓴다.
  절대온도는 랙마다 숫자로 그대로 나오므로 정보가 사라지지 않는다
- pytest 1,207건 · ruff · mypy 그대로 통과 (모델 코드를 건드리지 않았다)

#### 안 한 것 (범위 밖)

- `src/cdu_simul/` 전부 · `demo/*.json` · `demo/tag-map.md` · 정본 문서 4종 ·
  52열본 데이터셋 **무수정**
- 새 가정치 0 · 새 의존성 0 · 게이트 0
- 「계통도」 탭의 색·배치·숫자를 **하나도 고치지 않았다**
- 브라우저 확장이 이 PC 에 붙지 않아 **실제 렌더 화면은 보지 못했다** — 검증은
  전부 node 위에서 좌표·문자열로 했다. **사람이 직접 열어 확인해야 한다**

#### 다음 판이 알아야 할 것

- 여는 방법은 세션 7.0-D 와 같다. `file://` 은 `fetch` 가 막히므로 저장소 루트에서
  `python -m http.server` 로 열고 `/demo/pfd.html` 를 본다
- 서버실 탭은 **글자 크기를 viewBox 단위로 잡는다**(20~26). 1600×900 기준으로
  화면에는 13~17 px 로 나오지만, 창이 작아지면 그만큼 작아진다 — 글자가 안 읽히면
  줄일 곳은 랙 간격(`G.PITCH`)이 아니라 **랙 열 길이**다(등축에서 열 길이가 세로
  높이를 지배해 가로 여백이 남는 구조다)
- 새 미해결 **#47** 을 열었다 — 등축 뷰에 「공급 헤더」·「환수 헤더」 이름표를 놓을
  자리가 없다

### 세션 7.5 · 2026-09-01 · `CLAUDE.md` 에 컨텍스트 규칙 둘 (문서 판 · 코드 무수정)

**게이트 없다.** 6장 기준을 이 판으로 재판정하지 않았다. **새 숫자 0개.** 코드·
테스트·데이터셋·시연 화면(`demo/`)을 **하나도 열지 않았다** — 바꾼 파일은
`CLAUDE.md` 와 이 `PROCEED.md` 둘뿐이다.

#### 왜 이 판이 열렸나

세션 7.2-B·7.3·7.4 가 종료 보고에 남긴 `ctx` 관측 셋에서 **같은 것이 반복해서**
나왔다.

| 세션 | 가장 크게 먹은 것 | 줄일 수 있었던 것 |
|---|---|---|
| 7.2-B | `daily_brief` 출력 (열린 미해결 전문) | `git commit` here-string 으로 amend 왕복 |
| 7.3 | `PROCEED.md` 1~62행 (25.7 KB) | 마지막 행만 읽으면 됐다 |
| 7.4 | 셸 python 편집 후 파일 전문 두 번 반환 | 「현재 상태」 표 10행 덤프 |

반복은 둘이다. ⑴ **진행기록 문서** — 세 판 모두 1위이거나 「줄일 수 있었던 것」에
나왔다. 필요한 것은 늘 행 하나인데 표 전체를 읽는다. ⑵ **셸로 파일을 만들거나
고친다** — 두 판에 나왔고, **세션 7.4 프롬프트에는 이미 「편집 도구로 한다」가
적혀 있었는데도 그랬다.** 프롬프트로는 안 잡히므로 정본에 상주시킨다.

#### 한 일 — `CLAUDE.md` 세 자리

문안은 사람이 준 것을 **그대로** 넣었다(요약·개작 없음).

- 「판 시작·종료 정형 절차」 **시작** 3번을 「정본 문서 4종 중 **이 판에 걸리는
  절**과 `PROCEED.md` 미해결 목록을 읽고 시작한다」로 고치고, **4번**을 더했다 —
  「**`PROCEED.md` 「현재 상태」 표를 통째로 읽지 않는다.**」 (근거로 위 관측 셋을
  괄호에 적었다)
- 같은 절 **종료** 에 **6번**을 더했다 — 보고 마지막과 세션 로그에 `ctx:` 한 줄.
  수치는 **표시되는 값 그대로**, 나머지 둘은 짐작으로 채우지 않고 모르면 「모름」·
  없으면 「없음」
- 「하지 말 것」 에 한 줄 — 「**셸로 파일을 만들거나 고치기.** 편집 도구를 쓴다」
  (임시·일회성 스크립트 포함 · heredoc·here-string 금지 · 근거 괄호)

#### 하지 않은 일

- **「컨텍스트를 아낀다」 같은 새 절을 만들지 않았다**(C4). 관측 셋에서 확인된
  것만 기존 세 자리에 넣었다 — 긴 출력 리다이렉트·소스 부분 열기 같은 **관측에
  없는 절약 지침은 덧붙이지 않았다.** 근거가 없다. 몇 판 더 쌓이면 그때 정한다
- 다른 정본 3종·`src/cdu_simul/`·`demo/`·데이터셋을 **열지 않았다**
- pytest·ruff·mypy 를 **돌리지 않았다** — 코드가 한 줄도 바뀌지 않았고 게이트가
  없는 판이다(마지막 통과 상태는 세션 7.4 의 pytest 1,207건·ruff·mypy 그대로다)

#### 확인

`git diff --stat` 이 `CLAUDE.md` 한 파일 **21 삽입 · 1 삭제**만 냈다. 삭제 1행은
시작 3번을 두 줄로 늘려 쓴 것이고, 그 밖의 절은 **하나도 바뀌지 않았다.**

#### 다음 판이 알아야 할 것

- **이번 판부터 종료 6번(`ctx` 한 줄)이 적용된다.** 판마다 이 한 줄이 쌓이면
  다음 규칙의 근거가 된다 — 지금은 관측이 셋(7.2-B·7.3·7.4)이라 둘만 세웠다
- **정본을 고쳤다 — 프로젝트 지식 재업로드가 필요하다**(대상: `CLAUDE.md`)

ctx: 잔여 토큰 14,932,503 / 15,000,000 (표시된 것이 「잔여 토큰」이라 그 값을
적는다 — 사용률 표시는 이 판에 보이지 않았다) · 가장 크게 먹은 것: `PROCEED.md`
`tail -18` — 미해결 #45·#46·#47 세 항목의 긴 본문이 통째로 딸려왔다 ·
줄일 수 있었다고 보는 것: 그 `tail -18` 대신 **「현재 상태」 표 마지막 행 하나만**
뽑았으면 됐다(정작 필요한 것은 그 행의 서식과 삽입 위치뿐이었다)

### 세션 7.6 · 2026-09-01 · 오늘을 닫는 판 (기록 판 · 새 일 없음)

**게이트 없다.** 6장 기준을 재판정하지 않았다. **새 숫자 0개 · 새 일 0건.**
바꾼 파일은 `PROCEED.md` **한 장**이다 — 정본 문서 4종·`src/cdu_simul/`·
`demo/`·데이터셋을 **하나도 열지 않았다.** 코드가 안 바뀌었고 게이트가 없으므로
pytest·ruff·mypy 를 돌리지 않았다(마지막 통과 상태는 세션 7.4 의 pytest 1,207건·
ruff·mypy 그대로다).

#### 한 일

- **미해결 #48·#49 등재.** 오늘(7.0-C ~ 7.5) 관측에서 나왔는데 어디에도 안 적혀
  있던 둘이다. **둘 다 세션 7.5 자신이 만든 관측**이고, 둘 다 `CLAUDE.md` 를
  고쳐야 닫힌다 — 그러나 **이 판은 정본을 열지 않았다.** 다음에 정본을 고치는
  판에 얹는다
- **「현재 상태」 표 확인.** 오늘 일곱 판(7.0-C·7.0-D·7.2·7.2-B·7.3·7.4·7.5)이
  **전부 등재돼 있다 — 이상 없음.** 채워 넣은 행은 없다(이 판 자신의 7.6 행만
  더했다)
- **다음 판 첫 작업 기재** (아래)

#### 오늘의 두 관측이 같은 것을 말한다

#48 과 #49 는 따로 난 것 같지만 **한 가지**를 말한다. 세션 7.5 는 「하지 마라」를
둘 세웠는데, **그 판 자신이 둘 다 어겼다** — 규칙을 적어 넣은 화면에서 커밋
메시지를 heredoc 으로 실었고(#48), 「표를 통째로 읽지 않는다」를 세운 뒤 `tail -18`
로 파일 끝을 잘라 읽어 미해결 세 항목을 통째로 받았다(#49). **금지는 의도를
막지만 손이 뻗는 곳을 안 막는다.** 대안을 이름으로 줘야 잡힌다 —
`git commit -F <파일>`, 「필요한 절을 이름으로 찾아 연다」처럼. 그래서 둘은
**한 묶음으로 함께 정한다.**

#### 다음 판이 알아야 할 것

- **다음 세션 첫 작업: 사람이 `demo/pfd.html` 을 브라우저로 직접 열어 확인한다.**
  **렌더된 화면을 아직 아무도 눈으로 보지 못했다** — 세션 7.3·7.4 의 검증은 전부
  node 위에서 좌표·문자열로 한 것이고(이 PC 에 브라우저 확장이 붙지 않는다),
  대조 236항목 불일치 0·기하 검사 겹침 0 은 **계산상 그렇다는 뜻이지 보기에
  괜찮다는 뜻이 아니다.** 여는 방법은 세션 7.0-D 와 같다 — `file://` 은 `fetch`
  가 막히므로 저장소 루트에서 `python -m http.server` 로 열고 `/demo/pfd.html`
  를 본다. 기본 탭은 「서버실」이다

  **볼 것 셋** — ⑴ **1600×900 에서 세로 스크롤 없이 들어가는가** ⑵ 시나리오
  **「랙 하나가 막힌다」를 끝까지 훑었을 때 랙 08 이 튀어 보이는가**(주입 랙이다)
  ⑶ **서버실 탭이 설명 없이 읽히는가**(경영층 시연 기준)

  **그 뒤 후보 — 사람이 정한다.** 이 판이 고르지 않았다.
  · **화면 수정(7.7)** — ⑴~⑶ 에서 나온 것 + **#47**(등축 헤더 이름표 자리) +
    **#48·#49** 문안(정본 `CLAUDE.md` 를 여는 판이 된다)
  · **조건부 전환 ⑵ 전이 시간축 재수립**(**#40**)
  · **조건부 전환 ⑴ 누출 정의를 질량손실로 교체**(**#36**)
- **#48·#49 는 다음에 `CLAUDE.md` 를 고치는 판에 얹는다.** 이 둘만으로 정본을
  열지 않는다

ctx: 잔여 토큰 14,993,995 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다 — 사용률 표시는 이 판에 없었다) · 가장 크게 먹은 것: `PROCEED.md` 구조
파악용 `grep`·`sed` 조회 넷(그래도 전부 짧다 — 이 판은 오늘 가장 적게 먹었다) ·
줄일 수 있었다고 보는 것: 없음 — 세션 7.5 의 `tail -18` 자리에 이번에는 `grep -n`
으로 행 번호만 뽑고 `cut -c1-14` 로 폭을 잘라 읽었고, 딸려 온 것이 없다

### 세션 7.7 · 2026-09-02 · 서버실 탭에 CDU 내부 그림 (시연 화면 판 · 새 값 0개)

**게이트 없다.** 6장 기준을 재판정하지 않았다. **새 숫자 0개.** 바꾼 파일은
`demo/pfd.html` **한 장**이다 — `src/cdu_simul/`·`assumptions.py`·시연 JSON
생성 코드·`demo/demo_steady.json`·52열본 데이터셋·`demo/tag-map.md`·`pic/`·
정본 문서 4종을 **하나도 고치지 않았다.** `demo_version` 을 올리지 않았다.

**이 판의 목적은 값이 아니라 「보이는 것」이었다.** 이미 화면에 있던 값을,
그 값을 내는 장치의 자리로 옮긴 것이 전부다. 옮기기 전후로 **값 글자의 id
집합이 같다**(36개) — 늘지도 줄지도 않았다.

#### 한 일 (C1~C9)

- **C1 배치.** 서버실 탭을 왼쪽 **CDU 내부 그림**(약 28 %)과 오른쪽 **등축
  서버실**(약 72 %)로 나눴다. **등축은 `G` 도 `iso()` 도 손대지 않았다** —
  그림칸을 등축 상자 왼쪽에 내고 좌표만 평행이동했다(축척 불변). single 은
  위 칸 하나만 서고 아래는 비운다 · dual 은 둘이 세로로 선다. 시나리오·구성·
  부하·저항 컨트롤과 아래 편차 띠는 자리 그대로다
- **C2 그림.** `pic/edit.jpg` 의 CDU A·CDU B 상자 위치·크기와 그 안의
  열교환기·펌프·스트림 배치를 따랐다. **조형은 지금 화면 그대로 다시 그렸다**
  (같은 선 굵기 · 색 여섯 · 시스템 폰트). 영문 라벨을 옮기지 않고 한국어로
  적었다. 담은 것: 열교환기 상자(1차 입구 = 환수온도 · 1차 출구 = 공급온도 ·
  duty [kW] · 2차 입구 = 2차측 공급온도 · 2차 유량 = 그 CDU 몫) · 펌프(토출
  유량 [m³/h] · **양정 없음**) · 랙 쪽 공급/환수 스트림. **스트림 색은
  절대온도** 27~44 ℃ 고정이고 범위 밖은 양 끝으로 클램프한다 — 랙의 「평균
  대비」 계조와 기준이 달라 그림 아래에 「스트림 색 = 절대온도」 한 줄을
  적었다. **2차측 환수는 선만 그리고 숫자를 붙이지 않았다** — 파선으로 그리고
  「2차측 환수 — 모델 밖」이라고 적었다(절대 규칙 7)
- **C3 중복 제거.** 등축 옆 텍스트 블록에서 duty·공급온도·환수온도·총유량·
  2차측 배분을 빼고 그림 안 제자리에 붙였다. 블록에는 **이름과 부하만** 남는다
  (「CDU A」+「부하 nn %」). 2차측 공급온도 줄도 그림 쪽으로 옮기고 등축 바닥의
  그 줄을 지웠다. **문자열은 옮기기 전과 글자 그대로 같다**
- **C4 CDU B.** 골격(열교환기 상자·펌프·스트림)은 A 와 같게 세우되, 띄우지
  않기로 한 항목(duty·공급온도·총유량·2차 입구 온도)은 **라벨 자체를 뺐다** —
  빈 값을 「—」 로 채우지 않았다. 2차측 배분과 환수온도만 내고 R0 이 아니면
  Δ 배지를 붙인다
- **C5 움직임.** 도형·색 약 200 ms 전이 · 숫자는 즉시 바뀌며 한 번 깜빡인다 ·
  표시 문자열이 같으면 안 깜빡인다(기존 `setText` 그대로)
- **C6 표기.** 맨 아래 한 줄 그대로 · 저항 축 이름 「랙 배관 저항 증가」 ·
  「누출」·`heat_capacity_ratio`·`Cr` 이 화면에 0회(#41)
- **C9.** pytest **1,207건 전부 통과** · ruff · mypy 통과.
  `demo/demo_steady.json` **git 무변경** 확인

#### C7 대조 검증 — 불일치 0

`demo/pfd.html` 의 `<script>` 를 그대로 꺼내 **가짜 DOM 위에서 통째로** 돌리고
(`paint()` 까지 실제로 실행된다), 화면에 남은 표시 문자열을 **수로 되돌려**
JSON 원본과 비교했다. 서식은 검증 쪽에서 따로 썼다 — 화면 코드를 갖다 쓰지 않는다.

| 케이스 | 대조 | 불일치 |
|---|---|---|
| `single/L100/R0` | 두 탭 · CDU A | 0 |
| `single/L100/R50` | 두 탭 · CDU A | 0 |
| `dual/L70/R50` | 두 탭 · CDU A·B (Δ 배지 포함) | 0 |
| **합계** | **165항목** | **0** |

- **`paint` 가 만진 id 중 골격에 없는 것 0** · 텍스트 블록에서 뺀 id 를
  `paint` 가 아직 찾고 있는 일 0(그 id 들은 그림 안으로 자리만 옮겼다)
- **CDU 내부 그림의 값도 전부 포함**했다 · C3 으로 옮긴 값이 **옮기기 전과 같은
  문자열**인지도 함께 봤다

#### C8 기하 검사 — 위반 0

| 항목 | single | dual |
|---|---|---|
| 글자상자 / 도형 | 36 / 11 | 65 / 22 |
| 그림칸 ↔ 등축 겹침 | 0 | 0 |
| viewBox 이탈 | 0 | 0 |
| 글자 ↔ 글자 | 0 | 0 |
| 글자 ↔ 도형 | 0 | 0 |
| 도형 ↔ 도형 | 0 | 0 |
| 글자의 그림칸 이탈 | 0 | 0 |

CDU 경계 사각형은 **담는 상자**라 글자↔도형 대상에서 뺐다(그 안에 글자가 드는
것이 설계다). 열교환기 상자·펌프는 대상에 넣었다.

#### 판단이 필요한 것 · 못 한 것

- **렌더를 눈으로 보지 못했다.** C7·C8 은 전부 node 위에서 좌표·문자열로 한
  것이고, **계산상 겹치지 않는다는 뜻이지 보기에 좋다는 뜻이 아니다.**
  사람이 브라우저로 직접 열어 확인해야 한다
- **우하단 Short Spec 상자는 만들지 않았다** — 이 판의 범위 밖이다(`pic/edit.jpg`
  의 그 자리는 비워 뒀다)
- 새 미해결 **#50** 하나를 열었다 — C3 으로 등축 바닥의 2차측 줄을 지우면서
  **등축의 2차측 배관 두 줄이 이름표를 잃었다.** #47(등축 헤더 이름표 자리)과
  같은 성격이라 함께 정한다. **이 판에서 고치지 않았다**(지시 범위 밖)

ctx: 잔여 토큰 14,824,371 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다 — 사용률 표시는 이 판에 없었다) · 가장 크게 먹은 것: `demo/pfd.html`
전문 읽기(713행) — 그림을 얹을 자리를 정하려면 골격·`paint`·기하를 다 봐야 했다 ·
줄일 수 있었다고 보는 것: `pic/edit.jpg` 를 읽은 것(이미지 한 장이 컸다. 다만
배치를 정하는 근거였으므로 대체 수단이 없었다 — 줄이려면 사람이 좌표를 글로
주는 수밖에 없다)

### 세션 7.8 · 2026-09-02 · 글자를 줄이고 도형으로 (시연 화면 판 · 새 값 0개)

**게이트 없다.** 6장 기준을 재판정하지 않았다. **새 숫자 0개.** 바꾼 파일은
`demo/pfd.html` **한 장**이다 — `src/cdu_simul/`·`assumptions.py`·시연 JSON
생성 코드·`demo/demo_steady.json`·52열본 데이터셋·`demo/tag-map.md`·`pic/`·
「계통도」 탭·정본 문서 4종을 **하나도 고치지 않았다.** `demo_version` 을
올리지 않았다.

**이 판의 기준은 「읽는 법을 글로 설명해야 하는 표시는 실패다」였다.** 설명
문구를 지우고 그 뜻을 도형이 지게 했다 — 2차측이 모델 밖이라는 것은 **파선
사각형**이, 유량이 얼마나 움직였는지는 **값 옆의 막대**가 진다.

#### 한 일 (C1~C11)

- **C1 2차측 파선 상자.** 열교환기 왼쪽에서 꺾여 올라가던 2차측 공급선과
  아래로 빠지던 파선 환수선을 지우고, 그 자리에 **파선 사각형 하나**(국소
  좌표 `x −220 · y 160 · 200 × 170`)를 놓았다. 상자와 열교환기를 **두 줄**로
  이었다 — 위(`y 190`)가 상자에서 나가는 쪽, 아래(`y 300`)가 상자로 드는
  쪽이고, 1차측이 아래(`y 330`)에서 위(`y 170`)로 흐르므로 **대향류**다.
  두 줄에 화살촉을 붙였다. 상자 안에는 **「2차측」 · 공급온도 · 그 CDU 몫
  유량** 셋만 든다. 상자와 그 안의 값은 **A·B 양쪽에 선다**(`RA-Tsec`·
  `RB-Tsec` 로 갈랐다 — 옛 `R-Tsec` 하나는 없앴다). CDU B 는 지금 방침
  그대로 유량과 환수온도만 진다
- **C2 열교환기.** 「냉수 in」·「냉수 out」을 **넣지 않았다** — C1 의 상자가
  그 자리를 대신한다. 열교환기 상자 자체의 표시는 그대로다
- **C3 문구 삭제.** 「스트림 색 = 절대온도」(A·B 양쪽) · 「유량은 표시
  자릿수에서 움직이지 않아 싣지 않는다」(서버실 탭) 를 지웠다
- **C4 하단 띠.** `#devstrip` 을 **통째로** 지웠다(HTML·CSS·`innerHTML`
  모두). 그 자리는 비웠고 등축이 그만큼 높아진다
- **C5 편차 막대를 값 옆으로.** CDU A 「총유량 nn.nn m³/h」 오른쪽과 등축
  **랙 08 유량 숫자** 오른쪽에 하나씩. 축척은 **그대로 77/(m³/h)** 이고
  둘이 같은 축척이다. **라벨을 붙이지 않았다.** 길이 0 이면 아무것도
  그리지 않는다(둘 다 같은 방식) — 저항 0 % 에서 두 막대 다 안 보인다.
  색은 색 여섯 중 **`--warn`**(이상 표시)
- **C6 범례 한 줄.** 우상단 색 범례 상자 맨 위에 **「부하 = 슬래브 높이」**
  한 줄. 서버실 탭에서만 보인다(계통도 탭은 왼쪽 안내칸이 이미 진다).
  바닥 배관·숫자 두 줄 설명은 **되살리지 않았다**
- **C7 움직임.** 지금과 같다 — 도형·색 약 200 ms 전이, 숫자는 즉시 바뀌며
  한 번 깜빡이고 표시 문자열이 같으면 안 깜빡인다. 막대는 도형이므로
  전이한다(`transform: scaleX()` 에 `.2s` — 폭 속성 대신 `transform` 을
  쓴 이유는 전이가 확실히 걸려서다)
- **C8 표기.** 맨 아래 한 줄 그대로 · 저항 축 이름 「랙 배관 저항 증가」 ·
  「누출」·`heat_capacity_ratio`·`Cr` 이 화면에 **0회**(#41)
- **C11.** pytest **1,207건 전부 통과** · ruff · mypy 통과.
  `demo/demo_steady.json` **git 무변경** 확인(`git status` 가 낸 것은
  `demo/pfd.html` 하나뿐이다)

#### C9 대조 검증 — 불일치 0

`demo/pfd.html` 의 `<script>` 를 그대로 꺼내 **가짜 DOM 위에서 통째로** 돌리고
(`paint()` 까지 실제로 실행된다), 화면에 남은 표시 문자열을 **수로 되돌려**
JSON 원본과 비교했다. 서식은 검증 쪽에서 따로 썼다 — 화면 코드를 갖다 쓰지 않는다.

| 케이스 | 대조 | 항목 | 불일치 |
|---|---|---|---|
| `single/L100/R0` | 두 탭 · CDU A | 46 | 0 |
| `single/L100/R50` | 두 탭 · CDU A | 46 | 0 |
| `dual/L70/R50` (= `cdu_config: dual_asymmetric`) | 두 탭 · CDU A·B (Δ 배지 포함) | 74 | 0 |
| **합계** | | **166** | **0** |

- **`paint` 가 찾았으나 골격에 없는 id 0.** 이 판은 지운 것이 많아 이것을
  특히 봤다 — 없앤 `R-Tsec`·`R-totdev`·`R-hitdev` 를 `paint` 가 아직 찾고
  있지 않고, `RA-Tsec`·`RB-Tsec`·`RA-devtot`·`RA-devrack` 은 골격에 있다
- **CDU 내부 그림과 2차측 상자의 값도 전부 포함**했다

**C5 막대 — px 로 되돌린 값** (모형 단위 = 등축과 같은 축척)

| 케이스 | 랙 08 막대 | CDU A 총유량 막대 |
|---|---|---|
| `single/L100/R0` | dev 0.000000 m³/h → **0 px** | dev 0.000000 m³/h → **0 px** |
| `single/L100/R50` | dev −0.585929 m³/h → **45.1 px** | dev −0.144518 m³/h → **11.1 px** |
| `dual/L70/R50` | dev −0.585520 m³/h → **45.1 px** | dev −0.144368 m³/h → **11.1 px** |

세 케이스 모두 `px = |dev| × 77` 과 소수 둘째 자리까지 일치한다 —
**둘이 같은 축척**이다. 전 136 케이스에서 이 두 편차는 **항상 ≤ 0** 이라
(양수 0건) 막대는 크기만 진다.

#### C10 기하 검사 — 위반 0

| 항목 | single | dual |
|---|---|---|
| 글자상자 / 도형 | 35 / 15 | 62 / 28 |
| viewBox 이탈 | 0 | 0 |
| 글자 ↔ 글자 | 0 | 0 |
| 글자 ↔ 도형 | 0 | 0 |
| 도형 ↔ 도형 | 0 | 0 |
| **막대 ↔ 글자** (옆 숫자를 가리는가) | 0 | 0 |
| 그림칸 ↔ 등축 | 0 | 0 |
| **2차측 상자 ↔ 등축** | 0 | 0 |
| 글자의 그림칸 이탈 | 0 | 0 |

- 막대는 **최대 길이(50 px)로 잡아** 검사했다 — 실제 최대가 45.1 px 이므로
  이보다 넉넉한 판정이다
- **담는 상자 둘(CDU 경계 사각형 · 2차측 파선 사각형)은 글자↔도형 대상에서
  뺐다** — 그 안에 글자가 드는 것이 설계다. 열교환기 상자·펌프 원·막대는
  대상에 넣었다
- 도형↔도형은 **담는 상자와 막대끼리**만 봤다 — 배관·화살촉이 장치를
  지나가는 것은 설계다(펌프 원 위를 1차측 공급 스트림이 지난다)
- 하단 띠를 지운 뒤 남은 자리는 `#roomwrap` 이 `flex:1` 로 먹는다 —
  흘러내릴 다른 요소가 없다(`#pane-room` 의 자식이 하나만 남았다)

#### 판단이 필요한 것 · 못 한 것

- **렌더를 눈으로 보지 못했다.** C9·C10 은 전부 node 위에서 좌표·문자열로 한
  것이고, **계산상 겹치지 않는다는 뜻이지 보기에 좋다는 뜻이 아니다.**
  사람이 브라우저로 직접 열어 확인해야 한다
- **2차측 상자 안의 두 값은 라벨 없이 단위만 진다**(`27.000 ℃` ·
  `55.78 m³/h`). 「글자는 장치 이름과 운전값」 기준을 그대로 따른 것인데,
  **온도가 공급온도라는 것과 유량이 그 CDU 몫이라는 것은 상자 위치와
  화살표 방향으로만 읽힌다** — 사람이 보고 부족하면 라벨을 되살린다
- **#47·#50 은 이 판에서 고치지 않았다** — 이번 화면을 보고 사람이 판단한다.
  다만 #50 의 본문이 근거로 든 「그림 쪽이 2차측 공급·환수를 적고 있다」는
  이 판에서 사라졌다(그 글자를 지웠다) — **#50 은 닫히지 않았고 오히려
  등축 배관 두 줄의 이름표 없음이 더 뚜렷해졌다**
- 새 미해결 **#51** 하나를 열었다 — 서버실 탭에서 지운 문구가 계통도 탭에는
  그대로 남아 두 탭의 말이 갈렸다

ctx: 잔여 토큰 14,829,830 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다 — 사용률 표시는 이 판에 없었다) · 가장 크게 먹은 것: `demo/pfd.html`
전문 읽기(806행) — 지울 것과 옮길 것을 정하려면 골격·`paint`·기하를 다 봐야
했다 · 줄일 수 있었다고 보는 것: `PROCEED.md` 「현재 상태」 표를 `sed -n '40,50p'`
로 연 것 — 필요했던 것은 마지막 행의 서식과 삽입 위치뿐인데 **긴 표 행 일곱
개가 통째로 딸려왔다**(#49 가 지적한 그 수단을 줄 번호로 바꿔 되풀이한 것이다.
행 하나만 잘라 여는 편이 나았다)

### 세션 7.9 · 2026-09-02 · 랙 위 두 줄에 단위와 색 (시연 화면 판 · 새 값 0개)

**게이트 없다.** 6장 기준을 재판정하지 않았다. **새 숫자 0개.** 바꾼 파일은
`demo/pfd.html` **한 장**이다 — `src/cdu_simul/`·`assumptions.py`·시연 JSON
생성 코드·`demo/demo_steady.json`·52열본 데이터셋·`demo/tag-map.md`·`pic/`·
「계통도」 탭·정본 문서 4종을 **하나도 고치지 않았다.** `demo_version` 을
올리지 않았다.

**이 판의 기준은 「랙 위 숫자 두 줄이 무엇인지 화면 안에서 읽히는가」였다** —
글자를 여덟 번 반복하지 않고. 늘린 글자는 **단위 두 개**(맨 앞 랙 하나)뿐이다.

#### 한 일 (C1~C5)

- **C1 맨 아래 한 줄.** 「가정값 기반 — 실측 아님 · 프로젝트정리 5장」에서
  뒤의 출처만 뺐다. **줄은 그대로 화면에 있다**(절대 규칙 11). 데이터
  판번호·`assumption_tag` 는 그 줄의 `title` 툴팁이 그대로 진다
- **C2 단위는 맨 앞 랙에만.** **랙 01** 하나에만 「7.33 m³/h」·「41.959 ℃」
  처럼 붙였고 나머지 일곱은 숫자만 진다. 랙 01 을 고른 근거: 등축에서
  `rackY(i) = (7−i)·PITCH` 라 랙 01 이 **y 가 가장 커서 화면 맨 앞이자 맨
  왼쪽**에 선다(`sx=(x−y)cos30` 이므로 y 가 크면 왼쪽) — 시선이 먼저 닿는
  자리다. dual 이면 **CDU A·B 두 열 각각의 랙 01** 에 붙는다(B 열은 유량
  숫자가 없으므로 ℃ 하나뿐이다). 단위는 `<tspan class="unit">` 으로 값
  뒤에 붙고 **글자 크기 0.62배·불투명도 0.6** 이다(JS `UFS` 와 CSS
  `.unit` 이 같은 비를 쓴다)
- **C3 두 줄의 색.** **온도 줄만** 칠했다 — `devColor(tdev)`, 즉 **그 랙
  슬래브와 정확히 같은 색**이고 우상단 색 범례(−1.5~+1.5 K)가 재는 것과
  같은 값이다(검증에서 두 색이 같은 문자열임을 확인했다). 계조를 단색으로
  덮지 않았다 — 원래 이 글자에 계조가 없었고 새로 건 것이다. **유량 줄은
  손대지 않았다**(굵은 구조선 색 그대로). 색은 `--cold`↔`--hot` 보간
  안에서만 났고 이상 표시 `--warn` 은 쓰지 않았다
- **C4 움직임.** 지금과 같다. 단위가 붙은 값도 같은 규칙을 따른다 —
  `setText` 이 붙인 뒤의 `textContent`(=「7.33 m³/h」)를 그대로 비교하므로
  **값이 바뀌면 깜빡이고 값이 같으면 안 깜빡인다.** 글자 색은 도형과 같은
  등급이라 `.t-temp` 로 200 ms 전이한다
- **C5 표기.** 저항 축 이름 「랙 배관 저항 증가」 · 「누출」·
  `heat_capacity_ratio`·`Cr` 이 파일 전체에서 **0회**(#41)

#### C6 대조 검증 — 불일치 0

`demo/pfd.html` 의 `<script>` 를 그대로 꺼내 **가짜 DOM 위에서 통째로** 돌리고
(`paint()` 까지 실제로 실행된다), 화면에 남은 표시 문자열에서 **단위를 떼고
수로 되돌려** JSON 원본과 비교했다. 서식은 검증 쪽에서 따로 썼다.

| 케이스 | 대조 | 항목 | 불일치 |
|---|---|---|---|
| `single/L100/R0` | 두 탭 · CDU A | 46 | 0 |
| `single/L100/R50` | 두 탭 · CDU A | 46 | 0 |
| `dual/L70/R50` (= `cdu_config: dual_asymmetric`) | 두 탭 · CDU A·B (Δ 배지 포함) | 74 | 0 |
| **합계** | | **166** | **0** |

- **단위가 붙은 랙과 안 붙은 랙의 서식이 같다** — 되돌린 수의 소수 자릿수를
  항목마다 검사했다(유량 2자리 · 온도 3자리). 랙 01 과 랙 02~08 이 전부 일치
- **`paint` 가 찾았으나 골격에 없는 id 0**
- CDU 내부 그림과 2차측 파선 상자의 값도 전부 포함했다
- 편차 막대 px 는 앞 판과 같다(`|dev| × 77`): R0 에서 0 px · R50 에서 랙 08
  45.1 px · 총유량 11.1 px
- **C4 를 코드로 확인했다**: 같은 케이스를 다시 칠하면 깜빡임 **0회**,
  값이 바뀌면 검사한 다섯 글자 **전부** 깜빡였다(단위가 붙은 둘 포함)

#### C7 기하 검사 — 위반 0

| 항목 | single | dual |
|---|---|---|
| 글자상자 / 도형 | 35 / 15 | 62 / 28 |
| viewBox 이탈 | 0 | 0 |
| 글자 ↔ 글자 | 0 | 0 |
| 글자 ↔ 도형 | 0 | 0 |
| 도형 ↔ 도형 | 0 | 0 |
| 그림칸 ↔ 등축 | 0 | 0 |
| 글자의 그림칸 이탈 | 0 | 0 |

- 단위 폭까지 글자상자에 잡아두고(`txt()` 가 `textW(" "+unit, fs·UFS)` 를
  더한다) 검사했다. 넓어진 셋은 `RA-q0`·`RA-t0`·`RB-t0` 다
- 이 판이 좁힌 틈은 **랙 01 유량 글자 ↔ 랙 02 도형**(가로)으로
  **34.1 px → 11.5 px** 다(온도 줄은 15.6 px). 랙 간격 `PITCH 140` 이
  화면에서 121 px 이라 단위를 한 랙 더 붙이거나 더 긴 단위를 쓰면 닿는다 —
  **단위를 맨 앞 랙 하나에만 붙인 이유이기도 하다**
- 화면 전체에서 가장 좁은 틈은 **같은 랙 안 글자 두 줄 사이 2.2 px**
  (「RACK 01」↔유량)인데 **이 판이 만든 것이 아니다** — 세로 간격은
  건드리지 않았다
- 막대는 최대 길이(50 px)로 잡아 검사했다 · 담는 상자(CDU 경계·2차측 파선
  사각형)가 그 안의 것을 담는 것은 겹침으로 세지 않았다

#### C8

pytest **1,207건 전부 통과** · ruff · mypy 통과. `git status` 가 낸 것은
`demo/pfd.html` 하나뿐이다 — **`demo/demo_steady.json` git 무변경**.

#### 판단이 필요한 것 · 못 한 것

- **렌더를 눈으로 보지 못했다.** C6·C7 은 전부 node 위에서 좌표·문자열로 한
  것이고, **계산상 겹치지 않는다는 뜻이지 보기에 좋다는 뜻이 아니다.**
  특히 **랙 01 유량 글자와 랙 02 도형 사이 12 px** 은 사람이 봐야 한다
- **온도 숫자가 슬래브와 같은 색이 되면서, 평균에 가까운 랙의 글자는
  회보라(`rgb(120,98,106)` 부근)로 난다** — 흰 바닥에서 읽히는 것은
  확인했으나 진하기가 랙마다 달라 보이는 것은 의도다(그것이 계조다).
  사람이 보고 산만하면 온도 줄만 되돌린다
- **#47·#50·#51 은 이 판에서 고치지 않았다** — 지시 범위 밖이다
- 새 미해결을 열지 않았다

ctx: 잔여 토큰 14,858,565 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: `demo/pfd.html` 전문 읽기(761행) — 단위를 어디에
붙일지·겹치는지 정하려면 골격(`buildRoom`)·`paint`·기하를 다 봐야 했다 ·
줄일 수 있었다고 보는 것: `PROCEED.md` 미해결 목록 첫 행들을 딸려 읽은 것
(7.8 세션 로그 끝을 보려다 `## 미해결 목록` 아래 긴 표 행 열몇 개가 통째로
따라왔다 — 세션 로그 끝은 `## 미해결 목록` 앞에서 끊어 여는 편이 나았다)

### 세션 7.10 · 2026-09-02 · 왼쪽 CDU 그림을 등축으로 (시연 화면 판 · 새 값 0개)

**단계:** 왼쪽 CDU 내부 그림이 오른쪽 서버실과 **같은 등축으로 서게** 했다.
평면 사각형이던 장치 셋을 서버실이 쓰는 투영·면 구성 그대로 육면체로 세웠고,
계통도 탭의 설명 문구 둘을 서버실 탭과 같은 기준으로 걷었다(#51 닫힘).
**게이트 없음** — 물리 모델·가정치·데이터에 닿지 않는다.

**완료한 항목 (C1~C10)**

- **C1** 장치 셋을 등축 육면체로 다시 세웠다. **서버실의 `iso()`·`boxFaces()` 를
  그대로 재사용**했다(새로 짠 것 없음). 새로 만든 것은 「그 육면체를 그림칸의
  화면 원점에 얹는」 얇은 껍데기 셋뿐이다 — `pIso`·`isoSolid`·`isoDashed`.
  면 색·선 굵기는 서버실 랙과 같은 `.fc`/`.f-top`/`.f-side`/`.f-front` 를 그대로
  쓴다(장비 면 흰색 · 옆면만 `--faced` 음영 · 구조선 `--ink`). 2차측만 `.secout`
  (파선·`fill:none`)으로 실루엣 + 안쪽 모서리 셋을 그린다 — **모델 밖이라는 뜻이
  파선에 걸려 있으므로 채우지 않았다.**
  - 치수 [모형 단위]: CDU 경계 240×240×165 · 열교환기 95×105×115(경계 안 앞왼쪽)
    · 2차측 120×120×100. 화면 원점은 경계·열교환기가 (238, 235) 를 함께 쓰고
    2차측이 (−129, 280) 을 따로 쓴다(모델 밖이라 다른 공간이다).
  - **CDU 경계는 절개(cutaway)로 그린다** — 경계 육면체를 먼저 그리고 안의 장치를
    그 위에 얹는다. 평면판의 `frame` 사각형이 하던 규칙과 같다. 열교환기는 경계의
    실루엣 **안**에 완전히 든다(여덟 꼭짓점 전부 확인).
- **C2** 스트림 네 줄과 화살촉은 평면 그대로다 — 등축 축을 따라 꺾지 않았다.
  색 규칙도 그대로(절대온도 27~44 ℃ 클램프 · 2차측 되돌아가는 줄은 파선 회색).
  **끝점 여섯 개가 전부 육면체 면 위에 있다**(아래 C9 표).
- **C3** 펌프는 원형 심볼 그대로 두었다. 자리만 공급 스트림을 따라 옮겼다.
- **C4** 글자는 내용·자릿수·크기 전부 그대로다 — 도형이 움직인 만큼만 따라갔다.
  CDU A 총유량 옆 편차 막대도 같이 갔다(축척 77 px/(m³/h) 불변).
  **표시 문자열 집합이 세션 7.9 와 한 글자도 다르지 않음을 기계로 확인**했다.
- **C5** 계통도 탭에서 설명 문구를 걷었다 — **빠진 문구 셋 · 새로 든 문구 둘**:
  - 빠짐: 「2차측 배분 · CDU A」 · 「2차측 배분 · CDU B」 ·
    「유량은 표시 자릿수에서 움직이지 않아 싣지 않는다」
  - 듦: 「CDU A」 · 「CDU B」 — **값은 하나도 지우지 않았다.** 앞 둘은 라벨에서
    설명(「2차측 배분 ·」)만 떼고 **장치 이름**을 남긴 것이다(그 칸이 선 `#sec`
    띠의 첫 칸이 「2차측 공급온도 (고정 경계조건)」이라 「2차측」은 띠에 남는다).
    셋째는 값이 없는 순수 설명이라 칸을 통째로 뺐다.
  - 딸린 정리: 그 문구만 쓰던 CSS `.tag` 와, 평면 사각형만 쓰던 `.eq`·`.frame`
    이 이 판의 변경으로 고아가 되어 함께 지웠다.
- **C6** 움직임은 그대로다 — `.fc` 는 서버실 랙과 같이 전이가 없고(도형이 안
  움직인다), 색이 바뀌는 것(`.stream`·`.t-temp`·`.rfill`)과 편차 막대만 200 ms
  전이한다. 숫자는 `setText` 가 그대로 진다(표시 문자열이 같으면 안 깜빡인다).
- **C7** 맨 아래 한 줄 「가정값 기반 — 실측 아님」 그대로 · 저항 축 이름 「랙 배관
  저항 증가」 그대로 · 「누출」 0회 · `heat_capacity_ratio`/`Cr` 0회(파일 전체 grep).

**C8 대조 (표시 문자열 → 수 → JSON 원본)**

| 케이스 | 항목 | 불일치 | 편차 막대 px (랙08 / 총유량) |
|---|---|---|---|
| `single/L100/R0` | 46 | **0** | 0.0 / 0.0 |
| `single/L100/R50` | 46 | **0** | 45.1 / 11.1 |
| `dual_asymmetric/L70/R50` | 74 | **0** | 45.1 / 11.1 |
| **합계** | **166** | **0** | 세션 7.9 와 같음 |

- 두 탭 모두 · CDU 내부 그림과 2차측 파선 육면체 안의 값도 포함한다.
- 단위가 붙은 값은 단위를 떼고 수로 되돌려 `demo/demo_steady.json` 원본과 맞췄다.
- **`paint` 가 만지는 id 중 골격에 없는 것 0.**
- **항목 수가 앞 판과 같다(46/46/74 · 합계 166).** C5 로 지운 셋은 값이 없는
  정적 문구라 이 수에 들지 않는다 — 그래서 줄지 않았고, 줄지 않은 것이 맞다.
- 앞 판(`git show HEAD:demo/pfd.html`)을 같은 하네스로 돌려 **166개 표시 문자열을
  일대일 비교 — 차이 0.**

**C9 기하 (single·dual 양쪽)**

| 검사 | single | dual |
|---|---|---|
| 글자 ↔ 글자 겹침 | **0** | **0** |
| 글자 ↔ 도형 겹침 (담는 상자 뺀 뒤) | **0** | 20 (전부 등축 서버실 · 아래 참고) |
| 글자 ↔ 열교환기 육면체 겹침 | **0** | **0** |
| viewBox 이탈 | **0** | **0** |
| 글자·도형의 그림칸 이탈 | **0** | **0** |
| CDU 그림칸 ↔ 등축 서버실 겹침 | **0** | **0** |

- **그리는 순서 뒤→앞**: 2차측 → CDU 경계 → 열교환기 → 스트림 → 펌프 → 글자.
  **뒤 도형이 앞 도형을 덮는 자리는 없다.** 경계 육면체가 열교환기를 부피로는
  감싸지만 절개라 덮지 않는다(위 C1). 서버실 랙도 먼 것(랙 08)부터 그대로다.
- **스트림 끝점 6개가 전부 육면체 면 위** — 허공에서 끊긴 끝점 0:
  `sSup` 시작 (225.0, 270.0)·`sRet` 끝 (225.0, 340.0)·`sSec` 끝 (51.8, 255.0)·
  `secout` 시작 (51.8, 330.0) 은 열교환기 면, `sSec` 시작 (−25.1, 255.0)·
  `secout` 끝 (−25.1, 330.0) 은 2차측 면. 오른쪽 끝(`OUT_X` = 545) 둘은 랙으로
  나가는 자유단이라 검사 대상이 아니다.
- **담는 상자로 뺀 겹침 15건(single)** — CDU 경계 면 위의 글자 9 · 2차측 파선
  육면체 위의 글자 6. 그중 **다섯은 2차측의 안쪽 모서리(파선) 위에 글자가
  얹힌 것**이다: 상자 중심으로 모이는 모서리 셋이 글자 줄을 지난다. C9 가
  「담는 상자는 뺀다」로 정한 자리이고 파선은 불투명도 0.6 이라 글자가 위에
  읽히지만, **보기에 걸린다면 고칠 여지가 있는 자리**로 남긴다(아래 관측).
- **dual 의 20건은 이 판이 만든 것이 아니다.** 전부 **B열 랙 글자 ↔ A열 환수
  헤더·갈래 배관**이고, `git show HEAD:demo/pfd.html` 을 같은 검사기로 돌리면
  **같은 20건이 그대로 나온다.** 이 판은 등축 서버실의 도형·좌표를 한 줄도
  건드리지 않았다. 새 미해결 **#52** 로 올린다.
  (앞 판들이 「위반 0」으로 적은 것은 검사를 **같은 열 안에서만** 했기 때문으로
   보인다 — 이 판의 검사기는 두 열을 **교차로도** 본다. 바닥 타일(`.tile`,
   불투명도 0.13)은 배경 무늬라 검사에서 뺐다.)

**C10** 배분을 끈 상태에서 `pytest` 1,207건 · `ruff` · `mypy` 통과.
`demo/demo_steady.json` **git 무변경** · `demo_version` 그대로.

**만든/바꾼 파일**

- `demo/pfd.html` — CDU 내부 그림 기하(등축 육면체 셋 + 껍데기 함수 셋) ·
  글자 자리 · 계통도 탭 문구 둘 · 고아가 된 CSS 셋(`.tag`·`.eq`·`.frame`)
- `PROCEED.md` — 이 기록
- 그 밖은 **하나도 건드리지 않았다**: `src/cdu_simul/` · `assumptions.py` ·
  시연 JSON 생성 코드 · `demo/demo_steady.json` · 52열본 데이터셋 ·
  `demo/tag-map.md` · `pic/` · 정본 문서 4종

**미해결**

- **#51 을 닫았다** — 계통도 탭도 서버실 탭과 같은 기준으로 걷었다.
- **#47·#50 은 이 판에서 고치지 않았다** — 이번 화면을 보고 사람이 다시 판단한다.
  - **관측(판단 아님)**: #50 의 본문은 등축의 2차측 배관 두 줄이 「정체 불명」이라
    했다. 이 판에서 왼쪽 그림의 2차측이 **파선 육면체**로 서면서, 등축의 그 두
    줄과 **같은 파선·같은 등축 언어**를 쓰게 됐다. 두 그림이 한 화면에 있으므로
    「모델 밖으로 나가는 것」이라는 뜻이 이전보다 붙기는 쉬워졌다. 다만 **등축의
    두 줄에 이름표가 붙은 것은 아니다** — #50 이 요구한 것은 이름표다.
- **새 미해결 #52** — 등축 dual 에서 B열 랙 글자가 A열 환수 배관 위에 얹힌다(20곳).

**정본 문서 무수정 — 프로젝트 지식 재업로드 필요 없음.**

ctx: 잔여 토큰 14,784,605 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: `PROCEED.md` 「현재 상태」 표를 `sed -n '36,44p'` 로
연 것 — 마지막 행의 서식만 보려던 것인데 그 구간의 행 아홉 개가 전부 초장문이라
한 번에 크게 들어왔다(시작 4번을 어긴 셈이다) · 줄일 수 있었다고 보는 것:
같은 곳 — `grep -n "^| 7\.9 |"` 로 행 번호를 먼저 잡고 그 **한 행만** 열면
됐다(실제로 두 번째 시도는 그렇게 했고 한 행으로 끝났다)

### 세션 7.11 · 2026-09-02 · CDU 경계를 2D 로 (시연 화면 판 · 새 값 0개)

**무엇이 문제였나** — 세션 7.10 이 CDU 경계를 등축 육면체(절개)로 세운 결과,
경계의 윗면·옆면이 열교환기 뒤로 비쳐 장치가 상자 안에 갇힌 것처럼 보였다.
기하 검사기가 그것을 숫자로도 잡는다: 경계 육면체의 면 모서리가 값 줄
다섯(single 기준)을 지나고 있었다. **3차원으로 서야 하는 것은 장치이지
경계가 아니다.**

**C1 · 경계를 평면 사각형 하나로** — `PG.FR` 을 육면체 5튜플
`[0,0,240,240,165]`(원점 `PG.O`) 에서 평면 사각형 4튜플 `[24,148,520,280]`
(국소 `x 24..544 · y 148..428`)로 되돌리고, `isoSolid(O, PG.FR)` 대신
`<rect class="frame" …/>` 하나를 그린다. CSS `.frame` 을 다시 넣되
**7.9 의 그것과 다르다** — `fill:none`(면을 칠하지 않아 지면색 `--ground`
가 그대로 비친다) · `stroke-width:1`(장치의 1.2 보다 얇다) ·
`opacity:.45`(옅다). 색은 `--ink` 하나이므로 색 여섯을 넘지 않는다.
**안의 장치·스트림·펌프·글자는 좌표를 하나도 옮기지 않았다** — 바뀐 것은
경계 도형 하나뿐이다.

담김을 좌표로 확인했다(국소):
- **안** — 열교환기 육면체 세 면(`51.2..225.6 · 189.4..405.6`) · 펌프
  원과 화살촉 · 값 글자 넷(`열교환기 …kW`·`펌프`·`공급 …℃`·`총유량 …m³/h`
  +편차 막대 `438.2..488.2`·`환수 …℃`) · 2차측 유입 화살촉 · 환수 유입 화살촉
- **가로지름**(경계를 드나든다 — 의도한 것) — 스트림 네 줄
- **밖** — 제목 `CDU A`(경계 위) · 2차측 파선 육면체와 그 글자 셋(모델 밖)
  · 랙 쪽 출구 화살촉 · 2차측 복귀 화살촉

**C2 · 지운 것 없음** — 7.10 이 경계를 위해 넣은 것 중 **경계에만 쓰이고
이제 쓰이지 않는 것은 없다.** `pIso`·`pPts`·`isoSolid` 는 열교환기가,
`isoDashed` 는 2차측이, `PG.O` 는 열교환기가 계속 쓴다. 7.10 이 지웠던
`.eq`·`.tag` 는 지운 채로 둔다(경계와 무관하다).

**C3 · 움직임 무변경** — 전이·깜빡임 규칙에 손대지 않았다. `.frame` 은
값에 따라 바뀌지 않는 정적 도형이라 전이를 걸지 않는다.

**C4 · 표기** — 맨 아래 한 줄 「가정값 기반 — 실측 아님」 그대로 ·
저항 축 이름 「랙 배관 저항 증가」 그대로 · 화면에 「누출」 없음 ·
`heat_capacity_ratio`/`Cr` 없음(#41).

**C5 · 대조 검증** — 앞 판과 같은 세 케이스를 `git show HEAD:demo/pfd.html`
과 일대일로 돌렸다. **항목 46 / 46 / 74 · 합계 166 · 불일치 0**.
편차 막대도 같다(`single/L100/R0` 0 · `L100/R50` 총유량 11.1 · 랙 08 45.1 ·
`dual/L70/R50` 같은 값).

**C6 · 기하 검사** — 같은 검사기로 HEAD 와 이 판을 각각 돌려 비교했다.

| 항목 | HEAD(7.10) | 이 판 |
|---|---|---|
| 글자↔글자 | 0 / 0 | 0 / 0 |
| 글자↔도형 (single / dual) | 10 / 38 | **5 / 31** |
| viewBox 이탈 | 0 | 0 |
| 그림칸 이탈 | 0 | 0 |
| 그림칸↔등축 서버실 | 0 | 0 |
| 스트림 끝점이 면 위 | 6/6 | 6/6 |

줄어든 7건은 **전부 경계 육면체 면이 값 줄을 지나던 것**이다
(A: `공급`×2·`총유량`×2·`환수`×1 / B: `환수 Δ`·`환수`). **새 위반 0.**
앞 판이 남긴 둘은 그대로다 — **#52 의 20건**(dual · B열 글자 ↔ A열
`hdrp`·`RA-pipe1~7`) **20 → 20**, **2차측 파선 육면체 안쪽 모서리가 값 줄을
지나는 5건**(`27.000 ℃`×2·`55.92 m³/h`×3) **5 → 5**.
그리는 순서는 뒤→앞 그대로다: 2차측 → **경계** → 열교환기 → 스트림 →
펌프 → 화살촉 → 글자. 경계는 `fill:none` 이라 안의 장치를 덮지 않는다.

> **검사기 주석** — 이 판의 검사기는 바닥 타일(`.tile` · `opacity .13` 배경
> 격자)을 도형에서 제외한다. 넣으면 랙 이름표·숫자와 40건이 잡히는데,
> 이는 7.10 이 「0」으로 적은 것과 맞지 않아 앞 판도 같은 기준이었던 것으로
> 본다. 이 기준에서 검사기가 7.10 이 남긴 두 수(#52 의 20건 · 2차측 5건)를
> 그대로 재현한다.

**C7 · 검사기** — `pytest` **1,207건 통과**(배분 끈 상태 = 모델 기본값) ·
`ruff` · `mypy`(29 파일) 통과 · `demo/demo_steady.json` **git 무변경** ·
`demo_version` 무수정(`demo-7.2-B`).

**정본 문서 무수정 — 프로젝트 지식 재업로드 필요 없음.**
**미해결 무변경** — #47·#50·#52 를 이 판에서 고치지 않았고, 새로 연 것도 없다.

ctx: 잔여 토큰 14,837,785 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그
값을 적는다) · 가장 크게 먹은 것: `demo/pfd.html` 의 그리기 코드 구간을
읽은 것(`sed -n '270,400p'`·`'440,600p'`·`'600,873p'` — 화면 코드 전체를
사실상 한 번씩 열었다) · 줄일 수 있었다고 보는 것: 같은 곳 —
`600~873` 줄(컨트롤·이벤트 배선)은 이 판이 도형 하나만 바꾸는 판이라
열 필요가 없었다. 대조 하네스가 무엇을 흉내내야 하는지 보려고 열었는데,
`paintRoomCdu` 하나만 열어도 됐다

### 세션 7.12 · 2026-09-02 · 시연 꾸러미 (배포 판 · 새 값 0개)

**무엇이 필요했나** — 지금까지 시연 화면은 저장소를 clone 한 PC 에서만 열렸다.
사람이 다른 PC(파이썬은 있으나 저장소는 없는 자리)로 화면을 들고 가려면
파일 몇 개와 여는 법이 한 덩어리로 묶여 있어야 한다. 이 판은 그 꾸러미를
만드는 판이고, **화면 자체는 건드리지 않는다.**

**C1 · 화면이 데이터를 읽는 방식** — `demo/pfd.html:865` 가
`fetch("demo_steady.json")` 로 읽는다. 값이 HTML 안에 박혀 있지 않으므로
`file://` 로 열면 브라우저가 막는다(코드 자신도 실패 시 그 문구를 띄운다).
**간이 서버가 필요하다** — 그래서 C2 의 실행 파일 둘을 만들었다.

**C2 · 실행 파일 둘** — 둘 다 제 위치(`%~dp0` / `dirname "$0"`)로 옮겨간 뒤
`python -m http.server 8000` 을 띄우고 브라우저로 `pfd.html` 을 연다.
절대경로가 없으므로 어디에 풀어도 돈다. 파이썬은
`sys.version_info >= (3, 12)` 를 한 줄로 확인해, 미만이면 그 사실을 적고
멈춘다(`start.bat` 은 `pause` 로 창이 닫히지 않게 한다).
- `demo/start.bat` — `py -3` 이 있으면 그것을, 없으면 `python` 을 쓴다
- `demo/start.sh` — `python3` 이 있으면 그것을, 없으면 `python`.
  브라우저는 `open`(macOS) → `xdg-open`(Linux) 순으로 시도한다
- **문구는 영문이다** — Windows 콘솔 코드페이지가 자리마다 달라 한글이
  깨질 수 있다. 코드페이지를 바꾸는 것보다 영문 한 줄이 싸다(프롬프트가
  허용한 쪽)

**C3 · `demo/README.txt`** — 네 줄이다. 필요한 것(파이썬 3.12 이상) · 여는 법 ·
멈추는 법(Ctrl+C) · 맨 아래 `가정값 기반 — 실측 아님`. 화면 읽는 법과
공학 설명은 적지 않았다.

**C4 · `demo/make_bundle.py`** — `zipfile` 하나만 쓴다. 제 위치를 기준으로
다섯 파일(`pfd.html`·`demo_steady.json`·`start.bat`·`start.sh`·`README.txt`)을
`arcname=name` 으로 **평평하게** 담아 `dist/cdu-demo.zip` 으로 낸다.
푼 자리에서 `start.bat` 이 바로 도는 구조다.

**C5 · `.gitignore` 무변경** — `dist/` 가 이미 13행(「패키징/테스트 산출물」)에
있었다. 새로 넣을 것이 없어 **손대지 않았다** — 꾸러미는 커밋되지 않는다.

**C6 · 실제로 싸서 확인** — `dist/cdu-demo.zip` 87,814 B.
안: `pfd.html` 48,590 · `demo_steady.json` 562,248 · `start.bat` 556 ·
`start.sh` 637 · `README.txt` 234 (B). 다섯 개 전부 들었다. 임시 자리에 풀어
`pfd.html` 과 `demo_steady.json` 이 **같은 자리에 나란히** 놓이는 것을 확인하고
푼 자리는 지웠다.

**C7 · 화면 무변경** — `git status` 에 `demo/pfd.html`·`demo/demo_steady.json`
이 나오지 않는다(새 파일 넷만 `??`). 값·라벨·좌표 어느 것도 바뀌지 않았다.

**C8 · 검사기** — pytest 1,207건 통과 · ruff 통과 · mypy(`src tests`) 통과.
`make_bundle.py` 는 **ruff 대상에 든다**(`ruff check .` 가 저장소 전체를 본다) —
첫 시도에서 docstring 한 줄이 E501(93>90)로 걸려 줄을 나눴다.
**mypy 대상에는 들지 않는다**(명령이 `mypy src tests` 다) — 참고로 따로
돌려 보니 통과한다.

**오늘(2026-09-02) 한 판들** — 7.7 서버실 탭에 CDU 내부 그림 → 7.8 글자를
줄이고 도형으로 → 7.9 랙 위 두 줄에 단위와 색 → 7.10 왼쪽 CDU 그림을
등축으로 → 7.11 CDU 경계를 2D 로 → 7.12(이 판) 시연 꾸러미.
**화면 작업은 여기서 매듭짓는다** — 7.7~7.11 이 그림을 세웠고 이 판이 그것을
들고 다닐 수 있게 쌌다.

**다음 세션이 알아야 할 것**
- **시연 화면은 이제 읽기 전용으로 둔다.** 남은 미해결 #47·#50·#52 는 셋 다
  등축 좌표를 건드려야 닫히고, 그것은 사람이 화면을 보고 판정한 뒤에 연다
- **다음 판의 후보는 소개자료 HTML** 이다(시연 화면과 별개의 문서)
- 꾸러미를 옮길 때는 `dist/cdu-demo.zip` 하나만 옮기면 된다. 저장소가 없는
  자리에서 풀고 `start.bat`(또는 `start.sh`)을 실행한다

**미해결 무변경** — #47·#50·#52 를 이 판에서 고치지 않았고, 새로 연 것도 없다.

ctx: 잔여 토큰 14,925,917 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그
값을 적는다) · 가장 크게 먹은 것: pytest 전체 1,207건을 두 번 돌린 것
(첫 회가 백그라운드로 넘어가 결과를 못 받고 다시 돌렸다 — 9분×2) ·
줄일 수 있었다고 보는 것: 그 재실행 — 첫 회를 `ruff`·`mypy` 와 한 줄로
묶지 않고 따로 돌렸으면 백그라운드로 넘어간 뒤에도 결과를 그대로 받았다

### 세션 7.13 · 2026-09-03 · 단일 HTML 시연본 (배포 판 · 새 값 0개)

**무엇이 필요했나** — 세션 7.12 의 꾸러미(`dist/cdu-demo.zip`)는 받는 사람이
압축을 풀고 `start.bat` 을 실행해야 열린다. 메일로 받은 사람이 **파일 하나를
더블클릭**하는 것으로 끝나려면 데이터가 HTML 안에 들어 있어야 한다 —
`pfd.html` 이 `fetch` 로 데이터를 읽는 한 `file://` 에서는 열리지 않기 때문이다.

**C1 · `demo/make_single_html.py`** — 표준 라이브러리(`json`·`re`·`pathlib`)만
쓴다(절대 규칙 12). `demo/pfd.html` + `demo/demo_steady.json` 을 **읽기만 해서**
`dist/cdu-demo.html` 하나를 낸다. 경로는 제 파일 위치 기준 상대경로다(절대
규칙 15). 이름은 `demo/make_bundle.py`(zip 꾸러미) 옆에 나란히 서게 지었다.

**C2 · 치환은 한 자리다** — `pfd.html:865` 의 `fetch("demo_steady.json")` 를
`Promise.resolve({ json: () => (…데이터…) })` 로 바꾼다. 뒤따르는
`.then(r => r.json()).then(d => …)` 사슬이 **한 글자도 안 바뀌고 그대로 돈다.**
- 그 문자열이 **정확히 한 번** 나오지 않으면(0번이든 2번이든) 예외를 던지고
  멈춘다. 조용히 원본을 그대로 복사하지 않는다
- 스크립트를 끊는 글자를 유니코드 이스케이프(`u003c` 꼴)로 바꿨다: `<`
  (`</script>`·`<!--` 가 여기서 나온다) · U+2028·U+2029(예전 JS 문법의
  줄바꿈). JSON 에서 `<` 는 문자열 안에만 나올 수 있으므로 이렇게 바꿔도
  뜻이 보존된다. 바꾼 뒤 **남아 있는지 다시 센다**
- 데이터를 도로 뽑을 수 있게 `/*CDU-DEMO-DATA*/`…`/*CDU-DEMO-END*/` 로 감쌌다

**C3 · 빌드 직후 스스로 검사한다** — 셋 다 통과해야 파일을 쓴다(검사 전에는
`dist/` 에 아무것도 남기지 않는다). **실제 실행 결과**:
- ⑴ 산출물에서 도로 뽑아 `json.loads` 한 것이 `demo_steady.json` 을
  `json.loads` 한 것과 **완전히 같다**(`==` 로 비교) · 케이스 **136개**
- ⑵ `demo_steady.json` 을 읽는 `fetch` **잔존 0**
- ⑶ 「가정값 기반 — 실측 아님」 표기 **유지**(제목줄·화면 아래 띠 둘 다 원본 그대로)

**C4 · 크기** — `dist/cdu-demo.html` **596,859 B (582.9 KB)**. 원본 둘
(`pfd.html` 48,590 B + `demo_steady.json` 562,248 B)의 합보다 작다 —
`demo_steady.json` 이 작업트리에 CRLF 로 있어 읽으면서 LF 로 줄고, `<`
이스케이프가 그보다 적게 늘기 때문이다. **데이터가 같다는 것은 ⑴ 이 판정한다.**

**따로 본 것 하나 — 산출물의 JS 가 문법상 성립하는가** — `node --check` 로
`<script>` 본문(467,202자)을 파싱해 통과했다. **브라우저로 열어 본 것은
아니다**(이 PC 에서 브라우저 자동화가 연결되지 않았다) — 화면이 실제로
그려지는 것은 사람이 더블클릭해 확인한다.

**C5 · `.gitattributes` 에 `*.sh text eol=lf` 한 줄**(미해결 #53). 이미 커밋된
파일은 자동 정규화되지 않으므로 index 를 확인했다 — `git ls-files --eol
demo/start.sh` → `i/lf w/lf attr/text eol=lf` 로 **index·작업트리 둘 다 이미
LF** 였다. 그대로 두었고 `git add --renormalize` 는 돌리지 않았다.
`git status` 에 `start.sh` 가 나오지 않는다 — 파일 내용은 하나도 안 바뀌었다.

**C6 · 미해결 #53 등재** — 7.12 보고가 올리겠다고 적었으나 목록에 없던 것을
등재하고 **같은 판에서 닫았음**을 적었다. 등재 이력을 잃지 않게 닫힌 채로 남긴다.

**건드리지 않은 것** — `demo/pfd.html`·`demo/demo_steady.json` **git 무변경**
(`git status` 에 나오지 않는다) · `dist/cdu-demo.zip`·`start.bat`·`start.sh`·
`README.txt`·`make_bundle.py` 등 7.12 꾸러미 그대로 · `src/`·시험·`assumptions`
무수정.

**C7 · 검사기** — `pytest` **1,207건 통과**(9분 42초) · `ruff` 통과 ·
`mypy`(`src tests` · 29 파일) 통과. 새 스크립트는 **ruff 대상에 든다**
(`ruff check .` 가 저장소 전체를 본다) — 첫 시도에서 예외 메시지 한 줄이
E501(91>90)로 걸려 줄을 나눴다(`make_bundle.py` 때와 같은 자리다).
**mypy 대상에는 들지 않아**(명령이 `mypy src tests` 다) 따로 돌려 통과를
확인했다. 줄을 나눈 뒤 pytest 를 다시 돌리지는 않았다 — 시험 어디도 `demo/`
를 참조하지 않아 수집 대상이 아니다. 대신 **빌드를 다시 돌려** C3 검사 3종이
그대로 통과하고 산출물 크기가 같음을 확인했다.

**다음 세션이 알아야 할 것**
- 메일로 보낼 것은 이제 `dist/cdu-demo.html` **하나**다. `dist/` 는
  `.gitignore` 에 있어 커밋되지 않으므로, 보낼 때마다
  `.venv/Scripts/python.exe demo/make_single_html.py` 로 다시 뽑는다
- **꾸러미가 둘이 됐다**(zip + 단일 HTML). 단일 HTML 이 자리를 잡으면
  `start.bat`·`start.sh`·`README.txt`·`make_bundle.py`·`dist/cdu-demo.zip` 이
  필요 없어질 수 있으나 **이 판에서 정리하지 않았다**(절대 규칙 13) — 사람이 정한다
- `pfd.html` 의 `.catch` 안내문(「저장소 루트에서 `python -m http.server` 로
  연다」)은 단일 HTML 에서는 **닿지 않는 코드**다(fetch 가 없다). 원본을 고치지
  않기로 한 판이라 그대로 뒀다

**정본 문서 무수정 — 프로젝트 지식 재업로드 필요 없음.**
**미해결** — #53 을 등재하고 같은 판에서 닫았다. #47·#50·#52 는 손대지 않았다.

---

### 세션 7.14 · 2026-09-03 · 소개자료 수치 대조 (관측 판 · 새 가정치 0개)

**무엇이 필요했나** — `docs/intro-outline.md` §4·§5 에 수치가 여럿 적혀 있는데
일부가 세션 1-B·4 기록값으로 보였다. 5-1 「2차측 유체」가 **「세션 5-B 이전 로그의
숫자를 이후 것과 나란히 읽지 않는다」**고 못박고 있으므로 그대로 쓰면 금지된 나란히
놓기가 된다. 소개자료를 만들기 전에 **전부 지금 코드 기준으로 다시 확인해** 한
파일에 모았다.

**C1 · 데모 수치의 케이스를 밝혔다** — outline §4 3-1 의 네 수치가 어느 케이스인지
`demo/demo_steady.json` 을 훑어 찾았다: **`single/L100/R0`**(단일 CDU · 부하 100%
= 640 kW · 저항 증가 0% · 고정축 H22.4/dPb2/dPv3 · NTU 2 · 2차측 27℃ · 랙별
등가길이 배분 **켬**). 네 값 모두 outline 과 **같다** — 총유량 55.8329 m³/h ·
랙별 유량 6.7087~7.2687 m³/h · 공급 32.1695℃ · 랙 출구 42.0575~42.8829℃.
outline 에는 이 케이스 조건이 붙어 있지 않아 **대조표에 조건 열을 두었다.**

**C2 · energy balance 재산출** — `model.default_cases()` 4케이스 최대 |잔차|
**0.0050641 %** · `default_cdu_cases()` 32조합 **0.00506 %**. 세션 1-B·5-B 기록
**0.00506 % 와 같다.**

**C3 · cp 평가 기준별 판정 표 — 재산출 가능했다** (`cp_rule` 이 `SteadyStateCase`
의 필드라 **코드를 고치지 않고** 셋을 다 돌릴 수 있다).

| cp 평가 | 세션 1-B 기록 [%] | 재산출 [%] | |
|---|---|---|---|
| 벌크 평균온도 | 0.00506 | 0.0050641 | 같다 |
| 공급온도 | 0.30891 | 0.3089074 | 같다 |
| 환수온도 | 0.31524 | **0.3152212** | **다르다** (Δ 0.0000188 %p) |

**멈춤 조건에 걸렸다 — 고치지 않고 미해결 #54 로 올린다.** 판정 방향은 바뀌지
않고(셋 다 6장 기준 0.1% 대비 같은 쪽) outline 표기 0.315% 도 두 값에서 같다.
**차이의 원인은 적지 않았다** — 이 판은 관측 판이다.

**C4 · 여섯 수치의 출처·유효성** — 재산출이 값싼 것은 다 돌렸다
(`leak.all_steady_signals()` 96건이 0.9 s 였다).

| 수치 | 재산출 | 결과 |
|---|---|---|
| 양정 상대변화 5e-5 | 했다 | +5% 수준 **4.346e-05~9.814e-05**(32조합) · +50% 수준은 3.887e-04~8.799e-04 — outline 은 **어느 수준인지 적지 않았다** |
| 저항 +50% 총유량 −0.16~0.37% | 했다 | **−0.3710~−0.1642 %** · 세션 4 기록 −0.3704~−0.1638 과 **다르다**(4째 자리) · outline 표기로는 같다 |
| 잔여저항 60~83% | 했다 | **60.00~83.33 %** — outline 과 같다 |
| 25A 1.94 L/s → 3.48 m/s | 했다 (산술) | 내경 26.64 mm 로 **3.4805 m/s** — 같다 |
| 자동 시험 1,207건 · 데모 136 케이스 | 했다 | **1,207 통과** · **136 케이스** — 같다 |
| 데이터셋 정상 320 · 이상 960행 | 했다 | `_steady_rows_by_leak_level()` → 0/5/20/50 각 **320행** = 정상 320 · 이상 960 — 같다 |

**C5 · 편차 막대를 물리량으로 옮겼다** — `demo/pfd.html` `DEV_PX = 77`(px 당
1 m³/h) · `DEV_MAX = 50`(px, = **0.6494 m³/h**). 저항 50%(`single/L100/R50`)에서
총유량 막대 **11.13 px = −0.14452 m³/h** · 저항 주입 랙(랙 7) 막대 **45.12 px =
−0.58593 m³/h**. 136 케이스 전체 최대 편차가 0.5859 m³/h(45.1 px)라 **클램프에
닿는 케이스가 없다.** 세션 7.9~7.11 이 대조에 적어 온 「0 / 45.1 / 11.1」과 같다.
**자료에는 px 가 아니라 물리량으로 쓴다.**

**C6-b · `dist/`** — 이미 `.gitignore` 에 있었고(`# 패키징/테스트 산출물` 절)
`git ls-files dist/` 가 빈 결과라 **커밋된 파일이 없다.** `.gitignore` 를 고치지
않았다 — 저장소 설정 커밋이 비어 이 판의 커밋은 문서 하나다.

**만든 파일**

| 파일 | 성격 |
|---|---|
| `docs/intro-figures.md` | 신규 (25항목 대조표 · 갈린 값 셋을 펼친 절 · outline 관측 목록) |

**건드리지 않은 것** — `demo/pfd.html`·`demo/demo_steady.json`·`dist/` 는 읽기만,
`docs/intro-outline.md`·`src/`·`assumptions`·시험·데이터셋·정본 문서 4종은 무수정.

**다음 세션이 알아야 할 것**
- **소개자료 HTML 을 만들 때 `docs/intro-figures.md` 의 값을 쓴다** — 케이스 조건을
  함께 적는다. outline §4 3-1 의 네 수치는 조건이 붙어 있지 않다
- **저항 +50% 총유량 변화는 두 개다.** 세션 4 경로(32조합·균등 배분) −0.16~0.37%
  와 데모 경로(34건·배분 켬) −0.259% 는 **다른 것을 잰 값**이다. 나란히 놓지 않는다
- `docs/intro-outline.md` 에서 눈에 띈 넷은 **목록으로만** 적었다
  (`docs/intro-figures.md` §4) — 고칠지는 사람이 정한다

**정본 문서 무수정 — 프로젝트 지식 재업로드 필요 없음.**
**미해결** — **#54 를 새로 열었다.** #47·#50·#52 는 손대지 않았다.

---

### 세션 7.15 · 2026-09-03 · 소개자료 HTML 골격 (골격 판 · 새 숫자 0개)

**사람이 정한 것 셋** — ⑴ 성격 = **알리는 자료**(승인 요청·기간·비용을 넣지 않는다)
⑵ 용도 = **배포용 겸 발표용**(혼자 읽어도 되되 한 화면 다섯 줄을 넘기지 않는다 —
글이 넘치면 줄이지 말고 화면을 하나 더 만든다) ⑶ 시연 = **한 파일로 합친다**
(`dist/cdu-demo.html` 을 iframe 으로 품는다).

**자리 이름** — `demo/` 가 「화면 + 그 빌드 스크립트」를 한 폴더에 두는 관행이라
**`intro/`** 를 같은 모양으로 만들었다: `intro/intro.html` + `intro/make_intro_html.py`
→ `dist/cdu-intro.html`.

**C1 · 14 화면** (6장 12~15 범위 안). 각 화면은 **제목과 자리표시만** 담고 본문은
비어 있다. `outline` §1 표를 따르되 넷을 반영했다: ⑴ 6장에서 승인·기간·비용 문장을
뺐다 ⑵ 시연은 `demo/pfd.html` 링크가 아니라 **실물 iframe**(화면 03)이다
⑶ Aspen 표의 「AI 학습 데이터 생성 — 목적에 맞게 설계됨」 줄은 **넣지 않고**
「반복 시나리오 생성에 특화」로 두었다(판정 6 과 부딪힌다) ⑷ 2장은 캡처가 아니다.

**C2 · 조형** — `demo/pfd.html` 의 색 여섯(`#EDF0F2`·`#FFFFFF`·`#3E4C56`·`#2E7FA8`·
`#C2452D`·`#E8A33D`)을 **값만** 가져왔다. **CSS 를 복사하지 않았다** — 복사하면
시연 화면이 바뀔 때 갈린다. 시스템 폰트(`Malgun Gothic`…) · 외부 라이브러리 0개 ·
기준 1600×900 · `scroll-snap-type:y mandatory` 로 화면 단위.

**C3 · iframe** — `dist/cdu-demo.html` **전체**를 `srcdoc` 속성에 넣었다.
`&` 와 `"` 만 HTML 이스케이프했고 빌드가 도로 풀어 **476,063 자가 원본과 같음**을
확인한다(검사 ⑴). **시연 화면을 한 글자도 고치지 않았다.** iframe 은 별개 문서라
CSS·id 가 섞이지 않는다 — 산출물에서 iframe 을 떼어낸 겉껍질(15,633 자)에
`<style>` 이 **1개**(소개자료 것), `<script>` 가 **0개**임을 확인했다. 돌아오는 길
(「← 자료로 돌아가기」)은 **iframe 밖**에 두었다.

**C4 · 수치 자리표시** — 자료에 나갈 수치는 하나도 적지 않고 **`docs/intro-figures.md`
의 항목 번호**만 자리표시에 달았다(`<!-- figures #24: … -->` 형태 + 화면에 보이는
`figures #24` 표시). 표기 규칙 넷을 `intro.html` 머리 주석에 못박았다: ⑴ 랙 번호는
**「랙 08」**(0-based 를 쓰지 않는다) ⑵ 편차 막대는 px 가 아니라 **m³/h** ⑶ 양정
상대변화는 **#12·#13 두 수준을 함께** ⑷ 총유량 변화는 **#15 를 본문**, #14 는 각주.

**C5·C6 · 빌드가 검사한다** (`intro/make_intro_html.py` · 실패하면 **파일을 쓰지
않고** 예외를 던진다).

| 검사 | 실행값 |
|---|---|
| ⑴ 품은 시연 화면 = 원본 | 476,063 자 **동일** |
| ⑵ 금지어 「누출」·「정확」·「대체」 | 각 **0회** (iframe 안은 **대상에서 제외**) |
| ⑶ 화면 수 = 「가정값 기반 — 실측 아님」 표기 수 | **14 = 14** |
| ⑷ 화면 수 범위 | **14 ∈ [12, 15]** |

**iframe 안을 제외한 것을 밝힌다** — 시연 화면은 읽기 전용이라 고칠 수 없고, 그
화면에도 걸리는 말이 없다. 검사는 산출물에서 `<!--DEMO-BEGIN-->`~`<!--DEMO-END-->`
를 떼어낸 나머지에만 건다.

**검사가 헛돌지 않는지 확인했다** — 산출물 문자열을 메모리에서만 일부러 어긋뜨려
셋 다 걸리는 것을 봤다(파일은 건드리지 않았다): 「정확」 넣기 → 검사 ⑵ 걸림 ·
표기 한 줄 빼기 → 검사 ⑶ 걸림(14 대 13) · 시연 payload 한 글자 바꾸기 → 검사 ⑴ 걸림.

**C7 · 산출물** — `dist/cdu-intro.html` **702,743 B (686.3 KB)**.
빌드는 `demo/make_single_html.py` 를 **그대로 실행해**(`subprocess`) 시연 화면을
얻는다 — 그 스크립트를 고치지도 베끼지도 않았다.

**만든 파일**

| 파일 | 성격 |
|---|---|
| `intro/intro.html` | 신규 (소개자료 소스 · 14 화면 골격) |
| `intro/make_intro_html.py` | 신규 (시연을 품는 빌드 + 검사 4종) |

**건드리지 않은 것** — `demo/pfd.html`·`demo/demo_steady.json`·`demo/make_single_html.py`
는 읽기만, `docs/intro-figures.md`·`docs/intro-outline.md`·`src/`·`assumptions`·시험·
데이터셋·정본 문서 4종은 무수정.

**다음 세션이 알아야 할 것**
- **브라우저로 열어 보지 않았다.** 「열린다」고 적지 않는다 — 사람이 확인한다
- 다음 판은 **본문과 수치를 채운다.** 수치는 `docs/intro-figures.md` 에서만 가져오고,
  `intro.html` 머리 주석의 표기 규칙 넷을 지킨다
- **한 화면 다섯 줄을 넘기면 줄이지 말고 화면을 하나 더 만든다.** 빌드가 화면 수
  12~15 를 검사하므로, 16번째 화면이 필요하면 `SCREEN_MAX` 를 사람이 정해 올린다
- 자리표시가 쓰지 않은 figures 항목은 **#22·#23**(편차 막대 축척 77 px/(m³/h)·
  클램프)이다 — 표시 규약이라 자료에 낼 값이 아니다

**정본 문서 무수정 — 프로젝트 지식 재업로드 필요 없음.**
**미해결** — 무변경. 새로 연 것 없다. #47·#50·#52·#54 는 손대지 않았다.

**`docs/intro-outline.md` 에서 낡은 곳 — 목록으로만 (고치지 않았다)**
1. §3 「직접 열어보기 버튼 → `demo/pfd.html`」과 §10 「시연 화면은 별도 파일」은
   이 판에서 **iframe 으로 품기**로 바뀌었다
2. §3 구성 1 「정지 화면 캡처 1장」도 캡처가 아니라 **실물**이 됐다
3. §9 Aspen 표의 「AI 학습 데이터 생성 — 목적에 맞게 설계됨」 줄은 판정 6 과
   부딪혀 자료에 넣지 않았다
4. §3 시연 시나리오의 「랙 08 막대 45 px · 총유량 막대 11 px」은 표기 규칙 ⑵ 에
   따라 **m³/h** 로 바뀌어야 한다(figures #25 · #24)
5. §11 의 다섯 항목은 사람이 정해 끝났다 — 「제작 전에 정할 것」이 아니라 기록이다

ctx: 잔여 토큰 14,875,340 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그
값을 적는다) · 가장 크게 먹은 것: `PROCEED.md` 미해결 목록을 `grep` 으로
훑은 것 — `#53` 을 찾으려고 `grep -n "53"` 을 걸었더니 표의 대형 행
(#36·#41 등 한 행이 수천 자다)이 **통째로** 딸려 왔다 ·
줄일 수 있었다고 보는 것: 같은 곳 — 처음부터 `cut -c1-160` 을 붙였으면
행의 앞머리만 받아 항목 번호를 찾을 수 있었다(뒤늦게 붙여 효과를 봤다).
시작 4번이 「「현재 상태」 표를 통째로 읽지 않는다」인데, 미해결 목록도
같은 성질이라는 것이 이 판의 관측이다(#49 와 같은 자리)

---

### 세션 7.16 · 2026-09-03 · 소개자료 본문 채우기 (자리표시 48개 · 새 숫자 0개)

**세션 번호에 대해** — 이 판을 여는 프롬프트는 골격 판을 「7.16」이라 불렀으나
`PROCEED.md` 기록은 **7.15** 다(`intro/intro.html`·`intro/make_intro_html.py` 를
만든 판). 저장소 기록을 따라 **이 판을 7.16** 으로 적는다.

**C1 · Aspen 화면을 본문으로 올렸다.** 문서 순서가 `… s11(5장) · s13(Aspen) ·
s12(6장)` 이 됐다. 「부록」 표시를 떼고 장 표시를 **「도구 비교」** 로 두었다 —
5장과 6장 사이라 번호를 붙이면 6장과 부딪힌다. **id 는 만들 때 번호를 그대로
두었다**(체크리스트 「밀린 대로 두고 보고한다」). 돌아오는 링크 `#s02` 하나뿐이라
id 어긋남이 깨뜨리는 것은 없다.

**C2 · 화면별로 채운 자리표시**

| 화면 | 제목 | 채운 자리표시 | 쓴 figures |
|---|---|---|---|
| s00 | 표지 | 2 | — |
| s01 | 1장 왜 만들었나 | 4 | — |
| s02 | 2장 무엇을 만들었나 | 4 | #19 |
| s03 | 2장 시연 (iframe) | 0 (자리표시 없음) | — |
| s04 | 3장 이 숫자는 어떻게 나왔나 | 5 | #1 · #2 · #3 · #6 · #4 |
| s05 | 3장 단순 계산이 아니다 | 3 | — |
| s06 | 3장 표가 맞는지를 계산이 확인했다 | 3 | #3 · #5 |
| s07 | 3장 이게 맞는지 어떻게 아는가 | 4 | #7 · #8 · #18 · #9 · #10 · #11 |
| s08 | 3장 설비로는 못 하는 일 | 3 | #19 |
| s09 | 4장 어디에 계기를 달아야 하는지 | 4 | #25 · #24 · #15 · #14(각주) · #12 · #13 |
| s10 | 4장 지금 할 수 있는 것 | 4 | #16 · #17 · #20 · #21 |
| s11 | 5장 무엇이 더 필요한가 | 5 | — |
| s13 | 도구 비교 (Aspen) | 4 | #19 · #18 |
| s12 | 6장 그 다음에 할 수 있는 것 | 3 | — |
| **계** | **14 화면** | **48** | **#1~#21 · #24 · #25** |

**화면을 늘리지 않았다** — 한 화면 다섯 줄을 넘긴 곳이 없어 14 화면 그대로다
(빌드 범위 [12,15] 안). 안 쓴 figures 항목은 **#22 · #23** 하나뿐인데 표시 규약
(축척 77 px/(m³/h) · 클램프)이라 자료에 낼 값이 아니다 — 7.15 가 예고한 그대로다.

**C3 · 수치 표기** — 자릿수를 표에서 그대로 옮겼고 **반올림을 한 곳도 하지
않았다**(0.0050641 · 0.3089074 · 0.3152212 · 55.8329 · 42.0575~42.8829 · 3.4805
따위를 그대로 썼다). 각 수치 옆에 `.cond` 로 케이스 조건을 작게 달았다.
표기 규칙 넷을 지켰다 — ⑴ 랙 번호는 「랙 08」(figures #12·#13 의 케이스 조건에
있던 **「랙 0 주입」은 자료에서 「랙 01」로** 옮겨 적었다) ⑵ 편차 막대는 m³/h
⑶ 양정 상대변화는 #12·#13 **두 수준 함께** ⑷ 총유량 변화는 **#15 를 본문**,
#14 는 「케이스 집합이 다르다 · 나란히 읽지 않는다」를 붙여 **각주로만**.

**C4 · 편차 막대 SVG** — 캡처를 쓰지 않고 자료 안에서 그렸다. viewBox
`0 0 1200 220` · **축척 1500 px 당 1 m³/h 로 두 막대가 같다** ·
랙 08 −0.58593 → 878.9 px · 총유량 −0.14452 → 216.8 px(길이비 **4.05**) ·
눈금은 0 / −0.2 / −0.4 / −0.6 **m³/h**(px 눈금 아님) · 「4배」라는 글자를
쓰지 않았다.

**C5 · 어휘 재검사에서 고친 것 둘**

| 자리 | 골격(7.15) | 고친 뒤 | 까닭 |
|---|---|---|---|
| s01 업계 현황 | 「기존 **감지** 방식은 사고가 난 뒤에 알린다」 | 「현장 계기는 이상이 충분히 커진 뒤에야 값이 움직인다. 사고가 난 다음에 알게 되는 구조다」 | 「감지」는 기능이 서기 전에 그 방향을 말하는 우회 표현이다 |
| s12 1단계 | 「판별 규칙의 오경보율·**검출 지연**」 | 「판별 규칙이 얼마나 헛울리는지를 계산으로 먼저 재보기」 | 같은 까닭 — 「검출」도 쓰지 않는다 |

또 골격에 있던 **작성 지시용 주석 문구**(「여기서 …로 잇지 않는다」·「figures 표의
케이스 조건 열을 그대로 옮긴다」 따위)는 자료에 나갈 글이 아니므로 지웠다.
기계 검사로도 훑었다 — 「감지·검출·조기·예측·누설·경보·탐지·리크」 **0회**,
0-based 랙 번호 **0회**.

**C6 · 빌드 검사 4종 (실행값)**

| 검사 | 실행값 |
|---|---|
| ⑴ 품은 시연 화면 = 원본 | 476,063 자 **동일** |
| ⑵ 금지어 「누출」·「정확」·「대체」 | 각 **0회** (iframe 안 제외) |
| ⑶ 화면 수 = 표기 수 | **14 = 14** |
| ⑷ 화면 수 범위 | **14 ∈ [12, 15]** |

산출물 `dist/cdu-intro.html` **709,061 B (692.4 KB)** (7.15 의 702,743 B 에서
본문이 늘어난 만큼 +6,318 B). 화면 수가 바뀌지 않았으므로 ⑶⑷ 도 그대로다.

**멈춤 조건에 걸린 것 하나 — figures 표에 없는 숫자**
outline §5 ⑶ 의 「냉각수 통상 설계유속 **2~3 m/s**」가 `docs/intro-figures.md` 에
없다. 골격(7.15) s10 에는 그 문장이 그대로 있었다. **숫자를 새로 들이지 않고
그 대목을 다시 썼다** — 「유속 3.4805 m/s」(#17)와 케이스 조건(25A 내경 26.64 mm ·
랙당 정격 1.94 L/s)까지만 적고, 비교 기준 숫자 없이 「분기 구경이 바뀌면 저항과
열용량이 함께 움직인다」로 맺었다. **넣으려면 사람이 그 값을 figures 항목으로
먼저 올려야 한다.** outline 도 figures 도 고치지 않았다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `intro/intro.html` | 수정 (자리표시 48개 → 본문 · 화면 순서 변경 · SVG 추가 · CSS 마감) |
| `PROCEED.md` | 수정 (현재 상태 표 + 이 로그) |

**건드리지 않은 것** — `intro/make_intro_html.py`(검사를 늘리지 않았다) ·
`demo/` 전부 · `dist/` 산출물(빌드로만 생성) · `docs/intro-figures.md` ·
`docs/intro-outline.md` · `src/`·`assumptions`·시험·데이터셋·정본 문서 4종.

**다음 세션이 알아야 할 것**
- **브라우저로 열어 보지 않았다.** 「보기 좋다」·「열린다」를 적지 않았다 —
  판정은 사람이 한다. 특히 볼 것: 한 화면 안에서 글이 넘치지 않는가(1600×900
  기준) · s09 의 SVG 가 슬롯 안에 들어오는가 · s13→s12 순서가 읽히는가
- **s13 의 id 가 문서 순서와 어긋난다.** 정리하려면 id 두 개를 맞바꾸면 되지만,
  이 판은 「밀린 대로 두고 보고한다」에 따라 두었다
- outline 의 낡은 곳 다섯(7.15 기록)은 **이 판에서도 고치지 않았다.**
  「랙 08 막대 45 px · 총유량 막대 11 px」(§3)은 자료에서 m³/h 로 나갔으므로
  outline 과 자료가 갈린 채다

**정본 문서 무수정 — 프로젝트 지식 재업로드 필요 없음.**
**미해결** — 무변경. 새로 연 것 없다. #47·#50·#52·#54 는 손대지 않았다.

**닫기 검사** — pytest **1,207건 전부 통과**(573.47 s) · ruff `All checks passed!` ·
mypy `no issues found in 29 source files`. 이 판은 `src/`·시험을 건드리지 않았으므로
셋 다 무변경 확인용이다.

ctx: 잔여 토큰 14,856,259 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: `docs/intro-outline.md` 를 **전문(358줄)** 으로 연 것 —
이 판이 실제로 쓴 것은 §2·§4·§5·§6·§7·§9 이고 §10~§12(HTML 제작 지침 · 제작 전에
정할 것 · 제작 순서)는 7.15 가 이미 소화해 필요 없었다 ·
줄일 수 있었다고 보는 것: 같은 곳 — §1 표(장↔화면 대응)를 먼저 보고 필요한 절만
행 범위로 열 수 있었다. 시작 4번의 「통째로 읽지 않는다」가 `PROCEED.md` 말고
**정본·보조 문서에도 걸린다**는 것이 이 판의 관측이다(#49 와 같은 자리)

### 세션 7.17 · 2026-09-03 · 소개자료 다시 쓰기 (읽히게 · 새 숫자 0개)

**이 판의 전제** — 사람이 실물을 읽고 낸 판정 넷을 고친다. 내용을 새로 만들지
않는다. 수치는 전부 `docs/intro-figures.md` 에서 온 것을 다시 배치한 것이고
**표에 없는 숫자를 하나도 넣지 않았다.** 바뀐 파일은 `intro/intro.html` 하나다.

**C1 · scroll-snap 을 걷어냈다.** `html{scroll-snap-type}` 과
`.screen{height:100vh · scroll-snap-align · overflow:hidden}` 을 지웠다. 절 길이가
내용을 따라간다 — 짧은 절(s06 3항)은 짧고 무거운 절(s09 5항)은 길다.
`scroll-behavior:smooth` 도 함께 뺐다: 문서가 길어져 「자료로 돌아가기」가 부드럽게
미끄러지면 한참을 기다려야 한다. 이제 그 자리로 바로 뛴다.

**C2 · 골격 어휘 41개 — 무엇을 무엇으로 바꿨나 (전문)**

지운 것(내용 없이 이름표만 있던 것) 24개:

| 화면 | 지운 이름표 | 어떻게 했나 |
|---|---|---|
| s01 | 배경 · 문제의식 · 업계 현황 · 그래서 무엇을 했나 | 넷 다 지웠다. 첫 항을 `.key` 로 키우고 나머지는 문장만 남겼다 |
| s02 | 읽는 법 ① · 읽는 법 ② · 읽는 법 ③ | 셋 다 지웠다. 문장 자체가 읽는 법이다 |
| s02 | 움직일 수 있는 축 셋 | 지우고 문장 안으로 넣었다 — 「움직일 수 있는 축 셋 — 부하 20→100 % · …」 |
| s05 | 물성이 상수가 아니다 · 압력과 온도를 다른 방식으로 푼다 · 그 이유 | 셋 다 지웠다. 「그 이유」는 앞 문장과 합쳐 `.key` 한 문장이 됐다 |
| s06 | 주어진 것 · 계산한 것 · 읽는 법 | 셋 다 지웠다 |
| s07 | 에너지 balance — 보존법칙 잔차 (기준 0.1 %) · 자동 시험 · 딸린 사실 — 물성 평가 온도를 어디로 잡느냐로 판정이 갈린다 · 한 줄 결론 | 넷 다 지웠다. 「한 줄 결론」의 내용은 **절 제목으로 올라갔다** |
| s08 | 배치 실행 · 왜 계산이어야 하나 · 결과 형태 | 셋 다 지웠다 |
| s09 | 총량 계측의 한계 · 압력 계열 신호 · 그래서 무엇을 하나 | 셋 다 지웠다 |
| s10 | 선 긋기 | 지우고 그 내용을 `.key` 로 올렸다 |
| s11 | 한계 ① — 이상 상태 정의 · 한계 ② — 시간축 · 한계 ③ — 설계데이터 | 셋 다 지웠다. 번호를 매기던 것이 골격 어휘였다 |
| s13 | 이 접근이 잘하는 것 · Aspen 계열이 잘하는 것 · 이 접근의 좁은 곳 · 결론 | 「이 접근이 잘하는 것」·「결론」은 지웠고, 나머지 둘은 **문장 앞머리로** 내렸다(「Aspen 계열이 잘하는 것 — …」) |
| s04 | 위 네 값의 케이스 조건 | 「위 네 값을 낸 조건 — 단일 CDU · …」로 문장에 붙였다 |
| s03 | 시연 화면 — 이 자료 안에서 그대로 조작한다 | 「이 자료 안에서 그대로 조작합니다」 — 「시연 화면」은 화면 자체가 말한다 |

문장으로 바꾼 것(뜻은 서지만 이름표였던 것) 17개:

| 화면 | 전 | 후 |
|---|---|---|
| s04 | 펌프 | 펌프는 낼 수 있는 압력과 배관이 요구하는 압력이 같아지는 자리에서 돕니다 |
| s04 | 배관 | 여덟 갈래로 갈라진 물은 저항이 낮은 쪽으로 더 많이 갑니다 |
| s04 | 열교환기 | 열교환기는 2차측 냉각수로 1차측을 어디까지 식힐 수 있는지를 정합니다 |
| s04 | 랙 | 랙마다 받는 물의 양이 달라, 나가는 물의 온도가 벌어집니다 |
| s10 | 무엇이 유량분배를 지배하나 | 물이 랙마다 얼마씩 갈라지는지는 배관·밸브보다 CDU 안쪽과 헤더가 정합니다 |
| s10 | 구경 검토 | 배관 굵기를 정하기 전에, 물이 얼마나 빠르게 지나가는지를 먼저 봅니다 |
| s10 | 시나리오 반복 생성 | 정상일 때와 이상일 때의 데이터가 표로 그대로 나옵니다 |
| s11 | 열용량이 실제보다 작다 | (문장 안으로) 「… 물을 얼마나 담고 있는지가 가정치 표에 없어 빠졌습니다」 |
| s11 | CDU 간 연동이 절반만 들어 있다 | (문장 안으로) 「… CDU 끼리 물의 양으로만 얽히고 열로는 얽히지 않습니다」 |
| s12 | 1단계 / 2단계 / 3단계 | 단계 번호는 작은 표시로 남기고, 그 밑에 문장 제목을 새로 세웠다 — 「이상 상태의 정의를 바꾸고, 시간축을 다시 세웁니다」 · 「실제 설계데이터를 넣습니다」 · 「시운전하고 운영에 씁니다」 |
| s12 | 필요: … / 얻는 것: … | 「필요한 것 — …」 · 「얻는 것 — …」 (콜론을 없앴다) |
| 전 화면 | 장 번호(`1장`·`2장`·`3장`×5·`4장`×2·`5장`·`도구 비교`·`6장`) | **묶음 이름**으로 갈았다 — 왜 만들었나 · 무엇을 만들었나 · 어떻게 계산하나(5회) · 무엇에 쓰나(2회) · 아직 못 하는 일 · 다른 도구와의 관계 · 다음. 3장이 다섯 번, 4장이 두 번 나오던 번호가 읽는 사람에게 아무것도 주지 않았다 |
| 전 화면 | 우측 부제(`.lead` — 「무슨 과제가 있나」·「이 자료의 무게중심」·「가장 강한 활용처」 등 13개) | **전부 없앴다.** 제목이 문장이 되면서 부제가 같은 말을 두 번 하게 됐다 |

**C3 · 절 제목을 이름표가 아니라 문장으로 — 절 제목 전부**

| 화면 | 전 | 후 |
|---|---|---|
| s00 | AI 데이터센터 CDU 디지털 트윈 | **짓기 전에 계산으로 먼저 돌려본다** (사람이 정한 한 줄 메시지. 원래 제목은 그 위 작은 글씨로 내렸다) |
| s01 | 왜 만들었나 | 배관 안에서 무슨 일이 나는지, 밖에서는 보이지 않습니다 |
| s02 | 무엇을 만들었나 | 다음 화면에서 조건을 직접 바꿔 볼 수 있습니다 |
| s03 | (제목 없음 — 시연) | (그대로 제목 없음. 화면이 곧 내용이다) |
| s04 | 이 숫자는 어떻게 나왔나 | 화면의 숫자는 장치마다 식을 풀어서 나옵니다 |
| s05 | 단순 계산이 아니다 | 표 계산으로 한 번 곱하고 마는 일이 아닙니다 |
| s06 | 표가 맞는지를 계산이 확인했다 | 받은 표가 무엇을 전제한 값인지, 계산으로 되짚었습니다 |
| s07 | 이게 맞는지 어떻게 아는가 | 검사 규칙 하나만 바꿔도 합격이 불합격이 됩니다 |
| s08 | 설비로는 못 하는 일 | 실제 설비로는 해볼 수 없는 조건을 돌려 봅니다 |
| s09 | 어디에 계기를 달아야 하는지를 짓기 전에 안다 | 계기를 어디에 달아야 하는지, 짓기 전에 알 수 있습니다 |
| s10 | 지금 할 수 있는 것 | 지금은 설득 · 검토 · 설계를 돕는 도구까지입니다 |
| s11 | 무엇이 더 필요한가 | 지금 방식은 실제와 신호 방향이 다를 수 있습니다 |
| s13 | Aspen Plus · HYSYS 와의 관계 | Aspen Plus · HYSYS 와 겨루는 도구가 아니라, 용도가 다릅니다 |
| s12 | 그 다음에 할 수 있는 것 | 데이터가 들어오면 설계와 시운전까지 갑니다 |

**C4 · 계층.** 절마다 `.key`(21 px) 한 문장이 서고 본문은 16 px, 조건은 15 px,
식·코드 문자열과 전체 자릿수는 12.5 px 다. 흰 상자를 두른 것은 **셋뿐**이다 —
표지(전폭 흰 면) · s09 각주 · s12 3단계 카드. 나머지 칸의 테두리·배경을 전부
걷었다. 절 사이는 여백 108 px 이고 절 제목 밑의 밑줄(2 px)도 없앴다.
본문 글줄은 38em(한글 38자 안팎), `.key` 는 26em, 제목은 22em 으로 묶었다.

**C5 · 첫 화면 — ㄱ(흰 면)을 골랐다.**
「짓기 전에 계산으로 먼저 돌려본다」를 `clamp(34px, 5.6vw, 60px)` 로 가장 크게
세우고, 그 위에 원래 제목을 작게, 밑에 두 줄(무엇인지 · 「알리는 자료」)을 두었다.
**ㄴ(옅은 격자)을 고르지 않은 이유**: 이 자료에는 이미 무늬가 둘 있다 —
품고 있는 시연 화면(등축 서버실)과 s09 의 막대 그림. 표지에 격자를 깔면 세 번째
무늬가 되고, 격자는 「도면」을 연상시켜 **경영층 독자에게 이 자료를 도면으로
읽히게** 한다. ㄱ 은 이미 쓰는 색(`--face`)만으로 되고, 시연 화면의 흰 패널과
같은 계열이라 자료 전체가 한 벌로 남는다. 색을 새로 들이지 않았다.

**C6 · 다시 쓴 문장 — 어려웠던 다섯**

| # | 전 | 후 |
|---|---|---|
| 1 | 관성 시간상수가 다르다 — 압력은 거의 즉시 평형에 가고 온도는 분 단위로 움직인다 | 압력은 거의 순식간에 자리를 잡고, 온도는 몇 분에 걸쳐 천천히 움직입니다. 그래서 둘을 같은 방식으로 풀지 않습니다 |
| 2 | 질량이 보존되는 모사라 실제 물리와 신호 방향이 다를 수 있다 | 물이 밖으로 빠져나가지 않는 방식이라, 실제와 반대 방향의 신호가 나올 수 있습니다 |
| 3 | 에너지 balance — 보존법칙 잔차 (기준 0.1 %) | 들어온 열과 나간 열이 맞는지를 매번 확인합니다. 합격선은 0.1 % 이고, 지금은 그 스무 분의 일입니다 / 수치 이름은 「계산이 안 맞는 정도」 |
| 4 | 잔여저항(HX 1차측 + CDU 내부배관 + 헤더)이 총양정에서 차지하는 몫이다 | 물이 랙마다 얼마씩 갈라지는지는 배관·밸브보다 CDU 안쪽과 헤더가 정합니다 / 수치 이름은 「펌프가 만든 압력의 60 ~ 83 %」 (원문은 `.cond` 로 작게 남겼다) |
| 5 | 배관을 몇 개의 완전혼합조로 쪼개느냐에 따라 과도 파형이 달라진다 | 배관을 몇 토막으로 나눠 계산하느냐에 따라, 값이 변해 가는 모양이 달라집니다 |

그 밖에 갈아 낸 것 몇: 「슬래브 높이 = 랙 부하」→「막대 높이는 그 랙이 내는
열입니다」 · 「단상 비압축성만 다룬다」→「끓거나 얼지 않는 한 가지 상태의 액체만
다룹니다」 · 「PG25(25 wt% 프로필렌글리콜 수용액)의 비열·밀도를 CoolProp 에서
조회한다」→「온도에 따라 성질이 달라져서, 그때그때 물성 자료에서 다시 읽어
옵니다」(원어는 `.cond` 로 남겼다) · 「밀폐된 소형 배관이다」→「서버로 들어가는
배관은 가늘고 완전히 막혀 있습니다」.
**그대로 둔 것**(근거가 서는 공학 어휘): 대향류 열교환기 · 특성곡선과 시스템곡선의
교점 · ε-NTU · 헤더 · 등가길이 · 잔차.

**C7 · 수치 두 겹.** 본문에 **반올림값 + 이름**, 그 밑에 **figures 전체 자릿수**를
작게(`.exact`) 둔다. 반올림한 자릿수는 `intro.html` 의 수치마다 주석으로 적었다.
**`docs/intro-figures.md` 는 한 글자도 고치지 않았다.**

| figures | 표 값 | 본문(반올림) |
|---|---|---|
| #1 | 55.8329 m³/h | 총유량 **55.8 m³/h** |
| #2 | 6.7087 ~ 7.2687 | 랙별 유량 **6.71 ~ 7.27 m³/h** |
| #3 · #6 | 32.1695 ℃ · 640.00 kW | 1차측 공급 **32.2 ℃** · 열교환량 **640 kW** |
| #4 | 42.0575 ~ 42.8829 | 랙 출구온도 **42.06 ~ 42.88 ℃** |
| #5 | 42.4679 ℃ | 환수 **42.5 ℃** |
| #7 · #8 · #9 | 0.0050641 % · 0.00506 % | 계산이 안 맞는 정도 **0.005 %** |
| #10 · #11 | 0.3089074 % · 0.3152212 % | 공급온도 **0.309 %** · 환수온도 **0.315 %** |
| #12 · #13 | 4.346e-05~9.814e-05 · 3.887e-04~8.799e-04 | +5 % → **0.00004 ~ 0.0001** · +50 % → **0.0004 ~ 0.0009** |
| #15 | −0.2590 ~ −0.2586 % | 총유량 변화 **−0.26 %** |
| #16 | 60.00 ~ 83.33 % | 펌프가 만든 압력의 **60 ~ 83 %** |
| #17 | 3.4805 m/s | 유속 **3.5 m/s** |
| #24 · #25 | −0.14452 · −0.58593 m³/h | 막대 이름표 **−0.14** · **−0.59 m³/h** |
| #14(각주) · #18 · #19 · #20 · #21 | −0.3710~−0.1642 % · 1,207 · 136 · 320 · 960 | **그대로**(각주는 전체 자릿수 · 나머지는 정수라 반올림 없음) |

**C8 · 식과 코드 문자열을 내렸다(지우지 않았다).** `H = H₀ − aQ − bQ²` ·
`ΔP = K·ρv²/2` · `Q ∝ 1/√K` · `ε·C_min·ΔT_max` · `Q = ṁ·cp·ΔT` · `fsolve` ·
`solve_ivp` · `CoolProp` · `PG25(25 wt% …)` · 「고정축 H22.4 / dPb2 / dPv3」 ·
「NTU 2」 · 「랙별 등가길이 배분 켬/끔」이 전부 12.5 px `.cond` 로 갔다.
케이스 조건의 **말로 된 부분**(단일 CDU · 부하 100 % (640 kW) · 랙 배관 저항 증가
0 %)은 15 px `.when` 으로 본문 크기를 지켰다.

**C9 · s09 막대 그림을 전폭으로.** `.slots > li` 의 글줄 폭 제한(38em)을 이기려면
선택자가 더 좁아야 해서 `.slots > li.wide` 로 썼다(`.wide` 만으로는 특정도가 져서
그림이 608 px 로 눌렸다 — 렌더로 확인하고 고쳤다). **캡션을 그림 바로 밑에** 한 줄
달았다 — 「같은 이상을 두 자리에서 재면 이만큼 다르게 보입니다. 위는 막힌 그
랙에서 잰 것, 아래는 CDU 전체에서 잰 것입니다.」 숫자를 다시 읽어주지 않는다.
막대 이름표도 「랙 08」→「막힌 랙 08」, 「총유량」→「전체 총유량」으로 갈았다.
**막대 좌표·축척·눈금은 하나도 건드리지 않았다**(1500 px/(m³/h) 그대로).

**C10 · 절 단위 등장 효과.** 셋을 전부 지켰다 — ⑴ `section.screen` 에만 걸고
칸에는 걸지 않는다 ⑵ **CSS 기본값이 「보임」이다**: 스크립트가 뜨면 그때
`documentElement` 에 `.reveal-on` 을 붙이고, 감춤 규칙(`opacity:0`)은 전부
`.reveal-on` 아래에만 있다. 스크립트가 막히면 아무것도 감춰지지 않는다
(**실제로 스크립트를 뺀 사본으로 렌더해 확인했다** — 아래) ⑶
`prefers-reduced-motion: reduce` 면 스크립트가 **아예 걸지 않고 그대로 반환**한다
(CSS 로 되돌리는 것이 아니라 시작을 안 한다).

**C11 · 한글 줄바꿈.** `body` 에 `word-break:keep-all` + `overflow-wrap:break-word`.

**C12 · 조형.** 색 여섯 그대로 · 시스템 글꼴(`"Malgun Gothic", system-ui`) 그대로 ·
**외부 CDN·웹폰트 0** · **이모지 0** · 그라데이션·그림자·배경 글로우 0.
장식을 늘리지 않았다 — 늘린 것은 여백과 글자 크기 차이뿐이다.

**검사 4종과 렌더 확인**

빌드 실행값(`.venv/Scripts/python.exe intro/make_intro_html.py`):

- 검사 ⑴ 품은 시연 화면이 `dist/cdu-demo.html` 과 **476,063 자 동일**
- 검사 ⑵ 금지어 3종(누출 · 정확 · 대체) **0회** (iframe 안 제외)
- 검사 ⑶ 화면 **14개** · 「가정값 기반 — 실측 아님」 **14개**
- 검사 ⑷ 화면 수 **14 ∈ [12, 15]**
- 산출물 `dist/cdu-intro.html` **715,140 B (698.4 KB)**

**⚠ 검사 ⑶⑷ 의 뜻이 달라졌다 — 고치지 않고 보고만 한다(빌드 스크립트는 이 판의
범위 밖이다).** 두 검사는 `<section class="screen"` 을 세어 「화면 수」라 부른다.
scroll-snap 이 있던 동안 그 수는 **뷰포트 한 판의 개수**였고 [12,15] 는 「자료가
몇 판인가」를 묶는 뜻이었다. 자유 스크롤이 되면서 같은 수는 이제 **절(section)
개수**를 뜻한다 — 절 하나가 화면 반 판일 수도 두 판일 수도 있다. 통과 여부는
말할 수 있다(14개 · 범위 안). 다만 **⑷ 가 지금 무엇을 지키는 규칙인지는 사람이
다시 정해야 한다** — 분량을 묶으려면 절 개수가 아니라 문서 높이나 글자 수를 재야
한다. 검사 ⑶(절마다 표기 하나)은 뜻이 그대로다.

**브라우저 확인** — 사람의 Chrome 확장이 연결돼 있지 않아 헤드리스 크롬으로
직접 렌더해 보고 판정했다(`--headless=new --window-size=1440,1150`). 본 것:
표지 · s01 · s02 · s03(시연 iframe 이 실제로 그려지는 것까지) · s04 · s05 · s07 ·
s09 · s10 · s11 · s12. 렌더로 잡아 고친 것 둘 — ⓐ s09 막대가 글줄 폭에 눌린 것
(위 C9) ⓑ 시연 iframe 이 글 기둥(1080 px) 안에 갇혀 조작 막대가 접힌 것 →
`#s03` 만 글 기둥 밖으로 내보내고(최대 1400 px) 높이를 `min(86vh,880px)` 로 키웠다.

**아직 못 본 것** — 헤드리스의 virtual time 아래에서는 `IntersectionObserver` 가
첫 배달 뒤로 다시 배달되지 않아, **스크롤해 내려가며 절이 하나씩 떠오르는 것**은
확인하지 못했다. 확인한 것은 ⑴ 스크립트가 도는 판에서 표지 절이 **감췄다가
떠오르는 것**(전이 중간 프레임이 잡혔다)과 ⑵ **스크립트를 뺀 사본에서 14 절이
전부 보이는 것** 둘이다. 즉 **C10 의 안전장치는 확인됐고 효과 자체는 사람이
브라우저에서 봐야 한다.**

**닫기 검사** — pytest **1,207건 전부 통과**(525.57 s) · ruff `All checks passed!` ·
mypy `no issues found in 29 source files`. 이 판은 `src/`·시험을 건드리지
않았으므로 셋 다 무변경 확인용이다. `git status` 는 `intro/intro.html` 하나만
낸다 — `intro/make_intro_html.py`·`demo/`·`docs/`·`src/`·데이터셋 전부 무변경.

**미해결** — 하나도 열거나 닫지 않았다.

ctx: 잔여 토큰 14,781,725 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **헤드리스 크롬으로 화면을 보려는 시도** — 앵커
스크롤과 `IntersectionObserver` 가 헤드리스에서 미덥지 않아 스크린샷 방식을 다섯
번 갈아탔고(virtual time · reduced-motion 강제 · `--blink-settings` · 절 앞을
`display:none` 으로 접기), 그 과정에서 쓸모없는 스크린샷을 여러 장 되받았다 ·
줄일 수 있었다고 보는 것: 같은 곳 — **처음부터 「보고 싶은 절 앞을 접은 사본」**
(스크래치패드 `strip_script.py`)으로 갔으면 시도 넷을 건너뛸 수 있었다. 브라우저
확장이 연결돼 있지 않을 때 이 방법이 가장 짧다는 것을 다음 판이 알아 둔다

### 세션 7.18 · 2026-09-03 · 소개자료 구성 재편 (14 절 → 11 절 · 새 숫자 0개)

**이 판의 전제** — 앞 판의 구성을 지키지 않는다. 사람이 실물을 보고
「디자인·구성·내용 무엇 하나 마음에 들지 않는다 · 14 절 중 5 절만 읽힌다」로
판정했고, 원인을 **문장이 아니라 구성과 조형**으로 지목했다. 그래서 절 구성을
사람이 확정한 11 절로 새로 세웠다. **새 내용을 지어내지 않았다** — 재료는 앞
판의 자료 · `docs/intro-figures.md` · 정본 문서다. 바뀐 파일은
`intro/intro.html` 과 `intro/make_intro_html.py` 둘이다.

**11 절 · 각 절이 쓴 figures 항목**

| # | 제목 | 쓴 figures 항목 |
|---|---|---|
| 표지 | 짓기 전에, 계산으로 먼저 돌려봅니다 | 없음 |
| 01 | 데이터센터가 다뤄본 적 없는 설비입니다 | 없음 (정본 1장 · PG25) |
| 02 | 모르는 조건을 미리 시험해 볼 방법이 없습니다 | 없음 |
| 03 | 우리 목적에 맞게 고쳐 쓸 수 있는 시뮬레이터를 만들 수 있게 됐습니다 | 없음 (정본 3장) |
| 04 | 그래서 이렇게 만들었습니다 | 없음 (다이어그램 · 식 하나) |
| 05 | 직접 움직여 봅니다 (시연) | #19 (136 케이스) |
| 06 | 랙 하나가 막히면 무슨 일이 나는가 | #15 (−0.26 %) · #24·#25 (−0.14 / −0.59 m³/h) |
| 07 | 설계를 어떻게 돕는가 | #16 (60~83 %) · #17 (3.5 m/s) · #20·#21 (320 / 960 행) |
| 08 | 믿을 만한가 | #7·#8 (0.005 %) · #18 (1,207 건) |
| 09 | 아직 못 하는 일 | 없음 |
| 10 | 다음 | 없음 |

**절 제목은 뜻 그대로 쓰되 문장으로 다듬었다.** 프롬프트의 「그래서 이렇게
만들었습니다」·「직접 움직여 봅니다」·「랙 하나가 막히면 무슨 일이 나는가」·
「설계를 어떻게 돕는가」·「믿을 만한가」·「아직 못 하는 일」·「다음」은 그대로
썼고, 표지만 부제를 본문으로 올렸다(제목 「짓기 전에, 계산으로 먼저 돌려봅니다」 ·
그 위 작은 줄이 「AI 데이터센터 냉각 시스템」 — **CDU 를 앞세우지 않았다**).

**C4 — ㄴ(묶음을 없앤다)을 골랐다.** 이유 둘. ⑴ 11 절 제목이 전부 문장이고 그
문장들이 이미 「새로운 설비다 → 시험할 데가 없다 → 이제 만들 수 있다 → 이렇게
만들었다 → 직접 본다 → 이렇게 쓴다 → 믿을 만한가 → 못 하는 일 → 다음」이라는
차례를 읽히게 세운다. 그 위에 「왜 만들었나」·「무엇에 쓰나」를 얹으면 같은 말이
두 번 나온다. ⑵ 묶음을 크게 세우려면(ㄱ) 목차 줄이 절 제목보다 커야 하는데,
그러면 **읽는 눈이 제목이 아니라 분류를 먼저 만난다** — 지금 자료의 문제가
「사실이 먼저 오고 뜻이 나중에 온다」였으므로 같은 실수를 되풀이한다.
순서는 대신 **절 번호(01~10)** 가 진다 — 제목 왼쪽에 큰 회색 숫자로 세웠다.

**C6 다이어그램 — 시연 도형을 떼어 썼다(다시 그린 것이지 복사가 아니다).**
`demo/pfd.html` 을 **읽기만** 하고 그 투영·면 규칙을 그대로 적용해 SVG 를
새로 그렸다: 투영 `sx=(x−y)·cos30` · `sy=(x+y)·sin30−z`, 면은 윗면·앞면 흰색 ·
옆면 `#E0E0E0`(시연의 `--faced`) · 테두리 `--ink` 1.2, 모델 밖은 파선 육면체.
좌표는 그 식으로 다시 계산했다(랙 간격도 시연의 `PITCH/RW` 비를 지켰다).
**떼어 쓰지 못한 것 하나 — 밸브다.** 시연 화면에는 밸브 도형이 없다(CDU 안
그림은 열교환기 · 2차측 · 펌프 · 스트림뿐이다). 그래서 밸브만 표준 심볼
(마주 보는 삼각형 + 스템)로 대신 그렸고, 나머지 넷(펌프 원·삼각 화살촉 ·
굵은 배관선 · 열교환기 육면체 · 랙 육면체)은 시연과 같은 도형이다.
`demo/` 는 한 글자도 고치지 않았다(`git status` 무변경).

**C5 시연 iframe — 렌더로 확인했다. 조작 막대는 겹치지 않는다.**
원본 시연이 1600 px 기준 화면이라 **폭을 줄이지 않고 통째로 축소**하는 쪽을
골랐다: iframe 을 1600 × 920 px 로 두고 스크립트가 `min(1, 칸 폭/1600)` 배로
축소한다(스크립트가 막히면 축척이 걸리지 않고 가로 스크롤로 물러난다 — 잘리지
않는다). 1600 px 창에서 축척은 **0.97** 이고, 그 렌더는 시연을 1600 px 로
따로 띄운 것과 **배치가 같다** — 시나리오 버튼 3 줄 · 축 · 구성/부하/저항 ·
범례가 서로 겹치지 않는다. 테두리(2 px)와 「여기서부터 시연 화면입니다」 한 줄로
별도 화면임을 알린다.

**⚠ 남은 것 하나 — 축 눈금 「50 %」가 두 줄로 접힌다(폭 탓이 아니다).**
시연의 축 이름표는 `#axis u{position:absolute;left:…;transform:translateX(-50%)}`
이고, 마지막 이름표가 `left:100%` 에 놓인다. 절대배치 요소의 가용 폭이 그 자리에서
0 이라 브라우저가 최소폭으로 줄바꿈한다 — **폭과 무관하다.** 실제로 시연을 따로
2000 px 로 띄워도 똑같이 접힌다(확인함). `demo/` 는 읽기 전용이라 손대지 않았고,
**고칠지는 사람이 정한다**(고친다면 `#axis` 오른쪽에 여백을 두거나 마지막
이름표만 `anchor` 를 옮기는 한 줄이다).

**C9 — 지운 말 목록(전문).** 아래는 산출물 본문(iframe 바깥 · HTML 주석 제외)에서
**0 회**임을 세어 확인했다.

| 지운 말 | 바꾼 말 / 처리 |
|---|---|
| 양정 | 「펌프가 만든 압력」 |
| 잔여저항 | 「랙에 닿기 전에 CDU 안쪽과 물을 나눠 보내는 관에서 쓰이는 몫」 |
| 대향류 | **절을 없앴다** (앞 판 s06 역방향 검증) |
| NTU | 케이스 조건에서 삭제 (말로 된 조건만 남겼다) |
| 등가길이 배분 | 케이스 조건에서 삭제 |
| 잔차 | 「들어온 열과 나간 열이 맞는지」 |
| 정상상태 | 「정상일 때」 |
| 신호 방향 | 「실제와 다르게 나올 수 있습니다」 |
| 시간축 | 「시간에 따라 변해 가는 과정」 |
| 전이 파형 | **문장을 통째로 뺐다** (앞 판 s11 의 「배관을 몇 토막으로 나누느냐」) |
| 판별 규칙 | 「어떤 규칙을 세웠을 때 그 규칙이 공연히 울리는지」 |
| 유량분배 | 「물이 랙마다 얼마씩 갈라지는지」 |
| 케이스 집합 | **각주를 통째로 뺐다** (figures #14 각주) |

같이 뺀 것 — 코드 문자열 「고정축 H22.4 / dPb2 / dPv3」·「fsolve」·「solve_ivp」·
「ε-NTU」·「ΔP = K·ρv²/2」·「Q = ṁ·cp·ΔT」. **식은 펌프 특성곡선 하나만** 크게
남기고 기호를 풀어 썼다(C7).

**C8 수치 — 자료에 남은 것은 아홉이다.** #7·#8(0.005 %) · #15(−0.26 %) ·
#16(60~83 %) · #17(3.5 m/s) · #18(1,207 건) · #19(136 케이스) ·
#20·#21(320 / 960 행) · #24·#25(−0.14 / −0.59 m³/h). **뺀 것**: 전체 자릿수
병기(`.exact`) 전부 · #1 총유량 55.8 · #2 랙별 유량 6.71~7.27 · #3 공급 32.2 ℃ ·
#4 출구온도 42.06~42.88 ℃ · #5 환수 42.5 ℃ · #6 640 kW · #9~#11 cp 규칙 세 값 ·
#12·#13 펌프 압력 상대변화 · #14 각주. figures 항목 번호는 **HTML 주석으로만**
남겼다.

**빌드 검사 — ⑷ 를 없앴다(C15).** `intro/make_intro_html.py` 에서 `SCREEN_MIN`·
`SCREEN_MAX` 와 검사 ⑷ 를 지웠다. scroll-snap 이 사라진 뒤로 그 수는 「자료가 몇
판인가」가 아니라 절 개수를 뜻하게 됐고(세션 7.17 보고), 뜻을 잃은 범위는 아무것도
지키지 않으면서 절을 더할 때만 막았다. ⑴⑵⑶ 은 그대로 두었고, ⑶ 의 말만 「화면」
에서 「절」로 고쳤다. **분량은 사람이 읽고 판정한다.**

실행값:

- 검사 ⑴ 품은 시연 화면이 `dist/cdu-demo.html` 과 **476,063 자 동일**
- 검사 ⑵ 금지어 3종(누출 · 정확 · 대체) **0회** (iframe 안 제외)
- 검사 ⑶ 절 **11개** · 「가정값 기반 — 실측 아님」 **11개**
- 산출물 `dist/cdu-intro.html` **728,559 B (711.5 KB)**

**C13 렌더 확인 — 헤드리스 크롬(`--headless=new`)으로 봤다.**
본 것: 표지 · 01 · 02 · 03 · 04(다이어그램 · 창 1600×1400) · 05(시연) · 06(막대) ·
07 · 08 · 09 · 10 — **11 절 전부**. 시연은 따로 1600 px·2000 px 로도 띄워
자료 안 렌더와 대조했다. 렌더로 잡아 고친 것 둘:
ⓐ 4 절 다이어그램의 2 층 이름표가 1→2 화살표에 깔렸다 → 이름표를 화살표 위
줄로 올리고 2 층 이하를 10 px 씩 내렸다.
ⓑ 6 절 막대의 왼쪽 이름표가 viewBox 밖으로 잘려 「막힌」이 「힌」으로 나왔다 →
0 점을 x=1160→1210 으로 밀고 viewBox 폭을 1200→1260 으로 넓혔다(축척 불변 ·
1500 단위 = 1 m³/h).

**확인 못 한 것** — ⑴ **사람의 브라우저에서 절이 하나씩 떠오르는 것.**
헤드리스의 virtual time 아래에서는 `IntersectionObserver` 가 첫 배달 뒤로 다시
배달되지 않는다(세션 7.17 과 같은 한계). 확인한 것은 **안전장치 쪽**이다 —
`--force-prefers-reduced-motion` 으로 띄우면 스크립트가 `.reveal-on` 을 붙이지
않고 절이 처음부터 보인다(C11 의 「기본은 보임」과 같은 경로). ⑵ **시연을 실제로
조작했을 때**(부하 축을 끝까지 끌었을 때 등)의 눈금 — 스크린샷은 초기 상태 하나다.

**닫기 검사** — ruff `All checks passed!` · mypy `no issues found in 29 source
files` · pytest **1,207건 전부 통과**. 이 판은 `src/`·시험을 건드리지 않았으므로
셋 다 무변경 확인용이다. `git status` 는 `intro/intro.html` ·
`intro/make_intro_html.py` · `PROCEED.md` 셋만 낸다 — `demo/`·`docs/`·`src/`·
데이터셋 전부 무변경.

**보고만 하고 고치지 않은 것 — `docs/intro-outline.md` 와 어긋난 곳.**
이 판의 구성이 outline 을 앞선다(사람이 그렇게 정했다). outline 은 **읽기만**
했다. 어긋난 곳:

1. outline §1 은 **6장 구성**을 적는다. 이 판은 **11 절**이고 장 묶음이 없다.
2. outline §4(3장 「이 숫자는 어떻게 나왔나」)가 이 자료의 **핵심**이라 적고
   3-1~3-4 네 소절을 둔다. 이 판은 그 다섯 절을 **한 절(04)** 로 접고, 대신
   장치별 수치 대신 **다이어그램 하나 + 식 하나**로 바꿨다.
3. outline §4 3-2 ③ 「역방향 검증 — 표가 맞는지를 계산이 확인했다」는 이 판에서
   **절을 없앴다**(만든 사람에게만 의미 있는 이야기라는 판정).
4. outline §9 는 Aspen 대비를 **별도 절**로 두고 대비표를 권한다. 이 판은
   Aspen 을 03 절에서 **배경으로 한 번** 스치고, 「상용 도구가 잘하는 것」은
   09 절(아직 못 하는 일) 안에 넣었다 — 대비표를 만들지 않았다.
5. outline §10 「화면 구조」는 scroll-snap 한 화면 5 줄을 전제한다. 세션 7.17 이
   scroll-snap 을 걷어냈고 이 판은 2 단 자유 스크롤이다.
6. outline §5 ⑵ 의 펌프 압력 상대변화(figures #12·#13)를 이 판은 **뺐다**
   — 논증을 지지 않는다는 판정(C8).

**미해결** — 하나도 열거나 닫지 않았다.

ctx: 잔여 토큰 14,768,473 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **소개자료 HTML 전문을 한 번에 쓴 것** — 11 절을
새로 짜느라 파일 하나(약 900 줄)를 통째로 실었고, 그 뒤 렌더 확인용 스크린샷
9 장을 되받았다 · 줄일 수 있었다고 보는 것: **스크린샷 되받기** — 창 하나에
절 둘씩 담기게 접은 사본을 만들면 9 장이 5 장으로 줄었다. 앞 판(7.17)이 남긴
「절 앞을 접은 사본」 방법은 이 판에서 그대로 통했다(시도 한 번에 렌더가 됐다)

### 세션 7.19 · 2026-09-03 · 소개자료 사람 지적 반영 (11 절 → 10 절 · 새 숫자 0개)

**이 판의 전제** — **다시 짜는 판이 아니라 고치는 판이다.** 구성은 세션 7.18 이
세운 것을 그대로 두고, 사람이 실물을 처음부터 읽고 낸 지적 열넷(C1~C14)만
반영했다. 바뀐 파일은 `intro/intro.html` 과 `intro/make_intro_html.py` 둘이다.
`demo/`·`docs/`·`src/`·데이터셋·정본 문서 넷은 열지 않았다.

**10 절 · 번호와 제목**

| # | 제목 |
|---|---|
| 표지 | AI 데이터센터 냉각 시스템 시뮬레이션 (부제: 서버를 액체로 식히는 설비의 디지털 트윈화) |
| 01 | 데이터센터가 다뤄본 적 없는 설비입니다 |
| 02 | 모르는 조건을 미리 시험해 볼 방법이 없습니다 |
| 03 | 우리 목적에 맞게 고쳐 쓸 수 있는 시뮬레이터를 만들 수 있게 됐습니다 |
| 04 | 그래서 이렇게 만들었습니다 |
| 05 | 직접 움직여 봅니다 |
| 06 | 랙 하나가 막히면 무슨 일이 나는가 |
| 07 | 설계를 어떻게 돕는가 |
| 08 | 믿을 만한가 |
| 09 | 어디까지 할 수 있고, 다음은 무엇인가 |

**C3 어휘 교체 — 바꾼 자리 수**

| 옛말 | 새말 | 바꾼 자리 |
|---|---|---|
| 펌프가 내는 압력 | 펌프 압력 | 1 (04 절 상자) |
| 밸브가 죄는 정도 | 밸브 개도율 | 1 (04 절 상자) |
| 배관이 막는 정도 | 배관 차압 | 1 (04 절 상자) |
| 열 주고받기 | 열 교환 | 1 (04 절 상자) |
| 랙이 내는 열 | 랙 발열 | 2 (04 절 상자 · 05 절 시연 안내) |
| 냉각액 | 냉매 | 6 (01·03 절 본문 · 04 절 그림 대체텍스트·밑층 상자·설명줄·본문 · 08 절 본문) |

계 **12 자리.** 옛말 6 종은 지금 자료에 **0 회**다. 「냉각액」이 있던 자리 하나는
C9 ⑶ 로 문단째 사라져 교체 대상이 아니었다(7 회 중 6 회 교체 · 1 회 삭제).
04 절 아래 큰 식의 기호 풀이 「H 펌프가 만드는 압력」은 C4 가 「그대로 둔다」로
정해 손대지 않았다.

**C4 — 다섯 식과 그 출처.** 다섯 다 넣었고 **지어낸 것은 없다.**

| 상자 | 식 | 어디에 있는가 |
|---|---|---|
| 펌프 압력 | H = H₀ − aQ − bQ² | 정본 5 장 「주요 구성요소 · 펌프」 |
| 밸브 개도율 | Kv(x) = Kv,max · x | 5-1 「밸브 개도 특성」 |
| 배관 차압 | ΔP = K·ρv²/2 | 5-1 「배관 K값 (랙 분기)」 |
| 열 교환 | Q = ε·C_min·ΔT_max | 5 장 「열교환기 · ε-NTU법」 (코드: `dynamics.hx_capacity_terms`) |
| 랙 발열 | Q = ṁ·cp·ΔT | 5-1 「cp·ρ 평가 온도」가 전제하는 현열 상승식 (코드: `model.py` 256행) |

**5-1 소절 안에서 글자 그대로 찾히는 것은 밸브·배관 둘이다.** 열교환기는 5 장
표가 ε-NTU 로 지정하고 코드가 그 식을 쓰며, 랙은 5-1 이 「흡열량」을 말할 때
전제하는 식이고 코드가 그대로 쓴다. 펌프는 5 장 표에 식이 그대로 있다.
**멈추는 조건(「5-1 에서 찾을 수 없는 것」)에는 걸리지 않는다고 판단했다** —
셋 다 정본 5 장·5-1 과 코드에 근거가 있고 새로 만든 식이 하나도 없다.
다만 「5-1 에만 있는 것」으로 좁게 읽으면 열교환기·랙 둘이 5 장 표·코드에서
왔으므로 **여기 밝혀 둔다.**

**C10 표 — 실제로 들어간 칸 전문**

| | 지금 할 수 있는 일 | 설계도서·실측이 들어오면 |
|---|---|---|
| 설계 | 계기를 어디에 몇 개 달지 미리 본다 · 관 굵기를 바꿔 보고 결과를 본다 · 랙을 늘렸을 때 가장 먼 랙에 물이 가는지 본다 | 펌프 용량을 정한다 · 헤더 굵기를 확정한다 · 여유율을 검토한다 |
| 시운전 | (빈칸) | 랙마다 물 맞추기를 현장 시행착오 대신 모델이 먼저 낸다 · 돌려보지 못한 조건을 채운다 |
| 운영 | (빈칸) | 계기를 못 다는 자리의 값을 계산으로 세운다 · 모델과 실제가 벌어지는지 지켜본다 |
| AI 데이터 | 여러 조건을 한 번에 돌려 표로 낸다 | 물리에 맞는 시나리오 데이터를 쌓아 학습에 쓴다 · 언제부터 이상했는지를 시간으로 다룬다 |

빈칸은 **배경 없는 칸 + 짧은 가로줄**로 그렸다(`.can td.none`). 옛 09 절의
머리글 둘과 「상용 도구가 잘하는 것」 문단은 지시대로 **삭제**했고, 이 절에
문단을 하나도 붙이지 않았다 — 표가 전부다.

**C12 — 되돌리는 법 한 줄.** `intro/intro.html` 의 `:root` 블록에서 「어두운 한
벌」 값을 그 아래 주석 「밝은 한 벌」의 값으로 바꾸면 끝이다(다른 곳은 손대지
않는다). SVG 도형도 전부 같은 변수를 쓰므로 함께 따라온다 — 하드코딩된 색값이
자료에 **0 개**이고, 색값이 적힌 곳은 `:root` 와 `@media print` 두 블록뿐이다.
막대 위에 얹히는 글자만 `--on-warn`·`--on-cold` 로 따로 두었다(막대 색은 한 벌이
바뀌어도 밝기가 크게 안 변해 `--ink` 를 따라가면 안 읽힌다).

**C13 — 검사 ⑶.** 「절 수 = 표기 수」 → **「표기가 자료에 최소 한 번 있다」**.
바꾼 이유를 `verify()` docstring 에 적었다: 지켜야 할 것은 절대 규칙 11 이
요구하는 「산출물에 표시가 있다」이고 자리를 표지로 정하는 것은 사람의 판단이라
검사가 개수를 세지 않는다. ⑴⑵ 는 손대지 않았다.

**C14 — 렌더 확인.** 헤드리스 크롬(창 1600 px)으로 봤다. 등장 효과가 헤드리스
스크린샷에서 절을 `opacity:0` 으로 남기고 파일 URL 의 `#조각` 이동도 듣지
않아, **절 하나만 남긴 확인용 사본**을 스크래치패드에 만들어 찍었다
(`dist/` 산출물과 `intro/` 원본은 건드리지 않았다).

| 볼 것 | 결과 |
|---|---|
| ⑴ 표지 — 제목이 부제보다 큰가 | **확인** — 제목 clamp(40,5.8vw,74)px · 부제 clamp(20,2.0vw,26)px |
| ⑵ 04 절 다섯 상자의 식 | **확인** — 다섯 다 상자 안에 들어가고 넘치거나 겹치지 않는다 |
| ⑶ C10 표의 빈칸 | **확인** — 시운전·운영 왼쪽이 배경 없이 비어 보인다 |
| ⑷ 어두운 바탕의 본문 · 시연 iframe | **확인** — 본문 읽히고, 시연은 밝은 원본 그대로 테두리 안에 들어가며 겹치지 않는다 |
| (추가) 인쇄 한 벌 | **확인** — 04·06·09 절을 `@media print` 를 화면에 적용한 사본으로 봤다. 흰 바탕·검은 글자로 나오고 막대 위 글자도 읽힌다 |

**확인 못 한 것** — ① **사람의 실제 브라우저**에서 등장 효과가 도는 모습(헤드리스
확인용 사본에서는 껐다) · ② **실제 인쇄물**(프린터 출력). 화면에서 인쇄 한 벌을
적용해 봤을 뿐 종이로 뽑지 않았다 · ③ 07 절(설계)·01·02·03 절은 **어두운 한 벌
전체 렌더를 보지 않았다** — 01·08·09 절과 같은 조판이라 새로 볼 것이 없다고
판단했고, 01·08·09 는 봤다.

**남긴 자리 둘 (고치지 않았다)** — ① `<title>` 과 시연 iframe 의 `title` 속성에
「가정값 기반 · 실측 아님」(가운뎃점)이 남아 있다. C2 가 뺀 것은 **절마다 반복되던
표시 문구**이고 이 둘은 브라우저 탭 이름과 화면 낭독용 이름이라 그대로 두었다.
② `docs/intro-outline.md` 와 어긋난 곳은 세션 7.18 이 적은 여섯에 **하나가
늘었다** — outline 은 「아직 못 하는 일」과 「다음」을 **두 절**로 두는데 이 판이
하나로 합쳤다. **목록으로만 보고하고 outline 을 고치지 않았다**(절대 규칙 13).

**미해결** — 하나도 열거나 닫지 않았다.

ctx: 잔여 토큰 14,752,000 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **`intro/intro.html` 전문 읽기 + 렌더 확인
스크린샷 7 장** — 826 줄짜리 파일을 한 번 통째로 읽고 시작했고, 어두운 한 벌과
인쇄 한 벌을 따로 확인하느라 큰 그림(04 절 2150 px 두 장)을 두 번 되받았다 ·
줄일 수 있었다고 보는 것: **인쇄 한 벌 확인을 절 셋이 아니라 하나로** — 색
변수 한 벌만 갈리는 것이라 04 절 하나로 충분했고 06·09 절 두 장은 같은 것을
다시 본 셈이다

### 세션 7.20 · 2026-09-03 · 소개자료의 말 낮추기 (구성 무변경 · 새 숫자 0개)

**이 판의 전제** — **말을 낮추는 판이다.** 구성(10 절)은 세션 7.18~7.19 가
세운 것을 그대로 둔다. 사람의 판정: 자료의 **09 절 표와 06·07 절이 실제보다
크게 적혀 있다.** 지금 있는 것은 가정치로 도는 파일럿 코드와 136 케이스 시연
화면뿐인데, 자료는 「관 굵기를 바꿔 보고 결과를 본다」·「랙을 늘렸을 때 가장 먼
랙에 냉매가 가는지 본다」처럼 **지금 못 하는 일을 할 수 있는 것처럼** 적었다.
실제로는 관 굵기도 랙 수도 가정치에 고정돼 있다. **이 자료의 목적은 설득이
아니라 「무엇이 되어 있고 무엇이 안 되어 있는지를 알리고 앞으로의 개발에
의견을 받는 것」이다** — 겸손해도 되고 사실 그대로여야 한다.
바뀐 파일은 `intro/intro.html` **하나**다.

**C1·C2 — 09 절 표 (실제로 들어간 칸 내용 전문)**

머리줄: `단계` · `필요한 것` · `그러면 할 수 있는 일`

| 단계 | 필요한 것 | 그러면 할 수 있는 일 |
|---|---|---|
| 지금까지 한 것 | — | 장치별 계산식을 코드로 세우고, 물려서 계통이 돌아가는 것을 확인 · 여러 조건을 한 번에 돌려 정상·이상을 한 표로 냄 · 전체 계기만으로는 안 보이는 차이가 있음을 확인 |
| 다음 | 추가 개발 (설계데이터 없이도 됩니다) | 이상 상태를 넓히고, 언제부터 이상했는지를 알 수 있다 · 관 굵기·랙 수를 조건으로 바꿔 가며 돌릴 수 있게 만든다 |
| 설계데이터가 오면 | 펌프·밸브·열교환기 사양, 랙별 발열량 | 계기를 어디에 몇 개 설치할지 · 관 굵기 · 펌프 용량 · 여유율을 실제 값으로 검토 · 랙마다 냉매가 고르게 가는지 시공 전에 본다 |
| 실측이 오면 | 시운전·운영 실측 | 계기를 설치하지 못하는 자리의 값을 계산으로 세운다 · 모델과 실제가 벌어지는지 지켜본다 · 데이터를 쌓아 학습에 쓴다 |

- 첫 줄의 이름은 「할 수 있는 일」이 아니라 **「한 것」**이다(지시대로).
- **「필요한 것」 칸을 지어내지 않았다** — 넷 다 이미 이 문서와 정본에 있는
  말이다. 「지금까지 한 것」의 칸만 `—` 다(이미 한 것이라 필요한 것이 없다).
- 옛 표의 **빈칸(`.none`) 처리는 없앴다** — 빈 칸이 한 곳도 남지 않는다.
  `@media print` 의 `.can td:not(.none)` 도 `.can td` 로 되돌렸다.
- 절 제목도 시제를 맞췄다: 「어디까지 **할 수 있고**」 → 「어디까지 **했고**」.
- 표 앞뒤 문단(C2) — 앞: 「여기까지가 가정치로 돌아가는 계산 코드와 앞 화면의
  시연입니다. 아래 표는 **무엇이 있어야 무엇을 할 수 있게 되는지**를 단계별로
  적은 것입니다.」 / 뒤: 「**무엇을 더 해야 하는지에 대한 의견을 기다립니다.**」

**C3 — 06 절에서 낮춘 문장 (전 → 후)**

1. 마무리(`.key.rule`)
   - 전: 「전체만 봐서는 보이지 않는 변화가 있습니다. 그 차이를 **미리 볼 수
     있습니다.**」
   - 후: 「한 자리를 이렇게 비교해 봤습니다. 같은 이상인데도 재는 자리에 따라
     **이만큼 다르게 나왔습니다.**」
2. 오른쪽 머리 문장
   - 전: 「그런데 전체 유량은 거의 움직이지 않습니다.」
   - 후: 「그 조건을 넣고 돌려 보니, 전체 유량은 거의 움직이지 않았습니다.」
3. 그 아래 문단
   - 전: 「전체 유량만 재면, 랙 하나가 막힌 일은 0.3 % 도 나타나지 않습니다.
     **못 잡습니다.**」
   - 후: 「이 조건에서는 전체 유량에 0.3 % 도 나타나지 않았습니다.」
4. 그림 설명
   - 전: 「같은 이상을 두 자리에서 재면 이만큼 다르게 보입니다. 막힌 그 랙에서
     재면 **약 4 배** 크게 나타납니다.」
   - 후: 「같은 이상을 두 자리에서 계산해 봤더니 이만큼 다르게 나왔습니다.
     막힌 그 랙에서 잰 쪽이 **약 4 배** 컸습니다.」

**C4 — 07 절에서 낮춘 문장 (전 → 후) · 제목 포함**

- **제목**: 「설계를 어떻게 돕는가」 → **「돌려 보니 이런 것들이 나왔습니다」**
  (지시가 「기능 선언으로 읽히면 바꾼다」고 했고, 「돕는다」가 기능 선언으로
  읽혀 바꿨다)
- 항목 ①
  - 전: 「물이 랙마다 얼마씩 갈라지는지를 무엇이 정하는가」 / 「… 이미
    쓰입니다. … 그 나머지뿐입니다.」
  - 후: 「냉매가 랙마다 얼마씩 갈라지는지를 무엇이 정하는지 **따져 봤습니다**」 /
    「… 이미 **쓰였습니다**. … 그 **나머지뿐이었습니다**.」
- 항목 ②
  - 전: 「배관 굵기를 정하기 전에 물이 얼마나 빠르게 지나가는지」 / 「굵기를 한
    단계 바꾸면 저항과 열이 함께 움직입니다. **도면을 확정하기 전에 그 결과를
    먼저 봅니다.**」
  - 후: 「**한 조건에서** 냉매가 배관을 얼마나 빠르게 지나가는지 **계산해
    봤습니다**」 / 「**통상 설계 범위보다 빨랐습니다 — 굵기를 다시 볼
    자리입니다.**」
- 항목 ③ — **절에서 뺐다.** 「랙을 늘렸을 때 가장 먼 랙이 어떻게 되는지 /
  랙 수와 배치를 바꿔 가며 돌려, 가장 불리한 자리의 랙에 물이 충분히 가는지를
  시공 전에 봅니다」는 **지금 못 한다**(랙 8 개가 가정치 고정). 09 절 표의
  **「다음」 줄**로 보냈다.
- 항목 ④
  - 전: 「정상일 때와 이상일 때의 데이터가 표로 나옵니다」 / 「… 한 표에서
    봅니다.」 + 「정상 320 행 · 이상 960 행」
  - 후: 「정상일 때와 이상일 때가 한 표에 나왔습니다」 / 「… 한 표에
    담았습니다.」 (수치 삭제 — C5)
- 마무리(`.t.rule`) 「가정값 기반이라 운영을 판단하는 근거로는 아직 쓰지
  못합니다」는 **그대로 뒀다** — 이미 낮은 말이다.

**C5 — 뺀 숫자 셋 · 남긴 것**

- 뺐다: 「정상 **320** 행 · 이상 **960** 행」(07 절) · 「검사 **1,207** 건 전부
  통과」(08 절, 문장과 `.fig` 둘 다). 08 절 문장은 「고칠 때마다 자동으로 다시
  확인하는 **검사**를 두었고, 지금 전부 통과합니다」로 고쳤다(「검증 장치」도
  「검사」로).
- 남겼다: **0.005 %/99.995 %**(무엇을 재는지 문장이 밝힌다) · **136 케이스**
  (시연 화면이 실제로 그만큼이다).
- `docs/intro-figures.md` 의 #18(1,207 건) · #20(320 행) · #21(960 행)은
  **표에 그대로 있다** — 이 판은 표를 열지 않았고 자료에서만 뺐다.

**C6 — 「물」 → 「냉매」**

**낱말만 갈아 끼운 자리 8** — 04 절 셋(식 풀이 「흐르는 **물**의 양」·「**물**을
안 흘릴 때의 압력」 + 본문 「**물**이 여덟 갈래로 갈라지는데」) · 05 절 하나
(시연 안내 「**물**을 나눠 보내는 관」) · 07 절 셋(항목 ① 제목·본문, 항목 ②
제목) · 08 절 하나(「**물**의 흐름까지 묶은 32 조합」).

**문장이 통째로 갈리며 함께 사라진 자리 3** — 07 절 항목 ③ 「가장 불리한
자리의 랙에 **물**이 충분히 가는지」(항목을 뺐다) · 09 절 표 「가장 먼 랙에
**물**이 가는지」·「랙마다 **물** 맞추기」(표를 새로 짰다).
자료에 「물」이 냉매를 뜻하는 자리는 **하나도 남지 않았다.**

**그대로 둔 자리 4** — 진짜 물을 뜻한다: 01 절 「그 액체는 **물**이 아닙니다」 ·
「**물**에 프로필렌글리콜을 25 % 섞은 것」 · 「**물**과 공기에는 쌓인 경험이
있지만」 · 03 절 「다루는 액체가 **물**이나 공기가 아니면」.
(「물리」·「물어볼」·「실물」·「물려」·「물성」·「물러난다」는 다른 낱말이라
세지 않았다.)

**C7 — 말 다듬기 5**

「몇 개 달지」→「몇 개 **설치할지**」 · 「계기를 못 다는 자리」→「계기를
**설치하지 못하는** 자리」 · 「랙마다 물 맞추기를 현장 시행착오 대신 모델이
먼저 낸다」→「랙마다 **냉매가 고르게 가는지 시공 전에 본다**」 · 「**물리에
맞는** 시나리오 데이터」→「데이터」 · 「언제부터 이상했는지를 **시간으로
다룬다**」→「언제부터 이상했는지를 **알 수 있다**」.

**C8 — 렌더 확인 (헤드리스 크롬 `--headless=new`)**

세션 7.19 가 남긴 권고대로 **처음부터 「보고 싶은 절 앞을 접은 사본」**으로
갔다(스크래치패드 `peek.py` — 등장 효과를 `!important` 로 무력화하고 앞 절을
`display:none` 으로 접는다). 시도 한 번에 렌더가 됐고 쓸모없는 스크린샷은
**한 장**(앵커 `#s10` 만으로 찍어 본 첫 장)이다.

- **검사 ⑴ 09 절 4 단 표가 넘치거나 뭉개지지 않는가** — **통과.** 1440 px 와
  900 px 두 폭에서 세 칸이 다 서고 가로 넘침이 없다. **다만 첫 렌더에서
  단계 칸이 15 % 가 아니라 3 분의 1 을 먹었다** — `table-layout:fixed` 는
  **첫 줄(thead)의 너비만** 보는데 너비를 `tbody th`·`td.need` 에 줬기
  때문이다. **`<colgroup>`(`.c-step` 15 % · `.c-need` 27 %)으로 옮겨** 고쳤고
  좁은 화면(≤1180 px)은 19 %/28 % 로 둔다.
- **검사 ⑵ 06·07 절이 말을 낮춘 뒤에도 문단이 자연스러운가** — **통과.**
  07 절은 항목이 넷에서 셋으로 줄어 2 단 격자에서 셋째가 왼쪽 칸에 홀로
  서지만, 그 아래 전폭 마무리가 받아 준다.
- **검사 ⑶ 어두운 바탕에서 표가 읽히는가** — **통과.** 칸 배경 `--face`,
  글자 `--ink`, 「—」만 `--dim`.
- **덤으로 인쇄 한 벌도 봤다** — 사본에서 `@media print` → `@media all` 로
  끌어내 화면 렌더했다. 흰 바탕·검은 글자에 네 줄 전부 테두리가 선다.

**확인 못 한 것 둘** — ⑴ **실제 종이/PDF 렌더.** `--print-to-pdf` 로 PDF 는
나왔으나 이 환경에 PDF 를 그려 볼 도구가 없다(`pdftoppm` 미설치). 본 것은
위의 「인쇄 한 벌을 화면으로 끌어낸 것」이다. ⑵ **사람의 브라우저에서 절이
하나씩 떠오르는 것** — 헤드리스의 virtual time 아래에서 `IntersectionObserver`
가 첫 배달 뒤로 다시 배달되지 않는다(앞 판들과 같은 한계).

**빌드 검사 실행값 · 산출물 크기**

- 검사 ⑴ 품은 시연 화면이 `dist/cdu-demo.html` 과 **476,063 자 동일**
- 검사 ⑵ 금지어 3종(누출 · 정확 · 대체) **0회** (iframe 안 제외)
- 검사 ⑶ 「가정값 기반 — 실측 아님」 **2회**(최소 1) · 절 **10 개**
  — 보이는 것은 표지 하나, 나머지 하나는 머리 주석이다
- 산출물 `dist/cdu-intro.html` **732,284 B (715.1 KB)**

**닫기 검사** — pytest **1,207건 전부 통과**(558.66 s) · ruff `All checks
passed!` · mypy `no issues found in 29 source files`. 이 판은 `src/`·시험을
건드리지 않았으므로 셋 다
무변경 확인용이다. `git status` 는 `intro/intro.html` 과 `PROCEED.md` 둘만
낸다 — `intro/make_intro_html.py`·`demo/`·`docs/`·`src/`·데이터셋 무변경.

**미해결** — 하나도 열거나 닫지 않았다.

**정본 문서 4종 무수정 — 프로젝트 지식 재업로드 필요 없음.**

ctx: 잔여 토큰 14,834,788 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **`intro/intro.html` 전문 읽기(863 줄)** — 고칠
자리가 여섯 절에 흩어져 있어 통째로 읽고 시작했다 · 줄일 수 있었다고 보는 것:
**렌더 스크린샷 되받기 6 장** — 앞 판의 권고대로 「절 앞을 접은 사본」으로 가서
쓸모없는 장은 하나뿐이었으나, 09 절을 세 번(초판 · 너비 고침 뒤 · 좁은 화면)
되받았다. 너비를 처음부터 `<colgroup>` 에 줬으면 한 장이 줄었다

### 세션 7.21 · 2026-09-03 · 09 절을 표에서 가로 박스 셋으로 (배치 판 · 새 글 0줄)

**이 판이 한 일** — 직전 판(7.20)이 09 절에 넣은 **글은 한 글자도 고치지 않고**,
그 글을 담는 **틀만** 바꿨다. 사람의 판정: 「단계가 행이 아니라 **열(가로
박스)**이고, 박스 안에 「필요한 것 / 그러면」이 세로로 쌓이는 모양」이다.

**C1 — 형식의 본을 git 이력에서 찾았다**

세션 7.17 판(`54e2802`)의 s12(「데이터가 들어오면 설계와 시운전까지 갑니다」)를
꺼내 봤다. `ul.slots.cols` 로 단계 셋이 가로로 서고, 박스마다 「무엇을 하나 /
필요한 것 — / 얻는 것 —」이 세로로 쌓여 있었다. CSS 는 세 줄이었다 —
`.cols{flex-direction:row;gap:18px}` · `.cols > li{flex:1;max-width:none;
background:var(--face);border-radius:4px;padding:22px 24px;gap:9px}` ·
좁은 화면에서 `flex-direction:column`.

**클래스 이름을 그대로 옮기지는 않았다.** 지금 자료에는 `.slots` 가 없다(세션
7.18 이 구성을 다시 짜면서 사라졌다). 형식만 가져와 이 자료의 이름으로 세웠다 —
`div.done`(전폭 한 줄) · `ul.steps > li`(가로 박스) · `.step-name` · `.need`.

**들어간 구조**

```
div.done      [지금까지 한 것]              ← 「필요한 것」 층 없음
ul.steps
  li          [다음]              / 필요한 것 — / 그러면 —
  li          [설계데이터가 오면]  / 필요한 것 — / 그러면 —
  li          [실측이 오면]        / 필요한 것 — / 그러면 —
```

글은 직전 판 표의 칸 넷을 **그대로** 옮겨 담았다. 전폭 한 줄에는 표 첫 줄의
「그러면 할 수 있는 일」 칸 내용이 들어갔고, **「필요한 것」 칸(「—」)은 층째
없앴다** — 이미 한 것이라 필요한 것이 없다.

「필요한 것」이 이 절의 요점이므로(그 아래 「그러면」이 왜 아직 안 되는지가
거기서 보인다) `.need` 를 **강조 색 `--warn`** 으로 세웠다. 흐리게 묻지 않는다.

**C2·C3 — 앞뒤 문단과 제목**

절 제목(「어디까지 했고, 다음은 무엇인가」)과 앞뒤 문단은 그대로다. 앞 문단의
「아래 **표**는」만 「**아래는**」으로 고쳤다 — 더는 표가 아니다.

**C4 — 렌더 확인 (헤드리스 크롬 `--headless=new` · 「절 앞을 접은 사본」)**

- **박스 셋이 좁아 뭉개지지 않는가** — **통과.** 1440 px 에서 셋이 3 등분으로
  서고 `align-items:stretch` 로 높이가 같다. 가로 넘침 0. 900 px 에서는
  `@media (max-width:1180px)` 가 세로로 쌓는다(뭉개는 대신 쌓는다).
- **전폭 한 줄과 박스 셋의 층이 구분돼 보이는가** — **1440 px 통과 · 900 px 는
  고쳤다.** 세로로 쌓으니 전폭 한 줄이 넷째 박스처럼 보였다 → 좁은 화면에서
  `.steps` 위 여백을 26 px 로 벌렸다(박스 사이 간격은 12 px).
- **어두운 바탕에서 읽히는가** — **통과.** 상자 `--face`, 이름 `--ink` 굵게,
  「필요한 것」 `--warn`, 「그러면」 `--ink`. 세 층이 색과 굵기로 갈린다.
- **인쇄 한 벌에서도 서는가** — **선다. 다만 걸리는 것 하나.** 사본에서
  `@media print` → `@media all` 로 끌어내 화면 렌더했다. 흰 바탕에 네 상자
  전부 테두리가 서고(`.card,.done,.steps > li`), 글도 읽힌다. **그런데 인쇄
  한 벌의 `--warn:#d99425` 이 흰 바탕에서 본문(`#000`)보다 옅어, 「필요한 것」이
  화면만큼 서지 않는다.** 색 한 벌(`:root`·`@media print`)은 09 절 밖이라
  **고치지 않고 보고만 한다**(절대 규칙 13).

**확인 못 한 것 둘** — ⑴ **실제 종이/PDF 렌더.** 이 환경에 PDF 를 그려 볼
도구가 없다(`pdftoppm` 미설치 — 앞 판과 같다). 본 것은 위의 「인쇄 한 벌을
화면으로 끌어낸 것」이다. ⑵ **사람의 브라우저에서 절이 하나씩 떠오르는 것** —
헤드리스의 virtual time 아래에서 `IntersectionObserver` 가 첫 배달 뒤로 다시
배달되지 않는다(앞 판들과 같은 한계).

**빌드 검사 실행값 · 산출물 크기**

- 검사 ⑴ 품은 시연 화면이 `dist/cdu-demo.html` 과 **476,063 자 동일**
- 검사 ⑵ 금지어 3종(누출 · 정확 · 대체) **0회** (iframe 안 제외)
- 검사 ⑶ 「가정값 기반 — 실측 아님」 **2회**(최소 1) · 절 **10 개**
- 산출물 `dist/cdu-intro.html` **732,736 B (715.6 KB)**

**닫기 검사** — pytest **1,207건 전부 통과**(506.41 s) · ruff `All checks
passed!` · mypy `no issues found in 29 source files`. 이 판은 `src/`·시험을
건드리지 않았으므로 셋 다 무변경 확인용이다. `git status` 는
`intro/intro.html` 과 `PROCEED.md` 둘만 낸다 —
`intro/make_intro_html.py`·`demo/`·`docs/`·`src/`·데이터셋 무변경.

**미해결** — 하나도 열거나 닫지 않았다.

**정본 문서 4종 무수정 — 프로젝트 지식 재업로드 필요 없음.**

ctx: 잔여 토큰 14,997,744 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **pytest 를 백그라운드로 돌려 놓고 완료를
폴링한 것** — `tail` 한 번씩을 마흔 번 넘게 되받으며 「기다립니다」만 냈다.
앞에서 한 번 돌렸으면(506 s) 한 번의 왕복이었다 · 줄일 수 있었다고 보는 것:
**같은 것.** 렌더는 앞 판 권고대로 「절 앞을 접은 사본」으로 가서 스크린샷
되받기는 넉 장(1440 · 900 · 인쇄 · 좁은 화면 고침 뒤)에 그쳤고 쓸모없는 장은
없었다

---

### 세션 7.22 · 2026-09-03 · 오늘(7.13~7.21)을 닫는 기록 판 (코드·자료 무수정)

**이 판이 한 일** — 새 기능을 만들지 않았다. 오늘 아홉 판이 `PROCEED.md` 에
한 자리로 정리되어, **내일 다른 PC 에서 그대로 이어받을 수 있는 상태**가 됐다.
바꾼 파일은 `PROCEED.md` 하나다.

**C1 — 커밋 안 된 파일 하나. 사람의 판정을 받았다**

`git status` 가 추적되지 않은 `start-cdu.txt`(127 B) 하나를 냈다. 내용은 이 PC
전용 실행 메모다 — `cd <절대경로>` · `.venv\Scripts\activate` · `git pull` ·
`python daily_brief.py` · `claude --remote-control "CDU"`. 절대경로가 들어 있어
PC 마다 달라진다(절대 규칙 15 는 코드 대상이지만 성격이 같다).
**임의로 커밋하지 않고 멈춰 물었고, 사람이 「그대로 둔다」로 정했다** —
커밋하지 않았고 `.gitignore` 에도 넣지 않았다. 다음 판의 `git status` 에도
계속 뜬다(뜨는 것이 정상이다).

원격과의 어긋남은 없었다 — `git pull` **Already up to date** ·
`## main...origin/main`(ahead/behind 표시 없음).

**오늘의 커밋 — 열넷 (7.13~7.21)**

```
718e0e7 기록: 세션 7.21 — 09 절 배치 재편 (글 무수정 · 새 숫자 0개)
e6f043e 문서: 소개자료 09 절을 표에서 「전폭 한 줄 + 가로 박스 셋」으로 (7.21)
dab2655 기록: 세션 7.20 — 소개자료의 말 낮추기 (구성 무변경 · 새 숫자 0개)
0343335 문서: 소개자료의 말을 낮췄다 — 09 절 표 재구성 · 06·07 절 기능 선언 제거 (7.20)
65b9393 문서: 소개자료를 사람 지적 열넷대로 고쳤다 — 11 절 → 10 절 (7.19)
ccca630 도구: 소개자료 빌드 검사 ⑶ 을 「최소 한 번」으로 바꿨다 (7.19)
59b1dec 문서: 소개자료를 구성부터 다시 짰다 — 14 절 → 11 절 (7.18)
8208b93 도구: 소개자료 빌드 검사 ⑷(절 수 12~15)를 없앴다 (7.18)
54e2802 문서: 소개자료를 읽히게 다시 썼다 — 골격 어휘 제거·제목 문장화·계층 (7.17)
ead1462 문서: 소개자료 본문 — 자리표시 48개를 채웠다 (7.16)
e96716e 문서: 소개자료 HTML 골격 14 화면 (7.15)
d0ad519 도구: 소개자료 빌드 스크립트 — 시연을 iframe 으로 품고 검사 4종
57add4a 문서: 소개자료 수치 대조표 — 지금 코드 기준 재산출 (7.14)
8008ebe 문서: PROCEED.md — 7.13 단일 HTML 시연본 · 미해결 #53 등재·닫음
```

**C2 — 「현재 상태」 표에 오늘 아홉 판이 전부 있다. 채워 넣은 행 0**

7.13 · 7.14 · 7.15 · 7.16 · 7.17 · 7.18 · 7.19 · 7.20 · 7.21 — 아홉 행이 이미
올라와 있었고 빠진 것이 없었다. 이 판의 행(7.22)만 새로 붙였다.

**★ 세션 번호가 웹 대화창과 어긋난다 — 저장소가 정본이다**

이 판을 연 지시는 「7.14~7.15 = 수치 대조표 · 7.16 = 소개자료 골격 ·
7.17~7.21 = 내용·조형·구성」이라고 적었다. 저장소 기록은 다르다:

| | 웹 대화창이 부른 번호 | **저장소 기록 (정본)** |
|---|---|---|
| 수치 대조표 | 7.14~7.15 | **7.14** 하나 |
| 소개자료 HTML 골격 | 7.16 | **7.15** |
| 본문 채우기(자리표시 48개) | — | **7.16** |
| 내용·조형·구성 | 7.17~7.21 | **7.17~7.21** (같다) |

즉 **7.15 부터 웹 대화창의 번호가 하나 앞선다.** 지시대로 **저장소 쪽을
따랐고, 어긋난 사실만 여기 적는다** — 기록을 고치지 않았다.

**C3 — 미해결 셋을 등재했다: #55 · #56 · #57**

셋 다 오늘 판들이 세션 로그에 **보고만 하고 미해결 목록에는 올리지 않은** 것이다.

- **#55** 인쇄 한 벌에서 09 절 「필요한 것」의 `--warn` 이 흰 바탕에서 옅다
  (세션 7.21 관측 · **09 절 밖 · 색 한 벌 문제** · S)
- **#56** `docs/intro-outline.md` 가 지금 자료와 어긋난 자리 — 세션 **7.15**
  다섯 · **7.18** 여섯 · **7.19** 하나, 합쳐 **열둘**을 한 항목으로 묶었다 (S)
- **#57** 긴 시험(pytest 8 분대)을 백그라운드로 돌리고 완료를 폴링해 왕복이
  헛돌았다 (세션 7.21 ctx · **처음 나온 것** · S)

**#53 은 세션 7.13 에서 닫혔다. 이 판은 다시 열지 않았다.**
그 밖에 **연 것도 닫은 것도 없다.**

**C4 — 다음 판이 알아야 할 것 (오늘 정해진 것)**

1. **소개자료의 성격 = 「알리는 자료」다.** 승인 요청·기간·비용을 넣지 않는다.
2. **독자 = 사내 경영층.** 공학 배경이 없다고 보고 쓴다.
3. **실제로 해본 것만 「한 것」으로 적는다.** 기능 선언(「~할 수 있습니다」)은
   해본 것에만 쓴다 — 세션 7.20 이 06·07 절에서 선언을 걷어낸 이유다.
4. **「가정값 기반 — 실측 아님」은 표지 한 번만.** 절마다 반복하지 않는다
   (세션 7.19 · 빌드 검사 ⑶ 도 「절 수 = 표기 수」에서 **「최소 한 번」**으로
   바뀌어 있다).
5. **어두운 바탕이다.** 되돌리려면 `intro/intro.html` 의 `:root` **한 곳만**
   고친다 — 하드코딩 색 0, 전부 `var(--*)` 이고 색값은 `:root` 와
   `@media print` 두 블록에만 있다.
6. **시연 화면(`demo/`)은 읽기 전용이다.** 소개자료가 `iframe` 으로 품는다 —
   소개자료를 고치느라 `demo/` 를 건드리지 않는다.
7. **용어 일곱** — 냉매(냉각액 아님) · 펌프 압력 · 밸브 개도율 · 배관 차압 ·
   열 교환 · 랙 발열 · **랙 08**(0-based 「랙7」이 아니다).

**C5 — 내일 첫 작업 후보 (정하지 않았다. 사람이 고른다)**

- ⑴ **소개자료 전체를 처음부터 다시 읽고 남은 자리 고치기** — 지금까지는
  절 단위로 고쳐 왔고, 처음부터 끝까지 이어 읽은 판은 아직 없다.
- ⑵ **웹 대화창 소통 문제 목록 정리** — 사람이 각 대화창에서 뽑아 모으는 중이다.
- ⑶ **모델 쪽으로 돌아가기** — 미해결 **#36**(K값 근사 누출과 질량손실 누출이
  같은 방향을 가리키지 않는다) · **#40**(현재 2노드 전이 파형이 노드 수에
  수렴하지 않는다).

**닫기 검사** — pytest **1,207건 전부 통과**(482.01 s) · ruff
`All checks passed!` · mypy `no issues found in 29 source files`.
이 판은 `src/`·시험·`intro/`·`demo/`·`docs/` 를 하나도 건드리지 않았으므로
셋 다 **무변경 확인용**이다. **#57 대로 앞에서 한 번 돌렸다 — 왕복 1 회.**

**정본 문서 4종 무수정 — 프로젝트 지식 재업로드 필요 없음.**

ctx: 잔여 토큰 14,908,245 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **`PROCEED.md` 를 훑은 것** — 「현재 상태」 표와
미해결 목록 둘 다 한 행이 수천 자라, `awk` 로 앞머리만 잘라 받았는데도(#49 의
권고대로 했다) 그 두 번이 이 판에서 가장 컸다 · 줄일 수 있었다고 보는 것:
**`intro-outline` 어긋남을 모으느라 세션 로그 세 곳을 따로 읽은 것** —
`grep -n 'intro-outline'` 으로 자리를 먼저 잡은 뒤 세 구간을 **한 번의
`sed` 로 묶어 받았고**, 그래도 7.18 의 목록이 잘려 한 번 더 받았다. 처음부터
구간 끝을 넉넉히 잡았으면 한 번이었다

### 세션 7.23 · 2026-09-04 · collaboration.md 에 「웹 대화창이 실제로 저지른 것」 절 (문서 판)

**이 판이 한 일** — 코드를 하나도 건드리지 않았다. `collaboration.md` 에 절
하나가 생겼고, 새 대화창이 열릴 때마다 되풀이하던 다섯 가지를 **문서가
막게** 됐다. 바꾼 파일은 `collaboration.md` · `PROCEED.md` 둘이다.

**왜 이 자리인가** — 「이 프로젝트에서 특히 조심할 결함 유형」 절은 다섯
항목이 **전부 코드 쪽**(가정치·수렴 실패·물성치 중복·상태 이월·극단 케이스)
이고 웹 대화창 쪽은 한 줄도 없었다. 새 절은 「프롬프트를 쓸 때 (웹 → Code)」
**바로 아래**에 두었다 — 프롬프트를 쓰는 절 다음에 그 프롬프트에서 실제로
어긋났던 것이 온다.

**C2 — 다섯 줄은 사람이 추려 온 것을 그대로 올렸다.** 1~7일차 대화창 각각에
「너와 나 사이 소통에서 어긋난 자리」를 정리하게 해 **여섯 벌**을 모았고,
그중 **여러 날에 걸쳐 반복된 것**만 남긴 것이다. 하루만 나온 것은 넣지
않았다. **새 항목을 더하지 않았고 뜻과 반복 횟수 표기를 바꾸지 않았다** —
어투만 이 문서에 맞췄다.

```
① 볼 수 있는 것은 본다                                   6일 중 5일
② 프롬프트는 바깥 코드 블록 하나로만 낸다                실제 전달 사고 1회
③ 「거의」·「영향 없음」 전에 깨지는 경우를 하나 찾는다   4일
④ 정할 수 있으면 권고로 낸다                             4일
⑤ 결정을 청할 때 「하면/안 하면」을 함께 적는다          2일
```

**C3 — 실례는 지어내지 않았다.** 사람이 준 다섯을 한 줄씩 붙였다 —
세션 7.20(이전 버전 형식을 확인 없이 표로 짰다) · 7.6 무렵(프롬프트 안 코드
블록 중첩으로 앞부분만 전달) · 5 무렵(「거의 독립」·「새 숫자 0개」를 적고
다음 턴에 자기 정정) · 5.7 무렵(되돌리기 쉬운 것까지 확인 항목으로 올렸다) ·
5.6-B 무렵(결정 항목 이름만 올렸다). 이 문서가 결함 유형 절에서 이미 쓰는
방식(「실제로 나오면 사례를 채워 넣는다」)을 따랐다.

**C4 — `CLAUDE.md` 와 겹치는 것: 없음.** 절대 규칙 13(지시 범위)은 **Code
쪽** 범위를 다루고 새 ④ 는 **웹 대화창이 사람에게 확인을 청하는 빈도**를
다룬다 — 대상이 다르다. 프롬프트 형식(②)·자료 확인(①)·결정 청하는 법(⑤)은
`CLAUDE.md` 에 자리가 없다. 「여기서 되풀이하지 않는다」 절의 목록과도
겹치지 않는다.

**C4 보고 — `collaboration.md` 안에서 겹치는 것 하나(고치지 않았다).**
새 ③ 「주장이 깨지는 경우를 하나 찾아본다」는 판단 원칙 **⑥ 「가설은 반례와
함께 든다」**와 뜻이 같다. 다른 것은 **대상**이다 — ⑥ 은 검증 중 세우는
가설(예: 발산의 원인)에, ③ 은 프롬프트·보고에 적는 **단정 어구**(「거의」·
「영향 없음」·「추가 파라미터 0개」)에 걸린다. **사람이 추린 다섯을 줄이지
말라는 지시가 있어 그대로 올렸고, 겹친다는 것만 보고한다.** 나머지 넷은
겹치는 것이 없다(①은 판단 원칙 ③ 「짐작을 사실처럼 적지 않는다」와 인접하나,
③ 은 **결과 문서**에, 새 ① 은 **프롬프트를 쓰기 전 확인**에 걸린다).

**C5 보고 — 앞머리와 어긋난다. 고치지 않았다.** 문서 앞머리는 「이 파일은
*누가 무엇을 맡고, 무엇을 믿을 만한 것으로 치는가* 만 적는다」인데, 새 절은
**웹 대화창이 어떻게 쓰는가**(절차·형식)에 가깝다. 다만 **바로 위의
「프롬프트를 쓸 때」 절이 이미 같은 정도로 앞머리를 넘어 있다** — 새 절만
어긋나는 것이 아니라 앞머리 문구가 지금 문서보다 좁다. **앞머리를 고칠지는
사람이 정한다**(정본 문구 변경이라 임의로 하지 않는다).

**닫기 검사** — pytest **1,207건 전부 통과**(569.77 s) · ruff
`All checks passed!` · mypy `no issues found in 29 source files`.
이 판은 `src/`·시험·`intro/`·`demo/`·`docs/` 를 하나도 건드리지 않았으므로
셋 다 **무변경 확인용**이다. **#57 대로 앞에서 한 번 돌렸다 — 왕복 1 회.**

**정본 문서 중 `collaboration.md` 를 고쳤음 — 프로젝트 지식 재업로드 필요.**
(나머지 셋 — `CLAUDE.md`·`project-overview.md`·`AI서버_CDU_디지털트윈_프로젝트정리.md`
— 은 무수정.)

ctx: 잔여 토큰 14,916,143 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **`PROCEED.md` 의 「현재 상태」 표 끝 여섯 행을
받은 것** — 서식만 보려던 것인데 한 행이 수천 자라 그 한 번이 이 판에서 가장
컸다 · 줄일 수 있었다고 보는 것: **같은 것** — 마지막 **한 행**만(`sed -n '61p'`)
받으면 됐다. `grep` 으로 자리를 잡는 명령과 표를 받는 명령을 한 번에 묶으면서
범위를 7.16~7.22 로 넓게 잡았다

### 세션 7.24 · 2026-09-04 · `daily_brief.py` 에 웹 대화창용 축약 출력 `--web` (도구 판)

**이 판이 한 일** — `daily_brief.py` 한 파일에 `--web` 플래그를 더했다. 웹
대화창에 붙이던 브리핑이 **25,030자 → 1,472자(94.1% 감소 · 이 판 기록을 더한
뒤 잰 값)** 가 됐다. 물리
모델·가정치·데이터셋·시연 화면·소개자료·정본 문서 4종은 하나도 건드리지
않았다. 바꾼 파일은 `daily_brief.py` · `PROCEED.md` 둘이다.

**C1 — 파싱 규칙(출처는 `PROCEED.md` 하나다).** 목록을 옮겨 적은 새 파일을
만들지 않았다(collaboration.md ④).

```
ⓐ  `## 세션 로그` 절의 마지막 `### ` 제목 줄
ⓑ  `## 미해결 목록` 표에서 닫힘 표시가 없는 행의 `#` · `한 줄` 칸
    (닫힘 표시 = 기존 CLOSED_MARKERS: [해소 · [닫힘 · [확정 · **해소됨**)
ⓒ  `## 현재 상태` 표 마지막 행의 4번째 칸
    셀 분리는 기존 규칙 그대로 — escape 된 \| 는 구분자가 아니다
```

**제목 추출은 `--web` 안에서만 자른다.** 머리의 `[신설 · 세션 7.22]` 같은
대괄호 표시를 떼고 첫 문장 경계(`. ` 또는 ` — `)에서 자른다. **기본·`--full`
은 종전대로 전문이며 이 함수를 쓰지 않는다** — 세션 5.5-C 가 없앤 88자 절단이
되살아난 것이 아니다.

**C3 — 기본 출력 무변경을 문자열 비교로 확인했다.** HEAD 판 `daily_brief.py`
를 `git show` 로 받아 메모리 안에서 실행하고 현재 판과 `build_brief` 출력을
비교했다 — 기본(27,750자)·`--full`(58,136자) **둘 다 완전히 동일**(갱신 전
`PROCEED.md` 기준 글자 수다 — 이 판 기록을 더한 뒤로는 기본이 25,030자다). 비교용
임시 파일을 만들지 않았고 커밋하지도 않았다.

**C4 — 글자 수와 사람의 결정 둘.** 처음 구현본은 **1,641자**로 프롬프트의
중단선 1,500자를 넘어, 지시 7 대로 **거기서 멈추고 보고했다**(임의로 잘라내지
않았다). 사람이 정한 것은 둘이다.

- **ⓒ 를 첫 문장까지만 싣는다** → 1,641 → **1,480자**(그 시점 측정. 이 판
  기록을 더한 뒤로는 **1,472자**).
- **중단선을 1,500자 → 2,200자로 올린다.** 근거: 목적은 「1,000자」라는 수치가
  아니라 붙이는 것이 토큰을 아끼는 쪽이 되는 것이었고 94% 감소로 달성됐다.
  건당 약 45자이므로 **미해결 40건까지 버티는 값**이다. **이 상한을 검사하는
  코드를 넣지 않았고 수치를 코드·주석에 박지 않았다** — 사람의 판단 기준일
  뿐이다. ⓑ 는 「열린 미해결 전부」로 두었고 건수로 자르지 않는다.

**보고 — ⓒ 절단이 「프로젝트 지식 재업로드 필요」를 떨어뜨린다.** 7.23 행의
「다음 세션 첫 작업」은 `collaboration.md 를 고쳤음 — 프로젝트 지식 재업로드
필요 · …` 인데, 첫 ` — ` 에서 자르므로 `collaboration.md 를 고쳤음` 만 남는다.
7.22 행도 마찬가지로 `사람이 내일 첫 작업을 고른다` 만 남고 후보가 어디 있는지가
빠진다. 지시가 구분자를 `—` 로 명시했으므로 **그대로 구현했고 고치지 않았다** —
되살리려면 절단 경계를 첫 ` · ` 로 미루면 된다(그때 1,480 → 약 1,500자).

**닫기 검사** — pytest **1,207건 전부 통과**(392 + 815) · ruff
`All checks passed!` · mypy `no issues found in 29 source files`.
**#57 대로 백그라운드·폴링 없이 앞에서 돌렸다.** 다만 도구 호출 상한이 10분인데
지난 판 실측이 569.77 s 라 여유가 5% 밖에 없어, **파일 목록을 반으로 나눠 두 번
돌렸다**(46.67 s + 466.45 s). 건수 합이 1,207 로 전판 기록과 같다.

**정본 문서 4종은 무수정 — 프로젝트 지식 재업로드가 필요 없다.**

**다음이 알아야 할 것**

- 웹 대화창에 붙일 것은 이제 `daily_brief.py --web` 출력이다. 기본 출력과
  `--full` 은 그대로다(로컬 Code 용).
- `--web` 은 **미해결 건수에 비례해 커진다**(건당 약 45자). 사람이 정한 상한
  **2,200자**는 40건까지다. 코드가 검사하지 않으므로 넘는지는 사람이 본다.
- `--full` 과 `--web` 을 함께 주면 오류로 멈춘다(종료코드 2).

- **[후속 정정]** ⓒ 의 절단 경계가 첫 ` — ` 이 아니라 **첫 ` · `** 다. 정본
  글자 수는 **1,627자**(1,472자는 정정 전 값이다). 아래 「후속 정정」 절을
  함께 읽는다.

**[후속 정정 · 2026-09-04 · 같은 날 · 새 판 번호를 붙이지 않는다]**

ⓒ 의 절단 경계를 **첫 ` — ` 에서 첫 ` · ` 로** 고쳤다. 위 「보고」 절이 적은
그대로 첫 ` — ` 로는 7.23 행이 `collaboration.md 를 고쳤음` 만 남아
**「프로젝트 지식 재업로드 필요」가 떨어져 나갔다** — 웹 대화창이 놓치면
저장소 사본과 프로젝트 지식이 갈리는 항목이라 남아야 한다. 사람이 정한 것이고,
「구분자를 `—` 로 지시한 것이 잘못이었다」고 밝혔다. 고친 뒤 두 행 모두 살아난다.

```
7.22 → 사람이 내일 첫 작업을 고른다 — 후보 셋은 세션 7.22 로그 「내일 첫 작업 후보」에 있다
7.23 → collaboration.md 를 고쳤음 — 프로젝트 지식 재업로드 필요
```

**정본 글자 수는 1,627자다**(이 정정과 「다음 첫 작업」 두 항목을 적은 뒤 잰
값 · 상한 2,200자 안). 1,472자는 정정 전 값이다. 닫기 검사는 다시 돌렸다 —
pytest **1,207건 전부 통과**(392 + 815 · 45.75 s + 446.13 s) · ruff
`All checks passed!` · mypy `no issues found in 29 source files`. **#57 대로
백그라운드·폴링 없이 앞에서 돌렸고, 도구 호출 상한 10분 때문에 파일 목록을
반으로 나눠 두 번 돌렸다.**

**이 정정 판에서 하지 않은 것 — 다음 판으로 넘긴다.** 세션 7.25 프롬프트의
「샘」 승격 비용 계측(C1~C6)과 7.24 D4-3 의 미해결 신설(#58 자리)은 **시작하지
않았다.** 계측 스크립트를 만들지 않았고 데이터셋을 하나도 생성하지 않았다.
`src/`·`assumptions.py`·`demo/`·`intro/`·`dist/`·정본 문서 4종은 무수정이다.

ctx: 잔여 토큰 14,991,241 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **`daily_brief.py` 전문(269줄)을 받은 것** — 고칠
파일이라 필요했다 · 줄일 수 있었다고 보는 것: **제목 추출 규칙을 `python -c` 로
미리 시험한 왕복 한 번** — 구현한 뒤 한 번만 재도 됐다

ctx (후속 정정 회차): 잔여 토큰 14,988,137 / 15,000,000 (보이는 표시가 「잔여
토큰」이라 그 값을 적는다) · 가장 크게 먹은 것: **pytest 두 번과 ruff 재실행** —
docstring 줄바꿈에서 E501 이 두 번 걸려 ruff 를 세 번 돌렸다 · 줄일 수 있었다고
보는 것: **같은 것** — 한글 docstring을 고칠 때 90자 폭을 먼저 세었으면 한 번에
끝났다

### 세션 7.25 · 2026-09-04 · 「샘」(질량손실) 승격 비용 계측 (재는 판 · 새 값 0개)

**이 판이 한 일** — 「샘」을 정식 시나리오로 올릴 때의 **생성 시간과 파일 크기**를
실측하고, 지금 코드로 그대로 얹히는지를 확인해 목록으로 냈다. **정의를 바꾸지
않았다** — 누출 정의(절대 규칙 8 · K 근사) · `assumptions.py` · 데이터셋 스키마 ·
정본 문서 4종은 무수정이다. 데이터셋 산출물은 **스크래치패드에만** 썼고
`results/` 는 건드리지 않았다. 바꾼 파일은 `.gitignore` · `PROCEED.md` 둘이다.

**C1 — 「샘」 크기 수준값의 출처: 독립한 수준값이 없다.**

- **어디에 있나**: `src/cdu_simul/leak_massloss.py` 의 `leak_flow_bound_Lps()`
  와 `SWEEP_FRACTIONS`. `leak_massloss_thermal.leak_sizes_Lps()` 가 그대로
  다시 쓴다(세션 5.7-D 가 새로 정하지 않았다).
- **값**: 상한 = K 근사 **+50 %** 케이스의 **누출랙 통과유량 감소량**
  (정상 해의 랙 유량 − +50 % 해의 랙 유량). 스윕 지점 =
  `(0, 0.25, 0.5, 0.75, 1.0) × 상한` → **비영 4수준**.
- **누가 언제**: 세션 5.6 이 도입하고 세션 5.7-D 가 이어받았다. 코드 주석의
  근거 표기는 「[유도: 5장 누출 3수준 · 5-1 「누출 주입 지점」 — 새 숫자를
  만들지 않는다]」다.
- **5장·5-1 에 「샘」 크기 항목은 없다.** 5장 누출 항목은 K값 +5/+20/+50 % 뿐이다.
  위 값은 그 3수준에서 **유도한 것**이므로 새 가정치가 아니지만, **정식 수준값으로
  승격된 적도 없다** — 이 판에서 승격하지 않았고 새 수준값도 만들지 않았다.

따라서 중단 조건 「수준값 근거가 나오지 않으면 C3 를 멈춘다」에는 걸리지 않는다
— 근거가 코드와 5장 유도로 남아 있다.

**C2 — 지금 데이터셋(52열본) 재계측.** `dataset.generate(output_dir=<스크래치패드>)`
로 전량을 한 번 돌렸다.

| 항목 | 오늘 실측 | 저장소의 `results/cdu_dataset.csv` |
|---|---|---|
| 시나리오 | 1,792 (정상상태 768 · 전이 1,024) | — |
| 행 | 310,016 | — |
| 열 | **52** | **48** |
| 생성 시간 | **1,515.16 s (25.3 분)** | (기록 없음) |
| 크기 | **377,295,246 B (359.8 MiB)** | 304,608,266 B (2026-08-28) |
| 실패 행 | 0 | — |

**저장소에 남아 있는 `results/cdu_dataset.csv` 는 오늘 코드의 산출물이 아니다**
(48열 · 4열이 뒤에 붙었다). `results/` 는 `.gitignore` 대상이라 커밋되지 않으며,
이 판은 **재생성하지 않았다**(범위 밖). 크기 비교는 **오늘 값끼리만** 해야 한다.

행당 바이트 = 377,295,246 ÷ 310,016 = **1,217.0 B/행** — C4 크기 산출의 계수다.

**C2 부속 — 기존 데이터셋의 정상상태/전이 분해**(C4 가 규모 B 를 산출하려면
필요하다. 표본을 인덱스 균등 간격으로 뽑았다).

| 구분 | 케이스 | 행/케이스 | 행 | s/케이스(표본) | 시간 |
|---|---|---|---|---|---|
| 정상상태 | 768 | 1.67 | 1,280 | 0.0348 | 26.7 s |
| 전이 | 1,024 | 301.5 | 308,736 | 1.6089 | 1,647.5 s |
| 합 | 1,792 | — | **310,016** ✔ | — | 1,674.2 s |

행 수는 실측과 **정확히 일치**한다. 시간은 표본 합이 실측 1,515.16 s 보다
**+10.5 %** 높다 — 표본 오차이며, C4 는 이 표본 rate 를 그대로 쓴다(따라서
C4 의 시간은 **약 10 % 보수적**이다).

**C3 — 「샘」 경로 케이스당 소요.** 표본을 64 케이스에 고루 흩어 뽑았다(첫 케이스만
쓰면 어긋난다 — 이 판에서 실제로 겪었다. 아래 「걸린 것」 참조). **데이터셋 파일을
만들지 않았다 — 계측만 하고 결과를 버렸다.**

| 경로 | 호출 | 표본 | 소요 |
|---|---|---|---|
| 정상상태 (단일 CDU) | `solve_massloss_steady` | 32회 | **0.0081 s/해** |
| 전이 (단일 CDU) | `integrate_massloss_step` | 8회 | **0.3139 s/적분** (시각 표본 201) |
| 정상상태 (다중 CDU) | `solve_plant_massloss` | 8회 | **0.0481 s/해** |

**수렴 실패 0건** (`solver_converged` · `solver_success` 를 전 호출에서 확인했다 —
절대 규칙 5).

**C4 — 세 규모의 예상 생성 시간·크기.**

*산출에 쓴 축 (새로 정한 것이 아니라 지금 코드가 이미 도는 축이다)*

- 「샘」 크기: **비영 4수준** (C1)
- 배치(구조 자유도): **6** = g ∈ {0.0, 0.5, 1.0} × 펌프가 보는 유량 {공급, 환수}
  (`leak_topologies()`). 다중 CDU 는 여기에 **2차측 배분 읽기 2**가 더 곱해진다
- 나머지 축은 기존 데이터셋 그대로 — 32조합 × 부하 2 × 구성(정상상태 3 · 전이 2)
  × M 2(전이)

*산출식*

```
샘 정상상태 케이스 = 32×2×4×6 (single)  +  32×2×4×6×2(구성) ×2(배분읽기) (dual)
                   = 1,536 + 6,144 = 7,680
샘 정상상태 행     = 1,536×1 + 6,144×2 = 13,824
샘 정상상태 시간   = 1,536×0.0081 + 6,144×0.0481 = 12.4 + 295.5 = 307.9 s

샘 전이 케이스     = 32×2×4×6×2(M) = 3,072        (single 만 — C5 ⑷)
샘 전이 행         = 3,072×201 = 617,472
샘 전이 시간       = 3,072×0.3139 = 964.3 s

크기 = 행 × 1,217.0 B
```

| 규모 | 내용 | 행 | 시간 | 크기 |
|---|---|---|---|---|
| **A** (대조군) | 정상 + 막힘 (지금 그대로) | 310,016 | **1,515 s (25.3 분)** ← 실측 | **377 MB** ← 실측 |
| **B** | 정상 + 막힘 + 샘, **정상상태만** | 15,104 | **335 s (5.6 분)** | **18.4 MB** |
| **C** | 정상 + 막힘 + 샘, 정상상태 + 전이 | 941,312 | **2,947 s (49.1 분)** | **1.15 GB** |

**규모 B 는 A 의 부분집합이 아니다** — A 의 전이 1,024 케이스를 빼고 정상상태
768 케이스만 남긴 뒤 샘 정상상태를 더한 것이다. 그래서 A 보다 작다.

*배치를 전수로 돌리지 않고 하나로 고정하면* (사람이 g·펌프 위상·배분 읽기를
5-1 에 값으로 못박는 경우 — **절대 규칙 1 에 걸리는 결정이다**):

| 규모 | 행 | 시간 | 크기 |
|---|---|---|---|
| B (배치 1) | 3,584 | 78 s (1.3 분) | 4.4 MB |
| C (배치 1) | 415,232 | 1,886 s (31.4 분) | 0.51 GB |

*크기 산출의 한계* — 1,217.0 B/행 은 **지금 52열 스키마**의 값이다. 샘 행은
최소 4~5열(누출 기구 · Q_leak · g · 펌프 위상 · 배분 읽기)이 더 필요하므로
(C5 ⑶) 실제 크기는 이보다 **약 10 % 크다**. 스키마 변경은 이 판 범위 밖이라
반영하지 않았다.

**C5 — 「샘」 경로가 배치 실행에 그대로 얹히는가: 아니다. 손볼 곳 아홉.**
**이 판에서 하나도 고치지 않았다**(절대 규칙 13).

⑴ **import 금지 경계에 걸린다 — 사람의 승인이 필요하다.**
`leak_massloss.py`·`leak_massloss_thermal.py` 두 파일의 머리 주석이 절대 규칙 8
예외의 **조건**으로 「이 모듈은 데이터셋 생성 경로에 들어가지 않는다 — 저장소의
어느 모듈도 이 파일을 import 하지 않는다」를 못박았다. `dataset.py` 가 import
하는 순간 그 조건이 깨진다. **코드 문제가 아니라 승인 범위 문제다.**

⑵ **`ScenarioSpec` 에 누출 기구 축이 없다.** 누출이 `leak_multiplier`(K 배수)
하나로만 표현되고 `scenario_kind`·`leak_level_percent`·`leak_cdu_index` 세 속성이
전부 그 필드에서 파생된다. 「샘」을 넣으려면 필드 추가 + 세 속성 분기.

⑶ **52열 스키마에 담을 자리가 없다.** Q_leak · g · 펌프 위상 · 배분 읽기 ·
소실률(`inventory_loss_percent`) — 최소 4~5열이 는다. 5-1 「데이터셋 스키마 규약」
변경이다.

⑷ **다중 CDU 「샘」 전이가 코드에 없다.** `solve_plant_massloss` 는 **정상상태
전용**이고 `integrate_massloss_step` 은 **단일 CDU 전용**이다. 규모 C 의
`dual_asymmetric` 전이 행은 **지금 코드로 만들 수 없다** — C4 의 샘 전이 산출을
single 만으로 잡은 이유다. 새 코드가 필요하다.

⑸ **구조 자유도 셋이 값으로 정해져 있지 않다.** `residual_return_share`(g) ·
`pump_sees_supply_flow` · `share_uses_return_flow`. 지금은 **전수로 돈다**(값을
고르지 않는 것이 5.6/5.7-D 의 방침이었다). 데이터셋에 그대로 얹으면 정상상태가
×6, 다중 CDU 가 ×12 로 불어난다. 하나로 고정하려면 5-1 에 새 값이 들어간다
(절대 규칙 1) — **사람이 정할 것**이다.

⑹ **케이스 축의 모양이 다르다.** `dataset.enumerate_specs` 는 32조합을 받아
부하를 `ScenarioSpec` 필드로 얹지만, `leak_massloss_thermal.thermal_cases()` 는
32조합 × 부하 2 = **64 케이스로 미리 편다**. 합치려면 한쪽으로 맞춰야 한다.

⑺ **`PROVENANCE_COLUMNS` 의 `signal_sign_caveat` 이 K 근사 전용 문구다.**
「총유량·양정은 K값 증가 근사(5장)의 결과다 — 실제 누출은 계통 밖 유출이라
부호가 반대일 수 있다」. 샘 행에 이 문구를 그대로 실으면 **틀린 표기**가 된다
(샘 행은 그 「실제 누출」 쪽이다). 기구별로 갈라야 한다.

⑻ **미해결 #36 이 라벨에 걸린다.** K 근사와 질량손실이 **총유량 부호에서 갈린다**.
둘을 「누출」이라는 한 라벨 아래 나란히 실으면 같은 열(`total_flow_Lps`)에서 신호
방향이 상충하는 행이 생긴다. 라벨을 「막힘」/「샘」으로 가르는 것이 이 판의 전제이지만,
갈라도 **열은 같으므로** 학습 쪽에서 기구 열을 반드시 읽어야 한다.

⑼ **미해결 #38 이 전이 행에 그대로 들어온다.** 질량손실 전이의 **상수 M 근사**가
관측 창 뒷부분을 지탱하지 못한다. 규모 C 를 만들면 그 한계가 학습 데이터에
들어가므로 `inventory_loss_percent`(와 t63 기준값)를 **열로 실어** 표시해야 한다
(절대 규칙 11).

**걸린 것 — 계측 자체가 도구 호출 상한에 두 번 닿았다.**
⑴ C2 전량 생성을 10분 상한으로 걸었는데 **1,515 s** 가 걸려 백그라운드로 넘어갔다.
폴링하지 않고(#57) 완료 통지를 기다렸으며, 그 사이 계측이 아닌 항목(`.gitignore` ·
C1 · C5)을 처리했다. **첫 4/2 케이스 표본으로 잡은 사전 추정 175 s 는 실측의
1/8.6 이었다** — 데이터셋 앞머리 케이스가 유난히 싼 탓이다. C3 는 그것을 알고
표본을 **인덱스 균등 간격**으로 흩어 뽑았다. ⑵ 전체 pytest 는 **반으로 나눠**
돌렸다(#58).

**하지 않은 것.** 새 수준값을 만들지 않았고, 데이터셋 파일을 `results/` 에
쓰지 않았으며, 계측 스크립트를 저장소에 남기지 않았다(전부 일회성 `python -c` ·
산출물은 스크래치패드). `src/`·시험·`assumptions.py`·정본 문서 4종 무수정.

ctx: 잔여 토큰 14,872,561 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **`PROCEED.md` 「현재 상태」 표 마지막 행들을 읽은
것** — 서식을 보려고 `sed` 로 뽑았는데 한 행이 수천 자다 · 줄일 수 있었다고 보는
것: **같은 것** — `cut -c1-400` 으로 잘라 봤으면 됐다

### 세션 7.26 · 2026-09-04 · 이상 상태를 「막힘」·「샘」 둘로 세운다 (정의 판)

**이 판이 한 일** — 누출을 K값으로만 근사하도록 막고 있던 **절대 규칙 8 을 풀고**,
이상 상태를 「막힘」·「샘」 둘로 정본 문서에 세웠다. **코드는 한 줄도 고치지
않았다** — `src/` · 시험 · `assumptions.py` · 스키마 · 데이터셋 · `daily_brief.py` ·
`demo/` · `intro/` · `dist/` · `project-overview.md` · `collaboration.md` 무수정.
7.25 C5 의 아홉도 하나도 고치지 않았다. 바꾼 파일은 **`CLAUDE.md` ·
`AI서버_CDU_디지털트윈_프로젝트정리.md` · `PROCEED.md`** 셋이다.

**C1 — 절대 규칙 8 개정.** 원 문언은 「누출 시나리오는 배관저항(K값) 변화로
근사한다(5장 정의). 다른 방식(예: 질량 손실 직접 모델링)으로 바꾸려면 먼저
사람에게 확인한다」였다. 새 문언이 담은 것과 그 이유:

- **「이상 상태는 「막힘」과 「샘」 둘이다」로 머리를 바꿨다.** 원 문언은 「누출」을
  주어로 두고 그것을 K로 **근사**한다고 했다 — 근사라는 낱말이 K를 샘의 대용으로
  못박는다. 사람이 정한 것(3-1)은 막힘이 **독립된 이상 상태**라는 것이므로 주어를
  「이상 상태」로 올리고 「근사」를 뺐다. 「대용(proxy)이 아니라」를 명시로 적었다 —
  이 낱말이 `dataset.py`·`demo_steady.py`·`project-overview.md` 에 남아 있어서다.
- **셋째 갈래·정의 변경은 사람 확인** — 원 문언의 「다른 방식으로 바꾸려면 확인」이
  하던 일을 그대로 옮겼다. 규칙이 풀린 것이 아니라 **경계가 둘로 넓어진 것**임을
  분명히 하려는 것이다.
- **부호 항을 규칙 본문에 넣었다** — 지금까지 #36 은 미해결 목록과 5-1 한계에만
  있었다. 두 기구를 한 데이터셋에 실으면 같은 열(`total_flow_Lps`)에서 방향이
  상충하므로(7.25 C5 ⑻), 「한쪽 기구의 신호를 다른 쪽 방향으로 읽지 않는다」는
  **읽는 쪽 규칙**이 필요하다. 「구조 자유도 배치에 따라 샘이 수력 흔적을 전혀
  남기지 않는 경우도 있다」를 괄호로 붙였다 — 부호만 적으면 「샘은 항상 는다」로
  읽히는데 세션 5.6 관측은 그렇지 않다.

**C2 — 5-1 에 「「샘」(질량손실) 크기 수준」 신설.** 「누출 주입 지점」과 「보충수
처리」 사이에 넣었다. **확정값은 숫자가 아니라 역산 규칙**이다 — 상한 = 같은
조합에서 막힘 +50% 케이스가 만든 누출랙 통과유량 감소량, 스윕 =
`(0, 0.25, 0.5, 0.75, 1.0) × 상한` → 비영 4수준. 코드
(`leak_massloss.leak_flow_bound_Lps()` · `SWEEP_FRACTIONS`)가 재산출하고 숫자를
박지 않는다. **근거는 5-1 빈칸 처리 순서 1번(역산)이며 새 숫자가 0개**다.
한계 넷을 함께 적었다: ⑴ 부호가 막힘과 정반대(#36) ⑵ g=0·펌프=공급 배치에서
다섯 양이 **정확히 0** — 크기 수준이 신호 세기를 보장하지 않는다 ⑶ 랙에
국소화되지 않는다(랙 간 비대칭 2.220e-16 L/s) ⑷ energy balance 가 누출 엔탈피 항
없이는 닫히지 않는다(5.7-D). **정상상태만 얹는다**(규모 B)는 것과 전이는 #40 이
열린 채라 붙이지 않았다는 것도 항목 안에 적었다.

**C3 — 5-1 「누출 주입 지점」이 「샘」에 걸리는가: 부분적으로만 걸린다.
고치지 않았다.**

| 그 항목의 조각 | 「샘」에 걸리는가 |
|---|---|
| 「**랙 1개**에 주입한다」 | **걸린다.** 코드도 그렇게 돈다(`leak_rack_index` 하나) |
| 「랙별 배분을 켠 경우 **랙7**」 | **걸린다** — 걸리지만 **의미가 없다**. 랙7 을 고른 이유가 「K 배수 1.2 × 1.5 = 1.8 이라 어느 랙과도 겹치지 않는다」인데 샘은 K 를 건드리지 않는다 |
| 「균등 배분에서는 **어느 랙인지 결과에 영향하지 않는다**」 | **성립하지만 근거가 다르다.** 막힘에서는 8랙 대칭 때문이고, 샘에서는 **애초에 랙에 국소화되지 않기** 때문이다(헤더가 저항 0 공통 노드 · 랙 간 비대칭 2.220e-16 L/s) |
| 「**미량 누출 조기감지**(1장)이므로 국소 누출이 목적에 맞다」 | **샘에서는 국소가 아니다.** 주입 지점을 랙 하나로 적어도 계통은 그것을 국소로 보지 않는다 |
| 한계 문단 전체(「이 K값 근사는…」) | **막힘 전용이다.** 「질량이 보존되는 모사라 실제 누출과 다음이 다르다」는 K 를 **샘의 대용으로 읽는 문장**이라, 둘이 독립 상태가 된 지금 문장의 전제가 바뀐다 |

**사람이 정할 것**: ⑴ 항목명을 「누출 주입 지점」 그대로 둘지, ⑵ 「랙 1개」를
기구별로 갈라 적을지, ⑶ 한계 문단을 「막힘」 항목 쪽으로 옮길지. 이 판에서
**아무것도 고치지 않았다**(절대 규칙 13 · 지시 C3 가 「보고만 한다」다).

**C4 — 구조 자유도 셋의 5-1 기재 초안 (넣지 않았다).** 지시 문언이 「초안을
낸다」이고 C2 의 「신설한다」와 구별되므로 **5-1 에 쓰지 않았다.** 초안 전문은
아래 보고에 있다. 요지: 값으로 고정하지 않고 **전수**로 돈다는 것을 기록하는
형태이며, 셋은 ⓐ `residual_return_share`(g) = 계통 잔여저항 중 누출점 하류에
놓이는 몫 ⓑ `pump_sees_supply_flow` = 샘이 있으면 공급≠환수인데 펌프곡선의 Q 에
어느 쪽을 넣는가 ⓒ `share_uses_return_flow` = 5-1 「공유 2차측 결합 방식」의
「1차측 유량에 비례」에서 그 유량을 어느 쪽으로 읽는가(#39 · 다중 CDU 에서만
생긴다)다. 3 × 2 = **6 배치**, 다중 CDU 는 × 2 = **12**.

**C5 — 「누출」 낱말이 어긋나는 자리: 문서 10곳 · 코드 11곳. 고치지 않았다.**
미해결 **#59** 로 등재했다. 뿌리는 하나다 — 지금까지 「누출」이 **상위 개념**(이상
사건)과 **하위 기구**(K 근사) 둘 다를 가리켰는데, 이 판이 하위를 「막힘」·「샘」으로
갈랐으므로 상위로만 쓰는 낱말이 필요해졌다.

*문서 (4종)*

| 자리 | 지금 문구 | 어긋나는 이유 |
|---|---|---|
| 5장 표 항목명 「**누출 시나리오**」 | 「배관 K값 정상 대비 +5/+20/+50%」 | 값은 맞지만 **항목명이 이제 「막힘」만 가리킨다.** 5장 본문은 이 판에서 고치지 않는다(중단 조건) |
| 정리 4장 3번 | 「누출 시나리오 주입 — 배관 저항 변화**/유량 손실**로 근사 모사」 | **이미 둘을 다 적고 있다.** 규칙 8 이 K만 허용하던 동안 이 줄이 앞서 있었던 셈이다 — 이제 규칙과 맞는다. 「근사」만 남는다 |
| 5-1 「누출 주입 지점」 | 위 C3 표 | 다섯 조각 중 셋이 막힘 전용이다 |
| `CLAUDE.md` 1~4행(목적문) | 「정상/이상/**누출** 시나리오」·「**누출** 조기감지」 | 「이상」과 「누출」을 **나란히** 놓아 서로 다른 것처럼 읽힌다. 새 규칙 8 에서는 누출(샘)이 이상의 **하위**다 |
| `CLAUDE.md` 규칙 6 · 「하지 말 것」 · 세션 4 게이트 · 세션 5.5 게이트 | 「누출 시나리오」 | 세션 4 는 실제로 **막힘만** 돌았다. 지금 문구는 세션 4 게이트가 샘도 판정한 것처럼 읽힌다 |
| `project-overview.md` 66~69행 | 「데이터셋 안에서 정상/이상/**누출**이 구분되는가」·「본 프로젝트에서 **누출 정의를 질량손실로 교체**할 것인가 (#36) — 이 파일럿이 끝난 뒤」 | **이 판의 결정과 어긋난다.** 사람이 고른 것은 「교체」가 아니라 #36 가설 **(다) 병기**이고, 시점도 「파일럿 뒤」가 아니라 지금이다 |
| `project-overview.md` 112·244행 | 「누출 정의를 질량손실로 교체 (#36)」 | 위와 같다 |
| `project-overview.md` 161·177행 | 「누출 시나리오 주입 — 배관 K값 변화로 근사」 | 막힘 전용 문구다 |
| `project-overview.md` 224·232행 | 「절대 규칙 8 **예외**, 사람 승인」 | **예외가 아니게 됐다** — 규칙 8 이 샘을 정식으로 받는다 |
| `collaboration.md` 99행 | 「다수 시나리오(정상/이상/누출)」 | `CLAUDE.md` 목적문과 같은 나열 문제. 결함유형 ④ 설명이라 뜻은 상하지 않는다 |

*코드 (고치지 않았다 — 규칙 8 이 바뀌어 주석이 옛 규칙을 인용하게 된 자리)*

`assumptions.py:473·509`(「K값 변화로**만** 근사한다」) · `dataset.py:116` ·
`demo_steady.py:17·197`(「절대 규칙 8 근사」) · `dynamics.py:493`(「K값 배율로**만**」) ·
`hydraulics.py:215·244` · `leak.py:7`(「K값 변화로**만**」) ·
`leak_massloss.py:3·4·7·491` · `leak_massloss_thermal.py:7·8·604`
(「**절대 규칙 8 예외**」·「규칙 8 문구를 바꾸지 않는다」·「이 모듈은 데이터셋 생성
경로에 들어가지 않는다」) · `tests/test_hydraulics.py:97`.

이 중 **`leak_massloss*.py` 두 파일의 머리 주석이 가장 크게 어긋난다** — 그 조건
셋(별도 코드 · 데이터셋 미변경 · 규칙 8 문구 미변경)이 **전부 무효**가 됐다.
지시 3-4 가 import 금지 경계를 열었으므로 주석의 「어느 모듈도 이 파일을 import
하지 않는다」도 더는 지킬 조건이 아니다. **이 판에서 고치지 않았다** — 코드 판이
아니다(중단 조건 5-2 · collaboration.md ⑤).

**중단 조건에 걸렸는가: 걸리지 않았다.** C1·C2 문언은 5장 표의 **값을 하나도
바꾸지 않는다** — 막힘은 표의 +5/+20/+50% 를 그대로 쓰고, 샘 크기는 그 3수준에서
역산한다. 어긋나는 것은 값이 아니라 **항목명**(「누출 시나리오」)이고, 그것은 C5 가
목록으로 올리라고 지정한 바로 그 종류다 — 여기서 멈추면 C5 를 할 수 없다.
5장 본문(1~5장)은 한 글자도 고치지 않았다.

**검사** — pytest **1,207건 전부 통과**(#58 대로 반으로 나눠 돌렸다: 앞 7파일
392건 41.24 s · 뒤 7파일 815건 447.86 s. 백그라운드로 돌리지 않았다 · #57) ·
ruff `All checks passed!` · mypy `no issues found in 29 source files`.
**검사기 통과는 문법·타입·스타일이 성립한다는 뜻이지 물리가 맞다는 뜻이 아니다** —
이 판은 애초에 물리 판정이 없다(정의 판).

**문서를 고쳤음 — 프로젝트 지식 재업로드 필요**: `CLAUDE.md`(절대 규칙 8) ·
`AI서버_CDU_디지털트윈_프로젝트정리.md`(5-1 새 항목).

ctx: 잔여 토큰 14,893,570 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **프로젝트정리 5-1 표를 98행 통째로 읽은 것** — 삽입
자리와 행 서식만 필요했는데 행 하나가 수천 자다 · 줄일 수 있었다고 보는 것:
**같은 것** — `cut -c1-200` 으로 잘라 자리만 잡고, 서식이 필요한 이웃 두 행만
전문으로 봤으면 됐다

### 세션 7.27 · 2026-09-04 · 「누출」 낱말과 어긋난 문서를 맞춘다 (문서 판)

**이 판이 한 일** — 사람이 고른 낱말 규약 **(가)** 를 정본 문서 넷에 반영했다.
**코드는 한 줄도 고치지 않았다** — `src/` · 시험 · `assumptions.py` · 스키마 ·
데이터셋 · `demo/` · `intro/` · `dist/` · `daily_brief.py` 무수정. 7.25 C5 의
아홉도 하나도 고치지 않았다. 바꾼 파일은 **`AI서버_CDU_디지털트윈_프로젝트정리.md` ·
`CLAUDE.md` · `collaboration.md` · `project-overview.md` · `PROCEED.md`** 다섯이다.

**규약 (가)** — 「누출」은 **상위 개념**(실제로 일어나는 이상 사건) 전용이고, 모델
안의 기구는 언제나 「막힘」·「샘」으로 적는다. 「정상/이상/누출」처럼 이상과 누출을
나란히 놓지 않는다. `CLAUDE.md` 절대 규칙 8 끝에 「낱말 규약(세션 7.27)」으로 적었다.

**C1 — 5-1 「「샘」 구조 자유도 셋」 신설.** 「「샘」(질량손실) 크기 수준」 바로 뒤,
「보충수 처리」 앞에 넣었다. 확정 시점 = 세션 7.27. 값은 ⓐ `residual_return_share`(g)
= 잔여저항 중 누출점 하류(환수측) 몫 · ⓑ `pump_sees_supply_flow` = 펌프곡선의 Q 에
공급·환수 어느 쪽을 넣는가 · ⓒ `share_uses_return_flow` = 2차측 배분의 「1차측
유량」을 어느 쪽으로 읽는가(#39 · 다중 CDU 전용). **값으로 고르지 않고 전수**로
g `{0.0, 0.5, 1.0}` × ⓑ 둘 = **6 배치**, 다중 CDU 는 × ⓒ 둘 = **12 배치**.
**새 숫자 0개** — g 의 0·1 은 분수의 정의상 양 끝, 0.5 는 「양 끝 사이에서 부호가
갈리는 지점이 있는지」를 보려는 것이고(값을 고르는 것이 아니다), ⓑⓒ 는 참/거짓
양쪽이다.

**초안 전문에 대해 밝힐 것** — 세션 7.26 C4 초안의 **문언 전문은 저장소에 남아
있지 않다**(그 판이 5-1 에 넣지 않았고 보고에만 있었다). `PROCEED.md` 세션 7.26
로그의 요지(ⓐⓑⓒ 정의 · 6 배치 · 다중 12)와 `leak_massloss.py` 머리 주석·
`leak_topologies()` 를 근거로 **같은 내용을 다시 적었다.** 요지에 없던 것을
더하지 않았고, 새 숫자를 만들지 않았다. **초안과 글자 단위로 같다고 주장하지
않는다.**

**C2 — 5-1 「누출 주입 지점」을 기구별 **두 항목**으로 갈랐다.** 한 항목 안에서
기구별로 나눠 적는 대신 **항목 자체를 둘로 쪼갰다** — 그 항목의 근거란이
「랙 대칭이라 랙 번호가 결과에 영향하지 않는다」와 「K 배수 1.2 × 1.5 = 1.8 이라
겹치지 않는다」라는 **K 에 걸린 논증**으로 이루어져 있는데, 「샘」은 K 를 건드리지
않으므로 같은 셀 안에 두면 **한 근거란이 서로 성립하지 않는 두 전제를 동시에
싣게 된다**. 값란과 근거란이 기구마다 통째로 달라지는 경우라 쪼개는 쪽이 읽는
사람에게 덜 위험하다고 봤다.

- **「「막힘」 주입 지점」** — 원 항목의 값·근거를 유지했다(랙 1개 · 배분 시 랙7 ·
  랙0 겹침 −9.1e-13 kg/h · #45). 한계 문단만 다시 적었다 — 원 문언이 「이 K값
  근사는 누출을 배관 저항 변화로 **모사**한다」로 시작해 **K 를 「샘」의 대용으로
  읽는 전제**를 깔고 있었는데, 규칙 8 개정으로 그 전제가 없어졌다. 새 문언은
  「막힘은 질량이 보존되는 **독립된 이상 상태**이며 「샘」의 대용이 아니다」로
  시작하고, ⑴⑵⑶(부호 반대 · 흔적 없는 배치 · 공급−환수 불일치)은 **숫자를 그대로
  둔 채** 「막힘의 신호를 샘의 방향으로 읽지 않는다」라는 읽는 쪽 한계로 옮겼다.
- **「「샘」 주입 지점」**(신설) — 값: **랙 하나에 걸되 계통은 그것을 국소로 보지
  않는다**(랙 간 비대칭 2.220e-16 L/s · 세션 5.6). **랙별 배분을 켜도 랙7 을
  고르지 않는다** — 랙7 근거가 K 배수에서 나온 것이라 「샘」에 걸리지 않는다.
  「막힘」에서 랙 번호가 무의미한 것은 **8랙 대칭** 때문이고 「샘」에서는 **애초에
  국소화되지 않기** 때문이라는 **성립 이유의 차이**를 적었다. 한계 셋: 어느 랙에서
  새는지 이 모델은 답하지 않는다 · g=0·펌프=공급 배치에서 수력 신호가 정확히 0 ·
  부호가 「막힘」과 정반대(#36).

**C3 — 문서 아홉 자리를 고쳤다. 5장 표 항목명은 고치지 않았다.**
고친 자리는 아래 「고친 자리」에 있다. **5장 표 항목명 「누출 시나리오」는 중단
조건대로 보고하고 멈췄다** — 제안은 「**이상 시나리오 — 「막힘」**」이며 값
(+5/+20/+50%)은 건드리지 않는다. 사람이 답하기 전까지 그대로 둔다.

**C4 — `project-overview.md` 에 「그 뒤 무엇이 달라졌는가」 절 신설.**
**판정표는 한 글자도 고치지 않았다** — 2026-08-31 에 사람이 그렇게 판단한 것이
사실이고, 그 기록을 나중 결정에 맞춰 고쳐 쓰면 판정이 무엇을 보고 내려졌는지가
사라진다. 새 절이 담은 것: ⑴ 판정 7 이 가리킨 「질량손실로 **교체** · 파일럿이
**끝난 뒤**」와 실제로 일어난 「**둘로 세움** · **그 자리**」의 차이, 규칙 8 개정과
5-1 세 항목, **#36 은 닫히지 않았고 판정 5-b(X)도 그대로**라는 것 · ⑵ 「절대 규칙
8 **예외** · 사람 승인」(224·232행)은 **지워진 것이 아니라 예외가 규칙 본문으로
올라간 것**이며 그 두 줄은 당시 기록이라 그대로 둔다는 것 · ⑶ 낱말 규약과
`intro/` 예외 · ⑷ #59 의 코드 11곳이 열린 채라는 것. 「지금 어디인가」 목록에는
7.0~7.23(한 줄로 묶었다)·7.24·7.25·7.26·7.27 을 이었고, 남은 할 일을
「#59 코드 11곳」·「「샘」을 배치 실행에 얹기」·「전이 시간축(#40)」 셋으로 다시 적었다.

**C5 — `collaboration.md` 99행.** 「다수 시나리오(정상/이상/누출)」 →
「다수 시나리오(정상/이상 — 「막힘」·「샘」)」. 결함유형 ④ 의 뜻은 바뀌지 않는다.

**C6 — 미해결 #59 갱신.** 문서 10곳 중 **아홉 닫힘**, 5장 표 항목명 하나가
**사람 대기**, **코드 11곳은 열린 채**. 상태는 **열림**으로 둔다.

**지시 3-4(소개자료와 정본이 다른 규칙을 갖는다)를 어디에 남겼는가** —
`CLAUDE.md` 절대 규칙 8 「낱말 규약」과 `project-overview.md` 새 절 ⑶ 두 곳이다.
근거는 `intro/make_intro_html.py:44` 의 금지어 표(「누출」 → 「「랙 배관 저항
증가」로 쓴다. 기기 이름도 예외가 아니다」). **`intro/` 는 열지 않았다.**

**중단 조건에 걸렸는가 — 하나에 걸렸다.** ⑴ 5장 표 항목명: **보고하고 멈췄다**
(위 C3). ⑵ 5장 본문의 **값**을 바꿔야 할 일은 생기지 않았다 — 고친 것은 5-1(파생
가정치)과 4장 3번의 문장이고, 5장 표의 숫자는 한 글자도 건드리지 않았다.
⑶ 문서를 맞추는 중에 코드가 규칙 8 위반 상태가 되는 것은 **새로 보이지 않았다** —
#59 가 이미 코드 11곳을 담고 있고 거기에 더할 것이 없었다.

**목록 밖에서 눈에 띈 자리 셋 — 고치지 않았다**(절대 규칙 13 · 지시가 「C5 가 올린
10곳」이다). `project-overview.md` **45행**(「시나리오(정상/이상/누출) 간 신호」) ·
**133~136행**(문서 목적문의 「정상/이상/누출」) · **171행**(「누출 K값 변화율」).
셋 다 (가)안으로 보면 하위 기구를 「누출」로 부르는 자리다. **#59 에 얹을지는
사람이 정한다.**

**검사** — pytest **1,207건 전부 통과**(#58 대로 반으로 나눠 돌렸다: 앞 7파일
392건 **69.72 s** · 뒤 7파일 815건 **526.81 s**. 백그라운드로 돌리지 않았다 · #57) ·
ruff `All checks passed!` · mypy `no issues found in 29 source files`.
**검사기 통과는 문법·타입·스타일이 성립한다는 뜻이지 물리가 맞다는 뜻이 아니다** —
이 판은 애초에 물리 판정이 없다(문서 판이고 코드를 고치지 않았다).

**고친 자리**

| 파일 | 자리 |
|---|---|
| `AI서버_CDU_디지털트윈_프로젝트정리.md` | 4장 3번 · 5-1 「「막힘」 주입 지점」(항목명·한계 문단·확정 시점) · 5-1 「「샘」 주입 지점」**신설** · 5-1 「「샘」 구조 자유도 셋」**신설** |
| `CLAUDE.md` | 목적문(3행) · 절대 규칙 6 · 절대 규칙 8 「낱말 규약」**신설** · 게이트 표 세션 4 · 게이트 표 세션 5.5 · 「하지 말 것」 |
| `project-overview.md` | 「그 뒤 무엇이 달라졌는가」 절 **신설** · 「다음 단계」 목록 · 4장 인용 3번 · 「시나리오 가정치」 핵심 줄 · 「지금 어디인가」 목록 |
| `collaboration.md` | 99행 |
| `PROCEED.md` | 「현재 상태」 7.27 행 · 미해결 #59 · 이 로그 |

**문서를 고쳤음 — 프로젝트 지식 재업로드 필요**: `CLAUDE.md` ·
`AI서버_CDU_디지털트윈_프로젝트정리.md` · `project-overview.md` · `collaboration.md`
**넷 전부**.

**다음이 알아야 할 것**

- **5장 표 항목명은 사람이 답할 때까지 그대로다.** 그 전에 5장 본문을 고치지 않는다
- **#59 는 열린 채다** — 코드 11곳(주석이 옛 규칙 8 을 인용한다). 특히
  `leak_massloss.py`·`leak_massloss_thermal.py` 머리 주석의 조건 셋이 전부 무효다
- 「샘」을 배치 실행에 얹는 것은 7.25 C5 의 아홉(스키마 열 · 케이스 축 · 다중 CDU
  전이 코드 부재 등)이 먼저다. **스키마 변경은 사람이 정한다**

ctx: 잔여 토큰 14,875,540 / 15,000,000 (보이는 표시가 「잔여 토큰」이라 그 값을
적는다) · 가장 크게 먹은 것: **`PROCEED.md` 세션 7.26 로그 120행을 통째로 읽은
것** — C3 표·C4 요지·C5 목록이 다 거기 있어 필요하긴 했다 · 줄일 수 있었다고 보는
것: **`project-overview.md` 를 세 번에 나눠 훑은 것** — `grep -n "누출"` 한 번으로
자리를 다 잡을 수 있었는데 절 구조를 먼저 두 번 떠 봤다

## 미해결 목록

> 세션 1-B 시작 시 사람이 정한 것을 반영했다(㉮~㉳). 닫힌 항목도 지우지 않고
> 상태를 표시해 남긴다 — 다음 세션이 이력을 잃지 않게.

| # | 한 줄 | 크기 | 영향 |
|---|---|---|---|
| 1 | **[해소 · 세션 2.5]** 5-1 에 확정 기록됨 — 밸브 Cv(개도 100% 기준, ΔP 하한 3 mAq → Cv 18.72 / 상한 5 mAq → Cv 14.50) · 배관 K값(랙 분기 3.20~4.80). 둘 다 5장 조건의 **역산**이므로 새 가정치가 아니다. 5-1 「밸브 Cv」 항목의 관측(25A 에 1.94 L/s 는 유속 3.48 m/s 로 통상 설계유속보다 높다)을 함께 읽는다. 원문: 밸브 Cv · 배관 K값이 5장 표에 없다 — "ΔP 조건에서 역산"이라고만 적혀 있어 역산 규칙(어느 ΔP·어느 유량 기준)을 사람이 정해야 한다. 1-B는 단일 랙이라 유량을 풀 필요가 없어 5장 정격유량을 그대로 썼다 | M | 세션 3 압력평형(`fsolve`)·유량분배를 못 세운다 — **세션 3 착수 전 필요** |
| 2 | **[해소 · 세션 2.5]** 5-1 에 확정 기록됨 — 양정 하한 20 mAq → H₀=22.40·a=0.003819·b=0.009743 / 상한 30 mAq → H₀=33.60·a=0.005729·b=0.014615 (mAq, Q 는 L/s). 곡선 **형상만** 타 사례에서 차용하고 정격점은 5장 값으로 스케일했다 — 5-1 의 한계(물 기준 곡선·용량 4.4배 차이로 비속도 다름)를 함께 읽는다. 원문: 펌프 특성곡선 계수 H0·a·b 가 5장 표에 실제로 없는 빈칸이다 (형태 `H=H0-aQ-bQ²` 만 있음) — 세션 3 프롬프트 전에 사람이 채운다 | M | 세션 3 펌프 모델을 못 세운다 — **세션 3 착수 전 필요** |
| 3 | **[확정 · 방침 (B)]** 범위값의 대표값을 고르지 않는다. **양 끝을 둘 다 돌린다.** 이유: balance 는 보존법칙이므로 범위 안 어느 값에서도 성립해야 한다. 한쪽 끝에서만 닫히면 값 선택 문제가 아니라 모델 문제다. 1-B에서 2차측 27/30℃ × NTU 2/3 = 4케이스로 적용했다 | M | 해소됨 — 이후 세션도 같은 방침을 따른다 |
| 4 | 5장 표의 "CDU 정격 ~750 kW"가 근사 표기 — 열교환기 정격 750 kW 와 같은 값인지 별개 값인지 문서가 명시하지 않음 | S | 정격 대비 부하율 계산 기준이 갈릴 수 있음 |
| 5 | 5장 표는 의도된 자기정합으로 보인다(랙당 1.94 L/s × 8 ≈ 펌프 15.5 L/s) — 1-B에서 계산 ΔT가 10℃ 부근으로 재현되는지로 확인 | S | 문서 확인만 필요, 코드 영향 없음. **[닫힘 · 세션 5.5-C]** 근거: 세션 5-B 재계산에서 ΔT = **10.3008 K**, 1차측 **32.17/42.47℃** 가 5장 표의 32/42℃ 와 정합한다 — 표의 자기정합이 확인됐다 |
| 6 | **[관측 유지 · 다른 PC clone 시 확인]** 한글 파일명이 git 기본 출력에서 8진 이스케이프로 표기됨. 저장된 파일명 바이트는 정상 UTF-8임을 확인했다(표시 문제). 세션 1-C에서도 **이름·`.gitattributes`·git 설정 어느 것도 건드리지 않았다** — `core.quotepath` 는 개인 설정이라 저장소에 넣지 않는다 | M | 다른 PC·다른 로케일에서 clone 할 때 실제로 문제가 되는지 확인한다. 지금 이 PC의 동작에는 영향 없음. **[닫힘 · 세션 5.5-C]** 근거: **새 PC clone 에서 확인됐다.** 파일명은 UTF-8 NFC 로 정상 저장돼 있고 내용도 정상이다 — 8진 이스케이프는 `core.quotepath` 기본값에 따른 **표시 문제**일 뿐이다. `core.quotepath false` 가 표시를 고치는 것은 시험으로 확인했으나 **개인 설정이라 저장소에 넣지 않는다** |
| 7 | **[세션 3에서 재검토]** 물성 조회 기준 압력을 표준대기압(101325 Pa)으로 두었다 — 계통 압력이 실제로 등장하는 세션 3에서 다시 본다. 지금 닫지 않는다 | S | 비압축성 유체라 밀도·비열 영향은 작을 것으로 보이나 **확인하지 않았다**. **[닫힘 · 세션 5]** 재검토 기한을 「세션 3」으로 적었으나 3-A·3-A2·3-B 어디서도 다루지 않고 지나간 것을 확인하고, 세션 5에서 코드로 확인했다: 계통 압력은 대기압 대비 **193.6~290.4%**(짐작한 2~3% 가 아니다)이지만 CoolProp INCOMP 백엔드에서 **ρ·cp 는 압력에 정확히 무관**(0.0000%)하고 엔탈피 차만 −0.0138% 이동한다. 5-1 에 확정으로 기록했다 |
| 8 | **[해소 · 세션 2.5]** 파일럿 종료 조건 확정 — **사전 수치 기준을 두지 않는다.** 판정 시점은 **세션 5.5**(시나리오 배치 실행 → 텍스트 데이터셋) 완료 후, 판정 대상은 그 데이터셋과 세션 1~5.5 게이트 기록, 판정 주체는 **사람**이다. 설계데이터가 오지 않는 것을 전제하므로 "데이터 확보량" 형태의 기준은 성립하지 않는다 | L | 해소됨. 세션 6 은 설계데이터가 있어야 열리며 **파일럿의 정상 종착지가 아니다** |
| 9 | **[해소]** `.gitattributes` 추가(`* text=auto` · `*.py text eol=lf`). 확인 결과 저장소·작업트리 모두 **이미 LF** 였고(`git ls-files --eol` → `i/lf w/lf`) 파일 내용은 하나도 바뀌지 않았다 — 경고는 앞으로의 체크아웃 방향에 대한 것이었다 | S | 해소됨. 앞으로 다른 PC에서도 LF 로 고정된다 |
| 10 | **[해소 · 세션 2.5 확인만]** `CLAUDE.md` 절대 규칙 12·「명령어」·「코드 스타일」이 모두 `Python 3.12+` 로 맞춰졌고 `pyproject.toml` 의 `requires-python` 도 `">=3.12"` 다 — 코드와 문서가 정합하다. **3.11·3.13 에서는 여전히 안 돌려봤다**(넓히려면 그 버전에서 전체 테스트를 먼저 돌린다). `[tool.ruff] target-version = py311` 은 더 보수적인 쪽이라 그대로 뒀다. 원문:  세션 2에서 `pyproject.toml` 의 `requires-python` 을 `">=3.12"` 로 좁혔다(검증한 것이 3.12.10 하나뿐). **그러나 `CLAUDE.md` 절대 규칙 12는 아직 "Python 3.11+" 라고 적고 있어 코드와 문서가 어긋나 있다** — 문서 수정 권한이 없어 손대지 않았다. `[tool.ruff] target-version` 도 `py311` 로 남아 있다(더 보수적인 쪽이라 그대로 뒀다) | M | 사람이 `CLAUDE.md` 규칙 12를 맞춰야 한다. 3.11·3.13 에서는 **여전히 안 돌려봤다** |
| 11 | **[사람 소관 · 상시 항목]** 커밋 작성자 `git config user.name`·`user.email`. **작업 PC 가 둘이며 PC 마다 값이 다를 수 있다** — 한 PC 의 값만 적어 두면 다른 PC 에서 틀린다(원 문구가 그랬다). 새 PC 는 사람이 직접 지정했고 지금은 사람 이름·메일로 커밋된다. **코드·세션이 `git config` 를 스스로 설정하지 않는다.** | S | 이력 작성자 표기. **닫지 않는다** — **PC 를 새로 붙일 때마다 확인하는 상시 항목**이다 |
| 17 | **[해소]** `cdu-docs` 폴더 폐기됨. 정본은 저장소 루트 사본 하나뿐이고, 세션 2부터 저장소 밖에서 문서를 가져오지 않는다 | M | 해소됨 |
| 18 | `fluid.py` 의 `REFERENCE_PRESSURE_Pa` 주석이 아직 5-1 을 인용하지 않는다. **[인용 문구 정정 · 세션 5.5-C]** 원 문구는 5-1 을 "표준대기압 101325 Pa · **잠정** · 세션 3 재검토"로 인용했는데, **세션 5 에서 확정으로 바뀌었다**(#7 닫힘 근거: INCOMP 백엔드에서 ρ·cp 가 압력에 정확히 무관, 엔탈피 차만 −0.0138% 이동). 즉 5-1 은 이제 잠정이 아니라 **확정 기록**이다. 세션 1-C 지시 범위가 #12·#14 였고 세션 5.5-C 는 모델 코드 수정 범위 밖이라 **주석은 여전히 건드리지 않았다**(절대 규칙 13) | S | 같은 값의 출처가 두 군데에서 다르게 읽힐 소지. **여전히 열림** — 주석이 5-1(확정)을 인용하도록 고치는 것은 다음 위생 판의 몫이다 |
| 19 | **[문구 정정 · 세션 5.5-C — 코드로 확인했다]** 원 문구는 `model.py` 의 `heat_capacity_ratio`**(Cr=1)** 에 5-1 인용 주석이 없다는 것이었다. **세션 5-B 가 Cr=1 선언(필드·가정)을 전부 제거했으므로 그 형태는 더 이상 없다.** 다만 **이름 자체는 남아 있다** — `hx_effectiveness_counterflow(ntu, heat_capacity_ratio)` 의 인자이고, 값은 `hx_capacity_terms()` 가 매번 양측 물성에서 **유도**한다(선언하지 않는다). `Cr == 1.0` 분기는 일반식의 0/0 특이점 가드로만 남아 있고 주석이 그 취지를 적고 있다. 출처 표기 상태: 대향류 ε 식은 5-1 을 인용하고 있고, Cr 유도 규약은 「세션 5-B」로 인용한다 — **5-1 의 어느 행을 인용하는지가 아직 적혀 있지 않다** | S | **닫지 않는다.** 남은 일은 Cr 유도 규약의 인용을 「세션 5-B」에서 **5-1 의 해당 행**으로 바꾸는 것 하나다. 수치 영향 없음 |
| 20 | **[해소 · 세션 2.5]** 5-1 에 **공급 노드 50% · 환수 노드 50%** 로 확정 기록됨. 왕복을 두 다리로 읽으면 50:50 이 추가 파라미터가 0인 유일한 배분이다 — **물리 가정이 아니라 수치처리 규약**이다 | M | 해소됨. 정상상태 해에는 영향 없고 전이 파형에만 걸린다. **[민감도 확인 완료 · 세션 4]** 시험값 40:60 · 50:50 · 60:40 을 돌렸다. t→∞ 값은 배분비와 무관(편차 <2e-9 K — 5-1 진술이 성립). 전이 파형은 움직인다: t63 이 0.57~0.77 s(M 하한)·7.27~9.83 s(M 상한) 로 **±15% 이동**하고, 50:50 대비 T_return 최대 편차는 2.5e-4 ~ 2.2e-3 K(누출 수준에 비례). **누출 신호의 방향은 세 배분비 전부에서 뒤집히지 않았다** — 세션 4 게이트가 배분비 규약에 의존하지 않는다. 시험값은 5-1·`assumptions.py` 에 넣지 않았고 시험 코드 안에만 있다. **50:50 유지 여부는 사람이 판정한다** |
| 21 | **M 에 열교환기·CDU 내부·랙 콜드플레이트 보유량이 빠져 있다**(5장에 없음, 5-1 한계 기록). 실제보다 M 이 작고 수렴시간도 짧게 나온다 | M | **6장 「수렴시간」 기준을 이 상태로 판정할 수 없다.** 세션 2에서 판정하지 않았고, 설계데이터나 추가 가정치 없이는 이후에도 판정 불가. **세션 2.5 에서 6장 기준 자체가 재정의되었다** — 절대 수렴시간의 "합리성"은 판정하지 않고 τ 가 M 에 비례하고 유량에 반비례하는 **정합성**만 본다. 항목은 여전히 열려 있다(M 의 결손 자체는 그대로다) |
| 22 | `dynamics.py` 가 `model._property_temperature_from_state`(비공개 이름)를 import 한다. cp 평가 규칙을 두 곳에 적지 않으려는 것이고, 세션 2는 `model.py` 수정 금지라 공개화하지 않았다 | S | 위생 항목. 다음 정비 판에서 공개 이름으로 승격하면 된다 |
| 23 | 배관 규격 계열에 따라 M 이 이동한다 — 5-1 기록: KS D 3507 계열(25A 내경 27.5 mm)을 쓰면 약 7% 커진다. **ASME Sch40 으로 계산했고 다른 계열로는 안 돌려봤다** | S | 시간상수가 그만큼 이동. 설계데이터 확보 시 확정 |
| 12 | **[확정 · 대향류]** 프로젝트정리 5-1 에 기록됨. 근거가 "판형 HX 통례"에서 **"5장 온도 역산"**(대향류·NTU 2·2차측 27℃ → 32.15/42.44℃ 가 5장 32/42℃ 와 일치)으로 바뀌었다. 세션 1-C에서 `model.py` 주석·출처만 교체했고 **ε 식·값은 건드리지 않았다** | M | 해소됨 — 계산은 처음부터 대향류였으므로 수치 변화 없음(전후 동일성으로 확인) |
| 13 | **[세션 5로 이월]** 2차측 유체 미정. 잠정적으로 1차측과 동일 냉각액 + 유량비 1:1 → Cr = 1 (프로젝트정리 5-1 에 "미정"으로 기록됨). 2차측이 블랙박스인 동안은 판정 불가(절대 규칙 7) | M | Cr 이 1이 아니면 ε 값이 바뀐다. **[닫힘 · 세션 5]** 5-1 「2차측 유체」를 **1차측과 동일한 PG25** 로 확정했다 — 동일 유체면 유량비 1:1 에서 앞 게이트가 전부 불변이고 추가 파라미터가 0이다. **단 Cr=1 은 유량에서 유도되지 않는다**(미해결 #33 참조) |
| 14 | **[확정 · bulk_mean]** 프로젝트정리 5-1 에 **수치처리 규약**으로 기록됨. `cp_rule` 인자는 재현·전환용으로 그대로 남겼다. 규칙별 잔차(bulk_mean 0.005% 통과 · supply 0.309% 실패 · return 0.315% 실패)도 5-1 에 기록됐다 | M | 해소됨 — 다만 규칙을 바꾸면 1-B 판정이 뒤집힌다는 사실은 계속 유효하다 |
| 15 | **[여전히 열림]** 랙·HX 를 **독립적으로 푼 뒤 대조**하는 검증이 아직 없다. 세션 2의 정상상태 대조(t→∞ vs 1-B)는 계산 *경로*는 다르지만 동적 모델의 평형점이 설계상 1-B 와 같은 방정식이라 **구현 일관성 검사**에 그친다 | M | "1-B·세션 2 통과"를 모델 전체의 보존성 검증으로 읽으면 과대해석이다 |
| 16 | **[해소]** 갱신본 `CLAUDE.md` 「명령어」 절 127행에 `.venv/Scripts/python.exe -m cdu_simul.model` 이 들어갔음을 확인했다 | S | 해소됨 |
| 24 | **계통 잔여저항 배정이 유량분배를 지배한다.** 5-1 규칙대로 배정하면 잔여저항(HX 1차측 + CDU 내부배관 + 헤더)이 **총양정의 60~83%** 를 차지한다. 세션 3 결과를 해석할 때 이 사실을 함께 적는다 — 수렴·비발산 게이트 통과를 "유량분배 값이 타당하다"로 읽지 않는다. 잔여저항이 랙 공통 경로에 있으므로 정상상태 랙 간 유량은 균등해지고, 랙 간 상호작용은 **펌프곡선을 통해서만** 생긴다. **[번호 주의]** 세션 2.5 프롬프트는 이 항목을 `#23` 으로 지정했으나 `#23` 은 세션 2에서 이미 쓰였다 — 중복을 만들지 않으려고 `#24` 로 넣었다 | L | **세션 3 게이트 해석의 전제.** 잔여저항 배정이 바뀌면 유량분배 결과가 통째로 바뀐다 |
| 25 | **등가길이 기반 M 산출은 과대 방향 오차를 갖는다.** 등가길이는 부속(엘보·밸브 등) 손실을 직관 길이로 환산한 **압력강하용 개념**이라 실제 배관 길이보다 길다 — 그 길이로 보유수량을 내면 M 이 실제보다 크게 나온다. 현재 5-1 「계통 보유수량 M」 항목에는 **과소 방향 오차(HX·CDU 내부·콜드플레이트 누락)만** 적혀 있다. 부호가 반대인 오차가 하나 더 있다는 문구 보완 후보 | S | 두 오차의 방향이 반대라 상쇄 정도를 모른다 — #21(수렴시간 판정 불가)의 근거를 더 강하게 만들 뿐 판정을 가능하게 하지는 않는다. **문서 문구 보완 후보이며 이 판에서는 고치지 않았다**(지시 범위 밖) |
| 26 | **세션 5.5 신설 — 시나리오 배치 실행 · 데이터셋화.** 정상/이상/누출 다수 케이스를 반복 실행해 텍스트 데이터셋을 낸다. 게이트: **시나리오 간 상태 이월 없음**(collaboration.md 결함유형 ④) · 전 행에 가정치 출처와 "가정값 기반 — 실측 아님" 표기. **게이트 표 반영 완료**(`CLAUDE.md` · `project-overview.md` 체크리스트) | L | **파일럿 종료 판단의 대상**이 되는 산출물이다(#8). **[닫힘 · 세션 5.5-C]** 근거: 세션 5.5-A(설계·계수·실행시간 측정)·5.5-B(배치 실행 → 데이터셋 생성 · 게이트 판정)로 **이행이 완료**됐다 |
| 27 | **SG 가 5-1 기록(1.0124)과 CoolProp 값(1.01147)에서 0.09% 다르다.** 5-1 의 밸브 Kv·배관 K 는 5-1 의 SG 로 역산된 값이므로, `hydraulics.py` 가 정격점에서 재현하는 ΔP 가 5장 표값과 0.1% 이내로 어긋난다(분기 1.995 vs 2.000 mAq · 밸브 2.993 vs 3.000 mAq). 세션 3-A 는 **어느 쪽도 고치지 않았다** — 5-1 값 전사가 규칙이다 | S | 유량분배 결과에 0.1% 미만으로 전파된다(운전점 편차 +0.019~+0.046% 안에 이미 섞여 있다). 5-1 의 SG 출처를 사람이 확인하거나, CoolProp 값으로 Kv·K 를 재역산할지 정해야 한다 — **코드가 임의로 정하지 않는다**(절대 규칙 1). **[닫힘 · 세션 3-A2]** 사람이 5-1 을 값에서 **역산 규칙**으로 바꿨고, SG 정의를 `fluid.coolant_specific_gravity` 한 곳으로 모아 Kv·K 를 코드가 재역산하게 했다. 그 결과 정격점 ΔP 가 5장 표값(2/3 · 3/5 mAq)을 **잔차 1e-15 mAq 로 항등 재현**한다. SG 기준밀도 1000 kg/m³ 는 `[정의값]` 등급(5-1 빈칸 처리 순서 2번)이다 |
| 28 | **수력의 물성 온도가 아직 5장 고정값(37℃ 벌크평균)이다.** `hydraulics.property_temperature_C()` 는 5장 1차측 공급·환수 온도를 고정으로 읽는다. 열모델이 붙으면 이 온도가 풀린 상태에서 나와야 하고, 밀도가 유량에 의존하므로 **수력-열 연립**이 된다 | M | **세션 3-B 의 첫 결정 사항.** 결합 방식(연립 대 순차 반복)을 정하기 전에는 3-A 의 유량값을 열모델에 그대로 넘기면 안 된다. **[닫힘 · 세션 3-A2]** 고정 37℃ 를 제거했다 — `valve_dp_mAq`·`branch_dp_mAq`·`solve_flow_distribution` 이 물성 온도를 **기본값 없는 필수 인자**로 받는다. 5-1 에 「수력 계산의 물성 평가 온도 = 1차측 벌크 평균온도」 행을 추가해 규약을 명문화했다. **연립 대 순차 반복의 선택은 여전히 3-B 의 것이다** — 이 항목이 닫힌 것은 "온도가 인자로 들어올 자리가 생겼다"까지이고, 어떤 온도를 넣을지는 3-B 가 정한다 |
| 29 | **세션 3 게이트의 절반만 봤다.** 3-A 는 `fsolve` 수렴(8조합 `ier=1`)만 확인했고 **극단 케이스(부하 0/최대) 비발산은 안 봤다** — 부하가 온도를 통해 들어오므로 열모델이 필요하다. 부하 0 은 밸브 개도 0 이 아니라 발열량 0 이다(개도 0 은 Kv=0 이라 ΔP 정의 불가이며 `valve_dp_mAq` 가 `ValueError` 를 던진다) | M | **세션 3 게이트를 "통과"로 적지 않는다.** 3-B 가 나머지 절반을 판정해야 세션 3이 닫힌다. **[닫힘 · 세션 3-B]** 열결합 상태에서 **32조합 전수**로 둘 다 판정했다 — ㉠ 수력·열·결합 고정점 세 `fsolve` 가 전부 `ier=1`, ㉡ 부하 0%/100% 에서 정상상태·동적 모두 비발산. 부하 0 은 6장이 발산 검사용으로 명시한 극단값이라 5장 위반이 아니다 |
| 30 | **잔여저항만 온도 스케일을 따르지 않는다.** 분기 ΔP(K·ρv²/2)와 밸브 ΔP(SG·(Q/Kv)²)는 밀도에 비례해 움직이는데, 계통 잔여저항은 5-1 이 mAq/(L/s)² 계수로 정의하므로 정격 물성에서 역산한 뒤 고정된다. 정격 온도(37℃) 밖에서는 세 저항의 스케일이 어긋난다 | M | **세션 3-A2 는 고치지 않았다** — 5-1 의 잔여저항 규칙을 이 판에서 바꾸지 않기로 했기 때문이다(지시 범위). 열모델이 붙어 온도가 실제로 움직이는 **세션 3-B 에서 사람이 정한다**: 잔여저항도 K형 저항으로 보아 ρ 스케일을 태울지, 5-1 정의대로 고정할지. 정격점 근방에서는 영향이 작지만 극단 케이스에서 커질 수 있다. **[닫힘 · 세션 3-B]** 사람이 5-1 에 「계통 잔여저항의 물성 의존 = ρ 에 비례」를 확정했고, 코드가 정격 물성 기준 계수를 ρ(T)/ρ(37℃) 로 스케일한다. 세 저항이 같은 온도 스케일을 따르며 **추가 파라미터는 0**이다. 정격 온도에서 비가 1 이므로 8조합 표가 세션 3-A2 와 바이트 단위로 동일함을 격리 확인했다 |
| 31 | **M 의 8랙 해석이 없다.** 5-1 이 등가길이를 「랙 1개 회로의 왕복 전체」로 읽으므로 `holdup_bounds()` 가 내는 M 은 **랙 1개 회로분**이다. 랙이 8개가 되면 계통 전체 보유량을 어떻게 읽어야 하는지(분기는 8벌·헤더는 공용) 5장에도 5-1 에도 답이 없다 — 세션 3-B 는 **새 숫자를 만들지 않고 세션 2 값을 그대로 썼다**(절대 규칙 1) | M | **τ·t63·t95 의 절대값을 8랙에서 해석할 수 없다.** 결과 표에 그 문구를 넣었다. 게이트에는 영향이 없다 — 정상상태는 M 에 의존하지 않고, 전이의 부호도 M>0 이면 바뀌지 않는다(방향성·비발산 둘 다 M 무관). #21(M 에 HX·CDU 내부·콜드플레이트 누락)과 **별개의 결손**이며, 둘 다 열려 있는 한 6장 「수렴시간」은 판정 불가다. 사람이 배분 규칙을 정해야 한다. **[닫힘 · 세션 5.7]** 사람이 배분 규칙을 정했다 — 5-1 M 의 양 끝(전부 25A 하한 · 전부 80A 상한)을 **둘 다 8배**로 읽는다(추가 숫자 0 · 8 은 `SCENARIO.racks_per_cdu`). `holdup_bounds()` 가 이제 **8랙 계통 전체** M 을 낸다: 하한 11.2756 → **90.2050 kg** · 상한 144.6976 → **1157.5805 kg** (배수 정확히 8.0000000000). **정상상태 해는 불변**(`model`·`hydraulics` 표 바이트 단위 동일 · 전이 종점 온도 30.098/42.469 ℃ 그대로) — 5-1 「M 은 정상상태 해에 영향하지 않는다」가 코드에서 성립했다. **τ·t63·t95 가 정확히 8.000000배 이동**했다(부하 스텝 4 · 누출 스텝 6 전부). 정확히 8배인 까닭도 안다 — 적분 격자가 τ 단위이고 2노드 ODE 가 M 에 선형이라 궤적 전체가 스케일된다. **그러나 6장 「수렴시간」은 여전히 판정 불가다** — #21(M 결손) · #25(등가길이 과대) · #23(배관 규격 계열)이 그대로 열려 있고 #21 과 #25 는 방향이 반대라 상쇄 정도를 모른다. **이 항목이 닫힌 것은 「8랙 계통의 M 을 어떻게 읽는가」 하나이고, M 의 정확도가 좋아진 것이 아니다** |
| 32 | **밸브 선형 개도 특성이 아직 시험되지 않았다.** 5-1 「밸브 개도 특성」이 선형을 규약으로 확정하면서 "개도가 변수가 되는 시점(세션 4)에 재검토"라고 적었으나, 세션 4의 누출은 **K값 변화**이고 개도는 80% 고정이었다 — 재검토할 자극이 오지 않았다 | S | 선형 대 등비율의 차이는 개도가 실제로 움직일 때만 드러난다. 개도 제어가 들어오는 판(현재 계획에 없다)이나 설계데이터 확보 시 재검토한다. **지금 판정할 근거가 없다** |
| 33 | **5-1 두 행이 서로 어긋난다 — Cr=1 인가, 배분에서 유도하는가.** 5-1 「2차측 유체」는 "유량비 1:1과 합쳐 **Cr=1**" 이라고 선언하고, 5-1 「공유 2차측 결합 방식」은 총 2차측 유량을 정격으로 고정하고 1차측 유량에 비례 배분하라고 한다. 배분에서 Cr 을 유도하면 **어떤 배분 규칙으로도 정확히 1 이 나오지 않는다**: ① 총 2차측이 정격 15.5×2 로 고정인데 1차측 운전점은 15.5056 이라 2차:1차=0.99964 ② 설령 부피유량이 같아도 양쪽 온도가 달라 ρ·cp 가 다르므로 Cr=0.99863 (5장 「유량비 1:1」은 부피 기준이다) | L | **세션 5 는 배분에서 유도하는 쪽을 구현했다** — 그래야 세션 5 게이트(「CDU 간 연동 수렴」)가 공허하지 않기 때문이다. Cr=1 을 고수하면 배분이 열해에 영향하지 않아 **연동 자체가 사라진다**. 그 결과 대칭 케이스가 세션 4 단일 CDU 결과를 재현하지 못한다(32조합 최대 편차 **0.0245 K** — 세션 5 C4 의 기대와 다르다). **사람이 정해야 한다**: (가) 배분 유도를 유지하고 5-1 「2차측 유체」의 Cr=1 문구를 고치는가, (나) Cr=1 을 고수하고 연동 경로를 다른 것(예: 2차측 온도 결합)으로 바꾸는가. (나)는 냉각탑 파라미터가 필요해 5장에 근거가 없다. **[닫힘 · 세션 5-B]** 사람이 (가)를 골랐다 — Cr 을 선언하지 않고 물성에서 유도하고, 5장 「1:1」을 **부피유량비**로 읽어 2차측을 정격 15.5 L/s 에 고정하며, 이를 **단일 CDU 에도 소급**한다. 그 결과 대칭 재현 편차가 0.0245 K → **0.000e+00 K** 가 됐고, 세션 1-B~5 다섯 게이트를 새 전제 위에서 다시 판정해 전부 통과했다. **대가: 세션 1-B~5 의 기록 수치가 소급 이동했다** — 세션 5-B 이전 로그의 숫자를 이후 것과 나란히 읽지 않는다 |
| 34 | **다중 CDU 전이 행에서 누출이 두 CDU 모두의 랙 0 에 걸린다.** ⑴ 관측: `PlantLoadStepCase` 가 CDU 마다 다른 누출 템플릿을 받지 않아 다중 CDU 전이 시나리오는 **누출점이 2개**다. ⑵ 5-1 「누출 주입 지점」은 "랙 1개에 주입한다 · 전 랙 동시 누출은 5장에 근거가 없어 돌리지 않는다" 이므로 **현재 다중 CDU 전이 행은 그 규정 밖**이다. 또한 양쪽이 대칭이라 2차측 배분이 움직이지 않아, 세션 5 C7 이 관측한 "A 누출 → B 가 좋아진다" 연동이 **이 행에서는 상쇄된다**. ⑶ 선택지(사람이 정한다): **(가)** `plant.py` 가 CDU 별 누출 템플릿을 받게 고치고 `leak_cdu_index` 열을 추가한 뒤 재생성(약 28분) · **(나)** 다중 CDU 전이 행을 데이터셋에서 빼고 한계로 기록 — 재생성 불필요, 다만 연동 전이 데이터가 사라진다. ⑷ **이 항목이 정해지기 전에는 종료 판정 재료가 완결되지 않는다** | M | `regime=transient` 이고 `cdu_config=dual_asymmetric` 인 행 전체 (전체 310,016행 중 205,824행). **함께 읽을 것**: 단일 CDU 전이와 다중 CDU 전이는 **자극 형태가 다르다** — 전자는 누출이 **스텝으로** 들어오고(부하 불변), 후자는 누출이 **t=0 이전부터 있고** 부하가 스텝이다. `cdu_config` 로 구분해 읽어야 하며, 두 전이를 같은 자극으로 보면 안 된다. **[닫힘 · 세션 5.5-D]** 사람이 **(가)** 를 골랐다 — `plant.PlantLoadStepCase` 가 CDU 별 템플릿(`templates`)을 받게 하고, 누출을 CDU 0 의 랙 0 **하나에만** 걸고, `leak_cdu_index` 열을 추가해 재생성했다. 대칭 경로는 지우지 않았다(`templates=None` 이면 종전과 `==` 로 같다 — 테스트가 고정한다). **연동이 실제로 살아났다**: 부하가 그대로인 CDU B 의 2차측 몫이 누출 수준에 대해 단조 증가(+50% 에서 **+0.01908 L/s**)하고 환수온도가 단조 감소(**−0.01064 K**)한다 — 세션 5 C7 이 정상상태에서 관측한 것(+0.01905 L/s · −0.012649 K)과 **방향이 같고 배분 이동은 세 자리까지 일치**한다. 종전 양쪽 누출에서는 배분 이동이 **+1.9e-05 L/s** (≈0)로 상쇄됐고 B 의 온도는 **오히려 올랐다**(+0.021 K — B 자신에게도 누출이 있었으므로). ⑵ 의 자극 형태 차이는 **그대로 남아 한계로 이관했다**(`dataset_report` 열린 한계) |
| 35 | **의존성 버전이 `pyproject.toml` 에 고정돼 있지 않다.** ⑴ 관측: 새 PC 가 `pip install` 할 때 **그 시점 최신**을 받는다. 실제로 `ruff` 가 0.16.4 → 0.16.5 로 올라왔다(lint 도구라 수치 무관). 런타임 4종은 **우연히** 같았다 — `numpy` 2.5.2 · `scipy` 1.18.1 · `CoolProp` 8.0.0 · `pandas` 3.0.5. ⑵ 참이면 같이 보여야 할 것: `CoolProp`·`scipy` 가 갈리면 물성·solver 의 마지막 자리가 이동해 **데이터셋 재생성본이 달라진다.** 세션 5.5-B 인계가 "같은 버전이면 바이트 단위로 같다"고 적었는데 **그 전제를 보장하는 장치가 없다.** ⑶ 가설(사람이 정한다): `pyproject.toml` 에 런타임 4종을 **고정**하거나, **현재 버전을 기록만 하고 재생성 시 대조**한다. 고정하면 보안 패치를 못 받는다. **세션 5.5-C 는 `pyproject.toml` 을 고치지 않았다 — 기록만 했다**. **[갱신 · 세션 5.7 — 닫지 않는다]** 사람이 정했다: **고정하지 않고 기록한다.** `pyproject.toml` 을 고치지 않았고(고정하면 보안 패치를 못 받는다), 대신 `dataset_report` 가 **생성 환경 절**을 두어 Python·플랫폼·numpy·scipy·CoolProp·pandas 버전을 싣는다. 52열본은 Python 3.12.10 · Windows 11 · numpy 2.5.2 · scipy 1.18.1 · CoolProp 8.0.0 · pandas 3.0.5 에서 생성됐다. **기록은 대조를 가능하게 할 뿐 재현을 보장하지 않으므로 이 항목은 열린 채로 둔다** — **본 프로젝트 전환 시 고정한다** | M | 데이터셋 재현성의 전제. 세션 5.5-B 가 판정한 게이트 A·B·C 는 전부 `==` 비교이므로, 재생성 시 버전이 갈리면 같은 코드에서도 어긋날 수 있다 |
| 36 | **K값 근사 누출과 질량손실 누출이 같은 방향을 가리키지 않는다(수력 · 세션 5.6 관측).** ⑴ **관측**: 8조합 × 누출 크기 4수준 × 배치 6 전수(272회 `fsolve`, 전부 `ier=1`)에서 **총유량**은 K 근사가 감소(−2.5e-02 ~ −6.4e-03 L/s)·질량손실이 증가(+3.0e-03 ~ +1.5e-01 L/s)로 **정반대**이고, **누출랙 통과유량**도 K 근사 감소·질량손실 증가로 **정반대**다. **펌프 양정**은 부호가 배치에 따라 갈린다(펌프가 공급유량을 보면 하강, 환수유량을 보면 상승 — K 근사와 같은 부호). 또 질량손실 누출은 5-1 이 헤더를 **저항 0 의 공통 노드**로 두는 한 **랙에 국소화되지 않아**(랙 간 비대칭 2.220e-16 L/s) ⑶ 과 ⑷ 가 같이 움직인다 — K 근사가 ⑶↓/⑷↑ 로 갈라지는 것과 구조가 다르다. 배치를 고정하면 부호는 5장 범위 양 끝 전 조합에서 **뒤집히지 않는다** — 가르는 것은 가정치 범위가 아니라 배치다. ⑵ **참이면 같이 움직여야 할 것**: 데이터셋의 누출 신호는 5장이 정의한 **대용(proxy)** 의 거동이므로, 이 데이터로 학습한 모델이 **실제 누출에서 반대 부호를 볼 수 있다.** 특히 「총유량 감소 = 누출」과 「랙별 유량 하락으로 누출 랙을 특정」 두 규칙은 질량손실 쪽에 대응물이 없다. 온도 신호는 **보지 않았다**(이 판은 수력 한정 — 보충수 온도가 5장에 없다). ⑶ **가설(사람이 정한다)**: **(가)** K 근사를 유지하고 데이터셋·5-1 에 「누출은 대용이며 총유량·랙유량 부호가 실제와 반대일 수 있다」를 **한계로 명시**한다(재생성 불필요) · **(나)** 5-1 에 잔여저항의 누출점 상·하류 배분과 펌프 재고 보충 위치를 확정하고 질량손실을 데이터셋에 넣는다(규칙 8 개정 + 재생성 필요 + 보충수 온도라는 5장 밖 숫자가 열모델에서 필요해진다) · **(다)** 두 방식을 라벨로 병기한다. **이 판은 모델·데이터셋·5-1 을 고치지 않았다**(§7)  **[추가 · 세션 5.7-D — 열·전이까지 봤다]** 5.6·5.6-B 의 수력 한정이 풀렸다(5-1 「보충수 처리」 확정). **열에서도 부호가 갈린다**: 누출랙 출구온도와 CDU 환수온도가 K 근사(**+**)와 질량손실(**−**)에서 **정반대**이고 1,536건 전 조합에서 뒤집히지 않는다(CDU 공급온도는 K 근사 쪽만 갈려 「정반대」라고 말할 수 없다). **질량손실은 랙에 국소화되지 않으므로 전 랙 출구온도 = 환수온도**여서 「랙별 온도로 누출 랙을 특정한다」는 규칙에 대응물이 없다. **수력이 못 보는 것을 열이 본다** — 세션 5.6 관측 ④ 에서 다섯 양이 전부 0 이던 `g=0/펌프=공급` 배치에서도 환수온도가 −7.368e-02 ~ −1.886e-03 K 로 살아 있고, **부호는 배치 6 전부에서 같다**(크기만 약 2.3배 갈린다). 까닭은 질량손실이 배치와 무관하게 **HX 통과유량을 줄이기** 때문이며 5.6-B 의 Δ 와 같은 성질이다. 에너지 balance 는 **누출 엔탈피 항을 넣어야 닫힌다**(빼면 최대 1.2129% · 넣으면 0.000062%) — 모사가 자기정합적이다. **τ ∝ M 이 정확히 성립**(τ 비 = t63 비 = M 비 = 12.832779). 다중 CDU 는 `Q_ret` 읽기에서 **K 근사와 방향이 같다**(#39 참조). **이 판도 모델·데이터셋·5-1 을 고치지 않았다** — (가)/(나)/(다) 선택은 여전히 사람 몫이고, 이제 그 선택의 근거가 수력뿐 아니라 열·전이까지 있다.| L | **데이터셋 전체의 누출 라벨 해석**에 걸린다 — 세션 4·5·5.5 의 누출 신호 판정은 「K 근사 대용의 거동으로서」 여전히 유효하고 **하나도 뒤집히지 않는다**(게이트를 재판정하지 않았다). 걸리는 것은 그 신호를 **실제 누출의 지표로 읽을 때**다. 5장·5-1 이 정하지 않는 구조 자유도 둘(잔여저항의 환수측 몫 g · 펌프가 보는 유량 위치)이 부호를 가르므로, **미해결 #24(잔여저항 분해 금지)가 닫히기 전에는 ⑵ 의 부호를 정할 수 없다** — 설계데이터가 필요하다. **[추가 · 세션 5.6-B]** 공급−환수 유량 불일치 Δ 를 같은 전수로 확인했다: Δ 는 구조 자유도 둘에 **흔들리지 않고**(배치 6 편차 **1.776e-15 L/s**) 누출 크기에 **엄격히 단조**이며 공급유량 대비 **최대 1.211%** 다. 세션 5.6 관측 ④ 에서 다섯 양이 전부 0 이던 `g=0/펌프=공급` 배치에서도 **Δ 만 살아남는다**(+2.838e-02 ~ +1.877e-01 L/s). 다만 Δ = Q_leak 은 `solve_massloss` 의 정의를 되읽은 **구성상 항등식**이라 질량보존의 물리적 검증이 아니다. **[추가 · 세션 5.7]** 사람이 정했다 — **Δ 열을 데이터셋에 넣지 않는다**(현재 데이터셋은 K 근사 전용이라 전 행 정확히 0 이고 정보량이 0 인 열이 하나 느는 것뿐이다). 대신 **`leak_model` 열**(전 행 `K_approx`)을 넣어 이 데이터셋이 어느 모사인지 행마다 싣고, `dataset_report` ③절이 위 관측 전부를 **종료 판정 재료**로 출력하며, **#24 문구를 「신호의 존재 여부까지 지배한다」로 보강**했다. **(가)/(나)/(다) 선택은 여전히 사람 몫이며 이 판은 모델·데이터셋·5-1 을 고치지 않았다.** 5-1 「누출 시나리오」·「누출 주입 지점」의 한계 문구 추가는 **사람이 직접 한다**(정본 수정 · 재업로드 필요). **[갱신 · 세션 5.7-C]** 사람이 정했다 — **파일럿 안에서 실행되는 것은 (가)**(K 근사 유지 + 한계 명시)이고 5-1 문구 추가로 실행된다. **(나)로 가는 데 필요했던 것 중 보충수 온도는 오늘 닫혔다**: 이 과제의 목적이 미량 누출 조기감지(1장)이고 미량 구간에서는 밀폐루프 보충 로직이 작동하지 않으므로 **보충수는 조기감지 시나리오에 정의상 등장하지 않는다** — 새 가정치가 아니라 1장 목적과 5장 누출 크기에서 따라 나오는 **역산**이다(5-1 빈칸 처리 순서 1번). 남은 것은 **#24 잔여저항 분해**와 **계통 정압**(팽창탱크 용적·초기 충전압 — 5장 밖 숫자 둘)이며 **둘 다 설계데이터 대기**다. **최종 선택은 여전히 사람 몫이다.** **이 닫힘이 세션 5.6 §3 의 전제를 바꾼다** — 5.6 이 수력 한정을 택한 이유가 보충수 온도였고, 그것이 사라졌으므로 **5.7-D 가 열·전이를 볼 수 있다** |
| 37 | **시연·분석용 「정상」 표본에 누출이 섞여 있는지 확인되지 않았다.** ⑴ **관측**: 데이터셋의 `stimulus_kind=none`(regime=steady) **1,280행**에 누출 K값이 걸린 정상상태 케이스가 섞여 있는지 **확인하지 않았다**. `stimulus_kind` 는 세션 5.7 이 `regime`·`cdu_config` 에서 읽어 붙인 라벨이고 **「자극이 없다」는 뜻이지 「누출이 없다」는 뜻이 아니다** — 정상상태 행에는 누출 축(`leak_level_percent` 0/5/20/50)이 그대로 살아 있다. ⑵ **참이면 같이 보여야 할 것**: 시연이나 분석에서 「정상」으로 고른 표본에 누출 행이 들어가면 **정상 대비 누출 대조가 성립하지 않는다.** 세션 4·5·5.5 의 게이트는 조합 안에서 정상 해를 따로 풀어 비교하므로 **영향받지 않는다** — 걸리는 것은 **CSV 를 그대로 골라 쓰는 쪽**이다. ⑶ **가설**: 누출 관련 열(`leak_level_percent == 0` 또는 `leak_cdu_index` 빈 칸)로 걸러진다. **다음 판에서 CSV 를 읽어 확인한다** — 세면 되는 일이라 새 숫자가 필요 없다  **[확인 완료 · 세션 5.7-D — 닫지 않는다]** CSV 를 읽어 **셌다**(데이터셋을 고치지 않았다). **가설이 맞았다 — 섞여 있다.** `stimulus_kind=none` 은 `regime=steady` 1,280행과 **정확히 일치**하고(다른 regime 에 `none` 은 없다), 그 1,280행의 `leak_level_percent` 는 **0/5/20/50 이 각각 320행씩 정확히 4등분**돼 있다. 즉 **960행(75.0%)에 누출이 걸려 있다.** 누출 행의 `leak_cdu_index` 는 전부 **0**(5-1 「누출 주입 지점」·#34 대로 CDU 0 에만), `cdu_config` 는 dual_asymmetric 384 · dual_symmetric 384 · single 192, `leak_model` 은 전부 `K_approx`, `dataset_version` 은 `session-5.7` 이다. **「자극이 없다」와 「누출이 없다」가 다르다는 것이 수치로 확인됐다** — 정상 표본은 `stimulus_kind=none` 이 아니라 **`leak_level_percent == 0`** 으로 걸러야 하고, 그것으로 320행이 나온다. **리포트나 열 설명에 이 문구를 넣을지는 사람이 정한다**(§7) — 이 판은 확인만 했고 `dataset_report`·데이터셋·열 정의를 하나도 건드리지 않았다. **[문구 반영 · 세션 5.8 — 여전히 닫지 않는다]** `dataset_report` 에 「정상 표본을 어떻게 고르는가」 절을 넣었다: `stimulus_kind` 는 **자극의 종류**를 뜻하며 누출 유무를 뜻하지 않는다 · 정상 표본은 **`leak_level_percent == 0`** 으로 거른다 · 그 결과가 **320행**이다 · `stimulus_kind=none` 으로 거르면 누출 행이 **75% 섞인다**. **행수는 하드코딩하지 않고 `enumerate_specs()` 에서 센다**(`_steady_rows_by_leak_level`) — 다중 CDU 시나리오는 CDU 마다 한 행을 내므로 시나리오 수가 아니라 **행 수**를 센다(5-1 스키마 규약). 데이터셋 규모가 바뀌면 문구의 숫자도 같이 움직인다. **데이터셋·열 정의를 고치지 않았다**(리포트 문구만). 열 하나를 더 걸지 여부는 여전히 사람 몫이라 **닫지 않는다.**| S | `regime=steady` 인 1,280행을 「정상」으로 읽는 모든 용도. **데이터셋 자체의 결함이 아니라 읽는 법의 문제**이고, 필요하면 열 하나를 더 거는 것으로 끝난다. 세션 5.7 이 붙인 `stimulus_kind` 라벨이 **누출 유무를 뜻하지 않는다**는 것을 리포트나 열 설명에 적을지는 확인 뒤에 정한다 |
| 38 | **질량손실 전이에서 상수 M 근사가 관측 창 뒷부분을 지탱하지 못한다.** ⑴ **관측**(세션 5.7-D · 768 적분): 보충이 없으므로 계통이 줄어드는데 모델은 M 을 상수로 둔다. 소실률(Q_leak × 시간 × ρ / M)이 **전체 창 30τ 에서 21.89~36.37%**, **t63 까지는 0.62~1.87%** 다. ⑵ **참이면 같이 보여야 할 것**: 30τ 파형의 뒷부분은 실제보다 M 이 큰 계통의 응답이므로 **종점값과 후반 기울기를 그대로 읽으면 안 된다.** 반면 **부호는 t63 구간에서 이미 정해지므로 세션 5.7-D 의 A~G 부호는 이 유보에 흔들리지 않는다.** ⑶ **가설(사람이 정한다)**: **(가)** 관측 창을 t63 근방으로 줄여 상수 M 을 유지한다(새 숫자 0개) · **(나)** M(t) 를 적분한다 — **초기 보유량**이라는 처리가 새로 붙고, 계통이 비는 지점을 정해야 한다 · **(다)** 팽창탱크를 넣는다 — 용적·초기 충전압이라는 **5장 밖 숫자 둘**이 필요하다(5-1 「보충수 처리」 한계와 같은 둘이다). **이 판은 (가)~(다) 중 아무것도 고르지 않았고 M 을 상수로 둔 채 소실률을 표로 냈다** | M | **질량손실 전이의 정량값**에 걸린다. 세션 5.7-D 의 부호 판정과 τ ∝ M 확인에는 걸리지 않는다(둘 다 t63 구간에서 정해진다). K 근사 데이터셋은 질량이 보존되므로 **영향받지 않는다** — 걸리는 것은 #36 이 (나)·(다)로 갈 때뿐이다 |
| 39 | **공유 2차측 배분이 보는 「1차측 유량」이 누출 CDU 에서 처음으로 갈린다.** ⑴ **관측**(세션 5.7-D · 결합 3,840 해): 5-1 「공유 2차측 결합 방식」은 배분을 「1차측 유량에 비례」로만 정한다. 밀폐루프에서는 공급=환수라 문구가 갈리지 않았는데, 질량손실 누출 CDU 는 ṁ_sup ≠ ṁ_ret 다. 두 읽기의 결과가 **다르다**: `Q_ret`(HX 통과유량)로 읽으면 누출 없는 CDU B 의 2차측 몫이 **전 조합에서 증가**하고 환수온도가 **전 조합에서 감소**해 세션 5.5-D 의 K 근사 관측과 **방향이 같다**(+1.142e-02 ~ +9.457e-02 L/s · 엄격 단조 위반 0건). `Q_sup`(랙 통과유량)로 읽으면 **두 부호가 다 갈린다**(−7.586e-02 ~ +1.337e-04 L/s). ⑵ **참이면 같이 보여야 할 것**: 다중 CDU 누출 신호의 **연동 방향**이 이 읽기에 걸린다. `Q_sup` 읽기에서는 「A 가 누출이면 B 가 좋아진다」가 성립하지 않는 조합이 있다. ⑶ **가설(사람이 정한다)**: `Q_ret` 가 물리적으로 정해진 읽기다 — **열교환기를 실제로 지나는 것이 환수유량**이고, 세션 5.7-D 의 열모델도 같은 이유로 C_1차 에 ṁ_ret 를 쓴다. 그러면 **새 숫자가 아니라 5-1 문구의 해석**이고 5-1 에 한 줄 덧붙이면 닫힌다. 다만 **이 판은 값을 고르지 않고 양 끝을 둘 다 돌렸다**(g 와 같은 취급) | S | **#36 이 (나)·(다)로 갈 때의 다중 CDU 행**에 걸린다. 현재 데이터셋은 K 근사(질량 보존)라 공급=환수이므로 **영향받지 않는다**. 5-1 문구 보완은 정본 수정이라 **사람 몫**이다 |
| 40 | **현재 2노드 전이 파형이 노드 수에 수렴하지 않는다.** ⑴ **관측**(세션 5.8 · 1단 144 + 2단 768 적분): 각 다리를 N/2 탱크 직렬로 나눈 N-CSTR 로 N ∈ {2,4,8,16,32,64} 를 돌렸다. **정상상태는 N 에 완전히 불변**이고 **누출 신호의 부호도 전 조합 384 에서 뒤집히지 않는다**(최대 차이 7.5e-9 K · 경로 역부호 이탈 6.4e-14 K). **그러나 전이 파형은 수렴하지 않는다** — 직전 N 대비 파형 최대 편차가 N=4→64 로 16배 세분에도 3.05e-03 → 2.44e-03 K 로 **1.25배**밖에 줄지 않고 순신호 대비 **9.5~24.8%** 를 유지한다. N=2 대 N=16 은 순신호 대비 **31.7~58.0%** · t63 이 **−11.7%~+80.9%** 이동하며, 이동 **방향이 조합에 따라 갈린다**(줄기도 늘기도 한다). ⑵ **참이면 같이 보여야 할 것**: 데이터셋 **전이 행의 파형 모양과 t63 값**은 2노드 이산화가 지배한다 — 그대로 읽으면 안 된다. **정상상태 행과 부호 기반 판정(세션 4 게이트 · τ ∝ M)은 영향받지 않는다** — 둘 다 N 에 불변임이 확인됐다. ⑶ **가설(사람이 정한다)**: **(가)** 전이 파형을 **부호와 방향으로만** 읽고 파형 모양·t63 절대값을 쓰지 않는다(코드 변경 0) · **(나)** 노드 수를 늘려 다시 낸다 — N 을 얼마로 할지가 **5장에 없는 수치 선택**이고 수렴하지 않았으므로 어디서 끊을지 근거가 없다 · **(다)** 이동지연을 CSTR 직렬이 아닌 다른 방식(순수 지연·PDE)으로 모사한다 — 배관 길이 배분이라는 5장 밖 숫자가 필요하다. **이 판은 아무것도 고르지 않았고 모델·데이터셋을 고치지 않았다** | M | **데이터셋 전이 행(310,016행 중 정상상태 1,280행을 뺀 전부)의 파형·시간 지표**에 걸린다. 정상상태 행과 부호 기반 게이트는 걸리지 않는다. **#38 과 다른 결손이다** — #38 은 질량손실 모사 안의 상수 M 유보이고, 이것은 **현재 K 근사 데이터셋의 전이 행 전체**다. #21·#25·#23(M 자체의 결손)과도 별개이며, 그 셋이 닫혀도 이것은 남는다 |
| 41 | **52열본의 `heat_capacity_ratio` 열에 Cr 이 아니라 ε 가 들어 있다.** ⑴ **관측**(세션 7.0 · 시연 열 매핑 중): `dataset_plan.PROVENANCE_COLUMNS` 는 이 열을 「Cr — **유도값**이다(세션 5-B). 선언값이 아니다」로 적어 두었는데, `dataset.steady_rows`(368행)는 그 자리에 `result.thermal.hx_effectiveness` 를 넣는다. 같은 케이스(`default_cdu_cases()[0]` · 부하 100%)에서 **ε = 0.667051896745134 · Cr = 0.9982664640679125** 로 **다른 값**이다 — Cr 은 `PlantSteadyStateResult.heat_capacity_ratios()` 가 낸다. 5-1 「데이터셋 스키마 규약」이 「열 목록은 코드가 정본」이라 정했는데, **그 코드 안에서 계획(`dataset_plan`)과 구현(`dataset.py`)이 갈렸다.** ⑵ **참이면 같이 보여야 할 것**: 이 열을 **Cr 로 읽는 모든 용도가 어긋난다.** 5-1 스키마 규약이 「Cr 유도값 표기」를 전 행에 싣기로 한 취지가 이 열이므로 **표기 목적 자체가 달성되지 않았을 수 있다** — `cr_caveat` 열(「Cr 은 물성에서 유도된 값이다」)이 가리키는 대상이 실제로는 ε 인 셈이다. ⑶ **가설(사람이 정한다)**: **(가)** 열 이름을 내용에 맞춘다(`heat_capacity_ratio` → `hx_effectiveness`) · **(나)** 내용을 이름에 맞춘다(Cr 을 넣는다) · **(다)** 둘 다 싣는다(열이 53개가 된다). **어느 쪽이든 데이터셋 재생성이 필요하다(약 60분).** 이 판은 아무것도 고르지 않았고 `dataset*.py`·CSV 를 고치지 않았다 | M | **확인한 것 하나 — `dataset_report` 는 이 열의 값을 읽지 않는다**(세션 7.0-B 에서 코드로 확인). 리포트가 `cdu_simul.dataset` 에서 가져오는 것은 `DATASET_VERSION`·`LEAK_MODEL_K_APPROX`·`column_names`·`enumerate_specs` 넷뿐이고, `column_names()` 는 **`len()` 안에서만** 쓰여 「열 52개」라는 **개수**를 낼 뿐이다(373행). `column_names()` 내부가 `steady_rows(probe)` 를 돌긴 하나 **`.keys()`(이름)만** 취하고 값을 버린다. 리포트에 나오는 「대향류 Cr≈1」(430행)·「세션 5-B 에서 Cr 을 선언값에서 유도값으로 바꿨다」(551·590행)는 **세션 5-B 유도 규약에 대한 산문**이지 이 열에서 온 수치가 아니다 — 리포트 어디에도 0.667·0.998 같은 값이 없다. **따라서 2026-08-31 파일럿 종료 판정 재료에 이 결손이 닿지 않는다.** 걸리는 것은 **52열본을 직접 읽는 쪽**이다 — 이 열을 Cr 로 읽으면 어긋난다. 시연 데이터(`demo/demo_steady.json`)는 Cr 을 태그로 쓰지 않아 영향이 없다(`demo/tag-map.md` 「4. 보고만 한다」) |
| 42 | **부하 CDU `T_supply_C` 의 저항 응답이 비단조이고 중간부하에서 부호가 뒤집힌다.** ⑴ **관측**(세션 7.0-C · `demo/demo_steady.json` 읽기만): 같은 부하에서 랙 저항만 올리면 총유량은 **매끄럽게 단조 감소**한다(single 전 부하에서 R5 −4.26e-03 · R20 −1.64e-02 · R50 −3.81e-02 L/s, 부하 의존성 3자리 이내). 그런데 같은 조합의 `T_supply_C` 변화는 그렇지 않다 — single 에서 부하 20~45% 는 **+**(R50 +3.9e-06 ~ +5.4e-06 K), 부하 50% 에서 **−** 로 뒤집히고(−1.03e-05 K), 부하 60~80% 에서는 **R20 과 R50 이 거의 같다**(L60: R20 −1.116e-03 · R50 −1.112e-03 K — 유량은 2.3배 더 줄었는데 온도는 오히려 조금 덜 움직인다). dual 에서도 R5 응답이 부하 35% 부근에서 **1.24e-08 K** 까지 내려갔다가 다시 커진다. 전 범위에서 **|변화| ≤ 0.009 K** 다. ⑵ **참이면 같이 보여야 할 것**: `T_supply_C` 는 duty·2차측·1차유량의 함수이므로 **유량이 단조면 온도도 단조**여야 한다. 다른 태그(`T_return_C`·랙 유량·랙 출구온도)에서는 이런 뒤집힘이 **없다** — 전부 저항에 단조다. 즉 결손이 있다면 **열 쪽 고정점의 분해능**이지 수력이 아니다. ⑶ **가설(사람이 정한다)**: **(가)** `fsolve` 기본 `xtol`(≈1.49e-8)로 도는 CDU 바깥 고정점의 수렴 잔차가 `T_supply` 에서 1e-3 K 규모로 나타난다 — 그러면 이 크기의 차이는 **물리 신호가 아니라 solver 분해능**이고, 화면은 `T_supply` 를 저항 신호로 쓰지 않으면 된다(코드 변경 0) · **(나)** 실제 물리(2차측 배분·물성의 온도 되먹임)가 이 부하 구간에서 상쇄를 만든다 — 그러면 `xtol` 을 조여도 모양이 남는다 · **(다)** 열 고정점의 수렴 판정 자체를 조인다 — **5장 밖 수치(허용오차) 선택**이고 세션 1-B~5-B 게이트 재판정이 따라온다. **이 판은 아무것도 고르지 않았고 코드·데이터셋·JSON 을 고치지 않았다.** 가르는 시험은 하나다 — `xtol` 을 1e-12 로 조여 같은 조합을 다시 풀고 뒤집힘이 남는지 본다 | S | **시연 화면에 걸린다** — `T_supply_C` 를 「저항이 늘면 이렇게 움직인다」로 보여주면 안 된다(부호가 부하에 따라 갈린다). **파일럿 게이트에는 닿지 않는다** — 6장 기준 넷 중 `T_supply` 의 저항 단조성을 판정하는 것이 없고, 1-B energy balance·2 방향성·4 누출 신호는 전부 다른 양이며 이 크기(≤0.009 K)가 그 판정을 뒤집지 않는다. 데이터셋(52열본)에도 같은 열이 있으나 **세션 4 판정은 `T_return`·랙 유량으로 했다** |
| 43 | **고정 CDU 의 `total_flow_Lps` 는 4자리 표시에서 「한 번도 깜빡이지 않는」 태그가 아니다 — 반올림 경계를 걸친다.** ⑴ **관측**(세션 7.0-D · `demo/demo_steady.json` 읽기만): 세션 7.0-D 지시 C5 는 「고정 CDU 의 `total_flow`(폭 7e-05 L/s)와 `rack_flows`(폭 8.8e-09 L/s)는 위 자릿수에서 한 번도 깜빡이지 않는다 — 정상이며 결함으로 보고하지 않는다」로 적었다. **`rack_flows` 는 맞다**(dual 68 케이스 전부 `1.9382` 하나). **`total_flow_Lps` 는 아니다** — 값 범위 15.505512~15.505582 가 소수 4자리 반올림 경계 **15.50555** 를 걸쳐서 `15.5055`(31 케이스) 와 `15.5056`(37 케이스) **두 값으로 갈린다**(예: `dual/L20/R0` = 15.5055 · `dual/L25/R0` = 15.5056). 따라서 그 경계를 넘는 케이스 전환에서는 이 태그가 **깜빡인다**. ⑵ **참이면 같이 보여야 할 것**: 화면의 깜빡임 규칙(「표시된 문자열이 바뀐 태그만」)은 그대로 지켜지고 있다 — 문자열이 실제로 바뀌므로 깜빡이는 것이 **규칙에 맞는 동작**이다. 어긋난 것은 **코드가 아니라 예상**이고, 「연동 신호가 자릿수 아래로 묻힌다」는 서술이 이 태그에서는 부분적으로만 참이다(폭 7e-05 L/s 는 4자리에서 완전히 묻히지 않는다). ⑶ **가설(사람이 정한다)**: **(가)** 그대로 둔다 — 규칙대로 도는 것이고, 경계를 걸친 깜빡임은 실제로 값이 바뀌었다는 정직한 표시다(코드 변경 0) · **(나)** 이 태그만 자릿수를 3자리로 줄여 확실히 묻는다 — **C4 「태그별로 자릿수를 고정한다」와 충돌하지 않지만 유량 태그 사이의 자릿수가 갈린다** · **(다)** 자릿수를 늘려(5~6자리) 폭 7e-05 를 제대로 보여준다 — 부하 CDU 의 같은 태그도 같이 늘어난다. **이 판은 아무것도 고르지 않았고 `pfd.html` 을 그대로 두었다** | S | **시연 화면의 서술 한 줄에만 걸린다.** 데이터·모델·게이트에 닿지 않는다(표시 자릿수 문제일 뿐 값은 JSON 그대로다). 다음 판이 「고정 CDU 는 절대 안 움직인다」로 설명하면 화면과 어긋난다 — 정확히는 **`rack_flows` 는 안 움직이고 `total_flow` 는 경계에서 한 자리 움직인다** |
| 44 | **[닫힘 · 세션 7.2-B]** 가설 **(가)** 를 사람이 골랐다 — 5-1 근거란을 「**평균 K 가 보존된다**」로 고치고, 병렬 합성이 비선형이라 총유량이 **+0.023%** 이동한다를 함께 적었다. 수치 변경 0. 원문: **5-1 「랙별 등가길이 배분」 근거란의 「계통 전체 저항이 보존된다」가 정확하지 않다 — 평균 K 는 보존되지만 총유량은 +0.023% 이동한다.** ⑴ **관측**(세션 7.2): K 배수를 8랙 평균 1.0 으로 잡았으므로 **평균 K 는 정확히 보존된다**. 그러나 8랙은 병렬이고 병렬 합성은 K 에 선형이 아니다(각 다리가 Q ∝ 1/√K 로 응답한다). 실측: 같은 고정축 조합 `single/L100/R0` 총유량이 균등 **15.505582 L/s** → 배분 **15.509148 L/s** 로 **+0.003566 L/s(+0.023%)** 늘었다. 저항이 낮아지는 쪽(√ 의 오목성)으로 치우친 결과다. ⑵ **참이면 같이 보여야 할 것**: 5-1 근거란이 「다른 랙을 기준으로 잡으면 정격점이 이동해 5장 「랙당 ΔP 2~3 mAq @ 1.94 L/s」와 어긋난다」로 평균 기준을 정당화했는데, **평균 기준도 정격점을 0.023% 옮긴다.** 다만 이동 폭이 다른 기준(랙0 기준이면 평균 K 가 1.25배)보다 두 자리 작아 **선택 자체는 여전히 최소 이동**이다. ⑶ **가설(사람이 정한다)**: **(가)** 5-1 근거란 문구를 「**평균 K 가 보존된다** — 병렬 합성이 비선형이라 총유량은 +0.023% 이동한다」로 고친다(정본 수정 · 수치 변경 0) · **(나)** 총유량이 정확히 보존되도록 기준을 역산한다 — **5장에 없는 새 수치(등가 K)를 만드는 것**이라 절대 규칙 1 에 걸린다 · **(다)** 그대로 둔다 — 0.023% 는 이 파일럿의 어느 판정에도 닿지 않는다. **이 판은 아무것도 고르지 않았고 5-1 문구를 고치지 않았다**(그 행을 방금 사람 지시로 넣었으므로 다시 고치는 것은 이 판의 범위 밖이다) | S | **문서 문구 하나에 걸린다.** 데이터·게이트에 닿지 않는다 — 기존 게이트 기록은 전부 **균등** 경로이고 이 판에서 바이트 단위로 동일함을 재확인했다. 배분을 켠 산출물은 현재 `demo/demo_steady.json` 하나뿐이다 |
| 45 | **[닫힘 · 세션 7.2-B]** 가설 **(나)+(다)** 를 사람이 골랐다 — 배분을 켠 경우 주입 랙을 **랙7(가장 먼 랙)** 로 옮기고(5-1 정본 수정 · `demo-7.2-B` 재생성) 5-1 에 **「랙 간 비교로 찾지 않는다」 한계**를 함께 적었다. 실측 `single/L100/R50`: 주입 랙7 **6.12 m³/h · 43.898℃** vs 가장 가까운 정상 랙6 **6.84 m³/h · 42.664℃** — 표시 자릿수에서 확실히 구분된다. 원문: **랙별 배분을 켜면 저항 +50% 랙(랙0)이 가장 먼 랙(랙7)과 완전히 겹친다 — 한 장면 안에서는 구분되지 않는다.** ⑴ **관측**(세션 7.2 · `demo/demo_steady.json` demo-7.2): 랙0 은 가장 가까운 랙이라 K 배수가 **0.8** 이고, 저항 +50% 를 걸면 0.8 × 1.5 = **1.2** 로 **랙7 의 배수(1.2)와 정확히 같아진다**. 실측 `single/L100/R50`: 랙0 과 랙7 의 유량 차 **−9.1e-13 kg/h**, 출구온도 차 **정확히 0.000 K** — 부동소수점까지 같다. ⑵ **참이면 같이 보여야 할 것**: **랙 간 비교만으로 「저항이 걸린 랙」을 찾는 판독은 성립하지 않는다.** 균등 배분에서는 랙0 만 튀어 눈에 띄었지만(세션 7.0-C: 랙0 유량 −7.9% · 출구온도 +0.888 K) 배분을 켜면 정상인 랙7 과 구분되지 않는다. **다만 같은 랙의 시간·기준 대비 신호는 그대로 살아 있다** — 랙0 은 R0 → R50 에서 유량 **−513.1 kg/h** · 출구온도 **+0.7318 K** 로 크게 움직인다. 즉 **기준 대비 판독은 되고 랙 간 판독은 안 된다.** ⑶ **가설(사람이 정한다)**: **(가)** 그대로 둔다 — 실제 계통에서도 위치가 다른 랙끼리 절대값 비교로 이상을 찾을 수 없다는 것이 **물리적으로 옳은 결론**이고, AI 학습 데이터셋에는 오히려 정직한 재료다 · **(나)** 저항 주입 랙을 랙0(가장 가까운 랙)이 아닌 다른 랙으로 옮긴다 — 5-1 「누출 주입 지점」이 「8랙이 전부 동일하므로 어느 랙인지는 결과에 영향하지 않는다」로 정해 두었는데 **배분을 켠 순간 그 전제가 깨진다. 정본 수정이 필요하다** · **(다)** 화면·리포트에 「랙 간 비교로 찾지 않는다」를 표기로 남긴다. **이 판은 아무것도 고르지 않았고 주입 지점을 옮기지 않았다** | M | **5-1 「누출 주입 지점」의 전제(「어느 랙인지 결과에 영향하지 않는다」)가 배분을 켠 경우 성립하지 않는다** — 정본 문구가 걸린다. **52열본 데이터셋은 균등 경로이므로 영향받지 않는다.** 시연 화면(`demo/pfd.html`)이 랙 8개를 나란히 그리므로 **「저항 걸린 랙을 찾아보라」식 시연을 하면 안 된다**. 세션 4 게이트 판정은 랙 간 비교가 아니라 정상 대비 비교였으므로 걸리지 않는다 |
| 46 | **[닫힘 · 세션 7.4]** 사람이 **평균 대비 계조**를 골랐다 — 세션 7.4 의 「서버실」 탭은 랙 색을 **그 케이스·그 CDU 8랙 평균 대비 편차**로 칠하고 계조를 **−1.5~+1.5 K 로 고정**한다(케이스별 재정규화 없음 — 재정규화하면 케이스 간 색 비교가 깨진다). 전 136 케이스 실제 편차는 **−0.5538~+1.3849 K**(표본 1,632)라 잘리는 랙이 **0 개**다. 범례에 「8랙 평균 대비」를 적었고 절대온도는 랙마다 숫자로 그대로 나온다. **「계통도」 탭의 절대온도 계조(30.0~43.9℃)는 사람 지시로 그대로 두었다** — 즉 이 항목이 적은 현상은 계통도 탭에는 그대로 남아 있고, 색으로 랙을 가르는 일은 서버실 탭이 진다. **[신설 · 세션 7.3]** **고부하에서 온도 계조가 랙을 거의 못 가른다.** 계조 범위를 전 케이스 범위인 30.0~43.9℃ 로 고정했더니(C2 지시) 부하 100% 에서는 랙 8개가 전부 계조의 위쪽 10% 안에 몰린다 — `single/L100/R50` 에서 주입 랙8 `rgb(194,69,45)` 과 정상 랙7 `rgb(181,74,56)` 은 1.234 K 차(계조 폭 13.9 K 의 8.9%)뿐이라 **색만으로는 구분이 안 된다.** 실제로 랙을 가르는 것은 리본 두께(4.2 px vs 13.6 px)와 이상 테두리다. 색을 「부하축을 읽는 눈금」으로 두고 랙 구분은 리본·테두리에 맡긴 현재 배치가 맞는지, 아니면 케이스 안에서 계조를 다시 잡을지는 **사람이 정한다** — 케이스별 재정규화는 케이스 간 색 비교를 깨뜨린다 | S | 시연 화면 읽기 전용. 데이터·모델에 영향 없다 |
| 47 | **[신설 · 세션 7.4]** **등축 뷰에 「공급 헤더」·「환수 헤더」 이름표를 놓을 자리가 없다.** 세션 7.4 C12 기하 검사에서, 열 옆 통로에 놓은 이름표가 등축 투영상 **다른 열의 랙 도형**(dual)이나 **이웃 랙의 숫자 상자**와 겹쳤다(열 중간·열 끝·CDU 옆 세 자리를 시도했고 전부 겹쳤다 — 등축에서 통로는 뒤쪽 열의 랙과 같은 화면 자리를 쓴다). 그래서 **SVG 안의 이름표를 뺐고**, 헤더 이름은 화면 아래 띠의 설명줄이 진다(「바닥 배관 = 공급 헤더(랙 뒤) · 환수 헤더(랙 앞, 굵기 = 유량)」). 자리를 만들려면 랙 열을 짧게 하거나 이름표를 배관 위에 각도를 맞춰 얹어야 하는데 둘 다 이 판의 범위 밖이다 | S | 시연 화면 읽기 전용. 데이터·모델·게이트에 닿지 않는다 |
| 48 | **[신설 · 세션 7.6]** **셸 편집 금지 규칙에 「대신 무엇을 쓸지」가 없다.** 세션 7.5 가 `CLAUDE.md` 「하지 말 것」에 넣은 줄은 **금지만 적고 대안을 안 적었다**(「셸로 파일을 만들거나 고치기. 편집 도구를 쓴다 — … heredoc·here-string 으로 긴 문자열을 셸에 싣지 않는다」). 그 결과 **세션 7.5 는 그 규칙을 적어 넣은 직후** `CLAUDE.md` 커밋 메시지를 heredoc 으로 실었고, 두 번째 커밋(`PROCEED.md`)에서 스스로 스크래치패드 파일 + `git commit -F` 로 바꿨다. **금지만으로는 안 잡히고 대안을 이름으로 줘야 잡힌다**는 관측이다. **닫는 법**: 그 줄에 한 문장을 더한다 — 「커밋 메시지가 여러 줄이면 스크래치패드 파일에 쓰고 `git commit -F <파일>` 로 한다」. **이것만으로 정본을 열지 않는다 — 다음에 `CLAUDE.md` 를 고치는 판에 얹는다** | S | 규약 문서 한 줄. 코드·데이터·게이트에 닿지 않는다. #49 와 같은 성격이라 함께 정한다 |
| 49 | **[신설 · 세션 7.6]** **파일 끝을 잘라 읽으면 의도 밖의 것이 딸려 온다.** 세션 7.5 는 **걸리는 미해결이 하나도 없는 판**이었는데 `PROCEED.md` 를 `tail -18` 로 읽어 미해결 **#45·#46·#47 본문을 통째로** 받았고, 그것이 **그 판의 컨텍스트 1위**가 됐다. 세션 7.5 가 세운 시작 4번(「「현재 상태」 표를 통째로 읽지 않는다」)은 **의도는 막았으나 수단은 못 막았다** — 정작 필요했던 것은 표 마지막 행의 서식과 삽입 위치뿐이었다. **닫는 법**: #48 과 같은 성격이라 **함께 정한다** — 금지가 아니라 「대신 무엇을 쓸지」를 준다(필요한 절을 이름으로 찾아 여는 방법). **이것만으로 정본을 열지 않는다** | S | 규약 문서. 코드·데이터·게이트에 닿지 않는다. #48 과 한 묶음 |
| 50 | **[신설 · 세션 7.7]** **등축의 2차측 배관 두 줄이 이름표를 잃었다.** 세션 7.7 C3(「같은 값을 화면 두 곳에 놓지 않는다」)에 따라 등축 바닥의 2차측 글자 묶음(2차측 공급온도·배분·「2차측 공유」)을 지워 CDU 내부 그림으로 옮겼는데, **벽 밖으로 나가는 배관 두 줄은 등축에 그대로 남아 이제 아무 이름표도 달고 있지 않다.** 그림 쪽이 「2차측 공급」·「2차측 환수 — 모델 밖」을 적고 있어 뜻은 화면 어딘가에 있으나, **등축만 보는 사람에게는 정체 불명의 배관 두 줄**이다. **#47 과 같은 성격**(등축에서 배관 이름표를 놓을 자리가 없다)이라 **함께 정한다** — 배관 위에 각도를 맞춰 얹거나, 아래 띠의 설명줄이 지게 하거나, 그림과 등축을 잇는 지시선을 넣는 셋 중 하나다 | S | 시연 화면 읽기 전용. 데이터·모델·게이트에 닿지 않는다. #47 과 한 묶음 |
| 51 | **[닫힘 · 세션 7.10]** 계통도 탭도 **같은 기준으로 걷었다** — 글자는 장치 이름과 운전값만 남긴다. `#sec` 띠의 라벨 둘에서 설명(「2차측 배분 ·」)을 떼고 장치 이름 「CDU A」·「CDU B」를 남겼고(값·단위는 그대로), `SYS_B` 의 「유량은 표시 자릿수에서 움직이지 않아 싣지 않는다」는 값이 없는 순수 설명이라 칸째 뺐다. **지운 값 0** · 대조 166항목 불일치 0. 원문: **[신설 · 세션 7.8]** **두 탭의 말이 갈렸다.** 세션 7.8 은 서버실 탭에서 설명 문구를 걷어냈는데(「배분」·「2차측 공유」·「2차측 환수 — 모델 밖」·「스트림 색 = 절대온도」·「유량은 표시 자릿수에서 움직이지 않아 싣지 않는다」), **계통도 탭에는 그대로 남아 있다** — `#sec` 띠의 「2차측 배분 · CDU A/B」와 `SYS_B` 의 「유량은 표시 자릿수에서…」다. 이 판의 범위가 서버실 탭이라 손대지 않았다. 같은 값을 두 탭이 **다른 말로** 부르고 있어, 탭을 오가는 사람에게는 다른 것으로 보일 수 있다. **닫는 법**: 계통도 탭도 같은 기준으로 걷어내거나(글자는 장치 이름과 운전값만), 두 탭의 말이 갈려도 좋다고 정하고 이 항목을 닫는다 — **사람이 화면을 보고 정한다** | S | 시연 화면 읽기 전용. 데이터·모델·게이트에 닿지 않는다 |
| 52 | **[신설 · 세션 7.10]** **등축 서버실의 dual 뷰에서 B열 랙 글자가 A열 배관 위에 얹힌다.** 세션 7.10 의 기하 검사기를 **두 열을 교차로도** 보게 했더니 `dual` 에서 겹침 **20건**이 나왔다 — B열 랙 이름표·출구온도 글자가 A열의 환수 헤더(`.hdrp`)와 갈래 배관(`RA-pipe1`~`7`) 위에 얹힌다. **이 판이 만든 것이 아니다** — `git show HEAD:demo/pfd.html`(세션 7.9)을 같은 검사기로 돌리면 같은 20건이 그대로 나온다. 세션 7.4 에서 등축 두 열을 세울 때부터 있었고, 앞 판들이 「위반 0」으로 적은 것은 검사를 **같은 열 안에서만** 했기 때문으로 보인다. 겹치는 배관은 굵기 5(헤더)·최대 20(갈래)이라 글자가 완전히 가려지지는 않으나 읽기가 나빠진다. **닫는 법**: ⑴ B열 글자를 랙 위가 아니라 랙 옆(등축 오른쪽)으로 옮기거나 ⑵ 두 열 간격(`G.ROWDX`)을 넓히거나 ⑶ dual 에서는 B열 숫자를 줄이는 셋 중 하나 — **셋 다 등축 좌표를 건드리므로 사람이 정한다.** #47·#50 과 같은 자리(등축에서 글자 놓을 데가 모자란다)라 **함께 본다** | S | 시연 화면 읽기 전용. 데이터·모델·게이트에 닿지 않는다. #47·#50 과 한 묶음 |
| 53 | **[닫힘 · 세션 7.13 — 같은 판에서 등재하고 닫았다]** **`demo/start.sh` 가 CRLF 로 체크아웃될 수 있다.** ⑴ **등재 경위**: 세션 7.12 가 `start.sh` 를 만들면서 이 항목을 미해결에 올리겠다고 보고에 적었으나 **목록에 실제로 올라오지 않았다**(7.12 의 「미해결 무변경 — 새로 연 것도 없다」와 어긋난다). 세션 7.13 이 **등재와 닫음을 같은 판에서** 한다 — 등재 이력을 잃지 않기 위해 닫힌 채로 남긴다. ⑵ **내용**: `.gitattributes` 는 `* text=auto` + `*.py text eol=lf` 뿐이라 `.sh` 는 각 PC 의 `core.autocrlf` 를 따른다 — Windows 에서 clone 하면 CRLF 로 나오고, 그 작업트리를 그대로 복사해 macOS·Linux 로 옮기면 `#!/bin/sh` 의 `\r` 때문에 실행되지 않는다. ⑶ **닫는 법**: `.gitattributes` 에 `*.sh text eol=lf` 한 줄. **이미 커밋된 파일은 자동 정규화되지 않으므로** index 상태를 확인했다 — `git ls-files --eol demo/start.sh` → `i/lf w/lf attr/text eol=lf` 로 **index·작업트리 둘 다 이미 LF** 였다. 따라서 `git add --renormalize` 를 돌리지 않았고 파일 내용은 하나도 바뀌지 않았다(미해결 #9 때와 같은 모양이다) | S | 저장소 설정 한 줄. 코드·데이터·게이트에 닿지 않는다. 지금 이 PC 의 동작에는 영향 없고, **앞으로 다른 PC 에서 clone 할 때** LF 로 고정된다 |
| 54 | **[신설 · 세션 7.14]** **cp 평가 = 환수온도의 balance 잔차가 세션 1-B 기록과 다르다.** ⑴ **관측**: 세션 1-B 「다음 세션이 알아야 할 결정」 3 이 적은 세 값 중 `bulk_mean` **0.00506%** · `supply` **0.30891%** 는 지금 코드에서 그대로 나오는데, `return` 만 기록 **0.31524%** 대 재산출 **0.3152212%** 로 **Δ 0.0000188 %p** 다르다. 재산출은 `model.default_cases()` 4케이스에 `dataclasses.replace(case, cp_rule=...)` 를 걸어 **코드를 하나도 고치지 않고** 냈다. ⑵ **참이면 같이 보여야 할 것**: **1-B 게이트 판정은 흔들리지 않는다** — 게이트가 쓰는 것은 `bulk_mean` 하나이고 그 값은 기록과 같다. 세 값 모두 6장 기준 0.1% 대비 같은 쪽에 있어(통과·실패·실패) **「규칙 하나로 결과가 뒤집힌다」는 논지도 그대로**다. `docs/intro-outline.md` §4 가 적은 표기(0.315%)도 두 값에서 같다. 걸리는 것은 **PROCEED 의 기록값 자체**다. ⑶ **원인은 적지 않았다** — 세션 7.14 는 관측 판이고 「멈추는 조건」(재산출값이 기존 기록과 다르면 고치지 말고 멈춘다)에 따라 **아무것도 고치지 않았다.** ⑷ **닫는 법**: 기록값을 재산출값으로 고칠지, 두 값을 나란히 남길지, 원인을 먼저 규명할지 — **사람이 정한다**. 같은 성격의 것이 하나 더 있다: 세션 4 신호표의 저항 +50% 총유량 변화 **−0.3704~−0.1638%** 대 재산출 **−0.3710~−0.1642%**(`leak.all_steady_signals()` 32조합, 같은 진입점). 둘을 **함께 본다** | S | **기록 대 재산출의 어긋남이지 게이트 결함이 아니다.** 코드·데이터셋·게이트 판정에 닿지 않는다. 걸리는 것은 이 기록을 그대로 옮겨 적는 산출물(소개자료 등)이고, `docs/intro-figures.md` 가 두 값을 나란히 적어 두었다 |
| 55 | **[신설 · 세션 7.22]** **인쇄 한 벌에서 「필요한 것」의 강조 색이 흰 바탕에 옅다.** ⑴ **관측**(세션 7.21 · 헤드리스 크롬으로 인쇄 한 벌을 화면에 끌어내 렌더): `intro/intro.html` 의 `@media print` 팔레트가 쓰는 `--warn:#d99425` 이 흰 바탕에서 본문(`#000`)보다 옅어, 09 절의 「필요한 것 —」 층이 **어두운 화면 한 벌만큼 서지 않는다.** 그 층은 09 절의 요점이라 일부러 강조 색으로 세운 것이다. ⑵ **어디에 걸리나 — 09 절 하나가 아니다.** 인쇄 팔레트는 `@media print` **한 블록**에만 있고 다른 절도 같은 변수를 쓴다. 즉 **색 한 벌의 문제**이고, 09 절 안에서 고칠 수 있는 것이 아니다. 세션 7.21 은 범위(09 절 배치)를 넘지 않으려고 **고치지 않고 보고만 했다**(절대 규칙 13). ⑶ **아직 확인 못 한 것**: 실제 종이·PDF 에서 얼마나 옅은지 — 이 환경에 PDF 를 그려 볼 도구가 없다(`pdftoppm` 미설치 · 세션 7.19~7.21 이 같은 한계를 적었다). 본 것은 인쇄 한 벌을 화면으로 끌어낸 렌더뿐이다. ⑷ **닫는 법**: 인쇄 팔레트의 `--warn` 을 어둡게 할지, 인쇄에서는 색 대신 굵기·테두리로 세울지, 그대로 둘지 — **사람이 정한다** | S | **소개자료를 종이로 돌릴 때만** 걸린다. 화면 한 벌·시연 화면(`demo/`)·모델·데이터셋·게이트에 닿지 않는다 |
| 56 | **[신설 · 세션 7.22]** **`docs/intro-outline.md` 가 지금 소개자료와 어긋난다 — 자리 열둘.** ⑴ **관측**: 세 판이 각각 **목록으로만 보고하고 outline 을 고치지 않았다**(절대 규칙 13). **세션 7.15 다섯** — ① §3 「직접 열어보기 버튼 → `demo/pfd.html`」·§10 「시연 화면은 별도 파일」이 **iframe 으로 품기**로 바뀌었다 ② §3 구성 1 「정지 화면 캡처 1장」이 캡처가 아니라 **실물**이 됐다 ③ §9 Aspen 표의 「AI 학습 데이터 생성 — 목적에 맞게 설계됨」 줄은 **판정 6 과 부딪혀** 자료에 넣지 않았다 ④ §3 시연 시나리오의 막대 단위가 **m³/h** 로 바뀌어야 한다(figures #24·#25) ⑤ §11 의 다섯 항목은 **이미 사람이 정해 끝났다** — 「제작 전에 정할 것」이 아니라 기록이다. **세션 7.18 여섯** — ① §1 은 **6장 구성**을 적는데 자료는 장 묶음이 없다 ② §4 의 3-1~3-4 네 소절을 **한 절(04)** 로 접고 다이어그램 하나 + 식 하나로 바꿨다 ③ §4 3-2 ③ 「역방향 검증」은 **절을 없앴다** ④ §9 Aspen **대비표를 만들지 않았다**(03 절에서 배경으로 한 번 스친다) ⑤ §10 「화면 구조」가 전제한 **scroll-snap 을 세션 7.17 이 걷어냈다** ⑥ §5 ⑵ 의 펌프 압력 상대변화(figures #12·#13)를 **뺐다**. **세션 7.19 하나** — outline 은 「아직 못 하는 일」과 「다음」을 **두 절**로 두는데 자료는 **한 절**로 합쳤다(그 뒤 세션 7.21 이 그 절을 「전폭 한 줄 + 가로 박스 셋」으로 다시 짰다). ⑵ **왜 남겼나**: **자료의 구성이 outline 을 앞선다고 사람이 정했다.** outline 은 읽기만 했다. ⑶ **닫는 법**: outline 을 지금 자료에 맞춰 고칠지, 「초안 기록」으로 얼려 둘지 — **사람이 정한다** | S | `docs/` 안의 **초안 문서 하나**다. 자료(`intro/intro.html`)·코드·데이터셋·게이트에 닿지 않는다. 걸리는 것은 **다음 판이 outline 을 지침으로 읽을 때**다 — 지금 그대로 읽으면 이미 폐기된 구성을 따르게 된다 |
| 57 | **[신설 · 세션 7.22]** **긴 시험을 백그라운드로 돌리고 완료를 폴링하면 왕복이 헛돈다.** ⑴ **관측**(세션 7.21 ctx · **처음 나온 것**): pytest 를 백그라운드로 돌려 놓고 `tail` 로 완료를 확인하려다 **왕복 마흔 번 넘게** 「아직 돌고 있습니다」만 되받았고, 그것이 그 판의 컨텍스트 1위였다. 앞에서 한 번 돌렸으면 **왕복 1 회**다. ⑵ **왜 걸리나**: 전체 시험이 **8 분대**(7.21 506.41 s · 7.22 482.01 s)라 셸 기본 타임아웃(2 분)을 넘어, 「백그라운드로 돌리고 나중에 본다」가 자연스러운 선택으로 보인다. 같은 성질의 것이 앞에도 있었다 — 세션 5.7(백그라운드 한도에 걸려 중단) · 세션 7.13(첫 회가 백그라운드로 넘어가 결과를 못 받고 9 분을 다시 돌렸다). ⑶ **지금 아는 대처**: **타임아웃을 늘려 앞에서 한 번** 돌린다. 세션 7.22 가 그렇게 했고 왕복 1 회로 끝났다. ⑷ **닫는 법**: 이 대처를 `CLAUDE.md` 「명령어」 절에 한 줄로 적을지 — **사람이 정한다**(정본 수정이라 임의로 하지 않는다) | S | 계산 결과·게이트 판정에 닿지 않는다. **판마다 컨텍스트를 먹는 절차 문제**다 — 미해결 **#48·#49** 와 같은 자리 |
| 58 | **[신설 · 세션 7.25]** **전체 pytest 가 도구 호출 상한 10분에 근접한다.** ⑴ **관측**: 최근 실측 **7.21 506.41 s · 7.22 482.01 s · 7.23 569.77 s** — 569.77 s 는 상한 600 s 에 여유 **5 %** 뿐이다. 세션 7.24 와 7.25 는 그래서 **반으로 나눠** 돌렸다(7.25: 46.22 s + 476.24 s = **522.46 s**, 1,207건 전부 통과). ⑵ **왜 걸리나**: 시간의 대부분이 뒤쪽 게이트 시험 일곱 파일에 몰려 있다(7.25 실측 **476.24 s / 815건** vs 앞쪽 일곱 파일 **46.22 s / 392건**). 배치 실행이 붙는 판이 오면 시험이 더 길어져 **나눠도 걸릴 수 있다**. 세션 7.25 의 C2 계측(데이터셋 전량 생성 **1,515 s**)이 실제로 상한을 넘어 백그라운드로 넘어갔다 — 같은 성질이다. ⑶ **지금 아는 대처**: 파일 단위로 반씩 나눠 두 번 돌린다(7.24·7.25 가 그렇게 했다). ⑷ **닫는 법 — 사람이 정한다**: ⓐ 나눠 돌리는 것을 `CLAUDE.md` 「명령어」·「표준 절차」에 한 줄로 적을지(정본 수정이라 임의로 하지 않는다) · ⓑ 느린 시험에 마커를 붙여 판마다 고를 수 있게 할지(시험 코드 변경) · ⓒ 그대로 두고 판마다 나눌지 | S | 계산 결과·게이트 판정에 닿지 않는다. **#57 과 같은 자리의 절차 문제**이고, 배치 실행이 붙는 판에서 다시 걸린다 |
| 59 | **[신설 · 세션 7.26]** **「누출」이라는 낱말이 상위 개념과 하위 기구 둘 다를 가리킨다 — 어긋난 자리 문서 10곳 · 코드 11곳.** ⑴ **관측**: 세션 7.26 이 이상 상태를 「막힘」(K값 증가)·「샘」(질량손실) 둘로 가르면서, 지금까지 두 뜻을 겸하던 「누출」이 어느 쪽인지 문맥마다 갈린다. 문서: 5장 표 항목명 「누출 시나리오」(이제 막힘만 가리킨다) · 5-1 「누출 주입 지점」(다섯 조각 중 셋이 막힘 전용 — 세션 7.26 C3) · `CLAUDE.md` 목적문·규칙 6·「하지 말 것」·세션 4/5.5 게이트(「이상」과 「누출」을 나란히 놓아 별개처럼 읽힌다) · `project-overview.md` 66~69·112·244행(「누출 정의를 질량손실로 **교체**」 — 사람이 고른 것은 교체가 아니라 **병기**이고 시점도 「파일럿 뒤」가 아니다) · 161·177행(막힘 전용 문구) · 224·232행(「절대 규칙 8 **예외**」 — 예외가 아니게 됐다) · `collaboration.md` 99행. 코드: `assumptions.py:473·509` · `dataset.py:116` · `demo_steady.py:17·197` · `dynamics.py:493` · `hydraulics.py:215·244` · `leak.py:7` · `leak_massloss.py:3·4·7·491` · `leak_massloss_thermal.py:7·8·604` · `tests/test_hydraulics.py:97`. ⑵ **참이면 같이 움직여야 할 것**: 데이터셋 라벨과 산출물 표기가 기구를 구별하지 못하면, 규칙 8 이 새로 넣은 「한쪽 기구의 신호를 다른 쪽 방향으로 읽지 않는다」가 **읽는 쪽에서 지켜질 수 없다**(7.25 C5 ⑺⑻ 과 같은 뿌리다). ⑶ **가설(사람이 정한다)**: **(가)** 「누출」을 **상위 개념 전용**으로 두고 하위는 언제나 「막힘」·「샘」으로 적는다 · **(나)** 「누출」을 **「샘」의 동의어**로 좁히고 상위는 「이상 상태」로 적는다 · **(다)** 손대지 않고 각 자리에 뜻만 병기한다. **어느 쪽이든 문서 판이며, `leak_massloss*.py` 머리 주석의 조건 셋(별도 코드·데이터셋 미변경·규칙 8 문구 미변경)은 이미 전부 무효라 코드 주석 정리가 따라붙는다.** 세션 7.26 은 **아무것도 고치지 않았다**. ⑷ **[갱신 · 세션 7.27]** 사람이 **(가)** 를 골랐다 — 「누출」은 **상위 개념 전용**, 하위 기구는 언제나 「막힘」·「샘」. **문서 10곳 중 아홉이 닫혔다**(정리 4장 3번 · 5-1 「누출 주입 지점」을 기구별 두 항목으로 · `CLAUDE.md` 목적문·규칙 6·세션 4 게이트·세션 5.5 게이트·「하지 말 것」 · `project-overview.md` 66~69·112·161·177·224·232·244 · `collaboration.md` 99행). 규약은 `CLAUDE.md` 절대 규칙 8 「낱말 규약」에 적혔고, **`intro/` 는 예외**(빌드가 「누출」을 막는다 — `intro/make_intro_html.py:44`)라는 것도 거기 있다. **남은 문서 한 자리 = 5장 표 항목명 「누출 시나리오」** — 5장 본문이라 중단 조건에 걸려 **사람 대기**다. **코드 11곳은 그대로 열려 있다** — 세션 7.27 은 코드를 한 줄도 고치지 않았다(문서 판) | M | 열림 (문서 9/10 닫힘 · 5장 항목명 사람 대기 · 코드 11곳 열림) |

> **확인 완료 (세션 5.5-B 마무리)** — 사람이 검토해 **그대로 두기로** 한 것 둘.
> ① **저장 격자 최근접 점 선택(보간 없음)** — 그대로 둔다. 원본 균등 격자 간격
> 0.015τ 가 비균등 요구 간격 0.02τ 보다 촘촘해 시간 오차가 최대 0.0075τ 다.
> 보간하지 않은 것이 적분기가 실제로 낸 점만 담아 더 안전하다. **다만 `plant.py`
> 가 자기 `t_eval` 을 쓰는 구조는 남아 있어, 노드를 늘리는 확장에서 다시 걸린다**
> — 기록만 한다.
> ② **비대칭 부하의 거울상 중복** — 그대로 둔다. 사람이 정한 규모를 코드가 줄이지
> 않은 판단이 맞고, 중복이 남아 있으면 거울상 여부를 사용자가 직접 확인할 수 있어
> **검증 재료**가 된다.

> **기록만 (세션 2.5)** — **GUI 는 파일럿 밖이다.** 모사가 완료된 뒤 별도로 진행하며,
> 지금은 형태를 정하지 않는다. `project-overview.md` 「시각화 방향」 절은 이 판에서
> **손대지 않았다** — 그 절의 내용(정적 PFD 선호 · FreeSimpleGUI 후보 · 제작 주체 미정)은
> 결정이 아니라 참고 기록이며, 본 프로젝트 착수 시점에 다시 확인한다.
