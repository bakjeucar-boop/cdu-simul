# PROCEED.md — 진행 상태

> 이 파일이 "오늘 상태"의 정본이다. `project-overview.md` 「지금 어디인가」 절과
> 어긋나면 이 파일이 맞다. 대화 시작 시 `daily_brief.py` 출력을 첫 메시지에 붙인다.
> **모든 수치는 가정값 기반이며 실측이 아니다.**

## 현재 상태

| 세션 | 단계 | 게이트 통과 여부 | 다음 세션 첫 작업 |
|---|---|---|---|
| 1-A | 저장소 clone · 가상환경 · 저장소 골격 · 5장 가정치 전사 · CoolProp 래퍼 · `PROCEED.md`·`daily_brief.py` 초기 구성 · 문서 4종 커밋 | **해당 없음 (물리 모델 없음)** — 환경 스모크 테스트 5건 통과했으나 이는 feasibility 판정이 아니다 | (완료) |
| 1-B | 단일 랙 + 단일 CDU, 2차측 고정온도, 정상상태 모델 (`model.py`) · energy balance 자동 검사 | **통과** — 4케이스 최대 \|잔차\| = **0.00506 %** (기준 <0.1%). 단 아래 ※를 반드시 함께 읽는다 | (완료) |
| 1-C | 저장소 위생 정비 — ruff·mypy 도입 · 위생 미해결(#6·#9·#10·#11·#16) 처리 · 5-1 출처 반영 | **게이트 없음** (물리 모델을 건드리지 않음). 대신 **정비 전후 4케이스 수치가 `repr` 전체자리까지 동일**함을 확인했다. pytest 17건·ruff·mypy 전부 통과 | (완료) |
| 2 | 시간축 추가 — `dynamics.py` (2노드 온도 `solve_ivp` 적분) · 부하 스텝 응답 | **통과** — 부하 상승/하강 스텝에서 T_return 이 타당한 방향으로 반응(스텝 직후 기울기·순변화 둘 다). 5장 범위 **양 끝 16조합 전부**에서 성립. 단 아래 ※2 를 함께 읽는다 | (완료) |
| 2.5 | 문서 정합 판 — 5장 빈칸을 5-1 로 닫고 파일럿 종료 조건 확정 (코드 변경 없음) | **게이트 없음** (문서 판이다. feasibility 판정 없음). 코드를 건드리지 않았고 pytest 53건·ruff·mypy 전부 통과를 재확인했다 | (완료) |
| 2.5-B | `project-overview.md` 잔재 정리 (종료 조건 확정 반영 · 6장 기준 요약 동기화) | **게이트 없음** (문서 판) | (완료) |
| 3-A | 수력 모듈 — 5-1 수력 가정치 전사 · `hydraulics.py` (펌프곡선·밸브·분기저항·잔여저항 역산) · 8랙 헤더 압력평형 `fsolve` | **판정 없음** — 6장 기준은 열모델이 붙는 3-B 에서 본다. `fsolve` 수렴은 세션 3 게이트의 **절반**이고 8조합 전부 `ier=1` 이었다. 나머지 절반(극단 케이스 비발산)은 **아직 안 봤다** | (완료) |
| 3-A2 | 물성 단일화 보정 — 5-1 의 밸브 Kv·배관 K 를 값에서 **역산 규칙**으로 전환 · SG 정의를 CoolProp 래퍼 한 곳으로 · 수력 물성 온도 인자화 | **판정 없음** (물성 정합 보정 판). 정격점 ΔP 가 5장 표값(2/3 · 3/5 mAq)을 **잔차 1e-15 mAq 로 항등 재현**한다 — 세션 3-A 의 1.995/2.993 이 닫혔다. pytest 97건·ruff·mypy 통과 | (완료) |
| 3-B | 열모델 결합 — 8랙 확장 · 수력↔열 결합(고정점 안에 quasi-steady 수력) · 동적 매 시점 결합 | **세션 3 게이트 통과** — ㉠ `fsolve` 수렴: 열결합 32조합에서 수력·열·결합 고정점 세 solver 전부 `ier=1` · ㉡ 극단 케이스 비발산: 부하 0%/100% 정상상태 32조합 + 동적 4케이스 전부. **앞 게이트 재확인**: 1-B energy balance 32조합 최대 |잔차| **0.00507 %**(<0.1%) · 세션 2 방향성 16조합 전부. **수렴시간은 여전히 판정하지 않았다**(미해결 #21·#31). pytest 294건·ruff·mypy 통과 | (완료) |
| 4 | 누출 시나리오 주입 — 배관 K값 +5/+20/+50% 를 랙 1개에 · 정상상태 신호 32조합 × 3수준 · 동적 스텝 주입 · M 배분 민감도 | **세션 4 게이트 통과** — 판정 기준(A 부호 일관성 · B 수준 간 엄격 단조 · C 잡음 대비)을 코드보다 먼저 적고(커밋 803142e) 그대로 판정했다. 96건 전부 통과. **앞 게이트 유지**: 정상 경로 결과가 3-B 와 바이트 단위 동일 · 누출 상태 energy balance 최대 |잔차| 0.00507 % · 누출 3수준에서 수력·열·결합 fsolve 전부 수렴. pytest 659건·ruff·mypy 통과 | (완료) |
| 5 | 다중 CDU · 공유 2차측 결합 — CDU 2대 · 총 2차측 유량 고정 · 1차측 유량 비례 배분 · 상위 레벨 연립 `fsolve` | **세션 5 게이트 통과** — 판정 기준(A 수렴 · B 잔차 동시 소멸 · C 연동의 실재)을 코드보다 먼저 적고(커밋 acbc31c) 96 케이스 전수 판정했다. **단 C4(대칭 케이스의 세션 4 재현)는 실패** — 32조합 최대 편차 0.0245 K. 원인은 5-1 두 행의 내부 모순이며 미해결 #33 으로 올렸다. 앞 게이트 유지: 단일 CDU 경로가 세션 4와 부동소수점까지 동일 · 결합·누출 상태 energy balance <0.1%. pytest 1109건·ruff·mypy 통과 | (완료 — 단 게이트는 세션 5-B 에서 재판정됐다) |
| 5-B | Cr 유도로 전환 — 5장 「1:1」을 부피유량비로 읽고 2차측을 정격 15.5 L/s 고정 · Cr 을 매번 물성에서 유도 · 단일 CDU 에 소급 | **세션 1-B~5 다섯 게이트 전부 재판정 통과** — 1-B 32조합 최대 |잔차| 0.00506% · 2 방향성 16조합 · 3 수렴·비발산 32조합 · 4 누출 신호 96건 · 5 연동 수렴 96케이스. **판정 기준·허용오차를 하나도 조정하지 않았다.** C4 대칭 재현 편차가 0.0245 K → **0.000e+00 K** 로 해소됐다(미해결 #33 닫힘). pytest 1109건·ruff·mypy 통과 | (완료) |
| 5.5-A | 데이터셋 설계 · 조합 계수 · 실행시간 측정 (생성 없음) | **판정 없음 — 설계·측정 판이다.** 아무것도 확정하지 않았다: 스키마는 제안, 부하 축 값 미선택, 규모 3안 미선택. 모델 코드·문서 4종·기존 테스트를 건드리지 않았고 세 결과 표가 불변임을 확인했다. pytest 1109건·ruff·mypy 통과 | (완료) |
| 5.5 | 시나리오 배치 실행 → 텍스트 데이터셋 (**파일럿 마지막 판**) | **세션 5.5 게이트 통과** — 기준 A 순서 무관 · B 단독 재현 · C 정상 복귀 · D 전 행 표기. 판정 기준을 코드보다 먼저 적었다(커밋 cc790fa). 데이터셋 310,016행 · 48열 · 305 MB · 28분 · solver 실패 행 0. 앞 게이트 1-B·2·3·4·5 전부 재확인 통과 | **파일럿의 마지막 판이 끝났다.** 데이터셋 310,016행·48열·305 MB 가 `results/` 에 있고(원격에 없다) 요약 리포트가 종료 판정 재료를 모았다. **사람이 정할 것 — 다중 CDU 전이의 누출 주입 지점(미해결 #34). 그 결정 전에는 새 판을 열지 않는다.** 파일럿 종료 판정도 사람의 몫이다 — `python -m cdu_simul.dataset_report` |
| 5.5-C | 문서 정합 · 미해결 정리 · 브리핑 축약 (`project-overview.md` 체크리스트 3·4·5 · `CLAUDE.md` 경로 표기 PC 독립화 · `daily_brief.py` 축약 출력) | **게이트 없음** (문서·위생 판이다. feasibility 판정 없음). 모델 코드·기존 테스트·데이터셋을 건드리지 않았고 pytest 1,153건·ruff·mypy 전부 통과를 재확인했다 | **파일럿의 판은 전부 끝났다.** 남은 것은 사람의 판정 둘 — 미해결 #34(다중 CDU 전이의 누출 주입 지점)와 파일럿 종료 판정. 그 전에는 새 판을 열지 않는다 |
| 5.5-D | 다중 CDU 누출 주입 지점 정정 (미해결 #34) — `plant.py` CDU별 누출 템플릿 · 저장 격자를 `storage_times_s` 로 · `leak_cdu_index` 열 · 데이터셋 재생성 | **앞 게이트·세션 5.5 게이트 전부 재판정 통과** — 판정 기준·허용오차를 하나도 바꾸지 않았다. **이 판의 목적이 확인됐다**: 누출을 CDU A 에만 걸자 부하가 그대로인 B 의 2차측 몫이 단조 증가(+50% 에서 +0.01908 L/s)하고 환수온도가 단조 감소(−0.01064 K)한다 — 세션 5 C7 정상상태 관측(+0.01905 L/s · −0.012649 K)과 **방향 동일 · 배분 이동 세 자리 일치**. 종전 양쪽 누출에서는 배분 이동이 +1.9e-05 L/s 로 상쇄됐다. 데이터셋 310,016행 · **49열** · 305.1 MB · 61.3분 · solver 실패 행 0. pytest 1,175건·ruff·mypy 통과 | **파일럿 종료 판정 전에 세 판이 남았다 — 5.6(누출 부호) · 5.7(#31 시간축) · 5.8(이동지연 감도). 이 셋을 넘어가는 것은 전부 설계데이터가 필요하다** (아래 「사람이 정한 것 — 세션 5.5-D 마무리」). 미해결 #34 는 닫혔다. **사람이 정할 것 — C4 의견**(다중 CDU 전이도 누출을 스텝으로 넣을 것인가) |
| 5.6 | 누출 질량손실 **부호 확인** (수력 한정 · 절대 규칙 8 예외, 사람 승인) — `leak_massloss.py` 단독 모듈 · K 근사 대 질량손실 전수 272회 | **게이트 없음 · 관측 판이다** (6장 기준을 하나도 판정하지 않았다 — 열모델이 없다). 판정 기준을 코드보다 먼저 적고(커밋 `7285ef7`) 그대로 판정했다. **부호가 갈린다**: 총유량·누출랙 유량이 K 근사(−)와 질량손실(+)에서 **정반대**, 펌프 양정은 5장·5-1 이 정하지 않는 **잔여저항 배치에 따라 갈린다**. 질량손실 누출은 헤더가 저항 0 인 이 모델에서 **랙에 국소화되지 않는다**(랙 간 비대칭 2.220e-16 L/s). 격리 확인 2.220e-16 L/s · `fsolve` 272회 전부 `ier=1`. pytest 1,175건·ruff·mypy 통과 | **모델·데이터셋을 고치지 않았다** — 관측을 미해결 **#36** 으로 올렸다. 어느 쪽으로 갈지는 사람이 정한다. 남은 판: 5.7(#31 시간축) · 5.8(이동지연 감도) |
| 5.6-B | 공급·환수 유량 불일치 Δ = Q_sup − Q_ret 확인 (5.6 결과 **재출력** — 새로 푼 것 없음) | **게이트 없음 · 관측 판이다** (6장 기준을 하나도 판정하지 않았다). 판정 기준을 코드보다 먼저 적고(커밋 `9d210e3`) 그대로 판정했다. **A 검산 통과**(질량손실 Δ−Q_leak 최대 8.882e-16 L/s · K 근사 Δ 정확히 0) — **단 구성상 항등식이라 물리 검증이 아니다**. **B 단조 통과**(전 배치·전 조합 엄격 증가). **C 배치 불변 통과**(배치 6 편차 1.776e-15 L/s) — **가설이 맞았다**. `g=0/펌프=공급` 배치에서 다섯 양이 전부 0 인데 Δ 만 +2.838e-02 ~ +1.877e-01 L/s (최대 1.211%)로 **유일한 생존 신호**다. 격리 확인: 5.6 의 표 1·2·3 이 **6,295 바이트 동일**. pytest 1,175건·ruff·mypy 통과 | **모델·데이터셋·5-1·#36 문구를 고치지 않았다.** 5.7 스키마의 Δ 열 필요 여부는 **#36 의 선택에 종속**이다 — (가)면 불필요, (나)·(다)면 배치 자유도에 흔들리지 않아 열로 넣을 수 있다. 사람이 정한다 |
| 5.7 | 미해결 **#31 시간축 정정**(M 을 8랙으로) · 위생 3건(#18·#19·#22) · 스키마 3열(`dataset_version`·`stimulus_kind`·`leak_model`) · 데이터셋 재생성 | **앞 게이트 전부 기존 기준 그대로 재판정 통과** — 판정 기준·허용오차를 하나도 바꾸지 않았고 새 게이트를 만들지 않았다. **정상상태 해가 M 8배에 불변**(model·hydraulics 표 바이트 동일) — 5-1 서술이 코드에서 성립했다. **τ·t63·t95 가 정확히 8.000000배 이동**(부하 스텝 4 · 누출 스텝 6 전부). 위생 3건은 수치 표 6개가 바이트 동일. 데이터셋 **310,016행 · 52열 · 359.8 MB · 57.0분 · solver 실패 행 0** · 표기·신3열 빈 칸 0. 검사기 4회(②④⑥⑨) 전부 pytest 1,175건·ruff·mypy 통과 | **#31 닫힘. 그러나 6장 「수렴시간」은 여전히 판정 불가** — #21·#25·#23 이 열려 있다. **사람이 할 일: 5-1 「누출 시나리오」·「누출 주입 지점」에 #36 한계 문구 추가**(정본 수정 · 재업로드 필요). 남은 판: 5.8(이동지연 감도) |
| 5.7-C | 오늘 결정 기록 (기록 판 — 정지선 변경 · 보충수 불필요 · 계통 dP 정리 · 시연 관점 · 미해결 #36 갱신 · #37 신규) | **게이트 없음 · feasibility 판정 없음 · 수치 변경 0.** 모델 코드·테스트·데이터셋(52열본)·`dataset_report`·`leak_massloss.py`·정본 문서 4종을 하나도 건드리지 않았다. 바뀐 파일은 `PROCEED.md` 하나. pytest 1,175건·ruff·mypy 그대로 통과 | **정지선이 한 판 넓어졌다** — 새 순서는 **5.7-D(질량손실 열·전이 관측) → 5.8(이동지연 감도) → 종료 판정**. **보충수 온도 문제가 닫혀**(미량 조기감지 구간에서는 보충 로직이 작동하지 않는다) 5.6 이 수력 한정을 택한 이유가 사라졌다. **사람이 할 일: 5-1 에 문구 둘 추가**(누출 시나리오 한계 · 보충수 처리) → 재업로드 필요 |
| 5.7-E | 정본 5-1 에 문구 둘 추가 (사람 지시 · 정본 수정) — 「누출 주입 지점」 근거 끝에 누출 K값 근사의 한계(#36 · 세션 5.6·5.6-B) · 새 행 「보충수 처리」(세션 5.7-C 확정) | **게이트 없음 · 문서 판이다 · 수치 변경 0.** 모델 코드·테스트·데이터셋(52열본)·`dataset_report`·`leak_massloss.py` 를 하나도 건드리지 않았다. 바뀐 파일은 `AI서버_CDU_디지털트윈_프로젝트정리.md`(5-1 두 줄) 와 `PROCEED.md` 둘. diff 가 5-1 밖으로 나가지 않음을 확인했다(2 insertions · 1 deletion · 전부 5-1 표 안). pytest 1,175건·ruff·mypy 그대로 통과 | **정본을 고쳤다 — 프로젝트 지식 재업로드가 필요하다.** 미해결 #36 의 (가)/(나)/(다) 최종 선택은 여전히 사람 몫이며 이 판은 고르지 않았다. 다음 판: **5.7-D**(질량손실 열·전이 관측 · #37 확인 포함) → 5.8(이동지연 감도) → 종료 판정 |
| 5.7-D | 질량손실 누출의 **열·전이 관측**(절대 규칙 8 예외 · 사람 승인) — `leak_massloss_thermal.py` 별도 모듈 · A~G 전수 · #37 확인 | **게이트 없음 · 관측 판이다**(6장 기준을 하나도 판정하지 않았다). 판정 기준을 코드보다 먼저 적고(커밋 `9bd246e`) 그대로 판정했다. **열에서도 부호가 갈린다** — 누출랙 출구온도·CDU 환수온도가 K 근사(**+**)와 질량손실(**−**)에서 **정반대**이고 전 조합(1,536건)에서 뒤집히지 않는다. **수력이 못 보는 것을 열이 본다** — 세션 5.6 이 다섯 양 전부 0 이던 `g=0/펌프=공급` 배치에서도 환수온도가 −7.368e-02 ~ −1.886e-03 K 로 살아 있다(배치 6 전부 같은 부호). **balance 는 누출 엔탈피 항을 넣어야 닫힌다** (빼면 1.2129% · 넣으면 0.000062%). **τ ∝ M 이 정확히 성립**(τ 비 = t63 비 = M 비 = 12.832779). **다중 CDU 는 K 근사와 방향이 같다**(B 몫 + · B 환수온도 −). 격리 7.105e-15 K · `fsolve`·`solve_ivp` 실패 0건. pytest 1,199건·ruff·mypy 통과 | **모델·데이터셋·5-1·규칙 8 문언을 하나도 고치지 않았다.** 관측을 #36 에 append 하고 **#38**(상수 M 유보) · **#39**(2차측 배분이 보는 1차측 유량)를 새로 열었다. #36 의 (가)/(나)/(다) 선택은 여전히 사람 몫이다. 남은 판: 5.8(이동지연 감도) |

> **※ 1-B 게이트가 실제로 검증한 것 (과대해석 금지)**
> 게이트가 쓰는 잔차는 *CoolProp 엔탈피 경로로 다시 계산한 1차측 흡열량* 과
> *5장 랙 발열량* 의 차다. 이것이 재는 것은 **모델이 쓴 상수 cp 선형화가 실제
> 엔탈피 변화와 어긋나지 않는가** 하나다.
> 반면 *HX ε-NTU duty 와 랙 발열량의 일치* 는 모델 구조상 **항등적으로 0**이다
> (실측 1.8e-14 %). 그쪽은 아무것도 증명하지 않으며 게이트에 쓰지 않았다.
> 통과의 뜻은 "무너지지 않는다"까지이고 **"정확하다"가 아니다** — 실측이 없다.

> **※2 세션 2 게이트가 실제로 판정한 것 (과대해석 금지)**
> 판정한 feasibility 기준은 **T_return 방향성 하나**다.
> · **수렴시간은 판정하지 않았다.** M(계통 보유수량)이 배관 보유량만 담고 있어
>   열교환기·CDU 내부·콜드플레이트 보유량이 빠져 있다(프로젝트정리 5-1 한계).
>   실제보다 M 이 작고 수렴시간도 짧게 나온다 — 6장 「수렴시간」 기준은 이
>   상태로 판정할 수 없다(미해결 #21).
> · **극단 케이스(부하 0/최대)는 돌리지 않았다** — 세션 3 게이트다.
> · t→∞ 값이 1-B 정상상태와 4.5e-9 K 안에서 일치했으나, 이는 **구현 일관성
>   검사**이지 물리의 독립 확인이 아니다. 동적 모델의 평형점은 설계상 1-B 와
>   같은 방정식이다 — 미해결 #15 는 여전히 열려 있다.

## 세션 로그

### 세션 1-A · 2026-08-27 · 저장소 clone · 개발환경 · 가정치 전사

**완료한 항목**

- 저장소 clone (`https://github.com/bakjeucar-boop/cdu-simul`, 빈 저장소 · 기본 브랜치 `main`)
- 정본 문서 4종을 저장소 루트에 복사 (`CLAUDE.md` 포함 — Claude Code 자동 인식 위치)
- Python 3.12.10 확인 → 저장소 루트에 `.venv` 생성
- 저장소 골격: `pyproject.toml` · `.gitignore` · `README.md` · `src/cdu_simul/` · `tests/`
- 승인 패키지 설치 (numpy 2.5.2 · scipy 1.18.1 · CoolProp 8.0.0 · pandas 3.0.5 · pytest 9.1.1)
- `src/cdu_simul/assumptions.py` — 프로젝트정리 5장 표 전사 (출처 태그 포함)
- `src/cdu_simul/fluid.py` — CoolProp 물성 래퍼 (밀도·비열)
- `tests/test_environment.py` — 환경 스모크 테스트 5건, 전부 통과
- `PROCEED.md` · `daily_brief.py` 초기 구성
- `CLAUDE.md` 「명령어」 절을 확정된 실제 값으로 채움

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `pyproject.toml` · `.gitignore` · `README.md` · `src/cdu_simul/__init__.py` | 신규 (골격) |
| `AI서버_CDU_디지털트윈_프로젝트정리.md` · `CLAUDE.md` · `collaboration.md` · `project-overview.md` | 신규 (문서 4종 복사) |
| `CLAUDE.md` 「명령어」 절 | 수정 — **웹 프로젝트 지식 재업로드 필요** |
| `src/cdu_simul/assumptions.py` | 신규 (5장 전사) |
| `src/cdu_simul/fluid.py` | 신규 (CoolProp 래퍼) |
| `tests/test_environment.py` | 신규 (환경 스모크) |
| `PROCEED.md` · `daily_brief.py` | 신규 |

**돌린 것 / 안 돌린 것**

- 돌렸음: `pytest` (5 passed) · `daily_brief.py` · `pip list` · CoolProp 물성 조회
- **안 돌렸음**: 물리 모델 일체. 이 세션에는 `CDUModel`·압력평형(`fsolve`)·ε-NTU·
  `solve_ivp`·유량분배가 없다. energy balance 오차 · T_return 방향성 · 수렴시간 ·
  극단 케이스 발산 — **6장 네 기준 중 어느 것도 이 세션에서 보지 않았다.**

**미해결 항목** — 아래 「미해결 목록」 절 참조.

**다음 세션이 알아야 할 결정**

1. **CoolProp 유체 문자열 위치**: `assumptions.py` 의 `SCENARIO.coolant_coolprop_id`
   에 두고 `fluid.py` 가 참조한다. PG25 선택 자체가 5장 가정치이므로 교체 지점이
   assumptions 한 곳이어야 한다는 판단(절대 규칙 2 · collaboration.md ④).
2. **가정치 파일 형식은 `.py`** (yaml 아님) — 타입힌트·frozen dataclass 에 맞고
   의존성이 늘지 않는다.
3. **범위값은 `Range(low, high, unit)` 로 범위 그대로 보존.** 중앙값 등 대표값을
   임의로 고르지 않았다. `Range` 에 대표값 추출 메서드를 일부러 두지 않았다.
4. **5장 표 원단위를 그대로 보존**(mAq · L/s · ℃ · kW · %). 단위 변환은 하지 않았다.
   유일한 변환은 `fluid.py` 의 ℃→K (CoolProp 호출 경계 전용).
5. **파생값은 만들지 않았다** — 밸브 Cv 역산, 펌프 곡선 계수 H0·a·b, 배관 K값.
   5장 표에 수치가 없다. 1-B에서 필요해지면 사람에게 먼저 확인한다.
6. **Python 3.12.10 사용** (시스템에 3.13·3.12 존재, 3.11 없음). CoolProp 8.0.0 은
   cp312 휠로 설치됐다.
7. **패키지 임포트는 편집설치**(`pip install -e ".[dev]"`)로 해결했다. `PYTHONPATH`
   조작이나 `sys.path` 삽입을 코드에 넣지 않았다.

### 세션 1-B · 2026-08-27 · 단일 랙·단일 CDU 정상상태 모델 · energy balance 검증

**완료한 항목**

- 세션 시작 결정사항 ㉮~㉳ 를 미해결 목록에 반영 (#1·#2 → 세션 3, #3 → 방침 (B) 확정,
  #5 문구 교체, #7 → 세션 3 재검토, #10·#11 신규)
- `src/cdu_simul/model.py` — 단일 랙·단일 CDU 정상상태 모델 (ε-NTU + 고정점 fsolve)
- `src/cdu_simul/fluid.py` — `coolant_enthalpy_Jkg` 추가 (balance 검사용 독립 경로)
- `tests/test_energy_balance.py` — balance 자동 검사 12건
- 4케이스 실행 (2차측 27/30℃ × NTU 2/3, 부하 100% 고정)

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/model.py` | 신규 (정상상태 모델) |
| `src/cdu_simul/fluid.py` | 수정 (엔탈피 조회 추가) |
| `tests/test_energy_balance.py` | 신규 (balance 자동 검사) |
| `PROCEED.md` | 수정 (결정사항 반영 + 이 로그) |

**결과 표** (`.venv/Scripts/python.exe -m cdu_simul.model` · 가정값 기반 · 실측 아님)

| 케이스 | T_supply [℃] | T_return [℃] | ΔT [K] | HX duty [kW] | balance 잔차 [%] | solver |
|---|---|---|---|---|---|---|
| 2차측 27℃ · NTU 2 | 32.15 | 42.44 | 10.29 | 80.00 | -0.00502 | 수렴 |
| 2차측 27℃ · NTU 3 | 30.43 | 40.72 | 10.29 | 80.00 | -0.00506 | 수렴 |
| 2차측 30℃ · NTU 2 | 35.14 | 45.43 | 10.29 | 80.00 | -0.00494 | 수렴 |
| 2차측 30℃ · NTU 3 | 33.43 | 43.72 | 10.29 | 80.00 | -0.00498 | 수렴 |

**돌린 것 / 안 돌린 것**

- 돌렸음: 4케이스 정상상태 해 · `pytest` 17건 전부 통과 · cp 규칙 3종 감도 비교
- `fsolve` 성공 플래그(`ier`)를 4케이스 모두 확인했다 — 전부 `ier == 1`
  ("The solution converged.")
- **안 돌렸음**: 시간축·`solve_ivp`(세션 2) · 압력평형·`fsolve` 유량분배·랙 확장(세션 3) ·
  부하 0/최대 극단 케이스(세션 3 게이트) · 누출 시나리오(세션 4) · 다중 CDU(세션 5) ·
  시각화. **6장 네 기준 중 energy balance 하나만 봤다.** T_return 방향성 · 수렴시간 ·
  극단 케이스 발산은 보지 않았다.

**다음 세션이 알아야 할 결정**

1. **balance 잔차 정의** = ( m_dot·[h(T_return) - h(T_supply)] - Q_rack ) / Q_rack × 100.
   CoolProp 엔탈피 경로를 써서 모델이 해를 구할 때 쓴 상수 cp 경로와 **독립**하게 만들었다.
   이 정의를 바꾸면 1-B 통과의 의미가 바뀐다.
2. **HX duty 잔차는 구조상 항등적으로 0**(실측 1.8e-14 %). `test_energy_balance.py` 의
   `test_hx_duty_residual_is_structural_identity` 가 이 사실 자체를 박아 두었다 —
   게이트가 아니라 오해 방지용이다.
3. **cp·ρ 평가 온도 규칙 = 1차측 벌크 평균온도(`bulk_mean`)**. 5장 표에 없는
   수치처리 규약이며 이 판에서 정했다. **이 선택이 게이트 통과/실패를 가른다**:
   `bulk_mean` 0.00506% (통과) · `supply` 0.30891% (실패) · `return` 0.31524% (실패).
   즉 게이트는 헛돌지 않는다.
4. **HX 유동배열을 대향류로 골랐다** — 5장에 배열 지정이 없다(미해결 #12).
   ε = NTU/(1+NTU) (열용량유량비 1).
5. **2차측 유체를 1차측과 같은 냉각액으로 보았다** — 5장에 2차측 유체가 없다.
   5장의 유량비 1:1 과 합쳐 열용량유량비 Cr = 1 로 두었다(미해결 #13).
   물 등 다른 유체를 도입하는 것이야말로 5장 밖 도입이라고 판단했다.
6. **압력-유량을 풀지 않았다.** 단일 랙이라 분배할 곳이 없다. 하이브리드 구조
   (절대 규칙 4)를 없앤 것이 아니라 **아직 만들지 않은 것**이다 — 세션 3에서 도입한다.
7. **결과 표 출력 진입점은 `python -m cdu_simul.model`** 이다. 새 스크립트 파일을
   만들지 않고 모듈 `__main__` 으로 두었다. `CLAUDE.md` 「명령어」 절에는 넣지 않았다 —
   이번 판은 문서 수정 권한이 없다(미해결 #16).
8. **계산 ΔT ≈ 10.29 K** 로 5장의 ΔT 10℃ 를 근사 재현했다(미해결 #5 확인). 다만
   **관측이며 판정이 아니다.** 5장 값에 맞추려고 아무것도 조정하지 않았다.

### 세션 1-C · 2026-08-27 · 저장소 위생 정비 (물리 모델 손대지 않음)

**완료한 항목**

- 정본 문서 4종 상태 확인 — 갱신본이 **이미 저장소 루트에 있었다**. `cdu-docs` 쪽이
  세션 1-A 시점 구본이어서 **복사하지 않았다**(복사했으면 사람이 고친 내용을 되돌렸다).
- `ruff` 0.16.4 · `mypy` 2.3.1 도입 — 의존성·설정 모두 `pyproject.toml` 한 곳에
- ruff 위반 4건(전부 E501) 수정 — **주석 줄바꿈만**, 문구·의미 그대로
- mypy 위반 2건 수정 — `__defaults__` 의 `None` 좁히기만, 판정 내용 그대로
- `#12`(대향류)·`#14`(cp 규칙) 근거를 **프로젝트정리 5-1** 로 교체 — 주석·출처만
- `.gitattributes` 추가(#9) · #6·#10·#11 관측만 · #16 확인 후 닫음
- 정비 전후 4케이스 동일성 증명 · pytest 17건 · ruff · mypy 전부 통과

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` · `CLAUDE.md` · `project-overview.md` | 사람이 고친 갱신본을 커밋 (5-1 소절 · 정형 절차 절 · 1-C 게이트 행) |
| `pyproject.toml` | 수정 (ruff·mypy 의존성 + 설정) |
| `src/cdu_simul/assumptions.py` | 수정 (**주석 줄바꿈만** — 값·구조 불변) |
| `src/cdu_simul/model.py` | 수정 (**주석·출처만** — 계산식 불변) |
| `tests/test_environment.py` | 수정 (타입 좁히기만 — 판정 내용 불변) |
| `.gitattributes` | 신규 (줄바꿈 정규화) |
| `PROCEED.md` | 수정 (이 로그) |

**정비 전후 동일성 (이 판의 게이트 대용)**

`git stash` 로 정비 전 코드(HEAD)를 되살려 같은 스크립트를 돌리고, 정비 후 결과와
대조했다. **T_supply · T_return · ΔT · HX duty · ε · m_dot · cp · 물성평가온도 ·
balance 잔차 — 9개 물리량 × 4케이스가 `repr` 전체자리까지 한 글자도 다르지 않다.**
`-m cdu_simul.model` 출력 파일도 문자 단위로 동일했다.

**돌린 것 / 안 돌린 것**

- 돌렸음: `ruff check .` · `mypy src tests` · `pytest`(17 passed) ·
  `-m cdu_simul.model` 정비 전/후 2회 · `git ls-files --eol`
- **안 돌렸음**: 물리 모델 변경 일체 · 시간축(세션 2) · 압력평형(세션 3) ·
  누출(세션 4) · 다중 CDU(세션 5) · 시각화.
  **6장 feasibility 기준은 이번 판에서 하나도 보지 않았다.**
  ruff·mypy 통과는 "코드가 문법·타입·스타일상 성립한다"는 뜻이지 "물리적으로
  맞다"는 뜻이 아니다 — feasibility 로 적지 않는다.

**다음 세션이 알아야 할 결정**

1. **`cdu-docs` 폴더는 구본이다.** 갱신본은 저장소 루트에만 있다. `CLAUDE.md`
   「작업 시작 전 필독」 4 대로 **저장소 사본이 정본**이므로 그대로 두었으나,
   사람이 `cdu-docs` 를 동기화하거나 폐기하는 편이 안전하다(미해결 #17).
2. **ruff 는 한글을 2칸 폭으로 센다.** `line-length = 90` 은 표시폭 기준이며
   기존 코드의 최장 표시폭은 99(한글 주석)였다. 코드 자체의 최장 줄은 89자다.
   위반 4건은 전부 한글 주석이었고 줄바꿈으로 해소했다.
3. **`.gitattributes` 는 파일 내용을 하나도 바꾸지 않았다.** `git ls-files --eol`
   로 확인한 결과 저장소·작업트리 모두 이미 LF 였다(`i/lf w/lf`). 기존의
   `LF will be replaced by CRLF` 경고는 **앞으로의 체크아웃 방향**에 대한 것이었지
   저장된 바이트가 CRLF 였다는 뜻이 아니다. 따라서 "줄바꿈만 바뀌는 커밋"은
   실제로 발생하지 않았고, 해당 커밋에는 `.gitattributes` 한 파일만 들어 있다.
4. **`requires-python = ">=3.11"`** 은 그대로 두었다(고치지 않았다 — 환경 정책은
   사람 소관, 절대 규칙 12). 의견은 미해결 #10 에 적었다.
5. **커밋 작성자 설정을 바꾸지 않았다.** 현재 값은 `WIR Generator
   <seccpi002@gmail.com>` 이다(미해결 #11 유지).

### 세션 2 · 2026-08-27 · 시간축 추가 (solve_ivp) · T_return 응답 방향성

**완료한 항목**

- 프로젝트정리 5-1 갱신본(배관 내경 · 계통 보유수량 M) 커밋
- `assumptions.py` — 배관 내경 3종 `[규격값: ASME B36.10M Sch40 · 5-1]` +
  M 범위를 내는 두 극단 구경(`holdup_bound_inner_diameters_mm`)
- `src/cdu_simul/dynamics.py` — 계통 보유수량 계산 + 2노드 온도 `solve_ivp` 적분
- `tests/test_dynamics.py` — 방향성 게이트 16건 · solver 확인 16건 · 정상상태 대조 4건
- 부하 스텝 4케이스 실행 · `requires-python` 을 `">=3.12"` 로 좁힘

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 사람이 고친 5-1 갱신본을 커밋 (배관 내경 · 계통 보유수량 M) |
| `src/cdu_simul/assumptions.py` | 수정 (배관 내경 · M 규칙 — 5-1 출처 태그) |
| `src/cdu_simul/dynamics.py` | 신규 (동적 모델 + 열용량 계산) |
| `tests/test_dynamics.py` | 신규 (세션 2 게이트 + 정상상태 대조) |
| `pyproject.toml` | 수정 (`requires-python ">=3.12"`) |
| `PROCEED.md` | 수정 (이 로그) |
| `src/cdu_simul/model.py` | **건드리지 않았다** — 1-B 결과가 1-A 기준값과 문자 단위로 동일함을 재확인 |

**결과 표** (`.venv/Scripts/python.exe -m cdu_simul.dynamics` · 가정값 기반 · 실측 아님)

| 케이스 | M [kg] | τ [s] | t63 [s] | t95 [s] | T_return 초기→최종 [℃] | 방향 | solve_ivp |
|---|---|---|---|---|---|---|---|
| 상승 20→100% · M 하한 | 11.28 | 5.72 | 5.37 | 18.72 | 30.091 → 42.437 | 상승 | 성공 |
| 하강 100→20% · M 하한 | 11.28 | 5.75 | 5.39 | 18.71 | 42.437 → 30.091 | 하강 | 성공 |
| 상승 20→100% · M 상한 | 144.70 | 73.46 | 68.87 | 240.21 | 30.091 → 42.437 | 상승 | 성공 |
| 하강 100→20% · M 상한 | 144.70 | 73.75 | 69.14 | 240.06 | 42.437 → 30.091 | 하강 | 성공 |

※ M 은 배관 보유량만 — HX·CDU 내부·콜드플레이트 제외(과소평가). 위 수렴시간은
실제보다 짧다. **"합리적"이라고 판정하지 않았다.**

**정상상태 대조 (C7)**

| 1-B 케이스 | ΔT_supply [K] | ΔT_return [K] |
|---|---|---|
| 2차측 27℃ · NTU 2 | -2.342e-10 | +1.453e-10 |
| 2차측 27℃ · NTU 3 | +2.235e-09 | -4.476e-09 |
| 2차측 30℃ · NTU 2 | -1.602e-09 | +2.510e-09 |
| 2차측 30℃ · NTU 3 | -3.294e-10 | +6.555e-10 |

최대 편차 **4.476e-09 K**. 허용오차는 **1e-6 K** 로 정했다 — 관측된 적분 잡음보다
200배 이상 느슨하고, 두 경로가 실제로 어긋났을 때 나타날 크기(4케이스 온도 폭 ~3 K)
보다는 6자리 이상 엄격하다.

**돌린 것 / 안 돌린 것**

- 돌렸음: 부하 스텝 4케이스 · 방향성 게이트 16조합 · 정상상태 대조 4케이스 ·
  `pytest` 53건 전부 통과 · `ruff` · `mypy` · 1-B 결과 불변 재확인
- `solve_ivp` 의 `success` 를 20회 적분 전부에서 확인했다 — 전부 True
- **안 돌렸음**: 압력평형·`fsolve` 유량분배·랙 확장(세션 3) · 극단 케이스 부하
  0/최대(세션 3 게이트) · 누출(세션 4) · 다중 CDU·2차측 동특성(세션 5) · 시각화.
  **6장 네 기준 중 이 판에서 판정한 것은 T_return 방향성 하나뿐이다.**

**다음 세션이 알아야 할 결정**

1. **2노드 구조**: T_supply 노드(HX 출구~랙 입구) · T_return 노드(랙 출구~HX 입구).
   `dT/dt = 0` 을 넣으면 1-B 정상상태 식이 그대로 나온다 — C7 대조가 성립하는 이유다.
   동시에 그래서 C7 은 **구현 일관성 검사**이지 물리의 독립 확인이 아니다.
2. **M 을 두 노드에 절반씩 배분**했다. 5장 등가길이를 "왕복 전체"로 읽으라는 5-1
   규칙에서 공급측·환수측이 그 왕복의 두 다리이기 때문이다. **배분 비율 자체는
   5장에도 5-1 에도 없다**(미해결 #20).
3. **M 산출용 ρ 는 고정**(5장 1차측 벌크평균 37℃ → 1011.4681 kg/m³). 반면 평형
   온도를 정하는 cp·ρ 는 **미분방정식 안에서 매 시점 다시 평가**한다 — 그래야
   t→∞ 극한이 1-B 해와 정확히 일치한다.
4. **M 범위는 구경과 등가길이 양 끝을 함께 취했다**: 하한 = 25A + 20 m = 11.276 kg,
   상한 = 80A + 30 m = 144.698 kg. 둘 다 5장에서 범위로 주어졌으므로 한쪽만
   극단으로 잡으면 M 의 실제 범위가 아니다.
5. **적분 설정**: RK45 · rtol 1e-10 · atol 1e-12 · 구간 30τ. 물리 가정이 아니라
   수치 설정이며 `dynamics.py` 상수로 노출돼 있다.
6. **결과 표는 2차측·NTU 를 범위 하단으로 고정**했다(부하와 M 만 변화시켜 보려고).
   **게이트 테스트는 고정하지 않는다** — 2차측·NTU 양 끝까지 16조합 전부 돌린다.
7. **`dynamics.py` 가 `model._property_temperature_from_state`(비공개 이름)를
   import** 한다. cp 평가 규칙을 두 곳에 적지 않으려는 것이고, `model.py` 수정
   금지 때문에 공개화하지 않았다(미해결 #22).
8. **T_return 응답은 실질적으로 단조**다 — 반대부호 증분의 최댓값이 1.4e-10 K 이고
   전부 적분 끝의 평탄한 구간에서 나온다(전체 변화량 12.35 K). 적분기 잡음이며
   물리적 오버슈트가 아니다. **단조성을 게이트로 삼지는 않았다.**

### 세션 2.5 · 2026-08-28 · 문서 정합 판 (코드 변경 없음)

**완료한 항목**

- 사람이 어제 채운 5-1 파생 가정치(펌프 곡선 계수 H₀·a·b · 밸브 개도특성/Cv · 배관 K값 ·
  mAq 단위 규약)와 `CLAUDE.md` Python 버전 정정·`project-overview.md` 진행 표시를 커밋
- 프로젝트정리 5-1 머리말에 **「5-1 빈칸 처리 순서」** 추가 (역산 → 규격·정의값 →
  범위 양 끝 → 추가 파라미터 0의 규약 → 그래도 안 되면 사람에게 확인)
- 5-1 표에 2행 추가: **M 의 노드 배분(공급 50% · 환수 50% — 수치처리 규약)** ·
  **계통 잔여저항(HX 1차측 + CDU 내부배관 + 헤더) 배정 규칙**
- 6장 「수렴시간」 기준 재정의 — 절대 수렴시간의 "합리성"을 판정하지 않고,
  **τ 가 M 에 비례하고 유량에 반비례하는 정합성**만 본다
- `project-overview.md` 「파일럿 종료 조건」 절 전면 교체 — 사전 수치 기준을 두지 않고
  **세션 5.5 산출물을 보고 사람이 종합 판단**한다
- **세션 5.5 신설** (시나리오 배치 실행 → 텍스트 데이터셋) — `CLAUDE.md` 게이트 표와
  `project-overview.md` 체크리스트 양쪽에 반영

**만든/바꾼 파일** — 전부 문서다. **코드·테스트는 한 줄도 건드리지 않았다.**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 빈칸 처리 순서 · 5-1 표 2행 추가 · 6장 수렴시간 기준 교체 |
| `project-overview.md` | 파일럿 종료 조건 절 교체 · 체크리스트 5.5 추가 · 「지금 어디인가」 첫 줄 정정 |
| `CLAUDE.md` | 게이트 표 5.5 행 추가 · 표 하단 문단 교체 |
| `PROCEED.md` | 이 로그 · 미해결 목록 갱신 |

**다음 세션이 알아야 할 결정 사항**

1. **5장 빈칸은 이제 「기다린다」가 선택지가 아니다.** 5-1 빈칸 처리 순서 4단계가
   정본이며, 넷 다 안 될 때만 사람에게 묻는다. 설계데이터를 기다리는 항목은 없다.
2. **계통 잔여저항은 집중저항 하나**로 총유량 경로에 배정한다. 헤더는 저항 0 의 공통
   노드다. 잔여저항은 독립 범위가 아니라 각 조합의 **종속값**이므로 조합 수가 늘지 않는다.
   → **세션 3 의 유량분배 결과는 이 배정 방식이 지배한다**(미해결 #24). 수렴·비발산
   게이트 통과를 "유량분배 값이 타당하다"로 읽지 않는다.
3. **M 의 노드 배분 50:50 은 물리 가정이 아니라 규약**이다. 정상상태 해에는 영향이
   없고 전이 파형에만 걸린다 — 누출 신호가 전이 파형에 걸리는 **세션 4에서 민감도 확인**.
4. **파일럿의 마지막 판은 세션 5.5** 이고, 종료 판정 주체는 사람이다. 세션 6 은
   설계데이터가 있어야 열리며 **파일럿의 정상 종착지가 아니다.**
5. **5-1 의 새 값들을 `assumptions.py` 에 전사하는 것은 세션 3 이다** — 이 판은 문서
   판이라 넣지 않았다. 코드는 아직 잔여저항·M 배분·펌프 곡선 계수를 모른다.
6. 이 판에서 판정한 feasibility 기준은 **없다.** 검사기(pytest·ruff·mypy) 통과는
   "코드를 건드리지 않았음"의 확인이지 물리 판정이 아니다.

### 세션 3-A · 2026-08-28 · 수력 모듈 (유량분배 단독 · 열모델 미결합)

**완료한 항목**

- `assumptions.py` 에 5-1 수력 가정치 전사 — mAq 규약(`PASCAL_PER_MAQ`) · 펌프
  특성곡선 계수 2세트 · 밸브 개도특성(선형)·Kv_max 2값 · 랙 분기 K 범위 ·
  M 노드 배분 50:50. **전부 5-1 에 적힌 숫자를 그대로 옮겼고 코드가 유도하지 않았다**
- `hydraulics.py` 신규 — 펌프곡선 · 밸브 ΔP(Kv 식) · 분기 ΔP(K·ρv²/2) ·
  계통 잔여저항 역산(5-1 규칙) · 8랙 헤더 압력평형 `fsolve`
- `tests/test_hydraulics.py` 신규 — 36건 (수렴 8 · 균등분배 8 · K+50% 방향성 8 ·
  잔여저항 몫 8 · 펌프곡선 단조 2 · 전사값 고정 1 · 정격점 재현 1)
- 8조합 결과 표 출력 (`python -m cdu_simul.hydraulics`)

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/assumptions.py` | 수정 (+약 100줄 — 5-1 수력 가정치 전사) |
| `src/cdu_simul/hydraulics.py` | 신규 (약 400줄) |
| `tests/test_hydraulics.py` | 신규 (약 190줄) |
| `PROCEED.md` | 수정 (이 로그) |
| `model.py` · `dynamics.py` | **건드리지 않았다** — 두 모듈 출력이 판 시작 전과 **바이트 단위로 동일**함을 확인했다 |

**결과 표** (`python -m cdu_simul.hydraulics` · 개도 80% · 8랙 동일 · 가정값 기반 · 실측 아님)

| 조합 | 잔여저항 몫 [%] | 운전점 총유량 [L/s] | 정격 15.5 대비 [%] | ier |
|---|---|---|---|---|
| H20/K3.20/Kv16.19 | 75.01 | 15.5045 | +0.029 | 1 |
| H20/K3.20/Kv12.54 | 65.01 | 15.5062 | +0.040 | 1 |
| H20/K4.80/Kv16.19 | 70.01 | 15.5054 | +0.035 | 1 |
| H20/K4.80/Kv12.54 | 60.01 | 15.5071 | +0.046 | 1 |
| H30/K3.20/Kv16.19 | 83.34 | 15.5030 | +0.019 | 1 |
| H30/K3.20/Kv12.54 | 76.67 | 15.5042 | +0.027 | 1 |
| H30/K4.80/Kv16.19 | 80.01 | 15.5036 | +0.023 | 1 |
| H30/K4.80/Kv12.54 | 73.34 | 15.5048 | +0.031 | 1 |

**다음 세션이 알아야 할 결정 사항**

1. **잔여저항 몫이 60.01~83.34% 로 나왔다** — 5-1 이 적은 60~83% 가 코드에서
   그대로 재현됐다. 미해결 #24 가 **관측으로 확인된 것**이다. 유량분배 결과는
   사실상 이 배정 방식이 지배하며, `fsolve` 수렴을 "유량분배 값이 타당하다"로
   읽지 않는다. 이 문구를 결과 표 안에 넣어 산출물이 스스로 달고 다니게 했다.
2. **랙 간 유량은 정확히 균등하게 나왔다**(상대오차 1e-9 이내). 이것은 물리의
   발견이 아니라 **5-1 배정 규칙이 강제하는 결과**다 — 잔여저항이 랙 공통 경로에
   있고 8랙 조건이 동일하기 때문이다. 테스트 docstring 에도 그렇게 적었다.
3. **두 정격 기준의 0.13% 어긋남**(펌프 15.5 L/s 대 8×1.94=15.52)은 5장 자체의
   반올림이며 **어느 쪽도 고치지 않았다**. 운전점에는 +0.019~+0.046% 로 전파된다
   — 양쪽이 모두 Q² 형태라 어긋남이 줄어든 것이다. "충분히 작다"고 판단하지
   않았고 값만 적었다.
4. **SG 가 5-1 기록(1.0124)과 CoolProp 값(1.01147)에서 0.09% 다르다.** 5-1 의
   Kv·K 는 5-1 의 SG 로 역산된 값이라, 이 모듈이 정격점에서 재현하는 ΔP 가 5장
   표값과 0.1% 이내로 어긋난다(분기 1.995 vs 2.000 · 밸브 2.993 vs 3.000).
   **어느 쪽도 고치지 않았다** — 5-1 값 전사가 이 판의 규칙이다(미해결 #27).
5. **물성 온도가 아직 5장 고정값이다.** `property_temperature_C()` 는 5장 1차측
   공급·환수의 벌크평균(37℃)을 쓴다. 열모델이 붙으면 이 온도가 풀린 상태에서
   나와야 하고 밀도가 유량에 의존하므로 **수력-열 연립**이 된다 — 세션 3-B 의
   첫 결정 사항이다(미해결 #28).
6. **개도 0 을 막아 뒀다.** 선형 특성에서 개도 0 은 Kv=0 이라 ΔP 가 정의되지
   않으므로 `valve_dp_mAq` 가 `ValueError` 를 던진다. **세션 3-B 의 "부하 0"
   극단 케이스는 개도가 아니라 발열량 0 이다** — 혼동하지 않는다.
7. **누출은 구현하지 않았다.** K+50% 방향성 검사 하나만 했고(랙0 −7.93% ·
   나머지 랙 +0.85% · 총유량 −0.25%), 이것을 누출 결과로 적지 않는다. 신호 패턴
   판정은 세션 4 게이트다. `HydraulicCase.branch_K_multipliers` 가 세션 4 의
   진입점이 되도록 자리만 만들어 뒀다.

### 세션 3-A2 · 2026-08-28 · 물성 단일화 보정 (Kv·K 재역산 · 수력 물성 온도 인자화)

**왜 열었나** — 세션 3-A 관측 ④. 5-1 이 SG 를 숫자(1.0124)로 적어 두었고 CoolProp
값(1.01147)과 0.09% 갈렸다. 5-1 은 Kv·K 를 **「역산」 등급**으로 기록했는데, 역산값
이라면 같은 물성원에서 다시 역산했을 때 같은 값이 나와야 한다. 그것이 안 나와서
정격점 ΔP 가 5장 표의 2.000/3.000 이 아니라 1.995/2.993 으로 재현되고 있었다
(`collaboration.md` 결함유형 ③ — 같은 물성치가 다른 자리에서 다르게 정의).

**완료한 항목**

- 프로젝트정리 5-1: 「밸브 Cv」·「배관 K값」 두 행을 **값에서 역산 규칙으로** 교체
  (「계통 잔여저항」 행과 같은 형태). 「수력 계산의 물성 평가 온도」 행 신규 추가
- `fluid.py`: SG 정의(`SG_REFERENCE_DENSITY_kgm3` = 1000 · `coolant_specific_gravity`)를
  **CoolProp 래퍼 한 곳으로** 모음 — SG 가 두 자리에서 다르게 정의될 수 없게 했다
- `assumptions.py`: Kv 상수 2개 · K 상수 2개 **삭제**. 5장 원 조건(1.94 L/s ·
  개도 80% · ΔP 3~5 / 2~3 mAq)만 남기고 역산 규칙 포인터를 주석으로 남김
- `hydraulics.py`: 역산 함수 2개 신규(`valve_Kv_max_m3h_from_rated_dP` ·
  `branch_K_from_rated_dP`) · 고정 37℃ 제거 · 물성 온도를 **기본값 없는 필수 인자**로
- `tests/test_hydraulics.py`: 36건 → 44건. 허용오차를 늘리지 않았다

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 두 행 교체 + 한 행 추가 (사람의 지시) |
| `src/cdu_simul/fluid.py` | 수정 (SG 정의 단일화) |
| `src/cdu_simul/assumptions.py` | 수정 (Kv·K 상수 삭제 → 규칙 포인터) |
| `src/cdu_simul/hydraulics.py` | 수정 (역산 함수 · 물성 온도 인자화) |
| `tests/test_hydraulics.py` | 수정 (44건) |
| `model.py` · `dynamics.py` | **건드리지 않았다** — 두 모듈 출력이 판 시작 전과 바이트 단위로 동일 |

**정격점 ΔP 재현 (C6 — 이 판의 핵심)** · 37℃ · 랙당 1.94 L/s · 개도 80%

| 항목 | 5장 표값 [mAq] | 역산 K/Kv | 되계산 ΔP [mAq] | 잔차 [mAq] |
|---|---|---|---|---|
| 분기 하한 | 2.000 | K = 3.2014125498 | 2.000000000000000 | −4.4e-16 |
| 분기 상한 | 3.000 | K = 4.8021188247 | 3.000000000000000 | −4.4e-16 |
| 밸브 하한 | 3.000 | Kv = 16.1871124195 | 2.999999999999998 | −1.8e-15 |
| 밸브 상한 | 5.000 | Kv = 12.5384833648 | 5.000000000000002 | +1.8e-15 |

세션 3-A 에서는 같은 자리가 1.995 / 2.993 mAq(오차 2.5e-3 · 7e-3)였다.
**잔차가 배정밀도 반올림(1e-15)까지 내려간 것은 역산의 역이 항등적으로 성립한다는
뜻이며, 물리적 정확도가 좋아졌다는 뜻이 아니다** — 5장 표값 자체가 가정치다.

**8조합 재출력 · 세션 3-A 대비 변화** (가정값 기반 · 실측 아님)

| 조합 | 잔여저항 몫 [%] (3-A → 3-A2) | 운전점 총유량 [L/s] (3-A → 3-A2) |
|---|---|---|
| H20/dPb2/dPv3 | 75.01 → 75.00 | 15.5045 → 15.5045 |
| H20/dPb2/dPv5 | 65.01 → 65.00 | 15.5062 → 15.5062 |
| H20/dPb3/dPv3 | 70.01 → 70.00 | 15.5054 → 15.5054 |
| H20/dPb3/dPv5 | 60.01 → 60.00 | 15.5071 → 15.5071 |
| H30/dPb2/dPv3 | 83.34 → 83.33 | 15.5030 → 15.5030 |
| H30/dPb2/dPv5 | 76.67 → 76.67 | 15.5042 → 15.5042 |
| H30/dPb3/dPv3 | 80.01 → 80.00 | 15.5036 → 15.5036 |
| H30/dPb3/dPv5 | 73.34 → 73.33 | 15.5048 → 15.5048 |

유량은 소수 4자리에서 변화가 없고, 잔여저항 몫만 0.01%p 씩 움직였다 — 분기·밸브
ΔP 가 5장 표값을 정확히 맞추게 되면서 잔여분이 그만큼 정확해진 것이다.
**케이스 이름이 바뀌었다**: `K3.20/Kv16.19` → `dPb2/dPv3`. 5장이 주는 것은 ΔP 이고
K·Kv 는 그 역산값이므로, 이름도 5장 원 조건을 쓰는 것이 맞다.

**다음 세션이 알아야 할 결정 사항**

1. **Kv·K 는 이제 상수가 아니라 역산 결과다.** `assumptions.py` 에 그 숫자를 다시
   넣으면 물성원이 두 개로 갈린다 — `test_valve_Kv_and_branch_K_are_not_hardcoded_constants`
   가 이름의 부재로 그것을 막는다.
2. **Kv·K·잔여저항은 정격 물성(37℃)에서 한 번 역산하고 고정한다**(5-1). 온도에
   따라 변하는 것은 그것들이 아니라 그것으로 계산한 ΔP 다. 역산 함수는 온도 인자를
   받지 않으며, 테스트가 시그니처로 그것을 고정한다.
3. **물성 온도는 이제 호출부의 책임이다** — 기본값이 없다. 세션 3-B 는 열모델이
   푼 공급·환수 온도를 `bulk_mean_temperature_C(...)` 로 넣게 된다.
4. **잔여저항만 온도 스케일을 따르지 않는다**(미해결 #30). 5-1 규칙을 이 판에서
   바꾸지 않기로 했으므로 고치지 않고 관측으로 남겼다 — 3-B 에서 사람이 정한다.
5. **세션 3 게이트는 여전히 절반이다**(미해결 #29). 극단 케이스(부하 0/최대)
   비발산은 안 봤다. 이 판은 물성 정합 보정이며 feasibility 를 하나도 판정하지 않았다.

### 세션 3-B · 2026-08-28 · 열모델 결합 (8랙 정상상태·동적) · 세션 3 게이트 판정

**완료한 항목**

- 5-1 에 3행 추가(사람 확정, 코드보다 먼저): 계통 잔여저항의 물성 의존(ρ 비례) ·
  펌프곡선의 물성 의존(없음) · 부하 프로파일의 랙 배분(CDU 전체 일괄)
- `hydraulics.py`: 잔여저항을 ρ 스케일로. **37℃ 8조합 표가 세션 3-A2 와 바이트
  단위로 동일**함을 격리 확인한 뒤 단독 커밋
- `model.py`: 랙 축을 펴 8랙으로 확장(랙 수는 `assumptions.py` 에서 읽는다) ·
  `CduCase`/`solve_cdu_steady_state` 로 수력↔열 결합 · 부하 0 잔차 가드
- `dynamics.py`: 매 시점 압력평형 결합(절대 규칙 4 완성)
- `tests/test_session3_gates.py` 신규 · `test_energy_balance.py`·`test_dynamics.py` 갱신
- 결과 표 3종에 과대해석 금지 ※ 문구(`SESSION_3B_CAVEAT`) 삽입

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 에 3행 추가 (사람의 지시) |
| `src/cdu_simul/assumptions.py` | 수정 (`SESSION_3B_CAVEAT`) |
| `src/cdu_simul/hydraulics.py` | 수정 (잔여저항 ρ 스케일 · 표 문구) |
| `src/cdu_simul/model.py` | 수정 (8랙 확장 · 수력 결합 · 32조합 표) |
| `src/cdu_simul/dynamics.py` | 수정 (매 시점 수력 결합) |
| `tests/test_session3_gates.py` | 신규 (세션 3 게이트) |
| `tests/test_energy_balance.py` · `tests/test_dynamics.py` | 수정 (8랙 재확인) |
| `PROCEED.md` | 수정 (이 로그) |

**세션 3 게이트 판정** (가정값 기반 · 실측 아님)

| 게이트 | 판정 | 근거 |
|---|---|---|
| ㉠ 헤더 압력평형 유량분배 `fsolve` 수렴 | **통과** | 열결합 32조합 전수. 수력 `fsolve` `ier=1` · 열 `fsolve` 수렴 · 결합 고정점 `fsolve` 수렴 — 셋 다 32/32 |
| ㉡ 극단 케이스(부하 0/최대) 비발산 | **통과** | 32조합 × {0%, 100%} 정상상태 + 동적 4케이스. 아래 표 |

**앞 게이트 재확인**

| 게이트 | 판정 | 값 |
|---|---|---|
| 1-B energy balance <0.1% | **통과** | 32조합 최대 \|잔차\| = **0.00507 %** |
| 세션 2 T_return 방향성 | **통과** | 8랙에서 16조합 전부 성립 (스텝 직후 기울기·순변화 둘 다) |

**극단 케이스 — 예상과 결과** (예상을 먼저 적고 대조했다)

| 부하 | 사전 예상 | 결과 (32조합 범위) | 일치 |
|---|---|---|---|
| 0% | Q=0 이면 T_return = T_2차 + 0/(εC) = T_2차, T_supply = T_return − 0/C = T_2차. **둘 다 2차측 공급온도, ΔT=0.** ε·C 는 유량이 0 이 아닌 한 0 이 아니므로 0 으로 나누는 자리 없음 | T_supply·T_return 모두 **27.000000 / 30.000000 ℃**(2차측 값과 정확히 일치) · ΔT = 0.000000 K | **맞음** |
| 100% | 5장 정격이므로 32/42℃ 근방 · ΔT>0 · T_return > T_2차 | T_supply 30.435~35.146 ℃ · T_return 40.740~45.438 ℃ · ΔT 10.289~10.308 K | **맞음** |

동적: 100→0% 는 두 온도가 2차측 온도로 단조 접근(최종 잔차 <1e-6 K), 0→100% 는
정격 정상상태로 접근. `solve_ivp` 성공 · 매 시점 수력 `fsolve` 수렴 전부 확인.

**실행시간**: 동적 1케이스 평균 **0.71 초**(RK45 · rtol 1e-10 · 4001 출력점 ·
우변마다 8변수 `fsolve`). 전체 테스트 294건 **31 초**. 최적화하지 않았다.

**다음 세션이 알아야 할 결정 사항**

1. **8랙은 열적으로 1랙 집중모델과 동치다.** 랙이 동일하고 유량이 균등하므로
   Q 와 C 가 같은 배수로 커져 온도해가 변하지 않는다 — 8랙 대 1랙 온도 편차가
   **정확히 0.000e+00 K** 로 나왔다. "8랙 거동을 확인했다"로 읽지 않는다.
   랙 간 비대칭은 누출 K값이 들어오는 **세션 4에서 처음 생긴다.**
2. **결합 구조는 「고정점 안에 quasi-steady 수력」이다.** 바깥 `fsolve`(물성 온도)
   안에서 매번 8변수 수력 `fsolve` 를 푼다. 연립이 아니라 **중첩**이며, 절대 규칙 4
   의 하이브리드를 그대로 지킨다(미해결 #28 이 물은 것의 답).
3. **M 의 8랙 해석이 없다**(미해결 #31 신설). 새 숫자를 만들지 않았으므로 τ·t63·t95
   의 절대값은 8랙에서 해석할 수 없다. 게이트에는 영향이 없다.
4. **부하 0 은 밸브 개도 0 이 아니라 발열량 0 이다.** 개도 0 은 Kv=0 이라 ΔP 가
   정의되지 않아 `valve_dp_mAq` 가 `ValueError` 를 던진다. 개도가 변수가 되는 것은
   세션 4다.
5. **누출은 손대지 않았다.** `HydraulicCase.branch_K_multipliers` 진입점을 쓰지
   않았고, 랙별 부하·K·개도는 전부 동일하다(5-1 부하 랙 배분 규약).
6. **수렴시간은 여전히 판정하지 않는다** — 미해결 #21(M 결손) 과 #31(8랙 해석 부재)
   이 둘 다 열려 있다.

### 세션 4 · 2026-08-28 · 누출 시나리오 주입 · 신호 패턴 식별

**완료한 항목**

- `CLAUDE.md` 세션 4 게이트 셀: 「펌프RPM변화」 → 「펌프 운전점 이동」
- 5-1 에 2행 추가(사람 확정, 코드보다 먼저): 펌프 운전 방식(정속) · 누출 주입 지점(랙 1개)
- `assumptions.py`: 누출 수준 목록(정상 배율 1.0 + 5장 3수준) · 주입 랙 번호
- `hydraulics.py`: `apply_leak_to_rack` — 3-A 의 `branch_K_multipliers` 진입점 사용
- `dynamics.py`: 누출 스텝 적분기(`LeakStepCase`/`integrate_leak_step`) ·
  `_derivative` 가 수력 케이스를 인자로 받아 t0 전후 K값을 바꾼다
- `leak.py` 신규: 신호 산출·결과 표 2종
- 테스트 3종 신규: 세션 4 게이트 · M 배분 민감도 · 누출 후 앞 게이트 재확인

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `CLAUDE.md` | 게이트 표 세션 4 셀 하나 (사람의 지시) |
| `AI서버_...정리.md` | 5-1 에 2행 추가 (사람의 지시) |
| `src/cdu_simul/assumptions.py` | 수정 (누출 수준 목록 · 주입 지점 · `SESSION_4_CAVEAT`) |
| `src/cdu_simul/hydraulics.py` | 수정 (`apply_leak_to_rack`) |
| `src/cdu_simul/dynamics.py` | 수정 (누출 스텝 적분기 · 관측 함수 Protocol) |
| `src/cdu_simul/leak.py` | 신규 (누출 시나리오 실행) |
| `tests/test_session4_gates.py` | 신규 (판정 기준 선기재) |
| `tests/test_holdup_split_sensitivity.py` | 신규 (시험값 40:60·60:40) |
| `tests/test_gates_after_leak.py` | 신규 (정상 경로 불변성) |
| `model.py` | **건드리지 않았다** |

**세션 4 게이트 판정** (가정값 기반 · 실측 아님)

판정 기준은 `tests/test_session4_gates.py` 머리말에 **코드보다 먼저** 적었다(커밋 803142e).

| 기준 | 판정 | 근거 |
|---|---|---|
| A 부호 일관성 | **통과** | 32조합 × 3수준 = 96건 전부. 누출랙 유량↓ · 누출랙 출구온도↑ · 총유량↓ · 펌프 양정↑ · 비누출랙 유량↑ |
| B 수준 간 엄격 단조 | **통과** | 32조합 전부에서 +5% < +20% < +50% 로 다섯 신호의 크기가 커진다 |
| C 잡음 대비 | **통과** | +5% 신호가 임계보다 2~4자리 크다 (임계는 3-A/3-B 관측 잡음의 1000배 이상) |

→ **세션 4 게이트 통과.**

**정상상태 신호** (32조합 범위 · 랙 0 주입 · 같은 조합의 정상 해 대비)

| 누출 수준 | 누출랙 유량 [%] | 타랙 유량 [%] | 총유량 [%] | 누출랙 출구온도 [K] | 펌프 양정 [mAq] |
|---|---|---|---|---|---|
| +5% | −1.1159 ~ −0.6372 | +0.0608 ~ +0.1262 | −0.0412 ~ −0.0183 | +0.06666 ~ +0.11835 | +0.00130 ~ +0.00196 |
| +20% | −4.2459 ~ −2.4757 | +0.2362 ~ +0.4802 | −0.1589 ~ −0.0704 | +0.26383 ~ +0.46476 | +0.00501 ~ +0.00754 |
| +50% | −9.6855 ~ −5.8569 | +0.5580 ~ +1.0954 | −0.3704 ~ −0.1638 | +0.64640 ~ +1.12302 | +0.01163 ~ +0.01759 |

누출 상태 energy balance 최대 \|잔차\| = **0.00507 %** (6장 기준 <0.1%).

**동적 신호** (정격 운전 중 t=0 에 K값 계단 주입 · 부하는 그대로)

| 케이스 | τ [s] | t63 [s] | t95 [s] | T_return [℃] | 누출랙 Q [%] | 총 Q [%] | 양정 [mAq] |
|---|---|---|---|---|---|---|---|
| +5% · M 하한 | 0.72 | 0.67 | 2.34 | 42.4512 → 42.4555 | −0.8904 | −0.0274 | +0.00130 |
| +20% · M 하한 | 0.72 | 0.67 | 2.35 | 42.4512 → 42.4676 | −3.4208 | −0.1056 | +0.00501 |
| +50% · M 하한 | 0.72 | 0.67 | 2.35 | 42.4512 → 42.4892 | −7.9343 | −0.2455 | +0.01163 |
| +5% · M 상한 | 9.23 | 8.58 | 30.04 | 42.4512 → 42.4555 | −0.8904 | −0.0274 | +0.00130 |
| +20% · M 상한 | 9.23 | 8.58 | 30.11 | 42.4512 → 42.4676 | −3.4208 | −0.1056 | +0.00501 |
| +50% · M 상한 | 9.23 | 8.65 | 30.11 | 42.4512 → 42.4892 | −7.9343 | −0.2455 | +0.01163 |

`solve_ivp` 성공 · 매 시점 수력 `fsolve` 수렴 전수 확인. **시간 규모의 절대값은
해석하지 않았다**(#21 · #31).

**C7 정상 경로 불변 확인**: `model`·`dynamics`·`hydraulics` 세 표가 세션 3-B 와
**바이트 단위로 동일**. 배율 1.0 경로와 직접 경로의 32조합 편차 = **0.000e+00**
(허용오차 없이 `==` 로 고정했다 — `tests/test_gates_after_leak.py`).

**M 노드 배분 민감도** (시험값 — 5-1·`assumptions.py` 에 넣지 않았다)

| 배분(공급:환수) | t63 [s] (M 하한) | t63 [s] (M 상한) | 50:50 대비 T_return 최대 편차 [K] | t→∞ |
|---|---|---|---|---|
| 40:60 | 0.5663 | 7.2667 | 2.5e-4 ~ 2.2e-3 | 동일 |
| 50:50 | 0.6687~0.6741 | 8.5817~8.6509 | (기준) | (기준) |
| 60:40 | 0.7658 | 9.8274 | 2.0e-4 ~ 1.8e-3 | 동일 |

t→∞ 값은 세 배분비에서 편차 <2e-9 K 로 같았다 — 5-1 의 "정상상태에 영향하지
않는다"가 성립한다. **판정은 사람이 한다 — 결론을 쓰지 않는다.**

**다음 세션이 알아야 할 결정 사항**

1. **랙 간 비대칭이 여기서 처음 생겼다.** 3-B 까지 8랙은 열적으로 1랙 집중모델과
   동치였는데, 누출이 걸린 랙 하나만 유량·온도가 갈라진다. `rack_return_temps_C`
   가 그 차이를 담는다.
2. **펌프는 정속이다**(5-1 확정). RPM 은 모델에 없다 — 게이트의 「펌프 운전점
   이동」은 정속 곡선 위의 이동이며, 양정 변화는 +0.0013 ~ +0.0176 mAq 로
   양정(20~30 mAq) 대비 **상대 1e-4 수준**이다. 세 신호 중 가장 약하다.
3. **누출은 K값 대용(proxy)이다.** 질량 손실이 모델에 없으므로 총유량 감소는
   "누출된 만큼 빠져나갔다"가 아니라 "저항이 커져 덜 흐른다"다. 데이터셋에
   이 사실을 붙이지 않으면 학습 모델이 잘못된 물리를 배운다(세션 5.5).
4. **밸브 개도는 80% 고정이었다.** 5-1 「밸브 개도 특성」이 "개도가 변수가 되는
   시점에 재검토"라고 적었으나 누출은 K값 변화이므로 이번 판의 재검토 대상이
   아니었다 — **선형 특성은 아직 시험되지 않았다**(미해결 #32).
5. **전 랙 동시 누출은 돌리지 않았다** — 5장에 근거가 없다(5-1 「누출 주입 지점」).

### 세션 5 · 2026-08-28 · 다중 CDU · 공유 2차측 결합

**완료한 항목**

- 5-1 (사람 확정, 코드보다 먼저): 2차측 유체 확정 · 물성 조회 기준 압력 확정 ·
  CDU 대수(2대) · 공유 2차측 결합 방식 · 2차측 공급온도 고정 유지
- `assumptions.py`: `PlantAssumptions`(대수·총 2차측 유량·비례 배분)
- `model.py`: `hx_capacity_terms`(ε·C_min 을 한 곳에) · `SteadyStateCase.secondary_flow_Lps` ·
  `cdu_property_temperature_residual`(상위 레벨이 쓰는 공개 잔차)
- `dynamics.py`: `hx_capacity_terms` 사용 · 2차측 유량 인자
- `plant.py` 신규: 상위 레벨 연립 정상상태 · 비대칭 부하 스텝 동적 결합 · 표 2종
- 테스트 2종 신규 · `daily_brief.py` 에 열린 미해결 목록 출력

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 2행 확정 + 3행 추가 (사람의 지시) |
| `src/cdu_simul/assumptions.py` | 수정 (`PlantAssumptions` · `SESSION_5_CAVEAT`) |
| `src/cdu_simul/model.py` | 수정 (`hx_capacity_terms` · 2차측 유량 · 잔차 공개) |
| `src/cdu_simul/dynamics.py` | 수정 (`hx_capacity_terms` 사용 · 2차측 유량 인자) |
| `src/cdu_simul/plant.py` | 신규 (다중 CDU 결합) |
| `tests/test_session5_gates.py` · `tests/test_gates_after_plant.py` | 신규 |
| `daily_brief.py` | 수정 (열린 미해결 목록) |
| `leak.py` · `hydraulics.py` | **건드리지 않았다** |

**세션 5 게이트 판정** (가정값 기반 · 실측 아님)

판정 기준은 `tests/test_session5_gates.py` 머리말에 **코드보다 먼저** 적었다(커밋 acbc31c).

| 기준 | 판정 | 근거 |
|---|---|---|
| A 상위 연립 수렴 | **통과** | 96 케이스(32조합 × 부하 시나리오 3) 전부 상위 `fsolve` `ier=1` · 각 CDU 물성 온도·헤더 압력평형 `fsolve` 수렴 |
| B 잔차 동시 소멸 | **통과** | 최대 잔차 1e-13~1e-11 K (순차 대입이면 K 단위로 남는다) |
| C 연동의 실재 | **통과** | 비대칭에서 부하가 그대로인 CDU B 의 환수온도·2차측 배분이 실제로 움직인다 |

→ **세션 5 게이트 통과.**

**C4 는 실패했다 — 정지 조건에 해당한다.**

대칭 케이스가 세션 4 단일 CDU 결과를 재현하지 못한다: **32조합 최대 편차 0.0245 K**
(최악 `H22.4/dPb3/dPv5 / NTU=2 / T2nd=30C`). 원인은 5-1 두 행의 내부 모순이며
**미해결 #33 으로 등록**했다. 나머지 항목이 이 답에 의존하지 않으므로 완주한 뒤
보고한다 — 자세한 것은 #33 과 세션 5 보고를 본다.

**정상상태 결합 결과** (수력·NTU·2차측 하단 고정 · 가정값 기반 · 실측 아님)

| 시나리오 | CDU | 부하 [%] | 1차 Q [L/s] | 2차 배분 [L/s] | 배분비 [%] | Cr | T_sup [℃] | T_ret [℃] |
|---|---|---|---|---|---|---|---|---|
| 대칭 100/100 | A | 100 | 15.5056 | 15.5000 | 50.000 | 0.9982665 | 32.1683 | 42.4690 |
| | B | 100 | 15.5056 | 15.5000 | 50.000 | 0.9982665 | 32.1683 | 42.4690 |
| 대칭 20/20 | A | 20 | 15.4780 | 15.5000 | 50.000 | 0.9989152 | 28.0319 | 30.0978 |
| | B | 20 | 15.4780 | 15.5000 | 50.000 | 0.9989152 | 28.0319 | 30.0978 |
| 비대칭 100/20 | A | 100 | 15.5055 | 15.5138 | 50.044 | 0.9991572 | 32.1591 | 42.4599 |
| | B | 20 | 15.4780 | 15.4862 | 49.956 | 0.9998046 | 28.0328 | 30.0988 |

**연동 크기**: 비대칭에서 B 는 부하가 20% 로 같은데도 대칭 20/20 대비
T_return 이 **+0.0010 K**(30.0978 → 30.0988) 움직였다. 배분비가 50.000% 에서
49.956% 로 줄었기 때문이다. **크다/작다를 판정하지 않는다.**

**동적 결합** (A 만 20→100% · B 는 20% 불변)

| CDU | 부하 | T_ret 초기 [℃] | T_ret 최종 [℃] | ΔT_ret [K] | 2차 배분 초기 → 최종 [L/s] |
|---|---|---|---|---|---|
| A | 20→100% | 30.09784 | 42.45989 | +12.36205 | 15.50000 → 15.51378 |
| B | 20→20% | 30.09784 | 30.09876 | **+0.00092** | 15.50000 → 15.48622 |

M 하한·상한 둘 다 같은 값이다(정상상태 종점은 M 에 의존하지 않는다).
`solve_ivp` 성공 · 매 시점 수력 `fsolve` 수렴 전수 확인.

**C7 — 결합 상태의 누출 관측** (CDU A 에만 주입 · 게이트가 아니다)

| 수준 | A T_ret [℃] | B T_ret [℃] | B 변화 [K] | B 2차 배분 [L/s] |
|---|---|---|---|---|
| 정상 | 42.46905 | 42.46905 | — | 15.50000 |
| +5% | 42.47188 | 42.46763 | −0.001414 | 15.50213 |
| +20% | 42.47995 | 42.46361 | −0.005441 | 15.50819 |
| +50% | 42.49442 | 42.45640 | −0.012649 | 15.51905 |

A 의 누출로 A 의 1차측 유량이 줄자 A 의 배분 몫이 줄고 **B 의 몫이 늘어** B 가
더 냉각된다. 세션 4 신호 방향(A 유량↓·환수온도↑·양정↑)은 결합 뒤에도 유지된다.

**다음 세션이 알아야 할 결정 사항**

1. **#33 을 먼저 정해야 한다.** Cr 을 배분에서 유도할지 5장 명목 1 로 둘지에 따라
   세션 5 의 모든 수치가 바뀐다. 세션 5.5 데이터셋은 그 결정 뒤에 내는 것이 안전하다.
2. **연동은 유량 경로 하나뿐이다.** 2차측 공급온도가 고정이라 열 경로 결합이
   모델에 없다(5-1 한계). 연동 크기(+0.0010 K)를 "CDU 간 상호작용이 작다"로
   읽지 않는다 — 모델에 있는 경로만 잰 값이다.
3. **단일 CDU 경로는 그대로 남아 있다.** 2차측 유량을 주지 않으면 세션 4까지와
   부동소수점까지 같다 — 테스트가 `==` 로 고정한다.
4. **CDU 2대만 돌렸다.** 3대 이상은 배치·부하 비대칭 등 5장에 없는 선택을 부른다.

### 세션 5-B · 2026-08-28 · Cr 유도로 전환 · 전 게이트 소급 재판정

**왜 열었나** — 세션 5 C4 가 드러낸 것: 세션 1-B 이후의 Cr=1 은 **유도된 값이 아니라
5-1 이 선언한 값**이었다. 5장 「1차:2차 유량비 1:1」을 부피유량으로 읽으면 양쪽 온도가
달라 ρ·cp 가 다르므로 Cr≠1 이 된다. CDU 대수와 무관하므로 **단일 CDU 에도 소급**한다.

**사람이 정한 전제** (코드가 다시 정하지 않았다)

1. 5장 「1:1」은 **부피유량 기준**
2. 2차측 유량은 1차측 **정격** 15.5 L/s 와 1:1 로 **고정** (운전점 추종 없음)
3. Cr 은 선언하지 않고 **매번 물성에서 유도**
4. **단일 CDU 에도 소급 적용**

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 「2차측 유체」 교체 · 「열교환기 유동배열」 근거 재계산 (사람의 지시) |
| `src/cdu_simul/assumptions.py` | 수정 (부피유량비 명시 · `secondary_flow_Lps` · `SESSION_5B_CAVEAT`) |
| `src/cdu_simul/model.py` | 수정 (Cr 필드 제거 · `hx_capacity_terms` 의 `None` 분기 제거) |
| `src/cdu_simul/dynamics.py` · `leak.py` · `hydraulics.py` · `plant.py` | 수정 (Cr 인자 제거 · ※ 추가) |
| 테스트 7종 | 수정 (생성자 갱신 · 전제가 뒤집힌 검사 2건 교체) |

**C1(2) 유동배열 근거 재계산** — 중단 조건이었다

| | T_supply [℃] | T_return [℃] | ε |
|---|---|---|---|
| 세션 1-B 기록 (Cr=1 선언) | 32.1504 | 42.4512 | 0.666667 |
| 세션 5-B 재계산 (Cr 유도) | **32.1683** | **42.4690** | 0.667052 |
| 5장 표값 | 32 | 42 | — |

반올림하면 32/42℃ 로 **여전히 정합** → 대향류 확정 유지. dT = 10.3008 K.

**C2 Cmin 판정**: 32조합 **전부 2차측이 Cmin** 이다((ρcp)₂₇=4001.52 < (ρcp)₃₇=4007.02
J/(L·K)). 세션 5-B 이전 코드는 1차측을 Cmin 으로 가정하고 있었다. Cr 범위
**0.9976198 ~ 0.9989254**.
**Cr=1 전용 분기**: `hx_effectiveness_counterflow` 에 있었고 **남겼다** — 일반식이
0/0 이 되는 수학적 특이점 가드이지 5장 「1:1」을 되살리는 경로가 아니다(부하 0
극단 케이스에서 실제로 필요하다). 성격을 주석에 적었다. **가정으로서의 Cr=1 은 전부 제거**.

**C4 대칭 재현**: 32조합 최대 온도 편차 **0.000e+00 K** (세션 5 에서 0.0245 K 였다).
대칭 배분에서 각 CDU 가 정확히 15.5 L/s 를 받는다(오차 <1e-12).

**C5 전 게이트 소급 재판정 — 다섯 개 전부 통과**

| 게이트 | 판정 | 근거 |
|---|---|---|
| 1-B energy balance <0.1% | **통과** | 32조합 최대 \|잔차\| **0.00506 %** |
| 2 T_return 방향성 | **통과** | 16조합 전부 |
| 3 수렴 · 극단 케이스 비발산 | **통과** | 32조합 × 부하 0/100% · 동적 4케이스 |
| 4 누출 신호 식별 (기준 A·B·C) | **통과** | 96건 전부 (세션 4 선기재 기준 그대로) |
| 5 CDU 간 연동 수렴 (기준 A·B·C) | **통과** | 96 케이스 전부 (세션 5 선기재 기준 그대로) |

**판정 기준·허용오차를 하나도 조정하지 않았다.**

**C6 이동량 대조표** (세션 5-B 이전 → 이후)

| 항목 | 이전 | 이후 | 이동 |
|---|---|---|---|
| 1-B T2nd=27/NTU=2 T_sup [℃] | 32.15 | 32.1731 | +0.0231 |
| 1-B T2nd=27/NTU=2 T_ret [℃] | 42.44 | 42.4643 | +0.0243 |
| 1-B T2nd=27/NTU=3 T_sup·T_ret [℃] | 30.43 · 40.72 | 30.4524 · 40.7454 | +0.0224 · +0.0254 |
| 1-B T2nd=30/NTU=2 T_sup·T_ret [℃] | 35.14 · 45.43 | 35.1695 · 45.4580 | +0.0295 · +0.0280 |
| 1-B T2nd=30/NTU=3 T_sup·T_ret [℃] | 33.43 · 43.72 | 33.4498 · 43.7398 | +0.0198 · +0.0198 |
| energy balance 최대 \|잔차\| [%] | 0.00507 | 0.00506 | −0.00001 |
| 누출 +5% 온도 신호 [K] | +0.06666~+0.11835 | +0.06505~+0.11419 | 신호 **축소** |
| 누출 +50% 온도 신호 [K] | +0.64640~+1.12302 | +0.63153~+1.09541 | 신호 **축소** |
| 누출 유량·양정 신호 | −9.6855~−0.6372% · +0.00130~+0.01759 mAq | −9.6859~−0.6372% · +0.00130~+0.01763 mAq | 거의 불변 |
| 세션 5 연동 크기 [K] | +0.0010 | +0.000917 | −0.00008 |
| Cr | 1 (선언) | **0.9976198~0.9989254 (유도)** | — |
| C4 대칭 재현 편차 [K] | 0.0245 | **0.000000** | 해소 |

**"작다/무시할 만하다"는 판단을 쓰지 않는다 — 값만 적었다.**

**다음 세션이 알아야 할 결정 사항**

1. **세션 5-B 이전 로그의 수치를 이후 것과 나란히 읽지 않는다.** 세션 1-B~5 의
   기록이 전부 소급 이동했다. 5-1 「2차측 유체」에 그 한계를 적었다.
2. **Cr 은 어디에서도 선언되지 않는다.** `assumptions.py` 에 Cr 상수가 없고,
   케이스 dataclass 에 `heat_capacity_ratio` 필드가 없다. 되살리면 세션 5 C4 가
   드러낸 오류가 그대로 돌아온다.
3. **Cmin 은 2차측이다**(현재 조건에서 32조합 전부). 2차측 유량이나 온도가 바뀌면
   뒤집힐 수 있으므로 `hx_capacity_terms` 가 매번 판정한다.
4. **누출 온도 신호가 2.4% 작아졌다**(예: +50% 상한 1.12302 → 1.09541 K).
   Cmin 이 2차측으로 바뀌면서 열교환 용량이 조금 줄었기 때문이다. 세션 4 게이트
   기준 C(잡음 대비)는 여전히 2~4자리 여유로 통과한다.

### 세션 5.5-A · 2026-08-28 · 데이터셋 설계 · 조합 계수 · 실행시간 측정 (생성 없음)

**이 판은 아무것도 확정하지 않았다.** 스키마는 제안이고, 부하 축 값은 고르지 않았으며,
규모 3안 중 어느 것도 선택하지 않았다. **CSV 를 생성하지 않았다.**
미해결을 새로 닫지 않았다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/dataset_plan.py` | 신규 (축 정의 · 계수 · 시간 측정 · 크기 추정). 5.5-B 재사용 |
| `PROCEED.md` | 수정 (이 로그) |
| 모델 코드 전부 · 문서 4종 · 기존 테스트 | **건드리지 않았다** — 세 결과 표 불변 확인 |

**C1 스키마 초안 (제안 · 확정 아님)** — 46열. 행은 **(시나리오 × CDU × 시각) 하나당 하나**,
랙별 값은 열로 편다(rack0..rack7). 행으로 펴면 CDU 총계가 8번 중복된다.

| 묶음 | 열 |
|---|---|
| 시나리오 식별 (10) | `scenario_id` · `scenario_kind`(정상/이상/누출) · **`regime`(steady/transient)** · `cdu_config`(single/dual_symmetric/dual_asymmetric) · `cdu_index` · `leak_level_percent` · `leak_rack_index` · `load_percent` · `holdup_mass_kg` · `t_s` |
| 5장 범위 축 (5) | `pump_head_rated_mAq` · `branch_dp_rated_mAq` · `valve_dp_rated_mAq` · `ntu` · `T_secondary_supply_C` |
| 랙별 관측량 (16) | `rack{0..7}_flow_Lps` · `rack{0..7}_outlet_C` |
| CDU 총계 (7) | **`total_flow_Lps`**(부호 불확실) · **`pump_head_mAq`**(부호 불확실) · `T_supply_C` · `T_return_C` · `hx_duty_kW` · `secondary_share_Lps` · `heat_capacity_ratio`(유도값) |
| 검증 (1) | `energy_balance_residual_percent` |
| solver 플래그 (4) | `hydraulic_solver_ier` · `thermal_solver_converged` · `plant_solver_ier` · `integrator_success` |
| 출처·한계 (5) | `assumption_tag` · `signal_sign_caveat` · `transient_caveat` · `total_flow_caveat` · `cr_caveat` |

**표기 방식 두 안** (사람이 정한 것 1·2·3 의 표기를 어디에 둘지)

| 안 | 방식 | 행당 추가 바이트 | 안2 규모(31만 행) 기준 증가 |
|---|---|---|---|
| A | 전 행에 문구 5열 반복 | 약 210 B | +0.065 GB (0.20 → 0.26 GB, **+31%**) |
| B | 참조 키 1열 + 별도 메타 파일 | 약 12 B | 기준 |

**의견**: `CLAUDE.md` 세션 5.5 게이트가 **"전 행에"** 표기를 요구하므로 A 가 게이트
문언에 맞다. B 는 파일이 30% 작지만 CSV 한 장만 떼어 보면 표기가 사라진다 —
데이터셋이 나중에 조각나 유통될 것을 생각하면 A 가 안전하다. **사람이 정한다.**

**C2 조합 계수** (누출 랙 축 끔 · 정상상태에 M 축 없음)

축: 5장 범위 8(양정 2 × 분기ΔP 2 × 밸브ΔP 2) × NTU 2 × 2차측 2 × 부하 L ×
누출 수준 4 × CDU 구성 3. 전이는 여기에 M 2수준.

| 부하 축 후보 | 정상상태 | 전이 | 합계 | 5장 밖 숫자 |
|---|---|---|---|---|
| 양 끝만 (20 · 100%) | 768 | 1,536 | 2,304 | **0개** |
| 양 끝 + 중점 (20 · 60 · 100%) | 1,152 | 2,304 | 3,456 | 1개(60%) |
| 4수준 | 1,536 | 3,072 | 4,608 | 2개 |
| 5수준 (20/40/60/80/100%) | 1,920 | 3,840 | 5,760 | 3개 |

**중복·무의미 조합 지적**

1. **누출 랙 번호를 축으로 둘 필요가 없다.** 8랙이 전부 동일해 결과가 같다(5-1).
   축으로 두면 행이 8배가 되고 그 8배가 전부 같은 물리다. **랙 0 고정을 권한다** —
   단 `leak_rack_index` **열은 남긴다**(비대칭 랙이 생겨도 스키마가 안 바뀌게).
2. **단일 CDU 에 「비대칭」은 성립하지 않는다.** CDU 구성 축이 부하 축과 독립이 아니다.
3. **2대 대칭이 단일 CDU 를 정확히 재현한다**(세션 5-B: 편차 0.000e+00 K).
   둘 중 하나는 완전 중복이다 — 전이에서 2대 대칭을 빼면 시간이 크게 준다.
4. **정상상태에 M 축을 두면 전부 중복**이다(정상상태 해는 M 에 의존하지 않는다).

**C3 실행시간 실측** (이 PC · 케이스마다 1회 · 캐시 워밍업 별도 확인)

| 종류 | 평균 [s] | 범위 [s] | 산포 | n |
|---|---|---|---|---|
| 반복 영향(같은 케이스 3회) | 0.0061 | 0.0056~0.0064 | 1.15배 | 3 |
| 정상상태 · 단일 CDU | 0.0068 | 0.0054~0.0085 | 1.58배 | 5 |
| 정상상태 · 2대 결합 | 0.0485 | 0.0461~0.0544 | 1.18배 | 5 |
| 전이 · 단일 CDU | 0.3095 | 0.2870~0.3329 | 1.16배 | 3 |
| 전이 · 2대 결합 | **3.8800** | 3.7235~4.0729 | 1.09배 | 3 |

같은 케이스를 세 번 돌려도 시간이 1.15배 흔들린다 — **결과 캐시는 없고**
(케이스마다 solver 를 새로 돌린다) 흔들림은 OS 스케줄링에서 온다. 아래 추정에는
평균을 썼고, 산포만큼 추정이 흔들린다.

**C4 전량 추정 · 규모 3안** (부하 2수준 기준 · **추정이지 실측이 아니다**)

| 안 | 정상 케이스 | 전이 케이스 | 전이 표본 | 예상 시간 | 행 수 | 크기(A안) |
|---|---|---|---|---|---|---|
| **1 전수** | 768 | 1,536 | 4,001 | **1.16시간** | 10,243,840 | **8.75 GB** |
| **2 정상 전수 + 전이 부분집합** | 768 | 1,024 | 201 | **36.2분** | 310,016 | **0.26 GB** |
| **3 둘 다 부분집합** | 768 | 1,024 | 41 | **36.2분** | 64,256 | **0.05 GB** |

**무엇을 잃는가**

- **안1**: 잃는 것 없음. 다만 8.75 GB 는 git 에 올릴 수 없고 파일 하나로 열기도 어렵다
- **안2**: 전이에서 **2대 대칭 구성**을 뺐다 — 세션 5-B 가 단일 CDU 와 정확히 같음을
  증명했으므로 **정보 손실이 없다**. 전이 표본을 4001 → 201 점으로 줄였다 —
  t63·t95 를 읽기에는 충분하지만 **스텝 직후 기울기**의 해상도가 20배 낮아진다
  (세션 2 방향성 게이트가 그 기울기를 쓴다)
- **안3**: 표본 41점. 시간은 안2 와 같은데(적분 자체가 비용이고 표본 추출은 공짜다)
  **파일만 1/5** 이 된다. 전이 파형의 모양을 학습시키기에는 성기다

**권고(의견이며 선택이 아니다)**: **안2**. 전이 시간의 대부분을 차지하던 2대 대칭이
증명된 중복이라 공짜로 빠지고, 0.26 GB 는 다룰 수 있는 크기다. 다만 **전이 표본
201점이 세션 2 방향성 게이트가 쓰는 스텝 직후 기울기를 충분히 담는지**는 5.5-B
에서 확인해야 한다 — 담지 못하면 표본을 늘리는 대신 **스텝 직후 구간만 조밀하게**
뽑는 방식을 검토한다.

**주의**: 시간과 크기는 **다른 축이 지배한다.** 시간은 전이 케이스 수(적분 횟수)가,
크기는 전이 표본 수가 정한다. 규모를 줄일 때 무엇을 줄이는지에 따라 어느 쪽이
줄어드는지가 다르다.

**C6 상태 이월 검사 설계안** (세션 5.5 게이트 · **코드는 5.5-B 에서 만든다**)

이월이 있다면 이렇게 드러난다:
· 같은 시나리오를 **다른 순서로** 돌렸을 때 결과가 달라진다
· 앞 시나리오의 해가 뒤 시나리오의 `fsolve` 초기값으로 새면 수렴점이 미세하게 이동한다
· 누출 시나리오 다음의 정상 시나리오가 정상값을 정확히 되찾지 못한다

검사안 셋:
1. **순서 무관성** — 시나리오 목록을 뒤집어(또는 섞어) 다시 돌리고 각 행이
   **바이트 단위로 동일**한지 본다. 세션 3-B 이후 여러 판에서 써 온 기법이고
   **여기에 그대로 쓸 수 있다** — 이월은 순서 의존으로 나타나므로 이 검사가
   정확히 그것을 잡는다
2. **단독 재현** — 임의의 시나리오 하나를 **혼자** 돌린 결과가 배치 안의 그 행과
   같은지 본다. 배치 문맥이 결과에 남지 않음을 직접 확인한다
3. **정상 복귀** — 누출 → 정상 순서로 돌렸을 때 정상 행이 배치 첫 정상 행과 같은지

1과 2가 서로를 보완한다: 1은 순서 의존을, 2는 배치 자체의 오염을 잡는다.
**셋 다 새 허용오차가 필요 없다** — 전부 `==` 로 본다.

**다음 세션이 알아야 할 결정 사항**

1. **사람이 정해야 할 것 넷**: ① 부하 축 값(양 끝만 / 중간값 추가) ② 규모 안
   (1·2·3) ③ 표기 방식(A 행마다 / B 메타 분리) ④ 전이 표본 수
2. **스키마는 아직 어디에도 기록하지 않았다** — `SCHEMA_DRAFT` 는 코드 안의 제안이다.
   5.5-B 에서 확정하면 그때 5-1 이나 `assumptions.py` 에 넣을지 정한다.
3. **시간과 크기는 다른 축이 지배한다** — 위 「주의」 참조.

### 세션 5.5-B · 2026-08-28 · 시나리오 배치 실행 → 텍스트 데이터셋 (파일럿 마지막 판)

**파일럿의 마지막 판이다.** 종료 판정은 하지 않았다 — 판정 주체는 사람이다
(`project-overview.md` 「파일럿 종료 조건」). 이 판은 **재료를 손에 잡히게** 했다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 5-1 「데이터셋 스키마 규약」 1행 추가 (사람의 지시) |
| `src/cdu_simul/dynamics.py` | 수정 (저장 표본 비균등 추출 — 적분 설정 불변) |
| `src/cdu_simul/dataset.py` | 신규 (시나리오 배치 실행 · CSV 생성) |
| `src/cdu_simul/dataset_report.py` | 신규 (파일럿 종료 판단용 요약) |
| `tests/test_session55_gates.py` | 신규 (게이트 — 기준 선기재) |
| `project-overview.md` | 체크리스트 5.5 완료 · 「지금 어디인가」 첫 줄 |
| `model.py`·`plant.py`·`leak.py`·`hydraulics.py`·`assumptions.py` | **건드리지 않았다** |

**세션 5.5 게이트 판정** — 판정 기준은 `tests/test_session55_gates.py` 머리말에
**코드보다 먼저** 적었다(커밋 cc790fa).

| 기준 | 판정 |
|---|---|
| A 순서 무관성 (목록을 뒤집어 재실행 · `==`) | **통과** |
| B 단독 재현 (배치 문맥이 새는가 · `==`) | **통과** |
| C 정상 복귀 (누출 뒤 정상값 오염 · `==`) | **통과** |
| D 전 행 표기 (프로그램 확인) | **통과** |

→ **세션 5.5 게이트 통과.** 표본은 전량 1,792 중 stride 61(약 30개)이고, 표본이
정상상태·전이·세 CDU 구성·네 누출 수준을 모두 덮는지도 테스트가 확인한다.

**데이터셋** (가정값 기반 · 실측 아님)

| 항목 | 값 |
|---|---|
| 시나리오 | 1,792 (정상상태 768 · 전이 1,024) |
| 행 | 310,016 |
| 열 | 48 |
| 파일 크기 | 304.6 MB |
| 소요시간 | 27.5분 (5.5-A 추정 36분) |
| solver 실패 행 | 0 |

출력은 `results/cdu_dataset.csv` 이고 `.gitignore` 대상이다 — **커밋하지 않았다.**

**이 판에서 스스로 판단해 조정한 것 — 사람의 확인이 필요하다**

1. **다중 CDU 전이의 저장 표본**: `plant.integrate_plant_load_step` 이 자기
   `t_eval`(균등 2001점)을 쓰는데 **`plant.py` 가 이 판의 범위 밖**이라, 단일 CDU 와
   시간 격자를 맞추려고 `dataset.py` 에서 비균등 격자에 **가장 가까운 점을 골랐다**
   (보간하지 않는다). 이 조정이 없었을 때 행 수가 합의 규모의 **3.5배**(109만 행)로
   나왔다. 근본 해결은 `plant.py` 의 `t_eval` 을 `storage_times_s` 로 바꾸는 것이다.
2. **다중 CDU 전이의 자극**: 처음에 `load_before == load_after` 로 두었더니 계가
   정상상태에서 출발해 아무것도 하지 않는 **평평한 「전이」 행**이 나왔다 — 그런 행으로
   학습하면 누출이 전이를 만들지 않는다고 배운다. **CDU A 에 부하 스텝**을 주고 누출은
   **t=0 이전부터 있는 조건**으로 바꿨다. 단일 CDU 전이는 누출이 **스텝으로** 들어오고
   다중 CDU 전이는 **기왕에 있는 조건**이라는 차이가 생겼다 — `cdu_config` 로 구분해
   읽어야 한다. `PlantLoadStepCase` 가 CDU 마다 다른 템플릿을 받지 않아 누출이 두 CDU
   **모두**의 랙 0 에 걸린다.
3. **비대칭 부하의 거울상 중복**: 부하 축이 2수준뿐이라 (100,20)과 (20,100)이 서로
   거울상이다 — 정보로는 중복이지만 **사람이 정한 규모를 코드가 줄이지 않았다.**

**기록만 — 미해결이 아니다** (파일럿 밖이거나 본 프로젝트의 몫)

· **펌프 스타트업·밸브 개도 변화 등 과도 시나리오는 파일럿 밖이다.** 5장이 아무것도
  주지 않아 새 숫자가 최소 넷 필요하다(N(t) 프로파일 · x(t) 프로파일 · 밸브
  rangeability 또는 최소 Kv · 배관 관성 L/A). 절대 규칙 1에 걸린다
· **quasi-steady 가정(규칙 4)이 기동에서 성립하지 않는다** — 유체 관성 시간상수가
  0.5초 안팎으로 추정되어 열 시간상수 0.72초(M 하한)와 같은 자릿수다
· **실 누출감지 AI의 오탐 주요 원인이 기동·정지 과도**이므로, 본 프로젝트에서
  설계데이터 확보 시 우선 확장 대상이다

**다음이 알아야 할 것**

1. **파일럿 종료 판정은 사람이 한다.** `python -m cdu_simul.dataset_report` 가
   `project-overview.md` 「파일럿 종료 조건」이 볼 것으로 적은 넷에 답하는 재료를 낸다.
2. **위 「스스로 판단해 조정한 것」 셋을 먼저 확인해 달라.** 특히 2번은 데이터셋의
   의미를 바꾼다.
3. **미해결 #21·#24·#31·#32 는 열려 있다.** 각각 데이터셋의 어느 열에 영향하는지를
   요약 리포트가 적었다.

### 세션 5.5-C · 2026-08-29 · 문서 정합 · 미해결 정리 · 브리핑 축약

**문서·위생 판이다. feasibility 판정은 하나도 하지 않았다.** 모델 코드·기존
테스트·데이터셋을 건드리지 않았다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `project-overview.md` | 체크리스트 3·4·5 를 완료로 · 「지금 어디인가」 두 줄 교체 (사람의 지시) |
| `CLAUDE.md` | 「작업 시작 전 필독」 1항의 로컬 경로 표기를 **PC 독립**으로 (사람의 지시) |
| `daily_brief.py` | 축약 출력 · **열린 미해결 전문 보장** · `--full` 플래그 |
| `PROCEED.md` | 미해결 #5·#6·#26 닫음 · #11·#18·#19 문구 정정 · #35 신설 · 이 로그 |
| `collaboration.md`·`AI서버_…정리.md`·`src/`·`tests/`·`results/`·`pyproject.toml` | **건드리지 않았다** |

**미해결 정리**

| 항목 | 처리 | 근거 |
|---|---|---|
| #5 5장 표 자기정합 | **닫음** | 세션 5-B 재계산 ΔT = 10.3008 K · 1차측 32.17/42.47℃ 가 5장 32/42℃ 와 정합 |
| #6 한글 파일명 | **닫음** | 새 PC clone 에서 확인 — UTF-8 NFC 정상 저장·내용 정상. 8진 이스케이프는 `core.quotepath` 기본값에 따른 **표시 문제**. `core.quotepath false` 는 개인 설정이라 저장소에 넣지 않는다 |
| #26 세션 5.5 신설 | **닫음** | 5.5-A·5.5-B 로 이행 완료 |
| #11 커밋 작성자 | 문구 정정 (**열림 유지**) | 한 PC 의 값만 적고 있었다. 작업 PC 가 둘이므로 **PC 를 새로 붙일 때마다 확인하는 상시 항목**으로 바꿨다 |
| #18 `REFERENCE_PRESSURE_Pa` | 문구 정정 (**열림 유지**) | 5-1 을 「잠정」으로 인용하고 있었으나 **세션 5 에서 확정으로 바뀌었다**. 주석 자체는 모델 코드라 이 판에서 손대지 않았다 |
| #19 `heat_capacity_ratio` | 문구 정정 (**열림 유지**) | **코드로 확인했다** — 세션 5-B 가 Cr=1 선언을 제거해 「(Cr=1)」 형태는 없다. 그러나 이름은 `hx_effectiveness_counterflow` 의 인자로 남아 있고(값은 물성에서 유도), Cr 유도 규약의 인용이 「세션 5-B」이지 5-1 의 행이 아니다. 그래서 **닫지 않았다** |
| #35 의존성 버전 미고정 | **신설** | C7 — `pyproject.toml` 은 **고치지 않았다**. 사람이 정한다 |

**의존성 버전 기록** (이 PC · 2026-08-29 시점 — 재생성 시 대조용, 미해결 #35)

| 구분 | 패키지 | 버전 |
|---|---|---|
| 런타임 | `numpy` | 2.5.2 |
| 런타임 | `scipy` | 1.18.1 |
| 런타임 | `CoolProp` | 8.0.0 |
| 런타임 | `pandas` | 3.0.5 |
| 인터프리터 | Python | 3.12.10 |
| 개발 | `pytest` 9.1.1 · `ruff` 0.16.5 · `mypy` 2.3.1 | (수치 무관) |

**`daily_brief.py` 축약 — 자르는 우선순위를 뒤집었다**

세션 5 이전 브리핑은 「현재 상태」 표와 마지막 세션 로그를 **통째로** 싣고 열린
미해결만 **88자에서 잘랐다**. 그 결과 미해결 #34 의 선택지 (가)·(나) 처럼 *사람이
정해야 할 것* 이 브리핑에서 사라졌다. 지금은 반대다.

| 모드 | 내용 | 길이 |
|---|---|---|
| 기본(축약) | 한 줄 현재 위치+다음 할 일 · 「현재 상태」 최근 2판만 상세(나머지 한 줄) · 마지막 세션 로그의 **「다음이 알아야 할 것」 절만** · **열린 미해결 전문** · 참조 한 줄 | **133줄 / 12,922자** |
| `--full` | 세션 5.5-B 까지와 같은 전문(「현재 상태」 절 전체 + 마지막 세션 로그 전체) + **열린 미해결 전문** | **211줄 / 25,070자** |
| (참고) 세션 5.5-B 까지 | 전문 + 미해결 **88자 절단** | 139줄 / 16,178자 |

길이는 이 로그를 포함한 **최종 `PROCEED.md` 기준**으로 다시 잰 값이다 — `--full` 은
마지막 세션 로그를 통째로 싣기 때문에 이 표 자체가 길이에 들어간다. 139줄/16,178자는
**구 스크립트를 구 `PROCEED.md` 에 돌린** 값이라 직접 비교 대상이 아니다.

**열린 미해결은 두 모드 모두 전문이고, 절단 코드(`SUMMARY_MAX_CHARS`)는 제거했다.**
#34 의 (가)·(나)·⑷ 가 축약형에서 끝까지 보이는 것을 출력으로 확인했다.

**`PROCEED.md` 파싱 규칙이 바뀌었다** (모듈 docstring에도 적었다)

- (신규) 「현재 상태」 표의 **행을 셀 단위로 읽는다** — 셀 안의 `\|`(escape 된 파이프,
  예 `\|잔차\|`)는 구분자로 세지 않는다.
- (신규) 마지막 세션 로그 블록에서 `**다음이 알아야 할 것**` 문단을 찾아 그 뒤를
  축약형에 싣는다. **못 찾으면 블록 전체**를 싣는다(조용히 비우지 않는다).
- (삭제) 열린 미해결의 88자 절단.
- 기존 규칙(절 경계 `## `, 마지막 `### ` 블록, 닫힘 표시 `[해소`·`[닫힘`·`[확정`·
  `**해소됨**`)은 그대로다.

**검사기** — pytest **1,153건** · ruff · mypy 전부 통과. 세션 5.5-B 와 같은 건수다
(모델 코드·테스트를 건드리지 않았으므로 같아야 한다).

**이 판에서 판정한 feasibility 기준: 없음.**

**프로젝트 지식 재업로드 필요** — `CLAUDE.md` · `project-overview.md` 2종.
`PROCEED.md` 는 대상이 아니다.

**다음이 알아야 할 것**

1. **파일럿의 판은 전부 끝났다.** 남은 것은 사람의 판정 둘 — 미해결 **#34**(다중
   CDU 전이의 누출 주입 지점: (가) 재생성 · (나) 해당 행 제외)와 **파일럿 종료
   판정**. 그 전에는 새 판을 열지 않는다.
2. **미해결 #35 를 사람이 정해야 한다** — `pyproject.toml` 에 런타임 4종을 고정할
   것인가, 기록만 하고 재생성 시 대조할 것인가.
3. **브리핑 기본이 축약형이 됐다.** 게이트 근거 수치와 과대해석 금지 문구는
   축약형에 없다 — `--full` 이나 `PROCEED.md` 를 직접 본다.

### 세션 5.5-D · 2026-08-29 · 다중 CDU 누출 주입 지점 정정 · 데이터셋 재생성

**미해결 #34 를 닫은 판이다.** 사람이 선택지 **(가)** 를 골랐다 — `plant.py` 를
고치고 `leak_cdu_index` 열을 추가한 뒤 재생성한다.

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/plant.py` | `plant_case_from_templates` 신설 · `PlantLoadStepCase.templates` · 저장 격자를 `storage_times_s` 로 |
| `src/cdu_simul/dataset.py` | `LEAK_CDU_INDEX` · `leak_cdu_index` 열 · 전이에 CDU별 템플릿 · `_storage_indices` 제거 |
| `src/cdu_simul/dataset_report.py` | 게이트 표 2행 갱신 · 열린 한계 2건 추가 |
| `tests/test_session55d_gates.py` | 신규 (정정 확인 22건 — 기준 선기재) |
| `project-overview.md` | 머리말 「최종 갱신」 · 「지금 어디인가」 로컬 경로 (사람의 지시) |
| `model.py`·`dynamics.py`·`leak.py`·`hydraulics.py`·`assumptions.py`·`dataset_plan.py` | **건드리지 않았다** |
| `CLAUDE.md`·`collaboration.md`·`AI서버_…정리.md` | **건드리지 않았다** |

**C1 — 5-1 에 새 행이 필요한가: 필요 없다(범위 안)**

| 물음 | 판단 |
|---|---|
| 누출점을 1개로 줄이는 것이 새 규약인가 | **아니다.** 5-1 「누출 주입 지점」이 이미 "**랙 1개**에 주입한다"이다. 종전 다중 전이가 **2개**여서 규정 **밖**이었고, 이 판은 코드를 **규정 안으로 되돌린 것**이다. 규정을 어기던 코드를 고치는 데 새 규정은 필요 없다 |
| 어느 CDU 인지 고르는 것이 새 숫자인가 | **아니다.** 5-1 이 랙 번호에 쓴 논리(대칭이므로 번호 선택이 새 숫자를 만들지 않는다)를 그대로 쓴다. CDU 사양은 전부 같다(5-1 「CDU 대수」). 게다가 **정상상태 경로는 세션 5.5-B 부터 이미 CDU 0 에만 걸고 있었다** — 전이를 거기에 맞춘 것이다 |
| 그래도 남는 것 | **대칭 논리가 전이에서는 완전하지 않다.** 비대칭 전이에서 CDU A 는 **부하가 스텝하는** 쪽, B 는 **불변인** 쪽이라 **역할이 다르다**. 누출을 A 에 고정하면 「누출이 정지 상태의 CDU 에 있고 옆 CDU 가 부하 변동을 겪는」 조합이 데이터셋에 없다. 이것은 새 가정이 아니라 **한계**이므로 5-1 이 아니라 `dataset_report` 열린 한계에 적었다 |

**C3 — 저장 격자 변경 후 세션 5 결과** (적분 설정 RK45·rtol·atol·구간 불변)

| 항목 | 종전 | 지금 |
|---|---|---|
| `t_eval` | 균등 2001점 | `storage_times_s` 201점 (`dynamics` 와 **같은 함수**) |
| `dataset.py` 의 보정 | 최근접 점 선택(`_storage_indices`) | **제거** |
| 저장 시각의 목표 대비 오차 | 최대 **0.005 τ** | **0** (정확히 같은 시각) |
| 데이터셋 시각 표본 수 | 201 | 201 (**불변**) |

`python -m cdu_simul.plant` 출력 표가 **바이트 단위로 동일**하다(`diff` 무차이).
`t_eval` 은 `solve_ivp` 의 스텝 선택에 영향하지 않고 dense output 을 어느 시각에서
평가할지만 정하므로 해가 이동하지 않는다. **세션 5 게이트 판정 불변** — 세션 5
게이트 테스트 450건(`test_session5_gates.py`·`test_gates_after_plant.py`)이 그대로
통과했다.

**C5 — 데이터셋 재생성** (가정값 기반 · 실측 아님)

| 항목 | 세션 5.5-B | 세션 5.5-D |
|---|---|---|
| 시나리오 | 1,792 | **1,792** (불변 — 사람이 정한 규모) |
| 행 | 310,016 | **310,016** (불변) |
| 열 | 48 | **49** (`leak_cdu_index` 추가) |
| 파일 크기 | 304.6 MB | **305.1 MB** |
| 소요시간 | 27.5분 | **61.3분** |
| solver 실패 행 | 0 | **0** |

**소요시간이 2.2배다 — 코드 때문이 아니라 PC 가 다르다.** 5.5-B 는 다른 PC 에서
돌았고 이 판은 새 PC 에서 돌았다. 이 판의 변경은 오히려 일을 **줄인다**(최근접 점
탐색 제거). 중단 조건(3배 = 82.5분)에는 걸리지 않았다. **두 판의 실행시간을 나란히
읽지 않는다** — 미해결 #35(의존성 버전 미고정)와 같은 이유로 PC 간 비교는 성립하지
않는다.

검증(`pandas` 로 전량 확인): 누출 있는 행의 `leak_cdu_index` 는 **전부 0**,
누출 없는 행은 **전부 빈 값**. 전이 시각 표본은 시나리오·CDU 마다 **201점 균일**.
표기 5열에 빈 칸 **0개**. `results/` 는 `.gitignore` 대상이라 커밋하지 않았다.

**C7 — 게이트 재판정 · 연동이 전이에 나타나는가**

앞 게이트 1-B·2·3·4·5 와 세션 5.5 게이트를 **판정 기준·허용오차를 하나도 바꾸지
않고** 다시 돌려 전부 통과했다(pytest 전량).

**이 판의 목적 — 부하가 그대로인 CDU B 가 A 의 누출에 반응하는가**
(범위 하단 조합 · A 20→100% 스텝 · B 20% 고정 / B 100% 고정 두 경우)

| 누출 | B 2차측 몫 변화 [L/s] | B 환수온도 변화 [K] (B=100%) | B 환수온도 변화 [K] (B=20%) |
|---|---|---|---|
| +5% | +2.130e-03 | −1.413e-03 | −1.419e-04 |
| +20% | +8.198e-03 | −5.437e-03 | −5.457e-04 |
| +50% | **+1.908e-02** | **−1.064e-02** | −1.268e-03 |

**세션 5 C7(정상상태·양쪽 100%)은 +50% 에서 B 가 −0.012649 K · 배분 +0.01905 L/s
였다.** 방향이 같고 **배분 이동은 세 자리까지 일치**한다(+0.01908 vs +0.01905).
온도 변화가 B=20% 에서 10배 작은 것은 B 의 ΔT 자체가 10배 작기 때문이다 — 같은
Cr 변화가 더 작은 K 변화로 나타난다.

**종전(양쪽 누출)에서는 상쇄됐다** — 같은 +50% 에서 배분 이동이 **+1.9e-05 L/s**
(≈0, A만일 때의 1/1000)이고 B 의 환수온도는 **오히려 +0.021 K 올랐다**(B 자신에게도
누출이 있었으므로). #34 가 말한 상쇄가 수치로 확인됐고, 이제 사라졌다.

**C4 의견 — 다중 CDU 전이도 누출을 스텝으로 넣는 편이 나은가 (사람이 정한다)**

**낫다고 본다. 다만 이 판에서 바꾸지 않았다.**
- **근거 ⑴** 데이터셋의 목적이 **누출 조기감지**(1장)다. 감지 모델이 배워야 할 것은
  「누출이 시작될 때의 파형」인데, 지금 다중 전이 행에는 그 파형이 없다 — 누출은
  t=0 이전부터 있고 움직이는 것은 부하다. 누출 스텝 파형은 `single` 행에만 있다.
- **근거 ⑵** 두 전이의 자극이 달라 `regime=transient` 를 한 덩어리로 읽을 수 없다.
  308,736 전이 행 중 205,824 행(67%)이 다른 자극이다.
- **반대 근거** 부하 스텝을 없애면 **연동을 보는 자극도 없어진다.** 5.5-B 가 부하
  스텝을 넣은 이유가 그것이었다(자극이 없으면 전이 행이 평평해진다). 누출 스텝만
  주면 A 의 누출이 A 의 유량을 바꾸고 그것이 배분을 움직이므로 연동은 **여전히
  생긴다** — 다만 크기가 부하 스텝보다 훨씬 작다.
- **가장 나은 안(제안)**: 부하 스텝 행은 그대로 두고 **다중 CDU 누출 스텝 행을
  따로 추가**한다. 규모가 늘어나므로 **사람이 정할 일이다**(규모는 사람의 결정).

**검사기** — pytest **1,175건**(종전 1,153 + 신규 22) · ruff · mypy 전부 통과.
**지운 테스트는 없다.**

**이 판에서 판정한 feasibility 기준** — 세션 5.5 게이트(A·B·C·D)와 앞 게이트
1-B·2·3·4·5. **전부 재판정 통과.** 새 기준을 만들지 않았고 허용오차도 바꾸지 않았다.
`test_session55d_gates.py` 의 넷은 **게이트가 아니라 정정의 확인**이다.

**프로젝트 지식 재업로드 필요** — `project-overview.md` 1종.
(`AI서버_…정리.md` 는 **고치지 않았다** — C1 판단이 「범위 안」이었다.)

**다음이 알아야 할 것**

1. **미해결 #34 가 닫혔다.** 파일럿 종료 판정을 막던 항목이 사라졌다 — 종료 판정
   재료가 완결됐다. **판정 주체는 사람이다**(`project-overview.md` 「파일럿 종료 조건」).
   `python -m cdu_simul.dataset_report` 가 재료를 낸다.
2. **C4 의견에 답이 필요하다** — 다중 CDU 전이도 누출을 스텝으로 넣을 것인가.
   추가하면 규모가 늘고 재생성이 다시 필요하다(이 PC 기준 약 1시간).
3. **데이터셋을 재생성했다면 `leak_cdu_index` 열이 있는 49열본인지 확인한다.**
   48열본(5.5-B)과 섞어 읽으면 안 된다 — 다중 전이 행의 **누출점 자체가 다르다**.
4. **미해결 #35 는 여전히 열려 있다** — 의존성 버전이 고정돼 있지 않다. 이 판의
   재생성이 5.5-B 와 실행시간이 2.2배 다른 것도 PC 가 다르기 때문이다.

**사람이 정한 것 — 세션 5.5-D 마무리** (기록이다. 미해결이 아니다)

**⑴ 파일럿 종료 판정 전에 세 판을 더 한다.** 판정 순서가 「판정 → (전환이면)
설계데이터 투입」이므로, **지금 자력으로 확인 가능한 것을 남긴 채 판정하면 비싼
데이터 투입 뒤에 무용지물임이 드러날 위험**이 있다.

| 판 | 내용 | 데이터셋 |
|---|---|---|
| **5.6** | 누출 질량손실 **부호 확인** (수력 한정) | **바꾸지 않는다** |
| **5.7** | 미해결 **#31 시간축 정정** (M 을 8랙으로) | **재생성** |
| **5.8** | **이동지연 감도** (N-CSTR 스윕) | 바꾸지 않는다 · **관측만** |

**⑵ 정지선 — 위 셋이 파일럿이 자력으로 갈 수 있는 끝이다.** 그 밖의 항목
(**#21** M 결손 · **#25** 등가길이 오차 · **#24** 잔여저항 분해 · 고정 NTU ·
2차측 온도 결합)은 **전부 설계데이터가 있어야 하며 판정을 막지 않는다.**

**⑶ 절대 규칙 8 예외 승인 (사람).** 누출을 K값 근사가 아닌 방식(**유량 손실**)으로
모사하는 것을 **부호 확인 목적의 별도 코드에 한해** 허용한다. 데이터셋과 5-1 의
누출 정의는 **K값 근사 그대로 둔다.** **규칙 8 자체를 바꾸지 않는다.**

**⑷ #31 배분 규칙 확정 (사람).** M 의 양 끝(전부 25A 하한 · 전부 80A 상한)을
**둘 다 8배**로 읽는다. 5-1 이 이미 두 극단으로 쓰므로 각각을 8벌로 읽는 것이며
**추가 숫자가 0**이다.

**⑸ 파일럿이 끝내 답하지 못하는 것** — 정확도(실측 없음) · 신호가 실측 가능한
크기인가(계측기 사양 없음) · 잔여저항 60~83% 가 실제 CDU 에서도 그런가(설계값).
**세 판을 다 해도 판정은 "정확하다"가 아니라 "이 구조로 설계데이터를 넣을 만하다"
까지다.**

**물리 검산 기록 (세션 5.5-D 마무리)**

**웹 대화창이 로그 수치를 손으로 대조한 것이다 — 이 판이 코드를 돌려 낸 값이
아니다.** 즉 **독립적인 재계산이 아니라 기록된 수치들 사이의 정합성 확인**이다.

· ΔT 가 32조합에서 **10.29 K 로 균일** — Q/(ṁcp) 손계산 10.30 K 와 일치.
  2차측·NTU 와 무관해야 하는 것이 그대로 나왔다
· 2차측 27→30℃ 에서 1차측이 **+2.9937~+2.9964 K 평행이동** (Q·ε 고정 시 강체 이동)
· NTU 2→3 에서 **ε 비 1.125 · 접근온도 비 1.125** — ΔT_max ∝ 1/ε 성립
· 대향류 Cr≈1 의 접근온도 균일: **저온단 5.17 K · 고온단 5.14 K**
· 누출 3수준에서 유량 감소율과 랙 온도상승이 **ΔT_rack=q/(ṁcp)** 로 상호 정합
· 질량보존: **(1/8)(누출랙) + (7/8)(타랙) = 총유량 변화**
· K 비 **1:4:10** 에 유량 신호 비 **1:3.80:8.68** — **Q ∝ 1/√C** 성립
· 양정 신호: 펌프곡선 **dH/dQ = −0.306 mAq/(L/s)** × 총유량 변화 = **+0.00130 mAq**,
  보고값과 **5자리 일치**
· τ 비 **12.79~12.82** 대 M 비 **12.83** — 6장 재정의 기준 **τ ∝ M** 성립
· 연동: 정상상태 대수해 **+0.01905 L/s** 대 전이 적분 **+0.01908 L/s**, 세 자리 일치

→ **설명되지 않는 수치는 없다.** 어색해 보이는 다섯(유속 3.48 m/s · 잔여저항
60~83% · 양정 신호 5e-5 · τ 0.72~9.2 s · 총유량 신호 0.16~0.37%)은 원인이 **전부
5장 가정치 또는 기록된 결손에 있다.**

### 세션 5.6 · 2026-08-30 · 누출 질량손실 부호 확인 (수력 한정) — 판정 기준 선기재

> **이 절은 코드보다 먼저 적고 먼저 커밋한다** — 세션 3-B 이후 원칙이며 세션 4
> (커밋 `803142e`) · 세션 5 (`acbc31c`) · 세션 5.5 (`cc790fa`) 와 같은 방식이다.
> **아래 기준을 결과에 맞춰 고치지 않는다.**

**절대 규칙 8 예외 (사람 승인 · 「사람이 정한 것 — 세션 5.5-D 마무리」⑶)**
이 판에 한해 누출을 **유량 손실**로 모사하는 것을 **별도 코드에서** 허용한다.
규칙 8 문구 · 데이터셋 · 5-1 「누출 시나리오」 정의는 **하나도 바꾸지 않는다.**
이 판의 코드는 데이터셋 생성 경로에 **들어가지 않는다**(어느 모듈도 import 하지
않는다). 기존 모듈은 **읽기만** 한다.

**이 판은 수력 한정이다.** 열·온도를 보지 않는다. 이유는 시간이 아니라 **절대
규칙 1**이다 — 빠져나간 유체를 보충수로 채우면 **보충수 온도**라는 5장에 없는
숫자가 필요해진다. 수력만 보면 그 숫자가 필요 없다. 물성은 정격 운전점의 1차측
벌크평균(37℃ · `rated_property_temperature_C()`) 하나로 고정한다 — 온도가 움직이지
않으므로 5-1 「수력 계산의 물성 평가 온도」 규약과 어긋나지 않는다.

#### 무엇을 대조하는가

네 양의 **부호**를 K 근사와 질량손실에서 각각 표로 낸다. 전부 **같은 수력 조합의
정상(누출 없음) 해 대비 차이**다 — 조합 안에서만 비교한다.

    ⑴ 펌프 총유량   ⑵ 펌프 양정   ⑶ 누출랙 콜드플레이트 통과유량   ⑷ 타 7랙 유량

#### 가설 (예단하지 말 것)

K 근사는 저항을 늘리므로 총유량이 줄고 양정이 오른다. 질량손실은 대기압으로
빠지는 **병렬 경로가 열리는 것**이라 계통 저항이 오히려 줄어 총유량이 늘고 양정이
내릴 수 있다 — 즉 ⑴⑵ 의 부호가 정반대일 수 있다. **이것은 가설이다. 결과를 여기에
맞추지 않는다. 반대로 나오면 반대로 적는다.**

#### 구조 자유도 — 5장·5-1 이 정하지 않는 둘 (코드를 읽어 먼저 적는다)

질량손실을 넣으려면 **누출점의 상류와 하류를 구분해야 한다.** 현재 모델은 공통
경로 저항을 **집중저항 하나**(계통 잔여저항 `C_res`)로 두고, 5-1 이 그것을 **분해
하지 말라고 못박고 있다**(「HX·CDU 내부·헤더로 분해하지 않는다」 · 미해결 #24).
그래서 다음 둘이 **5장에도 5-1 에도 없다**:

- **g — 잔여저항 중 환수측(누출점 → 펌프) 몫** [0, 1]. `g=0` 이면 누출점 하류에
  저항이 없어 질량손실이 **수력적으로 아무 흔적도 남기지 않는다**. `g=1` 이면
  최대로 남는다.
- **펌프가 보는 유량** — 계통 재고(보충수를 모델링하지 않으므로 줄어드는 재고)가
  펌프 **흡입** 쪽에서 채워지면 펌프는 공급유량 ΣQ 를, **토출** 쪽이면 환수유량
  ΣQ−Q_leak 을 본다.

**이 둘에 값을 고르지 않는다**(절대 규칙 1 · §7). 대신 **정의상 양 끝을 포함해
전수로 돌리고, 부호가 그것에 의존하는지를 결과로 적는다.** 분수의 양 끝(0·1)은
측정이 필요한 숫자가 아니므로 새 가정치를 만드는 것이 아니다. 세션 4 가 M 노드
배분 민감도(미해결 #20)를 **시험 코드 안에서만** 40:60·50:50·60:40 으로 돌리고
`assumptions.py` 에 넣지 않은 것과 같은 취급이다 — **이 판의 g 도 `assumptions.py`
에 넣지 않는다.**

#### 누출 위치 — 랙 출구 고정

5-1 「누출 주입 지점」이 누출을 **랙 1개 단위**로 정의했고 모델의 랙 분기는 집중
저항 하나이므로, 출구는 그 정의를 쪼개지 않는 지점이다. 분기 저항의 상류/하류를
나누면 5장에 없는 **배관 형상 선택**을 새로 만드는 것이 된다(5-1 빈칸 처리 순서
4번의 대상이며 방침 B 의 대상이 아니다 — 방침 B 는 5장이 범위로 준 값에만 쓴다).

> **먼저 적어 두는 구조적 사실**: 5-1 은 헤더를 **저항 0 의 공통 노드**로 둔다.
> 그러면 랙 0 의 출구 노드와 나머지 7랙의 출구 노드는 **같은 노드**다. 즉 이
> 모델에서 질량손실 누출은 **랙에 국소화될 수 없다.** ⑶ 과 ⑷ 가 정확히 같이
> 움직일 것으로 예상되며, 그렇게 나오면 그것이 K 근사와의 가장 큰 구조 차이다.
> **예상이지 판정이 아니다** — 코드가 낸 값으로 확인한다.

#### 누출 크기 — 새 값을 정하지 않는다

K 근사 3수준(+5/+20/+50%)이 만든 **누출랙 통과유량 감소량**을 코드가 조합마다 먼저
산출하고, **0 부터 그 상한까지 스윕**한다. 부호는 크기와 무관하게 정해지므로 크기를
고를 필요가 없다.

#### 판정 기준

- **기준 A — 부호 표.** ⑴~⑷ 를 두 방식에서 각각 낸다. 부호가 갈리면 갈린다고
  적는다. 어느 쪽이 "옳다"고 적지 않는다 — 이 판은 관측이다.
- **기준 B — 반례 확인**(collaboration.md ⑥). **누출 크기 × 양정 양 끝 × 분기 ΔP
  양 끝 × 밸브 ΔP 양 끝 × 구조 자유도(g · 펌프 유량 위치)** 전 조합에서 부호가
  **뒤집히는 경우가 있는지** 반드시 같이 돌리고 적는다. "전 조합에서 일정"과
  "조건에 따라 갈린다"는 결론의 무게가 다르다.
- **기준 C — 격리 확인.** 누출 크기 0 에서 질량손실 해가 K 근사의 정상 해와
  **부동소수점까지** 같아야 한다. 다르면 코드가 정상 경로를 건드린 것이다.
- **기준 D — solver**(절대 규칙 5). 전 조합의 `fsolve` `ier` 를 확인해 결과에
  싣는다. 실패 조합이 있으면 **버리지 않고 표시한다.**

#### 이 판이 판정하지 않는 것 (과대해석 금지)

- **6장 기준 넷을 하나도 보지 않았다** — energy balance · T_return 방향성 ·
  수렴시간 · 극단 케이스. 열모델이 이 판에 없다.
- **어느 누출 모사가 실제에 가까운가** — 실측이 없다. 두 방식의 부호를 나란히
  놓을 뿐이다.
- **계통이 비어가는 것** — 보충수를 모델링하지 않으므로 재고 감소는 시간축 문제이고
  이 판 범위 밖이다.
- **부호가 갈려도 모델을 고치지 않는다**(§7). 관측으로 기록하고 미해결 목록에
  올린다. 기존 데이터셋·5-1 누출 정의를 이 결과에 맞춰 손대지 않는다 — **사람 몫이다.**

#### 결과 (2026-08-30 · 위 기준 그대로 판정했다. 기준을 고치지 않았다)

**돌린 것이다 — 표의 모든 수치는 `python -m cdu_simul.leak_massloss` 실행 결과다.**
손계산은 아래 「물리 검산」 넷뿐이며 그 자리에 표시했다.

**[표 1] K 근사 — 정상 대비 변화 (8조합 × 3수준 범위)**

| 양 | 누출 +5% | 누출 +20% | 누출 +50% | 부호 |
|---|---|---|---|---|
| ⑴ 랙 통과 총유량 [L/s] | −6.405e-03 ~ −2.843e-03 | −2.468e-02 ~ −1.094e-02 | −5.755e-02 ~ −2.546e-02 | **−** |
| ⑴' 펌프 통과유량 [L/s] | (⑴ 과 같다) | (⑴ 과 같다) | (⑴ 과 같다) | **−** |
| ⑵ 펌프 양정 [mAq] | +1.305e-03 ~ +1.960e-03 | +5.018e-03 ~ +7.551e-03 | +1.166e-02 ~ +1.761e-02 | **+** |
| ⑶ 누출랙 통과유량 [L/s] | −2.163e-02 ~ −1.235e-02 | −8.230e-02 ~ −4.798e-02 | −1.877e-01 ~ −1.135e-01 | **−** |
| ⑷ 타 7랙 유량 [L/s] | +1.178e-03 ~ +2.445e-03 | +4.574e-03 ~ +9.301e-03 | +1.081e-02 ~ +2.122e-02 | **+** |

K 근사에서는 계통 밖으로 나가는 유량이 없으므로 ⑴ 과 ⑴' 이 같은 값이다.

**[표 2] 질량손실 — 정상(Q_leak=0) 대비 부호 (8조합 × 크기 4수준 × 배치 6)**

누출 크기는 조합마다 0 부터 **K 근사 +50% 가 만든 누출랙 유량 감소량**(0.113515 ~
0.187732 L/s)까지 스윕했다 — 새 숫자가 0이다.

| 배치 (구조 자유도) | ⑴ 총유량 | ⑴' 펌프유량 | ⑵ 양정 | ⑶ 누출랙 | ⑷ 타 7랙 |
|---|---|---|---|---|---|
| g=0.0 / 펌프=공급 | **0** | **0** | **0** | **0** | **0** |
| g=0.0 / 펌프=환수 | + | − | + | + | + |
| g=0.5 / 펌프=공급 | + | + | **−** | + | + |
| g=0.5 / 펌프=환수 | + | − | + | + | + |
| g=1.0 / 펌프=공급 | + | + | **−** | + | + |
| g=1.0 / 펌프=환수 | + | − | + | + | + |

**어느 칸에도 `갈림` 이 없다.** 배치를 고정하면 부호는 **누출 크기 4수준 × 양정 양
끝 × 분기 ΔP 양 끝 × 밸브 ΔP 양 끝 전 조합에서 뒤집히지 않는다**(기준 B). 부호를
가르는 것은 5장 가정치의 범위가 아니라 **배치(구조 자유도)** 다.

크기 범위(참고 · 배치별 최대):
g=1.0/펌프=공급 에서 ⑴ +1.673e-02 ~ +1.326e-01 L/s · ⑵ −6.110e-02 ~ −2.559e-03 mAq
· ⑶=⑷ +2.091e-03 ~ +1.657e-02 L/s.

#### 관측 넷 (결론이 아니라 관측이다 — 모델을 고치지 않았다)

**① ⑴ 과 ⑶ 의 부호가 정반대다.** K 근사는 총유량이 **줄고** 누출랙 유량이 **줄지만**,
질량손실은 (배치가 무신호인 경우를 빼면) 총유량이 **늘고** 누출랙 통과유량도 **는다**.
§5 의 가설이 이 두 양에서는 맞았다 — 질량손실은 대기압으로 빠지는 **병렬 경로**라
계통 저항이 줄어든다. **가설에 맞춰 고른 결과가 아니다**: ⑵ 는 아래처럼 가설과
다르게 나왔고 그대로 적었다.

**② ⑵ 펌프 양정의 부호는 5장·5-1 이 정하지 않는다.** 펌프가 **공급유량**을 보면
양정이 내리고(가설대로), **환수유량**을 보면 양정이 **오른다**(K 근사와 같은 부호).
줄어드는 계통 재고가 펌프 흡입 쪽에서 채워지는가 토출 쪽인가는 5장에도 5-1 에도
없다 — **양정만으로는 두 누출 모사를 구분할 수 없다.**

**③ 질량손실 누출은 이 모델에서 랙에 국소화되지 않는다.** 랙 간 유량 비대칭이
전 조합에서 최대 **2.220e-16 L/s**(부동소수점 잡음)였다. 5-1 이 헤더를 **저항 0 의
공통 노드**로 두므로 랙 0 의 출구 노드는 나머지 7랙의 출구 노드와 **같은 노드**다.
그래서 ⑶ 과 ⑷ 가 **정확히 같이** 움직인다 — K 근사가 ⑶↓ / ⑷↑ 로 갈라지는 것과
**구조가 다르다**. 데이터셋의 랙별 신호(어느 랙이 새는가)는 K 근사가 만들어 낸
것이고, 질량손실 쪽에는 대응물이 없다.

**④ 배치 g=0 / 펌프=공급 에서는 질량손실이 수력적으로 아무 흔적도 남기지 않는다**
(다섯 양 전부 정확히 0). 누출점 하류에 저항이 없고 펌프도 유량 변화를 못 보는
배치다. 즉 **"질량손실이 수력 신호를 만든다"는 것 자체가 잔여저항 배치에 의존한다.**

#### 물리 검산 (손계산 — 위 표의 실행값과 대조했다)

- **랙 간 질량보존**: `H33.6/dPb3/dPv3` · g=1.0/펌프=공급 · 최대 크기에서
  ΔQ_sup = **1.325955e-01** L/s, 8 × ΔQ_rack = **1.325955e-01** L/s — 차 **0.000e+00**.
- **펌프곡선 위 이동**: 같은 점에서 dH/dQ = −(a + 2bQ) = **−0.45890** mAq/(L/s),
  선형항 −0.45890 × 0.1325955 = **−6.0848e-02**, 곡률항 −b·ΔQ² = −0.014615 × 0.1326²
  = **−2.57e-04**, 합 **−6.1105e-02** — 실행값 **−6.110482e-02** 와 일치.
- **g 선형성**: g=0.5 의 ⑴ 최대 +6.616e-02 대 g=1.0 의 +1.326e-01 — 비 **2.004**.
- **격리 확인(기준 C)**: Q_leak=0 의 질량손실 해와 K 근사 정상 해의 랙 유량 최대 차
  **2.220e-16** L/s. 정상 경로를 건드리지 않았다.

#### solver (기준 D · 절대 규칙 5)

`fsolve` **272 회 전부 `ier=1` · 실패 0 건.** 압력평형 최대 잔차는 K 근사 경로
**5.592e-12 mAq** · 질량손실 경로 **2.324e-11 mAq** (양정 20~30 mAq 대비 상대
1e-12 수준). 실패 조합이 없었으므로 버린 결과도 없다.

**기존 테스트**: `pytest` **1,175건 전부 통과**(21분 05초) · `ruff`·`mypy` 통과.
이 판은 테스트를 하나도 더하거나 고치지 않았다 — 새 모듈이 기존 경로를 건드리지
않았음을 기존 테스트가 그대로 확인한 것이다.

#### 이 판이 판정하지 않은 것 (과대해석 금지)

- **6장 feasibility 기준을 하나도 보지 않았다** — energy balance · T_return 방향성 ·
  수렴시간 · 극단 케이스. 이 판에 열모델이 없다. 게이트 판정이 아니다.
- **어느 누출 모사가 실제에 가까운지 판정하지 않았다.** 실측이 없다. 두 방식의
  부호를 나란히 놓았을 뿐이다.
- **신호가 계측 가능한 크기인지 보지 않았다** — 계측기 사양이 없다.
- **계통이 비어가는 것을 보지 않았다** — 보충수를 모델링하지 않았고, 재고 감소는
  시간축 문제라 이 판 범위 밖이다.
- **모델·데이터셋·5-1 을 고치지 않았다.** §7 대로 관측으로만 기록하고 미해결
  **#36** 으로 올렸다. 어느 쪽으로 갈지는 **사람이 정한다.**

#### 만든/바꾼 파일

- **새로**: `src/cdu_simul/leak_massloss.py` (데이터셋 생성 경로 밖 · 어느 모듈도
  import 하지 않는다)
- **고침**: `PROCEED.md` (이 절 · 「현재 상태」 표 · 미해결 #36)
- **건드리지 않음**: `model.py` · `plant.py` · `dynamics.py` · `hydraulics.py` ·
  `leak.py` · `dataset*.py` · `assumptions.py` · 데이터셋 · 문서 4종 · 기존 테스트

#### 보고만 하는 발견 (절대 규칙 13 — 고치지 않았다)

1. `CLAUDE.md` 「명령어」 절에 `python -m cdu_simul.leak_massloss` 가 없다. 넣을지는
   사람이 정한다 — 이 모듈이 상시 실행 대상인지가 먼저다.
2. 세션 5.7·5.8 항목(#18·#19·#22 위생 · M 8배 · `stimulus_kind` · `dataset_version`)은
   이 판에서 **손대지 않았다.** 다음 판의 것이다.

### 세션 5.6-B · 2026-08-30 · 공급·환수 유량 불일치 확인 (5.6 결과 재출력) — 판정 기준 선기재

> **이 절은 코드보다 먼저 적고 먼저 커밋한다** — 세션 3-B 이후 원칙이다.
> **아래 기준을 결과에 맞춰 고치지 않는다.**

**새로 푸는 것이 없다.** 세션 5.6 이 이미 돌린 8조합 × 누출 크기 4수준 × 배치 6
전수를 **다른 양으로 출력**하는 판이다. `leak_massloss.py` 의 계산 로직·solver
설정·스윕 범위·배치 정의를 **하나도 바꾸지 않는다.** 바뀌는 것은 출력부와,
출력에 필요한 값을 모으는 자리뿐이다.

**격리 확인 방법**: 세션 5.6 이 낸 표 1·2·3 이 **바이트 단위로 동일**해야 한다.
표 4 만 새로 붙는다. 동일하지 않으면 계산부를 건드린 것이다.

#### 무엇을 내는가

각 케이스마다 넷을 낸다.

    ⑴ 펌프 공급유량 Q_sup   ⑵ 환수유량 Q_ret (= HX 1차측 입구 유량)
    ⑶ 불일치 Δ = Q_sup − Q_ret  [L/s 와 공급유량 대비 %]
    ⑷ 같은 케이스의 누출 유량 Q_leak

**K 근사 경로도 같은 표로 낸다.** 전 케이스에서 Δ = 0 이 나오는지 확인한다 —
0 이 아니면 그것은 5.6 계산의 결함이므로 **그 자리에서 멈추고 보고한다**(§6).

#### 먼저 적어 두는 것 — Δ 는 구성상 항등식이다 (결과보다 먼저)

`leak_massloss.solve_massloss` 는 환수유량을 `Q_ret = Q_sup − Q_leak` 으로 **정의**
한다. 따라서 Δ = Q_leak 은 **모델이 참이면 성립하는 물리 검산이 아니라, 코드가
정의대로 계산했는지 보는 항등식**이다. 기준 A 가 통과해도 그것이 확인해 주는 것은
"코드가 적힌 대로 계산한다"까지이고 **질량보존이 물리적으로 검증됐다는 뜻이 아니다**
— 애초에 이 모델에는 계통 밖으로 나가는 경로가 하나뿐이라 다른 결과가 나올 수 없다.
같은 이유로 기준 B·C 의 결과도 **대부분 정의에서 따라 나올 것으로 예상한다.**
**예상이지 판정이 아니다 — 코드가 낸 값으로 확인하고, 어긋나면 어긋난 대로 적는다.**

이 판의 실질 내용은 검산이 아니라 **크기(%)와, 그 양이 배치에 어떻게 반응하는가**다.

#### 판정 기준

- **기준 A — 검산.** 질량손실 경로에서 **Δ = Q_leak** 인가(수치오차 범위 내).
  K 근사 경로에서 **Δ = 0** 인가. 부동소수점 잔차를 값으로 적는다.
- **기준 B — 단조.** 누출 크기 4수준에서 Δ 가 **단조 증가**하는가.
- **기준 C — 배치 불변.** Δ 가 **배치 6에 걸쳐 같은가.** 세션 5.6 이 드러낸 구조
  자유도 둘(잔여저항 하류 몫 g · 펌프가 보는 유량 위치)에 이 양이 흔들리는지 본다.
  **가설**: Δ 는 누출 유량 그 자체이므로 배치와 무관할 것이다. **이것은 가설이다.**
  특히 **`g=0 / 펌프=공급` 배치를 반드시 포함해 확인한다** — 세션 5.6 관측 ④ 에서
  다섯 양이 전부 정확히 0 이었던 배치다. 그 배치에서 **Δ 도 0 이면 가설이 틀린
  것이고, Δ 가 0 이 아니면 그 배치의 유일한 생존 신호**라는 뜻이다. **어느 쪽이든
  그대로 적는다.**

#### 하지 않는 것

- **세션 5.6 이 낸 네 양(총유량·양정·누출랙 유량·타 7랙 유량)과 크기를 비교하지
  않는다.** 크기 비교는 계측기 사양이 있어야 뜻이 생긴다(「사람이 정한 것 — 세션
  5.5-D 마무리」⑸).
- **6장 feasibility 기준을 하나도 보지 않는다** — 이 판도 수력 한정이고 열모델이
  없다. 게이트 판정이 아니다.
- **새 숫자를 하나도 만들지 않는다.** 이 판은 새 숫자가 **0 이어야 한다** — 필요해
  지면 멈춘다(절대 규칙 1).
- **모델·데이터셋·5-1·`CLAUDE.md`·미해결 #36 문구를 고치지 않는다.** 5.7 스키마에
  열을 넣을지는 **사람이 정한다** — 이 판은 **재료만** 적는다.

#### 결과 (2026-08-30 · 위 기준 그대로 판정했다. 기준을 고치지 않았다)

**새로 푼 것이 없다.** 세션 5.6 의 272 회 해를 다른 양으로 읽었을 뿐이다. 표의
수치는 전부 `python -m cdu_simul.leak_massloss` 재실행 결과이며, 재실행은 5.6 과
같은 값을 낸다(아래 격리 확인).

**[표 4] 공급·환수 유량 불일치 Δ = Q_sup − Q_ret**

| 경로 · 배치 | Δ [L/s] | Δ/Q_sup [%] | 항등식 잔차 [L/s] | 단조 |
|---|---|---|---|---|
| K 근사 | +0.000e+00 ~ +0.000e+00 | +0.000e+00 ~ +0.000e+00 | 0.000e+00 | 해당없음 |
| g=0.0 / 펌프=공급 | +0.000e+00 ~ +1.877e-01 | 0 ~ **+1.211** | 8.882e-16 | 예 |
| g=0.0 / 펌프=환수 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.209 | 8.882e-16 | 예 |
| g=0.5 / 펌프=공급 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.206 | 8.882e-16 | 예 |
| g=0.5 / 펌프=환수 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.205 | 8.882e-16 | 예 |
| g=1.0 / 펌프=공급 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.202 | 8.882e-16 | 예 |
| g=1.0 / 펌프=환수 | +0.000e+00 ~ +1.877e-01 | 0 ~ +1.200 | 8.882e-16 | 예 |

Δ 의 하한 0 은 **누출 크기 0(정상)** 행이다. 누출이 있는 4수준만 보면
`g=0.0/펌프=공급` 배치에서 **+2.838e-02 ~ +1.877e-01 L/s** 다.

#### 판정

**기준 A — 검산: 통과.** 질량손실 경로에서 Δ 와 누출 유량의 차가 전 케이스 최대
**8.882e-16 L/s**(부동소수점 1 ulp 수준), K 근사 경로에서 Δ 가 전 케이스 정확히
**0.000e+00 L/s** 다. K 근사에서 Δ ≠ 0 인 케이스는 **없었다** — §6 의 멈춤 조건에
걸리지 않았다.

> **그러나 이것은 구성상 항등식이다**(판정 기준에 미리 적어 둔 대로).
> `solve_massloss` 가 환수유량을 `Q_ret = Q_sup − Q_leak` 으로 **정의**하고 K 근사
> 에는 유출 경로가 아예 없다. 통과의 뜻은 **「코드가 정의대로 계산한다」까지이고
> 질량보존이 물리적으로 검증됐다는 뜻이 아니다.** 8.882e-16 은 물리가 아니라
> 부동소수점 반올림의 크기다.

**기준 B — 단조: 통과.** 질량손실 **전 배치 · 전 조합**에서 Δ 가 누출 크기 4수준에
대해 **엄격히 증가**한다. K 근사는 누출 크기 축 자체가 없고(축은 K 배율이다)
Δ ≡ 0 이라 기준 B 의 대상이 아니다 — 표에 `해당없음` 으로 적었다.

**기준 C — 배치 불변: 통과. 가설이 맞았다.** 같은 (조합 · 누출 크기)에서 배치 6 에
걸친 Δ 의 최대 편차가 **1.776e-15 L/s** 다. Δ 는 세션 5.6 이 드러낸 구조 자유도
둘(잔여저항 하류 몫 g · 펌프가 보는 유량 위치)에 **흔들리지 않는다.**

**`g=0.0 / 펌프=공급` 배치에서 Δ 는 0 이 아니다** — 누출 4수준에서 **+2.838e-02 ~
+1.877e-01 L/s**(공급유량 대비 최대 **1.211%**). 세션 5.6 관측 ④ 에서 다섯 양
(총유량·펌프유량·양정·누출랙 유량·타 7랙 유량)이 **전부 정확히 0** 이었던 배치다.
즉 **Δ 는 그 배치의 유일한 생존 신호다.**

#### 관측 (판단이 아니라 재료다)

**① Δ 는 K 근사에 원리적으로 대응물이 없다.** K 근사는 질량이 보존되므로 Δ ≡ 0
이다. 세션 5.6 이 낸 네 양은 **부호가 갈리거나(⑴⑵⑶) 같았지만(⑷) 어쨌든 양쪽에
존재했다.** Δ 는 **한쪽에만 존재하는 첫 양**이다.

**② Δ 는 세션 5.6 이 드러낸 구조 자유도에 의존하지 않는 유일한 양이다.**
5.6 의 ⑵ 펌프 양정은 배치에 따라 부호가 갈렸고, 다섯 양 전부가 0 이 되는 배치도
있었다. Δ 는 배치 6 전체에서 편차 1.776e-15 L/s 다. 미해결 **#24**(잔여저항 분해
금지)가 닫히지 않아도 이 양은 정해진다.

**③ 다만 ①②는 모델 구조에서 따라 나온다.** Δ 는 이 모델에서 **강제한 누출 유량
그 자체**이고, K 근사에 유출 경로가 없다는 것도 정의다. **새로 알게 된 사실이라기
보다 정의를 값으로 확인한 것**이며, 그 값의 크기(공급유량 대비 최대 1.2%)만이
정의에서 바로 읽히지 않는 부분이다.

#### 세션 5.7 스키마에 대한 재료 (판단하지 않는다 — 사람이 정한다)

- **넣는 쪽 재료**: Δ 는 배치에 흔들리지 않고(편차 1.8e-15 L/s) 누출 크기에 엄격히
  단조이며, 다섯 양이 전부 0 인 배치에서도 **유일하게 살아남는다**. 데이터셋에
  대응물이 없다.
- **안 넣는 쪽 재료**: 현재 데이터셋은 **K 근사 전용**이라 이 열은 **전 행에서 항상
  0** 이 된다 — 정보량이 0 인 열이 하나 늘 뿐이다. 질량손실을 데이터셋에 넣기로
  정하지 않는 한(미해결 **#36** 의 (나)·(다)) Δ 열은 값을 갖지 못한다.
- **그래서 이 판이 남기는 것은**: 「Δ 열이 필요한가」가 **#36 의 선택에 종속**이라는
  사실이다. #36 이 (가)로 정해지면 Δ 열은 필요 없고, (나)·(다)로 정해지면 Δ 는
  **배치 자유도에 흔들리지 않으므로 열로 넣을 수 있는 양**이다.

#### 격리 확인 — 계산부를 건드리지 않았다

세션 5.6 이 낸 **표 1·2·3 의 6,295 바이트가 바이트 단위로 동일**하다(표 4 앞에
넣은 빈 줄 하나만 다르다). `git stash` 로 5.6 판 코드를 그대로 되돌려 실행해
대조했다. 계산 경로에 닿는 유일한 diff 는 스윕 루프에 `enumerate` 를 붙여 크기
순번을 얻은 것 한 줄이고, **순회 대상과 값은 그대로다.**

#### solver

세션 5.6 의 플래그를 그대로 인용한다 — `fsolve` **272 회 전부 `ier=1` · 실패 0 건**.
이 판에서 재실행했고 같은 값이 나왔다(위 바이트 동일성이 그것을 보인다).

**기존 테스트**: `pytest` **1,175건 전부 통과**(18분 38초) · `ruff`·`mypy` 통과.
이 판도 테스트를 하나도 더하거나 고치지 않았다 — 출력부만 바뀌었고 기존 경로가
그대로임을 기존 테스트가 확인한 것이다.

#### 이 판이 판정하지 않은 것 (과대해석 금지)

- **Δ 가 크게 나와도 계측 가능하다는 뜻이 아니다** — 계측기 사양이 없다.
- **실제 누출에 가깝다는 뜻도 아니다** — 실측이 없다.
- **세션 5.6 이 낸 네 양과 크기를 비교하지 않았다** — 크기 비교는 계측기 사양이
  있어야 뜻이 생긴다(「사람이 정한 것 — 세션 5.5-D 마무리」⑸).
- **6장 feasibility 기준을 하나도 보지 않았다** — 이 판도 수력 한정이고 열모델이
  없다. 게이트 판정이 아니다.
- **모델·데이터셋·5-1·`CLAUDE.md`·미해결 #36 문구를 고치지 않았다.**

#### 만든/바꾼 파일

- **고침**: `src/cdu_simul/leak_massloss.py` (**출력부만** — `MismatchRow` ·
  `_mismatch_row` · `format_mismatch_table` · `_mismatch_spread_across_topologies`
  추가 · `ComparisonRun` 에 수집 자리 하나)
- **고침**: `PROCEED.md` (판정 기준 · 이 절 · 「현재 상태」 표)
- **건드리지 않음**: `model.py` · `plant.py` · `dynamics.py` · `hydraulics.py` ·
  `leak.py` · `dataset*.py` · `assumptions.py` · 데이터셋 · 문서 4종 · 기존 테스트 ·
  미해결 #36 문구

#### 보고만 하는 발견 (절대 규칙 13 — 고치지 않았다)

1. 세션 5.6 의 「보고만」 1번이 그대로 열려 있다 — `CLAUDE.md` 「명령어」에
   `python -m cdu_simul.leak_massloss` 가 없다.
2. 세션 5.7 항목(#18·#19·#22 · M 8배 · `stimulus_kind` · `dataset_version` ·
   `leak_model` · #36 리포트 출력)은 이 판에서 **손대지 않았다.**

### 세션 5.7 · 2026-08-30 · M 8랙 정정(#31) · 스키마 3열 · 데이터셋 재생성

**게이트를 새로 만들지 않았다.** 앞 게이트(1-B · 2 · 3 · 4 · 5 · 5.5)를 기존 기준
그대로 재판정했다 — **판정 기준·허용오차를 하나도 바꾸지 않았다.** 그래서 판정 기준
선기재 대상이 없다.

#### 실행 순서 — 재생성 전에 문제를 전부 드러냈다

① 위생 3건 → ② 검사기 → ③ M 8배 → ④ 검사기 → ⑤ 스키마·리포트 → ⑥ 검사기 →
**⑦ 멈춤·사람 승인** → ⑧ 재생성 → ⑨ 검사기. ⑦ 을 넣은 이유는 M 이 정상상태 해에
영향하지 않는다는 5-1 서술을 재생성 **전에** 확인할 수 있었기 때문이다.

#### ① 위생 3건 — 계산 결과를 비트 하나도 바꾸지 않았다

| # | 내용 | 성격 |
|---|---|---|
| **#18** | `fluid.py` `REFERENCE_PRESSURE_Pa` 주석이 5-1 **확정** 문언을 인용하게 | 주석만 |
| **#19** | Cr 유도 규약 인용을 「세션 5-B」 → **5-1 「2차측 유체」 행** | 주석·docstring만 |
| **#22** | `model._property_temperature_from_state` → **공개 이름** 승격 | 이름만, 본문 무수정 |

확인: 수치 표 6개(`model`·`hydraulics`·`dynamics`·`leak`·`plant`·`leak_massloss`)가
**바이트 단위로 동일**. `dataset_plan` 만 달랐고 차이는 **측정 벽시계 시간 줄뿐**이다
(그 모듈은 실행시간을 재는 것이라 원래 비결정적이다 — 수치 표가 아니다).
② 검사기: pytest **1,175건 전부 통과**(19분 11초) · ruff · mypy 통과.

#### ③ M 8랙 정정 (#31)

`holdup_bounds()` 가 내던 M 은 5-1 등가길이 정의(「랙 1개 회로의 왕복 전체」) 때문에
**랙 1개 회로분**이었다. 사람이 정한 규칙대로 **양 끝을 둘 다 8배**로 읽는다.
8 은 `SCENARIO.racks_per_cdu`(5장 「CDU당 연결 랙 수」)에서 읽으므로 **코드에 숫자를
박지 않았고 추가 숫자가 0**이다. 노드 배분(공급 50% · 환수 50%)은 5-1 그대로이며
**8배는 총량에만** 걸린다.

| | 랙 1개 회로분 | 8랙 계통 | 배수 |
|---|---|---|---|
| M 하한 (전부 25A · 등가길이 하한) | 11.2756 kg | **90.2050 kg** | 8.0000000000 |
| M 상한 (전부 80A · 등가길이 상한) | 144.6976 kg | **1157.5805 kg** | 8.0000000000 |

**⒝ 정상상태 해는 불변이다** — `model`(32조합 열결합) · `hydraulics`(8조합 유량분배)
표가 **바이트 단위로 동일**하고 전이 종점 온도도 그대로다(30.098 / 42.469 ℃).
**5-1 「M 은 정상상태 해에 영향하지 않는다」가 코드에서 성립했다** — 멈춤 조건에
걸리지 않았다.

**⒞ τ·t63·t95 의 실제 이동 배수 = 정확히 8.000000배** (부하 스텝 4케이스 · 누출
스텝 6케이스 전부). 예상(약 8배)과 어긋나지 않았고 **정확히 8배인 까닭도 안다** —
적분 격자가 τ 단위로 잡히고 2노드 ODE 가 M 에 대해 선형이라 궤적 전체가 그대로
스케일된다. (인쇄 표의 반올림값으로 계산하면 7.99~8.17 배로 보인다. 위는 반올림 전
원값으로 재계산한 것이다.)
④ 검사기: pytest **1,175건 전부 통과**(16분 14초) · ruff · mypy 통과.

#### ⑤ 스키마 3열 (49 → **52열**) · 리포트

| 열 | 값 | 성격 |
|---|---|---|
| `dataset_version` | **`session-5.7`** | 판본 축을 세션 번호로. 48열본(5.5-B)·49열본(5.5-D)은 이 열 자체가 없어 **부재가 곧 5.7 이전**이라는 표지다. **파일명은 바꾸지 않았다** |
| `stimulus_kind` | `none` / `leak_step` / `load_step` | `regime`·`cdu_config` 에서 **읽는다**. 새 시나리오 0 · 규모 증가 0 |
| `leak_model` | `K_approx` (전 행 상수) | 질량손실과 부호가 반대이므로(#36) 이 열이 없으면 어느 쪽 데이터인지 알 수 없다 |

행별 `transient_caveat` 문구도 고쳤다 — #31 이 닫혔고 τ 가 정확히 8배로 이동했으나
**#21·#25·#23 때문에 절대 시간 규모는 여전히 해석하지 않는다**는 것을 전 행이 싣는다.

`dataset_report` 에 셋을 넣었다: **⒜ 생성 환경 절**(Python 3.12.10 · Windows 11 ·
numpy 2.5.2 · scipy 1.18.1 · CoolProp 8.0.0 · pandas 3.0.5 — **#35 는 닫지 않는다**,
「본 프로젝트 전환 시 고정」) · **⒝ ③절에 #36 종료 판정 재료 6항목** · **⒞ #24 문구
보강**(「유량분배 값의 크기」에 더해 **「누출 신호의 존재 여부까지」** 지배).
⑥ 검사기: pytest **1,175건 전부 통과**(20분 22초) · ruff · mypy 통과.

#### ⑦ 멈춤 — 사람이 재생성을 승인했다

⒜~⒡ 를 재생성 **없이** 보고하고 승인을 받은 뒤 ⑧ 로 넘어갔다.

#### ⑧ 재생성 — 판본 수치

| | |
|---|---|
| 행 | **310,016** (5.5-D 와 같다 — 규모를 늘리지 않았다) |
| 열 | **52** (49 → 52) |
| 용량 | **377,295,246 바이트 (359.8 MB)** |
| 소요 | **3,417.7 초 (57.0 분)** |
| solver 실패 행 | **0** |
| `dataset_version` | `session-5.7` — 310,016행 전부 |

CSV 를 직접 읽어 검증했다: 헤더가 `column_names()` 와 일치 · 표기 5열과 신 3열에
**빈 칸 0** · `leak_model` 이 전 행 `K_approx` · `stimulus_kind` 분포가
steady 1,280 (`none`) · single 전이 102,912 (`leak_step`) · dual_asymmetric 전이
205,824 (`load_step`) 로 자극 형태와 일치.

**중단 한 번 있었다(기록)**: 첫 실행이 하네스 백그라운드 작업 한도에 걸려 1200/1792
지점(37.9분)에서 kill 됐다. 모델·코드 문제가 아니다. `nohup` 으로 분리해 다시 돌려
완주했고, 두 번째 실행이 처음부터 새로 쓴 것이다(이어붙이지 않았다).

⑨ 검사기(재생성 후): pytest **1,175건 전부 통과**(16분 37초) · ruff · mypy 통과.

#### 앞 게이트 재판정

**전부 기존 기준 그대로 통과했다 — 판정 기준·허용오차를 하나도 조정하지 않았다.**
검사기 ②·④·⑥·⑨ 네 번 전부 pytest 1,175건 통과이며, 여기에 1-B·2·3·4·5·5.5·5.5-D
게이트 테스트가 모두 들어 있다. M 8배는 정상상태 게이트의 **값을 바꾸지 않았고**,
전이 게이트의 **부호·방향을 바꾸지 않았다**(시간축만 정확히 8배로 이동).

#### 6장 기준 — 무엇을 봤고 무엇을 안 봤나

- ① energy balance · ② T_return 방향성 · ③ 극단 케이스 비발산 — 기존 판정 유지
- ④ **수렴시간은 여전히 판정 불가다.** #31 이 닫혀도 **#21**(HX·CDU 내부·콜드플레이트
  M 결손) · **#25**(등가길이의 과대 방향 오차) · **#23**(배관 규격 계열 약 7% 이동)이
  그대로 열려 있다

#### ※ 과대해석 금지

**M 8배는 결손을 메운 것이 아니다.** 랙 1개 회로분을 8랙 계통으로 **해석**한 것뿐이고,
M 에 빠진 것(#21)·등가길이의 방향 반대 오차(#25)·규격 계열 의존(#23)은 하나도 줄지
않았다. 두 오차의 방향이 반대라 **상쇄 정도를 모른다.** **τ 의 절대값은 여전히
해석 불가**이며, 이 판을 「이제 수렴시간을 판정할 수 있다」로 읽지 않는다.

#### 만든/바꾼 파일

- **고침**: `fluid.py`(#18 주석) · `model.py`(#19 인용 · #22 이름) · `dynamics.py`
  (#22 호출부 · #31 M 8배 · 한계 문구) · `leak.py`·`plant.py`·`assumptions.py`(한계
  문구) · `dataset.py`(3열) · `dataset_plan.py`(행별 caveat) · `dataset_report.py`
  (⒜⒝⒞) · `PROCEED.md`
- **재생성**: `results/cdu_dataset.csv` (52열본 · 원격에 없다 — `.gitignore` 대상)
- **건드리지 않음**: 5장 표 · `CLAUDE.md` · `collaboration.md` ·
  `project-overview.md` · 5-1 · `pyproject.toml` · `leak_massloss.py` · 기존 테스트 ·
  미해결 #36 의 (가)/(나)/(다) 선택

#### 사람이 밟아야 하는 것

**5-1 「누출 시나리오」·「누출 주입 지점」에 #36 한계 문구 추가 — 사람이 직접 한다.**
정본 수정이라 이 판은 손대지 않았다. **고치면 프로젝트 지식 재업로드가 필요하다.**

#### 보고만 하는 발견 (절대 규칙 13 — 고치지 않았다)

1. `CLAUDE.md` 「명령어」에 `python -m cdu_simul.leak_massloss` 가 없다 (세션 5.6 부터
   열려 있다). 상시 실행 대상인지 사람이 정할 일이다
2. `dataset_plan.SCHEMA_DRAFT` 는 5.5-D 의 `leak_cdu_index` 와 이 판의 3열을 담고
   있지 않다. 5-1 이 「열 목록은 코드가 정본이다」라고 적었고 그 목록은 설계 초안이라
   **고치지 않았다** — 초안으로 남길지 지울지는 사람이 정한다


### 세션 5.7-C · 2026-08-30 · 오늘 결정 기록 (기록 판 · 코드 무수정)

**기록 판이다. 게이트 없음 · feasibility 판정 없음 · 수치 변경 0.**
모델 코드·테스트·데이터셋(52열본)·`dataset_report`·`leak_massloss.py`·정본 문서
4종을 **하나도 건드리지 않았다.** 바뀐 파일은 `PROCEED.md` 하나다.

#### ⑴ 정지선 변경 — 사람이 정했다

세션 5.5-D 「정지선」(⑵항 — 5.6·5.7·5.8 이 파일럿 자력의 끝)을 **한 판 넓힌다.**

**까닭**: 세션 5.6·5.6-B 는 그 정지선을 그은 **뒤에** 나왔다. 그 둘이 드러낸 것은
**누출 정의가 교체 대상**이라는 사실이고(K 근사와 질량손실의 부호가 총유량·누출랙
유량에서 정반대 · 미해결 #36), **교체된 정의가 열·전이에서도 서는지는 아직 아무도
보지 않았다** — 5.6·5.6-B 는 **수력 정상상태 한정**이었다.

**새 순서**::

    5.7-D (질량손실 열·전이 관측) → 5.8 (이동지연 감도) → 파일럿 종료 판정

**5.7-D 를 5.8 앞에 두는 까닭**: 5.8 은 **시간축 이산화**를 보는 판이고, 5.7-D 가
누출 정의를 바꿔 열·전이를 돌리면 그 결과도 **같은 시간축 위에** 있다. 순서를
바꾸면 5.8 을 두 정의에 대해 **두 번** 보게 된다.

**5.5-D 정지선의 나머지는 그대로다** — #21 · #25 · #24 · 고정 NTU · 2차측 온도
결합은 여전히 **설계데이터가 있어야 하며 종료 판정을 막지 않는다.**

#### ⑵ 보충수 불필요 — 사람이 닫았다 (5-1 문구는 사람이 직접 넣는다)

질량손실 누출 모사에 **보충수 온도는 필요하지 않다.**

**까닭**: 이 과제의 목적은 **미량 누출 조기감지**(1장)이고, 미량 구간에서는 밀폐루프
**보충 로직이 작동하지 않는다.** 보충이 돌 만큼 커진 누출은 이미 조기감지 구간이
아니다. 따라서 조기감지 시나리오에서 보충수는 **정의상 등장하지 않는다** — 새
가정치를 만드는 것이 아니라 **1장 목적과 5장 누출 크기에서 따라 나오는 역산**이다
(5-1 빈칸 처리 순서 1번).

**이것이 세션 5.6 §3 의 전제를 바꾼다.** 5.6 은 "빠져나간 유체를 보충수로 채우면
보충수 온도라는 5장에 없는 숫자가 필요해진다"는 이유로 **수력 한정**을 택했다.
그 이유가 닫혔으므로 **5.7-D 가 열·전이를 볼 수 있다.**

**한계 — 그 대신 계통이 시간에 따라 줄어든다.** 계통 **정압** 강하를 누출 신호로
쓰려면 **팽창탱크 용적 · 초기 충전압**이라는 **5장 밖 숫자 둘**이 필요하다 —
설계데이터 확보 시 재검토한다. **누출을 푸는 것 자체에는 필요 없다**: 세션 5.6 이
계통 압력 없이 272 회를 수렴시켰다(전부 `ier=1`).

**5-1 에 넣는 것은 사람이 직접 한다**(아래 「사람이 밟아야 하는 것」). 여기 적은
것은 **인계용 기록**이다.

#### ⑶ 계통 dP 정리 (오늘 대화에서 확인한 것)

폐쇄루프 **총 ΔP = 펌프 양정** = 5장 **20~30 mAq = 196.1~294.2 kPa**.
내역: 랙 분기 **2~3 mAq** · 밸브 **3~5 mAq** · 계통 잔여저항(**종속값** — 총양정의
**60~83%** · 미해결 #24).

**이것은 순환 dP 이고 계통 정압이 아니다.** 실제 CDU 운전압은 **정압 + dP** 이며,
**정압은 이 모델에 없다**(팽창탱크를 모델링하지 않는다). 누출 신호로서의 **압력
강하는 정압 쪽**이고, 그래서 ⑵ 의 「5장 밖 숫자 둘」이 거기에 걸린다.

#### ⑷ 시연 관점 — 앞으로의 판이 감안할 것 (사람 지시)

`project-overview.md` 「시각화 방향」이 **파일럿에서는 손대지 않는다**로 정해 뒀고
**그것은 그대로다.** 다만 데이터셋 작업이 끝난 뒤 「보여줄 것이 없다」가 되지 않도록,
앞으로의 판은 **결과를 남길 때** 다음을 감안한다.

- **시연 후보 재료는 현재 데이터셋에 이미 있다** — 정상상태 **1,280행**(단일 256 ·
  2대 대칭 512 · 2대 비대칭 512) · 다중 CDU 부하 전이 **205,824행**
  (`stimulus_kind=load_step`). 특히 **다중 CDU 연동**(한쪽 부하가 오르면 공유 2차측
  배분이 이동한다 — 세션 5 C7 · 5.5-D)은 **2장 문제의식을 직접 보여주는 재료**다.
- **미확인 하나 → 미해결 #37 로 열었다.** `stimulus_kind=none`(steady) 1,280행에
  **누출 K값이 걸린 정상상태 케이스가 섞여 있는지 확인되지 않았다.** `none` 은
  「자극이 없다」는 뜻이지 **「누출이 없다」는 뜻이 아니다.** 시연에서 누출을 빼려면
  누출 관련 열(`leak_level_percent` · `leak_cdu_index`)로 한 번 더 걸러야 하는지
  **다음 판에서 확인한다.**
- **시연의 범위·도구·형태는 지금 정하지 않는다.** 종료 판정 뒤다 — 판정이 「전환」
  으로 나오면 **누출 정의가 바뀌고 보여줄 것도 바뀐다.**

#### ⑸ 미해결 갱신 — 전부 append. 덮어쓰지 않았다

- **#36 갱신** — (가)/(나)/(다) 중 **파일럿 안에서 실행되는 것은 (가)**(K 근사 유지 +
  한계 명시)이고, 5-1 문구 추가로 실행된다. (나)로 가는 데 필요했던 것 중 **보충수
  온도는 오늘 닫혔다**(⑵). 남은 것은 **#24 잔여저항 분해**와 **계통 정압**이며 둘 다
  설계데이터 대기다. **최종 선택은 여전히 사람 몫이다.**
- **#37 신규** — 시연 재료의 누출 혼입 여부 미확인.

#### 검사기 — 코드를 안 건드렸으므로 그대로 통과해야 한다

`pytest` **1,175건 전부 통과**(16분 21초) · `ruff` · `mypy` 통과. 하나라도 실패하면
멈추기로 했으나 걸린 것이 없었다. 데이터셋 파일도 재생성본 그대로다(377,295,246 바이트).

#### 만든/바꾼 파일

- **고침**: `PROCEED.md` 하나
- **건드리지 않음**: 모델 코드 전부 · 테스트 · 데이터셋(52열본) · `dataset_report` ·
  `leak_massloss.py` · 정본 문서 4종 · 세션 5.8 항목

#### 사람이 밟아야 하는 것 — 5-1 에 문구 둘 (사람이 직접)

정본 수정이므로 이 판은 건드리지 않았다. **넣은 뒤 프로젝트 지식 재업로드가 필요하다.**

⒜ **누출 시나리오 한계** — 「누출 시나리오」 또는 「누출 주입 지점」 행.
   K 근사가 5장이 정의한 **대용(proxy)** 이고 질량손실과 총유량·누출랙 유량의 부호가
   **정반대**라는 것(#36 · 세션 5.6).
⒝ **보충수 처리** — 위 ⑵ 문안(미량 조기감지 구간에서는 보충 로직이 작동하지 않으므로
   보충수 온도가 정의상 등장하지 않는다 · 대신 계통 정압은 5장 밖 숫자 둘이 필요하다).

**문안은 웹 대화창이 초안을 냈고 사람이 확정한다** — 이 판은 초안을 만들지 않았다.

#### 다음 판

**5.7-D — 질량손실 열·전이 관측.** ⑵ 가 보충수 문제를 닫았으므로 세션 5.6 이 수력
한정을 택한 이유가 사라졌다.



### 세션 5.7-E · 2026-08-31 · 5-1 에 문구 둘 추가 (사람 지시 · 정본 수정)

**문서 판이다. 게이트 없음 · 코드 무수정 · 수치 변경 0.** 모델 코드·테스트·
데이터셋(52열본)·`dataset_report`·`leak_massloss.py` 를 하나도 건드리지 않았다.

#### 무엇을 어디에 넣었는가

**`AI서버_CDU_디지털트윈_프로젝트정리.md` 의 5-1 절만** 고쳤다. 문안은 사람이
확정한 것을 **한 글자도 바꾸지 않고** 실었다 — 표 한 줄에 담기게 줄바꿈만
이었다(마크다운 표 셀은 개행을 담지 못한다).

⒜ **「누출 주입 지점」 행** — 근거란 **끝에 이어 붙였다**. 기존 문장은 지우지
   않았다. 내용: K값 근사가 누출을 **배관 저항 변화로** 모사하는 **대용(proxy)**
   이고, 질량이 보존되는 모사라 실제 누출(질량손실)과 ⑴ 총유량·누출랙 통과유량의
   **부호가 정반대** ⑵ 잔여저항 배치(#24)에 따라 수력 흔적이 **전혀 남지 않는
   경우**가 있다 ⑶ 실제 누출의 **공급−환수 유량 불일치**가 K 근사에는 **원리적으로
   존재할 수 없다**는 것. **데이터셋의 누출 신호를 실제 누출의 방향으로 그대로
   읽지 않는다.** 세션 4 게이트 96건 전수 통과는 「K 근사 대용의 거동으로서」
   **여전히 유효하다**. 미해결 #36.

⒝ **새 행 「보충수 처리」** — 「누출 주입 지점」 **바로 다음 줄**에 넣었다.
   확정값은 「보충수를 모델링하지 않는다 — 조기감지 시나리오에 정의상 등장하지
   않는다」, 확정 시점은 **세션 5.7-C**. 근거: 미량 구간에서는 밀폐루프 보충
   로직이 작동하지 않으므로 보충수는 **정의상 등장하지 않는다** — 새 가정치가
   아니라 1장 목적과 5장 누출 크기에서 따라 나오는 **역산**이다(5-1 빈칸 처리
   순서 1번). 한계도 함께 실었다 — 계통 정압 강하를 누출 신호로 쓰려면
   팽창탱크 용적·초기 충전압이라는 **5장 밖 숫자 둘**이 필요하다.

#### diff 확인

`git diff --stat` = **1 file changed · 2 insertions · 1 deletion**. 바뀐 두 줄은
전부 5-1 표 안이다(125행 「누출 주입 지점」 · 126행 신규 「보충수 처리」).
**5장 표 본체 · 6장 · 7장 · 그 밖의 절 · `CLAUDE.md` · `collaboration.md` ·
`project-overview.md` 는 하나도 건드리지 않았다.**

#### 검사기

코드를 안 건드렸으므로 그대로 통과했다 — pytest **1,175건** · `ruff check .`
**All checks passed** · `mypy src tests` **24 files, no issues**.

#### 재업로드

**정본 문서를 고쳤다 — 프로젝트 지식 재업로드가 필요하다**(`CLAUDE.md`
「작업 시작 전 필독」 4). 대상: `AI서버_CDU_디지털트윈_프로젝트정리.md`.

#### 이 판이 정하지 않은 것

- 미해결 **#36 의 (가)/(나)/(다) 최종 선택** — 사람 몫이다. 5-1 문구 추가는
  **(가)의 실행**이지만 (나)·(다)를 닫지 않는다
- 세션 5.7-D · 5.8 의 내용

#### 다음 판

**5.7-D — 질량손실 열·전이 관측**(#37 확인 포함) → 5.8(이동지연 감도) → 종료 판정.

### 세션 5.7-D · 2026-08-31 · 질량손실 누출의 열·전이 관측 — 판정 기준 선기재

> **이 절은 코드보다 먼저 적고 먼저 커밋한다** — 세션 3-B 이후 원칙이며 세션 4
> (커밋 `803142e`) · 세션 5 (`acbc31c`) · 세션 5.5 (`cc790fa`) · 세션 5.6
> (`7285ef7`) · 세션 5.6-B (`9d210e3`) 와 같은 방식이다.
> **아래 기준을 결과에 맞춰 고치지 않는다.**

**절대 규칙 8 예외 — 사람 승인이 이 판까지 이어진다.** 세션 5.5-D 마무리 ⑶ 이
질량손실 모사를 **부호 확인 목적의 별도 코드에 한해** 허용했고, 세션 5.7-C 에서
사람이 정지선을 한 판 넓혀 이 판을 열었다. 조건은 그대로다 — **별도 코드 ·
데이터셋 미변경 · 5-1 의 누출 정의는 K 근사 그대로 · 규칙 8 문언 무수정.**
이 판의 코드는 데이터셋 생성 경로에 **들어가지 않는다.**

**5-1 「보충수 처리」(세션 5.7-C 확정)가 이 판의 전제다.** 보충수를 모델링하지
않는다 — 조기감지 시나리오에 정의상 등장하지 않는다. 세션 5.6 이 수력 한정을
택한 이유가 이것이었고, 그것이 닫혔으므로 열·전이를 볼 수 있다.

**게이트가 아니다.** 통과/실패가 아니라 **방향을 적는 것**이 산출물이다.

#### 열모델을 어떻게 세우는가 — 먼저 적어 둔다

세션 5.6 의 수력 위에 **2노드 온도 모델**(`dynamics.py` 구조)을 그대로 얹는다.
질량손실이 바꾸는 것은 **딱 한 군데**다.

    환수(hot) 노드 :  M_hot·cp·dT_ret/dt = ṁ_sup·cp·(T_sup − T_ret) + Q_rack
    공급(cold) 노드:  M_cold·cp·dT_sup/dt = ṁ_ret·cp·(T_ret − T_sup) − Q_hx

- **환수 노드는 그대로다.** 유입 ṁ_sup, 유출 ṁ_ret + ṁ_leak = ṁ_sup 이므로
  이 노드의 **질량은 정확히 보존된다.** 누출 유체가 T_ret 로 나가므로 유출
  엔탈피가 ṁ_sup·cp·T_ret 로 합쳐져 식의 형태가 밀폐루프와 같아진다.
- **공급 노드만 ṁ_ret 로 바뀐다.** 열교환기를 지나는 것은 환수유량이다.
  `Q_hx = ε·C_min·(T_ret − T_2차)` 의 `C_1차` 도 ṁ_ret·cp 다.
- 재고가 줄어드는 것은 **공급 노드**다 (dM_cold/dt = −ṁ_leak).

**절대온도가 식에 남지 않는다.** 변질량 형태 `d(M·T)/dt` 에서 출발해 M 을
얼리면 `T·dM/dt` 항이 유출항의 절대온도 항과 **정확히 상쇄**되어 위 식이 된다.
온도 영점에 의존하는 항이 남았다면 그것은 상수 M 근사를 잘못 적용한 것이다 —
**이 상쇄를 먼저 적어 두는 이유는 결과를 보고 식을 고르지 않기 위해서다.**

**정상상태 해**(dT/dt = 0)는 닫힌 형태다::

    ΔT = Q_rack / (ṁ_sup·cp)
    T_ret = T_2차 + ṁ_ret·cp·ΔT / (ε·C_min)
    T_sup = T_ret − ΔT

물성 평가온도의 고정점과 매 시점 quasi-steady 수력은 **기존 규약 그대로**다
(5-1 「cp·ρ 평가 온도」·「수력 계산의 물성 평가 온도」 · 절대 규칙 4).

#### 무엇을 대조하는가 — A~G

각 항목을 **K 근사와 질량손실에서 각각** 내고 부호를 대조한다. 전부 **같은
조합의 정상(누출 0) 해 대비 차이**다.

- **A. 누출랙 온도** — 콜드플레이트 출구온도의 변화 방향
- **B. CDU 환수온도 T_return** — 변화 방향
- **C. CDU 공급온도 T_supply** — 변화 방향
- **D. 에너지 balance** — 질량손실에서는 **누출 유체가 엔탈피를 들고 나간다.**
  잔차를 **두 벌** 낸다::

      leak 항 없이 : (Q_hx − Q_rack) / Q_rack × 100
      leak 항 포함 : (Q_hx + ṁ_leak·[h(T_ret) − h(T_sup)] − Q_rack) / Q_rack × 100

  `Q_hx` 는 ε-NTU 경로, `Q_rack` 은 5장 입력, 엔탈피 차는 **CoolProp 직접
  조회**다 — 세 경로가 다르므로 항등식이 아니다. **leak 항을 넣어야 닫히는지**가
  이 기준이 묻는 것이고, 넣고도 닫히지 않으면 **멈추고 보고한다**(§9).
  **6장 기준 ①의 판정이 아니다** — 별도 코드의 자기정합성 확인이다.
- **E. 시간응답** — τ·t63 이 M 에 정합하는가(τ ∝ M). 질량손실에서도 성립하는지
- **F. 다중 CDU** — 누출을 CDU A 에만 걸었을 때 **B 의 반응 방향**. 세션 5.5-D 가
  K 근사에서 관측한 것(B 의 2차측 몫 단조 증가 · 환수온도 단조 감소)과 대조한다
- **G. 배치 불변성** — A~F 중 **배치 6 에 흔들리지 않는 양이 있는가.** 특히
  **g=0/펌프=공급 배치**(세션 5.6 관측 ④ 에서 수력 다섯 양이 전부 0 이던 배치)
  에서 **열 신호가 살아 있는지** 반드시 본다

#### 가설 (예단하지 말 것)

질량손실은 누출랙 통과유량을 **늘리므로** ΔT_rack = q/(ṁcp) 가 줄어 랙 출구온도가
**내려갈** 수 있다. K 근사는 유량을 줄이므로 **올라간다** — 즉 A 의 부호도 정반대일
수 있다. B·C·F 는 유량과 온도가 반대로 움직여 예측이 갈린다.
**가설을 세우지 말고 결과를 그대로 적는다.**

#### 구조 자유도 — 세션 5.6 의 둘 + 이 판이 마주치는 하나

- **g** (잔여저항 중 환수측 몫) · **펌프가 보는 유량** — 세션 5.6 과 같다.
  배치 6 전수. `assumptions.py` 에 넣지 않는다.
- **[이 판에서 새로 마주친다] 공유 2차측 배분이 보는 「1차측 유량」** — 5-1
  「공유 2차측 결합 방식」이 배분을 "1차측 유량에 비례"로 정하는데, 누출 CDU 는
  ṁ_sup ≠ ṁ_ret 라 **어느 쪽인지가 처음으로 갈린다.** 열교환기를 지나는 것은
  환수유량이므로 `Q_ret` 가 물리적으로 정해진 읽기이지만, **값을 고르지 않고
  양 끝(Q_ret · Q_sup)을 둘 다 돌려** F 의 부호가 그것에 의존하는지 적는다.
  분기의 양 끝이므로 새 가정치가 아니다 — g 와 같은 취급이다.

#### 계통이 줄어드는 처리 — M 을 상수로 둔다

보충이 없으므로 계통 보유량 M 이 시간에 따라 줄어든다. **이 판은 M 을 상수로
두고 돌린다** — 감소를 넣으면 초기 보유량이라는 새 처리가 붙고, 이 판은 방향을
보는 판이다.

**대신 그 근사의 크기를 반드시 낸다.** 케이스마다::

    소실률[%] = Q_leak × 관측시간 × ρ / M × 100

를 표로 적는다. **작으면 상수 M 근사가 그 창 안에서 성립하고, 크면 결과 해석에
그만큼의 유보가 붙는다. 어느 쪽인지 미리 정하지 않는다** — 수치를 내고 나서
적는다. 관측 창 길이는 **기존 전이 설정을 그대로 쓴다**(새 숫자 0개).

#### 판정 기준 (부호 판정 외)

- **반례 확인**(collaboration.md ⑥). 부호가 어느 쪽으로 나오든 **배치 6 × 누출
  크기 × 부하 2수준 × 양정·분기 ΔP·밸브 ΔP 양 끝 전 조합**에서 뒤집히는 경우가
  있는지 같이 돌리고 적는다.
- **격리 확인.** 누출 크기 0 에서 열·전이 해가 기존 밀폐루프 해와 **부동소수점
  까지** 같아야 한다. 다르면 코드가 정상 경로를 건드린 것이다.
- **solver**(절대 규칙 5). `fsolve` `ier` · `solve_ivp` `success` 를 전 조합에서
  확인해 결과에 싣는다. 실패 조합은 **버리지 않고 표시한다.**

#### 이 판이 판정하지 않는 것 (과대해석 금지)

- **6장 기준 넷을 게이트로 판정하지 않는다.** D 의 balance 는 별도 코드의
  자기정합성이지 6장 ① 판정이 아니다.
- **어느 모사가 실제에 가까운지 재지 않는다** — 실측이 없다.
- **신호가 계측 가능한 크기인지 재지 않는다** — 계측기 사양이 없다.
- **M 을 상수로 뒀다** — 위 소실률 표가 그 유보의 크기다.
- 미해결 **#36 의 (가)/(나)/(다) 선택을 하지 않는다.** 사람 몫이다.

### 세션 5.7-D · 2026-08-31 · 질량손실 누출의 열·전이 관측 (관측 판 · 게이트 없음)

**관측 판이다. 게이트 없음 · feasibility 판정 없음 · 데이터셋 재생성 없음.**
판정 기준은 위 「세션 5.7-D … 판정 기준 선기재」에 **코드보다 먼저** 적고 먼저
커밋했다(`9bd246e`). **기준을 결과에 맞춰 고치지 않았다.**

**절대 규칙 8 예외**(사람 승인 · 세션 5.5-D ⑶ + 5.7-C 정지선 확장). 조건을 그대로
지켰다 — 별도 모듈 `leak_massloss_thermal.py` 는 **데이터셋 생성 경로에 들어가지
않고**(어느 모듈도 import 하지 않는다), 데이터셋·5-1·규칙 8 문언을 하나도 고치지
않았다. `model.py`·`plant.py`·`dynamics.py` 는 **읽기·호출만** 했다.

#### 규모와 실행시간 (C4 — 먼저 산출하고 돌렸다)

| 스윕 | 규모 | 측정 |
|---|---|---|
| 정상상태 | 64 케이스 × 배치 6 × 누출 5수준 = **1,920 해** | 약 0.8 분 |
| 전이 | 64 × 배치 6 × M 양 끝 = **768 적분** | 약 15 분 |
| 다중 CDU | 64 × 배치 6 × 누출 5 × 배분 읽기 2 = **3,840 결합 해** | 약 9 분 |

합계 약 25 분으로 **60분 정지선 안**이다. 64 케이스는 `model.default_cdu_cases`
(5장·5-1 양 끝 32조합) × 부하 양 끝 2수준이며 **축을 새로 고르지 않았다**.

#### A~G 결과 — 방향을 적는다 (판정이 아니다)

**A·B 누출랙 출구온도 · CDU 환수온도 — 부호가 정반대다**

| 양 | K 근사 | 질량손실 | 질량손실 범위 [K] |
|---|---|---|---|
| A 누출랙 출구온도 | **+** | **−** | −1.719e-01 ~ −1.886e-03 |
| B CDU 환수온도 | **+** | **−** | −1.719e-01 ~ −1.886e-03 |
| C CDU 공급온도 | **+/−** | **−** | −7.391e-02 ~ −1.389e-03 |

K 근사 192건 · 질량손실 1,536건에서 **하나도 뒤집히지 않았다**(반례 확인 —
누출 크기 × 부하 2수준 × 양정·분기 ΔP·밸브 ΔP 양 끝 × 배치 6 전 조합).

**가설이 맞았다**: 질량손실은 누출랙 통과유량을 늘려 ΔT_rack = q/(ṁcp) 를 줄이므로
랙 출구온도가 **내려가고**, K 근사는 유량을 줄이므로 **올라간다**.

**A 와 B 의 값이 정확히 같다.** 5-1 이 헤더를 저항 0 의 공통 노드로 두므로
질량손실이 **랙에 국소화되지 않고**(세션 5.6 관측), 그 결과 전 랙 출구온도가
환수 헤더 온도와 같아진다. **K 근사에는 없는 구조 차이다** — K 근사는 누출랙만
온도가 갈린다. **「랙별 온도로 누출 랙을 특정한다」는 규칙은 질량손실 쪽에
대응물이 없다.**

**C 는 K 근사에서만 갈린다.** 질량손실은 전 조합에서 −, K 근사는 조건에 따라
갈린다 — 세 온도 중 C 만 두 모사의 「부호가 정반대」라고 말할 수 없다.

**D 에너지 balance — 누출 엔탈피 항을 넣어야 닫힌다**

| | 최소 [%] | 최대 [%] | 최대 절대값 [%] |
|---|---|---|---|
| leak 항 **없이** | −1.212916 | −0.182636 | **1.212916** |
| leak 항 **포함** | −0.000062 | −0.000009 | **0.000062** |

`Q_hx`(ε-NTU) · `Q_rack`(5장 입력) · 엔탈피 차(CoolProp 직접 조회) **세 경로가
다르므로 항등식이 아니다.** 항을 빼면 닫히지 않고(최대 1.21%) 넣으면 닫힌다
(최대 0.000062%). **모사가 자기정합적이다.** 판정 기준이 요구한 「멈추고 보고」
조건에 해당하지 않는다.

**격리 확인**: 누출 0 에서 기존 밀폐루프 해와 최대 편차 **7.105e-15 K** —
정상 경로를 건드리지 않았다.

**※ 이것은 6장 기준 ① 판정이 아니다** — 별도 코드의 자기정합성 확인이다.

**E 시간응답 — τ ∝ M 이 정확히 성립한다**

| 양 | 최소 | 최대 |
|---|---|---|
| τ 비 (M 상한/M 하한) | 12.832779 | 12.832779 |
| t63 비 (M 상한/M 하한) | 12.832779 | 12.832779 |

M 비(1157.5805/90.2050 = **12.832779**)와 **소수점까지 일치**하고 768 적분 전부
같은 값이다. 세션 5.7 이 K 근사에서 8.000000배를 확인한 것과 같은 성질이
**질량손실에서도 성립한다.** T_return 순변화는 −0.171875 ~ −0.007523 K 로
전부 음수 — 정상상태 B 와 부호가 같다.

**F 다중 CDU — 배분 읽기가 부호를 가른다**

| 배분이 보는 1차측 유량 | B 몫 부호 | B 환수온도 부호 | B 몫 변화 [L/s] |
|---|---|---|---|
| `Q_ret` (HX 통과 · 정해진 읽기) | **+** | **−** | +1.142e-02 ~ +9.457e-02 |
| `Q_sup` (대안 읽기) | **+/−** | **+/−** | −7.586e-02 ~ +1.337e-04 |

**`Q_ret` 읽기에서는 세션 5.5-D 의 K 근사 관측과 방향이 같다** — 누출 없는 B 의
2차측 몫이 늘고 환수온도가 내려간다(5.5-D: +0.01908 L/s · −0.01064 K). 누출
크기에 대한 **엄격 단조 위반 0건**.

**`Q_sup` 읽기에서는 부호가 갈린다.** 5-1 「공유 2차측 결합 방식」이 배분을
「1차측 유량에 비례」로만 정해 두었는데, 누출 CDU 는 ṁ_sup ≠ ṁ_ret 라 **이 판에서
처음으로 그 문구가 갈렸다.** 새 미해결 **#39** 로 올린다.

**G 배치 불변성 — 열 신호는 배치 6 전부에서 살아 있다**

| 배치 | A | B | C | B 크기 범위 [K] |
|---|---|---|---|---|
| g=0.0/펌프=공급 | − | − | − | −7.368e-02 ~ −1.886e-03 |
| g=0.0/펌프=환수 | − | − | − | −8.675e-02 ~ −2.219e-03 |
| g=0.5/펌프=공급 | − | − | − | −1.159e-01 ~ −2.840e-03 |
| g=0.5/펌프=환수 | − | − | − | −1.287e-01 ~ −3.177e-03 |
| g=1.0/펌프=공급 | − | − | − | −1.591e-01 ~ −3.767e-03 |
| g=1.0/펌프=환수 | − | − | − | −1.719e-01 ~ −4.104e-03 |

**이 판의 가장 큰 소득이다.** 세션 5.6 관측 ④ 는 `g=0/펌프=공급` 배치에서
**수력 다섯 양이 전부 정확히 0** 이라 「잔여저항 배치에 따라 누출이 수력 흔적을
전혀 남기지 않는다」고 적었다. **열은 그 배치에서도 신호를 낸다** — 환수온도가
−7.368e-02 ~ −1.886e-03 K 로 움직인다.

**까닭**: 질량손실은 배치와 무관하게 **열교환기를 지나는 유량을 줄인다**
(ṁ_ret = ṁ_sup − ṁ_leak). 그것은 잔여저항이 어디 있든 성립하므로 열 신호는
구조 자유도에 걸리지 않는다. 세션 5.6-B 가 수력에서 찾아낸 Δ 와 **같은 성질**이다.

**크기는 배치에 걸린다**(g=1.0/펌프=환수 쪽이 g=0/펌프=공급 쪽의 약 2.3배).
**부호는 걸리지 않는다.**

#### 상수 M 근사의 크기 — 판정 기준이 요구한 표

| 창 | 최소 [%] | 최대 [%] |
|---|---|---|
| 전체 창(30τ) | 21.8876 | 36.3704 |
| **t63 까지** | **0.6154** | **1.8669** |

**둘을 나란히 읽어야 한다.** 전체 창에서는 계통의 **22~36%** 가 빠져나가므로
상수 M 근사가 그 구간에서 성립하지 않는다 — **30τ 파형의 뒷부분을 그대로 읽지
않는다.** 반면 신호가 서는 시간척도인 **t63 까지는 0.6~1.9%** 라 그 구간의
근사 오차는 작다. **위 A~G 의 부호는 t63 구간에서 이미 정해지므로 이 유보가
부호를 뒤집지 않는다.** 정량값(특히 30τ 종점값)에는 유보가 붙는다.
새 미해결 **#38** 로 올린다.

#### solver (절대 규칙 5)

정상상태 1,920 해 · 전이 768 적분 · 결합 3,840 해 **전부 성공**. `fsolve` `ier`
와 `solve_ivp` `success` 를 전 조합에서 확인했고 **실패 0건**이다.

#### 이 판이 판정하지 않은 것 (과대해석 금지)

- **6장 기준 넷을 게이트로 판정하지 않았다.** D 의 balance 는 별도 코드의
  자기정합성이지 6장 ① 판정이 아니다. T_return 방향성(6장 ②)도 이 판의 관측
  대상이 아니다 — 여기서 본 것은 **누출에 대한** 온도 반응이지 부하에 대한 것이
  아니다. 수렴시간(③)은 #21·#25·#23 때문에 여전히 판정 불가다.
- **어느 모사가 실제에 가까운지 재지 않았다** — 실측이 없다.
- **신호가 계측 가능한 크기인지 재지 않았다** — 계측기 사양이 없다. B 의 크기가
  1.9e-03 ~ 1.7e-01 K 라는 것은 적었지만 그것이 「검출된다」는 뜻이 아니다.
- **M 을 상수로 뒀다** — 위 소실률 표가 그 유보의 크기다.
- **미해결 #36 의 (가)/(나)/(다) 를 고르지 않았다.** 사람 몫이다.

#### 만든/바꾼 파일

- **신규**: `src/cdu_simul/leak_massloss_thermal.py` ·
  `tests/test_session57d_massloss_thermal.py`
- **바꿈**: `src/cdu_simul/leak_massloss.py` — `_SWEEP_FRACTIONS` 를
  `SWEEP_FRACTIONS` 로 **순수 rename**(본문·값 무수정). 새 모듈이 같은 스윕을
  쓰기 위해서다.
- **바꿈**: `PROCEED.md`
- **안 건드림**: 데이터셋(52열본) · `dataset_report` · `model.py` · `plant.py` ·
  `dynamics.py` · `assumptions.py` · 정본 문서 4종

#### 다음 판

**5.8 — 이동지연 감도**(N-CSTR 스윕). 그 뒤 종료 판정.

### 세션 5.8 · 2026-08-31 · 이동지연 감도 (N-CSTR 수치 수렴) — 판정 기준 선기재

> **이 절은 코드보다 먼저 적고 먼저 커밋한다** — 세션 3-B 이후 원칙이며 세션 4
> (`803142e`) · 5 (`acbc31c`) · 5.5 (`cc790fa`) · 5.6 (`7285ef7`) · 5.6-B
> (`9d210e3`) · 5.7-D (`9bd246e`) 와 같은 방식이다.
> **아래 기준을 결과에 맞춰 고치지 않는다.**

**관측 판이다. 게이트 없음 · feasibility 판정 없음 · 데이터셋 재생성 없음 ·
모델 무수정.** 세션 5.5-D ⑴ 이 이 판을 「바꾸지 않는다 · 관측만」으로 정했다.
스윕은 **별도 파일**에서 하고 현재 2노드 구조를 건드리지 않는다.

**파일럿의 마지막 판이다.** 이 판이 끝나면 남는 것은 사람의 종료 판정뿐이다.

#### N 을 무엇으로 읽는가 — 수치처리이지 물리 가정이 아니다

**사람이 정했다**: N 값 집합은 **수치 수렴 확인**이다. N 을 늘리다가 결과가 더
움직이지 않는 지점을 찾는 것이며 **격자 수렴 시험과 같은 성격**이다. 5-1 빈칸
처리 순서 **4번**(추가 파라미터 0의 규약)에 해당하고 **새 가정치를 만들지
않는다.** N 을 `assumptions.py` 에 넣지 않는다 — 세션 4 의 M 배분 민감도(#20),
세션 5.6 의 배치 자유도와 **같은 취급**이다.

**누출은 K 근사로 본다**(사람이 정했다). 이 판이 답하는 것은 「현재 데이터셋의
시간축을 믿을 수 있는가」이고 데이터셋은 K 근사 전용이다. 질량손실 정의(#36)는
이 판에서 보지 않는다.

#### 모델 — 현재 구조를 N=2 로 정확히 포함한다

현재 2노드는 **공급 다리 1탱크 · 환수 다리 1탱크**다. N 을 **총 노드 수**로
읽고 각 다리에 **N/2 탱크를 직렬**로 둔다(N 은 짝수). 5-1 「M 의 노드 배분」이
공급 50 · 환수 50 이므로 다리별 질량은 M/2 이고, **각 다리 안에서 균등 분배**
한다 — 불균등 분배는 5장에 없는 배분비를 새로 요구한다(추가 파라미터 0)::

    [공급 다리 N/2 탱크] → 랙(Q_rack 주입) → [환수 다리 N/2 탱크] → HX(Q_hx 제거) ↩

    공급 탱크 j : m_c·cp·dTc_j/dt = ṁ·cp·(Tc_{j-1} − Tc_j),  Tc_0 = T_ret − Q_hx/(ṁcp)
    환수 탱크 j : m_h·cp·dTh_j/dt = ṁ·cp·(Th_{j-1} − Th_j),  Th_0 = T_sup + Q_rack/(ṁcp)

    T_sup = Tc_마지막 · T_ret = Th_마지막 · m_c = m_h = (M/2)/(N/2)

랙과 열교환기는 **보유량 0 의 경계 열원**이다 — 5-1 이 콜드플레이트·HX 보유량을
M 에서 빼 두었으므로(#21) 그것이 현재 모델의 읽기이고, 이 판은 그 읽기를 바꾸지
않는다. 물성 평가온도와 매 시점 quasi-steady 수력은 **기존 규약 그대로**다
(5-1 · 절대 규칙 4).

**N=2 에서 위 식은 현재 `dynamics._derivative` 와 항등적으로 같다** — 대입하면
`ṁcp·(T_ret − T_sup) − Q_hx` 와 `ṁcp·(T_sup − T_ret) + Q_rack` 이 그대로 나온다.
그래서 N=2 는 **대조 기준**이지 새 계산이 아니다.

#### N 값 집합

**N ∈ {2, 4, 8, 16, 32, 64}** — 2 를 반드시 포함하고(현재 구조) 배로 늘린다.
현재 구조의 **32배**까지 뻗으므로 기준 C 의 수렴 판정이 성립할 여지가 있다.
**수렴하지 않으면 「스윕 범위 안에서 수렴하지 않았다」를 그대로 적는다.**

#### 판정 기준 — 게이트가 아니다

- **A. 정상상태 불변.** N 을 바꿔도 정상상태 해가 변하지 않아야 한다. 노드 분할은
  보유량을 나누는 것이지 정상상태 방정식을 바꾸지 않는다(dT/dt=0 이면 같은 다리
  안의 탱크가 전부 같은 온도가 되므로 N 이 사라진다). **변하면 그 자리에서 멈추고
  보고한다** — 스윕 구현에 결함이라는 뜻이다.
- **B. 전이 지표의 이동.** N 별로 **τ · t63 · t95** 를 내고 **N=2 대비 변화율(%)**
  을 적는다.
- **C. 수렴.** N 을 늘릴 때 B 의 지표가 **더 움직이지 않는 지점이 있는가.** 있으면
  그 N 과 **그 지점에서 N=2 대비 차이**를 적는다. 없으면 **수렴하지 않았다고 적는다.**
- **D. 누출 신호의 부호.** 누출 3수준의 신호(총유량 · 펌프 양정 · 누출랙 온도 ·
  환수온도)의 **부호가 N 에 따라 뒤집히는가.** 뒤집히지 않으면 세션 4 게이트가 N 에
  강인하다는 뜻이다. **뒤집히면 그것이 이 판의 가장 큰 발견이므로 그대로 적는다.**
- **E. M 정합성.** **τ ∝ M** 이 N 을 바꿔도 성립하는가 — 6장 재정의 기준(세션 2.5)이
  노드 수에 흔들리는지 본다.

#### 가설 (예단하지 말 것)

이동지연은 실제 배관에서 **플러그 흐름**에 가깝고 CSTR 1개는 그것을 완전혼합으로
근사하므로, N 이 늘면 응답이 **더 지연되고 파형이 가팔라질** 수 있다.
**가설이다. 결과를 여기에 맞추지 않는다.**

#### 규모 — 측정 후 확정하고 줄인 것을 적는다

**전 조합은 32(5장·5-1 양 끝) × 부하 2 × M 2 × 누출 3 = 384 적분/N** 이고 N 6수준
이면 2,304 적분이다. 현재 2노드 1건이 **약 1.18 s**(실측)이고 N 이 늘면 ODE 차원과
스텝 수가 함께 늘므로 **60분 정지선을 넘는다.** 그래서 **2단으로 나눈다**:

1. **수렴 스윕** — N 6수준 전부. 케이스는 **5장 범위를 벌리는 축만** 남긴다.
2. **반례 확인** — N 을 **2 와 수렴 N 둘**로 고정하고 **전 조합 384** 를 돌려
   C·D 의 결론이 조합에 걸리는지 본다.

**정확한 규모와 실측 시간은 코드를 돌리기 전에 산출해 적고, 줄였다면 무엇을 왜
줄였는지 함께 적는다**(§6). 60분을 넘으면 멈추고 보고한다.

#### solver (절대 규칙 5)

`fsolve` `ier` 와 `solve_ivp` `success` 를 전 조합에서 확인해 결과에 싣는다.
실패는 **버리지 않고 표시한다**. **수치해 수렴(기준 C)과 solver 수렴은 다른
것이며 보고에서 헷갈리게 적지 않는다.**

#### 이 판이 판정하지 않는 것 (과대해석 금지)

- **6장 기준 넷을 게이트로 판정하지 않는다.**
- **N 수렴은 「이동지연이 실제와 맞다」는 뜻이 아니다** — 실측이 없다. 수치
  이산화가 결과를 지배하지 않는다는 뜻**까지**다.
- **수렴시간의 절대값을 해석하지 않는다** — #21·#25·#23 이 열려 있다.
- 미해결 **#36 의 (가)/(나)/(다) · #38 · #39 를 고르지 않는다.**

## 미해결 목록

> 세션 1-B 시작 시 사람이 정한 것을 반영했다(㉮~㉳). 닫힌 항목도 지우지 않고
> 상태를 표시해 남긴다 — 다음 세션이 이력을 잃지 않게.

| # | 한 줄 | 크기 | 영향 |
|---|---|---|---|
| 1 | **[해소 · 세션 2.5]** 5-1 에 확정 기록됨 — 밸브 Cv(개도 100% 기준, ΔP 하한 3 mAq → Cv 18.72 / 상한 5 mAq → Cv 14.50) · 배관 K값(랙 분기 3.20~4.80). 둘 다 5장 조건의 **역산**이므로 새 가정치가 아니다. 5-1 「밸브 Cv」 항목의 관측(25A 에 1.94 L/s 는 유속 3.48 m/s 로 통상 설계유속보다 높다)을 함께 읽는다. 원문: 밸브 Cv · 배관 K값이 5장 표에 없다 — "ΔP 조건에서 역산"이라고만 적혀 있어 역산 규칙(어느 ΔP·어느 유량 기준)을 사람이 정해야 한다. 1-B는 단일 랙이라 유량을 풀 필요가 없어 5장 정격유량을 그대로 썼다 | M | 세션 3 압력평형(`fsolve`)·유량분배를 못 세운다 — **세션 3 착수 전 필요** |
| 2 | **[해소 · 세션 2.5]** 5-1 에 확정 기록됨 — 양정 하한 20 mAq → H₀=22.40·a=0.003819·b=0.009743 / 상한 30 mAq → H₀=33.60·a=0.005729·b=0.014615 (mAq, Q 는 L/s). 곡선 **형상만** 타 사례에서 차용하고 정격점은 5장 값으로 스케일했다 — 5-1 의 한계(물 기준 곡선·용량 4.4배 차이로 비속도 다름)를 함께 읽는다. 원문: 펌프 특성곡선 계수 H0·a·b 가 5장 표에 실제로 없는 빈칸이다 (형태 `H=H0-aQ-bQ²` 만 있음) — 세션 3 프롬프트 전에 사람이 채운다 | M | 세션 3 펌프 모델을 못 세운다 — **세션 3 착수 전 필요** |
| 3 | **[확정 · 방침 (B)]** 범위값의 대표값을 고르지 않는다. **양 끝을 둘 다 돌린다.** 이유: balance 는 보존법칙이므로 범위 안 어느 값에서도 성립해야 한다. 한쪽 끝에서만 닫히면 값 선택 문제가 아니라 모델 문제다. 1-B에서 2차측 27/30℃ × NTU 2/3 = 4케이스로 적용했다 | M | 해소됨 — 이후 세션도 같은 방침을 따른다 |
| 4 | 5장 표의 "CDU 정격 ~750 kW"가 근사 표기 — 열교환기 정격 750 kW 와 같은 값인지 별개 값인지 문서가 명시하지 않음 | S | 정격 대비 부하율 계산 기준이 갈릴 수 있음 |
| 5 | 5장 표는 의도된 자기정합으로 보인다(랙당 1.94 L/s × 8 ≈ 펌프 15.5 L/s) — 1-B에서 계산 ΔT가 10℃ 부근으로 재현되는지로 확인 | S | 문서 확인만 필요, 코드 영향 없음. **[닫힘 · 세션 5.5-C]** 근거: 세션 5-B 재계산에서 ΔT = **10.3008 K**, 1차측 **32.17/42.47℃** 가 5장 표의 32/42℃ 와 정합한다 — 표의 자기정합이 확인됐다 |
| 6 | **[관측 유지 · 다른 PC clone 시 확인]** 한글 파일명이 git 기본 출력에서 8진 이스케이프로 표기됨. 저장된 파일명 바이트는 정상 UTF-8임을 확인했다(표시 문제). 세션 1-C에서도 **이름·`.gitattributes`·git 설정 어느 것도 건드리지 않았다** — `core.quotepath` 는 개인 설정이라 저장소에 넣지 않는다 | M | 다른 PC·다른 로케일에서 clone 할 때 실제로 문제가 되는지 확인한다. 지금 이 PC의 동작에는 영향 없음. **[닫힘 · 세션 5.5-C]** 근거: **새 PC clone 에서 확인됐다.** 파일명은 UTF-8 NFC 로 정상 저장돼 있고 내용도 정상이다 — 8진 이스케이프는 `core.quotepath` 기본값에 따른 **표시 문제**일 뿐이다. `core.quotepath false` 가 표시를 고치는 것은 시험으로 확인했으나 **개인 설정이라 저장소에 넣지 않는다** |
| 7 | **[세션 3에서 재검토]** 물성 조회 기준 압력을 표준대기압(101325 Pa)으로 두었다 — 계통 압력이 실제로 등장하는 세션 3에서 다시 본다. 지금 닫지 않는다 | S | 비압축성 유체라 밀도·비열 영향은 작을 것으로 보이나 **확인하지 않았다**. **[닫힘 · 세션 5]** 재검토 기한을 「세션 3」으로 적었으나 3-A·3-A2·3-B 어디서도 다루지 않고 지나간 것을 확인하고, 세션 5에서 코드로 확인했다: 계통 압력은 대기압 대비 **193.6~290.4%**(짐작한 2~3% 가 아니다)이지만 CoolProp INCOMP 백엔드에서 **ρ·cp 는 압력에 정확히 무관**(0.0000%)하고 엔탈피 차만 −0.0138% 이동한다. 5-1 에 확정으로 기록했다 |
| 8 | **[해소 · 세션 2.5]** 파일럿 종료 조건 확정 — **사전 수치 기준을 두지 않는다.** 판정 시점은 **세션 5.5**(시나리오 배치 실행 → 텍스트 데이터셋) 완료 후, 판정 대상은 그 데이터셋과 세션 1~5.5 게이트 기록, 판정 주체는 **사람**이다. 설계데이터가 오지 않는 것을 전제하므로 "데이터 확보량" 형태의 기준은 성립하지 않는다 | L | 해소됨. 세션 6 은 설계데이터가 있어야 열리며 **파일럿의 정상 종착지가 아니다** |
| 9 | **[해소]** `.gitattributes` 추가(`* text=auto` · `*.py text eol=lf`). 확인 결과 저장소·작업트리 모두 **이미 LF** 였고(`git ls-files --eol` → `i/lf w/lf`) 파일 내용은 하나도 바뀌지 않았다 — 경고는 앞으로의 체크아웃 방향에 대한 것이었다 | S | 해소됨. 앞으로 다른 PC에서도 LF 로 고정된다 |
| 10 | **[해소 · 세션 2.5 확인만]** `CLAUDE.md` 절대 규칙 12·「명령어」·「코드 스타일」이 모두 `Python 3.12+` 로 맞춰졌고 `pyproject.toml` 의 `requires-python` 도 `">=3.12"` 다 — 코드와 문서가 정합하다. **3.11·3.13 에서는 여전히 안 돌려봤다**(넓히려면 그 버전에서 전체 테스트를 먼저 돌린다). `[tool.ruff] target-version = py311` 은 더 보수적인 쪽이라 그대로 뒀다. 원문:  세션 2에서 `pyproject.toml` 의 `requires-python` 을 `">=3.12"` 로 좁혔다(검증한 것이 3.12.10 하나뿐). **그러나 `CLAUDE.md` 절대 규칙 12는 아직 "Python 3.11+" 라고 적고 있어 코드와 문서가 어긋나 있다** — 문서 수정 권한이 없어 손대지 않았다. `[tool.ruff] target-version` 도 `py311` 로 남아 있다(더 보수적인 쪽이라 그대로 뒀다) | M | 사람이 `CLAUDE.md` 규칙 12를 맞춰야 한다. 3.11·3.13 에서는 **여전히 안 돌려봤다** |
| 11 | **[사람 소관 · 상시 항목]** 커밋 작성자 `git config user.name`·`user.email`. **작업 PC 가 둘이며 PC 마다 값이 다를 수 있다** — 한 PC 의 값만 적어 두면 다른 PC 에서 틀린다(원 문구가 그랬다). 새 PC 는 사람이 직접 지정했고 지금은 사람 이름·메일로 커밋된다. **코드·세션이 `git config` 를 스스로 설정하지 않는다.** | S | 이력 작성자 표기. **닫지 않는다** — **PC 를 새로 붙일 때마다 확인하는 상시 항목**이다 |
| 17 | **[해소]** `cdu-docs` 폴더 폐기됨. 정본은 저장소 루트 사본 하나뿐이고, 세션 2부터 저장소 밖에서 문서를 가져오지 않는다 | M | 해소됨 |
| 18 | `fluid.py` 의 `REFERENCE_PRESSURE_Pa` 주석이 아직 5-1 을 인용하지 않는다. **[인용 문구 정정 · 세션 5.5-C]** 원 문구는 5-1 을 "표준대기압 101325 Pa · **잠정** · 세션 3 재검토"로 인용했는데, **세션 5 에서 확정으로 바뀌었다**(#7 닫힘 근거: INCOMP 백엔드에서 ρ·cp 가 압력에 정확히 무관, 엔탈피 차만 −0.0138% 이동). 즉 5-1 은 이제 잠정이 아니라 **확정 기록**이다. 세션 1-C 지시 범위가 #12·#14 였고 세션 5.5-C 는 모델 코드 수정 범위 밖이라 **주석은 여전히 건드리지 않았다**(절대 규칙 13) | S | 같은 값의 출처가 두 군데에서 다르게 읽힐 소지. **여전히 열림** — 주석이 5-1(확정)을 인용하도록 고치는 것은 다음 위생 판의 몫이다 |
| 19 | **[문구 정정 · 세션 5.5-C — 코드로 확인했다]** 원 문구는 `model.py` 의 `heat_capacity_ratio`**(Cr=1)** 에 5-1 인용 주석이 없다는 것이었다. **세션 5-B 가 Cr=1 선언(필드·가정)을 전부 제거했으므로 그 형태는 더 이상 없다.** 다만 **이름 자체는 남아 있다** — `hx_effectiveness_counterflow(ntu, heat_capacity_ratio)` 의 인자이고, 값은 `hx_capacity_terms()` 가 매번 양측 물성에서 **유도**한다(선언하지 않는다). `Cr == 1.0` 분기는 일반식의 0/0 특이점 가드로만 남아 있고 주석이 그 취지를 적고 있다. 출처 표기 상태: 대향류 ε 식은 5-1 을 인용하고 있고, Cr 유도 규약은 「세션 5-B」로 인용한다 — **5-1 의 어느 행을 인용하는지가 아직 적혀 있지 않다** | S | **닫지 않는다.** 남은 일은 Cr 유도 규약의 인용을 「세션 5-B」에서 **5-1 의 해당 행**으로 바꾸는 것 하나다. 수치 영향 없음 |
| 20 | **[해소 · 세션 2.5]** 5-1 에 **공급 노드 50% · 환수 노드 50%** 로 확정 기록됨. 왕복을 두 다리로 읽으면 50:50 이 추가 파라미터가 0인 유일한 배분이다 — **물리 가정이 아니라 수치처리 규약**이다 | M | 해소됨. 정상상태 해에는 영향 없고 전이 파형에만 걸린다. **[민감도 확인 완료 · 세션 4]** 시험값 40:60 · 50:50 · 60:40 을 돌렸다. t→∞ 값은 배분비와 무관(편차 <2e-9 K — 5-1 진술이 성립). 전이 파형은 움직인다: t63 이 0.57~0.77 s(M 하한)·7.27~9.83 s(M 상한) 로 **±15% 이동**하고, 50:50 대비 T_return 최대 편차는 2.5e-4 ~ 2.2e-3 K(누출 수준에 비례). **누출 신호의 방향은 세 배분비 전부에서 뒤집히지 않았다** — 세션 4 게이트가 배분비 규약에 의존하지 않는다. 시험값은 5-1·`assumptions.py` 에 넣지 않았고 시험 코드 안에만 있다. **50:50 유지 여부는 사람이 판정한다** |
| 21 | **M 에 열교환기·CDU 내부·랙 콜드플레이트 보유량이 빠져 있다**(5장에 없음, 5-1 한계 기록). 실제보다 M 이 작고 수렴시간도 짧게 나온다 | M | **6장 「수렴시간」 기준을 이 상태로 판정할 수 없다.** 세션 2에서 판정하지 않았고, 설계데이터나 추가 가정치 없이는 이후에도 판정 불가. **세션 2.5 에서 6장 기준 자체가 재정의되었다** — 절대 수렴시간의 "합리성"은 판정하지 않고 τ 가 M 에 비례하고 유량에 반비례하는 **정합성**만 본다. 항목은 여전히 열려 있다(M 의 결손 자체는 그대로다) |
| 22 | `dynamics.py` 가 `model._property_temperature_from_state`(비공개 이름)를 import 한다. cp 평가 규칙을 두 곳에 적지 않으려는 것이고, 세션 2는 `model.py` 수정 금지라 공개화하지 않았다 | S | 위생 항목. 다음 정비 판에서 공개 이름으로 승격하면 된다 |
| 23 | 배관 규격 계열에 따라 M 이 이동한다 — 5-1 기록: KS D 3507 계열(25A 내경 27.5 mm)을 쓰면 약 7% 커진다. **ASME Sch40 으로 계산했고 다른 계열로는 안 돌려봤다** | S | 시간상수가 그만큼 이동. 설계데이터 확보 시 확정 |
| 12 | **[확정 · 대향류]** 프로젝트정리 5-1 에 기록됨. 근거가 "판형 HX 통례"에서 **"5장 온도 역산"**(대향류·NTU 2·2차측 27℃ → 32.15/42.44℃ 가 5장 32/42℃ 와 일치)으로 바뀌었다. 세션 1-C에서 `model.py` 주석·출처만 교체했고 **ε 식·값은 건드리지 않았다** | M | 해소됨 — 계산은 처음부터 대향류였으므로 수치 변화 없음(전후 동일성으로 확인) |
| 13 | **[세션 5로 이월]** 2차측 유체 미정. 잠정적으로 1차측과 동일 냉각액 + 유량비 1:1 → Cr = 1 (프로젝트정리 5-1 에 "미정"으로 기록됨). 2차측이 블랙박스인 동안은 판정 불가(절대 규칙 7) | M | Cr 이 1이 아니면 ε 값이 바뀐다. **[닫힘 · 세션 5]** 5-1 「2차측 유체」를 **1차측과 동일한 PG25** 로 확정했다 — 동일 유체면 유량비 1:1 에서 앞 게이트가 전부 불변이고 추가 파라미터가 0이다. **단 Cr=1 은 유량에서 유도되지 않는다**(미해결 #33 참조) |
| 14 | **[확정 · bulk_mean]** 프로젝트정리 5-1 에 **수치처리 규약**으로 기록됨. `cp_rule` 인자는 재현·전환용으로 그대로 남겼다. 규칙별 잔차(bulk_mean 0.005% 통과 · supply 0.309% 실패 · return 0.315% 실패)도 5-1 에 기록됐다 | M | 해소됨 — 다만 규칙을 바꾸면 1-B 판정이 뒤집힌다는 사실은 계속 유효하다 |
| 15 | **[여전히 열림]** 랙·HX 를 **독립적으로 푼 뒤 대조**하는 검증이 아직 없다. 세션 2의 정상상태 대조(t→∞ vs 1-B)는 계산 *경로*는 다르지만 동적 모델의 평형점이 설계상 1-B 와 같은 방정식이라 **구현 일관성 검사**에 그친다 | M | "1-B·세션 2 통과"를 모델 전체의 보존성 검증으로 읽으면 과대해석이다 |
| 16 | **[해소]** 갱신본 `CLAUDE.md` 「명령어」 절 127행에 `.venv/Scripts/python.exe -m cdu_simul.model` 이 들어갔음을 확인했다 | S | 해소됨 |
| 24 | **계통 잔여저항 배정이 유량분배를 지배한다.** 5-1 규칙대로 배정하면 잔여저항(HX 1차측 + CDU 내부배관 + 헤더)이 **총양정의 60~83%** 를 차지한다. 세션 3 결과를 해석할 때 이 사실을 함께 적는다 — 수렴·비발산 게이트 통과를 "유량분배 값이 타당하다"로 읽지 않는다. 잔여저항이 랙 공통 경로에 있으므로 정상상태 랙 간 유량은 균등해지고, 랙 간 상호작용은 **펌프곡선을 통해서만** 생긴다. **[번호 주의]** 세션 2.5 프롬프트는 이 항목을 `#23` 으로 지정했으나 `#23` 은 세션 2에서 이미 쓰였다 — 중복을 만들지 않으려고 `#24` 로 넣었다 | L | **세션 3 게이트 해석의 전제.** 잔여저항 배정이 바뀌면 유량분배 결과가 통째로 바뀐다 |
| 25 | **등가길이 기반 M 산출은 과대 방향 오차를 갖는다.** 등가길이는 부속(엘보·밸브 등) 손실을 직관 길이로 환산한 **압력강하용 개념**이라 실제 배관 길이보다 길다 — 그 길이로 보유수량을 내면 M 이 실제보다 크게 나온다. 현재 5-1 「계통 보유수량 M」 항목에는 **과소 방향 오차(HX·CDU 내부·콜드플레이트 누락)만** 적혀 있다. 부호가 반대인 오차가 하나 더 있다는 문구 보완 후보 | S | 두 오차의 방향이 반대라 상쇄 정도를 모른다 — #21(수렴시간 판정 불가)의 근거를 더 강하게 만들 뿐 판정을 가능하게 하지는 않는다. **문서 문구 보완 후보이며 이 판에서는 고치지 않았다**(지시 범위 밖) |
| 26 | **세션 5.5 신설 — 시나리오 배치 실행 · 데이터셋화.** 정상/이상/누출 다수 케이스를 반복 실행해 텍스트 데이터셋을 낸다. 게이트: **시나리오 간 상태 이월 없음**(collaboration.md 결함유형 ④) · 전 행에 가정치 출처와 "가정값 기반 — 실측 아님" 표기. **게이트 표 반영 완료**(`CLAUDE.md` · `project-overview.md` 체크리스트) | L | **파일럿 종료 판단의 대상**이 되는 산출물이다(#8). **[닫힘 · 세션 5.5-C]** 근거: 세션 5.5-A(설계·계수·실행시간 측정)·5.5-B(배치 실행 → 데이터셋 생성 · 게이트 판정)로 **이행이 완료**됐다 |
| 27 | **SG 가 5-1 기록(1.0124)과 CoolProp 값(1.01147)에서 0.09% 다르다.** 5-1 의 밸브 Kv·배관 K 는 5-1 의 SG 로 역산된 값이므로, `hydraulics.py` 가 정격점에서 재현하는 ΔP 가 5장 표값과 0.1% 이내로 어긋난다(분기 1.995 vs 2.000 mAq · 밸브 2.993 vs 3.000 mAq). 세션 3-A 는 **어느 쪽도 고치지 않았다** — 5-1 값 전사가 규칙이다 | S | 유량분배 결과에 0.1% 미만으로 전파된다(운전점 편차 +0.019~+0.046% 안에 이미 섞여 있다). 5-1 의 SG 출처를 사람이 확인하거나, CoolProp 값으로 Kv·K 를 재역산할지 정해야 한다 — **코드가 임의로 정하지 않는다**(절대 규칙 1). **[닫힘 · 세션 3-A2]** 사람이 5-1 을 값에서 **역산 규칙**으로 바꿨고, SG 정의를 `fluid.coolant_specific_gravity` 한 곳으로 모아 Kv·K 를 코드가 재역산하게 했다. 그 결과 정격점 ΔP 가 5장 표값(2/3 · 3/5 mAq)을 **잔차 1e-15 mAq 로 항등 재현**한다. SG 기준밀도 1000 kg/m³ 는 `[정의값]` 등급(5-1 빈칸 처리 순서 2번)이다 |
| 28 | **수력의 물성 온도가 아직 5장 고정값(37℃ 벌크평균)이다.** `hydraulics.property_temperature_C()` 는 5장 1차측 공급·환수 온도를 고정으로 읽는다. 열모델이 붙으면 이 온도가 풀린 상태에서 나와야 하고, 밀도가 유량에 의존하므로 **수력-열 연립**이 된다 | M | **세션 3-B 의 첫 결정 사항.** 결합 방식(연립 대 순차 반복)을 정하기 전에는 3-A 의 유량값을 열모델에 그대로 넘기면 안 된다. **[닫힘 · 세션 3-A2]** 고정 37℃ 를 제거했다 — `valve_dp_mAq`·`branch_dp_mAq`·`solve_flow_distribution` 이 물성 온도를 **기본값 없는 필수 인자**로 받는다. 5-1 에 「수력 계산의 물성 평가 온도 = 1차측 벌크 평균온도」 행을 추가해 규약을 명문화했다. **연립 대 순차 반복의 선택은 여전히 3-B 의 것이다** — 이 항목이 닫힌 것은 "온도가 인자로 들어올 자리가 생겼다"까지이고, 어떤 온도를 넣을지는 3-B 가 정한다 |
| 29 | **세션 3 게이트의 절반만 봤다.** 3-A 는 `fsolve` 수렴(8조합 `ier=1`)만 확인했고 **극단 케이스(부하 0/최대) 비발산은 안 봤다** — 부하가 온도를 통해 들어오므로 열모델이 필요하다. 부하 0 은 밸브 개도 0 이 아니라 발열량 0 이다(개도 0 은 Kv=0 이라 ΔP 정의 불가이며 `valve_dp_mAq` 가 `ValueError` 를 던진다) | M | **세션 3 게이트를 "통과"로 적지 않는다.** 3-B 가 나머지 절반을 판정해야 세션 3이 닫힌다. **[닫힘 · 세션 3-B]** 열결합 상태에서 **32조합 전수**로 둘 다 판정했다 — ㉠ 수력·열·결합 고정점 세 `fsolve` 가 전부 `ier=1`, ㉡ 부하 0%/100% 에서 정상상태·동적 모두 비발산. 부하 0 은 6장이 발산 검사용으로 명시한 극단값이라 5장 위반이 아니다 |
| 30 | **잔여저항만 온도 스케일을 따르지 않는다.** 분기 ΔP(K·ρv²/2)와 밸브 ΔP(SG·(Q/Kv)²)는 밀도에 비례해 움직이는데, 계통 잔여저항은 5-1 이 mAq/(L/s)² 계수로 정의하므로 정격 물성에서 역산한 뒤 고정된다. 정격 온도(37℃) 밖에서는 세 저항의 스케일이 어긋난다 | M | **세션 3-A2 는 고치지 않았다** — 5-1 의 잔여저항 규칙을 이 판에서 바꾸지 않기로 했기 때문이다(지시 범위). 열모델이 붙어 온도가 실제로 움직이는 **세션 3-B 에서 사람이 정한다**: 잔여저항도 K형 저항으로 보아 ρ 스케일을 태울지, 5-1 정의대로 고정할지. 정격점 근방에서는 영향이 작지만 극단 케이스에서 커질 수 있다. **[닫힘 · 세션 3-B]** 사람이 5-1 에 「계통 잔여저항의 물성 의존 = ρ 에 비례」를 확정했고, 코드가 정격 물성 기준 계수를 ρ(T)/ρ(37℃) 로 스케일한다. 세 저항이 같은 온도 스케일을 따르며 **추가 파라미터는 0**이다. 정격 온도에서 비가 1 이므로 8조합 표가 세션 3-A2 와 바이트 단위로 동일함을 격리 확인했다 |
| 31 | **M 의 8랙 해석이 없다.** 5-1 이 등가길이를 「랙 1개 회로의 왕복 전체」로 읽으므로 `holdup_bounds()` 가 내는 M 은 **랙 1개 회로분**이다. 랙이 8개가 되면 계통 전체 보유량을 어떻게 읽어야 하는지(분기는 8벌·헤더는 공용) 5장에도 5-1 에도 답이 없다 — 세션 3-B 는 **새 숫자를 만들지 않고 세션 2 값을 그대로 썼다**(절대 규칙 1) | M | **τ·t63·t95 의 절대값을 8랙에서 해석할 수 없다.** 결과 표에 그 문구를 넣었다. 게이트에는 영향이 없다 — 정상상태는 M 에 의존하지 않고, 전이의 부호도 M>0 이면 바뀌지 않는다(방향성·비발산 둘 다 M 무관). #21(M 에 HX·CDU 내부·콜드플레이트 누락)과 **별개의 결손**이며, 둘 다 열려 있는 한 6장 「수렴시간」은 판정 불가다. 사람이 배분 규칙을 정해야 한다. **[닫힘 · 세션 5.7]** 사람이 배분 규칙을 정했다 — 5-1 M 의 양 끝(전부 25A 하한 · 전부 80A 상한)을 **둘 다 8배**로 읽는다(추가 숫자 0 · 8 은 `SCENARIO.racks_per_cdu`). `holdup_bounds()` 가 이제 **8랙 계통 전체** M 을 낸다: 하한 11.2756 → **90.2050 kg** · 상한 144.6976 → **1157.5805 kg** (배수 정확히 8.0000000000). **정상상태 해는 불변**(`model`·`hydraulics` 표 바이트 단위 동일 · 전이 종점 온도 30.098/42.469 ℃ 그대로) — 5-1 「M 은 정상상태 해에 영향하지 않는다」가 코드에서 성립했다. **τ·t63·t95 가 정확히 8.000000배 이동**했다(부하 스텝 4 · 누출 스텝 6 전부). 정확히 8배인 까닭도 안다 — 적분 격자가 τ 단위이고 2노드 ODE 가 M 에 선형이라 궤적 전체가 스케일된다. **그러나 6장 「수렴시간」은 여전히 판정 불가다** — #21(M 결손) · #25(등가길이 과대) · #23(배관 규격 계열)이 그대로 열려 있고 #21 과 #25 는 방향이 반대라 상쇄 정도를 모른다. **이 항목이 닫힌 것은 「8랙 계통의 M 을 어떻게 읽는가」 하나이고, M 의 정확도가 좋아진 것이 아니다** |
| 32 | **밸브 선형 개도 특성이 아직 시험되지 않았다.** 5-1 「밸브 개도 특성」이 선형을 규약으로 확정하면서 "개도가 변수가 되는 시점(세션 4)에 재검토"라고 적었으나, 세션 4의 누출은 **K값 변화**이고 개도는 80% 고정이었다 — 재검토할 자극이 오지 않았다 | S | 선형 대 등비율의 차이는 개도가 실제로 움직일 때만 드러난다. 개도 제어가 들어오는 판(현재 계획에 없다)이나 설계데이터 확보 시 재검토한다. **지금 판정할 근거가 없다** |
| 33 | **5-1 두 행이 서로 어긋난다 — Cr=1 인가, 배분에서 유도하는가.** 5-1 「2차측 유체」는 "유량비 1:1과 합쳐 **Cr=1**" 이라고 선언하고, 5-1 「공유 2차측 결합 방식」은 총 2차측 유량을 정격으로 고정하고 1차측 유량에 비례 배분하라고 한다. 배분에서 Cr 을 유도하면 **어떤 배분 규칙으로도 정확히 1 이 나오지 않는다**: ① 총 2차측이 정격 15.5×2 로 고정인데 1차측 운전점은 15.5056 이라 2차:1차=0.99964 ② 설령 부피유량이 같아도 양쪽 온도가 달라 ρ·cp 가 다르므로 Cr=0.99863 (5장 「유량비 1:1」은 부피 기준이다) | L | **세션 5 는 배분에서 유도하는 쪽을 구현했다** — 그래야 세션 5 게이트(「CDU 간 연동 수렴」)가 공허하지 않기 때문이다. Cr=1 을 고수하면 배분이 열해에 영향하지 않아 **연동 자체가 사라진다**. 그 결과 대칭 케이스가 세션 4 단일 CDU 결과를 재현하지 못한다(32조합 최대 편차 **0.0245 K** — 세션 5 C4 의 기대와 다르다). **사람이 정해야 한다**: (가) 배분 유도를 유지하고 5-1 「2차측 유체」의 Cr=1 문구를 고치는가, (나) Cr=1 을 고수하고 연동 경로를 다른 것(예: 2차측 온도 결합)으로 바꾸는가. (나)는 냉각탑 파라미터가 필요해 5장에 근거가 없다. **[닫힘 · 세션 5-B]** 사람이 (가)를 골랐다 — Cr 을 선언하지 않고 물성에서 유도하고, 5장 「1:1」을 **부피유량비**로 읽어 2차측을 정격 15.5 L/s 에 고정하며, 이를 **단일 CDU 에도 소급**한다. 그 결과 대칭 재현 편차가 0.0245 K → **0.000e+00 K** 가 됐고, 세션 1-B~5 다섯 게이트를 새 전제 위에서 다시 판정해 전부 통과했다. **대가: 세션 1-B~5 의 기록 수치가 소급 이동했다** — 세션 5-B 이전 로그의 숫자를 이후 것과 나란히 읽지 않는다 |
| 34 | **다중 CDU 전이 행에서 누출이 두 CDU 모두의 랙 0 에 걸린다.** ⑴ 관측: `PlantLoadStepCase` 가 CDU 마다 다른 누출 템플릿을 받지 않아 다중 CDU 전이 시나리오는 **누출점이 2개**다. ⑵ 5-1 「누출 주입 지점」은 "랙 1개에 주입한다 · 전 랙 동시 누출은 5장에 근거가 없어 돌리지 않는다" 이므로 **현재 다중 CDU 전이 행은 그 규정 밖**이다. 또한 양쪽이 대칭이라 2차측 배분이 움직이지 않아, 세션 5 C7 이 관측한 "A 누출 → B 가 좋아진다" 연동이 **이 행에서는 상쇄된다**. ⑶ 선택지(사람이 정한다): **(가)** `plant.py` 가 CDU 별 누출 템플릿을 받게 고치고 `leak_cdu_index` 열을 추가한 뒤 재생성(약 28분) · **(나)** 다중 CDU 전이 행을 데이터셋에서 빼고 한계로 기록 — 재생성 불필요, 다만 연동 전이 데이터가 사라진다. ⑷ **이 항목이 정해지기 전에는 종료 판정 재료가 완결되지 않는다** | M | `regime=transient` 이고 `cdu_config=dual_asymmetric` 인 행 전체 (전체 310,016행 중 205,824행). **함께 읽을 것**: 단일 CDU 전이와 다중 CDU 전이는 **자극 형태가 다르다** — 전자는 누출이 **스텝으로** 들어오고(부하 불변), 후자는 누출이 **t=0 이전부터 있고** 부하가 스텝이다. `cdu_config` 로 구분해 읽어야 하며, 두 전이를 같은 자극으로 보면 안 된다. **[닫힘 · 세션 5.5-D]** 사람이 **(가)** 를 골랐다 — `plant.PlantLoadStepCase` 가 CDU 별 템플릿(`templates`)을 받게 하고, 누출을 CDU 0 의 랙 0 **하나에만** 걸고, `leak_cdu_index` 열을 추가해 재생성했다. 대칭 경로는 지우지 않았다(`templates=None` 이면 종전과 `==` 로 같다 — 테스트가 고정한다). **연동이 실제로 살아났다**: 부하가 그대로인 CDU B 의 2차측 몫이 누출 수준에 대해 단조 증가(+50% 에서 **+0.01908 L/s**)하고 환수온도가 단조 감소(**−0.01064 K**)한다 — 세션 5 C7 이 정상상태에서 관측한 것(+0.01905 L/s · −0.012649 K)과 **방향이 같고 배분 이동은 세 자리까지 일치**한다. 종전 양쪽 누출에서는 배분 이동이 **+1.9e-05 L/s** (≈0)로 상쇄됐고 B 의 온도는 **오히려 올랐다**(+0.021 K — B 자신에게도 누출이 있었으므로). ⑵ 의 자극 형태 차이는 **그대로 남아 한계로 이관했다**(`dataset_report` 열린 한계) |
| 35 | **의존성 버전이 `pyproject.toml` 에 고정돼 있지 않다.** ⑴ 관측: 새 PC 가 `pip install` 할 때 **그 시점 최신**을 받는다. 실제로 `ruff` 가 0.16.4 → 0.16.5 로 올라왔다(lint 도구라 수치 무관). 런타임 4종은 **우연히** 같았다 — `numpy` 2.5.2 · `scipy` 1.18.1 · `CoolProp` 8.0.0 · `pandas` 3.0.5. ⑵ 참이면 같이 보여야 할 것: `CoolProp`·`scipy` 가 갈리면 물성·solver 의 마지막 자리가 이동해 **데이터셋 재생성본이 달라진다.** 세션 5.5-B 인계가 "같은 버전이면 바이트 단위로 같다"고 적었는데 **그 전제를 보장하는 장치가 없다.** ⑶ 가설(사람이 정한다): `pyproject.toml` 에 런타임 4종을 **고정**하거나, **현재 버전을 기록만 하고 재생성 시 대조**한다. 고정하면 보안 패치를 못 받는다. **세션 5.5-C 는 `pyproject.toml` 을 고치지 않았다 — 기록만 했다**. **[갱신 · 세션 5.7 — 닫지 않는다]** 사람이 정했다: **고정하지 않고 기록한다.** `pyproject.toml` 을 고치지 않았고(고정하면 보안 패치를 못 받는다), 대신 `dataset_report` 가 **생성 환경 절**을 두어 Python·플랫폼·numpy·scipy·CoolProp·pandas 버전을 싣는다. 52열본은 Python 3.12.10 · Windows 11 · numpy 2.5.2 · scipy 1.18.1 · CoolProp 8.0.0 · pandas 3.0.5 에서 생성됐다. **기록은 대조를 가능하게 할 뿐 재현을 보장하지 않으므로 이 항목은 열린 채로 둔다** — **본 프로젝트 전환 시 고정한다** | M | 데이터셋 재현성의 전제. 세션 5.5-B 가 판정한 게이트 A·B·C 는 전부 `==` 비교이므로, 재생성 시 버전이 갈리면 같은 코드에서도 어긋날 수 있다 |
| 36 | **K값 근사 누출과 질량손실 누출이 같은 방향을 가리키지 않는다(수력 · 세션 5.6 관측).** ⑴ **관측**: 8조합 × 누출 크기 4수준 × 배치 6 전수(272회 `fsolve`, 전부 `ier=1`)에서 **총유량**은 K 근사가 감소(−2.5e-02 ~ −6.4e-03 L/s)·질량손실이 증가(+3.0e-03 ~ +1.5e-01 L/s)로 **정반대**이고, **누출랙 통과유량**도 K 근사 감소·질량손실 증가로 **정반대**다. **펌프 양정**은 부호가 배치에 따라 갈린다(펌프가 공급유량을 보면 하강, 환수유량을 보면 상승 — K 근사와 같은 부호). 또 질량손실 누출은 5-1 이 헤더를 **저항 0 의 공통 노드**로 두는 한 **랙에 국소화되지 않아**(랙 간 비대칭 2.220e-16 L/s) ⑶ 과 ⑷ 가 같이 움직인다 — K 근사가 ⑶↓/⑷↑ 로 갈라지는 것과 구조가 다르다. 배치를 고정하면 부호는 5장 범위 양 끝 전 조합에서 **뒤집히지 않는다** — 가르는 것은 가정치 범위가 아니라 배치다. ⑵ **참이면 같이 움직여야 할 것**: 데이터셋의 누출 신호는 5장이 정의한 **대용(proxy)** 의 거동이므로, 이 데이터로 학습한 모델이 **실제 누출에서 반대 부호를 볼 수 있다.** 특히 「총유량 감소 = 누출」과 「랙별 유량 하락으로 누출 랙을 특정」 두 규칙은 질량손실 쪽에 대응물이 없다. 온도 신호는 **보지 않았다**(이 판은 수력 한정 — 보충수 온도가 5장에 없다). ⑶ **가설(사람이 정한다)**: **(가)** K 근사를 유지하고 데이터셋·5-1 에 「누출은 대용이며 총유량·랙유량 부호가 실제와 반대일 수 있다」를 **한계로 명시**한다(재생성 불필요) · **(나)** 5-1 에 잔여저항의 누출점 상·하류 배분과 펌프 재고 보충 위치를 확정하고 질량손실을 데이터셋에 넣는다(규칙 8 개정 + 재생성 필요 + 보충수 온도라는 5장 밖 숫자가 열모델에서 필요해진다) · **(다)** 두 방식을 라벨로 병기한다. **이 판은 모델·데이터셋·5-1 을 고치지 않았다**(§7)  **[추가 · 세션 5.7-D — 열·전이까지 봤다]** 5.6·5.6-B 의 수력 한정이 풀렸다(5-1 「보충수 처리」 확정). **열에서도 부호가 갈린다**: 누출랙 출구온도와 CDU 환수온도가 K 근사(**+**)와 질량손실(**−**)에서 **정반대**이고 1,536건 전 조합에서 뒤집히지 않는다(CDU 공급온도는 K 근사 쪽만 갈려 「정반대」라고 말할 수 없다). **질량손실은 랙에 국소화되지 않으므로 전 랙 출구온도 = 환수온도**여서 「랙별 온도로 누출 랙을 특정한다」는 규칙에 대응물이 없다. **수력이 못 보는 것을 열이 본다** — 세션 5.6 관측 ④ 에서 다섯 양이 전부 0 이던 `g=0/펌프=공급` 배치에서도 환수온도가 −7.368e-02 ~ −1.886e-03 K 로 살아 있고, **부호는 배치 6 전부에서 같다**(크기만 약 2.3배 갈린다). 까닭은 질량손실이 배치와 무관하게 **HX 통과유량을 줄이기** 때문이며 5.6-B 의 Δ 와 같은 성질이다. 에너지 balance 는 **누출 엔탈피 항을 넣어야 닫힌다**(빼면 최대 1.2129% · 넣으면 0.000062%) — 모사가 자기정합적이다. **τ ∝ M 이 정확히 성립**(τ 비 = t63 비 = M 비 = 12.832779). 다중 CDU 는 `Q_ret` 읽기에서 **K 근사와 방향이 같다**(#39 참조). **이 판도 모델·데이터셋·5-1 을 고치지 않았다** — (가)/(나)/(다) 선택은 여전히 사람 몫이고, 이제 그 선택의 근거가 수력뿐 아니라 열·전이까지 있다.| L | **데이터셋 전체의 누출 라벨 해석**에 걸린다 — 세션 4·5·5.5 의 누출 신호 판정은 「K 근사 대용의 거동으로서」 여전히 유효하고 **하나도 뒤집히지 않는다**(게이트를 재판정하지 않았다). 걸리는 것은 그 신호를 **실제 누출의 지표로 읽을 때**다. 5장·5-1 이 정하지 않는 구조 자유도 둘(잔여저항의 환수측 몫 g · 펌프가 보는 유량 위치)이 부호를 가르므로, **미해결 #24(잔여저항 분해 금지)가 닫히기 전에는 ⑵ 의 부호를 정할 수 없다** — 설계데이터가 필요하다. **[추가 · 세션 5.6-B]** 공급−환수 유량 불일치 Δ 를 같은 전수로 확인했다: Δ 는 구조 자유도 둘에 **흔들리지 않고**(배치 6 편차 **1.776e-15 L/s**) 누출 크기에 **엄격히 단조**이며 공급유량 대비 **최대 1.211%** 다. 세션 5.6 관측 ④ 에서 다섯 양이 전부 0 이던 `g=0/펌프=공급` 배치에서도 **Δ 만 살아남는다**(+2.838e-02 ~ +1.877e-01 L/s). 다만 Δ = Q_leak 은 `solve_massloss` 의 정의를 되읽은 **구성상 항등식**이라 질량보존의 물리적 검증이 아니다. **[추가 · 세션 5.7]** 사람이 정했다 — **Δ 열을 데이터셋에 넣지 않는다**(현재 데이터셋은 K 근사 전용이라 전 행 정확히 0 이고 정보량이 0 인 열이 하나 느는 것뿐이다). 대신 **`leak_model` 열**(전 행 `K_approx`)을 넣어 이 데이터셋이 어느 모사인지 행마다 싣고, `dataset_report` ③절이 위 관측 전부를 **종료 판정 재료**로 출력하며, **#24 문구를 「신호의 존재 여부까지 지배한다」로 보강**했다. **(가)/(나)/(다) 선택은 여전히 사람 몫이며 이 판은 모델·데이터셋·5-1 을 고치지 않았다.** 5-1 「누출 시나리오」·「누출 주입 지점」의 한계 문구 추가는 **사람이 직접 한다**(정본 수정 · 재업로드 필요). **[갱신 · 세션 5.7-C]** 사람이 정했다 — **파일럿 안에서 실행되는 것은 (가)**(K 근사 유지 + 한계 명시)이고 5-1 문구 추가로 실행된다. **(나)로 가는 데 필요했던 것 중 보충수 온도는 오늘 닫혔다**: 이 과제의 목적이 미량 누출 조기감지(1장)이고 미량 구간에서는 밀폐루프 보충 로직이 작동하지 않으므로 **보충수는 조기감지 시나리오에 정의상 등장하지 않는다** — 새 가정치가 아니라 1장 목적과 5장 누출 크기에서 따라 나오는 **역산**이다(5-1 빈칸 처리 순서 1번). 남은 것은 **#24 잔여저항 분해**와 **계통 정압**(팽창탱크 용적·초기 충전압 — 5장 밖 숫자 둘)이며 **둘 다 설계데이터 대기**다. **최종 선택은 여전히 사람 몫이다.** **이 닫힘이 세션 5.6 §3 의 전제를 바꾼다** — 5.6 이 수력 한정을 택한 이유가 보충수 온도였고, 그것이 사라졌으므로 **5.7-D 가 열·전이를 볼 수 있다** |
| 37 | **시연·분석용 「정상」 표본에 누출이 섞여 있는지 확인되지 않았다.** ⑴ **관측**: 데이터셋의 `stimulus_kind=none`(regime=steady) **1,280행**에 누출 K값이 걸린 정상상태 케이스가 섞여 있는지 **확인하지 않았다**. `stimulus_kind` 는 세션 5.7 이 `regime`·`cdu_config` 에서 읽어 붙인 라벨이고 **「자극이 없다」는 뜻이지 「누출이 없다」는 뜻이 아니다** — 정상상태 행에는 누출 축(`leak_level_percent` 0/5/20/50)이 그대로 살아 있다. ⑵ **참이면 같이 보여야 할 것**: 시연이나 분석에서 「정상」으로 고른 표본에 누출 행이 들어가면 **정상 대비 누출 대조가 성립하지 않는다.** 세션 4·5·5.5 의 게이트는 조합 안에서 정상 해를 따로 풀어 비교하므로 **영향받지 않는다** — 걸리는 것은 **CSV 를 그대로 골라 쓰는 쪽**이다. ⑶ **가설**: 누출 관련 열(`leak_level_percent == 0` 또는 `leak_cdu_index` 빈 칸)로 걸러진다. **다음 판에서 CSV 를 읽어 확인한다** — 세면 되는 일이라 새 숫자가 필요 없다  **[확인 완료 · 세션 5.7-D — 닫지 않는다]** CSV 를 읽어 **셌다**(데이터셋을 고치지 않았다). **가설이 맞았다 — 섞여 있다.** `stimulus_kind=none` 은 `regime=steady` 1,280행과 **정확히 일치**하고(다른 regime 에 `none` 은 없다), 그 1,280행의 `leak_level_percent` 는 **0/5/20/50 이 각각 320행씩 정확히 4등분**돼 있다. 즉 **960행(75.0%)에 누출이 걸려 있다.** 누출 행의 `leak_cdu_index` 는 전부 **0**(5-1 「누출 주입 지점」·#34 대로 CDU 0 에만), `cdu_config` 는 dual_asymmetric 384 · dual_symmetric 384 · single 192, `leak_model` 은 전부 `K_approx`, `dataset_version` 은 `session-5.7` 이다. **「자극이 없다」와 「누출이 없다」가 다르다는 것이 수치로 확인됐다** — 정상 표본은 `stimulus_kind=none` 이 아니라 **`leak_level_percent == 0`** 으로 걸러야 하고, 그것으로 320행이 나온다. **리포트나 열 설명에 이 문구를 넣을지는 사람이 정한다**(§7) — 이 판은 확인만 했고 `dataset_report`·데이터셋·열 정의를 하나도 건드리지 않았다. **[문구 반영 · 세션 5.8 — 여전히 닫지 않는다]**  에 「정상 표본을 어떻게 고르는가」 절을 넣었다:  는 **자극의 종류**를 뜻하며 누출 유무를 뜻하지 않는다 · 정상 표본은 **** 으로 거른다 · 그 결과가 **320행**이다 ·  으로 거르면 누출 행이 **75% 섞인다**. **행수는 하드코딩하지 않고  에서 센다**() — 데이터셋 규모가 바뀌면 문구의 숫자도 같이 움직인다. **데이터셋·열 정의를 고치지 않았다**(리포트 문구만). 열 하나를 더 걸지 여부는 여전히 사람 몫이라 **닫지 않는다.**| S | `regime=steady` 인 1,280행을 「정상」으로 읽는 모든 용도. **데이터셋 자체의 결함이 아니라 읽는 법의 문제**이고, 필요하면 열 하나를 더 거는 것으로 끝난다. 세션 5.7 이 붙인 `stimulus_kind` 라벨이 **누출 유무를 뜻하지 않는다**는 것을 리포트나 열 설명에 적을지는 확인 뒤에 정한다 |
| 38 | **질량손실 전이에서 상수 M 근사가 관측 창 뒷부분을 지탱하지 못한다.** ⑴ **관측**(세션 5.7-D · 768 적분): 보충이 없으므로 계통이 줄어드는데 모델은 M 을 상수로 둔다. 소실률(Q_leak × 시간 × ρ / M)이 **전체 창 30τ 에서 21.89~36.37%**, **t63 까지는 0.62~1.87%** 다. ⑵ **참이면 같이 보여야 할 것**: 30τ 파형의 뒷부분은 실제보다 M 이 큰 계통의 응답이므로 **종점값과 후반 기울기를 그대로 읽으면 안 된다.** 반면 **부호는 t63 구간에서 이미 정해지므로 세션 5.7-D 의 A~G 부호는 이 유보에 흔들리지 않는다.** ⑶ **가설(사람이 정한다)**: **(가)** 관측 창을 t63 근방으로 줄여 상수 M 을 유지한다(새 숫자 0개) · **(나)** M(t) 를 적분한다 — **초기 보유량**이라는 처리가 새로 붙고, 계통이 비는 지점을 정해야 한다 · **(다)** 팽창탱크를 넣는다 — 용적·초기 충전압이라는 **5장 밖 숫자 둘**이 필요하다(5-1 「보충수 처리」 한계와 같은 둘이다). **이 판은 (가)~(다) 중 아무것도 고르지 않았고 M 을 상수로 둔 채 소실률을 표로 냈다** | M | **질량손실 전이의 정량값**에 걸린다. 세션 5.7-D 의 부호 판정과 τ ∝ M 확인에는 걸리지 않는다(둘 다 t63 구간에서 정해진다). K 근사 데이터셋은 질량이 보존되므로 **영향받지 않는다** — 걸리는 것은 #36 이 (나)·(다)로 갈 때뿐이다 |
| 39 | **공유 2차측 배분이 보는 「1차측 유량」이 누출 CDU 에서 처음으로 갈린다.** ⑴ **관측**(세션 5.7-D · 결합 3,840 해): 5-1 「공유 2차측 결합 방식」은 배분을 「1차측 유량에 비례」로만 정한다. 밀폐루프에서는 공급=환수라 문구가 갈리지 않았는데, 질량손실 누출 CDU 는 ṁ_sup ≠ ṁ_ret 다. 두 읽기의 결과가 **다르다**: `Q_ret`(HX 통과유량)로 읽으면 누출 없는 CDU B 의 2차측 몫이 **전 조합에서 증가**하고 환수온도가 **전 조합에서 감소**해 세션 5.5-D 의 K 근사 관측과 **방향이 같다**(+1.142e-02 ~ +9.457e-02 L/s · 엄격 단조 위반 0건). `Q_sup`(랙 통과유량)로 읽으면 **두 부호가 다 갈린다**(−7.586e-02 ~ +1.337e-04 L/s). ⑵ **참이면 같이 보여야 할 것**: 다중 CDU 누출 신호의 **연동 방향**이 이 읽기에 걸린다. `Q_sup` 읽기에서는 「A 가 누출이면 B 가 좋아진다」가 성립하지 않는 조합이 있다. ⑶ **가설(사람이 정한다)**: `Q_ret` 가 물리적으로 정해진 읽기다 — **열교환기를 실제로 지나는 것이 환수유량**이고, 세션 5.7-D 의 열모델도 같은 이유로 C_1차 에 ṁ_ret 를 쓴다. 그러면 **새 숫자가 아니라 5-1 문구의 해석**이고 5-1 에 한 줄 덧붙이면 닫힌다. 다만 **이 판은 값을 고르지 않고 양 끝을 둘 다 돌렸다**(g 와 같은 취급) | S | **#36 이 (나)·(다)로 갈 때의 다중 CDU 행**에 걸린다. 현재 데이터셋은 K 근사(질량 보존)라 공급=환수이므로 **영향받지 않는다**. 5-1 문구 보완은 정본 수정이라 **사람 몫**이다 |

> **확인 완료 (세션 5.5-B 마무리)** — 사람이 검토해 **그대로 두기로** 한 것 둘.
> ① **저장 격자 최근접 점 선택(보간 없음)** — 그대로 둔다. 원본 균등 격자 간격
> 0.015τ 가 비균등 요구 간격 0.02τ 보다 촘촘해 시간 오차가 최대 0.0075τ 다.
> 보간하지 않은 것이 적분기가 실제로 낸 점만 담아 더 안전하다. **다만 `plant.py`
> 가 자기 `t_eval` 을 쓰는 구조는 남아 있어, 노드를 늘리는 확장에서 다시 걸린다**
> — 기록만 한다.
> ② **비대칭 부하의 거울상 중복** — 그대로 둔다. 사람이 정한 규모를 코드가 줄이지
> 않은 판단이 맞고, 중복이 남아 있으면 거울상 여부를 사용자가 직접 확인할 수 있어
> **검증 재료**가 된다.

> **기록만 (세션 2.5)** — **GUI 는 파일럿 밖이다.** 모사가 완료된 뒤 별도로 진행하며,
> 지금은 형태를 정하지 않는다. `project-overview.md` 「시각화 방향」 절은 이 판에서
> **손대지 않았다** — 그 절의 내용(정적 PFD 선호 · FreeSimpleGUI 후보 · 제작 주체 미정)은
> 결정이 아니라 참고 기록이며, 본 프로젝트 착수 시점에 다시 확인한다.
