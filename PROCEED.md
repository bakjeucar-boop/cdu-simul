# PROCEED.md — 진행 상태

> 이 파일이 "오늘 상태"의 정본이다. `project-overview.md` 「지금 어디인가」 절과
> 어긋나면 이 파일이 맞다. 대화 시작 시 `daily_brief.py` 출력을 첫 메시지에 붙인다.
> **모든 수치는 가정값 기반이며 실측이 아니다.**

## 현재 상태

| 세션 | 단계 | 게이트 통과 여부 | 다음 세션 첫 작업 |
|---|---|---|---|
| 1-A | 저장소 clone · 가상환경 · 저장소 골격 · 5장 가정치 전사 · CoolProp 래퍼 · `PROCEED.md`·`daily_brief.py` 초기 구성 · 문서 4종 커밋 | **해당 없음 (물리 모델 없음)** — 환경 스모크 테스트 5건 통과했으나 이는 feasibility 판정이 아니다 | (완료) |
| 1-B | 단일 랙 + 단일 CDU, 2차측 고정온도, 정상상태 모델 (`model.py`) · energy balance 자동 검사 | **통과** — 4케이스 최대 \|잔차\| = **0.00506 %** (기준 <0.1%). 단 아래 ※를 반드시 함께 읽는다 | (완료) |
| 1-C | 저장소 위생 정비 — ruff·mypy 도입 · 위생 미해결(#6·#9·#10·#11·#16) 처리 · 5-1 출처 반영 | **게이트 없음** (물리 모델을 건드리지 않음). 대신 **정비 전후 4케이스 수치가 `repr` 전체자리까지 동일**함을 확인했다. pytest 17건·ruff·mypy 전부 통과 | (완료) |
| 2 | 시간축 추가 — `dynamics.py` (2노드 온도 `solve_ivp` 적분) · 부하 스텝 응답 | **통과** — 부하 상승/하강 스텝에서 T_return 이 타당한 방향으로 반응(스텝 직후 기울기·순변화 둘 다). 5장 범위 **양 끝 16조합 전부**에서 성립. 단 아래 ※2 를 함께 읽는다 | 세션 3: 랙 개수 확장 → 헤더 압력평형(`fsolve`) 유량분배. **① 세션 3 착수 전 사람이 채워야 할 것**: 미해결 #1(밸브 Cv·배관 K값 역산 규칙) · #2(펌프 곡선 계수 H₀·a·b) — 둘 다 5장 표의 빈칸이라 압력평형을 세울 수 없다. **② 웹 대화창이 먼저 처리할 것**: 파일럿 종료 조건(#8) 검토 · `CLAUDE.md` 절대 규칙 12의 Python 버전 어긋남(#10) 정정. **③ 세션 3은 ①·②가 정해지기 전에는 열지 않는다.** |

> **※ 1-B 게이트가 실제로 검증한 것 (과대해석 금지)**
> 게이트가 쓰는 잔차는 *CoolProp 엔탈피 경로로 다시 계산한 1차측 흡열량* 과
> *5장 랙 발열량* 의 차다. 이것이 재는 것은 **모델이 쓴 상수 cp 선형화가 실제
> 엔탈피 변화와 어긋나지 않는가** 하나다.
> 반면 *HX ε-NTU duty 와 랙 발열량의 일치* 는 모델 구조상 **항등적으로 0**이다
> (실측 1.8e-14 %). 그쪽은 아무것도 증명하지 않으며 게이트에 쓰지 않았다.
> 통과의 뜻은 "무너지지 않는다"까지이고 **"정확하다"가 아니다** — 실측이 없다.

> **※2 세션 2 게이트가 실제로 판정한 것 (과대해석 금지)**
> 판정한 feasibility 기준은 **T_return 방향성 하나**다.
> · **수렴시간은 판정하지 않았다.** M(계통 보유수량)이 배관 보유량만 담고 있어
>   열교환기·CDU 내부·콜드플레이트 보유량이 빠져 있다(프로젝트정리 5-1 한계).
>   실제보다 M 이 작고 수렴시간도 짧게 나온다 — 6장 「수렴시간」 기준은 이
>   상태로 판정할 수 없다(미해결 #21).
> · **극단 케이스(부하 0/최대)는 돌리지 않았다** — 세션 3 게이트다.
> · t→∞ 값이 1-B 정상상태와 4.5e-9 K 안에서 일치했으나, 이는 **구현 일관성
>   검사**이지 물리의 독립 확인이 아니다. 동적 모델의 평형점은 설계상 1-B 와
>   같은 방정식이다 — 미해결 #15 는 여전히 열려 있다.

## 세션 로그

### 세션 1-A · 2026-08-27 · 저장소 clone · 개발환경 · 가정치 전사

**완료한 항목**

- 저장소 clone (`https://github.com/bakjeucar-boop/cdu-simul`, 빈 저장소 · 기본 브랜치 `main`)
- 정본 문서 4종을 저장소 루트에 복사 (`CLAUDE.md` 포함 — Claude Code 자동 인식 위치)
- Python 3.12.10 확인 → 저장소 루트에 `.venv` 생성
- 저장소 골격: `pyproject.toml` · `.gitignore` · `README.md` · `src/cdu_simul/` · `tests/`
- 승인 패키지 설치 (numpy 2.5.2 · scipy 1.18.1 · CoolProp 8.0.0 · pandas 3.0.5 · pytest 9.1.1)
- `src/cdu_simul/assumptions.py` — 프로젝트정리 5장 표 전사 (출처 태그 포함)
- `src/cdu_simul/fluid.py` — CoolProp 물성 래퍼 (밀도·비열)
- `tests/test_environment.py` — 환경 스모크 테스트 5건, 전부 통과
- `PROCEED.md` · `daily_brief.py` 초기 구성
- `CLAUDE.md` 「명령어」 절을 확정된 실제 값으로 채움

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `pyproject.toml` · `.gitignore` · `README.md` · `src/cdu_simul/__init__.py` | 신규 (골격) |
| `AI서버_CDU_디지털트윈_프로젝트정리.md` · `CLAUDE.md` · `collaboration.md` · `project-overview.md` | 신규 (문서 4종 복사) |
| `CLAUDE.md` 「명령어」 절 | 수정 — **웹 프로젝트 지식 재업로드 필요** |
| `src/cdu_simul/assumptions.py` | 신규 (5장 전사) |
| `src/cdu_simul/fluid.py` | 신규 (CoolProp 래퍼) |
| `tests/test_environment.py` | 신규 (환경 스모크) |
| `PROCEED.md` · `daily_brief.py` | 신규 |

**돌린 것 / 안 돌린 것**

- 돌렸음: `pytest` (5 passed) · `daily_brief.py` · `pip list` · CoolProp 물성 조회
- **안 돌렸음**: 물리 모델 일체. 이 세션에는 `CDUModel`·압력평형(`fsolve`)·ε-NTU·
  `solve_ivp`·유량분배가 없다. energy balance 오차 · T_return 방향성 · 수렴시간 ·
  극단 케이스 발산 — **6장 네 기준 중 어느 것도 이 세션에서 보지 않았다.**

**미해결 항목** — 아래 「미해결 목록」 절 참조.

**다음 세션이 알아야 할 결정**

1. **CoolProp 유체 문자열 위치**: `assumptions.py` 의 `SCENARIO.coolant_coolprop_id`
   에 두고 `fluid.py` 가 참조한다. PG25 선택 자체가 5장 가정치이므로 교체 지점이
   assumptions 한 곳이어야 한다는 판단(절대 규칙 2 · collaboration.md ④).
2. **가정치 파일 형식은 `.py`** (yaml 아님) — 타입힌트·frozen dataclass 에 맞고
   의존성이 늘지 않는다.
3. **범위값은 `Range(low, high, unit)` 로 범위 그대로 보존.** 중앙값 등 대표값을
   임의로 고르지 않았다. `Range` 에 대표값 추출 메서드를 일부러 두지 않았다.
4. **5장 표 원단위를 그대로 보존**(mAq · L/s · ℃ · kW · %). 단위 변환은 하지 않았다.
   유일한 변환은 `fluid.py` 의 ℃→K (CoolProp 호출 경계 전용).
5. **파생값은 만들지 않았다** — 밸브 Cv 역산, 펌프 곡선 계수 H0·a·b, 배관 K값.
   5장 표에 수치가 없다. 1-B에서 필요해지면 사람에게 먼저 확인한다.
6. **Python 3.12.10 사용** (시스템에 3.13·3.12 존재, 3.11 없음). CoolProp 8.0.0 은
   cp312 휠로 설치됐다.
7. **패키지 임포트는 편집설치**(`pip install -e ".[dev]"`)로 해결했다. `PYTHONPATH`
   조작이나 `sys.path` 삽입을 코드에 넣지 않았다.

### 세션 1-B · 2026-08-27 · 단일 랙·단일 CDU 정상상태 모델 · energy balance 검증

**완료한 항목**

- 세션 시작 결정사항 ㉮~㉳ 를 미해결 목록에 반영 (#1·#2 → 세션 3, #3 → 방침 (B) 확정,
  #5 문구 교체, #7 → 세션 3 재검토, #10·#11 신규)
- `src/cdu_simul/model.py` — 단일 랙·단일 CDU 정상상태 모델 (ε-NTU + 고정점 fsolve)
- `src/cdu_simul/fluid.py` — `coolant_enthalpy_Jkg` 추가 (balance 검사용 독립 경로)
- `tests/test_energy_balance.py` — balance 자동 검사 12건
- 4케이스 실행 (2차측 27/30℃ × NTU 2/3, 부하 100% 고정)

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `src/cdu_simul/model.py` | 신규 (정상상태 모델) |
| `src/cdu_simul/fluid.py` | 수정 (엔탈피 조회 추가) |
| `tests/test_energy_balance.py` | 신규 (balance 자동 검사) |
| `PROCEED.md` | 수정 (결정사항 반영 + 이 로그) |

**결과 표** (`.venv/Scripts/python.exe -m cdu_simul.model` · 가정값 기반 · 실측 아님)

| 케이스 | T_supply [℃] | T_return [℃] | ΔT [K] | HX duty [kW] | balance 잔차 [%] | solver |
|---|---|---|---|---|---|---|
| 2차측 27℃ · NTU 2 | 32.15 | 42.44 | 10.29 | 80.00 | -0.00502 | 수렴 |
| 2차측 27℃ · NTU 3 | 30.43 | 40.72 | 10.29 | 80.00 | -0.00506 | 수렴 |
| 2차측 30℃ · NTU 2 | 35.14 | 45.43 | 10.29 | 80.00 | -0.00494 | 수렴 |
| 2차측 30℃ · NTU 3 | 33.43 | 43.72 | 10.29 | 80.00 | -0.00498 | 수렴 |

**돌린 것 / 안 돌린 것**

- 돌렸음: 4케이스 정상상태 해 · `pytest` 17건 전부 통과 · cp 규칙 3종 감도 비교
- `fsolve` 성공 플래그(`ier`)를 4케이스 모두 확인했다 — 전부 `ier == 1`
  ("The solution converged.")
- **안 돌렸음**: 시간축·`solve_ivp`(세션 2) · 압력평형·`fsolve` 유량분배·랙 확장(세션 3) ·
  부하 0/최대 극단 케이스(세션 3 게이트) · 누출 시나리오(세션 4) · 다중 CDU(세션 5) ·
  시각화. **6장 네 기준 중 energy balance 하나만 봤다.** T_return 방향성 · 수렴시간 ·
  극단 케이스 발산은 보지 않았다.

**다음 세션이 알아야 할 결정**

1. **balance 잔차 정의** = ( m_dot·[h(T_return) - h(T_supply)] - Q_rack ) / Q_rack × 100.
   CoolProp 엔탈피 경로를 써서 모델이 해를 구할 때 쓴 상수 cp 경로와 **독립**하게 만들었다.
   이 정의를 바꾸면 1-B 통과의 의미가 바뀐다.
2. **HX duty 잔차는 구조상 항등적으로 0**(실측 1.8e-14 %). `test_energy_balance.py` 의
   `test_hx_duty_residual_is_structural_identity` 가 이 사실 자체를 박아 두었다 —
   게이트가 아니라 오해 방지용이다.
3. **cp·ρ 평가 온도 규칙 = 1차측 벌크 평균온도(`bulk_mean`)**. 5장 표에 없는
   수치처리 규약이며 이 판에서 정했다. **이 선택이 게이트 통과/실패를 가른다**:
   `bulk_mean` 0.00506% (통과) · `supply` 0.30891% (실패) · `return` 0.31524% (실패).
   즉 게이트는 헛돌지 않는다.
4. **HX 유동배열을 대향류로 골랐다** — 5장에 배열 지정이 없다(미해결 #12).
   ε = NTU/(1+NTU) (열용량유량비 1).
5. **2차측 유체를 1차측과 같은 냉각액으로 보았다** — 5장에 2차측 유체가 없다.
   5장의 유량비 1:1 과 합쳐 열용량유량비 Cr = 1 로 두었다(미해결 #13).
   물 등 다른 유체를 도입하는 것이야말로 5장 밖 도입이라고 판단했다.
6. **압력-유량을 풀지 않았다.** 단일 랙이라 분배할 곳이 없다. 하이브리드 구조
   (절대 규칙 4)를 없앤 것이 아니라 **아직 만들지 않은 것**이다 — 세션 3에서 도입한다.
7. **결과 표 출력 진입점은 `python -m cdu_simul.model`** 이다. 새 스크립트 파일을
   만들지 않고 모듈 `__main__` 으로 두었다. `CLAUDE.md` 「명령어」 절에는 넣지 않았다 —
   이번 판은 문서 수정 권한이 없다(미해결 #16).
8. **계산 ΔT ≈ 10.29 K** 로 5장의 ΔT 10℃ 를 근사 재현했다(미해결 #5 확인). 다만
   **관측이며 판정이 아니다.** 5장 값에 맞추려고 아무것도 조정하지 않았다.

### 세션 1-C · 2026-08-27 · 저장소 위생 정비 (물리 모델 손대지 않음)

**완료한 항목**

- 정본 문서 4종 상태 확인 — 갱신본이 **이미 저장소 루트에 있었다**. `cdu-docs` 쪽이
  세션 1-A 시점 구본이어서 **복사하지 않았다**(복사했으면 사람이 고친 내용을 되돌렸다).
- `ruff` 0.16.4 · `mypy` 2.3.1 도입 — 의존성·설정 모두 `pyproject.toml` 한 곳에
- ruff 위반 4건(전부 E501) 수정 — **주석 줄바꿈만**, 문구·의미 그대로
- mypy 위반 2건 수정 — `__defaults__` 의 `None` 좁히기만, 판정 내용 그대로
- `#12`(대향류)·`#14`(cp 규칙) 근거를 **프로젝트정리 5-1** 로 교체 — 주석·출처만
- `.gitattributes` 추가(#9) · #6·#10·#11 관측만 · #16 확인 후 닫음
- 정비 전후 4케이스 동일성 증명 · pytest 17건 · ruff · mypy 전부 통과

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` · `CLAUDE.md` · `project-overview.md` | 사람이 고친 갱신본을 커밋 (5-1 소절 · 정형 절차 절 · 1-C 게이트 행) |
| `pyproject.toml` | 수정 (ruff·mypy 의존성 + 설정) |
| `src/cdu_simul/assumptions.py` | 수정 (**주석 줄바꿈만** — 값·구조 불변) |
| `src/cdu_simul/model.py` | 수정 (**주석·출처만** — 계산식 불변) |
| `tests/test_environment.py` | 수정 (타입 좁히기만 — 판정 내용 불변) |
| `.gitattributes` | 신규 (줄바꿈 정규화) |
| `PROCEED.md` | 수정 (이 로그) |

**정비 전후 동일성 (이 판의 게이트 대용)**

`git stash` 로 정비 전 코드(HEAD)를 되살려 같은 스크립트를 돌리고, 정비 후 결과와
대조했다. **T_supply · T_return · ΔT · HX duty · ε · m_dot · cp · 물성평가온도 ·
balance 잔차 — 9개 물리량 × 4케이스가 `repr` 전체자리까지 한 글자도 다르지 않다.**
`-m cdu_simul.model` 출력 파일도 문자 단위로 동일했다.

**돌린 것 / 안 돌린 것**

- 돌렸음: `ruff check .` · `mypy src tests` · `pytest`(17 passed) ·
  `-m cdu_simul.model` 정비 전/후 2회 · `git ls-files --eol`
- **안 돌렸음**: 물리 모델 변경 일체 · 시간축(세션 2) · 압력평형(세션 3) ·
  누출(세션 4) · 다중 CDU(세션 5) · 시각화.
  **6장 feasibility 기준은 이번 판에서 하나도 보지 않았다.**
  ruff·mypy 통과는 "코드가 문법·타입·스타일상 성립한다"는 뜻이지 "물리적으로
  맞다"는 뜻이 아니다 — feasibility 로 적지 않는다.

**다음 세션이 알아야 할 결정**

1. **`cdu-docs` 폴더는 구본이다.** 갱신본은 저장소 루트에만 있다. `CLAUDE.md`
   「작업 시작 전 필독」 4 대로 **저장소 사본이 정본**이므로 그대로 두었으나,
   사람이 `cdu-docs` 를 동기화하거나 폐기하는 편이 안전하다(미해결 #17).
2. **ruff 는 한글을 2칸 폭으로 센다.** `line-length = 90` 은 표시폭 기준이며
   기존 코드의 최장 표시폭은 99(한글 주석)였다. 코드 자체의 최장 줄은 89자다.
   위반 4건은 전부 한글 주석이었고 줄바꿈으로 해소했다.
3. **`.gitattributes` 는 파일 내용을 하나도 바꾸지 않았다.** `git ls-files --eol`
   로 확인한 결과 저장소·작업트리 모두 이미 LF 였다(`i/lf w/lf`). 기존의
   `LF will be replaced by CRLF` 경고는 **앞으로의 체크아웃 방향**에 대한 것이었지
   저장된 바이트가 CRLF 였다는 뜻이 아니다. 따라서 "줄바꿈만 바뀌는 커밋"은
   실제로 발생하지 않았고, 해당 커밋에는 `.gitattributes` 한 파일만 들어 있다.
4. **`requires-python = ">=3.11"`** 은 그대로 두었다(고치지 않았다 — 환경 정책은
   사람 소관, 절대 규칙 12). 의견은 미해결 #10 에 적었다.
5. **커밋 작성자 설정을 바꾸지 않았다.** 현재 값은 `WIR Generator
   <seccpi002@gmail.com>` 이다(미해결 #11 유지).

### 세션 2 · 2026-08-27 · 시간축 추가 (solve_ivp) · T_return 응답 방향성

**완료한 항목**

- 프로젝트정리 5-1 갱신본(배관 내경 · 계통 보유수량 M) 커밋
- `assumptions.py` — 배관 내경 3종 `[규격값: ASME B36.10M Sch40 · 5-1]` +
  M 범위를 내는 두 극단 구경(`holdup_bound_inner_diameters_mm`)
- `src/cdu_simul/dynamics.py` — 계통 보유수량 계산 + 2노드 온도 `solve_ivp` 적분
- `tests/test_dynamics.py` — 방향성 게이트 16건 · solver 확인 16건 · 정상상태 대조 4건
- 부하 스텝 4케이스 실행 · `requires-python` 을 `">=3.12"` 로 좁힘

**만든/바꾼 파일**

| 파일 | 성격 |
|---|---|
| `AI서버_...정리.md` | 사람이 고친 5-1 갱신본을 커밋 (배관 내경 · 계통 보유수량 M) |
| `src/cdu_simul/assumptions.py` | 수정 (배관 내경 · M 규칙 — 5-1 출처 태그) |
| `src/cdu_simul/dynamics.py` | 신규 (동적 모델 + 열용량 계산) |
| `tests/test_dynamics.py` | 신규 (세션 2 게이트 + 정상상태 대조) |
| `pyproject.toml` | 수정 (`requires-python ">=3.12"`) |
| `PROCEED.md` | 수정 (이 로그) |
| `src/cdu_simul/model.py` | **건드리지 않았다** — 1-B 결과가 1-A 기준값과 문자 단위로 동일함을 재확인 |

**결과 표** (`.venv/Scripts/python.exe -m cdu_simul.dynamics` · 가정값 기반 · 실측 아님)

| 케이스 | M [kg] | τ [s] | t63 [s] | t95 [s] | T_return 초기→최종 [℃] | 방향 | solve_ivp |
|---|---|---|---|---|---|---|---|
| 상승 20→100% · M 하한 | 11.28 | 5.72 | 5.37 | 18.72 | 30.091 → 42.437 | 상승 | 성공 |
| 하강 100→20% · M 하한 | 11.28 | 5.75 | 5.39 | 18.71 | 42.437 → 30.091 | 하강 | 성공 |
| 상승 20→100% · M 상한 | 144.70 | 73.46 | 68.87 | 240.21 | 30.091 → 42.437 | 상승 | 성공 |
| 하강 100→20% · M 상한 | 144.70 | 73.75 | 69.14 | 240.06 | 42.437 → 30.091 | 하강 | 성공 |

※ M 은 배관 보유량만 — HX·CDU 내부·콜드플레이트 제외(과소평가). 위 수렴시간은
실제보다 짧다. **"합리적"이라고 판정하지 않았다.**

**정상상태 대조 (C7)**

| 1-B 케이스 | ΔT_supply [K] | ΔT_return [K] |
|---|---|---|
| 2차측 27℃ · NTU 2 | -2.342e-10 | +1.453e-10 |
| 2차측 27℃ · NTU 3 | +2.235e-09 | -4.476e-09 |
| 2차측 30℃ · NTU 2 | -1.602e-09 | +2.510e-09 |
| 2차측 30℃ · NTU 3 | -3.294e-10 | +6.555e-10 |

최대 편차 **4.476e-09 K**. 허용오차는 **1e-6 K** 로 정했다 — 관측된 적분 잡음보다
200배 이상 느슨하고, 두 경로가 실제로 어긋났을 때 나타날 크기(4케이스 온도 폭 ~3 K)
보다는 6자리 이상 엄격하다.

**돌린 것 / 안 돌린 것**

- 돌렸음: 부하 스텝 4케이스 · 방향성 게이트 16조합 · 정상상태 대조 4케이스 ·
  `pytest` 53건 전부 통과 · `ruff` · `mypy` · 1-B 결과 불변 재확인
- `solve_ivp` 의 `success` 를 20회 적분 전부에서 확인했다 — 전부 True
- **안 돌렸음**: 압력평형·`fsolve` 유량분배·랙 확장(세션 3) · 극단 케이스 부하
  0/최대(세션 3 게이트) · 누출(세션 4) · 다중 CDU·2차측 동특성(세션 5) · 시각화.
  **6장 네 기준 중 이 판에서 판정한 것은 T_return 방향성 하나뿐이다.**

**다음 세션이 알아야 할 결정**

1. **2노드 구조**: T_supply 노드(HX 출구~랙 입구) · T_return 노드(랙 출구~HX 입구).
   `dT/dt = 0` 을 넣으면 1-B 정상상태 식이 그대로 나온다 — C7 대조가 성립하는 이유다.
   동시에 그래서 C7 은 **구현 일관성 검사**이지 물리의 독립 확인이 아니다.
2. **M 을 두 노드에 절반씩 배분**했다. 5장 등가길이를 "왕복 전체"로 읽으라는 5-1
   규칙에서 공급측·환수측이 그 왕복의 두 다리이기 때문이다. **배분 비율 자체는
   5장에도 5-1 에도 없다**(미해결 #20).
3. **M 산출용 ρ 는 고정**(5장 1차측 벌크평균 37℃ → 1011.4681 kg/m³). 반면 평형
   온도를 정하는 cp·ρ 는 **미분방정식 안에서 매 시점 다시 평가**한다 — 그래야
   t→∞ 극한이 1-B 해와 정확히 일치한다.
4. **M 범위는 구경과 등가길이 양 끝을 함께 취했다**: 하한 = 25A + 20 m = 11.276 kg,
   상한 = 80A + 30 m = 144.698 kg. 둘 다 5장에서 범위로 주어졌으므로 한쪽만
   극단으로 잡으면 M 의 실제 범위가 아니다.
5. **적분 설정**: RK45 · rtol 1e-10 · atol 1e-12 · 구간 30τ. 물리 가정이 아니라
   수치 설정이며 `dynamics.py` 상수로 노출돼 있다.
6. **결과 표는 2차측·NTU 를 범위 하단으로 고정**했다(부하와 M 만 변화시켜 보려고).
   **게이트 테스트는 고정하지 않는다** — 2차측·NTU 양 끝까지 16조합 전부 돌린다.
7. **`dynamics.py` 가 `model._property_temperature_from_state`(비공개 이름)를
   import** 한다. cp 평가 규칙을 두 곳에 적지 않으려는 것이고, `model.py` 수정
   금지 때문에 공개화하지 않았다(미해결 #22).
8. **T_return 응답은 실질적으로 단조**다 — 반대부호 증분의 최댓값이 1.4e-10 K 이고
   전부 적분 끝의 평탄한 구간에서 나온다(전체 변화량 12.35 K). 적분기 잡음이며
   물리적 오버슈트가 아니다. **단조성을 게이트로 삼지는 않았다.**

## 미해결 목록

> 세션 1-B 시작 시 사람이 정한 것을 반영했다(㉮~㉳). 닫힌 항목도 지우지 않고
> 상태를 표시해 남긴다 — 다음 세션이 이력을 잃지 않게.

| # | 한 줄 | 크기 | 영향 |
|---|---|---|---|
| 1 | **[세션 3으로 이동]** 밸브 Cv · 배관 K값이 5장 표에 없다 — "ΔP 조건에서 역산"이라고만 적혀 있어 역산 규칙(어느 ΔP·어느 유량 기준)을 사람이 정해야 한다. 1-B는 단일 랙이라 유량을 풀 필요가 없어 5장 정격유량을 그대로 썼다 | M | 세션 3 압력평형(`fsolve`)·유량분배를 못 세운다 — **세션 3 착수 전 필요** |
| 2 | **[세션 3으로 이동]** 펌프 특성곡선 계수 H0·a·b 가 5장 표에 실제로 없는 빈칸이다 (형태 `H=H0-aQ-bQ²` 만 있음) — 세션 3 프롬프트 전에 사람이 채운다 | M | 세션 3 펌프 모델을 못 세운다 — **세션 3 착수 전 필요** |
| 3 | **[확정 · 방침 (B)]** 범위값의 대표값을 고르지 않는다. **양 끝을 둘 다 돌린다.** 이유: balance 는 보존법칙이므로 범위 안 어느 값에서도 성립해야 한다. 한쪽 끝에서만 닫히면 값 선택 문제가 아니라 모델 문제다. 1-B에서 2차측 27/30℃ × NTU 2/3 = 4케이스로 적용했다 | M | 해소됨 — 이후 세션도 같은 방침을 따른다 |
| 4 | 5장 표의 "CDU 정격 ~750 kW"가 근사 표기 — 열교환기 정격 750 kW 와 같은 값인지 별개 값인지 문서가 명시하지 않음 | S | 정격 대비 부하율 계산 기준이 갈릴 수 있음 |
| 5 | 5장 표는 의도된 자기정합으로 보인다(랙당 1.94 L/s × 8 ≈ 펌프 15.5 L/s) — 1-B에서 계산 ΔT가 10℃ 부근으로 재현되는지로 확인 | S | 문서 확인만 필요, 코드 영향 없음 |
| 6 | **[관측 유지 · 다른 PC clone 시 확인]** 한글 파일명이 git 기본 출력에서 8진 이스케이프로 표기됨. 저장된 파일명 바이트는 정상 UTF-8임을 확인했다(표시 문제). 세션 1-C에서도 **이름·`.gitattributes`·git 설정 어느 것도 건드리지 않았다** — `core.quotepath` 는 개인 설정이라 저장소에 넣지 않는다 | M | 다른 PC·다른 로케일에서 clone 할 때 실제로 문제가 되는지 확인한다. 지금 이 PC의 동작에는 영향 없음 |
| 7 | **[세션 3에서 재검토]** 물성 조회 기준 압력을 표준대기압(101325 Pa)으로 두었다 — 계통 압력이 실제로 등장하는 세션 3에서 다시 본다. 지금 닫지 않는다 | S | 비압축성 유체라 밀도·비열 영향은 작을 것으로 보이나 **확인하지 않았다** |
| 8 | **[세션 2 이후로 미룸 · 사람 결정]** `project-overview.md` 「파일럿 종료 조건」에 결정과 이유가 기록됐다 — 1-B가 잰 것이 좁으므로 시간축이 들어와 축적 에너지와 적분 플럭스가 독립 경로로 대조되는 **세션 2 이후**에 채운다 | L | 그전까지 세션 6 진입 여부를 임의로 판단하지 않는다 |
| 9 | **[해소]** `.gitattributes` 추가(`* text=auto` · `*.py text eol=lf`). 확인 결과 저장소·작업트리 모두 **이미 LF** 였고(`git ls-files --eol` → `i/lf w/lf`) 파일 내용은 하나도 바뀌지 않았다 — 경고는 앞으로의 체크아웃 방향에 대한 것이었다 | S | 해소됨. 앞으로 다른 PC에서도 LF 로 고정된다 |
| 10 | **[일부 처리 · 문서 어긋남 남음]** 세션 2에서 `pyproject.toml` 의 `requires-python` 을 `">=3.12"` 로 좁혔다(검증한 것이 3.12.10 하나뿐). **그러나 `CLAUDE.md` 절대 규칙 12는 아직 "Python 3.11+" 라고 적고 있어 코드와 문서가 어긋나 있다** — 문서 수정 권한이 없어 손대지 않았다. `[tool.ruff] target-version` 도 `py311` 로 남아 있다(더 보수적인 쪽이라 그대로 뒀다) | M | 사람이 `CLAUDE.md` 규칙 12를 맞춰야 한다. 3.11·3.13 에서는 **여전히 안 돌려봤다** |
| 11 | **[사람 소관]** 커밋 작성자 `WIR Generator <seccpi002@gmail.com>` — 사람이 직접 설정한다. 세션 2에서도 git 설정을 건드리지 않았다 | S | 이력 작성자 표기 |
| 17 | **[해소]** `cdu-docs` 폴더 폐기됨. 정본은 저장소 루트 사본 하나뿐이고, 세션 2부터 저장소 밖에서 문서를 가져오지 않는다 | M | 해소됨 |
| 18 | `fluid.py` 의 `REFERENCE_PRESSURE_Pa` 주석이 아직 5-1 을 인용하지 않는다 (5-1 에 "표준대기압 101325 Pa · 잠정 · 세션 3 재검토"로 기록돼 있다). 세션 1-C 지시 범위가 #12·#14 였으므로 **건드리지 않았다**(절대 규칙 13) | S | 같은 값의 출처가 두 군데에서 다르게 읽힐 소지. 세션 3 재검토 때 함께 맞추면 된다 |
| 19 | `model.py` 의 `heat_capacity_ratio`(Cr=1) 에도 5-1 인용 주석이 없다 — #13 이 세션 5로 이월된 상태라 **건드리지 않았다** | S | 세션 5에서 2차측 유체를 확정할 때 함께 맞춘다 |
| 20 | **M 의 노드 배분(공급측·환수측 절반씩)이 5장에도 5-1 에도 없다.** 5장 등가길이를 "왕복 전체"로 읽으라는 규칙에서 두 다리로 읽은 판단이다 | M | 배분이 달라지면 두 시간상수의 비가 바뀐다. 정상상태 값은 영향 없음(C7 로 확인). 사람 확인 필요 |
| 21 | **M 에 열교환기·CDU 내부·랙 콜드플레이트 보유량이 빠져 있다**(5장에 없음, 5-1 한계 기록). 실제보다 M 이 작고 수렴시간도 짧게 나온다 | M | **6장 「수렴시간」 기준을 이 상태로 판정할 수 없다.** 세션 2에서 판정하지 않았고, 설계데이터나 추가 가정치 없이는 이후에도 판정 불가 |
| 22 | `dynamics.py` 가 `model._property_temperature_from_state`(비공개 이름)를 import 한다. cp 평가 규칙을 두 곳에 적지 않으려는 것이고, 세션 2는 `model.py` 수정 금지라 공개화하지 않았다 | S | 위생 항목. 다음 정비 판에서 공개 이름으로 승격하면 된다 |
| 23 | 배관 규격 계열에 따라 M 이 이동한다 — 5-1 기록: KS D 3507 계열(25A 내경 27.5 mm)을 쓰면 약 7% 커진다. **ASME Sch40 으로 계산했고 다른 계열로는 안 돌려봤다** | S | 시간상수가 그만큼 이동. 설계데이터 확보 시 확정 |
| 12 | **[확정 · 대향류]** 프로젝트정리 5-1 에 기록됨. 근거가 "판형 HX 통례"에서 **"5장 온도 역산"**(대향류·NTU 2·2차측 27℃ → 32.15/42.44℃ 가 5장 32/42℃ 와 일치)으로 바뀌었다. 세션 1-C에서 `model.py` 주석·출처만 교체했고 **ε 식·값은 건드리지 않았다** | M | 해소됨 — 계산은 처음부터 대향류였으므로 수치 변화 없음(전후 동일성으로 확인) |
| 13 | **[세션 5로 이월]** 2차측 유체 미정. 잠정적으로 1차측과 동일 냉각액 + 유량비 1:1 → Cr = 1 (프로젝트정리 5-1 에 "미정"으로 기록됨). 2차측이 블랙박스인 동안은 판정 불가(절대 규칙 7) | M | Cr 이 1이 아니면 ε 값이 바뀐다. **세션 5에서 확정** |
| 14 | **[확정 · bulk_mean]** 프로젝트정리 5-1 에 **수치처리 규약**으로 기록됨. `cp_rule` 인자는 재현·전환용으로 그대로 남겼다. 규칙별 잔차(bulk_mean 0.005% 통과 · supply 0.309% 실패 · return 0.315% 실패)도 5-1 에 기록됐다 | M | 해소됨 — 다만 규칙을 바꾸면 1-B 판정이 뒤집힌다는 사실은 계속 유효하다 |
| 15 | **[여전히 열림]** 랙·HX 를 **독립적으로 푼 뒤 대조**하는 검증이 아직 없다. 세션 2의 정상상태 대조(t→∞ vs 1-B)는 계산 *경로*는 다르지만 동적 모델의 평형점이 설계상 1-B 와 같은 방정식이라 **구현 일관성 검사**에 그친다 | M | "1-B·세션 2 통과"를 모델 전체의 보존성 검증으로 읽으면 과대해석이다 |
| 16 | **[해소]** 갱신본 `CLAUDE.md` 「명령어」 절 127행에 `.venv/Scripts/python.exe -m cdu_simul.model` 이 들어갔음을 확인했다 | S | 해소됨 |
