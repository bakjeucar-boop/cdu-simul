# 인수인계 — 2026-09-15 (세션 7.140 ~ 7.149)

만든 판: 세션 7.150(마무리 판 · 마크다운만). 이 문서는 **옮기기만 한다** — 출처는 전부
`PROCEED.md` 의 세션 7.140 ~ 7.149 판 로그이고, 새로 재거나 찾은 것은 없다. 새 판단이나
권고를 싣지 않는다. 미해결 항목은 번호와 제목까지만 적는다(본문은 `PROCEED.md`).
판 로그와 이 문서가 어긋나면 판 로그가 맞다.

## 1. 오늘 한 일

기록 커밋 해시는 판 로그에 「이 커밋」으로만 적혀 있어, 다음 판 로그의 「시작 HEAD」와
`git log` 로 댔다(7.149 는 7.150 시작 HEAD).

| 판 | 무엇을 한 판 | 고친 것 | 연 번호 · 닫은 번호 | 전수 | 커밋 |
|---|---|---|---|---|---|
| 7.140 | 확인 판 — 어제 판 아홉의 커밋 · `--web` 「다음 첫 작업」 절 · #61 · #39 본문 현황 · `project-overview.md` 두 목록 관계 · 정본 재업로드 상태를 쟀다 | 기록(`PROCEED.md`)만 | 없음 · 없음 | 없음(#101 예외) | `c349691` |
| 7.141 | 트래커 판 — 판 로그에만 있던 관측을 #61 · #39 행에 옮기고 새 번호 둘을 열었다. 사람의 결정 둘(리디렉션 범위 · 번호는 갈래로 묶어 연다)을 판 로그에 적었다 | 기록만 | #127 · #128 연다 · 없음 | 없음(#101 예외) | `dd0aa0a` |
| 7.142 | #95 조사 판 — 앞머리 트래커로 #129 를 열고, `DATASET_VERSION` 의 값 · 이력 · 후보 축 재료 · 「59열」 자리를 쟀다 | 기록만 | #129 연다 · 없음 | 없음(#101 예외) | `2a461fd`(A) · `3a84152` |
| 7.143 | #111 요약 문언 대조 조사 판 — 앞머리 트래커로 #95 · #96 · #61 행에 갱신, 요약 「전이 1,024」와 CSV 의 갈림 · caveat 138 의 원천을 쟀다 | 기록만 | 없음 · 없음 | 없음(#101 예외) | `42071b7`(A) · `74f2ab7` |
| 7.144 | 트래커 판 — 7.143 조사를 #114 · #102 · #96 · #104 네 행에 옮겼다 | 기록만 | 없음 · 없음 | 없음(#101 예외) | `ed5571d` |
| 7.145 | 고치는 판 — 요약 8줄을 「실린 수」와 「계획 수」로 갈라 적게 했다(사람 결정 2026-09-15). #104 행에 한 줄 | 코드(`dataset_report.py` `format_report()` 두 줄) · 기록 · 산출물 `results/dataset_summary.txt` 재생성(추적 밖) | 없음 · 없음 | **붙음** — 1,383 passed · 건너뜀 0(조각마다 한 번) | `21061f4`(A) · `4f7e537`(코드) · `3664824` |
| 7.146 | 고치는 판 — 「전량 9,472」 문언을 「계획 9,472」로 불렀다. #126 행에 7.145 실측 | 코드(`dataset_report.py:223`) · 시험(`test_session55_gates.py:35` · `:69`) · 기록 · 산출물 재생성(추적 밖) | 없음 · 없음 | **붙음** — 1,383 passed · 건너뜀 0(조각마다 한 번) | `9ee78e2`(A) · `16b0084`(코드) · `df9a02f`(시험) · `fd82af2` |
| 7.147 | 재는 판 — 수 없는 「전량」 13자리를 뜻별로 갈랐고 고칠 자리 0. #125 · #126 행에 한 줄씩 | 코드 · 시험 고친 것 없음 · 기록만 | 없음 · 없음 | **붙음** — 두 벌 · 벌마다 1,383 passed · 건너뜀 0 · 실패 0 | `9f2ed88`(A) · `644d5bf`(E) · `fe6bfee` |
| 7.148 | #126 판 — 정본 `CLAUDE.md` 에 조각 시간 판별 실측과 리디렉션 금지 범위를 실었다 | 정본(`CLAUDE.md`) · 기록 | 없음 · 없음 | 없음(#101 예외) | `d2df895`(정본) · `f82f09d` |
| 7.149 | 580 출처 찾기 판 — 「실측 580 s」의 잰 기록도 출처도 못 찾았고(판정 ㉡), `CLAUDE.md` 전경 상한 줄과 조각 시간 불릿을 고쳤다 | 정본(`CLAUDE.md`) · 기록 | 없음 · 없음 | 없음(#101 예외) | `effddbb`(정본) · `9736cc6` |

**오늘 열린 번호** — 셋이다.
- #127 필드 수와 「src 에서 안 읽힘」 수의 셈이 판마다 갈린다 — 세션 7.141
- #128 정본 project-overview.md 의 두 목록 관계를 적는 문단이 두 자리에서 서지 않는다 — 세션 7.141
- #129 massloss_gate.NOISE_THRESHOLD 주석이 원본으로 드는 줄번호가 원본 파일의 상수 줄과 어긋난다 — 세션 7.142(커밋 `2a461fd`)

**오늘 닫힌 번호** — 0 이다. 열린 항목은 45(7.140) → 47(7.141) → 48(7.142 이후)로 움직였다.

## 2. 지금 상태

- 열린 항목: **48**(`daily_brief.py --web` 머리 「열린 미해결 48건」 · 세션 7.150 시작 때).
- 마지막 전수 통과 근거: **세션 7.147 D** — 두 벌 · 벌마다 1,383건 passed · 건너뜀 0 · 실패 0 ·
  PC `BOOK-DC2VCGD99B`. 7.148 · 7.149 · 7.150 은 #101 예외로 전수를 돌리지 않았다.
- HEAD: 세션 7.150 시작 때 `9736cc6` = `origin/main` · 작업트리 깨끗. 이 문서는 그 뒤
  7.150 커밋에 들어간다(그 커밋의 해시는 이 문서에 적을 수 없다 — `git log` 로 본다).
- 산출물: `results/` · `dist/` 는 추적 밖이다. 7.145 · 7.146 이 이 PC 의
  `results/dataset_summary.txt` 를 다시 만들었고 **다른 PC 의 요약은 옛 문언인 채다**(7.145 F5 ·
  7.146 G2). `results/cdu_dataset.csv` 는 오늘 다시 만들지 않았다(수정시각 2026-09-14 08:53:15).

## 3. 정본 재업로드 상태

- **`CLAUDE.md` — 오늘 두 판에서 고쳤다(7.148 `d2df895` · 7.149 `effddbb`). 사람이 웹 대화창
  프로젝트 지식에 다시 올려야 한다.**
- `project-overview.md` · `AI서버_CDU_디지털트윈_프로젝트정리.md` · `collaboration.md` — 오늘
  고치지 않았다(각 판 로그 「정본을 고치지 않았다」 · 7.148 · 7.149 「나머지 정본 셋은 고치지 않았다」).
- 어제 세션 7.139 가 고친 `project-overview.md` 의 재업로드가 끝났는지는 7.140 이 알 수 없다고
  적었고, 오늘 어느 판도 확인하지 않았다.

## 4. 남은 것

가름은 판 로그에 적힌 것만 근거로 했다. ㉡ 은 「그 판이 표에 올리지 않았다고 적었고 뒤 판이
옮겼다는 기록이 없는 것」이다. 이미 어느 행에 실린 것(㉠)과 가르지 못한 것은
`PROCEED.md` 세션 7.150 판 로그 B2 에 있다.

### ㉡ 아직 어느 행에도 없다

- 7.143 E2 ⑶ — 1,024 가 `dataset.py:315` docstring 과 정본 프로젝트정리 152줄 「규모 선택」에
  전이를 포함한 계획 규모로 박혀 있고 산출물 규모 B(정상상태만)와 다르다(7.144 F2 「아직 판 로그에만」).
- 7.143 E2 ⑺ — #95 행은 칸이 둘뿐이다(크기 · 영향 칸 없음)(7.144 F2 「아직 판 로그에만」).
- 7.143 E3 둘째 — #102 의 「하나도 없다」를 `generate()` 경로까지 넣어 읽을지(사람이 정할 것 ·
  7.144 F3 「#102 에는 싣지 않았다」).
- 7.147 G2 — `dataset_plan.py` 두 자리(`:396` · `:425` 추정 생성 규모)를 사람 결정의 「계획 규모」
  잣대에 넣을지(7.147 「이 판이 정하지 않는다」 · 그 판 고친 자리에 없음).
- 7.147 G3 ⑵ — `dataset_plan.estimate_run` · `RunEstimate` 를 부르는 곳이 없고
  `count_by_configuration` 에 「샘」 축이 없어 계획 9,472 와 맞는 인자가 없다.
- 7.148 F3 ⑷ — 7.145 E 가 「첫 실행에」 `-p no:cacheprovider` 와 「조각마다 한 번만 쟀다」를 함께
  적어 둘째 실행이 있었는지 서지 않는다.
- 7.149 F2 ⑴ — `CLAUDE.md:203-204` 결정 머리 끝 문장이 이제 없는 불릿 문언을 인용한다.
- 7.149 F2 ⑵ — `CLAUDE.md:180-182` 「두 번 재고 큰 쪽을 쓴다」가 새 불릿 「「쓰는 값」을 하나
  고르지 않는다」와 나란히 읽힌다.
- 7.149 F3 ⑴ — 인용으로 남긴 옛 문언 「「10분」이 아니다」의 까닭이 7.36 ~ 7.38 판 로그에 없다.
- 7.149 F3 ⑵ — `docs/session757-review.md:147` 이 580 을 닫힘의 정의로 쓴다(정본 밖).
- 7.149 F3 ⑶ — #126 행 ⑵ 와 「현재 상태」 7.38 행이 「실측 (상한) 580 s」로 적는다(이력 문언).
- 7.149 F3 ⑷ — `4ead207` 제목에 세션 번호가 없다.

### ㉢ 다음 판이 재야 갈린다

- 7.140 F3 — 다른 PC 에서의 `--web` 출력.
- 7.142 G4 — 7.39 뒤 CSV 값이 판마다 얼마나 옮겨졌는지 · 310,016행(5.7) → 15,104행이 된 시점 ·
  생성 환경 축(㉧)의 바뀐 적 · 판본 무이동 · 「59열」 밖 표기(「열 59개」 등) · `DATASET_VERSION`
  동적 접근 · #96 ㉯ 를 센 세션 7.126 F 의 탐색 범위. (7.143 A 가 #95 · #96 에 이 가운데 무엇을
  실었는지는 판 로그로 낱낱이 서지 않는다.)
- 7.143 E4 — `dist/cdu-demo.html` · `dist/cdu-intro.html` 의 138 을 JSON 경로별로 가르기 ·
  `transient_rows()` 를 돌려 전이 행이 채우는 열 보기 · 요약이 적는 행 수와 CSV 15,104 대조 ·
  정본 프로젝트정리 152줄 1,024 의 이력 · `generate()` 동적 호출.
- 7.144 F3 — 7.143 줄번호를 지금 HEAD 에서 다시 보기 · 7.143 E2 ⑶ · ⑺ 이 닿을 행을 표에서 찾기.
- 7.147 G4 — `estimate_run` 이 `docs/` 나 과거 판에서 어떻게 쓰였는지 · 「전량」을 범위 밖(정본 4종 ·
  루트 파일 · `PROCEED.md`)에서 세기.
- 7.148 F2 ⑵ — `/dev/null` 리디렉션 항목이 규약 위반 목록에서 실제로 사라지는지(다음 판들이 올리지
  않는 것으로만 확인된다).
- 7.148 F4 — 세션 7.95 ~ 7.126 조각 시간 값을 판 로그와 대조하기.
- 7.149 F4 — 무시 파일(`results/` · `dist/` 등) 안의 580 · `PROCEED.md` 580 걸린 102줄을 줄 전문으로
  읽고 가르기 · 7.36 앞 판에서 상한을 다른 표기로 적었는지.

## 5. 규약에서 되풀이된 자리

오늘 판 로그 「규약을 비껴간 자리」(7.140 은 F5 · 7.141 은 G5)를 갈래로 묶어 센 것이다. 한 항목이
두 갈래에 걸치면 둘 다에 셌다. 새 규약은 만들지 않았다.

| 갈래 | 판 수 | 판 |
|---|---|---|
| `PROCEED.md` 전체에 범위 · 한도 · 줄 머리 고정 없이 건 찾기 | 10 | 7.140 · 7.141 · 7.142 · 7.143 · 7.144 · 7.145 · 7.146 · 7.147 · 7.148 · 7.149 |
| 대안 여러 갈래를 한 찾기에 묶음 | 7 | 7.141 · 7.142 · 7.143 · 7.144 · 7.147 · 7.148 · 7.149 |
| 미해결 행을 필요한 끝이나 둘레가 아니라 한 줄 통째로 읽음 | 6 | 7.141 · 7.142 · 7.143 · 7.144 · 7.145 · 7.149 |
| 판 로그를 필요한 범위보다 넓게 읽음 | 5 | 7.140 · 7.141 · 7.143 · 7.144 · 7.148 |
| 편집 도구 호출 실패 · 재시도(대상 문자열 불일치 · 중복 · 칸 구분자) | 4 | 7.144 · 7.145 · 7.146 · 7.147 |
| `PROCEED.md` 밖 파일을 셸로 봄(`grep` · `sed` · `cat` 등) | 4 | 7.142 · 7.143 · 7.147 · 7.148 |
| `/dev/null` 리디렉션 · 출력을 버리고 다시 돌림 | 3 | 7.140 · 7.142 · 7.144 |
| 같은 목적의 찾기가 헛돎 | 3 | 7.146 · 7.147 · 7.148 |
| `PROCEED.md` 를 셸로 뜸 | 2 | 7.141 · 7.142 |
| 판 로그 끝을 넘겨 읽음 | 2 | 7.147 · 7.148 |
| 읽기 도구 길이 초과로 거절 | 2 | 7.146 · 7.147 |
| 헛 셸 호출 · 헛셈 | 2 | 7.143 · 7.149 |
| 셸에서 파이썬으로 파일을 씀(스크래치) | 1 | 7.146 |
| 「명령어」 절과 다른 명령으로 조각 실행 | 1 | 7.145 |
| 그 밖 한 판씩 — 셸 변수 · 프로세스 치환 대조(7.145) · 스펙 목록 파이썬 실행(7.143) · 수를 잘못 옮겨 적음(7.149) | 각 1 | — |

## 6. 다음 판이 쓸 수 있는 재료

오늘 판 로그에 「다음에 붙일 자리」라는 이름으로 적은 것은 **없다.** 비슷한 이름의 한 자리(7.140 F2
「다음 트래커 판이 옮길 수 있게 된 것」)는 같은 날 7.141 이 옮겼다.

## 7. 다음에 무엇을 먼저 할지

이 문서는 고르지 않는다. **범위는 사람이 정한다.** 「현재 상태」 마지막 행의 「다음 첫 작업」도
「다음 첫 작업은 사람이 고른다 — 이 판은 범위를 정하지 않는다」다.
