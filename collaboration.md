# 일하는 방식 — CDU Feasibility Study

> 이 프로젝트는 **본 프로젝트 착수 전 파일럿**이다. 실제 설계사양 없이 가정치로
> feasibility만 확인하고, 파일럿 종료 조건은 `project-overview.md` 가 정본이다.
> 규칙·명령·금지사항은 `CLAUDE.md` 가 정본이다. **여기 옮겨 적지 않는다** — 가리키기만 한다.
> 이 문서를 포함한 4개 정본 문서는 저장소 루트에도 커밋된다(세션 1-A). 웹 프로젝트 지식과
> 두 벌이므로, 어긋나면 **저장소 사본이 맞다** (`CLAUDE.md` 「작업 시작 전 필독」 4).
> 기술 배경·설비구조·시나리오 가정치는 `AI서버_CDU_디지털트윈_프로젝트정리.md` 가 정본이다.
> 이 파일은 *누가 무엇을 맡고, 무엇을 믿을 만한 것으로 치는가* 만 적는다.

## 역할

| | 맡는 것 | 안 맡는 것 |
|---|---|---|
| **웹 대화창 (판단하는 쪽)** | 무엇을 검증할지 정한다 · 판을 연다 · 프롬프트를 쓴다 · 결과·수치를 검토한다 · 가정치 채택/기각 판단 · 문헌·설계자료 대조 | 코드를 쓰지 않는다. 시뮬레이션을 직접 돌리지 않는다 — **결과는 로컬에서 받아 검토한다** |
| **Claude Code (로컬, 이 저장소)** | 시뮬레이터 코드 · 테스트 · feasibility 검증 스크립트 · 문서 · 커밋 · 진행기록 | 가정치를 스스로 만들어내지 않는다(② 참조) · 단계(section 4)를 스스로 건너뛰지 않는다 |
| **사람 (사용자)** | 실제 설계데이터 확보 시 반영 판단 · 업계 자료 대조 · 최종 채택 판정 · (향후) 실측 대조 | — |

**설계데이터가 없는 지금 단계에서는 "그럴듯한 결과"만으로 다음 단계를 열지 않는다.**
`AI서버_CDU_디지털트윈_프로젝트정리.md` 6장의 feasibility 기준을 통과해야 다음 단계다.

## 하루의 모양

```
아침   git pull → `daily_brief.py` 출력(세션 1-A부터 존재)을 새 대화 첫 메시지에
       붙인다. 스크립트가 없던 시절 세션이면 `PROCEED.md` 맨 위 "현재 상태" 표를
       대신 붙인다
낮     판을 하나 연다 → 프롬프트 → 코드/검증 → 결과 검토 → 커밋(계층별로 나눈다)
저녁   PROCEED.md에 오늘 한 일 · 미해결 · 다음 세션 첫 작업 기록 → git push
```

**작업 PC는 여러 대일 수 있다.** GitHub를 사이에 두고 번갈아 쓴다.
시작 전 `git pull`, 종료 시 `git push`. 안 올리면 그 PC에만 남는다.

새 판은 언제나 **"이 판이 끝나면 무엇이 달라지는가"** 한 줄로 연다.
그 줄을 못 쓰면 아직 판이 아니다 — 범위가 안 정해진 것이다.

## 판단 원칙

**① 이 프로젝트의 결과는 "실측 검증"이 아니라 "feasibility 검증"이다.**
실측 데이터가 없으므로 "정답과 얼마나 가까운가"를 잴 수 없다. 대신 6장의 네 기준
(energy balance 오차, T_return 방향성, 수렴시간, 극단 케이스 발산 여부)으로 **모델이
붕괴하지 않고 물리적으로 말이 되는지**를 잰다. 이 기준을 통과했다고 "정확하다"고
말하지 않는다 — "무너지지 않는다"고만 말한다.

**② 가정치와 실측·설계값을 절대 섞지 않는다.** 이 프로젝트의 가장 큰 위험이다.
- 모든 파라미터는 출처가 붙는다 — `[가정값: 업계 공개자료 전형범위]` 또는
  `[설계값: 확보일자·출처]`. 출처 없는 숫자를 코드·문서·보고서 어디에도 적지 않는다.
- 가정값 표(5장)의 수치를 코드에 그대로 박아넣지 않는다 — **한 곳(assumptions 파일)**
  에만 두고 코드는 거기서 읽는다. 같은 숫자가 코드 여러 곳에 흩어지면, 나중에
  설계값으로 교체할 때 일부만 바뀌고 나머지가 낡은 채 남는다.
- 결과 그래프·리포트에 "시뮬레이션 결과"라고만 적고 그 밑에 깔린 가정치를 안 밝히면,
  다음에 보는 사람(자신 포함)이 실측인 줄 안다. **모든 산출물에 "가정값 기반 —
  실측 아님"을 붙인다.**

**③ 짐작을 문서에 사실처럼 적지 않는다.**
안 돌린 시나리오를 돌린 것처럼, 안 잰 수렴시간을 잰 것처럼 적지 않는다.
모르면 **"모른다 · 안 돌렸다"** 를 그대로 남긴다. 특히:
- fsolve/solve_ivp가 **수렴했는지 확인하지 않고** 결과를 썼다면 그 사실을 적는다
  (성공 플래그를 안 보고 넘어가는 것이 이런 종류 시뮬레이터에서 제일 흔한 사고다).
- "이 케이스는 될 것이다"는 판단이지 관측이 아니다. 돌려서 나온 수치만 결과로 적는다.

**④ 단일 진실.** 같은 물성치·가정치가 두 자리에 있으면 언젠가 갈린다.
- PG25 농도, 기준 온도, 정격유량 같은 값은 assumptions 파일 하나에서만 읽는다.
- CoolProp 유체 문자열(`INCOMP::MPG-25%` 등)을 여러 파일에 하드코딩하지 않는다.

**⑤ 발견은 목록으로 모은다.** 검증 중 이상한 결과가 보이면 **그 자리에서 고치지 않는다.**
`#번호 · 한 줄 · 크기(S/M/L) · 영향` 으로 목록에 올리고, 목록을 보고 다음 판을 고른다.

발견 한 건은 셋으로 적는다:
```
⑴ 관측                       무엇을 봤나 (수치·그래프 그대로)
⑵ 참이면 보여야 할 다른 값    그 관측이 물리적으로 참이면 같이 움직여야 하는 값
⑶ 어디를 고치라는 가설         파일·함수. 가설이라고 적는다
```

**⑥ 가설은 반례와 함께 든다.** 예: "발산은 압력방정식 초기값 문제다"라는 가설을 세웠으면,
초기값을 안 바꿔도 발산 안 하는 케이스가 있는지 같이 확인하고 적는다.

**⑦ 되돌릴 수 있게 남긴다.** 커밋은 계층별로 나눈다(`model:` · `test:` · `문서:`).
가정치를 설계값으로 교체할 때는 별도 커밋으로 — 모델 로직 변경과 섞지 않는다.

## 이 프로젝트에서 특히 조심할 결함 유형

이제 시작 단계라 아직 "반복해서 나온" 결함은 없다. 대신 이 시뮬레이터의 구조
(가정치 기반·수치해석·단계적 확장)상 **나올 가능성이 높은** 유형을 미리 적어둔다.
실제로 나오면 사례를 채워 넣는다.

**① 가정치가 실측처럼 보인다.** 5장 표의 숫자가 출처 표기 없이 코드/보고서에 등장.
→ 발견되면: 산출물 전체에서 "가정값" 표기가 빠진 곳을 훑는다.

**② 수렴 실패가 조용히 넘어간다.** `fsolve`/`solve_ivp`가 실패해도 마지막 값을 그대로
쓰면 결과가 있어 보이지만 의미가 없다. → 성공 플래그를 항상 확인하고, 실패 시
예외를 던지거나 명시적으로 표시한다.

**③ 같은 물성치가 다른 자리에서 다르게 정의된다.** 예: 1차측 ΔT를 어떤 함수는
32→42℃로, 다른 함수는 다른 기준온도로 계산. → 어휘는 assumptions 파일 하나에만.

**④ 시나리오 배치 실행에서 이전 상태가 넘어온다.** 다수 시나리오(정상/이상 — 「막힘」·「샘」)를
반복 실행할 때 이전 시나리오의 상태(초기값·캐시)가 다음 시나리오에 남는다.
→ 시나리오마다 초기조건을 명시적으로 리셋했는지 시험한다.

**⑤ balance 검증은 통과하는데 극단 케이스에서 무너진다.** 정상 부하 범위에서만
검증하고 "부하 0" · "부하 최대"를 안 돌려봤는데 통과라고 적는다.
→ 6장 기준의 "극단 케이스" 항목을 매번 실제로 돌린다.

## 프롬프트를 쓸 때 (웹 → Code)

```
1. 이 판이 끝나면 무엇이 달라지는가       ← 한 줄. 못 쓰면 아직 판이 아니다
2. 범위 — 건드릴 파일과 건드리지 않을 것   ← "하는 김에"를 막는다
3. 이번 판에서 쓰는 가정치는 어디서 온 것인가 (5장 표 그대로인지, 새로 도입하는지)
4. feasibility 기준 중 이번 판에서 확인할 것 / 아직 안 볼 것
5. 커밋을 몇 개로 나눌지
6. 사람이 밟아야 하는 것이 있으면 그것
```

돌아온 결과를 볼 때 묻는 것: **돌린 것인가 짐작인가 · 수렴했는가 · 어떤 가정치를
썼는가 · feasibility 기준 중 무엇을 통과했고 무엇을 안 봤는가.**

## 웹 대화창이 실제로 저지른 것

1~7일차 대화창 각각에 「너와 나 사이 소통에서 어긋난 자리」를 정리하게 해 여섯 벌을
모았고, 그중 **여러 날에 걸쳐 반복된 것**만 아래 다섯으로 추렸다. 하루만 나온 것은
넣지 않았다.

**① 볼 수 있는 것은 본다.** 첨부 그림 · 이전 버전 · 정본 문서 · 미해결 목록은 확인한
뒤에 쓴다. 「아마 이럴 것」을 프롬프트에 단정으로 적지 않고 「확인하고 보고하라」로
돌린다. 못 본 것은 못 봤다고 적는다. — 6일 중 5일
→ 실례: 세션 7.20 — 「이전 버전 10 절 형식으로」를 확인 없이 표로 짰다. 실제 형식은
가로 박스 셋이었다.

**② 프롬프트는 바깥 코드 블록 하나로만 낸다.** 그 안에 코드 블록·백틱을 쓰지 않는다.
명령어·경로는 들여쓰기로 표시한다. — 실제 전달 사고 1회
→ 실례: 세션 7.6 무렵 — 프롬프트 안에 코드 블록을 중첩해 앞부분만 전달되는 사고가 났다.

**③ 「거의」·「영향 없음」·「추가 파라미터 0개」를 쓰기 전에 그 주장이 깨지는 경우를
하나 찾아본다.** 못 찾았으면 못 찾았다고 적는다. — 4일
→ 실례: 세션 5 무렵 — 「두 CDU 가 거의 독립」·「새 숫자 0개」를 적고 다음 턴에 스스로
정정했다.

**④ 정할 수 있으면 권고로 낸다.** 수치 · 게이트 · 문서 범위 · 실질 비용(시간 ·
재생성) 중 하나가 걸릴 때만 확인을 받는다. 정본에 근거가 있으면 반영하고 한 줄로
밝힌다. — 4일
→ 실례: 세션 5.7 무렵 — 되돌리기 쉬운 문구·열 추가까지 확인 항목으로 올려, 사람이
「비용이 들지 않는 것을 확인받을 필요가 있나」로 지적했다.

**⑤ 결정을 청할 때는 「하면 무엇이 달라지고 안 하면 무엇이 남는가」를 함께 적는다.**
항목 이름만 올리지 않는다. — 2일
→ 실례: 세션 5.6-B 무렵 — 결정 항목 이름만 올리고 출력/미출력이 무엇을 바꾸는지 안
적어 사람이 다시 물었다.

## CLAUDE.md 에 있는 것 (여기서 되풀이하지 않는다)

절대 규칙(단위 규약 · 도구스택 · 가정값/설계값 분리 · 단계 게이트 · 2차측 경계조건 ·
Pyomo/IDAES 재도입 조건 · 환경 소유권 · 경로 하드코딩 금지 · 지시 범위),
개발 순서 게이트, 명령어, 세션 종료 시 절차(PROCEED.md·git push), 코드 스타일,
하지 말 것.
