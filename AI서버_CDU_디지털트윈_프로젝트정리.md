# AI 서버 CDU 디지털 트윈 구축 검토 — 프로젝트 정리 문서

> 이 문서는 **파일럿(feasibility study) 단계**의 기술 정본이다. 파일럿 종료 조건·
> 본 프로젝트 전환 기준은 `project-overview.md` 를 본다.

## 1\. 배경 및 목적

* AI/GPU 서버 고집적화에 따라 D2C(Direct-to-Chip) 액체냉각 CDU의 안정적 운영이 중요해지고 있음
* 기존 방식은 누출감지센서로 사고 발생 **이후** 감지하는 구조이며, 모든 배관·설비에 센서 설치가 어려워 미량 누출을 조기에 발견하기 힘듦
* CDU 디지털 트윈을 구축해 정상/이상/누출 시나리오를 가상으로 반복 시뮬레이션하고, 이를 AI 학습 데이터셋으로 활용하여 **누출 조기감지 및 이상상태 예측**에 활용하는 것이 목표
* 상위 배경: 데이터센터 냉각용 유기냉매(PG25 등) 적용이 초기 단계이며, 화학물질 위험성에 대한 사전 인지·완화 방안을 설계/운영에 반영하려는 기술개발과제와 연계

## 2\. 실제 설비 구조

* CDU 1대에 다수의 랙(Rack)이 병렬로 연결
* 이러한 CDU가 여러 대 반복 배치된 형태
* CDU들은 **공유 2차측(냉각탑/드라이쿨러 등)** 에 연결된 구조 → CDU 간 상호작용 존재
* 냉각방식은 D2C(밀폐회로 순환) 우선 검토 — 액침 대비 인체 직접영향은 적으나, 소형 배관 누수 시 즉각 확인이 어렵다는 점이 과제의 핵심 문제의식
* 현재 상세 설계데이터(펌프·밸브·열교환기 사양, 랙별 발열량 등) 미확보 상태

## 3\. 시뮬레이션 도구 검토 결과

### 검토한 조합

|조합|평가|
|-|-|
|IDAES + Pyomo + CoolProp|기술적으로 가능하나, CoolProp 물성함수를 Pyomo의 gradient 기반 solver(IPOPT 등)에서 쓰려면 미분가능한 형태(다항식 피팅 등)로 별도 변환 필요 — 구현 난도 높음. IDAES property package도 화학공정(EOS) 중심이라 PG25 같은 비압축성 냉각액용 커스텀 패키지 직접 작성 필요|
|**NumPy + SciPy + CoolProp + Pandas**|CoolProp을 직접 호출 가능(미분가능성 문제 없음), `solve\_ivp` 등으로 동특성 표현이 자연스러움, Pandas로 다수 시나리오 배치 실행·데이터셋화에 유리 → **현재 목적(상관관계 도출용 시뮬레이터)에 더 적합**하다고 판단|

### 결론

* 채택: **Python + NumPy + SciPy + CoolProp + Pandas**
* 사내 보유 HYSYS는 정상상태/동적 모듈 활용 시 물성·미분가능성 문제가 없어 교차검증용으로 병행 고려 가능
* 최적화(설계변수 자동탐색)가 필요해지는 단계가 오면 Pyomo/IDAES 재검토 여지 있음

## 4\. 단계적 확장 전략

1. **단일 CDU 모델 구축** (현재 단계) — 2차측은 고정 경계조건(블랙박스)으로 단순화
2. 랙 개수 확장 — 헤더 압력평형 기반 유량분배 로직 추가
3. 누출 시나리오 주입 — 배관 저항 변화/유량 손실로 근사 모사
4. 다중 CDU 확장 — 공유 2차측 결합 시 상위 레벨 연립방정식으로 CDU 간 연동 (2차측 공유 구조이므로 완전 독립 병렬화 불가, 계층적/모듈형 구조 필요)

### 단일 CDU 모델 구조(안)

```
CDUModel
 ├─ params: pump\_curve, valve\_Cv, HX\_UA, pipe\_K, n\_racks, rack\_loads
 ├─ state: \[T\_supply, T\_return, m\_dot, P\_pump\_out, ...]
 ├─ compute\_pressure\_balance()   # 유량분배: fsolve로 압력강하 일치조건 해석
 ├─ compute\_rack\_heat(m\_dot\_i, load\_i)   # CoolProp cp 활용
 ├─ compute\_HX\_duty(T\_return, T\_secondary)   # ε-NTU법
 └─ step(t, y)   # solve\_ivp용 미분방정식 (온도 동특성)
```

* 압력-유량: 매 시점 정상상태 대수방정식으로 해석(quasi-steady)
* 온도: 열용량 기반 시간 적분 (관성 시간상수 차이를 고려한 하이브리드 방식)

## 5\. 가상 시나리오 가정치 (설계데이터 미확보 — feasibility 검증용 가정값)

> 아래 수치는 업계 공개자료 기반의 전형적 범위이며, 실측·설계값이 아님. 실제 설계데이터 확보 시 교체 예정.

**시나리오 조건**

|항목|가정값|
|-|-|
|데이터센터 유형|AI 트레이닝/추론 GPU 클러스터|
|랙당 구성|8-GPU 서버 다수 (H100급 상당)|
|랙당 IT 발열량|80 kW|
|CDU당 연결 랙 수|8개|
|CDU당 총 발열량|640 kW (여유율 20% 감안 CDU 정격 \~750 kW)|
|냉각액|PG25 (25wt% 프로필렌글리콜 수용액, CoolProp `INCOMP::MPG-25%`)|
|1차측 공급/환수 온도|32℃ / 42℃ (ΔT 10℃)|
|2차측 공급온도(경계조건)|27\~30℃ 고정|

**주요 구성요소**

|구성요소|가정치|
|-|-|
|펌프|정격유량 약 15.5 L/s(930 L/min), 정격양정 20\~30 mAq, 특성곡선 H=H₀-aQ-bQ²|
|밸브(랙별)|랙당 정격유량 약 1.94 L/s, 정격개도 80%에서 ΔP 3\~5 mAq 되도록 Cv 역산|
|열교환기|정격용량 750 kW, ε-NTU법(NTU 2\~3), 1차:2차 유량비 1:1 가정|
|배관(1차측)|랙 분기 25A(1"), CDU 헤더 65\~80A, 등가길이 20\~30m, 랙당 ΔP 2\~3 mAq|
|부하 프로파일|유휴 20% \~ 정격 100% 스텝/랜덤 변화|
|누출 시나리오|배관 K값 정상 대비 +5%(미량)/+20%(중간)/+50%(대규모)|

### 5-1. 파생 가정치 (파일럿 진행 중 확정)

> 위 표에 없어서 모델 구현 중 필요해진 항목을 **사람이 확정한 뒤** 여기 기록한다.
> 코드가 임의로 만들지 않는다(`CLAUDE.md` 절대 규칙 1). 이 소절도 5장의 일부이며,
> 값은 `assumptions.py` 한 곳에서만 읽는다(절대 규칙 2).

> **5-1 빈칸 처리 순서.** 5장 표에 없는 값이 필요해지면 아래 순서로 처리하고, 어느
> 방식을 썼는지 근거란에 밝힌다. 설계데이터를 기다리는 선택지는 없다 — 이 파일럿은
> 데이터가 오지 않는 것을 전제로 한다.
> 1. **역산** — 5장의 다른 조건에서 유도되는가. 그렇다면 새 가정이 아니다
>    (예: 밸브 Cv · 배관 K · 계통 잔여저항)
> 2. **규격·정의값** — 규격표나 물리 상수에서 나오는가 (예: 배관 내경 · mAq)
> 3. **범위 양 끝** — 5장이 범위로 주면 중점을 고르지 않고 둘 다 돌린다 (방침 B)
> 4. **추가 파라미터 0의 규약** — 위 셋이 안 되면, 새 숫자를 가장 적게 요구하는 쪽을
>    택하고 **「물리 가정이 아니라 규약」으로 등급을 낮춰** 기록한다. 재검토 시점을
>    함께 적는다 (예: cp 벌크평균 · 밸브 선형 · M 노드 배분)
>
> 넷 다 안 되면 그때는 사람에게 확인한다.

|항목|확정값|근거|확정 시점|
|-|-|-|-|
|열교환기 유동배열|**대향류**|대향류·NTU 2·2차측 27℃ 조건에서 계산된 1차측 온도가 **32.17/42.47℃** 로 나와 위 표의 1차측 32/42℃와 일치했다 (세션 5-B 재계산 — Cr 유도 전환 반영. 세션 1-B 당시 Cr=1 선언으로 계산한 값은 32.15/42.44℃ 였고, 전환 후에도 5장 표와 정합한다). 5장 표가 대향류를 상정해 만들어진 것으로 읽는다 — 새 가정 도입이 아니라 표에서 역으로 읽어낸 것|세션 1-B|
|cp·ρ 평가 온도|**1차측 벌크 평균온도** (T_supply+T_return)/2|흡열량의 참값은 ∫cp(T)dT이고 PG25의 cp는 이 온도대에서 거의 선형이므로, 중점법(=벌크 평균온도의 cp)이 계통 편향 없이 가장 가깝다. 공급·환수 한쪽에서 평가하면 치우친다. **물리 가정이 아니라 수치처리 규약**이다|세션 1-B|
|수력 계산의 물성 평가 온도|**1차측 벌크 평균온도** — cp·ρ 평가 규칙(위 행)을 수력에도 그대로 적용한다. 밀도는 매 시점 그 온도에서 다시 얻는다|`CLAUDE.md` 규칙 4가 압력-유량을 "매 시점 quasi-steady"로 규정하므로 물성만 고정하면 그 구조와 어긋난다. 기존 cp·ρ 규칙을 그대로 쓰는 것이라 **추가 파라미터가 0**이다. 공급측·환수측 중 하나를 고르면 5장에 없는 선택이 된다. **물리 가정이 아니라 수치처리 규약**이다|세션 3-A2|
|2차측 유체|**1차측과 동일한 PG25** (INCOMP::MPG-25%). **Cr 은 선언하지 않고 물성에서 유도한다** — 5장 「1차:2차 유량비 1:1」을 **부피유량 기준**으로 읽고, 2차측 부피유량을 1차측 **정격** 15.5 L/s 로 고정한다|5장이 2차측 유체를 지정하지 않는다. 동일 유체가 추가 파라미터 0 이다. **세션 5 C4 가 드러낸 것**: 이전 기록의 "유량비 1:1과 합쳐 Cr=1"은 유도가 아니라 선언이었다. 부피유량이 같아도 1차측(벌크 37℃)과 2차측(27~30℃)의 ρ·cp 가 달라 Cr≈0.9986 이 된다. 5장이 유량을 전부 L/s 로 쓰므로 「1:1」을 부피로 읽는 것이 가장 충실하고, 열용량유량으로 읽으려면 5장에 없는 개념이 필요하다. **정격 고정**은 운전점 추종이 5장에 없는 처리이기 때문이며, 다중 CDU 대칭 배분이 단일 CDU 를 정확히 재현하게 만든다. **한계: 이 전환으로 세션 1-B~5 의 기록 수치가 소급 이동했다 — 세션 5-B 이전 로그의 숫자를 이후 것과 나란히 읽지 않는다**|세션 5-B|
|물성 조회 기준 압력|**표준대기압 101325 Pa** (확정)|물리 상수 정의값이며 가정값·설계값이 아니다. 비압축성 냉각액이라 영향이 작을 것으로 보이나 확인하지 않았다 — 계통 압력이 실제로 등장하는 **세션 3에서 재검토**. **재검토 기한을 「세션 3」으로 적었으나 3-A·3-A2·3-B 어디서도 다루지 않은 채 지나갔다. 세션 5에서 코드로 확인했다**: 계통 압력(펌프 양정 20~30 mAq = 196,133~294,200 Pa)은 대기압 대비 **193.6%~290.4%** 로, 당초 짐작한 「2~3%」가 아니라 두 자릿수 큰 값이다. 그럼에도 CoolProp INCOMP 백엔드에서 **ρ·cp 는 압력에 정확히 무관**하고(양 끝에서 0.0000% 변화), energy balance 가 쓰는 **엔탈피 차**만 −0.0138% 이동한다(현재 잔차 0.005%와 같은 자릿수이며 6장 기준 0.1%의 1/7). **확정**한다 — 압력 의존을 모델에 넣지 않는다. **한계: 압력이 실제로 물성에 걸리는 유체(2상·기체)로 바뀌면 이 확정은 무효다**|세션 5|
|배관 내경|**ASME B36.10M Sch40 규격값** — 25A(NPS 1) 26.64 mm · 65A(NPS 2½) 62.71 mm · 80A(NPS 3) 77.92 mm|5장 배관 항목이 호칭경만 적고 내경이 없어 계통 보유수량(열용량 M) 산출이 불가능했다. 호칭경→내경은 배관 규격표의 **정의값**이며 새 가정치가 아니다. KS D 3507 계열(25A 내경 27.5 mm)을 쓰면 약 7% 커지고 시간상수가 그만큼 이동한다 — 실제 배관 재질·규격 계열은 설계데이터 확보 시 확정|세션 2|
|계통 보유수량 M (열용량)|5장 등가길이 20~30 m를 **랙 1개 회로의 왕복 전체 등가길이**로 읽는다. 구경 배분(25A 대 헤더)이 5장에 없으므로 **전부 25A(하한) / 전부 80A(상한)** 두 극단으로 M의 범위를 낸다|배분을 특정하면 5장에 없는 숫자를 만드는 것이 된다. 범위값 방침 (B)(양 끝을 둘 다 돌린다)를 그대로 적용한다. **한계: 열교환기·CDU 내부 보유량과 랙 콜드플레이트 보유량이 5장에 없어 M에서 빠진다 — 실제보다 M이 작고 수렴시간도 실제보다 짧게 나온다.** 이 사실 없이 수렴시간을 "합리적"이라고 읽지 않는다|세션 2|
|압력 단위 규약 mAq|**1 mAq = 9806.65 Pa** (수두 환산 압력 단위로 읽는다)|5장이 mAq·L/s·℃를 섞어 쓰는데 mAq를 PG25 액주 높이로 읽으면 값이 1.3% 달라진다. 배관 실무에서 mAq는 통상 수두 환산 압력이므로 그쪽을 택했다. **물리 가정이 아니라 단위 규약**이다|세션 3|
|펌프 특성곡선 계수 H₀·a·b|양정 하한 20 mAq → **H₀=22.40 · a=0.003819 · b=0.009743** / 상한 30 mAq → **H₀=33.60 · a=0.005729 · b=0.014615** (단위: mAq, Q는 L/s)|곡선 **형상만** 타 프로젝트 사례(4 kW 인라인·3550 rpm·정격 210 L/min·25 m·체절 28 m)에서 차용하고, 정격점은 5장 값(15.5 L/s · 20~30 mAq)으로 스케일했다. 무차원 형상 H/H정격 = 1.12 − 0.00296q − 0.11704q² (q=Q/Q정격). **한계: 차용 곡선은 물 기준이며 PG25 점도 영향 미반영. 원 펌프와 용량이 4.4배 차이 나 비속도가 달라 실제 형상은 다를 수 있다.** `[가정값]` — 설계데이터 확보 시 교체|세션 3|
|밸브 개도 특성|**선형** Kv(x) = Kv_max · x|등비율(equal-percentage)이 실제 제어밸브에 더 흔하나 rangeability R이라는 **새 숫자가 하나 더 필요**하고 그것을 정할 근거가 없다. 선형은 추가 파라미터가 0이다. 개도가 80% 고정인 동안은 어느 쪽이든 결과가 같으므로, **개도가 변수가 되는 시점(세션 4)에 재검토**한다|세션 3|
|밸브 Kv (개도 100% 기준)|**역산 규칙**: 5장 조건(랙당 1.94 L/s · 개도 80% · ΔP 3~5 mAq)에 Kv = Q√(SG/ΔP) 를 적용해 80% 값을 구하고, 선형 개도 특성으로 100%로 환산한다. **SG 는 CoolProp 물성에서 얻는다**(1차측 벌크평균 37℃ · 기준밀도 1000 kg/m³). Kv 는 기기 특성이므로 이 정격 물성에서 **한 번 역산한 뒤 고정**한다 — 온도에 따라 변하는 것은 Kv 가 아니라 그것으로 계산한 ΔP 다. 참고값(코드 산출): ΔP 3 mAq → Kv≈16.18 / 5 mAq → Kv≈12.53 m³/h. **이 숫자를 코드에 박지 않는다** — 코드가 규칙대로 재산출한다|5장 조건의 역산이며 새 가정치가 아니다. 세션 3 에서 SG 를 숫자로 적었다가 CoolProp 값과 0.09% 갈렸다 — 물성은 한 출처에서만 읽는다(`collaboration.md` ③·④)|세션 3-A2|
|배관 K값 (랙 분기)|**역산 규칙**: 5장 "랙당 ΔP 2~3 mAq @ 1.94 L/s" 와 5-1 배관 내경(25A)으로 ΔP = K·ρv²/2 를 역산한다. **ρ 는 CoolProp 에서 얻는다**(1차측 벌크평균 37℃). K 는 무차원 형상 특성이므로 정격 물성에서 **한 번 역산한 뒤 고정**한다. 참고값(코드 산출): K≈3.20~4.80. **이 숫자를 코드에 박지 않는다**. 누출 시나리오의 K값 +5/+20/+50%가 이 값에 걸린다. **관측: 25A에 1.94 L/s는 유속 3.48 m/s로 냉각수 통상 설계유속(2~3 m/s)보다 높다 — 설계데이터 확보 시 랙 분기 구경이 커질 가능성이 있고, 그러면 K와 M이 함께 이동한다**|위와 같다|세션 3-A2|
|랙별 등가길이 배분|**5장 등가길이 20~30 m 를 랙 8개의 공간 분포로 읽는다** — 헤더에서 가장 가까운 랙 **20 m**, 가장 먼 랙 **30 m**, 사이 6랙은 **균등 배분**(간격 10/7 ≈ 1.4286 m). 랙 분기 K 는 이 등가길이에 **비례**하며, 역산 규칙(위 「배관 K값 (랙 분기)」)은 그대로 두고 **8랙 평균 등가길이(25 m)를 기준으로 스케일**한다 → 랙별 K 배수 = L_i / 25 m = **0.800 ~ 1.200**. 밸브 Kv·잔여저항·헤더 처리는 건드리지 않는다. **기본값은 균등(전 랙 동일)이며 랙별 배분은 명시적으로 켤 때만 적용된다** — 기존 시험·데이터셋·게이트 기록은 균등 그대로다|**5장 범위 안에서 닫히므로 새 숫자가 0 이다** — 범위의 양 끝(20·30 m)을 그대로 쓰고 사이를 균등 배분할 뿐이며 범위 밖으로 나가지 않는다. 실제 데이터센터에서 헤더로부터의 거리가 랙마다 다르다는 물리를 반영한다. **평균 기준으로 스케일하는 이유**: 8랙 평균 K 가 오늘의 K 와 같아 **평균 K 가 보존**되고 바뀌는 것은 랙 간 배분뿐이다. **계통 전체 저항이 보존되는 것은 아니다** — 8랙은 병렬이고 병렬 합성은 K 에 비선형(각 다리가 Q ∝ 1/√K)이라 총유량이 균등 15.505582 L/s → 배분 15.509148 L/s 로 **+0.023%** 이동한다(세션 7.2 실측 · single/L100/R0). 다른 랙을 기준으로 잡으면 정격점 이동이 두 자리 커지므로(랙0 기준이면 평균 K 가 1.25배) 평균 기준이 5장 「랙당 ΔP 2~3 mAq @ 1.94 L/s」에서 **가장 적게 벗어나는 선택**이다. 문구 정정 세션 7.2-B(미해결 #44 닫음). **범위의 뜻을 「불확실성」에서 「공간 분포」로 바꾸는 것이므로 시나리오 정의 변경이며 사람이 확정했다(2026-09-01).** **한계**: 같은 범위를 5-1 「계통 보유수량 M」 항목이 방침 B(양 끝을 둘 다 돌린다)로 쓰고 있어 두 항목이 같은 범위를 다르게 읽는다. **이 판은 M 을 건드리지 않는다** — 정상상태 해가 M 에 불변이기 때문이다(세션 5.7). 두 항목의 읽기를 맞추는 것은 전이 시간축을 다시 세울 때(미해결 #40) 한다|세션 7.2|
|계통 보유수량 M의 노드 배분|**공급 노드 50% · 환수 노드 50%**|5-1의 M 규칙이 등가길이를 "왕복 전체"로 읽으므로 왕복은 두 다리이고, 비대칭 배분은 5장에 없는 배분비를 새로 요구한다. 50:50은 추가 파라미터가 0인 유일한 배분이다. **물리 가정이 아니라 수치처리 규약**이다. 배분비는 정상상태 해에 영향하지 않고 전이 파형에만 영향한다 — 수렴시간이 이미 판정 불가이므로 현재 게이트를 바꾸지 않는다. 누출 신호가 전이 파형에 걸리는 **세션 4에서 민감도를 확인**한다|세션 3|
|계통 잔여저항 (HX 1차측 + CDU 내부배관 + 헤더)|5장의 정격유량 15.5 L/s와 정격양정을 "시스템곡선이 펌프곡선을 만나는 점"으로 읽고, **잔여저항 = H정격 − (랙 분기 ΔP + 밸브 ΔP)** 를 총유량 경로의 **집중저항 하나**로 배정한다. 헤더는 저항 0의 공통 노드로 본다. 잔여저항은 독립 범위가 아니라 각 조합의 **종속값**이다 — 양정·분기ΔP·밸브ΔP가 정해지면 잔여분도 정해지므로 조합 수가 늘지 않는다|5장의 저항(랙 분기 2~3 + 밸브 3~5 = 5~8 mAq)만으로는 시스템곡선이 펌프 정격점에서 닫히지 않아 운전점이 정격의 1.5~2배로 나온다. 5장의 두 조건이 서로 모순되지 않게 강제하는 역산이며 새 가정치가 아니다. 정체는 5장에 개별 값이 없어 분해하지 않는다. **한계: 잔여저항이 총양정의 60~83%를 차지한다 — 세션 3의 유량분배 결과는 사실상 이 배정 방식이 지배한다. 수렴·비발산 게이트 통과를 "유량분배 값이 타당하다"로 읽지 않는다.** 잔여저항이 랙 공통 경로에 있으므로 정상상태 랙 간 유량은 균등해지고, 랙 간 상호작용은 **펌프곡선을 통해서만** 생긴다|세션 3|
|계통 잔여저항의 물성 의존|**ρ 에 비례한다.** 분기 ΔP·밸브 ΔP 와 같은 온도 스케일을 따른다. 계수는 정격 물성(37℃)에서 역산해 고정하고, ΔP 계산 시 그때의 ρ 로 스케일한다|5-1 이 잔여저항을 「집중저항」으로 규정했고 집중저항의 물리는 K·ρv²/2 다. 계수를 mAq/(L/s)² 로 고정하면 세 저항 중 잔여저항만 온도에 얼어붙는데, 그것이 총양정의 60~83%를 차지한다(세션 3-A2 관측 · 미해결 #30). 나머지 두 저항이 이미 따르는 물리를 세 번째에 적용하는 것이라 **추가 파라미터가 0**이다|세션 3-B|
|펌프곡선의 물성 의존|**없음** — H₀·a·b 로 계산한 양정을 ρ 로 보정하지 않는다|5-1 mAq 규약이 mAq 를 PG25 액주 높이가 아니라 **수두 환산 압력**으로 읽기로 했다. 그 규약대로면 5장의 20~30 mAq 는 압력으로 직접 주어진 값이고, ρ 보정은 "물 기준 곡선을 PG25 로 환산한다"는 **5장에 없는 새 처리**가 된다. 5-1 펌프 항목이 이미 "차용 곡선은 물 기준·PG25 점도 영향 미반영"을 한계로 적었으므로 그 한계 안에 둔다|세션 3-B|
|부하 프로파일의 랙 배분|**CDU 전체 일괄** — 8랙에 동일 부하. 5장 "유휴 20%~정격 100%"를 CDU 전체 비율로 읽는다|랙별로 다르게 주려면 5장에 없는 배분비가 필요하다. 일괄은 추가 파라미터가 0이다. **물리 가정이 아니라 규약**이다. 랙 간 비대칭은 누출 K값이 들어오는 **세션 4에서 처음 생기며**, 랙별 부하 분산이 필요해지면 그때 재검토한다|세션 3-B|
|펌프 운전 방식|**정속** — RPM 을 모델링하지 않는다. 5-1 펌프 곡선 하나만 쓴다|5장 펌프 항목이 특성곡선 **하나**만 주고 제어 로직(정압·정유량·온도 추종)이나 목표값을 말하지 않는다. 가변속으로 두려면 제어 대상과 목표값이라는 새 숫자가 최소 둘 필요하다. 곡선이 하나라는 것을 정속으로 읽는 것이 5장에 가장 충실하고 **추가 파라미터가 0**이다. 신호는 사라지지 않는다 — 누출 시 K 증가 → 총유량 감소 → 운전점이 곡선을 따라 이동해 **양정이 오른다**. `CLAUDE.md` 세션 4 게이트의 「펌프RPM변화」를 「펌프 운전점 이동」으로 바꾼 근거다|세션 4|
|누출 주입 지점|**랙 1개**에 주입한다. **랙별 등가길이 배분을 켠 경우 가장 먼 랙(랙7)에 주입한다.** 균등 배분(기본값)에서는 8랙이 전부 동일하므로 어느 랙인지 결과에 영향하지 않는다 — 52열본 데이터셋은 균등 경로이므로 랙0 그대로다|5장 누출 항목이 "배관 K값 정상 대비 +5/+20/+50%"라고만 적고 몇 개 랙인지 말하지 않는다. 이 과제의 목적이 **미량 누출 조기감지**(1장)이므로 국소 누출이 그 목적에 맞고, 랙 대칭성 때문에 랙 번호를 고르는 것이 새 숫자를 만들지 않는다. **물리 가정이 아니라 시나리오 정의**다. 전 랙 동시 누출은 5장에 근거가 없어 이번 판에서 돌리지 않는다 **랙별 배분을 켜면 랙 대칭이 깨지므로 위 근거(「어느 랙인지 영향하지 않는다」)가 성립하지 않는다(세션 7.2-B).** 가장 가까운 랙(랙0 · K 배수 0.8)에 +50% 를 걸면 0.8 × 1.5 = 1.2 로 정상인 가장 먼 랙(랙7 · 배수 1.2)과 **완전히 겹친다** — 실측 single/L100/R50 에서 두 랙의 유량 차 −9.1e-13 kg/h · 출구온도 차 0.000 K 로 부동소수점까지 같았다(미해결 #45). 가장 먼 랙에 걸면 1.2 × 1.5 = 1.8 이라 어느 랙과도 겹치지 않으므로 **배분을 켠 경우 주입 랙을 랙7 로 정한다** — 랙 개수에서 유도한 것이라 새 숫자가 아니다. **한계**: 배분을 켜면 **랙 간 비교만으로 저항 걸린 랙을 찾는 판독은 일반적으로 성립하지 않는다** — 랙7 주입이 겹치지 않는 것은 이 배수 조합에서 그런 것일 뿐 판독법이 성립한다는 뜻이 아니다. **같은 랙의 기준 대비 신호는 유효하다**(세션 4 게이트 판정도 랙 간 비교가 아니라 정상 대비 비교였다). **균등 경로(52열본 데이터셋)는 랙 대칭이 성립하므로 이 변경의 영향을 받지 않는다.** **한계 (세션 5.6·5.6-B).** 이 K값 근사는 누출을 **배관 저항 변화로** 모사하며, 그 범위 안에서 신호는 일관되게 나온다(세션 4 게이트 96건 전수 통과 — 이 판정은 유효하다). 다만 **질량이 보존되는 모사**라 실제 누출(질량손실)과 다음이 다르다: ⑴ 총유량·누출랙 통과유량의 **부호가 정반대**다 ⑵ 잔여저항 배치(#24)에 따라 수력 흔적이 **전혀 남지 않는 경우**가 있다(g=0/펌프=공급, 다섯 양 전부 정확히 0) ⑶ 실제 누출의 **공급−환수 유량 불일치**는 배치 자유도에 강인하나(배치 6 편차 1.776e-15 L/s · 최대 1.211%) K 근사에는 **원리적으로 존재할 수 없다**. **데이터셋의 누출 신호를 실제 누출의 방향으로 그대로 읽지 않는다.** 어느 모사가 실제에 가까운지는 재지 않았다(실측 없음). 미해결 #36.|세션 4 · 배분 시 랙7 은 세션 7.2-B|
|보충수 처리|**보충수를 모델링하지 않는다 — 조기감지 시나리오에 정의상 등장하지 않는다**|질량손실 누출 모사에 **보충수 온도는 필요하지 않다.** 이 과제의 목적은 **미량 누출 조기감지**(1장)이고, 미량 구간에서는 밀폐루프 보충 로직이 작동하지 않는다. 보충이 돌 만큼 커진 누출은 이미 조기감지 구간이 아니다. 따라서 조기감지 시나리오에서 보충수는 **정의상 등장하지 않는다** — 새 가정치가 아니라 1장 목적과 5장 누출 크기에서 따라 나오는 **역산**이다(5-1 빈칸 처리 순서 1번). **한계: 그 대신 계통이 시간에 따라 줄어든다. 계통 정압 강하를 누출 신호로 쓰려면 팽창탱크 용적·초기 충전압이라는 5장 밖 숫자 둘이 필요하며, 설계데이터 확보 시 재검토한다. 누출을 푸는 것 자체에는 필요 없다 — 세션 5.6 이 계통 압력 없이 272 회를 수렴시켰다.**|세션 5.7-C|
|CDU 대수|**2대**|5장이 "여러 대 반복 배치"라고만 적고 수를 주지 않는다. 세션 5 게이트가 「CDU 간 연동 수렴」이므로 2대가 연동이 성립하는 최소 수이고 새 숫자를 가장 적게 만든다. 3대 이상은 배치·부하 비대칭 등 5장에 없는 선택을 부른다. **시나리오 정의이지 물리 가정이 아니다**|세션 5|
|공유 2차측 결합 방식|**병렬 · 총 2차측 유량 고정 · 각 CDU 배분은 1차측 유량에 비례**. 총 2차측 유량 = 2 × 정격 1차측 유량|5장 「1차:2차 유량비 1:1」에서 총유량이 유도되므로 새 숫자가 아니다. 비례 배분은 추가 파라미터가 0이고, 두 CDU가 같으면 각자 정확히 1:1을 받아 단일 CDU 결과가 그대로 재현된다. **총합이 고정이라 두 CDU가 경쟁하고, 이것이 연동의 실체다** — 한쪽 부하가 오르면 온도·밀도가 변해 1차측 유량이 이동하고 공유 2차측 배분이 따라 이동한다|세션 5|
|2차측 공급온도|**고정 유지** (5장 27~30℃). 냉각탑·드라이쿨러를 모델링하지 않는다|온도 결합(한 CDU의 방열이 다른 CDU의 공급온도를 올린다)을 넣으려면 냉각탑 용량이나 접근온도라는 **새 숫자가 최소 둘** 필요하고 5장에 없다. **한계: 그 결과 CDU 간 연동은 유량 경로로만 생기고 열 경로로는 생기지 않는다 — 2장이 말하는 "CDU 간 상호작용"의 일부만 다룬 것이다. 연동이 작게 나오더라도 "상호작용이 작다"로 읽지 않는다**|세션 5|
|데이터셋 스키마 규약|**행 단위 = (시나리오 × CDU × 시각) 하나당 한 행.** 랙별 값은 행으로 펴지 않고 **열로 편다**(rack0..rack7). **전 행에** 출처와 한계 표기를 반복한다 — 「가정값 기반 — 실측 아님」·가정치 출처·총유량/양정 부호 불확실·전이 시간축의 M 배분 의존·Cr 유도값 표기. solver 플래그를 전 행에 담고 **실패 행을 버리지 않는다**. **열 목록은 코드가 정본이다** — 이 표에 나열하지 않는다|랙별 값을 행으로 펴면 CDU 총계가 8번 중복된다. 표기를 전 행에 두는 것은 `CLAUDE.md` 세션 5.5 게이트 문언이 「전 행에」이기 때문이고, 데이터셋이 조각나 유통돼도 표기가 따라가게 하려는 것이다(별도 메타 파일로 빼면 30% 작지만 CSV 한 장만 떼어 보면 표기가 사라진다). 실패 행을 버리면 데이터셋이 성공만 담아 편향된다. **규모 선택**: 정상상태 전수 768 + 전이 1,024(2대 대칭 제외) · 부하는 5장 양 끝 2수준(5장 밖 숫자 0개) · 누출 랙 0 고정. **중복 4건을 뺀 결과다** — ① 누출 랙 번호는 8랙 대칭이라 결과 불변(5-1 「누출 주입 지점」) ② 2대 대칭은 단일 CDU 를 정확히 재현(세션 5-B: 편차 0.000e+00 K)하므로 전이에서 제외 ③ 정상상태 해는 M 에 의존하지 않아 정상상태에 M 축을 두면 전부 중복 ④ 단일 CDU 에 「비대칭」은 성립하지 않는다. **근거 수치(세션 5.5-A 실측)**: 케이스당 정상상태 단일 0.0068 s · 2대 결합 0.0485 s · 전이 단일 0.3095 s · 전이 2대 결합 3.88 s. 전수(전이 표본 4001점)는 1.16시간·8.75 GB 이고, 위 규모는 약 36분·0.26 GB 다|세션 5.5-B|

> **cp 평가 규칙이 세션 1-B 게이트 판정을 갈랐다**: bulk_mean 0.005%(통과) ·
> supply 0.309%(실패) · return 0.315%(실패). 규칙을 바꾸면 판정이 뒤집힌다는 사실을
> 기록으로 남긴다 — 통과를 "모델이 물리적으로 옳다"로 읽지 않기 위한 단서다.

## 6\. Feasibility 검증 기준 (실측 없이 확인 가능한 것)

* 에너지 balance 오차 <0.1% 수준인지
* 부하 증가 시 T\_return이 물리적으로 타당한 방향(상승)으로 반응하는지
* 정상상태 수렴 시간이 계통 열용량 M에 대해 **정합적으로 움직이는지** — τ가 M에 비례하고 유량에 반비례하는지. **절대적 수렴시간이 "합리적"인지는 판정하지 않는다** (M에 열교환기·CDU 내부·콜드플레이트 보유량이 빠져 있고, 이 파일럿에서는 그 값이 확보되지 않는다 — 5-1 「계통 보유수량 M」 한계)
* 극단 케이스(부하 0 / 최대)에서 모델이 발산하지 않는지

## 7\. 다음 단계

* \[ ] 단일 랙 + 단일 CDU, 2차측 고정온도, 정상상태 모델부터 구현 → balance 검증
* \[ ] 시간축 추가(solve\_ivp) → 부하 변화에 대한 온도 응답 확인
* \[ ] 랙 개수 확장 → 유량분배 로직 추가
* \[ ] 누출 시나리오 주입 → 정상 대비 신호 패턴(온도상승/유량감소/펌프RPM변화) 분석
* \[ ] 다중 CDU 공유 2차측 결합 구조로 확장
* \[ ] (설계데이터 확보 시) 가정치 → 실측/설계값으로 교체
* \[ ] HYSYS 결과와 교차검증 검토

\---

*본 문서는 Claude 프로젝트 지식(Knowledge) 등록용으로 정리한 요약본이며, 세부 코드 구현·파라미터는 프로젝트 내 후속 대화에서 계속 갱신 예정.*

