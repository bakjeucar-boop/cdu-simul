# 줄바꿈 정규화 (미해결 #9 — 여러 PC 사이에서 줄바꿈만 바뀐 diff 방지)
# 저장소에는 LF 로 저장한다. 체크아웃 시 변환은 각 PC 설정을 따른다.
* text=auto

# 파이썬 소스는 체크아웃도 LF 로 고정한다.
*.py text eol=lf

# 셸 스크립트도 체크아웃을 LF 로 고정한다 (미해결 #53 — demo/start.sh 가
# CRLF 로 체크아웃되면 macOS·Linux 에서 실행되지 않는다).
*.sh text eol=lf
