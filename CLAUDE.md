# CLAUDE.md — CDU Feasibility Study

AI/GPU 서버 D2C(Direct-to-Chip) 액체냉각 CDU를 파이썬으로 모사해, 정상/이상(「막힘」·「샘」)
시나리오를 시뮬레이션하고 그 결과를 **누출 조기감지·이상상태 예측용 AI 학습
데이터셋**으로 쓰는 것이 목표. **지금은 파일럿 단계** — 실제 설계사양이 없어
feasibility(무너지지 않는가)만 본다. 정확도 검증은 파일럿 종료 이후(세션 6+)다.
상세 배경·설비구조·시나리오 가정치는 `AI서버_CDU_디지털트윈_프로젝트정리.md`(정본)를
본다. 이 문서는 코딩 규약만 적는다.

## 작업 시작 전 필독

0. **`git pull` 을 먼저 한다.** 여러 PC를 GitHub를 사이에 두고 번갈아 쓸 수 있다.
   당기지 않고 시작하면 다른 PC의 작업 위에 덮어쓰게 된다.
1. **저장소**: `https://github.com/bakjeucar-boop/cdu-simul`
   (로컬 경로는 PC마다 다르다 — 저장소 루트에서 실행하는 것만 전제한다.
   코드에 절대경로를 넣지 않는다: 절대 규칙 15)
2. `PROCEED.md` — 지금까지의 진행 이력과 미해결 항목. 없으면 세션 1-A에서 만든다.
3. `daily_brief.py` — 대화 시작 시 붙일 브리핑을 뽑는 스크립트. 세션 1-A에서 저장소와
   함께 만든다.
4. **문서 4종은 저장소 루트에 있고, 저장소 사본이 정본이다.**
   (`CLAUDE.md`는 반드시 루트에 둔다 — Claude Code가 자동으로 읽는 위치다.)
   같은 문서가 웹 대화창의 Claude 프로젝트 지식에도 올라가 있다. **두 벌이므로
   갈릴 수 있다.** 규칙:
   - 로컬 Claude Code는 이 4개 문서를 **임의로 고치지 않는다.** 고칠 것이 보이면
     목록으로 보고만 한다(절대 규칙 13).
   - 사람의 지시로 고쳤다면, 세션 종료 보고에 **"문서 N을 고쳤음 — 프로젝트 지식
     재업로드 필요"** 를 반드시 남긴다.
   - 두 벌이 어긋나면 **저장소 사본이 맞다.**

## 절대 규칙

1. **모든 파라미터에 출처를 표기한다.** 값을 코드에 넣을 때 `[가정값]` 또는
   `[설계값]` 태그와 근거를 주석 또는 assumptions 파일에 함께 남긴다. 출처 없는
   숫자를 도입하지 않는다 — 5장 표에 없는 값이 필요하면 먼저 사람에게 확인한다.
   **사람이 확정해 준 값은 프로젝트정리 5장 「5-1. 파생 가정치」에 기록된다** —
   코드는 그 소절에 있는 값만 쓴다. 소절에 없으면 아직 확정된 것이 아니다.
2. **가정치는 한 곳에 모은다.** 5장의 시나리오 조건·구성요소 가정치는 코드 곳곳에
   흩어 적지 않고 별도 파일(예: `assumptions.py` 또는 `assumptions.yaml`)에서
   읽는다. 설계데이터로 교체할 때 그 파일 하나만 바꾸면 되게 만든다.
3. **도구스택은 확정되어 있다 — 재검토 없이 바꾸지 않는다.**
   `NumPy + SciPy + CoolProp + Pandas`. CoolProp은 직접 호출한다(3장 결론).
   Pyomo/IDAES는 **설계변수 자동탐색(최적화)이 실제로 필요해지는 단계 전까지
   도입하지 않는다** — 도입하려면 먼저 사람에게 이유를 확인받는다. HYSYS 교차검증은
   선택 경로이지 필수 경로가 아니다.
4. **압력-유량과 온도는 서로 다른 방식으로 푼다(하이브리드 방식, 4장).**
   압력-유량 분배는 매 시점 quasi-steady 대수방정식(`fsolve` 등)으로, 온도는
   열용량 기반 시간적분(`solve_ivp`)으로 푼다. 편의를 위해 둘 다 정상상태로
   단순화하거나 둘 다 시간적분으로 바꾸지 않는다 — 관성 시간상수 차이가 이 방식을
   쓰는 이유다.
5. **수치해석 solver의 성공 여부를 항상 확인한다.** `fsolve`의 `ier`, `solve_ivp`의
   `success`를 확인하지 않고 결과값을 쓰지 않는다. 실패 시 조용히 넘어가지 않고
   예외를 던지거나 결과에 실패 표시를 남긴다.
6. **단계(4장)를 순서대로 밟는다.** 단일 CDU → 랙 개수 확장 → 이상 시나리오
   (「막힘」·「샘」) → 다중 CDU 확장. 앞 단계의 feasibility 기준(6장)을 통과하기 전에 다음 단계로
   넘어가지 않는다.
7. **2차측은 현재 고정 경계조건(블랙박스)이다.** 다중 CDU 확장(4단계) 전까지
   2차측 동특성을 임의로 모델링하지 않는다 — 2차측 온도는 5장 가정치의 고정값을
   쓴다.
8. **이상 상태는 「막힘」과 「샘」 둘이다.** 이 둘이 이상 시나리오의 전부다.
   - **막힘 — 배관저항(K값) 증가.** 5장 누출 항목의 K값 정상 대비
     +5/+20/+50% 를 그대로 쓴다. 질량은 보존된다. 스케일·이물질·밸브 고착으로
     실제 생기는 현상이므로 「샘」의 대용(proxy)이 아니라 **독립된 이상 상태**다.
   - **샘 — 질량손실.** 계통 밖으로 유체가 나간다. 크기 수준은 5-1 「「샘」
     크기 수준」의 역산 규칙을 따르고, 그 숫자를 코드에 박지 않는다.

   **셋째 갈래를 더하거나 이 둘의 정의를 바꾸려면 먼저 사람에게 확인한다** —
   AI 학습 데이터셋의 일관성이 걸린 결정이다.

   **둘은 총유량 부호에서 갈린다(미해결 #36).** 막힘은 총유량과 누출랙 통과유량이
   **줄고**, 샘은 같은 두 양이 **는다** — 방향이 정반대다(구조 자유도 배치에 따라
   샘이 수력 흔적을 전혀 남기지 않는 경우도 있다). **한쪽 기구의 신호를 다른 쪽
   방향으로 읽지 않는다.** 산출물·데이터셋은 행마다 어느 기구인지 밝히고,
   기구를 읽지 않은 채 「유량 감소 = 이상」 같은 규칙을 세우지 않는다.

   **낱말 규약(세션 7.27).** 「누출」은 **상위 개념**(실제로 일어나는 이상 사건)
   전용으로 쓰고, 모델 안의 기구는 언제나 「막힘」·「샘」으로 적는다. 「정상/이상/
   누출」처럼 이상과 누출을 나란히 놓지 않는다 — 누출은 이상의 하위다.
   **예외: 소개자료(`intro/`)는 「누출」을 빌드로 막고 있어 이 규약을 따르지
   않는다** — 정본 문서 넷과 `intro/` 는 이 낱말에서 서로 다른 규칙을 갖는다.
9. **단위를 변수명에 넣는다.** `T_supply_C`, `m_dot_kgs`, `P_pump_kPa` 처럼.
   같은 물리량을 다른 단위로 섞어 쓰지 않는다(5장 표는 mAq·L/s·℃가 혼재하므로
   코드 안에서 변환 지점을 명확히 한다).
10. **feasibility 기준(6장)을 자동 검사 코드로 만든다.** energy balance 오차,
    T_return 방향성, 수렴시간, 극단 케이스(부하 0/최대) 발산 여부 — 이 넷을
    사람이 눈으로 보고 판단하지 않고 테스트가 판정하게 만든다.
11. **가정치를 실측처럼 산출물에 내지 않는다.** 그래프·리포트·데이터셋 어디에도
    "가정값 기반 — 실측 아님"을 표시한다. AI 학습 데이터셋으로 쓸 산출물이라
    이 표시가 특히 중요하다 — 나중에 이 데이터로 학습한 모델의 한계를 아는
    유일한 단서가 될 수 있다.
12. **파이썬 환경은 사용자 소유다.** 패키지 설치·삭제·업그레이드를 임의로 하지
    않는다. **승인된 의존성은 아래가 전부이고, 이 목록 밖은 먼저 물어본다.**
    - 런타임: `numpy` · `scipy` · `CoolProp` · `pandas`
    - 개발: `pytest` · `ruff` · `mypy`   ← ruff·mypy는 세션 1-C에서 도입
    - Python 3.12+ · 가상환경은 저장소 루트의 `.venv`
      (`pyproject.toml` 은 `>=3.12`. 실측 환경은 3.12.10 하나뿐이고 3.11·3.13 에서는
      돌려보지 않았다 — 넓히려면 먼저 그 버전에서 전체 테스트를 돌린다)
    - matplotlib 등 시각화 라이브러리는 파일럿 단계에서 **승인되지 않았다**
      (`project-overview.md` 「시각화 방향」 — 본 프로젝트 이후).
13. **지시 범위를 넘지 않는다.** 짧은 지시는 그 일만 한다. 하는 김에 고칠 것이
    보이면 고치지 말고 먼저 보고한다(collaboration.md 「발견은 목록으로」).
14. **커밋은 계층별로 나눈다.** 모델 로직 변경 · 테스트 · 가정치 교체 · 문서는
    각각 별도 커밋.
15. **경로를 하드코딩하지 않는다.** 여러 PC(다른 사용자 계정·다른 드라이브)에서
    이어받을 수 있어야 한다. `pathlib.Path` + 저장소 루트 기준 상대경로만 쓴다.
    `C:\Users\SAMSUNG\...` 같은 절대경로를 코드에 넣지 않는다.

## 개발 순서 — 게이트 (프로젝트정리 4장·7장 기반)

앞 단계의 게이트를 통과하기 전에 다음 단계를 시작하지 않는다.

| 세션 | 대상 | 게이트 |
|---|---|---|
| 1-A | 저장소 clone · 가상환경 · 저장소 골격 · 5장 가정치 전사(`assumptions.py`) · CoolProp 래퍼 · `PROCEED.md`·`daily_brief.py` 초기 구성 · 문서 4종 커밋 | **게이트 없음** — 물리 모델이 없다. 환경 스모크 테스트(import·Python 버전·CoolProp 물성 조회)만 돌리고, 이것을 feasibility 통과로 적지 않는다 |
| 1-B | 단일 랙 + 단일 CDU, 2차측 고정온도, 정상상태 모델 | energy balance 오차 <0.1% |
| 1-C | 저장소 위생 정비 (lint·타입검사 도입 · 미해결 위생항목 정리) | **게이트 없음** — 물리 모델을 건드리지 않는다. 기존 테스트가 전부 그대로 통과하는 것만 확인한다 |
| 2 | 시간축 추가 (`solve_ivp`) | 부하 변화에 대한 T_return이 물리적으로 타당한 방향(상승)으로 반응 |
| 3 | 랙 개수 확장 | 헤더 압력평형 기반 유량분배(`fsolve`) 수렴, 극단 케이스(부하 0/최대) 비발산 |
| 4 | 이상 시나리오 주입 — **이 게이트는 「막힘」만 판정했다**(「샘」은 세션 5.6·5.7-D 관측 판이고 게이트가 없다) | 정상 대비 신호 패턴(온도상승·유량감소·펌프 운전점 이동) 식별 가능 |
| 5 | 다중 CDU, 공유 2차측 결합 | 상위 레벨 연립방정식으로 CDU 간 연동 수렴 |
| 5.5 | 시나리오 배치 실행 → 텍스트 데이터셋 생성 | 정상/이상 다수 케이스 반복 실행 시 **시나리오 간 상태 이월 없음**(collaboration.md 결함유형 ④) · 전 행에 가정치 출처·"가정값 기반 — 실측 아님" 표기 |
| 6 | **[파일럿 종료점]** (설계데이터 확보 시) 가정치 → 실측/설계값 교체 | 교체 전후 결과 diff를 기록 |
| 7 | HYSYS 교차검증 (선택, 본 프로젝트 단계) | 정상상태 결과 오차 범위 내 |

**파일럿의 마지막 판은 세션 5.5이고, 그 산출물을 보고 사람이 종료를 판정한다**
(`project-overview.md` 「파일럿 종료 조건」). 세션 6은 설계데이터가 확보돼야
열리며, 이 파일럿은 데이터가 오지 않는 것을 전제로 진행한다 — 즉 세션 6이
파일럿의 정상 종착지가 아니다. 세션 1~5.5는 전부 가정치 기반 feasibility 검증이다.

## 명령어

*(세션 1-A에서 확정. 저장소: `https://github.com/bakjeucar-boop/cdu-simul`.
가상환경은 저장소 루트의 `.venv` — Windows 이므로 인터프리터는 `.venv/Scripts/python.exe`.
**`activate` 에 의존하지 않는다**: 명령 사이에 셸 상태가 남지 않을 수 있으므로
아래처럼 인터프리터 경로를 명시해 실행한다. 저장소 루트에서 실행하는 것을 전제한다.)*

```bash
# 최초 1회 — 가상환경 생성 (Python 3.12+ 필요. 실측 환경: 3.12.10)
python -m venv .venv

# 의존성 설치 (런타임 numpy·scipy·CoolProp·pandas + 개발 pytest — 절대 규칙 12)
.venv/Scripts/python.exe -m pip install -e ".[dev]"

# 테스트 — 한 번에 다 돌리지 않는다. **조각으로 나눠 전경으로** 돌린다 (#58)
#
#   조각 가르는 기준:
#   · **파일 단위**로 가른다 — 나눌 수 있는 최소 단위가 파일 하나다(세션 7.37 실측).
#     한 파일이 상한을 넘으면 분할로는 못 푼다 — 그 파일 안을 고쳐야 한다.
#   · 도구 전경 상한은 **실측 580 s** 다(「10분」이 아니다 — 세션 7.37 관측).
#     **조각 시간은 실측 최대에 25 %(#62)를 얹어 읽고, 그 값이 580 s 를 넘으면
#     다시 가른다** — 같은 PC·같은 판 안에서도 시험 시간이 그만큼 흔들린다.
#   · 무거운 파일은 단독 조각으로 뺀다.
#   · **백그라운드로 넘기지 않는다**(#57) — 상한을 넘으면 도구가 자동으로 넘긴다.
#
#   세션 7.39 실측 (「샘」 행이 얹힌 뒤 · 1,333건 전부 통과):
.venv/Scripts/python.exe -m pytest tests/test_dynamics.py tests/test_energy_balance.py tests/test_environment.py tests/test_gates_after_leak.py tests/test_gates_after_plant.py tests/test_holdup_split_sensitivity.py tests/test_hydraulics.py   # 앞 7파일 · 392건 · 89 s (×1.25 = 111 s)
.venv/Scripts/python.exe -m pytest tests/test_session3_gates.py tests/test_session4_gates.py tests/test_session55d_gates.py tests/test_session57d_massloss_thermal.py tests/test_session58_transport_lag.py tests/test_session5_gates.py   # 뒤 6파일 · 771건 · 151 s (×1.25 = 189 s)
.venv/Scripts/python.exe -m pytest tests/test_session55_gates.py   # 170건 · 324 s (×1.25 = 405 s) — 단독 조각
#
#   **「샘」 행이 얹힌 뒤에도 이 가름이 성립한다**(세션 7.39 재측) — 조각별 최대가
#   324 s 이고 25 % 를 얹어도 405 s 로 상한 580 s 에 여유 30 % 다. 「샘」이 늘린 것은
#   `test_session55_gates.py` 의 **건수**(45 → 170)이지 시간이 아니다 — 표본이
#   시나리오 수(1,792 → 9,472)를 따라가 기준 B·D 의 파라미터가 는 것이고,
#   「샘」 정상상태 해가 전이 해보다 훨씬 싸서 시간은 324 s 로 거의 그대로다.
#
#   **모르는 것**: 「샘」 **전이** 행(#40 이 열린 채라 이 판이 붙이지 않았다)이
#   얹히면 다시 걸릴 수 있다 — 전이는 케이스당 비용이 정상상태의 수십 배다.
#
# tests/test_environment.py  환경 스모크(세션 1-A) — feasibility 판정이 아니다
# tests/test_energy_balance.py  세션 1-B 게이트(energy balance <0.1%) + HX duty 항등성

# 정상상태 결과 표 (4케이스 · "가정값 기반 · 실측 아님" 표시 포함)
.venv/Scripts/python.exe -m cdu_simul.model

# lint · 타입검사 (세션 1-C 도입 — 안 쓰기로 하면 이 두 줄과 규칙 12의 ruff·mypy를 지운다)
.venv/Scripts/python.exe -m ruff check .
.venv/Scripts/python.exe -m mypy src tests

# 아침 브리핑 (PROCEED.md 「현재 상태」 표 + 마지막 세션 로그)
git pull
.venv/Scripts/python.exe daily_brief.py

# 환경 확인 (시스템 파이썬이 아니라 .venv 인지)
.venv/Scripts/python.exe -c "import sys; print(sys.executable)"
.venv/Scripts/python.exe -m pip list
```

## 판 시작·종료 정형 절차

모든 판이 같은 절차로 열리고 닫힌다. **프롬프트에 다시 적지 않는다** — "표준 절차"라고만
쓰면 아래를 뜻한다.

**시작**
1. `git pull` — 다른 PC의 작업 위에 덮어쓰지 않기 위해 먼저 한다
2. `.venv/Scripts/python.exe daily_brief.py` — 오늘 상태를 확인한다
3. 정본 문서 4종 중 **이 판에 걸리는 절**과 `PROCEED.md` 미해결 목록을
   읽고 시작한다
4. **`PROCEED.md` 「현재 상태」 표를 통째로 읽지 않는다.** 갱신에
   필요한 것은 마지막 행의 서식과 삽입 위치뿐이다. 미해결 목록도
   이 판에 걸리는 항목만 본다
   (근거: 세션 7.2-B·7.3·7.4 의 `ctx` 관측 셋에서 세 판 모두 이 문서가
    가장 크게 먹었거나 줄일 수 있었던 것으로 지목됐다)

**종료** (하나라도 빠지면 판이 안 끝난 것이다)
1. `pytest` — **전부 통과**. 실패를 남긴 채 끝내지 않는다. 「명령어」 절의
   **조각 분할대로 나눠 전경으로** 돌린다 — 백그라운드로 넘기지 않는다(#57).
   조각 합이 전 시험을 덮는지 건수로 확인한다(빠진 조각을 통과로 읽지 않는다)
2. `.venv/Scripts/python.exe -m ruff check .` · `-m mypy src tests` — 통과
3. `PROCEED.md` 갱신 — 완료 항목 · 만든/바꾼 파일 · 미해결과 이유 · 다음 세션 결정사항
4. 계층별 커밋 분리(절대 규칙 14)
5. `git push` — 올리지 않으면 이 PC에만 남는다
6. 보고 마지막과 `PROCEED.md` 세션 로그에 한 줄을 남긴다:
   `ctx: <사용률 또는 잔여 토큰 — 표시되는 값 그대로> ·
    가장 크게 먹은 것 하나 · 줄일 수 있었다고 보는 것 하나`

   수치는 실제 표시값을 적는다 — 이것만은 「모름」이 아니다. 어느
   표시가 보이는지는 세션마다 다르므로 보이는 대로 적고 무엇을
   적었는지 밝힌다. **나머지 둘을 짐작으로 채우지 않는다.** 어느 것이
   컸는지 모르면 「모름」, 줄일 수 있었다고 볼 것이 없으면 「없음」.

**검사기가 통과했다는 것은 "코드가 문법·타입·스타일상 성립한다"는 뜻이지
"물리적으로 맞다"는 뜻이 아니다.** feasibility 판정은 6장 기준으로만 한다.

## 세션 종료 시

작업을 마치면 `PROCEED.md`에 아래를 추가하고 커밋한다.

- 완료한 항목
- 만든/바꾼 파일 목록
- 미해결 항목과 그 이유
- 다음 세션이 알아야 할 결정 사항(가정치 채택/기각 등)

**커밋 후 `git push`로 원격에 올린다.** 올리지 않으면 이 PC에만 남는다 — 다음
세션이 다른 PC에서 열리면 없는 일이 된다.

## 코드 스타일

- Python 3.12+, 타입힌트 필수, `from __future__ import annotations`
- dataclass는 `frozen=True` 기본. 가변 상태(`state`)가 필요한 클래스는 이유를 주석으로.
- 함수명·변수명은 영문 snake_case. 주석·docstring은 한국어 허용.
- 물성치 계산 함수는 순수 함수로 작성한다. 전역 상태를 읽거나 쓰지 않는다.
- 단위를 변수명에 넣는다(절대 규칙 9).
- CoolProp 호출은 별도 래퍼 함수로 감싸, 유체 문자열(`INCOMP::MPG-25%` 등)이
  한 곳에서만 정의되게 한다.

## 하지 말 것

- 출처 표기 없는 숫자를 코드·문서·보고서에 적기
- 5장 표의 가정치를 코드 여러 곳에 하드코딩하기
- `fsolve`/`solve_ivp` 성공 여부 확인 없이 결과값 사용하기
- 단계(4장)를 건너뛰어 예를 들어 랙 확장 전에 이상 시나리오(「막힘」·「샘」)부터 만들기
- Pyomo/IDAES를 최적화 필요성 확인 없이 조기 도입하기
- 2차측을 다중 CDU 확장 전에 임의로 동특성 모델링하기
- 압력-유량과 온도를 같은 시간적분 방식으로 통일해 하이브리드 구조를 없애기
- 가정값 기반 산출물을 표시 없이 실측처럼 제시하기
- `pip uninstall` 실행, 패키지 다운그레이드, 환경 임의 변경
- 요청하지 않은 범위까지 "하는 김에" 고치기
- 성격이 다른 변경(모델 로직 vs 가정치 교체)을 한 커밋에 묶기
- 코드에 절대경로(`C:\Users\SAMSUNG\...`)를 하드코딩하기
- 세션을 커밋만 하고 `git push` 없이 마치기
- `PROCEED.md` 기록 없이 세션을 마치기
- **셸로 파일을 만들거나 고치기.** 편집 도구를 쓴다 — 임시·일회성
  스크립트도 그렇다. heredoc·here-string 으로 긴 문자열을 셸에
  싣지 않는다
  (세션 7.2-B 는 커밋 메시지 here-string 으로 amend 왕복을 한 번 더
   했고, 세션 7.4 는 셸 python 편집 때문에 파일 전문을 두 번 되받아
   그 판의 컨텍스트 1위가 됐다. 프롬프트에 같은 지시가 있었는데도 그랬다)
